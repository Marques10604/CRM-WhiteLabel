---
phase: 03-sales-pipeline-funnel-view
plan: 04
type: execute
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - src/actions/lead-actions.ts
autonomous: true
requirements: [PIPE-01, PIPE-03]

must_haves:
  truths:
    - "Criar ou editar um lead a partir do próprio board de pipeline atualiza a contagem das colunas e a lista de cards imediatamente, sem recarregar a página."
    - "Editar um lead na lista (/) reflete imediatamente no board (/pipeline) e vice-versa — a contagem por etapa é de fato 'ao vivo' independente de qual página disparou a mutação."
    - "Mudar a etapa de um lead pelo dropdown do modal de edição reinicia o relógio de 'esfriando' (stageChangedAt), exatamente como o drag-and-drop já faz."
    - "Um lead movido para 'Contatado' pelo dropdown do formulário passa a ser sinalizado 'esfriando' após 5+ dias parado (não fica preso invisível para sempre)."
  artifacts:
    - path: "src/actions/lead-actions.ts"
      provides: "createLead, updateLead e updateLeadStage revalidam AMBOS '/' e '/pipeline'; updateLead faz SELECT-current-stage-then-compare e grava stageChangedAt apenas em mudança real de etapa (mesmo padrão de updateLeadStage), preservando o guard isNull(deletedAt)"
      contains: "revalidatePath(\"/pipeline\")"
  key_links:
    - from: "src/actions/lead-actions.ts (updateLead)"
      to: "leads.stageChangedAt"
      via: "conditional bump em mudança real de etapa (SELECT stage atual → compara → set new Date())"
      pattern: "stageChangedAt: new Date\\(\\)"
    - from: "src/actions/lead-actions.ts (createLead/updateLead/updateLeadStage)"
      to: "/pipeline cache"
      via: "revalidatePath após o write"
      pattern: "revalidatePath\\(\"/pipeline\"\\)"
---

<objective>
Fechar os 2 gaps BLOQUEADORES encontrados pelo verificador da Fase 3 (ver `03-VERIFICATION.md`), ambos isolados em `src/actions/lead-actions.ts`. Correção cirúrgica, não reescrita — espelha lógica que já existe correta em `updateLeadStage`.

**Gap 1 (PIPE-01, parcial — Success Criterion 1):** `createLead`/`updateLead` só chamam `revalidatePath("/")`; `updateLeadStage` só chama `revalidatePath("/pipeline")`. Como o board reutiliza o mesmo `LeadFormDialog`/`createLead`/`updateLead` (botão "Novo lead" e clique-no-card-para-editar), criar/editar um lead a partir do board deixa a contagem e a lista de cards desatualizadas até um reload manual. A contagem não é de fato "ao vivo".

**Gap 2 (PIPE-03, falhou — Success Criterion 3, o mais importante):** `stageChangedAt` (o relógio que dispara o flag "esfriando") só é escrito por `updateLeadStage` (caminho do drag-and-drop). `updateLead` (o dropdown de etapa do modal de edição, caminho igualmente suportado e proeminente) nunca toca `stageChangedAt`: `leadSchema` não tem esse campo e `updateLead` faz `.set(parsed.data)` sem SELECT-then-compare. Qualquer lead cuja etapa só mudou pelo formulário nunca é sinalizado "esfriando" — derrotando silenciosamente a proposta de valor central "leads não esfriam sem ninguém ver".

Purpose: Restaurar as duas garantias que o board promete — contagem viva em qualquer caminho de mutação (PIPE-01) e relógio de "esfriando" preciso em qualquer caminho de mudança de etapa (PIPE-03).
Output: `src/actions/lead-actions.ts` corrigido; nenhuma mudança de schema/migração/UI.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Fonte da verdade para ESTE plano (o que consertar e por quê):
@.planning/phases/03-sales-pipeline-funnel-view/03-VERIFICATION.md

# O arquivo a modificar (leia inteiro antes de editar — é curto, ~143 linhas):
@src/actions/lead-actions.ts

<interfaces>
<!-- Contratos que o executor precisa — extraídos do código. Use diretamente, sem explorar. -->

De src/actions/lead-actions.ts (estado ATUAL, o que já existe e o que falta):

- `updateLeadStage(id, stage, motivoPerda?)` — JÁ implementa CORRETAMENTE o padrão SELECT-then-compare:
    const [current] = await db
      .select({ stage: leads.stage })
      .from(leads)
      .where(and(eq(leads.id, parsed.data.id), isNull(leads.deletedAt)));
    if (!current) { return { errors: { id: ["Lead inválido."] } }; }
    const stageChanged = current.stage !== parsed.data.stage;
    await db.update(leads).set({
      stage: parsed.data.stage,
      ...(stageChanged ? { stageChangedAt: new Date() } : {}),
    }).where(and(eq(leads.id, parsed.data.id), isNull(leads.deletedAt)));
    revalidatePath("/pipeline");   // <-- só /pipeline (Gap 1: falta "/")

- `createLead(_prevState, formData)` — termina com `revalidatePath("/");`  (Gap 1: falta "/pipeline")
- `updateLead(_prevState, formData)` — faz `db.update(leads).set(parsed.data).where(and(eq(leads.id, id), isNull(leads.deletedAt)))`
    e termina com `revalidatePath("/");`
    (Gap 1: falta "/pipeline"; Gap 2: nunca grava stageChangedAt, sem SELECT-then-compare)

De src/lib/validations.ts (NÃO precisa ser modificado):
- `leadSchema` JÁ tem o campo `stage: z.enum([...5 valores]).default("novo")`, então `parsed.data.stage` está disponível
  dentro de `updateLead`. A correção do Gap 2 NÃO adiciona `stageChangedAt` ao schema — em vez disso faz
  SELECT do stage atual e compara com `parsed.data.stage`, idêntico a `updateLeadStage`.

Imports já presentes no arquivo (não adicionar duplicados): `and, eq, isNull` de drizzle-orm; `revalidatePath` de next/cache; `db`, `leads`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revalidar ambos '/' e '/pipeline' em todas as três Server Actions (Gap 1 / PIPE-01)</name>
  <files>src/actions/lead-actions.ts</files>
  <read_first>
    - `src/actions/lead-actions.ts` inteiro (as três actions terminam em `revalidatePath(...)`; ver linhas 62, 99, 141).
    - `03-VERIFICATION.md` gap #1 e a tabela "Key Link Verification" (linha `createLead/updateLead → /pipeline cache ... ✗ NOT WIRED`).
  </read_first>
  <action>
    Corrige o Gap 1 (PIPE-01): toda mutação de lead precisa invalidar o cache de AMBAS as páginas que a consomem — a lista `/` e o board `/pipeline` — porque o mesmo `LeadFormDialog`/`createLead`/`updateLead` é reutilizado nas duas telas.

    Fazer com que `createLead`, `updateLead` e `updateLeadStage` cada uma revalidem tanto `"/"` quanto `"/pipeline"`. Implementação recomendada (mantém consistência e mínimo diff): adicionar `revalidatePath("/pipeline")` logo após o `revalidatePath("/")` existente em `createLead` e `updateLead`; e adicionar `revalidatePath("/")` logo após o `revalidatePath("/pipeline")` existente em `updateLeadStage`. Manter os dois como chamadas literais explícitas em cada action (NÃO extrair para um helper — mantém o gate de verificação determinístico e o padrão idêntico ao restante do arquivo).

    Não tocar em nenhuma outra lógica dessas actions (validação Zod, `subnichoExists`, backstop de FK, guard `isNull(deletedAt)` permanecem exatamente como estão).
  </action>
  <verify>
    <automated>test $(grep -c 'revalidatePath("/pipeline")' src/actions/lead-actions.ts) -ge 3 && test $(grep -c 'revalidatePath("/")' src/actions/lead-actions.ts) -ge 3 && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `createLead` termina revalidando `"/"` E `"/pipeline"`.
    - `updateLead` termina revalidando `"/"` E `"/pipeline"`.
    - `updateLeadStage` termina revalidando `"/pipeline"` E `"/"`.
    - `grep -c 'revalidatePath("/pipeline")'` == 3 e `grep -c 'revalidatePath("/")'` == 3 em `src/actions/lead-actions.ts`.
    - `npx tsc --noEmit` passa sem erros.
    - Nenhuma outra lógica das actions foi alterada.
  </acceptance_criteria>
  <done>As três Server Actions revalidam ambos os caminhos; tsc limpo; contagem do board fica "ao vivo" independentemente de qual página disparou a criação/edição.</done>
</task>

<task type="auto">
  <name>Task 2: Persistir stageChangedAt no caminho de edição por formulário (Gap 2 / PIPE-03)</name>
  <files>src/actions/lead-actions.ts</files>
  <read_first>
    - `src/actions/lead-actions.ts` — especificamente `updateLead` (o bloco que faz `.set(parsed.data)`) e `updateLeadStage` (o padrão SELECT-then-compare já correto, ~linhas 120-139), que é o modelo EXATO a espelhar.
    - `03-VERIFICATION.md` gap #2 (a falha de PIPE-03) e a nota "the fix ... reuse it, don't reinvent".
    - Bloco `<interfaces>` acima (confirma que `parsed.data.stage` já existe via `leadSchema` — sem mudança de schema).
  </read_first>
  <action>
    Corrige o Gap 2 (PIPE-03), a falha central "leads esfriam silenciosamente". Fazer `updateLead` manter o relógio `stageChangedAt` exatamente como `updateLeadStage` já faz, para o caminho do dropdown de etapa do modal de edição.

    Dentro de `updateLead`, ANTES do `db.update(...).set(...)`, adicionar um SELECT do stage atual do lead com o MESMO guard de soft-delete já usado no write e em `updateLeadStage`:
    fazer `db.select({ stage: leads.stage }).from(leads).where(and(eq(leads.id, id), isNull(leads.deletedAt)))`. Se nenhuma linha voltar (`!current`), retornar `{ errors: { id: ["Lead inválido."] } }` (lead inexistente ou soft-deletado — não prosseguir com o write).

    Calcular `const stageChanged = current.stage !== parsed.data.stage;` e trocar o `.set(parsed.data)` por um set que inclui condicionalmente o bump: espalhar `parsed.data` e, quando `stageChanged`, adicionar `stageChangedAt: new Date()`. Só grava `stageChangedAt` quando a etapa realmente muda — edições que não mexem na etapa (ex.: só notas/follow-up) NÃO devem reiniciar o relógio de "esfriando" (mesma garantia de `updateLeadStage`).

    Preservar tudo o mais em `updateLead`: validação `leadSchema`, checagem `subnichoExists`, o `try/catch` do backstop de FK envolvendo o write, e o guard `isNull(deletedAt)` no `.where(...)`. Colocar o SELECT dentro/antes do fluxo existente sem remover o backstop de FK. NÃO modificar `src/lib/validations.ts` — `parsed.data.stage` já é suficiente para comparar contra o stage armazenado.
  </action>
  <verify>
    <automated>test $(grep -c 'stageChangedAt: new Date()' src/actions/lead-actions.ts) -ge 2 && test $(grep -c 'select({ stage: leads.stage })' src/actions/lead-actions.ts) -ge 2 && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `updateLead` faz um SELECT do `stage` atual com guard `isNull(leads.deletedAt)` antes do update e retorna `{ errors: { id: [...] } }` quando o lead não existe / está soft-deletado.
    - `updateLead` grava `stageChangedAt: new Date()` SOMENTE quando `current.stage !== parsed.data.stage`.
    - Edição que não muda a etapa NÃO altera `stageChangedAt`.
    - `grep -c 'stageChangedAt: new Date()'` >= 2 e `grep -c 'select({ stage: leads.stage })'` >= 2 (ou seja, o padrão de `updateLeadStage` foi espelhado em `updateLead`).
    - `src/lib/validations.ts` NÃO foi modificado; validação/FK/soft-delete guards preservados.
    - `npx tsc --noEmit` passa.
  </acceptance_criteria>
  <done>Mudar a etapa de um lead via dropdown do formulário reinicia `stageChangedAt`; um lead colocado em "Contatado" pelo formulário passa a poder ser sinalizado "esfriando" após 5+ dias — PIPE-03 restaurado em ambos os caminhos de UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| formulário/board (cliente) → Server Action | Entrada não confiável (id, stage, campos do lead) cruza para o write no banco |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | novo SELECT + set condicional em `updateLead` | mitigate | Preservar o guard `isNull(leads.deletedAt)` no SELECT adicionado E no `.where` do update — impede editar/mover etapa de lead soft-deletado (invariante da Lixeira, 01-04). Retornar `{ errors: { id } }` quando `!current`. |
| T-03-02 | Tampering | `stage`/`id` submetidos pelo formulário | mitigate | Já mitigado e mantido: `updateLead` valida via `leadSchema` (enum de 5 valores para `stage`, id inteiro positivo checado). Nenhuma validação existente é removida por este plano. |
| T-03-SC | Tampering | dependências (npm) | accept | Nenhum install de pacote neste plano (fix single-file); superfície de supply-chain inalterada. Nada a verificar. |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo.
- `npm run build` (Next 16) conclui sem erro.
- `grep -c 'revalidatePath("/pipeline")' src/actions/lead-actions.ts` == 3 e `grep -c 'revalidatePath("/")' src/actions/lead-actions.ts` == 3.
- `grep -c 'stageChangedAt: new Date()' src/actions/lead-actions.ts` >= 2 e `grep -c 'select({ stage: leads.stage })' src/actions/lead-actions.ts` >= 2.
- `git diff --name-only` mostra APENAS `src/actions/lead-actions.ts` alterado (nenhuma mudança de schema/migração/validations/UI).

<human-check>
(human_verify_mode=end-of-phase — confirmação comportamental na verificação de fim de fase)
1. No board `/pipeline`, clicar "Novo lead", salvar → o card aparece e a contagem da coluna incrementa SEM reload.
2. No board, clicar um card, mudar a etapa no dropdown, salvar → o card muda de coluna e as contagens atualizam SEM reload.
3. Editar um lead na lista `/` → navegar para `/pipeline` mostra o estado já atualizado (e vice-versa).
4. Colocar um lead em "Contatado" pelo dropdown do formulário; após 5+ dias parado (ou ajustando `stage_changed_at` no DB via Drizzle Studio para 6 dias atrás), o card exibe a borda âmbar + "Esfriando".
5. Editar um lead SEM mudar a etapa (ex.: só as notas) → `stage_changed_at` NÃO muda (relógio de esfriando preservado).
</human-check>
</verification>

<success_criteria>
- PIPE-01 (Gap 1) fechado: contagem do board é "ao vivo" para criação/edição originadas de qualquer página — `createLead`, `updateLead` e `updateLeadStage` revalidam `/` e `/pipeline`.
- PIPE-03 (Gap 2) fechado: `updateLead` mantém `stageChangedAt` via SELECT-then-compare, então o flag "esfriando" funciona independentemente de a etapa ter sido mudada por drag-and-drop OU pelo dropdown do formulário.
- Escopo cirúrgico respeitado: somente `src/actions/lead-actions.ts` modificado; renderização do board, mecânica de drag-and-drop optimista, modal de motivoPerda e schema/migração permanecem intocados.
- `tsc` e `build` limpos.
</success_criteria>

<output>
Criar `.planning/phases/03-sales-pipeline-funnel-view/03-04-SUMMARY.md` ao concluir.
</output>
