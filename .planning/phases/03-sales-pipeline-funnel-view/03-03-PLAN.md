---
phase: 03-sales-pipeline-funnel-view
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - package.json
  - package-lock.json
  - src/actions/lead-actions.ts
  - src/components/pipeline-board.tsx
  - src/components/pipeline-column.tsx
  - src/components/pipeline-lead-card.tsx
  - src/components/motivo-perda-dialog.tsx
autonomous: false
requirements: [PIPE-02]
user_setup: []
must_haves:
  truths:
    - "O admin arrasta um card de lead de uma coluna para outra e a etapa do lead é persistida no banco (PIPE-02)"
    - "O card se move instantaneamente (otimista) ao soltar, e reverte para a coluna original se a Server Action falhar"
    - "Arrastar um card para a coluna Perdido abre um modal opcional de 'Motivo da perda' que NÃO bloqueia o movimento"
    - "Clicar (sem arrastar) num card continua abrindo o modal de edição — o drag não engole o clique"
    - "stageChangedAt só é atualizado quando a etapa realmente muda (não em toda escrita)"
  artifacts:
    - path: "src/actions/lead-actions.ts"
      provides: "Server Action updateLeadStage(id, stage, motivoPerda?) validada por stageUpdateSchema"
      contains: "updateLeadStage"
    - path: "src/components/motivo-perda-dialog.tsx"
      provides: "Modal opcional não-bloqueante de captura de motivoPerda ao soltar em Perdido"
      contains: "Mover para Perdido"
    - path: "src/components/pipeline-board.tsx"
      provides: "DndContext + useOptimistic + startTransition chamando updateLeadStage no onDragEnd"
      contains: "useOptimistic"
    - path: "package.json"
      provides: "dependências @dnd-kit/core e @dnd-kit/sortable"
      contains: "@dnd-kit/core"
  key_links:
    - from: "src/components/pipeline-board.tsx"
      to: "src/actions/lead-actions.ts (updateLeadStage)"
      via: "onDragEnd → startTransition → await updateLeadStage"
      pattern: "updateLeadStage"
    - from: "src/components/pipeline-lead-card.tsx"
      to: "@dnd-kit/core useDraggable"
      via: "listeners/attributes + activationConstraint distance 8"
      pattern: "useDraggable"
    - from: "src/components/pipeline-column.tsx"
      to: "@dnd-kit/core useDroppable"
      via: "setNodeRef com id = stage"
      pattern: "useDroppable"
    - from: "src/actions/lead-actions.ts (updateLeadStage)"
      to: "leads.stageChangedAt"
      via: "bump condicional só quando previousStage !== newStage"
      pattern: "stageChanged"
---

<objective>
Adicionar o drag-and-drop persistente ao board criado em 03-02: instalar `@dnd-kit/core` + `@dnd-kit/sortable`, criar a Server Action `updateLeadStage` (chamada direto do `onDragEnd`, não via FormData/useActionState), e envolver os componentes do board com `DndContext` + `useOptimistic` para mover cards instantaneamente com reversão automática em falha. Soltar um card em "Perdido" abre um modal opcional de motivo da perda que não bloqueia o movimento. Ao final, o admin move leads pelo funil arrastando cards — fechando PIPE-02 e completando o valor central da Fase 3.

Purpose: PIPE-02 (mover lead entre etapas via drag-and-drop) é satisfeito aqui. A Server Action consome o `stageUpdateSchema` já definido em 03-01 e a coluna `stageChangedAt` já migrada, e anexa handlers aos componentes de board já renderizados em 03-02 — sem reestruturar o layout.
Output: dependências dnd-kit instaladas (após gate de legitimidade), Server Action `updateLeadStage`, `motivo-perda-dialog.tsx`, e os três componentes de board com wiring de drag/drop/otimista.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sales-pipeline-funnel-view/03-CONTEXT.md
@.planning/phases/03-sales-pipeline-funnel-view/03-RESEARCH.md
@.planning/phases/03-sales-pipeline-funnel-view/03-PATTERNS.md
@.planning/phases/03-sales-pipeline-funnel-view/03-UI-SPEC.md
@.planning/phases/03-sales-pipeline-funnel-view/03-01-PLAN.md
@.planning/phases/03-sales-pipeline-funnel-view/03-02-PLAN.md

<interfaces>
<!-- Contratos atuais. Use diretamente. -->

Contrato definido em 03-01 (src/lib/validations.ts) — consumir sem redefinir:
```typescript
export const stageUpdateSchema = z.object({
  id: z.coerce.number().int().positive(),
  stage: z.enum(["novo", "contatado", "negociacao", "fechado", "perdido"]),
  motivoPerda: z.string().trim().optional(),
});
```

Tipo ActionState + convenções da Server Action (src/actions/lead-actions.ts, linhas 1-12, 65-100) — seguir a forma de updateLead:
```typescript
type ActionState = { success: true } | { errors: Record<string, string[] | undefined> } | undefined;
// imports já presentes: and, eq, isNull (drizzle-orm); revalidatePath (next/cache); db; leads; leadSchema
// updateLead usa: db.update(leads).set(...).where(and(eq(leads.id, id), isNull(leads.deletedAt))) + revalidatePath("/")
```

Forma-alvo de updateLeadStage (RESEARCH.md Open Question 1 + 03-PATTERNS.md seção lead-actions.ts) — função async simples (NÃO ligada a useActionState), SELECT do stage atual antes do UPDATE para gatear o bump de stageChangedAt:
```typescript
export async function updateLeadStage(
  id: number, stage: Lead["stage"], motivoPerda?: string
): Promise<ActionState> {
  const parsed = stageUpdateSchema.safeParse({ id, stage, motivoPerda });
  if (!parsed.success) return { errors: parsed.error.flatten().fieldErrors };
  const [current] = await db.select({ stage: leads.stage }).from(leads)
    .where(and(eq(leads.id, parsed.data.id), isNull(leads.deletedAt)));
  if (!current) return { errors: { id: ["Lead inválido."] } };
  const stageChanged = current.stage !== parsed.data.stage;
  await db.update(leads).set({
    stage: parsed.data.stage,
    ...(parsed.data.motivoPerda !== undefined ? { motivoPerda: parsed.data.motivoPerda } : {}),
    ...(stageChanged ? { stageChangedAt: new Date() } : {}),
  }).where(and(eq(leads.id, parsed.data.id), isNull(leads.deletedAt)));
  revalidatePath("/pipeline");
  return { success: true };
}
```

Padrão @dnd-kit (RESEARCH.md Pattern 1) — DndContext no board, useDroppable na coluna (id = stage), useDraggable no card (id = lead.id); onDragEnd lê `event.active.id` (lead) e `event.over?.id` (stage destino).

Padrão useOptimistic + startTransition (RESEARCH.md Pattern 2):
```tsx
const [optimisticLeads, setOptimisticStage] = useOptimistic(
  leads,
  (state, { id, stage }: { id: number; stage: Lead["stage"] }) =>
    state.map((l) => (l.id === id ? { ...l, stage } : l))
);
// dentro do onDragEnd:
startTransition(async () => {
  setOptimisticStage({ id: leadId, stage: newStage });
  const result = await updateLeadStage(leadId, newStage);
  if (result && "errors" in result) toast.error("Não foi possível mover o lead. Tente novamente.");
  else toast.success(`Lead movido para ${label}.`);
});
```

Modal analog (src/components/discard-changes-dialog.tsx) — mesmo esqueleto Dialog/DialogContent/DialogHeader/DialogFooter; ver 03-PATTERNS.md seção "motivo-perda-dialog.tsx" para a forma completa.

Toast (sonner) — mesma convenção de src/components/lead-form-dialog.tsx e subnicho-manager.tsx (`toast.success` / `toast.error`).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Nada ainda — este checkpoint de SEGURANÇA gateia a instalação de `@dnd-kit/core` e `@dnd-kit/sortable`. RESEARCH.md marcou ambos os pacotes como `[ASSUMED]` porque o slopcheck não pôde rodar neste ambiente; o protocolo de legitimidade de pacote exige verificação humana antes do `npm install` (que pode disparar scripts de postinstall). Este gate NÃO é auto-aprovável (auto_advance ignorado).</what-built>
  <how-to-verify>
    1. Abrir https://www.npmjs.com/package/@dnd-kit/core e confirmar: repositório oficial `github.com/clauderic/dnd-kit`, downloads na casa de dezenas de milhões/semana, última publicação da versão 6.3.1 coerente com a pesquisa (2024-12-05), sem `postinstall` suspeito na aba de scripts.
    2. Abrir https://www.npmjs.com/package/@dnd-kit/sortable e confirmar: mesmo repositório `github.com/clauderic/dnd-kit`, downloads na casa de dezenas de milhões/semana, versão 10.0.0.
    3. Confirmar que ambos apontam para o org `clauderic` (autor original do dnd-kit) — não um fork/typosquat.
    Se qualquer sinal divergir (repo diferente, downloads baixos, postinstall inesperado), NÃO aprovar e reportar.
  </how-to-verify>
  <resume-signal>Digite "approved" para liberar a instalação, ou descreva a divergência encontrada.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Instalar @dnd-kit + Server Action updateLeadStage (contrato stageUpdateSchema + bump condicional de stageChangedAt)</name>
  <files>package.json, package-lock.json, src/actions/lead-actions.ts</files>
  <read_first>
    - src/actions/lead-actions.ts (arquivo inteiro — imports 1-7, ActionState 9-12, updateLead 65-100 como analog)
    - src/lib/validations.ts (stageUpdateSchema definido em 03-01 — confirmar export)
    - src/types/index.ts (tipo Lead)
    - 03-PATTERNS.md seção "src/actions/lead-actions.ts" (forma completa de updateLeadStage)
    - 03-RESEARCH.md Pitfall 3 (gatear stageChangedAt em mudança real de etapa) + Open Question 1 (função direta, não useActionState)
  </read_first>
  <behavior>
    - updateLeadStage(5, "negociacao") move o lead 5 para negociacao e retorna { success: true }
    - Chamar com stage inválido (ex.: "fechado_perdido") retorna { errors } via stageUpdateSchema, sem escrita
    - Chamar com id de lead inexistente/soft-deletado retorna { errors: { id: [...] } }
    - Quando previousStage === newStage, stageChangedAt NÃO é alterado; quando difere, stageChangedAt vira agora
    - motivoPerda ausente não sobrescreve o valor existente; motivoPerda presente é gravado
  </behavior>
  <action>
    Instalar as dependências (somente após o checkpoint de legitimidade acima ser aprovado): `npm install @dnd-kit/core@6.3.1 @dnd-kit/sortable@10.0.0`. Confirmar que ambas aparecem em `dependencies` no package.json.
    Em `src/actions/lead-actions.ts`: adicionar `updateLeadStage` seguindo a forma-alvo das interfaces (analog de updateLead, mas função async de argumentos posicionais, NÃO `(_prevState, formData)` — RESEARCH.md Open Question 1). Importar `stageUpdateSchema` de `@/lib/validations` e o tipo `Lead` de `@/types`. Validar via `stageUpdateSchema.safeParse({ id, stage, motivoPerda })`. Fazer um `SELECT` do `stage` atual (com o mesmo guard `isNull(leads.deletedAt)`) antes do `UPDATE`; se não existir, retornar `{ errors: { id: ["Lead inválido."] } }`. Computar `stageChanged = current.stage !== parsed.data.stage` e SÓ incluir `stageChangedAt: new Date()` no `.set(...)` quando `stageChanged` for true (Pitfall 3 — não resetar o relógio de esfriando em edições que não mudam etapa). Incluir `motivoPerda` no `.set(...)` apenas quando `!== undefined`. NÃO precisa do backstop de FK (`isForeignKeyViolation`) nem do `subnichoExists` (não há subnichoId aqui — manter enxuto). Finalizar com `revalidatePath("/pipeline")` (não `/`).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `@dnd-kit/core` e `@dnd-kit/sortable` presentes em `dependencies` do package.json nas versões 6.3.1 / 10.0.0
    - `npx tsc --noEmit` e `npm run build` passam
    - grep confirma `export async function updateLeadStage` em lead-actions.ts, com `stageUpdateSchema`, `revalidatePath("/pipeline")` e o bump condicional `stageChanged ? { stageChangedAt` na chamada `.set`
    - grep confirma que updateLeadStage NÃO usa `(_prevState` nem `FormData` (é função de argumentos posicionais)
    - grep confirma o guard `isNull(leads.deletedAt)` tanto no SELECT quanto no UPDATE de updateLeadStage
  </acceptance_criteria>
  <done>@dnd-kit instalado; updateLeadStage valida via stageUpdateSchema, respeita soft-delete, gateia stageChangedAt em mudança real de etapa e revalida /pipeline; tsc + build limpos.</done>
</task>

<task type="auto">
  <name>Task 2: Wiring de drag-and-drop otimista no board + modal opcional de motivoPerda ao soltar em Perdido</name>
  <files>src/components/pipeline-board.tsx, src/components/pipeline-column.tsx, src/components/pipeline-lead-card.tsx, src/components/motivo-perda-dialog.tsx</files>
  <read_first>
    - src/components/pipeline-board.tsx, pipeline-column.tsx, pipeline-lead-card.tsx (estado read-only entregue por 03-02)
    - src/components/discard-changes-dialog.tsx (analog estrutural do dialog)
    - src/components/lead-form-dialog.tsx (convenção de toast sonner) + src/components/ui/textarea.tsx
    - 03-PATTERNS.md seções "pipeline-board.tsx", "pipeline-column.tsx", "pipeline-lead-card.tsx", "motivo-perda-dialog.tsx"
    - 03-RESEARCH.md Pattern 1 (DndContext/useDroppable/useDraggable), Pattern 2 (useOptimistic + startTransition), Pitfall 4 (activationConstraint distance 8 para não engolir o clique)
    - 03-UI-SPEC.md "Copywriting Contract" (título "Mover para Perdido", corpo com nome, placeholder, botões "Pular"/"Salvar motivo", toasts)
  </read_first>
  <action>
    `src/components/motivo-perda-dialog.tsx` (novo, `"use client"`): seguir o esqueleto de `discard-changes-dialog.tsx` com props `{ open, onOpenChange, leadNome, onSkip, onSave }`. Estado local `motivo` (useState). Título "Mover para Perdido"; `DialogDescription` = ``Por que "${leadNome}" foi perdido? (opcional)``; `<Textarea>` com placeholder "Ex: sem orçamento, escolheu concorrente, não respondeu mais..."; `DialogFooter` com "Pular" (`variant="outline"`, chama `onSkip` — apenas fecha, NÃO reverte o drag, D-04) e "Salvar motivo" (teal `bg-[#0D9488]`, chama `onSave(motivo)`). É opcional/não-bloqueante: o card já se moveu otimisticamente antes deste modal abrir.

    `src/components/pipeline-column.tsx` (modificar): adicionar `const { setNodeRef, isOver } = useDroppable({ id: stage })` de `@dnd-kit/core` e aplicar `ref={setNodeRef}` no contêiner da coluna; a prop passa a incluir `stage` (o valor da etapa, usado como id do droppable). Opcional: usar `isOver` para um highlight leve. Manter min-w-288px/estado vazio/cabeçalho inalterados.

    `src/components/pipeline-lead-card.tsx` (modificar): adicionar `const { setNodeRef, listeners, attributes, transform } = useDraggable({ id: lead.id })`; aplicar `ref`, `{...listeners}`, `{...attributes}` e `style` de translate no card. Manter o `onClick` de edição separado dos listeners — a disambiguação clique-vs-drag vem do sensor no board (Pitfall 4), não de remover o onClick.

    `src/components/pipeline-board.tsx` (modificar): envolver a estrutura de colunas num `<DndContext>` de `@dnd-kit/core`. Configurar sensores com `useSensor(PointerSensor, { activationConstraint: { distance: 8 } })` (Pitfall 4 — movimentos < 8px contam como clique, preservando o abre-modal de 03-02). Trocar o uso direto de `leads` pela cópia otimista: `const [optimisticLeads, setOptimisticStage] = useOptimistic(leads, reducer)` (reducer conforme interfaces) e renderizar/agrupar as colunas a partir de `optimisticLeads`. Implementar `handleDragEnd(event)`: ler `leadId = Number(event.active.id)` e `newStage = event.over?.id as Lead["stage"] | undefined`; se `!newStage` retornar (soltou fora); envolver em `startTransition(async () => { setOptimisticStage({ id: leadId, stage: newStage }); ... })`. Se `newStage === "perdido"`, abrir o `MotivoPerdaDialog` (estado local guardando `{ leadId, leadNome }`) e chamar `updateLeadStage(leadId, "perdido", motivo?)` a partir de `onSave`/`onSkip` do modal (onSkip chama sem motivo — não reverte); para as demais etapas, chamar `await updateLeadStage(leadId, newStage)` direto. Em sucesso `toast.success(\`Lead movido para ${label}.\`)`; em erro `toast.error("Não foi possível mover o lead. Tente novamente.")` e NÃO tocar no estado base (React reverte o otimista quando a transição assenta — RESEARCH.md Pattern 2 caveat). Renderizar `<MotivoPerdaDialog>` ao lado do `<LeadFormDialog>` já existente.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
    <human-check>Em /pipeline: arrastar um card entre colunas move-o e persiste após reload; soltar em Perdido abre o modal "Mover para Perdido" — "Pular" fecha mantendo o movimento, "Salvar motivo" grava o motivo; clicar num card (sem mover) ainda abre o modal de edição; simular falha (ex.: parar o processo) confirma que o card volta à coluna original.</human-check>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` e `npm run build` passam
    - grep confirma `DndContext`, `useOptimistic`, `startTransition` e `PointerSensor` com `activationConstraint` em pipeline-board.tsx
    - grep confirma `useDroppable` em pipeline-column.tsx e `useDraggable` em pipeline-lead-card.tsx
    - grep confirma `updateLeadStage` chamado dentro de `startTransition` em pipeline-board.tsx
    - grep confirma `Mover para Perdido` e `variant="outline"` (botão Pular) em motivo-perda-dialog.tsx
    - pipeline-lead-card.tsx mantém o `onClick` de edição (grep confirma que não foi removido)
  </acceptance_criteria>
  <done>Cards arrastáveis entre colunas com persistência via updateLeadStage, movimento otimista com reversão em falha, modal opcional de motivoPerda ao soltar em Perdido, clique-para-editar preservado; tsc + build limpos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador (admin) → Server Action updateLeadStage → SQLite | Única fronteira de escrita desta fase. `{ id, stage, motivoPerda? }` cruza aqui via chamada direta do onDragEnd. Ferramenta solo, local, sem auth. |
| registry npm → node_modules (instalação de @dnd-kit) | Cadeia de suprimento: `npm install` pode executar scripts de pacote. Pacotes marcados `[ASSUMED]` (slopcheck não rodou). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-SC | Tampering | instalação de @dnd-kit/core + @dnd-kit/sortable (supply chain) | mitigate | Checkpoint blocking-human de legitimidade antes do `npm install` (verificação em npmjs.com: org clauderic, 20M+ downloads/semana, sem postinstall suspeito, versões 6.3.1/10.0.0). Gate não auto-aprovável. |
| T-03-03-01 | Tampering | entrada de stage do updateLeadStage | mitigate | `stageUpdateSchema` (z.enum de 5 valores + `z.coerce.number().int().positive()` no id) rejeita stage/id forjado no servidor antes de qualquer write; guard `isNull(leads.deletedAt)` impede mover lead soft-deletado. |
| T-03-03-02 | Tampering | motivoPerda (texto livre) | mitigate | `z.string().trim().optional()` normaliza no servidor; exibido como conteúdo React escapado, sem render de HTML bruto. |
| T-03-03-03 | Repudiation / Info disclosure / DoS / Spoofing / EoP | app inteiro | accept | Solo-admin, local, sem rede/auth/multi-usuário (out of scope em PROJECT.md). Drag-and-drop não adiciona superfície remota. |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo em todo o projeto.
- `npm run build` passa.
- @dnd-kit/core e @dnd-kit/sortable em dependencies (após gate de legitimidade aprovado).
- grep confirma o wiring: DndContext/useOptimistic/PointerSensor (board), useDroppable (coluna), useDraggable (card), updateLeadStage dentro de startTransition.
- Verificação visual (end-of-phase): arrastar persiste, reverte em falha, modal de Perdido opcional, clique-para-editar preservado.
</verification>

<success_criteria>
- O admin move leads entre etapas arrastando cards, com persistência no banco (PIPE-02).
- O movimento é otimista (instantâneo) e reverte automaticamente se a Server Action falhar.
- Soltar em Perdido oferece capturar o motivo da perda sem bloquear o movimento (D-04).
- O clique-para-editar de 03-02 continua funcionando (sensor com activationConstraint).
- `stageChangedAt` só muda em mudança real de etapa, preservando o relógio de esfriando (Pitfall 3).
</success_criteria>

<output>
Criar `.planning/phases/03-sales-pipeline-funnel-view/03-03-SUMMARY.md` ao concluir.
</output>
