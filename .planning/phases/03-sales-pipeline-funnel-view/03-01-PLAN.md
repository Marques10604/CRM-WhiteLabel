---
phase: 03-sales-pipeline-funnel-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/lib/validations.ts
  - src/components/etapa-badge.tsx
  - src/components/lead-form-dialog.tsx
  - src/db/migrations/
  - scripts/verify-pipeline-migration.cjs
autonomous: true
requirements: [PIPE-01]
must_haves:
  truths:
    - "As etapas Fechado e Perdido aparecem como badges distintos (verde/vermelho) na lista de leads existente"
    - "O modal de edição de lead oferece 5 opções de etapa (Novo, Contatado, Negociação, Fechado, Perdido)"
    - "O modal mostra um campo opcional 'Motivo da perda' quando a etapa selecionada é Perdido"
    - "Os 2 leads existentes que estavam em fechado_perdido passam a estar em fechado"
    - "Toda linha de leads passa a ter stage_changed_at preenchido (não-nulo)"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "enum stage de 5 valores + colunas motivoPerda e stageChangedAt"
      contains: "fechado"
    - path: "src/lib/validations.ts"
      provides: "leadSchema com enum 5 valores + motivoPerda; stageUpdateSchema"
      contains: "stageUpdateSchema"
    - path: "src/components/etapa-badge.tsx"
      provides: "STAGE_CONFIG com entradas fechado e perdido"
      contains: "perdido"
    - path: "src/db/migrations"
      provides: "migração ADD COLUMN + migração custom de backfill"
  key_links:
    - from: "src/db/migrations (custom)"
      to: "leads.stage"
      via: "UPDATE fechado_perdido -> fechado"
      pattern: "fechado_perdido"
    - from: "src/components/lead-form-dialog.tsx"
      to: "form.watch stage === perdido"
      via: "renderização condicional do campo motivoPerda"
      pattern: "motivoPerda"
---

<objective>
Desmembrar a etapa combinada `fechado_perdido` em duas etapas distintas (`fechado`, `perdido`) em todo o data-layer e refletir isso na UI já existente (badge + modal de lead), além de adicionar os dois campos novos que o board vai consumir (`motivoPerda`, `stageChangedAt`). Este é o corte vertical de fundação da Fase 3: ao final, na lista de leads que já existe (`/`), Fechado e Perdido aparecem como estados separados (verde/vermelho) e selecionáveis no modal — uma melhoria observável pelo usuário — e o banco fica pronto para o board (03-02) e o drag-and-drop (03-03).

Purpose: PIPE-01 (modelo de 5 etapas) exige o split do enum, e o board/esfriando dependem das colunas novas. Toda mudança de schema e contrato (Zod) vive aqui para as fases seguintes apenas consumirem.
Output: schema Drizzle de 5 etapas + 2 colunas nullable, migração versionada (ADD COLUMN) + migração custom de backfill aplicadas ao `./data/crm.db`, `stageUpdateSchema`, badge e modal atualizados.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sales-pipeline-funnel-view/03-CONTEXT.md
@.planning/phases/03-sales-pipeline-funnel-view/03-RESEARCH.md
@.planning/phases/03-sales-pipeline-funnel-view/03-PATTERNS.md
@.planning/phases/03-sales-pipeline-funnel-view/03-UI-SPEC.md

<interfaces>
<!-- Estado atual dos arquivos que este plano modifica. Use estes contratos diretamente. -->

Estado atual — src/db/schema.ts (coluna stage + timestamps, linhas 28-33):
```typescript
stage: text("stage", { enum: ["novo", "contatado", "negociacao", "fechado_perdido"] })
  .notNull()
  .default("novo"),
deletedAt: integer("deleted_at", { mode: "timestamp" }), // nullable = ativo
createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`),
updatedAt: integer("updated_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`),
```

Estado atual — src/lib/validations.ts (leadSchema.stage, linhas 37-39):
```typescript
stage: z.enum(["novo", "contatado", "negociacao", "fechado_perdido"]).default("novo"),
```

Estado atual — src/components/etapa-badge.tsx (STAGE_CONFIG, linhas 12-17):
```typescript
const STAGE_CONFIG: Record<Stage, { label: string; bg: string; text: string }> = {
  novo: { label: "Novo", bg: "#F4F4F5", text: "#3F3F46" },
  contatado: { label: "Contatado", bg: "#DBEAFE", text: "#1D4ED8" },
  negociacao: { label: "Negociação", bg: "#FEF3C7", text: "#B45309" },
  fechado_perdido: { label: "Fechado/Perdido", bg: "#E2E8F0", text: "#1E293B" },
};
// STAGE_OPTIONS (linhas 20-22) e EtapaBadge derivam de STAGE_CONFIG genericamente — não precisam mudar.
```

Estado atual — src/components/lead-form-dialog.tsx tem sua PRÓPRIA cópia duplicada de STAGE_OPTIONS (linhas 54-59, ainda com "fechado_perdido") e o bloco Field de `stage` fica na seção "Negócio" (linhas 256-285). O campo `notas` (linhas 307-314) é o template de textarea opcional.

Convenção de migração do projeto (travada em CLAUDE.md / 01-RESEARCH.md): usar `drizzle-kit generate` + `drizzle-kit migrate` (NÃO `push`). Config em drizzle.config.ts aponta out `./src/db/migrations`, db `./data/crm.db`. O único journal atual é `0000_gifted_slapstick`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split do enum stage + colunas novas no schema e nas validações Zod</name>
  <files>src/db/schema.ts, src/lib/validations.ts</files>
  <read_first>
    - src/db/schema.ts (tabela leads, linhas 16-41)
    - src/lib/validations.ts (leadSchema, linhas 9-42)
    - src/db/migrations/meta/0000_snapshot.json (confirma que stage é gravado como "type":"text" sem metadado de enum — Pitfall 1)
    - 03-PATTERNS.md seções "src/db/schema.ts" e "src/lib/validations.ts"
    - 03-RESEARCH.md Pitfall 1 (enum widen = zero SQL diff) e Pitfall 2 (ADD COLUMN sem default de expressão)
  </read_first>
  <behavior>
    - stageUpdateSchema aceita { id: 5, stage: "perdido", motivoPerda: "x" } e retorna success
    - stageUpdateSchema rejeita stage fora dos 5 valores (ex: "fechado_perdido")
    - stageUpdateSchema rejeita id <= 0 ou não-inteiro
    - leadSchema aceita motivoPerda ausente (opcional) e presente
  </behavior>
  <action>
    Em `src/db/schema.ts`: alargar o enum da coluna `stage` para `["novo", "contatado", "negociacao", "fechado", "perdido"]` (mantendo `.notNull().default("novo")`). Adicionar DUAS colunas novas nullable logo após `stage`, seguindo a convenção de nullable já usada por `deletedAt` — NÃO copiar o padrão `.default(sql\`(unixepoch())\`)` de createdAt/updatedAt (proibido em ADD COLUMN sobre tabela não-vazia, Pitfall 2): `motivoPerda: text("motivo_perda")` (nullable, D-03) e `stageChangedAt: integer("stage_changed_at", { mode: "timestamp" })` (nullable, sem default). Não adicionar índice novo.
    Em `src/lib/validations.ts`: trocar o enum de `leadSchema.stage` para os 5 valores acima (mantendo `.default("novo")`); adicionar `motivoPerda: z.string().trim().optional()` ao `leadSchema`. NÃO incluir `stageChangedAt` no leadSchema (é gerenciado só pelo servidor). Exportar um schema novo enxuto `stageUpdateSchema = z.object({ id: z.coerce.number().int().positive(), stage: z.enum(["novo","contatado","negociacao","fechado","perdido"]), motivoPerda: z.string().trim().optional() })` — este é o contrato consumido pelo `updateLeadStage` em 03-03 (interface-first). O tipo `Lead["stage"]` (derivado em src/types/index.ts via InferSelectModel) passa a ser a união de 5 valores automaticamente.
  </action>
  <verify>
    <automated>node scripts/verify-pipeline-schema-types.cjs || npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passa sem erros
    - grep confirma `"fechado", "perdido"` no enum de schema.ts e a ausência de `fechado_perdido` em schema.ts e validations.ts
    - grep confirma `motivo_perda` e `stage_changed_at` em schema.ts, sem `.default(` na mesma linha dessas duas colunas
    - grep confirma export `stageUpdateSchema` em validations.ts
  </acceptance_criteria>
  <done>schema.ts tem enum de 5 valores + motivoPerda/stageChangedAt nullable; validations.ts tem leadSchema com 5 valores + motivoPerda opcional e exporta stageUpdateSchema; tsc limpo.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Migração — ADD COLUMN gerado + backfill custom + apply + verificação no DB</name>
  <files>src/db/migrations/, scripts/verify-pipeline-migration.cjs</files>
  <read_first>
    - src/db/migrations/0000_gifted_slapstick.sql (estilo de SQL do projeto + separador `--> statement-breakpoint`)
    - src/db/migrations/meta/_journal.json (sequência atual: só 0000)
    - drizzle.config.ts (out/schema/dialect/url)
    - scripts/verify-schema.cjs (analog de script de verificação de DB via better-sqlite3, fileMustExist)
    - 03-PATTERNS.md seção "Migration: enum widen (no-op) + 2 new columns + custom data backfill"
    - 03-RESEARCH.md Pitfalls 1 e 2 (verificados contra sqlite.org/lang_altertable.html)
  </read_first>
  <action>
    Esta é a task BLOQUEANTE de sync de schema — build/tsc passam sem banco vivo (falso-positivo), então o DB real DEVE ser migrado aqui, após as edições de schema.ts (Task 1) e antes da verificação da fase. Passos, na ordem:
    1. Rodar `npx drizzle-kit generate`. Abrir o `.sql` gerado (0001_*) e CONFIRMAR que ele contém APENAS `ALTER TABLE \`leads\` ADD \`motivo_perda\` text;` e `ALTER TABLE \`leads\` ADD \`stage_changed_at\` integer;` (com o separador `--> statement-breakpoint` entre elas) e NENHUM ALTER na coluna `stage` nem qualquer sequência de rebuild `CREATE TABLE ... RENAME` (Pitfall 1 — se aparecer rebuild, investigar antes de prosseguir).
    2. Rodar `npx drizzle-kit generate --custom --name=backfill-fechado-perdido-split` para gerar um arquivo de migração custom vazio (0002_*). Escrever à mão nele exatamente dois statements separados por `--> statement-breakpoint`: `UPDATE leads SET stage = 'fechado' WHERE stage = 'fechado_perdido';` (D-02) e `UPDATE leads SET stage_changed_at = unixepoch() WHERE stage_changed_at IS NULL;` (backfill do relógio de esfriando via valor literal computado — permitido em UPDATE, ao contrário de DEFAULT em ADD COLUMN, Pitfall 2). NÃO adicionar cláusula DEFAULT de expressão em coluna.
    3. Rodar `npx drizzle-kit migrate` para aplicar 0001 e 0002 em ordem ao `./data/crm.db`.
    4. Criar `scripts/verify-pipeline-migration.cjs` no molde de `scripts/verify-schema.cjs` (better-sqlite3, `fileMustExist: true`, `process.exit(1)` em falha): asserir via `PRAGMA table_info(leads)` que as colunas `motivo_perda` e `stage_changed_at` existem; via `SELECT count(*)` que há 0 linhas com `stage = 'fechado_perdido'`; que há linhas com `stage = 'fechado'`; e que 0 linhas têm `stage_changed_at IS NULL`. Rodar o script e confirmar saída OK.
  </action>
  <verify>
    <automated>node scripts/verify-pipeline-migration.cjs</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo 0001_*.sql contém as 2 linhas ADD COLUMN e nenhuma linha com `RENAME`/rebuild de tabela ou ALTER em `stage`
    - O arquivo 0002_*.sql (custom) contém os 2 UPDATE (fechado_perdido->fechado e backfill de stage_changed_at), separados por `--> statement-breakpoint`
    - src/db/migrations/meta/_journal.json passa a listar 0001 e 0002 além de 0000
    - `node scripts/verify-pipeline-migration.cjs` sai com código 0 e imprime OK (colunas presentes, 0 fechado_perdido, 0 stage_changed_at nulos)
  </acceptance_criteria>
  <done>Migração ADD COLUMN + backfill custom geradas, versionadas e aplicadas ao ./data/crm.db; script de verificação confirma o estado migrado.</done>
</task>

<task type="auto">
  <name>Task 3: Refletir o split na UI — badge (5 cores) + modal de lead (5 opções + campo Motivo da perda)</name>
  <files>src/components/etapa-badge.tsx, src/components/lead-form-dialog.tsx</files>
  <read_first>
    - src/components/etapa-badge.tsx (STAGE_CONFIG linhas 12-22)
    - src/components/lead-form-dialog.tsx (STAGE_OPTIONS linhas 54-59, bloco Field stage linhas 256-285, bloco notas linhas 307-314, defaultValues linhas 91-102)
    - 03-UI-SPEC.md seções "Color" (paleta de badges 5 valores) e "Copywriting Contract" (linha do campo "Motivo da perda")
    - 03-PATTERNS.md seções "etapa-badge.tsx" e "lead-form-dialog.tsx"
  </read_first>
  <action>
    Em `src/components/etapa-badge.tsx`: substituir a entrada única `fechado_perdido` de `STAGE_CONFIG` por duas entradas com as cores travadas no UI-SPEC (D-05): `fechado: { label: "Fechado", bg: "#DCFCE7", text: "#15803D" }` e `perdido: { label: "Perdido", bg: "#FEE2E2", text: "#B91C1C" }`. Ajustar/remover o comentário de bloco que justificava a cor slate combinada. `STAGE_OPTIONS` e `EtapaBadge` não mudam (derivam de STAGE_CONFIG). Atualizar o comentário obsoleto sobre "estado terminal único".
    Em `src/components/lead-form-dialog.tsx`: atualizar a cópia duplicada local de `STAGE_OPTIONS` (linhas 54-59) para os 5 valores `novo/contatado/negociacao/fechado/perdido` com labels correspondentes (mesma forma `as const`) — mantém a duplicação pré-existente com etapa-badge em lockstep, não refatorar. Adicionar `motivoPerda: lead?.motivoPerda ?? ""` aos `defaultValues` do `useForm`. Adicionar, imediatamente APÓS o bloco Field de `stage` na seção "Negócio", um bloco Field condicional para `motivoPerda` seguindo o mesmo padrão Field/FieldContent/FieldDescription/FieldError do campo `notas`: label "Motivo da perda", `<Textarea id="motivoPerda" {...form.register("motivoPerda")}>`, FieldDescription curta, renderizado SOMENTE quando `form.watch("stage") === "perdido"` (exibição condicional). O campo é opcional — sem validação bloqueante (leadSchema já trata motivoPerda como optional).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` e `npm run build` passam
    - grep em etapa-badge.tsx confirma `fechado:` e `perdido:` com os hex #DCFCE7/#15803D e #FEE2E2/#B91C1C, e ausência de `fechado_perdido`
    - grep em lead-form-dialog.tsx confirma STAGE_OPTIONS com 5 entradas (sem `fechado_perdido`), `form.watch("stage")` e `motivoPerda` no register e nos defaultValues
  </acceptance_criteria>
  <done>Badge exibe Fechado (verde) e Perdido (vermelho) distintos; modal de lead lista 5 etapas e mostra o campo opcional "Motivo da perda" só quando a etapa é Perdido; tsc + build limpos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador (admin) → Server Action / DB | Única fronteira. Ferramenta solo, local, sem auth (PROJECT.md/CLAUDE.md). Entrada do formulário de lead e futura entrada de stage cruzam aqui. |
| arquivo de migração → SQLite | SQL de migração roda com privilégio total sobre `./data/crm.db`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | leadSchema.motivoPerda (entrada de texto livre) | mitigate | Zod `z.string().trim().optional()` valida/normaliza no server dentro de leadSchema antes de qualquer write; sem HTML/render bruto (texto é exibido como conteúdo React escapado). |
| T-03-02 | Tampering | contrato stageUpdateSchema (enum stage) | mitigate | `z.enum` de 5 valores rejeita qualquer stage forjado (inclusive o antigo `fechado_perdido`); `id` via `z.coerce.number().int().positive()`. Contrato definido aqui, aplicado em 03-03. |
| T-03-03 | Tampering / Data integrity | migração custom de backfill | mitigate | UPDATE restrito por `WHERE stage = 'fechado_perdido'` e `WHERE stage_changed_at IS NULL`; verificado por scripts/verify-pipeline-migration.cjs (0 linhas remanescentes). Volume de 2 linhas confirmado em pesquisa. |
| T-03-04 | Repudiation / Info disclosure / DoS / Spoofing / EoP | app inteiro | accept | Ferramenta de um único admin, rodando localmente sem rede/auth/multi-usuário (out of scope em PROJECT.md). Sem PII exposta a terceiros, sem superfície remota. |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo em todo o projeto.
- `npm run build` passa.
- `node scripts/verify-pipeline-migration.cjs` confirma o DB migrado (colunas novas, 0 fechado_perdido, 0 stage_changed_at nulos).
- grep confirma ausência de `fechado_perdido` em src/db/schema.ts, src/lib/validations.ts, src/components/etapa-badge.tsx e src/components/lead-form-dialog.tsx (exceto dentro dos arquivos de migração, onde é intencional).
</verification>

<success_criteria>
- Na lista de leads existente (`/`), leads em Fechado e Perdido aparecem como badges verde e vermelho distintos.
- O modal de edição de lead oferece as 5 etapas e mostra "Motivo da perda" (opcional) quando a etapa é Perdido.
- O `./data/crm.db` tem as colunas `motivo_perda` e `stage_changed_at`, nenhum lead em `fechado_perdido`, e `stage_changed_at` preenchido em todas as linhas.
- Contratos `stageUpdateSchema` e `leadSchema.motivoPerda` disponíveis para 03-02/03-03 consumirem.
</success_criteria>

<output>
Criar `.planning/phases/03-sales-pipeline-funnel-view/03-01-SUMMARY.md` ao concluir.
</output>
