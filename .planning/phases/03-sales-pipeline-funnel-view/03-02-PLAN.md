---
phase: 03-sales-pipeline-funnel-view
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/app/pipeline/page.tsx
  - src/components/pipeline-board.tsx
  - src/components/pipeline-column.tsx
  - src/components/pipeline-lead-card.tsx
  - src/components/app-sidebar.tsx
autonomous: true
requirements: [PIPE-01, PIPE-03]
must_haves:
  truths:
    - "O admin acessa /pipeline pelo item 'Pipeline' da sidebar e vê um board com 5 colunas (Novo, Contatado, Negociação, Fechado, Perdido)"
    - "Cada cabeçalho de coluna mostra o nome da etapa e a contagem de leads daquela etapa (PIPE-01)"
    - "Cada card mostra nome + sub-nicho + data de follow-up do lead (sem badge de etapa, sem valor)"
    - "Leads na coluna Contatado com 5+ dias desde a última mudança de etapa aparecem com borda âmbar de 2px e rótulo 'Esfriando' + ícone Clock (PIPE-03)"
    - "Clicar (não arrastar) num card abre o mesmo lead-form-dialog de edição, pré-preenchido"
    - "Coluna sem leads mostra 'Nenhum lead nessa etapa' em texto muted, sem botão"
    - "Quando as 5 colunas excedem a viewport, o board rola horizontalmente sem encolher as colunas (min 288px cada)"
  artifacts:
    - path: "src/app/pipeline/page.tsx"
      provides: "Server Component que busca leads ativos, agrupa por etapa e computa esfriando server-side"
      contains: "isNull(leads.deletedAt)"
    - path: "src/components/pipeline-board.tsx"
      provides: "Client Component que renderiza as 5 colunas e orquestra o dialog de edição no clique do card"
      min_lines: 40
    - path: "src/components/pipeline-column.tsx"
      provides: "Coluna com cabeçalho (nome · contagem), estado vazio e lista de cards"
      contains: "min-w-[288px]"
    - path: "src/components/pipeline-lead-card.tsx"
      provides: "Card de lead (nome/sub-nicho/follow-up) com indicador esfriando e onClick de edição"
      contains: "Esfriando"
    - path: "src/components/app-sidebar.tsx"
      provides: "Item de navegação 'Pipeline' apontando para /pipeline"
      contains: "/pipeline"
  key_links:
    - from: "src/app/pipeline/page.tsx"
      to: "src/components/pipeline-board.tsx"
      via: "props leads + subnichos + esfriandoLeadIds"
      pattern: "PipelineBoard"
    - from: "src/components/pipeline-lead-card.tsx"
      to: "LeadFormDialog (via setDialogState no pipeline-board)"
      via: "onClick que abre o modal de edição"
      pattern: "onClick"
    - from: "src/components/app-sidebar.tsx"
      to: "/pipeline"
      via: "entrada em NAV_ITEMS"
      pattern: "Pipeline"
---

<objective>
Construir o board de pipeline **somente-leitura** (`/pipeline`): 5 colunas fixas com contagem por etapa, cards com nome/sub-nicho/follow-up, o indicador "esfriando" para leads parados em Contatado, e o clique-no-card que reabre o modal de edição existente. Este é o corte vertical visível da Fase 3 que entrega o núcleo de valor do projeto — "enxergar o funil de vendas de relance" — mesmo antes do drag-and-drop (que vem em 03-03). Ao final deste plano, o admin abre `/pipeline` e vê seu funil inteiro de relance, com os leads esfriando destacados.

Purpose: PIPE-01 (board de 5 etapas + contagem) e PIPE-03 (flag "esfriando") são inteiramente satisfeitos aqui, consumindo o schema/coluna `stageChangedAt` já entregue por 03-01. Nenhuma mutação nova é introduzida — o board só lê. O drag-and-drop e a persistência (PIPE-02) ficam para 03-03, que vai anexar handlers aos componentes criados aqui.
Output: rota `/pipeline` (Server Component), três componentes novos (`pipeline-board`, `pipeline-column`, `pipeline-lead-card`), e o novo item de navegação "Pipeline" na sidebar.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sales-pipeline-funnel-view/03-CONTEXT.md
@.planning/phases/03-sales-pipeline-funnel-view/03-RESEARCH.md
@.planning/phases/03-sales-pipeline-funnel-view/03-PATTERNS.md
@.planning/phases/03-sales-pipeline-funnel-view/03-UI-SPEC.md
@.planning/phases/03-sales-pipeline-funnel-view/03-01-PLAN.md

<interfaces>
<!-- Contratos atuais no codebase. Use diretamente — sem exploração adicional. -->

Tipo Lead (src/types/index.ts) — após 03-01, `stage` é a união dos 5 valores e o tipo já inclui `motivoPerda: string | null` e `stageChangedAt: Date | null`:
```typescript
export type Lead = InferSelectModel<typeof leads>;
// stage: "novo" | "contatado" | "negociacao" | "fechado" | "perdido"
// stageChangedAt: Date | null   (nullable — pode ser null em leads criados após 03-01 sem passar por updateLeadStage)
export type Subnicho = InferSelectModel<typeof subnichos>;
```

Analog do Server Component (src/app/page.tsx, arquivo inteiro) — copiar a forma do fetch:
```typescript
import { asc, isNull } from "drizzle-orm";
import { db } from "@/db/client";
import { leads, subnichos } from "@/db/schema";

export default async function Home() {
  const [activeLeads, allSubnichos] = await Promise.all([
    db.select().from(leads).where(isNull(leads.deletedAt)).orderBy(asc(leads.followUpDate)),
    db.select().from(subnichos),
  ]);
  return (
    <div className="flex flex-col gap-6">
      <h1 className="text-[28px] font-semibold leading-tight">Leads</h1>
      <LeadTable leads={activeLeads} subnichos={allSubnichos} />
    </div>
  );
}
```

Padrão de dialog-state (src/components/lead-table.tsx, linhas 34-45, 76, 177-185) — reusar VERBATIM no pipeline-board:
```typescript
type DialogState = { mode: "closed" } | { mode: "create" } | { mode: "edit"; lead: Lead };
const [dialogState, setDialogState] = useState<DialogState>({ mode: "closed" });
const dialogLead = dialogState.mode === "edit" ? dialogState.lead : undefined;
// Card/row click:
onClick={() => setDialogState({ mode: "edit", lead })}
// Render do modal (mesmo contrato de props do LeadFormDialog):
<LeadFormDialog
  key={dialogState.mode === "edit" ? `edit-${dialogState.lead.id}` : "create"}
  open={dialogState.mode !== "closed"}
  onOpenChange={(open) => { if (!open) setDialogState({ mode: "closed" }); }}
  subnichos={subnichos}
  lead={dialogLead}
/>
```

Resolução subnichoId → nome (src/components/lead-table.tsx, linhas 49-52) — mesmo padrão Map:
```typescript
const subnichoNameById = useMemo(() => new Map(subnichos.map((s) => [s.id, s.nome])), [subnichos]);
```

Sidebar NAV_ITEMS (src/components/app-sidebar.tsx, linhas 7-11) — estado atual:
```typescript
const NAV_ITEMS = [
  { href: "/", label: "Leads" },
  { href: "/subnichos", label: "Sub-nichos" },
  { href: "/lixeira", label: "Lixeira" },
] as const;
```
A lógica de render (`isActive` via `pathname.startsWith(item.href)`, linhas 22-40) é genérica sobre NAV_ITEMS — só adicionar a entrada.

STAGE_CONFIG / STAGE_OPTIONS (src/components/etapa-badge.tsx) — após 03-01 já tem as 5 etapas com labels/cores. Use `STAGE_OPTIONS` (array `{ value, label }` derivado de STAGE_CONFIG) como fonte única dos nomes/ordem das colunas — NÃO hardcodar uma quarta cópia dos labels.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rota /pipeline (Server Component) + computação de esfriando server-side + item de nav na sidebar</name>
  <files>src/app/pipeline/page.tsx, src/components/app-sidebar.tsx</files>
  <read_first>
    - src/app/page.tsx (arquivo inteiro — analog exato do fetch de leads ativos)
    - src/components/app-sidebar.tsx (NAV_ITEMS linhas 7-11 + render genérico 22-40)
    - src/db/schema.ts (confirmar coluna stageChangedAt após 03-01)
    - 03-PATTERNS.md seções "src/app/pipeline/page.tsx" e "src/components/app-sidebar.tsx"
    - 03-RESEARCH.md Open Question 2 (esfriando computado no Server Component) + 03-UI-SPEC.md seção "Copywriting Contract" (título "Pipeline")
  </read_first>
  <action>
    Criar `src/app/pipeline/page.tsx` como Server Component `async`, copiando a forma de `src/app/page.tsx`: buscar `activeLeads` via `db.select().from(leads).where(isNull(leads.deletedAt)).orderBy(asc(leads.followUpDate))` e `allSubnichos` via `db.select().from(subnichos)` em `Promise.all` — board é NÃO-filtrado (D-12), sem cláusula de stage/subnicho. Computar o conjunto de ids "esfriando" server-side (D-06/D-07, per RESEARCH.md Open Question 2) usando `date-fns` `differenceInDays`: `const esfriandoLeadIds = activeLeads.filter((l) => l.stage === "contatado" && l.stageChangedAt != null && differenceInDays(new Date(), l.stageChangedAt) >= 5).map((l) => l.id)`. IMPORTANTE: guardar `l.stageChangedAt != null` — leads criados após 03-01 sem passar por updateLeadStage têm `stageChangedAt` nulo e NÃO devem ser flagados nem quebrar o cálculo. Renderizar `<h1 className="text-[28px] font-semibold leading-tight">Pipeline</h1>` (Display, per UI-SPEC) dentro de `<div className="flex flex-col gap-6">` e abaixo `<PipelineBoard leads={activeLeads} subnichos={allSubnichos} esfriandoLeadIds={esfriandoLeadIds} />`. Em `src/components/app-sidebar.tsx`: inserir `{ href: "/pipeline", label: "Pipeline" }` em `NAV_ITEMS` logo após a entrada `/` (D-13) — nenhuma outra mudança (o render já é genérico).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <human-check>Abrir /pipeline pela sidebar; o item "Pipeline" aparece e fica ativo (teal) na rota; o título "Pipeline" aparece no topo.</human-check>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passa (mesmo antes do PipelineBoard existir, se Task 2 já concluída; se rodada isolada, o import de PipelineBoard será resolvido por Task 2)
    - grep confirma `isNull(leads.deletedAt)` e `differenceInDays` em src/app/pipeline/page.tsx, com o guard `stageChangedAt != null` na mesma expressão de filtro de esfriando
    - grep confirma `{ href: "/pipeline", label: "Pipeline" }` em src/components/app-sidebar.tsx
    - src/app/pipeline/page.tsx NÃO contém filtro por stage/subnicho no where (board não-filtrado, D-12)
  </acceptance_criteria>
  <done>Rota /pipeline busca leads ativos não-filtrados, computa esfriandoLeadIds server-side (com guard de null), renderiza título "Pipeline" e passa dados para PipelineBoard; sidebar tem o item "Pipeline".</done>
</task>

<task type="auto">
  <name>Task 2: Componentes do board somente-leitura — PipelineBoard + PipelineColumn + PipelineLeadCard (5 colunas, contagem, esfriando, clique-abre-modal)</name>
  <files>src/components/pipeline-board.tsx, src/components/pipeline-column.tsx, src/components/pipeline-lead-card.tsx</files>
  <read_first>
    - src/components/lead-table.tsx (dialog-state linhas 34-45/76/177-185; subnichoNameById 49-52; CTA "Novo lead" 81-86; empty-state 89-103)
    - src/components/lead-table-columns.tsx (formatação de data — `format(row.original.followUpDate, "dd/MM/yyyy")`, ~linha 90)
    - src/components/etapa-badge.tsx (STAGE_OPTIONS como fonte de nomes/ordem das colunas)
    - src/components/lead-form-dialog.tsx (contrato de props: open/onOpenChange/subnichos/lead/key)
    - 03-PATTERNS.md seções "pipeline-board.tsx", "pipeline-column.tsx", "pipeline-lead-card.tsx"
    - 03-UI-SPEC.md seções "Color" (esfriando #F59E0B/#B45309, cabeçalho de coluna, card), "Typography", "Spacing Scale" (min-w-288px, overflow-x-auto, sticky header) e "Copywriting Contract" (cabeçalho "{etapa} · {n}", vazio "Nenhum lead nessa etapa")
  </read_first>
  <action>
    Todos os três componentes são `"use client"` (o card precisa de onClick e, em 03-03, de handlers de drag). NÃO adicionar `@dnd-kit` nada nesta fase — apenas render + clique.

    `src/components/pipeline-lead-card.tsx`: props `{ lead: Lead; subnichoNome: string; isEsfriando: boolean; onClick: () => void }`. Card `<div>` com `className` condicional (cn): base `"rounded-lg bg-white p-4 cursor-pointer"`; borda `isEsfriando ? "border-2 border-[#F59E0B]" : "border border-zinc-200"`. Conteúdo: nome (Body 16px, mais proeminente), sub-nicho (Label 14px, muted), e linha com a data `format(lead.followUpDate, "dd/MM/yyyy")` (Label 14px); quando `isEsfriando`, ao lado da data um `<span className="flex items-center gap-1 text-[#B45309]"><Clock className="size-3.5" /> Esfriando</span>` (ícone `Clock` de lucide-react). SEM `EtapaBadge` no card (D-09 — etapa é implícita pela coluna). O `onClick` do card dispara o prop `onClick` (edição).

    `src/components/pipeline-column.tsx`: props `{ label: string; count: number; children: React.ReactNode }`. `<div className="flex min-w-[288px] shrink-0 flex-col gap-2 rounded-lg bg-[#F4F4F5] p-2">` com cabeçalho `sticky top-0` mostrando `{label} · {count}` (nome em Heading 20px/600, contagem Label 14px — apenas contagem, sem soma de valor, D-11) e uma área de cards `overflow-y-auto`. Estado vazio (D-14): quando não há cards, `<p className="py-12 text-center text-[14px] text-muted-foreground">Nenhum lead nessa etapa</p>` — SEM botão/CTA (diferente do empty-state da lista). NÃO usar `useDroppable` ainda (entra em 03-03).

    `src/components/pipeline-board.tsx`: props `{ leads: Lead[]; subnichos: Subnicho[]; esfriandoLeadIds: number[] }`. Reusar o padrão de dialog-state VERBATIM de lead-table.tsx (`DialogState`, `setDialogState`, `dialogLead`) e o `subnichoNameById` (Map). Construir um `Set(esfriandoLeadIds)` para lookup O(1). Derivar a ordem/labels das 5 colunas de `STAGE_OPTIONS` (etapa-badge.tsx) e agrupar `leads` por `stage` em memória (dataset pequeno, sem GROUP BY SQL). Layout raiz: `<div className="flex gap-6 overflow-x-auto pb-8">` (rolagem horizontal, D-15 — nunca encolher colunas) mapeando `STAGE_OPTIONS` para uma `<PipelineColumn label count>` por etapa, cada uma renderizando seus leads como `<PipelineLeadCard>` com `subnichoNome={subnichoNameById.get(lead.subnichoId) ?? "—"}`, `isEsfriando={esfriandoSet.has(lead.id)}` e `onClick={() => setDialogState({ mode: "edit", lead })}`. Incluir o CTA global "Novo lead" (mesmo botão teal `bg-[#0D9488]`, abre `setDialogState({ mode: "create" })`) acima do board. Renderizar o `<LeadFormDialog>` com o mesmo contrato de props mostrado nas interfaces (key/open/onOpenChange/subnichos/lead). Esta é a estrutura que 03-03 vai envolver com `DndContext` — deixar o layout de colunas/cards limpo e estável.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
    <human-check>Em /pipeline: 5 colunas na ordem Novo→Perdido com contagem correta no cabeçalho; cards mostram nome+sub-nicho+data; um lead 5+ dias em Contatado tem borda âmbar + "Esfriando"; clicar num card abre o modal de edição pré-preenchido; coluna vazia mostra "Nenhum lead nessa etapa" sem botão; em janela estreita o board rola na horizontal sem encolher colunas.</human-check>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` e `npm run build` passam
    - grep confirma `min-w-[288px]` e `shrink-0` em pipeline-column.tsx e `overflow-x-auto` em pipeline-board.tsx
    - grep confirma `Nenhum lead nessa etapa` em pipeline-column.tsx e ausência de `<Button` no bloco de estado vazio da coluna
    - grep confirma `Clock` e `border-2 border-[#F59E0B]` em pipeline-lead-card.tsx
    - grep confirma `setDialogState({ mode: "edit"` e `LeadFormDialog` em pipeline-board.tsx
    - grep confirma ausência de `@dnd-kit` / `useDroppable` / `useDraggable` / `DndContext` nos três arquivos (drag entra só em 03-03)
  </acceptance_criteria>
  <done>Board somente-leitura completo: 5 colunas com contagem, cards com nome/sub-nicho/follow-up, indicador esfriando na coluna Contatado, clique-no-card reabre o modal de edição, coluna vazia sem CTA, rolagem horizontal; tsc + build limpos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador (admin) → Server Component (leitura DB) | Única fronteira desta fatia. `/pipeline` apenas LÊ leads ativos via `db.select()` — nenhuma entrada do usuário cruza para o servidor neste plano (sem mutação). Ferramenta solo, local, sem auth (PROJECT.md/CLAUDE.md). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Information Disclosure | /pipeline exibe nome/sub-nicho/follow-up de leads (PII) | accept | Ferramenta de um único admin, local, sem rede/multi-usuário — a mesma PII já é exibida na lista `/` (Fase 1). Sem nova superfície de exposição; render React escapa o conteúdo. |
| T-03-02-02 | Tampering | Board somente-leitura | accept | Este plano não introduz nenhuma mutação nem endpoint de escrita. A persistência de etapa (e sua validação Zod) chega apenas em 03-03 (`stageUpdateSchema`). Zero superfície de escrita nova aqui. |
| T-03-02-03 | Repudiation / DoS / Spoofing / EoP | app inteiro | accept | Solo-admin, local, sem auth/rede (out of scope em PROJECT.md). Sem superfície remota. |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo em todo o projeto.
- `npm run build` passa.
- grep confirma ausência total de `@dnd-kit`/`DndContext`/`useDroppable`/`useDraggable` nos componentes do board (drag é escopo de 03-03).
- Verificação visual (end-of-phase): board com 5 colunas + contagem, esfriando destacado, clique-abre-modal, coluna vazia sem CTA, rolagem horizontal.
</verification>

<success_criteria>
- O admin abre `/pipeline` pela sidebar e vê o funil inteiro em 5 colunas com contagem por etapa (PIPE-01).
- Leads parados 5+ dias em Contatado aparecem visualmente destacados como "Esfriando" (PIPE-03).
- Clicar num card reabre o modal de edição de lead existente, pré-preenchido (D-10 fase-3).
- Os componentes `pipeline-board`/`pipeline-column`/`pipeline-lead-card` estão prontos para 03-03 anexar drag-and-drop sem reestruturação.
</success_criteria>

<output>
Criar `.planning/phases/03-sales-pipeline-funnel-view/03-02-SUMMARY.md` ao concluir.
</output>
