---
context: phase
phase: 03-sales-pipeline-funnel-view
task: null
total_tasks: null
status: planned_not_executed
last_updated: 2026-07-21T05:00:03.845Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Planner Agent() cut off mid-response by API error | The first planner run for Phase 3 disconnected mid-stream after writing `03-01-PLAN.md` to disk but before returning a `## PLANNING COMPLETE` marker or committing. The orchestrator caught this via the filesystem fallback pattern (checked `ls *-PLAN.md` on disk) instead of trusting the empty/truncated Agent return, then re-spawned the planner in an explicit "resume" mode telling it 03-01 was already done and only 03-02/03-03 remained. | advisory | If any `/gsd-execute-phase` or future planner Agent() call returns empty/truncated, ALWAYS check disk (`ls .planning/phases/03-sales-pipeline-funnel-view/*.md`) before assuming nothing happened — recoverable work may exist uncommitted. |
| Orphaned uncommitted planning artifacts | Because of the above disconnect, `03-01-PLAN.md` and `03-PATTERNS.md` sat as untracked files for several turns and were nearly lost/overlooked. Caught before pausing by running `git status --porcelain` and comparing against what each planner/mapper agent claimed to have committed. | advisory | Before any pause/handoff, always run `git status --porcelain` and reconcile against agent-reported commits — don't trust an agent's "committed" claim without verifying. |
| ROADMAP.md checkbox marked `[x]` before execution | The planner marked `03-01-PLAN.md` as `[x]` (complete) in ROADMAP.md right after writing the plan file — but `[x]` in this project's established convention (see Phase 1) means EXECUTED (has a SUMMARY.md + code commit), not merely "plan written". Caught and corrected to `[ ]` before pausing. | blocking | Before executing Phase 3, verify: schema.ts still has the OLD 4-value enum (`novo/contatado/negociacao/fechado_perdido`) at time of pause — if you see `[x]` on any 03-0X-PLAN.md line in ROADMAP.md going forward, cross-check against actual code/SUMMARY.md before trusting it. |

**Do not proceed until the blocking row above is acknowledged: schema.ts is UNCHANGED (still 4-value enum) as of this pause — 03-01 has NOT been executed despite any stale checkbox appearance.**

<current_state>
Phase 3 (Sales Pipeline & Funnel View) is fully planned and plan-checker-verified, but ZERO execution has happened yet. All 3 plans (03-01, 03-02, 03-03) exist on disk and are committed. No SUMMARY.md files exist for this phase. `src/db/schema.ts` still has the original 4-value `stage` enum — nothing has touched the database yet.

Session also completed, earlier and unrelated to Phase 3 planning: two quick-task UI fixes (dialog/modal overflow on `src/components/ui/dialog.tsx`, and help-text additions on `src/components/lead-form-dialog.tsx`).
</current_state>

<completed_work>

Quick tasks (both fully executed and merged):
- 260720-x41: Dialog max-h + overflow-y-auto fix — commit `c00c8cb`
- 260721-0cl: Field help text (FieldDescription) on 8 lead-form fields — commit `689b168`

Phase 3 planning pipeline (all done, nothing executed yet):
- discuss-phase 3 — commit `ffcba99` (8 gray areas covered; the big one is the Fechado/Perdido split, see Decisions below)
- ui-phase 3 — commit `610a415` (UI-SPEC approved 6/6 dimensions)
- research-phase 3 — commit `3f75ceb`, resolved-questions fix `4220d64`
- pattern-map 3 — committed as part of `e3e00d5` (recovered after connection drop)
- plan-phase 3 — 03-01 planned (recovered, committed in `e3e00d5`), 03-02/03-03 planned and committed in `1c218bf`
- plan-checker — PASSED, commit implied by verification session record, doc-fix in `4220d64`
</completed_work>

<remaining_work>

- **Execute 03-01-PLAN.md**: schema migration (split `fechado_perdido` enum into `fechado`/`perdido`, add nullable `motivoPerda` + `stageChangedAt` columns, custom backfill migration), update `etapa-badge.tsx` STAGE_CONFIG and `lead-form-dialog.tsx` STAGE_OPTIONS in lockstep. Includes a `[BLOCKING]` schema-push task that MUST run before verification (types come from Drizzle schema, not the live DB — false-positive risk if skipped).
- **Execute 03-02-PLAN.md** (depends on 03-01): read-only Kanban board at new `/pipeline` route, 5 columns with counts, esfriando visual flag (amber border + Clock icon, server-computed), click-card-opens-edit-modal, sidebar nav entry, horizontal scroll.
- **Execute 03-03-PLAN.md** (depends on 03-02): install `@dnd-kit/core` + `@dnd-kit/sortable` (first new npm dependency in this project — **this plan opens with a mandatory, non-autonomous human checkpoint for package-legitimacy approval, cannot be auto-approved**), `updateLeadStage` Server Action (plain async function, called via `startTransition` + `useOptimistic`, NOT the `useActionState` convention), `motivoPerda` capture modal on drop-to-Perdido (optional field).
- Not part of this phase, but still outstanding from Phase 1: `01-04-PLAN.md` (soft-delete + Lixeira page) — user explicitly chose to do Phase 3 first; this was a deliberate deprioritization, not something forgotten.
</remaining_work>

<decisions_made>

- Split "Fechado/Perdido" into two distinct stages (Fechado, Perdido) — reverses Phase 1's locked `D-10` (see `01-CONTEXT.md`, now annotated SUPERSEDED). Board has 5 columns total.
- The 2 existing leads in `fechado_perdido` migrate to `fechado` by default (confirmed via live DB query during discussion — negligible migration risk at this volume).
- New `motivoPerda` field (nullable text) — optional prompt when a card is dragged to Perdido, does not block the move.
- "Esfriando" = 5+ days since last STAGE CHANGE (not last edit), computed server-side, flags only cards currently in Contatado.
- Board is unfiltered for v1 (no sub-nicho/follow-up filters), shows lead COUNT only per column (no value roll-up — stays `PIPE-V2-01`), new dedicated `/pipeline` route with its own sidebar nav item.
- `updateLeadStage` Server Action breaks from the `createLead`/`updateLead` `useActionState`/FormData convention — it's a plain async function invoked directly from `onDragEnd` via `startTransition` + `useOptimistic`, per RESEARCH.md's verified React 19 pattern.
- `stageChangedAt` column must be added nullable with NO default (SQLite rejects non-constant `ADD COLUMN` defaults on tables with existing rows) and backfilled via a hand-written custom migration — do NOT copy the `createdAt`/`updatedAt` `sql\`(unixepoch())\`` default pattern for this column.
</decisions_made>

<blockers>
None currently — Phase 3 is fully unblocked and ready for `/gsd-execute-phase 3`. The only known future gate is the human package-legitimacy checkpoint inside 03-03 (see Human Actions Pending below), which is expected and by design, not an error state.
</blockers>

## Required Reading (in order)
1. `.planning/phases/03-sales-pipeline-funnel-view/03-CONTEXT.md` — all 15 locked decisions (D-01 through D-15), read this FIRST
2. `.planning/phases/03-sales-pipeline-funnel-view/03-01-PLAN.md`, `03-02-PLAN.md`, `03-03-PLAN.md` — the 3 plans to execute, in order
3. `.planning/phases/03-sales-pipeline-funnel-view/03-RESEARCH.md` — dnd-kit setup, SQLite enum-migration pitfalls, esfriando computation pattern (Open Questions section now marked RESOLVED)
4. `.planning/phases/03-sales-pipeline-funnel-view/03-UI-SPEC.md` — approved visual/interaction contract
5. `.planning/phases/03-sales-pipeline-funnel-view/03-PATTERNS.md` — analog files/code excerpts for each new/modified file

## Human Actions Pending

- **[BLOCKING for 03-03 only]** Approve `@dnd-kit/core` + `@dnd-kit/sortable` package legitimacy when the checkpoint is reached mid-execution — this is the project's first new npm dependency since scaffold, so it cannot be auto-approved.
- **[non-blocking]** A real `npm run dev` click-through of the finished board/drag-and-drop is still recommended once 03-03 completes — every phase so far (1 and now 3) has been verified headlessly (tsc/build/synthetic scripts) with no actual browser session in this environment.

## Infrastructure State
- No running dev server, no background processes.
- SQLite DB at `./data/crm.db` currently has 2 leads in `fechado_perdido` (pre-migration state) — expect these to become `fechado` after 03-01 executes.
- Git: on branch `worktree-agent-ad346cc0697623e0c`, all work through this pause committed (`e3e00d5` is HEAD at pause time).

<context>
This was a long single session: started with two small UI quick-fixes, then the user asked to jump straight to Phase 3 (Pipeline/Funnel board) instead of finishing Phase 1's remaining 01-04 (soft-delete). During Phase 3's discussion, the user raised (again) wanting to split Fechado/Perdido — this had already been captured as a backlog todo earlier in the session and was explicitly folded into Phase 3's scope with full buy-in on the schema/migration implications. The UI-safety gate initially blocked plan-phase because no UI-SPEC existed; ran ui-phase first, which passed cleanly. Research and pattern-mapping were both run before planning per user's explicit choice (dnd-kit is new to this codebase). The planner hit a real API disconnect mid-run — this was diagnosed via the filesystem-fallback pattern from the GSD workflow doc (check disk before assuming failure), recovered by resuming the planner with explicit context about what 03-01 already covered, and then a cleanup pass caught that 03-01-PLAN.md/03-PATTERNS.md were never actually git-committed and that the ROADMAP had a premature [x] on 03-01. Everything is now consistent and committed. The user is stopping for the night and wants to resume tomorrow exactly where this leaves off — that's this handoff.
</context>

<next_action>
Start with: `/gsd-execute-phase 3` — this will execute 03-01 (wave 1), then 03-02 (wave 2), then 03-03 (wave 3, expect the human checkpoint for @dnd-kit approval partway through). Read 03-CONTEXT.md first if resuming in a fresh context window with no memory of this session's discussion.
</next_action>
