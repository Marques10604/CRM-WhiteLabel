---
phase: 13-rename-sub-nicho-nicho-reframe
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - src/app/layout.tsx
  - src/components/lead-form-dialog.tsx
  - src/components/template-form-dialog.tsx
  - src/app/nichos/page.tsx
  - src/components/nicho-manager.tsx
  - src/components/nicho-combobox.tsx
  - src/components/delete-nicho-dialog.tsx
  - src/components/lead-table-toolbar.tsx
  - src/components/csv-column-mapper.tsx
  - src/components/csv-import-wizard.tsx
  - scripts/test-relatorios-queries.cjs
  - scripts/test-lead-actions.cjs
autonomous: true
requirements: [COPY-01]

must_haves:
  truths:
    - "Nenhum label, placeholder, texto de ajuda, exemplo, título de página ou estado vazio visível no app menciona \"sub-nicho\"/\"Sub-nicho\", \"área da saúde\", ou usa \"nutricionista\"/\"terapeuta\" como categoria fixa (COPY-01, D-03)"
    - "O texto de ajuda do campo de nicho no form de lead usa exemplo GENÉRICO: \"Nicho do lead (ex: dentista, e-commerce de roupa, academia).\" (D-04)"
    - "A metadata `description` do `layout.tsx` não diz \"área da saúde\" (D-05)"
    - "Gate de grep: `grep -rniE \"sub-?nicho|área da saúde|nutricionista|terapeuta\" src/` não retorna nenhuma STRING de UI (só nomes físicos de banco e o doc-comment de divergência do schema)"
    - "`npx tsc --noEmit` e `npm run build` continuam exit 0"
    - "Os harnesses `.cjs` que referenciavam `subnicho` como identificador de função/import foram atualizados; `INSERT INTO subnichos` FICA (nome físico); todos os `npm run test:*` da fase saem exit 0"
  artifacts:
    - path: "src/components/lead-form-dialog.tsx"
      provides: "Campo de nicho com texto de ajuda genérico"
      contains: "e-commerce de roupa"
    - path: "src/app/layout.tsx"
      provides: "Metadata sem 'área da saúde'"
      contains: "description:"
  key_links:
    - from: "grep gate"
      to: "src/"
      via: "grep -rniE 'sub-?nicho|área da saúde|nutricionista|terapeuta' src/ (só nomes físicos)"
      pattern: "sub-?nicho"
---

<objective>
Onda 3 do rename (D-09): a **varredura de copy**. Materializa COPY-01 — nenhuma string visível ao usuário menciona "sub-nicho" ou "área da saúde", e o texto de ajuda do campo de nicho ganha um exemplo genérico (D-04). Fecha com um gate de grep provando que só sobraram os nomes FÍSICOS de banco (`subnichos`, `subnicho_id`, os nomes de índice) e o doc-comment do `schema.ts` que explica a divergência.

Pré-condição: `13-02-SUMMARY.md` traz a lista exata das strings "Sub-nicho"/"sub-nicho"/"área da saúde" que sobraram das Ondas 1-2 — esta onda executa essa lista + os 3 pontos "saúde" já conhecidos do CONTEXT.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-rename-sub-nicho-nicho-reframe/13-CONTEXT.md
@.planning/phases/13-rename-sub-nicho-nicho-reframe/13-02-SUMMARY.md

@src/app/layout.tsx
@src/components/lead-form-dialog.tsx
@src/components/template-form-dialog.tsx
</context>

<interfaces>
Pontos "saúde" confirmados no scout do discuss (13-CONTEXT §D-03):
- `src/app/layout.tsx:19` — `description: "CRM pessoal para organizar leads da área da saúde"`
- `src/components/lead-form-dialog.tsx:339` — `Categoria do lead (ex: nutricionista, terapeuta).`
- `src/components/template-form-dialog.tsx:129` — `placeholder="Ex: Primeiro contato — nutricionista"`

Strings "Sub-nicho"/"sub-nicho" visíveis: virão do `13-02-SUMMARY.md` (labels de coluna, títulos de página, placeholders de filtro, `<h1>` da tela de gestão, estados vazios, labels de mapeamento do wizard de CSV).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: os 3 pontos "saúde" + textos de ajuda</name>
  <files>src/app/layout.tsx, src/components/lead-form-dialog.tsx, src/components/template-form-dialog.tsx</files>
  <read_first>
    - `src/app/layout.tsx` (a `metadata` — `title` e `description`)
    - `src/components/lead-form-dialog.tsx` (~L330-345 — o `FieldDescription` do campo de nicho)
    - `src/components/template-form-dialog.tsx` (~L125-135 — o `placeholder` do campo nome)
    - `.planning/phases/13-rename-sub-nicho-nicho-reframe/13-CONTEXT.md` §D-04/D-05
  </read_first>
  <action>
    `src/app/layout.tsx`:
    - `description: "CRM pessoal para organizar leads da área da saúde"` → `description: "CRM pessoal para organizar leads e o funil de vendas."` (D-05 — sem nicho específico). Conferir o `title` também; se disser "área da saúde", generalizar (ex: "CRM de Leads").

    `src/components/lead-form-dialog.tsx`:
    - `<FieldDescription>Categoria do lead (ex: nutricionista, terapeuta).</FieldDescription>` → `<FieldDescription>Nicho do lead (ex: dentista, e-commerce de roupa, academia).</FieldDescription>` (D-04)
    - Qualquer label "Sub-nicho" do campo → "Nicho"

    `src/components/template-form-dialog.tsx`:
    - `placeholder="Ex: Primeiro contato — nutricionista"` → `placeholder="Ex: Primeiro contato — dentista"` (exemplo genérico, mantém o formato)
  </action>
  <verify>
    <automated>grep -rniE "área da saúde|nutricionista|terapeuta" src/ || echo "sem copy de saude"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rniE "área da saúde|nutricionista|terapeuta" src/` retorna VAZIO
    - `src/components/lead-form-dialog.tsx` contém `e-commerce de roupa` e `academia`
    - `src/app/layout.tsx` `description` não contém "saúde"
  </acceptance_criteria>
  <done>Zero referência a "área da saúde"/"nutricionista"/"terapeuta"; texto de ajuda com exemplo genérico.</done>
</task>

<task type="auto">
  <name>Task 2: varredura de "sub-nicho"/"Sub-nicho" visível + gate de grep</name>
  <files>src/app/nichos/page.tsx, src/components/nicho-manager.tsx, src/components/nicho-combobox.tsx, src/components/delete-nicho-dialog.tsx, src/components/lead-table-toolbar.tsx, src/components/csv-column-mapper.tsx, src/components/csv-import-wizard.tsx, src/components/lead-table-columns.tsx</files>
  <read_first>
    - `.planning/phases/13-rename-sub-nicho-nicho-reframe/13-02-SUMMARY.md` — §"strings de UI que sobraram" (a lista exata)
    - Saída de `grep -rniE "sub-?nicho" src/` no estado atual (rodar antes de editar)
    - Cada arquivo da lista `<files>` no ponto da string
  </read_first>
  <action>
    Rodar `grep -rniE "sub-?nicho" src/` e, para CADA ocorrência que seja uma STRING renderizada ao usuário (JSX text, `label=`, `placeholder=`, `title=`, `aria-label=`, `<h1>`/`<h2>`, texto de estado vazio, `FieldLabel`, `FieldDescription`, texto de `toast`), trocar:
    - "Sub-nicho" → "Nicho" · "Sub-nichos" → "Nichos" · "sub-nicho" → "nicho" · "sub-nichos" → "nichos"
    - Manter a capitalização do contexto (início de frase = "Nicho", meio de frase = "nicho")
    Pontos prováveis (confirmar com o SUMMARY da Onda 2):
    - `src/app/nichos/page.tsx` — `<h1>` / título da página / `<title>` metadata
    - `src/components/nicho-manager.tsx` — labels, placeholder do input de novo nicho, estado vazio ("Nenhum sub-nicho cadastrado" → "Nenhum nicho cadastrado")
    - `src/components/nicho-combobox.tsx` — placeholder ("Selecione um sub-nicho" → "Selecione um nicho"), texto do "criar novo"
    - `src/components/delete-nicho-dialog.tsx` — corpo do dialog de confirmação
    - `src/components/lead-table-toolbar.tsx` — label/placeholder do filtro de nicho
    - `src/components/csv-column-mapper.tsx` / `csv-import-wizard.tsx` — label do campo mapeável ("Sub-nicho" → "Nicho"), texto de ajuda do passo
    - `src/components/lead-table-columns.tsx` — header da coluna ("Sub-nicho" → "Nicho")

    **NÃO** trocar: `"subnichos"` / `"subnicho_id"` / `"subnicho_nome_unique_idx"` etc. quando são nome FÍSICO de tabela/coluna/índice (aparecem em `schema.ts`, no doc-comment de divergência, em `verify-schema.cjs`, e em `INSERT INTO subnichos` nos harnesses).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -rniE "sub-?nicho" src/ | grep -vE "subnicho_id|subnichos_deleted_at_idx|subnicho_nome_unique_idx|leads_subnicho_id_idx|sqliteTable\(.subnichos|física|D-01|divergência" || echo "GATE OK: só nomes físicos e o doc-comment restam"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exit 0
    - O grep filtrado acima imprime `GATE OK` (nenhuma string de UI com "sub-nicho" resta; o que sobra é `sqliteTable("subnichos", ...)`, `integer("subnicho_id")`, os 3 nomes de índice string, e o doc-comment do `schema.ts` que contém "física"/"D-01"/"divergência")
    - `src/components/nicho-manager.tsx` estado vazio diz "Nenhum nicho cadastrado" (ou equivalente sem "sub")
    - `src/components/nicho-combobox.tsx` placeholder diz "Selecione um nicho" (ou equivalente)
  </acceptance_criteria>
  <done>Nenhuma string visível diz "sub-nicho"; só os nomes físicos de banco + o doc-comment de divergência restam.</done>
</task>

<task type="auto">
  <name>Task 3: harnesses .cjs + suíte completa da fase</name>
  <files>scripts/test-relatorios-queries.cjs, scripts/test-lead-actions.cjs</files>
  <read_first>
    - `scripts/test-relatorios-queries.cjs` (L210 `INSERT INTO subnichos`, L292-295 `getContagemPorSubnicho` / "A categorizar")
    - `scripts/test-lead-actions.cjs` (qualquer `subnicho` — identificador de função importada vs. SQL de setup)
    - `package.json` (os scripts `test:*` / `verify:*` / `guard:*` da fase)
  </read_first>
  <action>
    Nos 2 harnesses:
    - Identificador de função importada / referenciada: `getContagemPorSubnicho` → `getContagemPorNicho`, `createSubnicho`/`softDeleteSubnicho`/`renameSubnicho` → `*Nicho`, require de `subnicho-actions` → `nicho-actions`
    - SQL de setup: `INSERT INTO subnichos (...)`, `CREATE TABLE subnichos (...)` → **FICAM** (nome físico da tabela). `DELETE FROM subnichos` em setup de `:memory:` também fica.
    - Nomes de variável JS locais (`subnichoId`, `porSubnicho`) → `nicho*` por consistência (opcional se não quebrar).
    - Strings de asserção/log que dizem "subnicho" como conceito → "nicho".

    Rodar a suíte completa da fase (ver <verification>). Se algum gate quebrar por identificador não migrado nas Ondas 1-2, corrigir no arquivo-fonte correspondente e re-rodar.
  </action>
  <verify>
    <automated>npm run test:relatorios</automated>
    <automated>npm run test:lead-actions</automated>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/test-relatorios-queries.cjs` contém `getContagemPorNicho` e ainda contém `INSERT INTO subnichos` (nome físico)
    - `grep -n "getContagemPorSubnicho\|require.*subnicho-actions\|createSubnicho" scripts/` retorna VAZIO
    - `npm run test:relatorios` → exit 0 · `npm run test:lead-actions` → exit 0
    - `npx tsc --noEmit` → exit 0 · `npm run build` → exit 0, `/nichos` gerada
    - `npm run verify:schema` → exit 0 · `npm run guard:no-hard-delete` → exit 0
  </acceptance_criteria>
  <done>Harnesses falam `nicho` no nível de identificador, `subnichos` no nível de SQL físico; suíte completa da Fase 13 verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| string de UI vs. nome físico de banco | O gate de grep precisa distinguir "Sub-nicho" (label, trocar) de `"subnichos"` (tabela, manter) — risco de trocar o nome físico e quebrar o banco |
| texto renderizado do usuário → DOM | Copy nova ("dentista, e-commerce de roupa, academia") é texto estático, sem injeção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | Tampering | grep/sed cego trocando `"subnichos"` (nome físico) por engano | mitigate | Task 2 e Task 3 são EXPLÍCITOS sobre o que NÃO trocar (lista de nomes físicos). O gate de grep filtrado (`grep -vE "subnicho_id|..."`) permite só os nomes físicos + o doc-comment. `verify:schema` roda ao fim e falha se a tabela física sumir. `npm run build` prova que o banco ainda responde. |
| T-13-08 | Denial of Service | `npm run build` quebra por string órfã não-migrada da Onda 2 | mitigate | Task 3 roda a suíte completa; qualquer quebra manda corrigir a causa raiz no fonte. `13-02-SUMMARY.md` é o roteiro das strings pendentes. |
| T-13-SC | Tampering | supply chain | accept | Rename de copy puro. |

</threat_model>

<verification>
Suíte completa da Fase 13 — sequência (host 4GB, dev server parado), todos exit 0:

1. `npx tsc --noEmit`
2. `npm run build` (log lista `/nichos`, não lista `/subnichos`)
3. `npm run verify:schema` (banco físico intacto)
4. `npm run guard:no-hard-delete` (protege `nichos`)
5. `npm run test:lead-actions`
6. `npm run test:relatorios`
7. `npm run test:tarefa-actions` · `npm run test:group-by-urgency` · `npm run test:compute-sequencia` (regressão geral — nada de nicho ali, devem passar intocados)
8. Gate de grep COPY-01: `grep -rniE "sub-?nicho|área da saúde|nutricionista|terapeuta" src/ | grep -vE "subnicho_id|subnichos_deleted_at_idx|subnicho_nome_unique_idx|leads_subnicho_id_idx|sqliteTable\(.subnichos|física|D-01|divergência"` → VAZIO

<human-check>
Com `npm run dev` (porta 3000/3001), no navegador:
1. Menu lateral: o item diz "Nichos" (não "Sub-nichos"), aponta pra `/nichos`.
2. Abrir `/subnichos` na barra de endereço → redireciona pra `/nichos` (301).
3. `/nichos`: título "Nichos", lista os nichos atuais dos 37 leads (nenhum sumiu), dá pra criar/renomear/remover/reativar igual antes.
4. Novo lead: o campo de categoria diz "Nicho", o texto de ajuda diz "Nicho do lead (ex: dentista, e-commerce de roupa, academia)."; o combobox filtra e cria nicho.
5. Lista `/leads`: a coluna diz "Nicho", o filtro da toolbar diz "Nicho", filtrar por um nicho funciona.
6. `/relatorios`: a seção "Leads por nicho" (era "por sub-nicho") mostra os mesmos números; "A categorizar" continua sendo uma linha normal.
7. Wizard de importação de CSV: o passo de mapeamento oferece mapear uma coluna pra "Nicho".
8. Nenhuma tela mostra "sub-nicho", "área da saúde", "nutricionista" ou "terapeuta".
</human-check>
</verification>

<success_criteria>
- COPY-01 satisfeito: nenhuma string de UI diz "sub-nicho"/"área da saúde"/"nutricionista"/"terapeuta"
- Texto de ajuda do campo de nicho com exemplo genérico (D-04)
- Metadata do `layout.tsx` sem nicho específico (D-05)
- Gate de grep passa (só nomes físicos + doc-comment restam)
- Suíte completa da Fase 13 verde, incluindo `npm run build`
- 37 leads mantêm sua categorização (nenhum nicho perdido)
</success_criteria>

<output>
Criar `.planning/phases/13-rename-sub-nicho-nicho-reframe/13-03-SUMMARY.md`: a lista de strings de UI trocadas (arquivo:linha antes→depois), a saída do gate de grep final, a tabela de gates (comando → exit code), evidência dos 3 success criteria (NICHO-01: rename completo; NICHO-02: `/nichos` CRUD + redirect; COPY-01: grep gate), e o resultado item a item do `<human-check>` se rodado.
</output>
