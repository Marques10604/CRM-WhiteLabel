---
phase: 13-rename-sub-nicho-nicho-reframe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/types/index.ts
  - src/lib/validations.ts
  - src/db/queries.ts
  - src/lib/csv-import.ts
  - src/actions/subnicho-actions.ts
  - scripts/guard-no-hard-delete.cjs
autonomous: true
requirements: [NICHO-01]

must_haves:
  truths:
    - "O export Drizzle chama `nichos` e a prop da FK do lead chama `nichoId`, mas os nomes FÍSICOS (`subnichos`, `subnicho_id`, os 3 índices) NÃO mudam (D-01) — zero migração, zero toque em data/crm.db"
    - "Um doc-comment no `schema.ts` explica a divergência intencional nome-lógico↔nome-físico"
    - "Os tipos `Subnicho`/`NewSubnicho` viram `Nicho`/`NewNicho`; `subnichoSchema` vira `nichoSchema`; os campos `subnichoId`/`subnichoNome` viram `nichoId`/`nichoNome` em todo `validations.ts`"
    - "`src/actions/subnicho-actions.ts` é renomeado para `src/actions/nicho-actions.ts` e as 3 funções viram `createNicho`/`softDeleteNicho`/`renameNicho`"
    - "`getContagemPorSubnicho` vira `getContagemPorNicho` em `queries.ts`"
    - "`CsvFieldKey` `\"subnichoNome\"` vira `\"nichoNome\"` em `csv-import.ts`"
    - "O `guard-no-hard-delete.cjs` protege a tabela renomeada: `CODE_PATTERNS` passa a casar `.delete(nichos` (o objeto Drizzle mudou); `CODE_SQL_PATTERNS` (`DELETE FROM subnichos`, `DROP TABLE subnichos`) FICAM (nome físico não mudou)"
    - "`npx tsc --noEmit` sai exit 0; `npm run verify:schema` sai exit 0 SEM mudança no script (verifica o banco físico, que não muda); `npm run guard:no-hard-delete` sai exit 0"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "export `nichos = sqliteTable(\"subnichos\", ...)` + `leads.nichoId: integer(\"subnicho_id\")` + doc-comment da divergência"
      contains: "sqliteTable(\"subnichos\""
    - path: "src/actions/nicho-actions.ts"
      provides: "createNicho / softDeleteNicho / renameNicho (movido de subnicho-actions.ts)"
      contains: "export async function createNicho"
      min_lines: 90
    - path: "src/types/index.ts"
      provides: "type Nicho / NewNicho"
      contains: "export type Nicho ="
  key_links:
    - from: "src/db/schema.ts"
      to: "src/db/schema.ts (leads)"
      via: "leads.nichoId .references(() => nichos.id, { onDelete: \"restrict\" })"
      pattern: "nichos.id"
    - from: "src/actions/nicho-actions.ts"
      to: "src/db/schema.ts"
      via: "import { nichos }"
      pattern: "import { nichos }"
    - from: "scripts/guard-no-hard-delete.cjs"
      to: "src/actions/*"
      via: "CODE_PATTERNS /\\.delete\\(\\s*nichos\\b/"
      pattern: "nichos"
---

<objective>
Onda 1 do rename `sub-nicho → nicho` (D-07): a **camada de dados**. O identificador lógico `subnicho`/`Subnicho` some do código de schema, tipos, validação, queries, contrato de CSV e Server Actions — vira `nicho`/`Nicho`. Os **nomes físicos do banco ficam intocados** (`subnichos`, `subnicho_id`, `subnicho_nome_unique_idx`, `subnichos_deleted_at_idx`, `leads_subnicho_id_idx`) por D-01 — sem migração, sem backup, sem esbarrar no snapshot divergente do `drizzle-kit`.

As superfícies de UI que consomem esses símbolos quebram temporariamente até a Onda 2 (13-02) — é esperado; `tsc` vai reclamar dos imports de UI, mas os arquivos DESTE plano compilam entre si e os 3 gates (`tsc` escopado mentalmente / `verify:schema` / `guard`) passam quando considerados isoladamente. O executor roda os gates ao fim; se `tsc` acusar SÓ erros nas superfícies de UI ainda não migradas, é OK — documentar no SUMMARY e seguir. Se acusar erro DENTRO dos 7 arquivos deste plano, corrigir.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-rename-sub-nicho-nicho-reframe/13-CONTEXT.md

@src/db/schema.ts
@src/types/index.ts
@src/lib/validations.ts
@src/db/queries.ts
@src/lib/csv-import.ts
@src/actions/subnicho-actions.ts
@scripts/guard-no-hard-delete.cjs
</context>

<interfaces>
<!-- Símbolos que ESTE plano renomeia. A Onda 2 (13-02) consome os nomes NOVOS. -->

Renomeados (antigo → novo):
- Drizzle export: `subnichos` → `nichos` (tabela física continua `"subnichos"`)
- Prop da FK: `leads.subnichoId` → `leads.nichoId` (coluna física continua `"subnicho_id"`)
- Tipos: `Subnicho` → `Nicho`, `NewSubnicho` → `NewNicho`
- Zod: `subnichoSchema` → `nichoSchema`; campos `subnichoId` → `nichoId`, `subnichoNome` → `nichoNome`
- Mensagens Zod: "Selecione um sub-nicho." → "Selecione um nicho.", "Sub-nicho é obrigatório." → "Nicho é obrigatório."
- Actions: arquivo `src/actions/subnicho-actions.ts` → `src/actions/nicho-actions.ts`; `createSubnicho` → `createNicho`, `softDeleteSubnicho(subnichoId)` → `softDeleteNicho(nichoId)`, `renameSubnicho` → `renameNicho`
- Query: `getContagemPorSubnicho` → `getContagemPorNicho`
- CSV: `CsvFieldKey` `"subnichoNome"` → `"nichoNome"`; campo `subnichoNome` da row type → `nichoNome`

NÃO renomeados (nomes físicos / strings de banco):
- `sqliteTable("subnichos", ...)`, `integer("subnicho_id")`
- Nomes de índice: `"subnicho_nome_unique_idx"`, `"subnichos_deleted_at_idx"`, `"leads_subnicho_id_idx"` (permanecem como string; só o `.on(table.subnichoId)` vira `.on(table.nichoId)`)
- `scripts/verify-schema.cjs` — `requiredTables` inclui `"subnichos"`, checa `subnicho_nome_unique_idx` — **nenhuma mudança**
- `CODE_SQL_PATTERNS` do guard: `DELETE FROM subnichos`, `DROP TABLE subnichos` — **ficam**
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: schema.ts + types/index.ts</name>
  <files>src/db/schema.ts, src/types/index.ts</files>
  <read_first>
    - `src/db/schema.ts` (arquivo inteiro — o `subnichos` L4-17, o doc-comment de `motivosPerda` L33-49 como molde de estilo de doc-comment, `leads.subnichoId` L77, `index("leads_subnicho_id_idx")` L98)
    - `src/types/index.ts` (L2 import, L7-8 os tipos)
    - `.planning/phases/13-rename-sub-nicho-nicho-reframe/13-CONTEXT.md` §D-01
  </read_first>
  <action>
    Em `src/db/schema.ts`:
    - Renomear o `export const subnichos` para `export const nichos`, MANTENDO `sqliteTable("subnichos", { ... })` e os 3 campos idênticos. Manter os nomes de índice string `"subnicho_nome_unique_idx"` e `"subnichos_deleted_at_idx"` — só os identificadores JS mudam onde aplicável (o `.on(sql\`lower(trim(${table.nome}))\`)` e `.on(table.deletedAt)` não referenciam `subnicho`).
    - Adicionar um doc-comment `/** ... */` acima de `export const nichos` explicando: "Export lógico `nichos`, tabela FÍSICA `subnichos` — divergência DELIBERADA (Fase 13, D-01). O rename `sub-nicho → nicho` acontece só na camada de código/UI; renomear a tabela/coluna no banco não traz benefício funcional (o usuário nunca vê o nome físico) e esbarraria no snapshot divergente do drizzle-kit. Mesmo racional dos nomes de índice, que continuam com o prefixo `subnicho`." (redigir no estilo dos doc-comments existentes de `motivosPerda` / coluna morta `motivo_perda`).
    - Em `leads`: `subnichoId: integer("subnicho_id").notNull().references(() => subnichos.id, ...)` → `nichoId: integer("subnicho_id").notNull().references(() => nichos.id, { onDelete: "restrict" })`. Manter `"subnicho_id"` como nome de coluna.
    - `index("leads_subnicho_id_idx").on(table.subnichoId)` → `index("leads_subnicho_id_idx").on(table.nichoId)` — nome do índice string FICA, o `.on()` passa a referenciar `table.nichoId`.
    - Atualizar a referência textual `mesma ordem \`subnichos\` → \`leads\`` no doc-comment de `motivosPerda` para `\`nichos\` → \`leads\`` (opcional, coerência).

    Em `src/types/index.ts`:
    - Import L2: `subnichos` → `nichos`
    - `export type Subnicho = InferSelectModel<typeof subnichos>` → `export type Nicho = InferSelectModel<typeof nichos>`
    - `export type NewSubnicho = InferInsertModel<typeof subnichos>` → `export type NewNicho = InferInsertModel<typeof nichos>`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "schema\.ts|types/index\.ts" || echo "no errors in schema/types"</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/schema.ts` contém `export const nichos = sqliteTable(` e `sqliteTable("subnichos"` (nome físico preservado) e `integer("subnicho_id")` e `nichoId:`
    - `src/db/schema.ts` contém um doc-comment mencionando "física" e "D-01" acima do `export const nichos`
    - `src/db/schema.ts` NÃO contém mais `export const subnichos` nem `subnichoId:` (a prop)
    - `src/types/index.ts` contém `export type Nicho =` e `export type NewNicho =`, NÃO contém `Subnicho`
    - `grep -c '"subnicho_nome_unique_idx"\|"subnichos_deleted_at_idx"\|"leads_subnicho_id_idx"' src/db/schema.ts` == 3 (nomes de índice intactos)
    - `npx tsc --noEmit` não acusa nenhum erro DENTRO de `schema.ts` ou `types/index.ts` (erros em arquivos de UI ainda não migrados são esperados nesta onda)
  </acceptance_criteria>
  <done>O schema e os tipos falam `nicho`; o banco físico segue `subnichos`/`subnicho_id`; doc-comment registra a divergência.</done>
</task>

<task type="auto">
  <name>Task 2: validations.ts + queries.ts + csv-import.ts</name>
  <files>src/lib/validations.ts, src/db/queries.ts, src/lib/csv-import.ts</files>
  <read_first>
    - `src/lib/validations.ts` (L6 `subnichoSchema`, L12/L219 comentários, L63 `subnichoId` + msg, L85/L104/L106 `subnichoId`/`subnichoNome` + msg)
    - `src/db/queries.ts` (L369 `getContagemPorSubnicho` e todo o corpo da função; qualquer `from(subnichos)` / `subnichos.` no arquivo)
    - `src/lib/csv-import.ts` (L16 union `"subnichoNome"`, L37 row type, L47-48 comentário, L130/L143 uso)
  </read_first>
  <action>
    `src/lib/validations.ts`:
    - `export const subnichoSchema` → `export const nichoSchema` (corpo idêntico)
    - comentários "espelho exato de `subnichoSchema`" / "`motivoPerdaSchema`/`subnichoSchema`" → `nichoSchema`
    - `subnichoId: z.coerce.number()...("Selecione um sub-nicho.")` → `nichoId: z.coerce.number()...("Selecione um nicho.")`
    - `.omit({ subnichoId: true, ... })` → `.omit({ nichoId: true, ... })`
    - `subnichoNome: z.string()...("Sub-nicho é obrigatório.")` → `nichoNome: z.string()...("Nicho é obrigatório.")`
    - comentário "`subnichoId` é ..." → "`nichoId` é ..."

    `src/db/queries.ts`:
    - `export async function getContagemPorSubnicho` → `getContagemPorNicho` (corpo idêntico; o `from(subnichos)` interno vira `from(nichos)` — importar `nichos` do schema)
    - Qualquer outro `subnichos`/`subnichoId` no arquivo → `nichos`/`nichoId` (ex: joins em `getActiveDashboardLeads`, `getLeadsForPipeline`, etc. — varrer o arquivo inteiro)
    - Import do schema no topo: `subnichos` → `nichos`

    `src/lib/csv-import.ts`:
    - `CsvFieldKey` `| "subnichoNome"` → `| "nichoNome"`
    - row type `subnichoNome: string` → `nichoNome: string`
    - `readMapped(row, "subnichoNome")` → `readMapped(row, "nichoNome")`, `const subnichoNome` → `const nichoNome`, e o campo no objeto de retorno
    - comentário sobre `subnichoNome` NUNCA recebe default → `nichoNome`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "validations\.ts|queries\.ts|csv-import\.ts" || echo "no errors in data-layer files"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "subnicho\|Subnicho" src/lib/validations.ts src/db/queries.ts src/lib/csv-import.ts` retorna VAZIO
    - `src/lib/validations.ts` contém `export const nichoSchema`, `nichoId:`, `nichoNome:`, `"Selecione um nicho."`, `"Nicho é obrigatório."`
    - `src/db/queries.ts` contém `export async function getContagemPorNicho` e `import { ... nichos ... } from` (ou equivalente)
    - `src/lib/csv-import.ts` contém `"nichoNome"` e NÃO contém `"subnichoNome"`
    - `npx tsc --noEmit` sem erro DENTRO desses 3 arquivos
  </acceptance_criteria>
  <done>Validação, queries e contrato de CSV falam `nicho`.</done>
</task>

<task type="auto">
  <name>Task 3: mover subnicho-actions.ts → nicho-actions.ts + atualizar o guard</name>
  <files>src/actions/subnicho-actions.ts, scripts/guard-no-hard-delete.cjs</files>
  <read_first>
    - `src/actions/subnicho-actions.ts` (arquivo inteiro — `createSubnicho` L14, `softDeleteSubnicho(subnichoId)` L65, `renameSubnicho` L85; os `db.update(subnichos)` L34/L109; o import do schema)
    - `scripts/guard-no-hard-delete.cjs` (L13-14/L26/L66 comentários, L71-76 `CODE_PATTERNS`, L78-91 `CODE_SQL_PATTERNS`, L176 string de OK)
    - `.planning/phases/13-rename-sub-nicho-nicho-reframe/13-CONTEXT.md` §D-07 (bullet do guard)
  </read_first>
  <action>
    **Renomear o arquivo** `src/actions/subnicho-actions.ts` → `src/actions/nicho-actions.ts` (git mv preferível; se o executor não puder, criar o novo e deletar o antigo). Dentro dele:
    - `createSubnicho` → `createNicho`
    - `softDeleteSubnicho(subnichoId: number)` → `softDeleteNicho(nichoId: number)` (e usos internos de `subnichoId` → `nichoId`)
    - `renameSubnicho` → `renameNicho`
    - `import { subnichos }` → `import { nichos }`; todos os `db.update(subnichos)` → `db.update(nichos)`, `eq(subnichos.id, ...)` → `eq(nichos.id, ...)`, `subnichos.nome` → `nichos.nome`
    - mensagens de toast/erro visíveis com "sub-nicho" → "nicho" (se houver — a varredura de copy final é a Onda 3, mas trocar aqui já ajuda)

    Em `scripts/guard-no-hard-delete.cjs`:
    - `CODE_PATTERNS`: `/\.delete\(\s*subnichos\b/` → `/\.delete\(\s*nichos\b/` (o objeto Drizzle agora é `nichos`; sem isso, `db.delete(nichos)` escaparia da guarda)
    - `CODE_SQL_PATTERNS`: `/\bDELETE\s+FROM\s+[\`"']?subnichos\b/i` e `/\bDROP\s+TABLE\s+[\`"']?subnichos\b/i` **FICAM** (a tabela física continua `subnichos`)
    - Comentários L13-14/L26/L66 e a string de OK L176: trocar "subnichos"/"sub-nicho" por "nichos"/"nicho" onde se refere ao OBJETO/conceito; onde cita a string SQL física (`DELETE FROM subnichos`), manter e adicionar nota `(nome físico)`.
    - Adicionar um comentário curto explicando a assimetria: `CODE_PATTERNS` casa o objeto Drizzle (mudou p/ `nichos`), `CODE_SQL_PATTERNS` casa o nome físico da tabela (continua `subnichos`).
  </action>
  <verify>
    <automated>test -f src/actions/nicho-actions.ts && ! test -f src/actions/subnicho-actions.ts && echo "moved OK"</automated>
    <automated>npm run guard:no-hard-delete</automated>
  </verify>
  <acceptance_criteria>
    - `src/actions/nicho-actions.ts` existe; `src/actions/subnicho-actions.ts` NÃO existe
    - `grep -c "export async function createNicho\|softDeleteNicho\|renameNicho" src/actions/nicho-actions.ts` == 3
    - `grep -n "subnicho\|Subnicho" src/actions/nicho-actions.ts` retorna VAZIO
    - `scripts/guard-no-hard-delete.cjs` contém `/\.delete\(\s*nichos\b/` e ainda contém `DELETE\s+FROM\s+[` ... `subnichos` (SQL pattern físico preservado)
    - `npm run guard:no-hard-delete` sai exit 0
  </acceptance_criteria>
  <done>As Server Actions de nicho estão em `nicho-actions.ts`; o guard protege a tabela renomeada e mantém o padrão SQL do nome físico.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| identificador Drizzle (`nichos`) ↔ nome físico (`subnichos`) | Divergência deliberada; risco de alguém "consertar" achando que é bug |
| `guard-no-hard-delete.cjs` → hard-delete de `nichos` | O pattern do guard casa o nome do objeto Drizzle; renomear o objeto sem renomear o pattern deixa a tabela desprotegida |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | `schema.ts` — alguém renomeia a tabela física achando que a divergência é erro | mitigate | Doc-comment explícito acima de `export const nichos` (Task 1) + esta entrada no threat model. `verify:schema` continua checando `subnichos` e falharia se a tabela física mudasse sem migração. |
| T-13-02 | Denial of Service (perda de dado) | `guard-no-hard-delete.cjs` CODE_PATTERN desatualizado | mitigate | Task 3 atualiza `/\.delete\(\s*subnichos\b/` → `/\.delete\(\s*nichos\b/` NO MESMO commit que renomeia o objeto. Gate `npm run guard:no-hard-delete` roda ao fim. `subnicho-actions.ts`/`nicho-actions.ts` só faz `db.update` (soft-delete), então a exposição real é defensiva. |
| T-13-03 | Tampering | migração acidental de coluna disparada por `drizzle-kit generate` vendo o rename | mitigate | D-01: nenhum comando `drizzle-kit` é executado nesta fase; o `package.json` não tem script `drizzle-kit`. O snapshot divergente (débito das Fases 4/6/7/8) não é tocado. |
| T-13-SC | Tampering | supply chain | accept | Nenhuma dependência npm nova, nenhum bloco de registry. Rename puro. |

</threat_model>

<verification>
Rodar em SEQUÊNCIA (host 4GB, dev server parado):

1. `npx tsc --noEmit` → erros SÓ em superfícies de UI ainda não migradas (esperado nesta onda). ZERO erro dentro dos 7 arquivos deste plano.
2. `npm run verify:schema` → exit 0, SEM edição do script (banco físico intacto)
3. `npm run guard:no-hard-delete` → exit 0
4. `grep -rn "subnicho\|Subnicho" src/db/ src/lib/validations.ts src/lib/csv-import.ts src/types/ src/actions/nicho-actions.ts` → só ocorrências que são STRING de nome físico (`"subnichos"`, `"subnicho_id"`, `"subnicho_nome_unique_idx"` etc.) ou o doc-comment que explica a divergência. Nenhum identificador JS `subnicho`.
5. `git status --porcelain src/actions` → mostra `subnicho-actions.ts` deletado (D) e `nicho-actions.ts` novo (A ou R)
</verification>

<success_criteria>
- Schema/tipos/Zod/queries/CSV-contract/actions falam `nicho`; nomes físicos do banco intocados (D-01)
- Doc-comment no schema explica a divergência lógico↔físico
- `subnicho-actions.ts` → `nicho-actions.ts` com `createNicho`/`softDeleteNicho`/`renameNicho`
- `guard-no-hard-delete.cjs` protege `nichos` (CODE_PATTERN) e mantém o padrão SQL físico
- `verify:schema` e `guard:no-hard-delete` verdes sem mudança no banco
- Nenhuma dependência nova, nenhum comando `drizzle-kit`, nenhum backup necessário
</success_criteria>

<output>
Criar `.planning/phases/13-rename-sub-nicho-nicho-reframe/13-01-SUMMARY.md` registrando: a lista de arquivos de UI que `tsc` acusou como quebrados (input pra Onda 2 / 13-02), se `git mv` foi usado pro rename do arquivo de actions, e qualquer `subnicho` físico que ficou de propósito (com o motivo).
</output>
