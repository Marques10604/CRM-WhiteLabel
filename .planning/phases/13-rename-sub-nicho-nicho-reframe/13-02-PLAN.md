---
phase: 13-rename-sub-nicho-nicho-reframe
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/app/subnichos/page.tsx
  - src/app/nichos/page.tsx
  - next.config.ts
  - src/components/subnicho-combobox.tsx
  - src/components/subnicho-manager.tsx
  - src/components/delete-subnicho-dialog.tsx
  - src/components/app-sidebar.tsx
  - src/components/lead-form-dialog.tsx
  - src/components/lead-table.tsx
  - src/components/lead-table-columns.tsx
  - src/components/lead-table-toolbar.tsx
  - src/components/lixeira-table.tsx
  - src/components/followup-dashboard.tsx
  - src/components/pipeline-board.tsx
  - src/components/pipeline-lead-card.tsx
  - src/components/post-import-lead-list.tsx
  - src/components/whatsapp-preview-dialog.tsx
  - src/components/csv-import-wizard.tsx
  - src/components/csv-import-preview-table.tsx
  - src/components/csv-column-mapper.tsx
  - src/components/motivo-perda-combobox.tsx
  - src/components/motivo-perda-manager.tsx
  - src/components/template-list.tsx
  - src/components/template-form-dialog.tsx
  - src/actions/lead-actions.ts
  - src/actions/import-actions.ts
  - src/actions/motivo-perda-actions.ts
  - src/actions/tarefa-actions.ts
  - src/app/page.tsx
  - src/app/leads/page.tsx
  - src/app/pipeline/page.tsx
  - src/app/lixeira/page.tsx
  - src/app/relatorios/page.tsx
  - src/app/motivos-perda/page.tsx
  - src/app/importar/page.tsx
  - src/app/importar/[batchId]/page.tsx
  - src/hooks/use-first-contact-trigger.ts
  - src/lib/whatsapp.ts
autonomous: true
requirements: [NICHO-01, NICHO-02]

must_haves:
  truths:
    - "A rota `/nichos` (movida de `src/app/subnichos/`) gerencia a lista: criar, renomear, remover com soft-delete, reativar por nome — comportamento idêntico ao que era `/subnichos`"
    - "`/subnichos` faz redirect 301 permanente para `/nichos` (`next.config.ts` `redirects()`) — D-02"
    - "O item do menu lateral diz \"Nichos\" e aponta para `/nichos`"
    - "Os 4 componentes dedicados foram renomeados: `subnicho-combobox.tsx`→`nicho-combobox.tsx`, `subnicho-manager.tsx`→`nicho-manager.tsx`, `delete-subnicho-dialog.tsx`→`delete-nicho-dialog.tsx` (o 4º, o arquivo de actions, já foi na Onda 1)"
    - "Todos os ~15 consumidores importam dos nomes novos (`nichoId`, `Nicho`, `createNicho`, `getContagemPorNicho`, `nichoNome`, `@/components/nicho-*`, `@/actions/nicho-actions`)"
    - "`npx tsc --noEmit` sai exit 0 (zero erro em qualquer arquivo)"
    - "`npm run build` sai exit 0; a rota `/nichos` é gerada; `/subnichos` NÃO aparece na lista de rotas (virou redirect)"
    - "`npm run test:lead-actions` e `npm run test:relatorios` continuam exit 0 (só renames de identificador nos harnesses, se necessário)"
  artifacts:
    - path: "src/app/nichos/page.tsx"
      provides: "Tela de gestão de nichos (movida de src/app/subnichos/page.tsx)"
      contains: "NichoManager"
    - path: "next.config.ts"
      provides: "redirect 301 /subnichos → /nichos"
      contains: "source: \"/subnichos\""
    - path: "src/components/nicho-combobox.tsx"
      provides: "Combobox criável de nicho (movido)"
      contains: "createNicho"
  key_links:
    - from: "next.config.ts"
      to: "/nichos"
      via: "redirects() { source: /subnichos, destination: /nichos, permanent: true }"
      pattern: "permanent: true"
    - from: "src/components/app-sidebar.tsx"
      to: "/nichos"
      via: "{ href: \"/nichos\", label: \"Nichos\" }"
      pattern: "\"/nichos\""
    - from: "src/components/lead-form-dialog.tsx"
      to: "src/components/nicho-combobox.tsx"
      via: "import { NichoCombobox }"
      pattern: "NichoCombobox"
---

<objective>
Onda 2 do rename (D-08): as **superfícies de UI**. A rota `src/app/subnichos/` vira `src/app/nichos/` com um redirect 301 de `/subnichos` (D-02), os 3 componentes dedicados restantes são renomeados (`subnicho-*` → `nicho-*`), o item do sidebar vira "Nichos", e os ~15 arquivos que consomem os símbolos renomeados na Onda 1 passam a importar os nomes novos.

Ao fim desta onda, `npx tsc --noEmit` e `npm run build` devem sair **exit 0** — nenhum `subnicho` como identificador JS/rota/componente em `src/`, só os nomes físicos de banco (tratados na Onda 1) e a copy de UI (Onda 3 / 13-03).
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-rename-sub-nicho-nicho-reframe/13-CONTEXT.md
@.planning/phases/13-rename-sub-nicho-nicho-reframe/13-01-SUMMARY.md

@src/app/subnichos/page.tsx
@next.config.ts
@src/components/subnicho-combobox.tsx
@src/components/subnicho-manager.tsx
@src/components/delete-subnicho-dialog.tsx
@src/components/app-sidebar.tsx
@src/components/lead-form-dialog.tsx
@src/components/lead-table-toolbar.tsx
</context>

<interfaces>
Consome (da Onda 1 / 13-01): `nichos`, `leads.nichoId`, `Nicho`/`NewNicho`, `nichoSchema`, `nichoId`/`nichoNome`, `createNicho`/`softDeleteNicho`/`renameNicho` de `@/actions/nicho-actions`, `getContagemPorNicho`, `CsvFieldKey "nichoNome"`.

Produz: rota `/nichos`, redirect `/subnichos`→`/nichos`, componentes `@/components/nicho-combobox` / `nicho-manager` / `delete-nicho-dialog` (nomes de export: `NichoCombobox`, `NichoManager`, `DeleteNichoDialog`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: rota /nichos + redirect + 3 componentes dedicados</name>
  <files>src/app/subnichos/page.tsx, src/app/nichos/page.tsx, next.config.ts, src/components/subnicho-combobox.tsx, src/components/subnicho-manager.tsx, src/components/delete-subnicho-dialog.tsx, src/components/app-sidebar.tsx</files>
  <read_first>
    - `src/app/subnichos/page.tsx` (arquivo inteiro)
    - `src/components/subnicho-combobox.tsx`, `src/components/subnicho-manager.tsx`, `src/components/delete-subnicho-dialog.tsx` (arquivos inteiros — nomes de export, props, imports)
    - `next.config.ts` (arquivo inteiro — o `turbopack.root` existente; o `redirects()` entra ao lado)
    - `src/components/app-sidebar.tsx` (L20-29 o array de nav)
    - `.planning/phases/13-rename-sub-nicho-nicho-reframe/13-CONTEXT.md` §D-02
  </read_first>
  <action>
    **Mover** `src/app/subnichos/` → `src/app/nichos/` (git mv do diretório; `page.tsx` e qualquer `loading.tsx`/`layout.tsx` junto). Dentro de `nichos/page.tsx`: imports `@/components/subnicho-*` → `@/components/nicho-*`, `Subnicho[]` → `Nicho[]`, `subnichos` (schema/query) → `nichos`, `<h1>Sub-nichos</h1>` / título → "Nichos".

    **Renomear os 3 componentes** (git mv):
    - `src/components/subnicho-combobox.tsx` → `src/components/nicho-combobox.tsx`; export `SubnichoCombobox` → `NichoCombobox`; props `subnichoId`/`subnichos` → `nichoId`/`nichos`; import de `@/actions/subnicho-actions` → `@/actions/nicho-actions`, `createSubnicho` → `createNicho`; qualquer `Subnicho` type → `Nicho`
    - `src/components/subnicho-manager.tsx` → `src/components/nicho-manager.tsx`; export `SubnichoManager` → `NichoManager`; `createSubnicho`/`softDeleteSubnicho`/`renameSubnicho` → `create/softDelete/renameNicho`; import `@/actions/nicho-actions`; `DeleteSubnichoDialog` → `DeleteNichoDialog` (import `@/components/nicho-manager`? não — `@/components/delete-nicho-dialog`)
    - `src/components/delete-subnicho-dialog.tsx` → `src/components/delete-nicho-dialog.tsx`; export `DeleteSubnichoDialog` → `DeleteNichoDialog`; prop `subnichoNome` → `nichoNome` (se existir); copy "sub-nicho" → "nicho" no corpo do dialog

    **`next.config.ts`**: adicionar ao `nextConfig`:
    ```ts
    async redirects() {
      return [{ source: "/subnichos", destination: "/nichos", permanent: true }];
    }
    ```
    (manter o `turbopack.root` existente).

    **`src/components/app-sidebar.tsx`**: `{ href: "/subnichos", label: "Sub-nichos", icon: Tag }` → `{ href: "/nichos", label: "Nichos", icon: Tag }`.
  </action>
  <verify>
    <automated>test -d src/app/nichos && ! test -d src/app/subnichos && echo "route moved"</automated>
    <automated>test -f src/components/nicho-combobox.tsx && test -f src/components/nicho-manager.tsx && test -f src/components/delete-nicho-dialog.tsx && ! test -f src/components/subnicho-combobox.tsx && echo "components renamed"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/nichos/page.tsx` existe; `src/app/subnichos/` NÃO existe
    - `next.config.ts` contém `source: "/subnichos"`, `destination: "/nichos"`, `permanent: true`
    - `src/components/app-sidebar.tsx` contém `href: "/nichos"` e `label: "Nichos"`; NÃO contém `/subnichos`
    - `grep -rl "subnicho-combobox\|subnicho-manager\|delete-subnicho-dialog\|SubnichoCombobox\|SubnichoManager\|DeleteSubnichoDialog" src/` retorna VAZIO
    - os 3 arquivos `src/components/nicho-*.tsx` exportam `NichoCombobox` / `NichoManager` / `DeleteNichoDialog`
  </acceptance_criteria>
  <done>Rota `/nichos` no ar, `/subnichos` redireciona, 3 componentes renomeados, sidebar diz "Nichos".</done>
</task>

<task type="auto">
  <name>Task 2: ~15 consumidores + actions + páginas</name>
  <files>src/components/lead-form-dialog.tsx, src/components/lead-table.tsx, src/components/lead-table-columns.tsx, src/components/lead-table-toolbar.tsx, src/components/lixeira-table.tsx, src/components/followup-dashboard.tsx, src/components/pipeline-board.tsx, src/components/pipeline-lead-card.tsx, src/components/post-import-lead-list.tsx, src/components/whatsapp-preview-dialog.tsx, src/components/csv-import-wizard.tsx, src/components/csv-import-preview-table.tsx, src/components/csv-column-mapper.tsx, src/components/motivo-perda-combobox.tsx, src/components/motivo-perda-manager.tsx, src/components/template-list.tsx, src/components/template-form-dialog.tsx, src/actions/lead-actions.ts, src/actions/import-actions.ts, src/actions/motivo-perda-actions.ts, src/actions/tarefa-actions.ts, src/app/page.tsx, src/app/leads/page.tsx, src/app/pipeline/page.tsx, src/app/lixeira/page.tsx, src/app/relatorios/page.tsx, src/app/motivos-perda/page.tsx, src/app/importar/page.tsx, src/app/importar/[batchId]/page.tsx, src/hooks/use-first-contact-trigger.ts, src/lib/whatsapp.ts</files>
  <read_first>
    - `.planning/phases/13-rename-sub-nicho-nicho-reframe/13-01-SUMMARY.md` — a lista de arquivos que `tsc` acusou como quebrados na Onda 1 (é o roteiro exato desta task)
    - Cada arquivo da lista `<files>` antes de editá-lo (ver o uso real de `subnicho`)
    - `src/components/lead-table-columns.tsx` (o filtro compara por `subnichoId` numérico — `DEFAULT_SORTING`)
    - `src/components/lead-table-toolbar.tsx` (17 occ — filtro de nicho)
  </read_first>
  <action>
    Para CADA arquivo, substituir os identificadores JS/TS renomeados na Onda 1:
    - `subnichoId` → `nichoId`, `subnichoNome` → `nichoNome`, `subnichoNames`/`subnichoNameById`/`subnichosById` e afins → `nicho*`
    - `Subnicho`/`NewSubnicho` (type) → `Nicho`/`NewNicho`
    - `import ... from "@/actions/subnicho-actions"` → `"@/actions/nicho-actions"`; `createSubnicho`/`softDeleteSubnicho`/`renameSubnicho` → `*Nicho`
    - `import { SubnichoCombobox } from "@/components/subnicho-combobox"` → `import { NichoCombobox } from "@/components/nicho-combobox"` (idem manager / delete-dialog)
    - `getContagemPorSubnicho` → `getContagemPorNicho`
    - `subnichos` (import do schema) → `nichos`; `db.select().from(subnichos)` etc. → `nichos`
    - `CsvFieldKey`/label de mapeamento `"subnichoNome"` / "Sub-nicho" no wizard → `"nichoNome"` / "Nicho" (o label VISÍVEL é copy — pode ficar pra Onda 3, mas a KEY tem que trocar agora)
    - `SEM_SUBNICHO_FALLBACK` (`csv-import-preview-table.tsx:50`) → `SEM_NICHO_FALLBACK` (valor `"A categorizar"` FICA)
    - Variáveis locais, props de componente, nomes de estado — tudo que era `subnicho*` como identificador → `nicho*`
    - **NÃO** tocar em strings de UI visíveis ("Sub-nicho", "Categoria do lead (ex: nutricionista...)") além do estritamente necessário pra compilar — isso é a Onda 3. Exceção: labels que são também identificadores (ex: chave de `CsvFieldKey`).

    Os arquivos `motivo-perda-*` e `template-*` têm 1-4 ocorrências cada (geralmente comentário "espelho de subnichos" ou import cruzado) — tratar igual.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sai **exit 0** (zero erro em qualquer arquivo de `src/`)
    - `grep -rn "subnichoId\|subnichoNome\|Subnicho\|subnicho-combobox\|subnicho-manager\|subnicho-actions\|getContagemPorSubnicho\|SEM_SUBNICHO_FALLBACK" src/` retorna VAZIO
    - `grep -rn "\"/subnichos\"\|'/subnichos'\|href=\"/subnichos\"" src/` retorna VAZIO (todos os links internos apontam `/nichos`)
    - `npm run build` sai exit 0; o log lista `/nichos` (`○` ou `ƒ`) e NÃO lista `/subnichos` como rota
    - `npm run test:lead-actions` e `npm run test:relatorios` saem exit 0 (renomear identificadores nos `.cjs` se `tsc`/o harness quebrar; `INSERT INTO subnichos` FICA — nome físico)
  </acceptance_criteria>
  <done>Todo o código de UI e actions fala `nicho`; `tsc` e `build` verdes; nenhum link `/subnichos`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| link interno `/subnichos` esquecido → 404 | Um `href` hardcoded não migrado quebraria a navegação |
| rename de componente → import quebrado | Um import de `@/components/subnicho-*` não atualizado quebra o build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Denial of Service | link `/subnichos` hardcoded esquecido | mitigate | Redirect 301 em `next.config.ts` (Task 1) é rede de segurança — qualquer `/subnichos` que sobrar cai em `/nichos`. Gate de grep na acceptance criteria (`href="/subnichos"` == vazio). |
| T-13-05 | Tampering | perda de comportamento do filtro de nicho na `lead-table` na migração | mitigate | O bloco de filtro é só renomeado (`subnichoId` → `nichoId`), lógica idêntica; `npm run test:lead-actions` roda ao fim; `read_first` inclui `lead-table-columns.tsx`/`lead-table-toolbar.tsx` pro executor ver o filtro real (compara por id numérico, não por nome — decisão da Fase 1). |
| T-13-06 | Tampering | `SEM_SUBNICHO_FALLBACK` renomeado mas valor `"A categorizar"` alterado | mitigate | Task 2 explícito: só o nome da const muda, o valor `"A categorizar"` (fallback D-12 / seed id 1) FICA. Gate de grep. |
| T-13-SC | Tampering | supply chain | accept | Rename puro, zero dependência nova. |

</threat_model>

<verification>
Sequência (host 4GB, dev server parado):

1. `npx tsc --noEmit` → **exit 0**
2. `npm run build` → exit 0, `/nichos` na lista, `/subnichos` ausente
3. `npm run guard:no-hard-delete` → exit 0
4. `npm run verify:schema` → exit 0
5. `npm run test:lead-actions` → exit 0
6. `npm run test:relatorios` → exit 0
7. `grep -rn "subnichoId\|subnichoNome\|Subnicho\|@/components/subnicho\|@/actions/subnicho\|getContagemPorSubnicho\|/subnichos" src/` → VAZIO (só sobra copy visível "Sub-nicho"/"sub-nicho", tratada na Onda 3)
</verification>

<success_criteria>
- Rota `/nichos` funcional (CRUD de nicho idêntico ao antigo `/subnichos`), `/subnichos` → redirect 301
- Sidebar diz "Nichos"
- 4 componentes dedicados renomeados (3 aqui + o de actions na Onda 1)
- Todos os consumidores importam os nomes novos; `tsc` e `build` exit 0
- Nenhum `subnicho` como identificador JS/rota/componente em `src/` (só nomes físicos de banco + copy pendente pra Onda 3)
- Harnesses de regressão verdes
</success_criteria>

<output>
Criar `.planning/phases/13-rename-sub-nicho-nicho-reframe/13-02-SUMMARY.md`: lista final de arquivos tocados, se `git mv` funcionou pros diretórios/componentes, qualquer `.cjs` de teste que precisou de rename de identificador, e a lista exata de strings de UI "Sub-nicho"/"sub-nicho"/"área da saúde" que sobraram (input pra Onda 3 / 13-03).
</output>
