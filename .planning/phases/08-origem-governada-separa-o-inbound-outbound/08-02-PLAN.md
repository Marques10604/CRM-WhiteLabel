---
phase: 08-origem-governada-separa-o-inbound-outbound
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/lead-form-dialog.tsx
  - src/lib/csv-import.ts
  - src/actions/import-actions.ts
autonomous: true
requirements: [ORIGEM-01, ORIGEM-02]

must_haves:
  truths:
    - "O modal de lead mostra um campo 'Tipo de origem' logo abaixo de 'Origem', com as opções Inbound e Outbound"
    - "Ao criar um lead manualmente, o campo Tipo de origem começa vazio (placeholder), sem valor pré-selecionado"
    - "Submeter o formulário de criação sem escolher Tipo de origem bloqueia o salvamento e exibe a mensagem de erro no campo"
    - "Abrir o modal de edição de um lead pré-existente (backfillado) mostra 'Outbound' selecionado no controle"
    - "Leads criados pelo wizard de import CSV nascem com origem_tipo = 'outbound' na tabela leads, sem nenhum passo de UI novo no wizard"
  artifacts:
    - path: "src/components/lead-form-dialog.tsx"
      provides: "Constante ORIGEM_TIPO_OPTIONS e Field/Controller/Select de origemTipo"
      contains: "ORIGEM_TIPO_OPTIONS"
    - path: "src/lib/csv-import.ts"
      provides: "Entrada origemTipo em CSV_DEFAULTS (documentação/paridade do default do import)"
      contains: "origemTipo"
    - path: "src/actions/import-actions.ts"
      provides: "origemTipo persistido no insert explícito de bulkImportLeads"
      contains: "origemTipo: row.origemTipo"
  key_links:
    - from: "src/components/lead-form-dialog.tsx"
      to: "leadSchema.origemTipo (src/lib/validations.ts)"
      via: "Controller name=\"origemTipo\" + Select name=\"origemTipo\" (input nativo oculto lido pelo FormData bruto do DOM)"
      pattern: "name=\"origemTipo\""
    - from: "src/actions/import-actions.ts bulkImportLeads"
      to: "leads.origemTipo (coluna física)"
      via: "tx.insert(leads).values({ ..., origemTipo: row.origemTipo })"
      pattern: "origemTipo:\\s*row\\.origemTipo"
---

<objective>
Expor `origemTipo` como campo obrigatório no formulário de lead (criação e edição) seguindo
exatamente o padrão do campo `canal`, e garantir que o fluxo de import CSV em lote persista
`origemTipo = "outbound"` em toda linha importada sem nenhum passo de UI novo no wizard.

Purpose: Fecha o Requirement 1 (classificação manual) e o Requirement 2 (default do import CSV)
do 08-SPEC.md. Sem a task 2, a validação do `csvRowSchema` passa mas o valor nunca chega ao
banco — o insert de `bulkImportLeads` é uma lista explícita de campos, não um spread.

Output: Modal de lead com o select "Tipo de origem" posicionado após "Origem"; `CSV_DEFAULTS`
documentando o default; `bulkImportLeads` persistindo o campo.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-origem-governada-separa-o-inbound-outbound/08-SPEC.md
@.planning/phases/08-origem-governada-separa-o-inbound-outbound/08-CONTEXT.md
@.planning/phases/08-origem-governada-separa-o-inbound-outbound/08-PATTERNS.md
@.planning/phases/08-origem-governada-separa-o-inbound-outbound/08-01-SUMMARY.md
@src/components/lead-form-dialog.tsx
@src/lib/csv-import.ts
@src/actions/import-actions.ts

<interfaces>
<!-- Contratos entregues pelo plano 08-01. Usar diretamente, sem explorar o codebase. -->

De `src/lib/validations.ts` (após 08-01):
```typescript
// leadSchema — obrigatório, SEM default (D-04)
origemTipo: z.enum(["inbound", "outbound"], { error: "Selecione o tipo de origem." })

// csvRowSchema — override COM default; o wizard nunca envia esse campo
origemTipo: z.enum(["inbound", "outbound"]).default("outbound")

export type CsvRowValues = z.infer<typeof csvRowSchema>; // já inclui origemTipo: "inbound" | "outbound"
export type LeadFormValues = z.input<typeof leadSchema>; // já inclui origemTipo obrigatório
```

De `src/db/schema.ts` (após 08-01): `origemTipo: text("origem_tipo", { enum: ["inbound","outbound"] }).notNull().default("outbound")`.
`Lead` (`src/types/index.ts`) é `InferSelectModel<typeof leads>` — `lead.origemTipo` já existe e é `"inbound" | "outbound"`.

Padrão a espelhar em `src/components/lead-form-dialog.tsx` — constante de opções (linhas 55-58):
```typescript
const CANAL_OPTIONS = [
  { value: "instagram", label: "Instagram" },
  { value: "whatsapp", label: "WhatsApp" },
] as const;
```

Padrão a espelhar — bloco `Field`/`Controller`/`Select` de `canal` (linhas 211-242) e `defaultValues`
(linhas 106-118, note `canal: lead?.canal` SEM fallback e `origem: lead?.origem ?? ""`).
Todos os primitivos usados (`Field`, `FieldLabel`, `FieldContent`, `FieldDescription`, `FieldError`,
`Controller`, `Select`, `SelectTrigger`, `SelectValue`, `SelectContent`, `SelectItem`) JÁ estão
importados no arquivo — nenhum import novo é necessário.

Insert explícito atual de `bulkImportLeads` (`src/actions/import-actions.ts`, linhas ~143-157):
```typescript
tx.insert(leads).values({
  nome: row.nome, telefone: row.telefone, canal: row.canal, origem: row.origem,
  valorEstimado: row.valorEstimado, notas: row.notas,
  subnichoId: subnichoIdByNome.get(chave)!, stage: "novo",
  stageChangedAt: new Date(), followUpDate: new Date(), importBatchId: batchId,
}).run();
```

`CSV_DEFAULTS` atual (`src/lib/csv-import.ts`, linhas 51-56): `{ canal, origem, notas, valorEstimado }`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Campo obrigatório "Tipo de origem" no modal de lead</name>
  <files>src/components/lead-form-dialog.tsx</files>
  <read_first>
    - `src/components/lead-form-dialog.tsx` (arquivo inteiro — em especial: `CANAL_OPTIONS` linhas 55-58, `defaultValues` do `useForm` linhas 104-119, o bloco `Field` de `canal` linhas 211-242, e o bloco `Field` de `origem` linhas 244-253 dentro da seção "Contato" que abre na linha 186)
    - `.planning/phases/08-origem-governada-separa-o-inbound-outbound/08-CONTEXT.md` §decisions (D-01 a D-05 — são a especificação completa e final deste campo; não inventar padrão alternativo)
    - `.planning/phases/08-origem-governada-separa-o-inbound-outbound/08-PATTERNS.md` seção `src/components/lead-form-dialog.tsx` (blocos exatos a copiar)
    - `src/lib/validations.ts` (confirmar que `leadSchema.origemTipo` já existe após 08-01 e que NÃO tem `.default()`)
  </read_first>
  <action>
    1. Declarar, logo após `CANAL_OPTIONS` (linhas 55-58), a constante
       `const ORIGEM_TIPO_OPTIONS = [{ value: "inbound", label: "Inbound" }, { value: "outbound", label: "Outbound" }] as const;`
       Rótulos em inglês, sem tradução para PT-BR (D-01 — mesmo vocabulário já usado em
       `PROJECT.md`/`REQUIREMENTS.md`). Ordem: `inbound` primeiro, `outbound` depois.

    2. Em `defaultValues` do `useForm` (linhas 106-118), adicionar `origemTipo: lead?.origemTipo,`
       imediatamente após a linha `origem: lead?.origem ?? "",`. CRÍTICO: SEM fallback
       (`?? "outbound"`, `?? ""` ou qualquer outro) — na criação o valor precisa ser `undefined`
       para o `SelectValue` renderizar o placeholder vazio (D-04: o admin escolhe conscientemente).
       Isso espelha `canal: lead?.canal` (linha 109), que já usa exatamente esse padrão.

    3. Inserir um novo bloco `<Field>` IMEDIATAMENTE APÓS o `</Field>` de fechamento do campo
       `origem` (linha 253) e ANTES do `</div>` que fecha a seção "Contato" (linha 254) — D-02.
       O bloco replica estrutura por estrutura o do campo `canal` (linhas 211-242), trocando:
       `data-invalid={!!errors.origemTipo}`; `FieldLabel htmlFor="origemTipo"` com o texto
       `Tipo de origem`; `Controller` com `name="origemTipo"`; `Select` com `name="origemTipo"`,
       `items={ORIGEM_TIPO_OPTIONS as unknown as { value: string; label: string }[]}`,
       `value={(field.value as string | undefined) ?? null}` e
       `onValueChange={(value) => field.onChange(value)}`; `SelectTrigger` com `id="origemTipo"`,
       `aria-invalid={!!errors.origemTipo}` e `className="w-full"`; `SelectValue` com
       `placeholder="Selecione o tipo de origem"`; `SelectContent` mapeando `ORIGEM_TIPO_OPTIONS`
       em `SelectItem` com `key`/`value` = `option.value` e conteúdo = `option.label`;
       `FieldError errors={[errors.origemTipo]}`. Incluir um `FieldDescription` em PT-BR
       explicando o conceito de negócio, na linha de: Inbound = o lead procurou você (anúncio,
       formulário, indicação); Outbound = você iniciou o contato (prospecção fria).

       O atributo `name="origemTipo"` no `Select` é LOAD-BEARING: `onSubmit` submete
       `new FormData(formRef.current)` (FormData bruto do DOM, linha 164), então o valor só chega
       ao `createLead`/`updateLead` porque o `Select` renderiza um input nativo oculto com esse
       `name` — exatamente o mecanismo pelo qual `canal` já funciona hoje. Não substituir por
       `form.register`.

    4. NÃO adicionar badge, coluna, ícone ou qualquer indicador de `origemTipo` fora deste modal
       (D-05, YAGNI explícito) — não tocar em `lead-table-columns.tsx`, `pipeline-lead-card.tsx`
       nem em nenhuma tela de listagem. NÃO alterar o campo `origem` (texto livre) nem sua
       `FieldDescription`. NÃO mexer nas seções "Negócio" e "Acompanhamento".
  </action>
  <verify>
    <automated>cd "C:/Users/Vencedor/Desktop/crm-leads" && npx tsc --noEmit && node -e "const s=require('node:fs').readFileSync('src/components/lead-form-dialog.tsx','utf8'); const checks={optionsConst:/const ORIGEM_TIPO_OPTIONS = \[/.test(s), inboundLabel:/label:\s*\x22Inbound\x22/.test(s), outboundLabel:/label:\s*\x22Outbound\x22/.test(s), controller:/name=\x22origemTipo\x22/.test(s), placeholder:/placeholder=\x22Selecione o tipo de origem\x22/.test(s), fieldError:/errors=\{\[errors\.origemTipo\]\}/.test(s), defaultNoFallback:/origemTipo:\s*lead\?\.origemTipo,/.test(s), noFallbackLiteral:!/origemTipo:\s*lead\?\.origemTipo\s*\?\?/.test(s), afterOrigem:s.indexOf('errors.origemTipo')>s.indexOf('errors=\{\[errors.origem\]\}')}; console.log(checks); if(Object.values(checks).some(v=>!v))process.exit(1); console.log('OK: campo origemTipo no formulario');"</automated>
    <human-check>
      1. Com o dev server parado, rodar `npm run dev` e abrir `http://localhost:3000/leads`.
      2. Clicar em "Novo lead". Confirmar que existe um campo "Tipo de origem" LOGO ABAIXO de
         "Origem", ainda dentro do bloco cinza "Contato", e que ele mostra o placeholder
         "Selecione o tipo de origem" (nenhum valor pré-selecionado).
      3. Abrir o select e confirmar exatamente duas opções: "Inbound" e "Outbound".
      4. Preencher todos os outros campos e clicar em Salvar SEM escolher o tipo de origem —
         confirmar que o salvamento é bloqueado e a mensagem "Selecione o tipo de origem."
         aparece abaixo do campo.
      5. Escolher "Inbound", salvar, e conferir por query direta que a linha nova tem
         `origem_tipo = 'inbound'`.
      6. Abrir o modal de EDIÇÃO de qualquer lead pré-existente (backfillado pelo plano 08-01) —
         confirmar que o campo já vem com "Outbound" selecionado.
      7. Parar o dev server antes de seguir para o plano 08-03 (host de 4GB, evitar processos
         paralelos).
    </human-check>
  </verify>
  <acceptance_criteria>
    - `src/components/lead-form-dialog.tsx` contém `const ORIGEM_TIPO_OPTIONS = [` com exatamente dois itens: `{ value: "inbound", label: "Inbound" }` e `{ value: "outbound", label: "Outbound" }`
    - O arquivo contém `origemTipo: lead?.origemTipo,` em `defaultValues` e NÃO contém nenhum `origemTipo: lead?.origemTipo ??`
    - O arquivo contém `name="origemTipo"` tanto no `Controller` quanto no `Select`
    - O arquivo contém `placeholder="Selecione o tipo de origem"` e `<FieldError errors={[errors.origemTipo]} />`
    - O índice de `errors.origemTipo` no arquivo é MAIOR que o índice de `errors={[errors.origem]}` (campo posicionado depois de Origem — D-02)
    - `git diff --stat` mostra apenas `src/components/lead-form-dialog.tsx` alterado — nenhum badge/coluna adicionado em `lead-table-columns.tsx`, `lead-table.tsx` ou `pipeline-lead-card.tsx` (D-05)
    - `npx tsc --noEmit` exit code 0
    - `npx eslint src/components/lead-form-dialog.tsx` exit code 0
  </acceptance_criteria>
  <done>Campo "Tipo de origem" renderizado após "Origem", com placeholder vazio na criação, valor do lead na edição, erro de validação visível quando não escolhido, e nenhuma outra superfície tocada.</done>
</task>

<task type="auto">
  <name>Task 2: Persistir origemTipo="outbound" no fluxo de import CSV em lote</name>
  <files>src/lib/csv-import.ts, src/actions/import-actions.ts</files>
  <read_first>
    - `src/lib/csv-import.ts` (`CSV_DEFAULTS` linhas 51-56 e seu comentário JSDoc; confirmar que `origemTipo` NÃO é um `CsvFieldKey` e não deve virar um)
    - `src/actions/import-actions.ts` (tipo `ConfirmedImportRow` linhas 67-75, o loop de validação com `csvRowSchema.safeParse` linhas ~88-100, e o insert explícito `tx.insert(leads).values({...})` linhas ~143-157)
    - `.planning/phases/08-origem-governada-separa-o-inbound-outbound/08-RESEARCH.md` Pitfall 3 (o insert é lista explícita de campos, não spread — esquecer a linha faz a validação passar e o valor nunca chegar ao banco)
    - `.planning/phases/08-origem-governada-separa-o-inbound-outbound/08-PATTERNS.md` seções `src/lib/csv-import.ts` e `src/actions/import-actions.ts`
  </read_first>
  <action>
    1. Em `src/lib/csv-import.ts`, adicionar a chave `origemTipo: "outbound",` ao objeto
       `CSV_DEFAULTS` (após `valorEstimado: "0"`). Esta entrada é de documentação/paridade —
       `origemTipo` NÃO é um `CsvFieldKey` mapeável de coluna do arquivo (Requirement 2 do
       08-SPEC.md: valor fixo, sem passo de UI no wizard, decisão permanente registrada em
       `08-DECISOES.md`). Estender o comentário JSDoc acima de `CSV_DEFAULTS` com uma frase
       registrando que `origemTipo` é um default FIXO (nunca vem de coluna do CSV, nunca é
       escolhido pelo admin no wizard) e que o ponto de aplicação real é o
       `.default("outbound")` do `csvRowSchema` em `src/lib/validations.ts`.
       NÃO adicionar `origemTipo` a `CsvFieldKey`, `CsvColumnMapping`, `MappedCsvRow` nem a
       nenhuma estrutura consumida por `mapCsvRows` — nenhum novo branch de lógica neste arquivo.

    2. Em `src/actions/import-actions.ts`, dentro do loop `for (const row of validatedRows)`,
       adicionar a linha `origemTipo: row.origemTipo,` ao objeto `tx.insert(leads).values({...})`,
       posicionada IMEDIATAMENTE APÓS `origem: row.origem,` (mesma ordem lógica dos demais
       campos). `row` é `CsvRowValues`, que já carrega `origemTipo` preenchido pelo
       `.default("outbound")` do `csvRowSchema` após o `safeParse` do plano 08-01.
       Sem esta linha, `npx tsc --noEmit` NÃO acusa nada (o campo é opcional-com-default no tipo
       de insert do Drizzle) e o `DEFAULT 'outbound'` físico da coluna mascara o bug até alguém
       precisar importar um lead `inbound` via CSV.

    3. NÃO adicionar `origemTipo` ao tipo `ConfirmedImportRow` (linhas 67-75) e NÃO tocar em
       `src/components/csv-import-wizard.tsx` nem em `src/components/csv-import-preview-table.tsx`:
       o wizard nunca coleta esse campo do cliente (nenhum passo de UI novo — decisão travada em
       `08-DECISOES.md` / `08-INTENT-REVIEW.md` Pergunta 2), e o `csvRowSchema.safeParse(row)` do
       servidor preenche o valor sozinho.
  </action>
  <verify>
    <automated>cd "C:/Users/Vencedor/Desktop/crm-leads" && npx tsc --noEmit && node -e "const fs=require('node:fs');const c=fs.readFileSync('src/lib/csv-import.ts','utf8');const a=fs.readFileSync('src/actions/import-actions.ts','utf8');const checks={csvDefault:/origemTipo:\s*\x22outbound\x22/.test(c), notAFieldKey:!/CsvFieldKey[\s\S]{0,400}origemTipo/.test(c), insertLine:/origemTipo:\s*row\.origemTipo,/.test(a), afterOrigem:a.indexOf('origemTipo: row.origemTipo')>a.indexOf('origem: row.origem'), confirmedRowUntouched:!/ConfirmedImportRow = \{[\s\S]*?origemTipo[\s\S]*?\};/.test(a)}; console.log(checks); if(Object.values(checks).some(v=>!v))process.exit(1); console.log('OK: wiring do import CSV');"</automated>
    <human-check>
      1. Com o dev server rodando, abrir `http://localhost:3000/importar` e importar um CSV de
         teste pequeno (2-3 linhas) — confirmar que o wizard NÃO ganhou nenhum passo/campo novo
         em relação ao comportamento anterior.
      2. Após confirmar a importação, rodar query direta em `data/crm.db` filtrando pelo
         `import_batch_id` do lote recém-criado e conferir que TODAS as linhas do lote têm
         `origem_tipo = 'outbound'`.
      3. Remover (soft-delete) os leads de teste importados, se desejar manter o banco limpo.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `src/lib/csv-import.ts` contém `origemTipo: "outbound",` dentro do objeto `CSV_DEFAULTS`
    - `src/lib/csv-import.ts` NÃO adiciona `origemTipo` a `CsvFieldKey`/`MappedCsvRow` (nenhum novo campo mapeável)
    - `src/actions/import-actions.ts` contém a linha exata `origemTipo: row.origemTipo,` dentro do `tx.insert(leads).values({...})` de `bulkImportLeads`
    - O índice de `origemTipo: row.origemTipo` é maior que o de `origem: row.origem` no arquivo
    - O tipo `ConfirmedImportRow` continua sem o campo `origemTipo`
    - `git diff --name-only` não inclui `src/components/csv-import-wizard.tsx` nem `src/components/csv-import-preview-table.tsx`
    - `npx tsc --noEmit` exit code 0
    - Import real de um lote de teste produz linhas com `origem_tipo = 'outbound'` verificadas por query direta em `data/crm.db`
  </acceptance_criteria>
  <done>`CSV_DEFAULTS` documenta o default fixo, `bulkImportLeads` persiste `origemTipo` no insert explícito, e o wizard permanece sem passo de UI novo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Formulário do browser → `createLead`/`updateLead` | `FormData` bruto do DOM (incluindo o input oculto do `Select`) é entrada não confiável |
| Wizard CSV do browser → `bulkImportLeads` | `ConfirmedImportRow[]` vem do cliente e pode ser forjado (payload de Server Action) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering | `createLead`/`updateLead` via FormData forjado (ASVS V5) | mitigate | O `Select` do cliente só oferece dois valores, mas a garantia real é server-side: `leadSchema.safeParse(Object.fromEntries(formData))` com `origemTipo: z.enum(["inbound","outbound"])` — qualquer outro valor retorna `{ errors }` antes do insert. Nenhuma validação nova é adicionada no cliente como substituto |
| T-08-04 | Tampering | `bulkImportLeads` com `origemTipo` injetado no payload (ASVS V5) | mitigate | `csvRowSchema.safeParse(row)` roda linha a linha ANTES de qualquer insert (defesa em profundidade já existente, linhas ~88-100); `z.enum(["inbound","outbound"]).default("outbound")` rejeita valor fora do enum e preenche a ausência com o default seguro. O insert usa `row.origemTipo` de `validatedRows`, nunca o objeto cru vindo do cliente |
| T-08-06 | Information Disclosure | `FieldDescription` do novo campo | accept | O texto explica apenas o conceito de negócio Inbound/Outbound; não expõe dado de lead, PII, nem detalhe de infraestrutura |
| T-08-SC | Tampering | Instalação de pacotes npm | accept | Nenhum pacote novo instalado neste plano — todos os primitivos de UI já estão em `src/components/ui/` e importados no arquivo |
</threat_model>

<verification>
1. `npx tsc --noEmit` — exit 0
2. `npx eslint src/components/lead-form-dialog.tsx src/lib/csv-import.ts src/actions/import-actions.ts` — exit 0
3. Asserções estáticas das duas tasks (comandos `node -e` acima) — exit 0
4. `<human-check>` do formulário: placeholder vazio na criação, erro visível ao submeter sem escolher, valor "Outbound" pré-carregado ao editar lead backfillado
5. `<human-check>` do import CSV: lote de teste importado produz `origem_tipo = 'outbound'` em todas as linhas, sem passo novo no wizard
6. `git diff --name-only` limitado aos 3 arquivos de `files_modified`
</verification>

<success_criteria>
- Campo "Tipo de origem" existe no modal, posicionado após "Origem", com rótulos "Inbound"/"Outbound" e sem pré-seleção na criação
- Submeter a criação sem escolher o tipo bloqueia o salvamento com erro visível no campo
- Editar um lead backfillado mostra "Outbound" selecionado
- Import CSV persiste `origem_tipo = 'outbound'` em toda linha do lote, sem passo de UI novo
- Nenhum badge/coluna de `origemTipo` adicionado fora do modal (D-05)
- `npx tsc --noEmit` e `npx eslint` limpos
</success_criteria>

<output>
Criar `.planning/phases/08-origem-governada-separa-o-inbound-outbound/08-02-SUMMARY.md` ao concluir.
Registrar: (a) o diff resumido de cada arquivo, (b) o resultado das asserções estáticas,
(c) o resultado dos `<human-check>` (ou a declaração explícita de que não houve acesso a
navegador nesta sessão, seguindo o precedente registrado em SUMMARYs anteriores do projeto),
(d) o `import_batch_id` e as contagens do lote de teste, se o import foi testado ao vivo.
Documentos em PT-BR.
</output>
