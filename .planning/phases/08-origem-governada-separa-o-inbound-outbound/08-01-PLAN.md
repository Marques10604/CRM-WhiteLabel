---
phase: 08-origem-governada-separa-o-inbound-outbound
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/lib/validations.ts
  - scripts/backfill-origem-tipo.cjs
autonomous: true
requirements: [ORIGEM-01, ORIGEM-02]

must_haves:
  truths:
    - "A coluna origem_tipo existe fisicamente na tabela leads de data/crm.db, com NOT NULL e DEFAULT 'outbound'"
    - "As 33 linhas de leads (22 ativas + 11 soft-deletadas) têm origem_tipo = 'outbound' após a migração"
    - "Rodar scripts/backfill-origem-tipo.cjs uma segunda vez não lança erro nem altera nenhuma linha"
    - "Existe um arquivo de backup de data/crm.db criado antes de qualquer escrita da migração"
    - "leadSchema exige origemTipo (enum fechado) sem .default(), e csvRowSchema aceita a ausência do campo aplicando o default 'outbound'"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Coluna origemTipo na tabela leads (enum inbound|outbound, notNull, default outbound)"
      contains: "origem_tipo"
    - path: "src/lib/validations.ts"
      provides: "origemTipo obrigatório em leadSchema e override com default em csvRowSchema"
      contains: "origemTipo"
    - path: "scripts/backfill-origem-tipo.cjs"
      provides: "Migração ALTER TABLE + backfill idempotente com backup automático"
      min_lines: 40
  key_links:
    - from: "src/db/schema.ts"
      to: "data/crm.db tabela leads"
      via: "coluna origem_tipo aplicada por scripts/backfill-origem-tipo.cjs"
      pattern: "origem_tipo"
    - from: "src/lib/validations.ts csvRowSchema"
      to: "leadSchema.origemTipo"
      via: ".extend() sobrescrevendo o campo herdado com .default(\"outbound\")"
      pattern: "origemTipo:\\s*z\\.enum\\(\\[\"inbound\", \"outbound\"\\]\\)\\.default"
---

<objective>
Estabelecer os contratos de `origemTipo` (coluna Drizzle + schemas Zod) e aplicar a coluna
física `origem_tipo` no banco real `data/crm.db` com backfill idempotente das 33 linhas
existentes, sem usar `drizzle-kit push`.

Purpose: Este é o plano de fundação da Fase 8 — os planos 08-02 (formulário + import CSV) e
08-03 (harness de testes + verificação) dependem tanto do tipo TypeScript quanto da coluna
física existirem. Sem a coluna física, qualquer `db.select().from(leads)` contra o banco real
quebra assim que `schema.ts` declara a coluna (Drizzle lista todas as colunas no SELECT).

Output: `src/db/schema.ts` e `src/lib/validations.ts` atualizados, `scripts/backfill-origem-tipo.cjs`
criado e executado contra `data/crm.db`, backup do `.db` gerado, 0 linhas com `origem_tipo IS NULL`.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-origem-governada-separa-o-inbound-outbound/08-SPEC.md
@.planning/phases/08-origem-governada-separa-o-inbound-outbound/08-RESEARCH.md
@.planning/phases/08-origem-governada-separa-o-inbound-outbound/08-PATTERNS.md
@src/db/schema.ts
@src/lib/validations.ts
@scripts/verify-pipeline-migration.cjs

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código atual. Não explorar o codebase. -->

Estado atual relevante de `src/db/schema.ts` (tabela `leads`, linhas 32-62) — precedentes de enum:
```typescript
canal: text("canal", { enum: ["instagram", "whatsapp"] }).notNull(),
origem: text("origem").notNull(),
stage: text("stage", { enum: ["novo", "contatado", "negociacao", "fechado", "perdido"] })
  .notNull()
  .default("novo"),
contactAttempts: integer("contact_attempts").notNull().default(0),
```

Estado atual relevante de `src/lib/validations.ts` (linhas 26-29 e 53-55):
```typescript
canal: z.enum(["instagram", "whatsapp"], { error: "Selecione um canal de contato." }),
origem: z.string().trim().min(1, "Origem é obrigatória."),
// ...
export const csvRowSchema = leadSchema.omit({ subnichoId: true, followUpDate: true }).extend({
  subnichoNome: z.string().trim().min(1, "Sub-nicho é obrigatório."),
});
export type CsvRowValues = z.infer<typeof csvRowSchema>;
```

Estado real de `data/crm.db` (verificado 2026-08-06 por `PRAGMA table_info(leads)`):
colunas atuais = id, nome, telefone, canal, origem, valor_estimado_centavos, notas,
follow_up_date, subnicho_id, stage, deleted_at, created_at, updated_at, motivo_perda,
stage_changed_at, import_batch_id, contact_attempts. Total 33 linhas, 22 com `deleted_at IS NULL`.
`origem_tipo` NÃO existe ainda.

Idioma de script `.cjs` do projeto (de `scripts/verify-pipeline-migration.cjs`):
```javascript
const path = require("node:path");
const Database = require("better-sqlite3");
const DB_PATH = process.env.DB_FILE_NAME ?? path.join(__dirname, "..", "data", "crm.db");
function fail(message) { console.error(`[nome-do-script] FALHOU: ${message}`); process.exit(1); }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declarar origemTipo no schema Drizzle e nos schemas Zod</name>
  <files>src/db/schema.ts, src/lib/validations.ts</files>
  <read_first>
    - `src/db/schema.ts` (estado atual da tabela `leads`, linhas 32-62 — ver posição exata de `origem` e o idioma de `canal`/`stage`)
    - `src/lib/validations.ts` (estado atual de `leadSchema` linhas 9-41 e `csvRowSchema` linhas 45-57)
    - `.planning/phases/08-origem-governada-separa-o-inbound-outbound/08-RESEARCH.md` seção "Code Examples" itens 1 e 2 (forma exata recomendada) e "Anti-Patterns to Avoid"
    - `.planning/phases/08-origem-governada-separa-o-inbound-outbound/08-PATTERNS.md` seções `src/db/schema.ts` e `src/lib/validations.ts`
  </read_first>
  <action>
    Em `src/db/schema.ts`, dentro de `export const leads = sqliteTable("leads", {...})`, inserir a
    coluna `origemTipo` IMEDIATAMENTE APÓS a linha `origem: text("origem").notNull(),` (D-02 — o
    agrupamento semântico do formulário é espelhado no schema). Forma exata a usar:
    nome da propriedade TypeScript `origemTipo`, nome da coluna física `"origem_tipo"`, tipo
    `text` com a opção `{ enum: ["inbound", "outbound"] }`, encadeado com `.notNull()` e
    `.default("outbound")` — exatamente o mesmo idioma já usado por `stage` (linhas 44-46) e
    `contactAttempts` (linha 49). Adicionar um comentário curto na linha explicando que o
    `.default("outbound")` espelha o `DEFAULT` físico OBRIGATÓRIO exigido pelo SQLite no
    `ALTER TABLE ADD COLUMN NOT NULL` sobre tabela não-vazia (restrição do SQLite, não escolha
    de produto) e que ele nunca é acionado pelos fluxos da aplicação, porque o Zod sempre
    entrega o campo preenchido. NÃO alterar nenhum índice em `(table) => [...]`.

    Em `src/lib/validations.ts`, dentro de `leadSchema`, inserir `origemTipo` IMEDIATAMENTE APÓS
    a linha `origem: z.string().trim().min(1, "Origem é obrigatória."),`. Usar
    `z.enum(["inbound", "outbound"], { error: "Selecione o tipo de origem." })` — mesmo formato de
    mensagem de erro já usado por `canal` (linhas 26-28). CRÍTICO: NÃO adicionar `.default(...)`
    aqui — a ausência de default é o que garante D-04 (o formulário manual nunca pré-seleciona um
    valor, forçando escolha consciente do admin). Adicionar um comentário na linha registrando
    esse motivo.

    Ainda em `src/lib/validations.ts`, no bloco `.extend({...})` de `csvRowSchema` (linhas 53-55),
    adicionar a chave `origemTipo` com `z.enum(["inbound", "outbound"]).default("outbound")`. Isso
    SOBRESCREVE o campo herdado de `leadSchema` (que é obrigatório sem default). Sem essa
    sobrescrita, TODA linha do import CSV passa a falhar `csvRowSchema.safeParse` por campo
    ausente e a tela `/importar` para de funcionar por completo (Pitfall 2 do 08-RESEARCH.md).
    Estender também o comentário JSDoc de `csvRowSchema` (linhas 45-52) com uma frase explicando
    por que `origemTipo` recebe default aqui e não em `leadSchema`.

    NÃO tocar em `stageUpdateSchema`, `whatsappContactSchema`, `templateSchema` nem
    `configuracoesSchema`. NÃO alterar o campo `origem` (texto livre) de forma alguma — esta fase
    é estritamente aditiva sobre ele (Constraint do 08-SPEC.md).
  </action>
  <verify>
    <automated>cd "C:/Users/Vencedor/Desktop/crm-leads" && node -e "const s=require('node:fs').readFileSync('src/db/schema.ts','utf8'); const v=require('node:fs').readFileSync('src/lib/validations.ts','utf8'); const ok=[ /origemTipo:\s*text\(\s*\x22origem_tipo\x22/.test(s), /enum:\s*\[\x22inbound\x22,\s*\x22outbound\x22\]/.test(s), /\.default\(\x22outbound\x22\)/.test(s), s.indexOf('origem_tipo')>s.indexOf('text(\x22origem\x22)'), /origemTipo:\s*z\.enum\(\[\x22inbound\x22,\s*\x22outbound\x22\]/.test(v), /origemTipo:\s*z\.enum\(\[\x22inbound\x22,\s*\x22outbound\x22\]\)\.default\(\x22outbound\x22\)/.test(v) ]; console.log(ok); if(ok.some(x=>!x)) process.exit(1); console.log('OK: schema + zod wiring');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/schema.ts` contém `origemTipo: text("origem_tipo", { enum: ["inbound", "outbound"] })` encadeado com `.notNull()` e `.default("outbound")`
    - A declaração de `origemTipo` aparece DEPOIS de `origem: text("origem").notNull(),` e ANTES de `valorEstimado` no objeto de colunas de `leads`
    - `src/lib/validations.ts` contém, dentro de `leadSchema`, `origemTipo: z.enum(["inbound", "outbound"], { error: "Selecione o tipo de origem." })` SEM nenhum `.default(` encadeado
    - `src/lib/validations.ts` contém, dentro do `.extend({...})` de `csvRowSchema`, `origemTipo: z.enum(["inbound", "outbound"]).default("outbound")`
    - `npx tsc --noEmit` termina com exit code 0
    - `git diff --stat src/lib/validations.ts` não mostra alteração na linha de `origem: z.string().trim().min(1, "Origem é obrigatória.")`
  </acceptance_criteria>
  <done>Coluna Drizzle e ambos os schemas Zod declaram `origemTipo`, com default apenas no Drizzle e no override de `csvRowSchema`, nunca em `leadSchema`. `npx tsc --noEmit` limpo.</done>
</task>

<task type="auto">
  <name>Task 2: Criar scripts/backfill-origem-tipo.cjs (migração + backfill idempotente + backup)</name>
  <files>scripts/backfill-origem-tipo.cjs</files>
  <read_first>
    - `scripts/verify-pipeline-migration.cjs` (idioma completo de script `.cjs` do projeto: resolução de `DB_PATH` via `process.env.DB_FILE_NAME`, helper `fail()` com `process.exit(1)`, leitura de `PRAGMA table_info(leads)`, log final `[nome] OK: ...`)
    - `.planning/phases/08-origem-governada-separa-o-inbound-outbound/08-RESEARCH.md` Pattern 2 (corpo completo do script já redigido e conferido — usar como referência primária)
    - `.planning/phases/08-origem-governada-separa-o-inbound-outbound/08-PATTERNS.md` seção `scripts/backfill-origem-tipo.cjs` (mesma referência + anti-padrões)
    - `scripts/guard-no-hard-delete.cjs` (linhas 45-62 — quais padrões SQL são proibidos em `scripts/`; o script novo não pode conter `DELETE FROM leads` nem `DROP TABLE leads`)
  </read_first>
  <action>
    Criar `scripts/backfill-origem-tipo.cjs` (CommonJS, só módulos nativos + `better-sqlite3`),
    seguindo o corpo já redigido em `08-RESEARCH.md` Pattern 2. O script executa, nesta ordem:

    1. Resolver `DB_PATH` como `process.env.DB_FILE_NAME ?? path.join(__dirname, "..", "data", "crm.db")`
       (mesma linha de `verify-pipeline-migration.cjs`).
    2. BACKUP ANTES DE QUALQUER ESCRITA: abrir uma conexão dedicada, executar
       `pragma("wal_checkpoint(TRUNCATE)")` (o `src/db/client.ts` roda em `journal_mode=WAL`, então
       copiar o arquivo principal sem checkpoint pode perder escritas ainda no `-wal`), fechar essa
       conexão, e então `fs.copyFileSync(DB_PATH, backupPath)` onde `backupPath` é
       `` `${DB_PATH}.backup-${new Date().toISOString().replace(/[:.]/g, "-")}` ``. Logar o caminho do
       backup criado com o prefixo `[backfill-origem-tipo]`.
    3. Abrir a conexão de trabalho e capturar `before = SELECT count(*) AS c FROM leads`.
    4. GUARDA DE IDEMPOTÊNCIA DO DDL: ler `PRAGMA table_info(leads)` e checar se alguma coluna se
       chama `origem_tipo`. Se NÃO existir, executar via `db.exec(...)` a DDL exata
       ``ALTER TABLE `leads` ADD `origem_tipo` text DEFAULT 'outbound' NOT NULL;`` (mesmo formato de
       identificadores/ordem `DEFAULT ... NOT NULL` que o conversor do drizzle-kit gera — precedente
       real `ALTER TABLE `leads` ADD `contact_attempts` integer DEFAULT 0 NOT NULL;` da Fase 06-01).
       Se JÁ existir, logar que está pulando o ALTER por idempotência e seguir. Nunca deixar o
       `ALTER` fora dessa guarda — o SQLite lança `duplicate column name` num re-run.
    5. GUARDA DE IDEMPOTÊNCIA DO DADO: executar SEMPRE
       `UPDATE leads SET origem_tipo = 'outbound' WHERE origem_tipo IS NULL` e logar `result.changes`.
       O `WHERE origem_tipo IS NULL` é obrigatório pela Constraint do 08-SPEC.md — um `UPDATE`
       incondicional sobrescreveria uma linha já reclassificada para `inbound` pelo admin. Na prática
       deve afetar 0 linhas, porque o `DEFAULT` do `ALTER TABLE` já preenche todas as linhas
       existentes retroativamente.
    6. VERIFICAÇÃO PÓS-MIGRAÇÃO: capturar `after = count(*) FROM leads`,
       `nullCount = count(*) FROM leads WHERE origem_tipo IS NULL`,
       `outboundCount = count(*) FROM leads WHERE origem_tipo = 'outbound'`,
       `inboundCount = count(*) FROM leads WHERE origem_tipo = 'inbound'`. Falhar (via helper `fail()`
       com `process.exit(1)`) se `before !== after` ou se `nullCount !== 0`. Logar a linha final
       `[backfill-origem-tipo] OK: <after> linhas totais, 0 NULL, <outboundCount> outbound, <inboundCount> inbound`
       e fechar o banco com `db.close()`.

    Todas as mensagens de log/erro em PT-BR, prefixadas com `[backfill-origem-tipo]`. NÃO usar
    `drizzle-kit push` nem `drizzle-kit generate` em lugar nenhum deste script (Pitfall 1 —
    dois precedentes reais de comportamento destrutivo/não-confiável neste repositório, Fases
    06-01 e 07-01). NÃO criar nenhum arquivo novo em `src/db/migrations/` — esta fase segue o
    precedente das Fases 04-02/06-01/07-01 de não reconciliar o snapshot do drizzle-kit
    (débito técnico pré-existente, documentado em STATE.md). NÃO executar o script nesta task.
  </action>
  <verify>
    <automated>cd "C:/Users/Vencedor/Desktop/crm-leads" && node scripts/guard-no-hard-delete.cjs && node -e "const s=require('node:fs').readFileSync('scripts/backfill-origem-tipo.cjs','utf8').split('\n').filter(l=>!/^\s*(\/\/|\*|\/\*)/.test(l)).join('\n'); const req=[/wal_checkpoint/,/copyFileSync/,/PRAGMA table_info\(leads\)/,/ALTER TABLE .leads. ADD .origem_tipo. text DEFAULT 'outbound' NOT NULL/,/WHERE origem_tipo IS NULL/,/origem_tipo IS NULL/]; const forb=[/drizzle-kit/,/UPDATE leads SET origem_tipo = 'outbound'(?!\s+WHERE)/]; const r=req.map(x=>x.test(s)); const f=forb.map(x=>x.test(s)); console.log('req',r,'forb',f); if(r.some(x=>!x)||f.some(x=>x)) process.exit(1); console.log('OK: script structure');"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/backfill-origem-tipo.cjs` existe e tem no mínimo 40 linhas
    - O corpo (ignorando comentários) contém `wal_checkpoint`, `fs.copyFileSync`, `PRAGMA table_info(leads)` e a DDL literal ``ALTER TABLE `leads` ADD `origem_tipo` text DEFAULT 'outbound' NOT NULL;``
    - Todo `UPDATE leads SET origem_tipo` do arquivo é seguido de `WHERE origem_tipo IS NULL` — nenhum `UPDATE` incondicional
    - O `ALTER TABLE` está dentro de um bloco condicional sobre a ausência da coluna em `PRAGMA table_info(leads)`
    - O arquivo não contém a string `drizzle-kit`
    - `node scripts/guard-no-hard-delete.cjs` termina com exit code 0
    - O script NÃO foi executado ainda nesta task (nenhum arquivo `data/crm.db.backup-*` criado por ela)
  </acceptance_criteria>
  <done>Script criado, estruturalmente correto (backup → guarda de DDL → guarda de dado → verificação), aprovado pelo guard anti-hard-delete, ainda não executado.</done>
</task>

<task type="auto">
  <name>Task 3 [BLOCKING]: Executar a migração contra data/crm.db e provar backfill + idempotência</name>
  <files>data/crm.db (dado real — não versionado), data/crm.db.backup-* (gerado)</files>
  <read_first>
    - `scripts/backfill-origem-tipo.cjs` (o script recém-criado na Task 2 — reler o corpo final antes de executar contra dado real)
    - `.planning/phases/08-origem-governada-separa-o-inbound-outbound/08-SPEC.md` §Constraints e §Acceptance Criteria (backup obrigatório, 33 linhas, idempotência)
    - `.planning/STATE.md` §Blockers/Concerns (linha "Phase 8: backup de `data/crm.db` antes de qualquer ... que altere a tabela `leads`")
  </read_first>
  <action>
    PRÉ-CONDIÇÃO OBRIGATÓRIA: parar o dev server do Next (`npm run dev`) se estiver rodando, para
    que nenhuma conexão concorrente segure o WAL durante o checkpoint. Este host tem 4GB de RAM —
    não iniciar processos paralelos durante esta task.

    1. Registrar o estado PRÉ-migração para comparação, via `node -e` com `better-sqlite3` em modo
       readonly sobre `data/crm.db`: total de linhas em `leads` (esperado 33), linhas com
       `deleted_at IS NULL` (esperado 22), e a lista de colunas de `PRAGMA table_info(leads)`
       (esperado: SEM `origem_tipo`).
    2. Executar `node scripts/backfill-origem-tipo.cjs`. Capturar a saída completa. O script deve
       imprimir o caminho do backup criado, a confirmação do `ALTER TABLE` aplicado, o número de
       linhas afetadas pelo `UPDATE` idempotente (esperado 0) e a linha final `OK:`.
    3. Verificar o resultado por query direta independente do script (não confiar só no log dele):
       `PRAGMA table_info(leads)` deve listar `origem_tipo` com `notnull=1` e `dflt_value='outbound'`;
       `SELECT count(*) FROM leads WHERE origem_tipo IS NULL` deve retornar 0;
       `SELECT count(*) FROM leads` deve retornar 33 (inalterado);
       `SELECT count(*) FROM leads WHERE origem_tipo = 'outbound'` deve retornar 33 (backfill
       uniforme — decisão do usuário registrada em 08-INTENT-REVIEW.md Pergunta 1: os 5 leads
       soft-deletados com `origem` "Teste"/"insta" também recebem "outbound", sem regra
       diferenciada por texto).
    4. PROVA DE IDEMPOTÊNCIA: executar `node scripts/backfill-origem-tipo.cjs` uma SEGUNDA vez.
       Deve terminar com exit code 0, logar que está pulando o `ALTER TABLE` porque a coluna já
       existe, e reportar 0 linhas afetadas pelo `UPDATE`. Re-verificar por query direta que
       `count(*) FROM leads` continua 33 e que `count(*) WHERE origem_tipo='outbound'` continua 33.
    5. Confirmar que o(s) arquivo(s) `data/crm.db.backup-*` existe(m) em disco e listar seu nome
       exato — ele precisa ser referenciado nominalmente no SUMMARY desta fase (Acceptance
       Criteria do 08-SPEC.md). `data/*.db*` já está no `.gitignore`, então o backup não entra em
       nenhum commit — a referência é textual no SUMMARY.

    PROIBIDO nesta task: rodar `npx drizzle-kit push`, `npx drizzle-kit generate` ou qualquer
    comando do drizzle-kit contra `data/crm.db`. Se o script falhar por qualquer motivo, NÃO
    tentar `push` como fallback — restaurar o backup (`cp data/crm.db.backup-* data/crm.db`),
    reportar a falha e parar.
  </action>
  <verify>
    <automated>cd "C:/Users/Vencedor/Desktop/crm-leads" && node -e "const D=require('better-sqlite3'),fs=require('node:fs');const db=new D('data/crm.db',{readonly:true});const col=db.prepare('PRAGMA table_info(leads)').all().find(c=>c.name==='origem_tipo');const nulls=db.prepare('select count(*) c from leads where origem_tipo is null').get().c;const total=db.prepare('select count(*) c from leads').get().c;const out=db.prepare(\"select count(*) c from leads where origem_tipo='outbound'\").get().c;const bk=fs.readdirSync('data').filter(f=>f.startsWith('crm.db.backup-'));console.log({col,nulls,total,out,backups:bk});if(!col||col.notnull!==1||nulls!==0||total!==33||out!==33||bk.length===0)process.exit(1);console.log('OK: migracao+backfill verificados');"</automated>
  </verify>
  <acceptance_criteria>
    - `PRAGMA table_info(leads)` em `data/crm.db` retorna uma coluna `origem_tipo` com `type='TEXT'`, `notnull=1` e `dflt_value` igual a `'outbound'`
    - `SELECT count(*) FROM leads WHERE origem_tipo IS NULL` retorna exatamente 0
    - `SELECT count(*) FROM leads` retorna exatamente 33 (nenhuma linha perdida ou criada)
    - `SELECT count(*) FROM leads WHERE origem_tipo = 'outbound'` retorna exatamente 33
    - `SELECT count(*) FROM leads WHERE deleted_at IS NULL` continua retornando 22
    - Existe pelo menos um arquivo `data/crm.db.backup-*` em disco, e seu nome exato foi registrado na saída do executor para citação no SUMMARY
    - A segunda execução de `node scripts/backfill-origem-tipo.cjs` termina com exit code 0, imprime que pulou o `ALTER TABLE`, e reporta 0 linhas afetadas pelo `UPDATE`
    - Nenhum comando `drizzle-kit` foi executado durante esta task
  </acceptance_criteria>
  <done>Coluna `origem_tipo` existe fisicamente com as 33 linhas em `'outbound'`, backup pré-migração em disco e nomeado, idempotência provada por segunda execução sem efeito.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script de migração → `data/crm.db` | DDL destrutiva em potencial sobre dado real de produção sem migration history versionado |
| Formulário/FormData do browser → Server Action (`createLead`/`updateLead`) | Entrada não confiável define o valor de `origemTipo` (contrato Zod criado nesta plano) |
| Linha de CSV do cliente → `bulkImportLeads` | Entrada não confiável poderia carregar um `origemTipo` forjado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Denial of Service (perda de dados) | `scripts/backfill-origem-tipo.cjs` → `data/crm.db` | mitigate | Backup obrigatório ANTES de qualquer escrita: `wal_checkpoint(TRUNCATE)` + `fs.copyFileSync` para `data/crm.db.backup-<timestamp>` (Task 2 passo 2, Task 3 critério de aceite). `drizzle-kit push` proibido explicitamente na Task 3 — dois precedentes reais de comportamento destrutivo (Fases 06-01, 07-01) |
| T-08-02 | Tampering | Re-execução do script de backfill | mitigate | `UPDATE leads SET origem_tipo='outbound' WHERE origem_tipo IS NULL` — nunca incondicional; uma linha já reclassificada para `inbound` pelo admin nunca tem `origem_tipo IS NULL` e portanto nunca é sobrescrita. Guarda de `PRAGMA table_info` evita `duplicate column name` no `ALTER` |
| T-08-03 | Tampering | `leadSchema` / `createLead` / `updateLead` (ASVS V5) | mitigate | `origemTipo: z.enum(["inbound","outbound"])` obrigatório em `leadSchema`, validado por `safeParse` server-side em toda Server Action de escrita — um FormData forjado com `origemTipo="qualquer"` é rejeitado antes do insert |
| T-08-04 | Tampering | `csvRowSchema` / `bulkImportLeads` (ASVS V5) | mitigate | `origemTipo: z.enum(["inbound","outbound"]).default("outbound")` — uma linha de CSV forjada com valor fora do enum é rejeitada; ausência do campo cai no default seguro |
| T-08-05 | Tampering | `INSERT` SQL cru fora da aplicação com `origem_tipo` fora do enum | accept | Mesma exposição já existente de `canal`/`stage` — `text(..., {enum})` do Drizzle é anotação TypeScript, o SQL gerado é `text NOT NULL` puro sem `CHECK` (confirmado em `0000_gifted_slapstick.sql:5-9`). Postura pré-existente e documentada em `08-SPEC.md` §Constraints, não regressão desta fase |
| T-08-SC | Tampering | Instalação de pacotes npm | accept | Nenhum pacote novo é instalado nesta fase — `drizzle-orm`, `better-sqlite3` e `zod` já estão em `package.json` e em uso ativo (confirmado em `08-RESEARCH.md` §Package Legitimacy Audit). Nenhum gate de legitimidade aplicável |
</threat_model>

<verification>
1. `npx tsc --noEmit` — exit 0 (a inferência de `Lead`/`NewLead` em `src/types/index.ts` absorve a coluna nova automaticamente)
2. `node scripts/guard-no-hard-delete.cjs` — exit 0
3. Query direta em `data/crm.db`: `PRAGMA table_info(leads)` lista `origem_tipo` (`notnull=1`, `dflt_value='outbound'`); `count(*) FROM leads` = 33; `count(*) WHERE origem_tipo IS NULL` = 0; `count(*) WHERE origem_tipo='outbound'` = 33
4. Segunda execução de `node scripts/backfill-origem-tipo.cjs` — exit 0, 0 linhas afetadas, nenhum erro de `duplicate column name`
5. `ls data/crm.db.backup-*` — pelo menos um arquivo presente
6. `npm run build` NÃO é exigido neste plano (o formulário ainda não coleta `origemTipo`; o build completo é gate do plano 08-03, com o dev server parado — host de 4GB)
</verification>

<success_criteria>
- `src/db/schema.ts` declara `origemTipo` como `text("origem_tipo", { enum: ["inbound","outbound"] }).notNull().default("outbound")`, posicionada após `origem`
- `leadSchema` exige `origemTipo` sem `.default()`; `csvRowSchema` sobrescreve com `.default("outbound")`
- `scripts/backfill-origem-tipo.cjs` existe, faz backup antes de escrever, é idempotente em DDL e em dado, e verifica o resultado
- `data/crm.db` tem `origem_tipo` NOT NULL DEFAULT 'outbound' com as 33 linhas em `'outbound'` e 0 NULL
- Backup pré-migração existe em disco e seu nome exato está registrado para citação no SUMMARY
- `npx tsc --noEmit` limpo
</success_criteria>

<output>
Criar `.planning/phases/08-origem-governada-separa-o-inbound-outbound/08-01-SUMMARY.md` ao concluir.
Registrar obrigatoriamente: (a) o nome exato do arquivo de backup gerado, (b) a saída literal das
duas execuções do script (primeira e segunda, provando idempotência), (c) as contagens
pós-migração verificadas por query direta, (d) a confirmação explícita de que nenhum comando
`drizzle-kit` foi executado. Documentos em PT-BR.
</output>
