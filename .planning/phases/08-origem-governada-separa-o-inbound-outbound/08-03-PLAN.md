---
phase: 08-origem-governada-separa-o-inbound-outbound
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - scripts/test-lead-actions.cjs
  - scripts/verify-origem-tipo.cjs
  - package.json
autonomous: true
requirements: [ORIGEM-01, ORIGEM-02]

must_haves:
  truths:
    - "scripts/test-lead-actions.cjs roda de ponta a ponta e termina com 0 falhas (o bootstrap do banco temporário deixou de estar desatualizado)"
    - "Existe um caso de teste automatizado provando que createLead REJEITA um FormData sem origemTipo, com a mensagem 'Selecione o tipo de origem.'"
    - "Existe um caso de teste automatizado provando que createLead PERSISTE o origemTipo escolhido"
    - "scripts/verify-origem-tipo.cjs falha (exit 1) se qualquer elo da fiação de origemTipo for removido ou se o banco real tiver alguma linha com origem_tipo NULL"
    - "npm run build completo termina com sucesso, com o dev server parado"
  artifacts:
    - path: "scripts/test-lead-actions.cjs"
      provides: "Bootstrap de banco temporário atualizado + cobertura de origemTipo em createLead/updateLead"
      contains: "origemTipo"
    - path: "scripts/verify-origem-tipo.cjs"
      provides: "Guarda permanente da fiação de origemTipo (estática + banco real)"
      min_lines: 50
    - path: "package.json"
      provides: "Scripts npm para as duas guardas desta fase"
      contains: "verify:origem-tipo"
  key_links:
    - from: "scripts/test-lead-actions.cjs"
      to: "src/db/migrations/0001_grey_xavin.sql + ALTERs manuais (import_batch_id, contact_attempts, origem_tipo)"
      via: "bootstrap do banco temporário aplicando todas as migrações, não só 0000"
      pattern: "0001_grey_xavin"
    - from: "scripts/verify-origem-tipo.cjs"
      to: "data/crm.db leads.origem_tipo"
      via: "PRAGMA table_info + SELECT count(*) WHERE origem_tipo IS NULL"
      pattern: "origem_tipo IS NULL"
---

<objective>
Fechar a Fase 8 com verificação automatizada real: consertar o bootstrap desatualizado do banco
temporário de `scripts/test-lead-actions.cjs` (débito pré-existente que faz o script falhar hoje,
antes mesmo de tocar em `origemTipo`), cobrir `origemTipo` nos casos de teste de
`createLead`/`updateLead`, e criar uma guarda permanente da fiação da fase.

Purpose: Sem este plano, os critérios de aceite do Requirement 1 do 08-SPEC.md ("submeter sem
`origemTipo` bloqueia o submit", "criar com `origemTipo` persiste o valor") só teriam prova
manual. Além disso, `npx tsc --noEmit` e `npm run build` passam mesmo com a coluna física
ausente ou com o `origemTipo` faltando no insert do import — é um estado de falso-positivo de
verificação que só uma asserção contra o banco real detecta.

DECISÃO EXPLÍCITA SOBRE DÉBITO PRÉ-EXISTENTE (opção "a"): o bootstrap quebrado de
`scripts/test-lead-actions.cjs` (`SqliteError: no such column: "motivo_perda"`, confirmado por
execução real em 2026-08-06) É CONSERTADO NESTA FASE, não deixado para depois. Motivo: os
critérios de aceite do Requirement 1 dependem desse script conseguir exercitar
`createLead`/`updateLead`, e o conserto é contido (um único bloco de bootstrap num único arquivo).
O escopo do conserto é estritamente o bootstrap do banco temporário — nenhuma outra correção de
débito técnico entra nesta fase.

Output: `scripts/test-lead-actions.cjs` verde de ponta a ponta com 2 casos novos de `origemTipo`,
`scripts/verify-origem-tipo.cjs` criado, ambos expostos como scripts npm, `npm run build` limpo.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-origem-governada-separa-o-inbound-outbound/08-SPEC.md
@.planning/phases/08-origem-governada-separa-o-inbound-outbound/08-PATTERNS.md
@.planning/phases/08-origem-governada-separa-o-inbound-outbound/08-01-SUMMARY.md
@.planning/phases/08-origem-governada-separa-o-inbound-outbound/08-02-SUMMARY.md
@scripts/test-lead-actions.cjs
@scripts/verify-pipeline-migration.cjs
@scripts/verify-schema.cjs

<interfaces>
<!-- Contratos entregues por 08-01 e 08-02. Usar diretamente. -->

Colunas reais de `leads` em `data/crm.db` ANTES desta fase (verificado 2026-08-06):
id, nome, telefone, canal, origem, valor_estimado_centavos, notas, follow_up_date, subnicho_id,
stage, deleted_at, created_at, updated_at, motivo_perda, stage_changed_at, import_batch_id,
contact_attempts — mais `origem_tipo` adicionada pelo plano 08-01.

O que `src/db/migrations/0000_gifted_slapstick.sql` cria em `leads` (única migração aplicada hoje
pelo bootstrap do script de teste): id, nome, telefone, canal, origem, valor_estimado_centavos,
notas, follow_up_date, subnicho_id, stage, deleted_at, created_at, updated_at. E em `subnichos`:
id, nome, created_at (SEM `deleted_at`).

`src/db/migrations/0001_grey_xavin.sql` (conteúdo integral, 2 statements separados por
`--> statement-breakpoint`):
```sql
ALTER TABLE `leads` ADD `motivo_perda` text;
ALTER TABLE `leads` ADD `stage_changed_at` integer;
```

Colunas adicionadas por migrações MANUAIS (nunca viraram arquivo `.sql` — Fases 05/06-01, quick
task 260725-lai, e esta fase). Estas são as DDLs que o bootstrap precisa reproduzir:
```sql
ALTER TABLE `leads` ADD `import_batch_id` text;
ALTER TABLE `leads` ADD `contact_attempts` integer DEFAULT 0 NOT NULL;
ALTER TABLE `leads` ADD `origem_tipo` text DEFAULT 'outbound' NOT NULL;
ALTER TABLE `subnichos` ADD `deleted_at` integer;
```

`makeFormData()` atual em `scripts/test-lead-actions.cjs` (linhas ~99-116) — objeto `base`:
nome, telefone, canal, origem, valorEstimado, notas, followUpDate, subnichoId (+ `...overrides`).

Idioma de guarda `.cjs` do projeto (`scripts/verify-pipeline-migration.cjs`):
```javascript
const DB_PATH = process.env.DB_FILE_NAME ?? path.join(__dirname, "..", "data", "crm.db");
function fail(message) { console.error(`[verify-x] FALHOU: ${message}`); process.exit(1); }
const columns = db.prepare("PRAGMA table_info(leads)").all().map((c) => c.name);
```

Scripts npm atuais em `package.json`: `dev`, `build`, `start`, `lint`, `guard:no-hard-delete`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consertar o bootstrap do banco temporário e cobrir origemTipo em test-lead-actions.cjs</name>
  <files>scripts/test-lead-actions.cjs</files>
  <read_first>
    - `scripts/test-lead-actions.cjs` (arquivo inteiro — em especial o bootstrap `runBehaviorTests()` linhas ~78-95, o helper `makeFormData()` linhas ~99-116, e o Caso 2 linhas ~154-165 como molde para os casos novos)
    - `src/db/migrations/0000_gifted_slapstick.sql` e `src/db/migrations/0001_grey_xavin.sql` (o que cada uma cria)
    - `src/lib/validations.ts` (após 08-01 — a mensagem exata do erro de `origemTipo`)
    - `.planning/phases/08-origem-governada-separa-o-inbound-outbound/08-PATTERNS.md` seção `scripts/test-lead-actions.cjs` (achado crítico do pattern-mapper sobre o bootstrap quebrado)
  </read_first>
  <action>
    1. CONSERTAR O BOOTSTRAP (débito pré-existente, decisão "a" desta fase). Hoje, dentro de
       `runBehaviorTests()`, o bootstrap lê e aplica APENAS
       `src/db/migrations/0000_gifted_slapstick.sql`, então `countLeads()` (que faz
       `db.select().from(leads)`, listando todas as colunas de `schema.ts`) explode com
       `SqliteError: no such column: "motivo_perda"` antes de qualquer asserção rodar.
       Substituir esse trecho por um bootstrap que aplica, na ordem:
       (a) o conteúdo de `0000_gifted_slapstick.sql`,
       (b) o conteúdo de `0001_grey_xavin.sql`,
       (c) uma lista literal de `ALTER TABLE` reproduzindo as migrações manuais que nunca viraram
           arquivo `.sql`: `import_batch_id text` em `leads`, `contact_attempts integer DEFAULT 0 NOT NULL`
           em `leads`, `origem_tipo text DEFAULT 'outbound' NOT NULL` em `leads`, e `deleted_at integer`
           em `subnichos`.
       Manter o `.replace(/--> statement-breakpoint/g, "")` já usado para os arquivos `.sql`.
       Cada `ALTER TABLE` da lista (c) deve rodar dentro de um try/catch que engole SOMENTE erros
       cuja mensagem contenha `duplicate column name` (tornando o bootstrap tolerante caso alguma
       dessas colunas venha a virar arquivo de migração no futuro) e re-lança qualquer outro erro.
       Adicionar um comentário de bloco acima explicando por que a lista (c) existe: o snapshot de
       migrações do drizzle-kit está divergente do banco real desde as Fases 04-02/06-01/07-01
       (débito pré-existente documentado em `STATE.md`), então o banco temporário precisa
       reconstruir essas colunas manualmente para espelhar `src/db/schema.ts`.
       NÃO tentar rodar `drizzle-kit push` contra o banco temporário.

    2. Adicionar `origemTipo: "outbound",` ao objeto `base` de `makeFormData()`, logo após
       `origem: "Instagram Ads",`. Sem isso, os Casos 1 e 7 (que esperam sucesso) passam a falhar
       por validação assim que `origemTipo` vira obrigatório em `leadSchema`.
       Os DOIS `db.insert(leads).values({...})` diretos do arquivo (Caso 7 e Caso 8) NÃO precisam
       de mudança — eles bypassam `leadSchema` e o `DEFAULT 'outbound'` físico da coluna cobre a
       ausência sem erro. Não alterá-los.

    3. Adicionar um CASO NOVO (numerar como Caso 9), modelado no Caso 2 (nome vazio):
       `createLead` com `makeFormData({ origemTipo: "" })` NÃO deve inserir linha, e
       `result.errors.origemTipo` deve ser um array contendo a mensagem exata definida em
       `leadSchema` (`"Selecione o tipo de origem."`). Este caso é a prova automatizada do
       critério de aceite "submeter o formulário de criação sem selecionar `origemTipo` bloqueia
       o submit com erro de validação visível" (08-SPEC.md Requirement 1).

    4. Adicionar um CASO NOVO (Caso 10): `createLead` com
       `makeFormData({ origemTipo: "inbound" })` deve inserir exatamente 1 linha, e a leitura
       dessa linha via `db.select().from(leads)` deve retornar `origemTipo === "inbound"`.
       Usar `callToleratingRevalidate` (mesmo padrão do Caso 1) porque `revalidatePath` lança fora
       do contexto de request do Next. Este caso é a prova automatizada do critério "criar um lead
       com `origemTipo` selecionado persiste o valor".

    Todas as mensagens de asserção em PT-BR, no mesmo estilo das existentes.
  </action>
  <verify>
    <automated>cd "C:/Users/Vencedor/Desktop/crm-leads" && node scripts/test-lead-actions.cjs && node -e "const s=require('node:fs').readFileSync('scripts/test-lead-actions.cjs','utf8');const c={m0001:/0001_grey_xavin/.test(s),importBatch:/ADD .import_batch_id./.test(s),contactAttempts:/ADD .contact_attempts./.test(s),origemTipoDdl:/ADD .origem_tipo. text DEFAULT 'outbound' NOT NULL/.test(s),dupGuard:/duplicate column name/.test(s),base:/origemTipo:\s*\x22outbound\x22,/.test(s),caseEmpty:/origemTipo:\s*\x22\x22/.test(s),caseInbound:/origemTipo:\s*\x22inbound\x22/.test(s),msg:/Selecione o tipo de origem\./.test(s)};console.log(c);if(Object.values(c).some(v=>!v))process.exit(1);console.log('OK: harness atualizado');"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/test-lead-actions.cjs` termina com exit code 0 e imprime `[test-lead-actions] OK: todas as asserções passaram.` (hoje ele falha com `SqliteError: no such column: "motivo_perda"`)
    - A saída do script NÃO contém nenhuma linha começando com `FAIL`
    - `scripts/test-lead-actions.cjs` referencia `0001_grey_xavin` e contém as 4 DDLs manuais (`import_batch_id`, `contact_attempts`, `origem_tipo`, `subnichos.deleted_at`)
    - O bootstrap engole apenas erros contendo `duplicate column name` e re-lança os demais
    - `makeFormData()` inclui `origemTipo: "outbound",` no objeto `base`
    - Existe um caso que chama `createLead` com `origemTipo: ""` e assere que `errors.origemTipo` inclui `"Selecione o tipo de origem."` e que a contagem de linhas não mudou
    - Existe um caso que chama `createLead` com `origemTipo: "inbound"` e assere, por leitura do banco, que a linha criada tem `origemTipo === "inbound"`
    - Os dois `db.insert(leads).values({...})` diretos permanecem sem o campo `origemTipo`
  </acceptance_criteria>
  <done>O harness roda de ponta a ponta sem falhas, com o bootstrap alinhado ao `schema.ts` atual e 2 casos novos cobrindo obrigatoriedade e persistência de `origemTipo`.</done>
</task>

<task type="auto">
  <name>Task 2: Criar scripts/verify-origem-tipo.cjs e expor as guardas em package.json</name>
  <files>scripts/verify-origem-tipo.cjs, package.json</files>
  <read_first>
    - `scripts/verify-pipeline-migration.cjs` (arquivo inteiro — idioma exato de guarda: `DB_PATH`, `fail()`, `PRAGMA table_info`, `process.exit(0)` final)
    - `scripts/verify-schema.cjs` (segunda referência de idioma, checagem via `sqlite_master`)
    - `package.json` (bloco `scripts` atual: `dev`, `build`, `start`, `lint`, `guard:no-hard-delete`)
    - `src/db/schema.ts`, `src/lib/validations.ts`, `src/components/lead-form-dialog.tsx`, `src/actions/import-actions.ts` (os 4 elos que a guarda vai assertar — confirmar as strings exatas já aplicadas por 08-01/08-02)
  </read_first>
  <action>
    Criar `scripts/verify-origem-tipo.cjs` (CommonJS, só `node:fs`/`node:path` + `better-sqlite3`),
    seguindo o idioma de `verify-pipeline-migration.cjs`: prefixo de log `[verify-origem-tipo]`,
    helper `fail(message)` com `process.exit(1)`, `DB_PATH` resolvido de
    `process.env.DB_FILE_NAME ?? path.join(__dirname, "..", "data", "crm.db")`, e
    `console.log("[verify-origem-tipo] OK: ...")` + `process.exit(0)` no final.

    METADE ESTÁTICA (lê os arquivos-fonte, acumula falhas em vez de abortar na primeira):
    - `src/db/schema.ts` declara a coluna `origem_tipo` como `text` com `enum: ["inbound", "outbound"]`,
      encadeada com `.notNull()` e `.default("outbound")`
    - `src/lib/validations.ts` declara `origemTipo` em `leadSchema` SEM `.default(` na mesma
      declaração (D-04 — se alguém adicionar um default lá, o formulário volta a pré-selecionar e a
      governança se esvazia)
    - `src/lib/validations.ts` declara, dentro do `.extend({...})` de `csvRowSchema`,
      `origemTipo` COM `.default("outbound")` (sem isso, todo import CSV quebra)
    - `src/components/lead-form-dialog.tsx` contém `ORIGEM_TIPO_OPTIONS` com os dois valores
      `inbound`/`outbound` e um `name="origemTipo"`
    - `src/actions/import-actions.ts` contém `origemTipo: row.origemTipo` dentro do insert de
      `bulkImportLeads` (Pitfall 3 — sem essa linha o valor nunca chega ao banco e nenhum
      `tsc`/`build` acusa)
    IMPORTANTE ao escrever regex sobre arquivos-fonte: filtrar linhas de comentário antes de contar
    ocorrências (o próprio arquivo de origem pode mencionar os tokens em comentários), ou usar
    padrões ancorados em sintaxe real (`origemTipo:` seguido de `z.enum(`/`text(`), nunca uma busca
    solta pela palavra.

    METADE DE BANCO REAL (só executa se o arquivo em `DB_PATH` existir; se não existir, logar aviso
    e pular esta metade sem falhar, para o script continuar utilizável em ambiente limpo):
    - `PRAGMA table_info(leads)` contém `origem_tipo` com `notnull = 1` e `dflt_value` = `'outbound'`
    - `SELECT count(*) FROM leads WHERE origem_tipo IS NULL` retorna 0
    - `SELECT count(*) FROM leads WHERE origem_tipo NOT IN ('inbound','outbound')` retorna 0
      (nenhum terceiro estado entrou no banco por caminho fora da aplicação)
    Logar a distribuição final (`count` por valor de `origem_tipo`) na linha de OK.

    Em `package.json`, adicionar ao bloco `scripts`, mantendo os existentes intactos:
    `"verify:origem-tipo": "node scripts/verify-origem-tipo.cjs"` e
    `"test:lead-actions": "node scripts/test-lead-actions.cjs"`. Não alterar dependências nem
    nenhum outro campo do `package.json`.
  </action>
  <verify>
    <automated>cd "C:/Users/Vencedor/Desktop/crm-leads" && npm run verify:origem-tipo && node scripts/guard-no-hard-delete.cjs && node -e "const p=require('./package.json');const c={v:!!p.scripts['verify:origem-tipo'],t:!!p.scripts['test:lead-actions'],g:!!p.scripts['guard:no-hard-delete'],b:p.scripts.build==='next build'};console.log(c);if(Object.values(c).some(x=>!x))process.exit(1);console.log('OK: scripts npm');"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-origem-tipo.cjs` existe, tem no mínimo 50 linhas e usa o prefixo de log `[verify-origem-tipo]`
    - `npm run verify:origem-tipo` termina com exit code 0 e imprime uma linha `OK:` com a distribuição de `origem_tipo` (esperado: 33 outbound, mais quaisquer leads criados nos testes ao vivo do plano 08-02)
    - A guarda FALHA (exit 1) quando um elo é removido — provar comprometendo temporariamente um arquivo: remover a linha `origemTipo: row.origemTipo,` de `src/actions/import-actions.ts`, rodar `npm run verify:origem-tipo` (deve sair 1), e RESTAURAR a linha em seguida (`git checkout -- src/actions/import-actions.ts`), rodando de novo para confirmar exit 0
    - `package.json` contém `"verify:origem-tipo"` e `"test:lead-actions"` no bloco `scripts`, e os scripts pré-existentes (`dev`, `build`, `start`, `lint`, `guard:no-hard-delete`) continuam inalterados
    - `node scripts/guard-no-hard-delete.cjs` exit code 0 (o script novo não introduz SQL destrutivo)
  </acceptance_criteria>
  <done>Guarda permanente de `origemTipo` criada, provada por teste de mutação (falha ao remover um elo, passa ao restaurar), e exposta como script npm junto do harness de lead actions.</done>
</task>

<task type="auto">
  <name>Task 3: Gates finais da fase (build de produção, lint, guardas)</name>
  <files>nenhum arquivo modificado — apenas execução de gates</files>
  <read_first>
    - `.planning/STATE.md` §Session Continuity (nota sobre o host de 4GB: `npm run build` já crashou por falta de memória quando o dev server estava ativo ao mesmo tempo — rodar SEMPRE com o dev server parado)
    - `.planning/phases/08-origem-governada-separa-o-inbound-outbound/08-SPEC.md` §Acceptance Criteria (a lista completa a conferir item a item)
  </read_first>
  <action>
    PRÉ-CONDIÇÃO: garantir que NENHUM `npm run dev` está rodando antes de iniciar. Este host tem
    4GB de RAM e já derrubou o worker do Next duas vezes por rodar build de produção com o dev
    server ativo. Não iniciar processos em paralelo durante esta task.

    Executar, em sequência, um comando por vez, capturando a saída de cada um:
    1. `npx tsc --noEmit`
    2. `npx eslint`
    3. `node scripts/guard-no-hard-delete.cjs`
    4. `npm run test:lead-actions`
    5. `npm run verify:origem-tipo`
    6. `npm run build`

    Se `npm run build` falhar por erro de memória (worker do Next morto / OOM), e não por erro de
    compilação real, registrar isso explicitamente na saída e no SUMMARY como limitação de
    ambiente (precedente já documentado em `STATE.md`), tentando novamente uma única vez com o
    dev server comprovadamente parado. Não mascarar erro de compilação real como OOM.

    Ao final, conferir item a item a lista de Acceptance Criteria do `08-SPEC.md` e produzir, na
    saída, uma tabela marcando cada item como atendido (com a evidência: comando + resultado, ou
    query + contagem) ou pendente (com o motivo). Os itens que dependem de clique em navegador
    são os `<human-check>` do plano 08-02 — se não houve acesso a navegador nesta sessão,
    declarar isso explicitamente em vez de marcar como atendido.

    NÃO fazer nenhuma alteração de código nesta task. Se algum gate falhar por defeito real,
    parar e reportar — a correção é uma nova iteração, não um remendo dentro do gate.
  </action>
  <verify>
    <automated>cd "C:/Users/Vencedor/Desktop/crm-leads" && npx tsc --noEmit && npx eslint && node scripts/guard-no-hard-delete.cjs && npm run test:lead-actions && npm run verify:origem-tipo && echo "TODOS OS GATES NAO-BUILD PASSARAM"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exit code 0
    - `npx eslint` exit code 0
    - `node scripts/guard-no-hard-delete.cjs` exit code 0
    - `npm run test:lead-actions` exit code 0, sem nenhuma linha `FAIL` na saída
    - `npm run verify:origem-tipo` exit code 0
    - `npm run build` exit code 0 com o dev server parado — OU, se falhar, a saída prova que a causa foi OOM/worker morto e não erro de compilação, e isso está registrado no SUMMARY
    - A saída do executor contém uma tabela cobrindo os 8 itens de `## Acceptance Criteria` do `08-SPEC.md`, cada um marcado como atendido (com evidência) ou pendente (com motivo)
    - `git status --porcelain` não mostra nenhuma alteração de código feita por esta task
  </acceptance_criteria>
  <done>Todos os gates automatizados verdes, build de produção limpo com o dev server parado, e checklist do SPEC conferido item a item com evidência.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script de teste → banco SQLite temporário em `os.tmpdir()` | Escrita em disco fora do projeto; nunca deve tocar `data/crm.db` |
| Guarda de verificação → `data/crm.db` | Leitura de dado real de produção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Tampering | `scripts/test-lead-actions.cjs` escrevendo no banco real por engano | mitigate | O script já define `process.env.DB_FILE_NAME` para um caminho em `os.tmpdir()` ANTES de importar `@/db/client` (o módulo cacheia a conexão no primeiro import) — o bootstrap novo mantém essa ordem inalterada e faz cleanup dos arquivos `.db`/`-shm`/`-wal` temporários no final |
| T-08-08 | Information Disclosure | `scripts/verify-origem-tipo.cjs` logando dado de lead | mitigate | A guarda só imprime CONTAGENS agregadas (`count(*)` por valor de `origem_tipo`) e nomes de coluna — nunca `nome`, `telefone` ou qualquer campo de PII de lead |
| T-08-09 | Denial of Service (ambiente) | `npm run build` concorrente com `npm run dev` neste host de 4GB | mitigate | Task 3 exige explicitamente que o dev server esteja parado antes do build; precedente de dois crashes por OOM documentado em `STATE.md` |
| T-08-10 | Tampering | Regressão silenciosa da fiação de `origemTipo` em fases futuras (10/11 dependem dela) | mitigate | `scripts/verify-origem-tipo.cjs` como guarda permanente exposta em `package.json`, provada por teste de mutação (remover um elo → exit 1) |
| T-08-SC | Tampering | Instalação de pacotes npm | accept | Nenhum pacote novo instalado — `better-sqlite3` já é dependência ativa; a alteração em `package.json` toca apenas o bloco `scripts` |
</threat_model>

<verification>
1. `node scripts/test-lead-actions.cjs` — exit 0, zero linhas `FAIL` (hoje falha com `SqliteError: no such column: "motivo_perda"`)
2. `npm run verify:origem-tipo` — exit 0
3. Teste de mutação da guarda: remover `origemTipo: row.origemTipo,` de `import-actions.ts` → `npm run verify:origem-tipo` exit 1 → `git checkout -- src/actions/import-actions.ts` → exit 0
4. `npx tsc --noEmit`, `npx eslint`, `node scripts/guard-no-hard-delete.cjs` — todos exit 0
5. `npm run build` com dev server parado — exit 0
6. Query final em `data/crm.db`: `count(*) WHERE origem_tipo IS NULL` = 0 e `count(*) WHERE origem_tipo NOT IN ('inbound','outbound')` = 0
</verification>

<success_criteria>
- `scripts/test-lead-actions.cjs` roda de ponta a ponta sem falhas, com bootstrap alinhado ao `schema.ts` atual (débito pré-existente do `motivo_perda` resolvido)
- Dois casos automatizados provam a obrigatoriedade e a persistência de `origemTipo` em `createLead`
- `scripts/verify-origem-tipo.cjs` existe, passa, e falha comprovadamente quando um elo da fiação é removido
- `package.json` expõe `verify:origem-tipo` e `test:lead-actions`
- Todos os gates (`tsc`, `eslint`, guard, harness, guarda, `build`) verdes
- Checklist de Acceptance Criteria do `08-SPEC.md` conferido item a item com evidência
</success_criteria>

<output>
Criar `.planning/phases/08-origem-governada-separa-o-inbound-outbound/08-03-SUMMARY.md` ao concluir.
Registrar obrigatoriamente: (a) a tabela de conferência dos 8 itens de Acceptance Criteria do
`08-SPEC.md` com evidência por item, (b) a saída do teste de mutação da guarda, (c) a nota de que
o conserto do bootstrap de `test-lead-actions.cjs` era débito PRÉ-EXISTENTE (falha
`no such column: "motivo_perda"` confirmada antes desta fase) resolvido deliberadamente aqui
porque os critérios de aceite do Requirement 1 dependiam dele, (d) o nome do arquivo de backup
do banco gerado no plano 08-01, para rastreabilidade do backfill. Documentos em PT-BR.
</output>
