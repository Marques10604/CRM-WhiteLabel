---
phase: 10-sequ-ncia-de-follow-up-escalonada
plan: 03
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/actions/configuracoes-actions.ts
  - src/components/configuracoes-form.tsx
autonomous: true
requirements: [SEQ-01]

must_haves:
  truths:
    - "O admin adiciona e remove linhas de intervalo em /configuracoes, sem número fixo de campos"
    - "Salvar com 3 intervalos persiste os 3 valores, não apenas o último digitado"
    - "Recarregar /configuracoes depois de salvar mostra exatamente os intervalos salvos, na mesma ordem"
    - "Tentar salvar com a lista vazia mostra 'Adicione ao menos um intervalo.' e não persiste nada"
    - "Tentar salvar com um intervalo menor que 1 mostra 'Mínimo de 1 dia.' e não persiste nada"
    - "Os três campos de dias-parado da Fase 7 continuam salvando normalmente, no mesmo botão 'Salvar configurações'"
    - "Remover a linha do meio de [1,2,3] renumera os rótulos para Intervalo 1 e Intervalo 2, sem buraco na numeração"
  artifacts:
    - path: "src/actions/configuracoes-actions.ts"
      provides: "saveConfiguracoes lendo a lista dinâmica via formData.getAll e validando por configuracoesServerSchema"
      contains: "formData.getAll(\"intervaloDias\")"
    - path: "src/components/configuracoes-form.tsx"
      provides: "Seção 'Sequência de reabordagem' com lista dinâmica, validação client e erros por linha"
      contains: "Sequência de reabordagem"
  key_links:
    - from: "src/components/configuracoes-form.tsx"
      to: "src/actions/configuracoes-actions.ts"
      via: "inputs repetidos name=intervaloDias no mesmo <form> nativo, lidos por formData.getAll"
      pattern: "name=\"intervaloDias\""
    - from: "src/actions/configuracoes-actions.ts"
      to: "configuracoes.sequenciaIntervalosDias"
      via: "upsert insert+onConflictDoUpdate com o array já validado"
      pattern: "onConflictDoUpdate"
---

<objective>
Entregar SEQ-01: a tela `/configuracoes` (singleton, Fase 7) ganha a seção "Sequência de reabordagem" com uma lista dinâmica de intervalos em dias (adicionar/remover linhas, sem teto), salva junto com os três campos de dias-parado pelo mesmo botão.

Purpose: sem esta tela a sequência é um array congelado no default do banco — o admin não tem como definir a cadência dele, e o Success Criteria #1 do ROADMAP fica em aberto.
Output: Server Action que lê corretamente N valores repetidos do FormData e um formulário com lista dinâmica seguindo o `10-UI-SPEC.md` linha a linha.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-CONTEXT.md
@.planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-RESEARCH.md
@.planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-PATTERNS.md
@.planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-UI-SPEC.md

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código atual — não explorar o repositório para redescobrir. -->

`src/lib/validations.ts` (após o plano 10-01):
- `configuracoesSchema` — INALTERADO, ainda só os 3 campos escalares; `ConfiguracoesFormValues = z.input<typeof configuracoesSchema>` continua sendo o tipo do `useForm`
- `sequenciaIntervalosSchema = z.array(z.coerce.number().int().min(1, "Mínimo de 1 dia.")).min(1, "Adicione ao menos um intervalo.")`
- `configuracoesServerSchema = configuracoesSchema.extend({ sequenciaIntervalosDias: sequenciaIntervalosSchema })`

`src/db/queries.ts`:
- `export type Configuracoes = typeof configuracoes.$inferSelect;` — já inclui `sequenciaIntervalosDias: number[]` depois do plano 10-01
- `getConfiguracoes()` semeia a linha `id=1` quando ausente (os DEFAULTs físicos preenchem a coluna nova com `[4,10,20]`)

`src/actions/configuracoes-actions.ts` (estado atual, 49 linhas):
```
export async function saveConfiguracoes(_prevState: ActionState, formData: FormData): Promise<ActionState> {
  const parsed = configuracoesSchema.safeParse(Object.fromEntries(formData));
  if (!parsed.success) return { errors: parsed.error.flatten().fieldErrors };
  await db.insert(configuracoes).values({ id: 1, ...parsed.data, updatedAt: new Date() })
    .onConflictDoUpdate({ target: configuracoes.id, set: { ...parsed.data, updatedAt: new Date() } });
  revalidatePath("/configuracoes");
  revalidatePath("/pipeline");
  return { success: true };
}
```

`src/components/configuracoes-form.tsx` (estado atual, 163 linhas):
- `useActionState<ActionState, FormData>(saveConfiguracoes, undefined)` → `[state, formAction, pending]`; `formRef = useRef<HTMLFormElement>(null)`
- `useForm<ConfiguracoesFormValues>({ resolver: zodResolver(configuracoesSchema), defaultValues: { diasParadoNovo, diasParadoContatado, diasParadoNegociacao } })`
- `useEffect` sobre `state` → `toast.success("Configurações salvas.")` / `toast.error("Não foi possível salvar as configurações. Tente novamente.")`
- `function onSubmit() { if (!formRef.current) return; formAction(new FormData(formRef.current)); }`
- `<form ref={formRef} onSubmit={form.handleSubmit(onSubmit)} noValidate className="flex flex-col gap-4">`, com um `eslint-disable-next-line react-hooks/refs` documentado logo acima de `onSubmit` — NÃO remover
- Container externo: `<div className="rounded-lg border border-zinc-200 bg-white p-6 max-w-md">`
- Cada campo fixo: `<Field data-invalid={...}><FieldLabel htmlFor=...>…</FieldLabel><FieldContent><Input id type="number" min={1} step={1} inputMode="numeric" aria-invalid {...form.register(...)} /><FieldDescription>…</FieldDescription><FieldError errors={[errors.X]} /></FieldContent></Field>`
- Rodapé: `<div className="flex justify-end"><Button type="submit" className="bg-[#0D9488] text-white hover:bg-[#0D9488]/90" disabled={pending}>{pending ? "Salvando..." : "Salvar configurações"}</Button></div>`
- Primitivos disponíveis em `@/components/ui/field`: `Field`, `FieldContent`, `FieldDescription`, `FieldError`, `FieldGroup`, `FieldLabel`

Ícones lucide já confirmados presentes no pacote instalado: `Plus`, `Trash2`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: saveConfiguracoes lendo a lista repetida do FormData e revalidando o dashboard</name>
  <files>src/actions/configuracoes-actions.ts</files>
  <read_first>
    - src/actions/configuracoes-actions.ts (arquivo inteiro, 49 linhas — cabeçalho documental, `ActionState` local, o upsert e a lista de `revalidatePath`)
    - src/lib/validations.ts (versão do plano 10-01 — `configuracoesServerSchema` e `sequenciaIntervalosSchema`)
    - .planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-RESEARCH.md §Common Pitfalls 3 (por que `Object.fromEntries(formData)` sozinho perde a lista)
    - .planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-PATTERNS.md §`src/actions/configuracoes-actions.ts`
  </read_first>
  <action>
Trocar o import de `configuracoesSchema` por `configuracoesServerSchema` e substituir a linha do `safeParse` por uma montagem explícita do objeto de entrada, em duas etapas:
- `const raw = Object.fromEntries(formData);` — continua correto para os 3 campos escalares
- `const intervalos = formData.getAll("intervaloDias");` — devolve `string[]`, um item por linha da lista dinâmica
- `const parsed = configuracoesServerSchema.safeParse({ ...raw, sequenciaIntervalosDias: intervalos });`

Comentário obrigatório acima explicando por que `Object.fromEntries(formData)` NÃO pode ser usado sozinho aqui: `FormData` suporta nativamente várias entradas com a mesma chave, mas `Object.fromEntries` sobre esse iterável mantém só o ÚLTIMO valor de cada chave repetida — salvar 3 intervalos gravaria só o terceiro, silenciosamente e sem erro de validação. É comportamento padrão de ECMAScript, não bug de biblioteca; as outras Server Actions do projeto continuam usando o spread genérico porque nenhuma delas tem campo repetido.

O `z.array(z.coerce.number()...)` de `sequenciaIntervalosSchema` converte cada string do array em número, então `parsed.data.sequenciaIntervalosDias` já sai como `number[]` e entra no upsert pelo mesmo `...parsed.data` que já existe — o `values(...)` e o `onConflictDoUpdate(...)` não precisam de nenhuma chave nova escrita à mão. Lista vazia chega como `[]` e é reprovada por `.min(1)` antes de tocar o banco.

Acrescentar `revalidatePath("/")` à lista de revalidações (hoje só `/configuracoes` e `/pipeline`): por D-05 a data sugerida também aparece no dashboard de follow-ups, e ela é recalculada a partir dos intervalos — sem esta linha o dashboard exibiria sugestões calculadas com a configuração antiga até a próxima revalidação por outro caminho.

Não alterar o tipo `ActionState` local, a forma do retorno de erro (`parsed.error.flatten().fieldErrors`) nem a estrutura do upsert.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const s=require('fs').readFileSync('src/actions/configuracoes-actions.ts','utf8');const c=[[/configuracoesServerSchema/.test(s),'usa configuracoesServerSchema'],[/formData\.getAll\(\"intervaloDias\"\)/.test(s),'formData.getAll(intervaloDias)'],[/sequenciaIntervalosDias:\s*intervalos/.test(s),'monta sequenciaIntervalosDias'],[!/configuracoesSchema\.safeParse/.test(s),'nao usa mais configuracoesSchema.safeParse'],[/revalidatePath\(\"\/\"\)/.test(s),'revalidatePath da raiz'],[/revalidatePath\(\"\/configuracoes\"\)/.test(s)&&/revalidatePath\(\"\/pipeline\"\)/.test(s),'revalidacoes pre-existentes mantidas'],[/onConflictDoUpdate/.test(s),'upsert preservado']];const bad=c.filter(x=>!x[0]).map(x=>x[1]);if(bad.length){console.error('FALTANDO: '+bad.join(', '));process.exit(1)}console.log('OK saveConfiguracoes 10-03 T1');"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` termina com exit 0
    - `saveConfiguracoes` chama `formData.getAll("intervaloDias")` e passa o resultado como `sequenciaIntervalosDias` para `configuracoesServerSchema.safeParse`
    - A string `configuracoesSchema.safeParse` não existe mais no arquivo
    - As três chamadas `revalidatePath` presentes são `/configuracoes`, `/pipeline` e `/`
    - O upsert continua sendo `insert(...).onConflictDoUpdate(...)` com `id: 1` e `...parsed.data`, sem chave escrita à mão para a coluna nova
    - Comprovação comportamental (rodar e colar no SUMMARY): um `FormData` com os 3 campos escalares e TRÊS entradas `intervaloDias` = 4, 10 e 20 produz `parsed.data.sequenciaIntervalosDias` igual a `[4,10,20]`; o mesmo `FormData` sem nenhuma entrada `intervaloDias` produz o erro "Adicione ao menos um intervalo."; com uma entrada de valor "0" produz "Mínimo de 1 dia."
  </acceptance_criteria>
  <done>A Server Action lê corretamente N intervalos do formulário, valida server-side pelo contrato autoritativo e revalida as três superfícies afetadas.</done>
</task>

<task type="auto">
  <name>Task 2: Seção "Sequência de reabordagem" com lista dinâmica no formulário de configurações</name>
  <files>src/components/configuracoes-form.tsx</files>
  <read_first>
    - src/components/configuracoes-form.tsx (arquivo inteiro, 163 linhas — em especial o doc-comment do componente, a mecânica `formRef`/`onSubmit`/`useActionState`, os dois `eslint-disable` documentados e a estrutura de um bloco `Field`)
    - .planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-UI-SPEC.md (arquivo inteiro — §Copywriting Contract e §Layout > "Seção 'Sequência de reabordagem'" são contrato vinculante, copiar as cópias LITERALMENTE)
    - .planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-PATTERNS.md §`src/components/configuracoes-form.tsx`
    - src/lib/validations.ts (versão do plano 10-01 — `sequenciaIntervalosSchema`)
    - src/db/queries.ts (`export type Configuracoes`, para o formato de `config.sequenciaIntervalosDias`)
    - src/components/ui/field.tsx (props reais de `Field`/`FieldError`/`FieldDescription`)
  </read_first>
  <action>
Estender `ConfiguracoesForm` com a seção nova. A mecânica de submissão existente (`formRef` + `formAction(new FormData(formRef.current))` + `useActionState`) NÃO muda: a lista dinâmica vive DENTRO do mesmo `<form>` nativo, então os inputs repetidos entram no mesmo `FormData` e um único botão "Salvar configurações" salva tudo junto (`10-UI-SPEC.md` §Layout, último item).

**Estado da lista.** Usar `useState<string[]>` inicializado com `config.sequenciaIntervalosDias.map(String)` — strings, não números, para que o input seja controlado sem perder dígitos parciais durante a digitação. NÃO registrar a lista no `react-hook-form`: o `useForm` continua tipado por `ConfiguracoesFormValues` e com `resolver: zodResolver(configuracoesSchema)` (só os 3 escalares). Isto é load-bearing — se a lista fosse incluída no schema do resolver sem estar registrada como campo do RHF, `form.handleSubmit(onSubmit)` reprovaria para sempre e o botão Salvar pararia de funcionar inteiro.

**Validação da lista no cliente.** Estado local `useState<string | null>` para o erro da lista (ou um array de erros por índice, se preferir granularidade por linha). Dentro de `onSubmit` — que só roda depois de o RHF aprovar os 3 escalares — rodar `sequenciaIntervalosSchema.safeParse(intervalos)` ANTES de chamar `formAction`. Se falhar, gravar as mensagens no estado de erro e RETORNAR sem chamar `formAction` (nada é enviado); se passar, limpar o erro e seguir com `formAction(new FormData(formRef.current))`. Usar o mesmo schema exportado, nunca reescrever as mensagens à mão — o servidor revalida por conta própria em `configuracoesServerSchema` (Task 1), esta checagem é só feedback antecipado, mesma disciplina já documentada no doc-comment do componente.

**Layout da seção** (segundo `10-UI-SPEC.md` §Layout, sem inventar variações):
- Segundo card irmão do card atual, mesmo container visual `rounded-lg border border-zinc-200 bg-white p-6 max-w-md`, empilhado ABAIXO do primeiro. Como os dois precisam estar dentro do MESMO `<form>`, mover o `<form>` para envolver os dois cards (ou envolver os dois cards num wrapper `flex flex-col gap-6` dentro do form) — o card de dias-parado existente não pode mudar de aparência.
- Cabeçalho da seção: título "Sequência de reabordagem" em `text-[20px] font-semibold leading-tight`, seguido do texto de ajuda `text-[14px] text-muted-foreground`: "Intervalos crescentes, em dias, para sugerir quando reabordar um lead frio (Outbound). Leads Inbound nunca recebem esta sugestão."
- Cada linha: `flex items-center gap-2` com `FieldLabel` "Intervalo {N} (dias)" (N = posição atual na lista, 1-based), um `<Input type="number" min={1} step={1} inputMode="numeric" name="intervaloDias" />` controlado (`value` + `onChange` atualizando o array por índice) e um `Button type="button" variant="ghost" size="icon-lg"` com `<Trash2 className="size-4" />`, `aria-label="Remover intervalo {N}"` e `title="Remover intervalo"`. O `name="intervaloDias"` é IDÊNTICO em todas as linhas de propósito — é assim que `formData.getAll("intervaloDias")` recupera a lista no servidor (Task 1). Nunca usar nomes indexados como `intervalo[0]`.
- `FieldDescription` por linha: "Dias após a última interação de WhatsApp para sugerir esta reabordagem."
- Renumeração: os rótulos derivam sempre do índice atual do array. Remover a linha do meio de `[1,2,3]` precisa resultar em rótulos "Intervalo 1" e "Intervalo 2" — nunca "Intervalo 1" e "Intervalo 3".
- Remover linha NÃO abre confirmação — é edição de estado local, só persiste no Salvar.
- Botão "Adicionar intervalo" abaixo da última linha, alinhado à esquerda, `variant="outline"` com `<Plus className="size-4" />` + texto; acrescenta uma linha vazia ao fim e move o foco para o input novo.
- Estado vazio (todas as linhas removidas): renderizar a frase "Nenhum intervalo configurado." em `text-[14px] text-muted-foreground` no lugar das linhas, mantendo o botão "Adicionar intervalo" visível. NÃO desabilitar o botão Salvar — a lista vazia é barrada pela validação, exibindo "Adicione ao menos um intervalo." via `FieldError`.
- Cores: o indicador/rótulos usam os tons neutros já em uso; o único uso de destrutivo é o ícone `Trash2` (`text-[#DC2626]`); o teal `#0D9488` continua reservado ao botão Salvar e ao anel de foco. Nenhum tamanho de fonte, espaçamento ou cor novo fora do inventário do `10-UI-SPEC.md`.

**Chaves de lista:** não usar o índice como `key` do React em linhas removíveis (remover a linha do meio embaralharia o estado de foco/valor das linhas seguintes). Gerar um id estável por linha ao criar/carregar (contador incremental em `useRef` ou `crypto.randomUUID()`), guardando `{ id, valor }` no estado.

Não remover nem afrouxar os `eslint-disable` já documentados (`react-hooks/refs` no `onSubmit`), o `noValidate` do `<form>` (sem ele a validação HTML5 nativa intercepta o submit e as mensagens Zod nunca aparecem — gap já corrigido na quick task 260801-ij4), nem o comportamento de não resetar o formulário após salvar (D-02 da Fase 7).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/components/configuracoes-form.tsx && node -e "const s=require('fs').readFileSync('src/components/configuracoes-form.tsx','utf8');const c=[[s.includes('Sequência de reabordagem'),'titulo da secao'],[s.includes('Intervalos crescentes, em dias, para sugerir quando reabordar um lead frio (Outbound). Leads Inbound nunca recebem esta sugestão.'),'texto de ajuda literal'],[s.includes('Dias após a última interação de WhatsApp para sugerir esta reabordagem.'),'descricao por linha literal'],[s.includes('Adicionar intervalo'),'botao adicionar'],[s.includes('Nenhum intervalo configurado.'),'estado vazio'],[/name=\"intervaloDias\"/.test(s),'name repetido intervaloDias'],[!/intervalo\[\d\]|intervaloDias\$\{/.test(s),'sem nomes indexados'],[/sequenciaIntervalosSchema/.test(s),'valida com o schema compartilhado'],[/zodResolver\(configuracoesSchema\)/.test(s),'resolver continua no schema de 3 campos'],[/Trash2/.test(s)&&/Plus/.test(s),'icones Plus/Trash2'],[/eslint-disable-next-line react-hooks\/refs/.test(s),'eslint-disable preservado'],[/noValidate/.test(s),'noValidate preservado'],[/key=\{[^}]*\.id\}/.test(s),'key estavel por id (nao indice)']];const bad=c.filter(x=>!x[0]).map(x=>x[1]);if(bad.length){console.error('FALTANDO: '+bad.join(', '));process.exit(1)}console.log('OK formulario 10-03 T2');"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exit 0 e `npx eslint src/components/configuracoes-form.tsx` exit 0 (sem novos warnings; os disables documentados continuam no lugar)
    - Todas as cópias do `10-UI-SPEC.md` §Copywriting Contract aparecem literalmente no arquivo: título da seção, texto de ajuda da seção, descrição por linha, "Adicionar intervalo", "Remover intervalo {N}" no `aria-label`, "Nenhum intervalo configurado."
    - Todos os inputs de intervalo têm `name="intervaloDias"` idêntico; não existe nenhum nome indexado
    - O `useForm` continua com `resolver: zodResolver(configuracoesSchema)` e a lista NÃO é registrada no react-hook-form
    - `onSubmit` roda `sequenciaIntervalosSchema.safeParse` e retorna sem chamar `formAction` quando a lista é inválida
    - As linhas usam `key` derivada de um id estável por linha, nunca do índice do array
    - O botão "Salvar configurações" permanece único na página e não foi duplicado por seção
    - Comprovação em navegador registrada no SUMMARY (ver `<verification>` deste plano): salvar 3 intervalos, recarregar e confirmar que os 3 voltam na mesma ordem
  </acceptance_criteria>
  <done>O admin configura uma sequência de N intervalos em `/configuracoes`, com renumeração correta, estado vazio tratado e persistência confirmada após recarregar a página.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Action (`saveConfiguracoes`) | Endpoint HTTP interno: o `FormData` pode ser forjado via DevTools com qualquer quantidade e qualquer conteúdo de entradas `intervaloDias` |
| formulário → coluna JSON | O array digitado pelo admin é serializado como JSON e lido por todas as páginas que calculam sugestão |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-11 | Tampering | `saveConfiguracoes` com payload forjado (array de não-números, negativos, zero) | mitigate | `configuracoesServerSchema.safeParse` (Task 1) valida a FORMA antes de qualquer escrita: array de inteiros >= 1, mínimo 1 item; falha devolve `{errors}` sem tocar o banco. A validação client é só feedback antecipado e nunca é a autoridade |
| T-10-12 | Tampering | Perda silenciosa de dados por `Object.fromEntries` descartando chaves repetidas | mitigate | `formData.getAll("intervaloDias")` explícito (Task 1) + comprovação comportamental de que 3 entradas persistem 3 valores |
| T-10-13 | Denial of Service | Lista com milhares de intervalos inflando a coluna JSON e o cálculo por página | accept | Uso solo, entrada manual linha a linha na UI; o payload realista é de dezenas de itens. Impor teto artificial contradiria D-04 ("sem teto"). Reavaliar só se o uso real mostrar listas absurdas |
| T-10-14 | Tampering | JSON malformado gravado na coluna quebrando a leitura de `configuracoes` em todas as páginas | mitigate | A escrita passa pelo Drizzle (`mode:"json"` serializa com `JSON.stringify`) sobre um `number[]` já validado — nenhuma string crua do cliente chega à coluna |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo nesta fase (`10-RESEARCH.md` §Package Legitimacy Audit: "não aplicável"); nenhum bloco novo do registry shadcn é instalado (`10-UI-SPEC.md` §Registry Safety) |
</threat_model>

<verification>
**Host de 4GB RAM: rodar UM COMANDO DE CADA VEZ, sequencialmente, com o `npm run dev` PARADO durante os gates estáticos.** Nunca encadear com `&`.

1. `npx tsc --noEmit` — exit 0, sem saída
2. `npx eslint src/components/configuracoes-form.tsx src/actions/configuracoes-actions.ts` — exit 0
3. `npm run verify:schema` — exit 0 (a coluna precisa existir de fato para o salvamento funcionar)

<human-check>
Com `npm run dev` rodando (e só ele), no navegador:
1. Abrir `http://localhost:3000/configuracoes` — a seção "Sequência de reabordagem" aparece abaixo do card de dias-parado, com 3 linhas pré-preenchidas (4, 10, 20).
2. Clicar "Adicionar intervalo" — surge uma 4ª linha vazia rotulada "Intervalo 4 (dias)" e o foco vai para ela.
3. Preencher 30, clicar "Salvar configurações" — toast "Configurações salvas.".
4. Recarregar a página (F5) — as 4 linhas voltam com 4, 10, 20, 30 nessa ordem. **Este é o passo que prova que o `formData.getAll` funcionou; se voltar só o último valor, a Task 1 está errada.**
5. Remover a linha do meio (Intervalo 2) — as linhas restantes renumeram para 1, 2, 3 sem buraco.
6. Remover TODAS as linhas — aparece "Nenhum intervalo configurado."; clicar "Salvar configurações" mostra "Adicione ao menos um intervalo." e nada é salvo (recarregar confirma que os valores anteriores continuam lá).
7. Adicionar uma linha com valor 0 e salvar — mensagem "Mínimo de 1 dia.", nada é salvo.
8. Alterar um dos três campos de dias-parado (Novo/Contatado/Negociação) e salvar — o valor persiste após recarregar, provando que a Fase 7 não regrediu.
</human-check>
</verification>

<success_criteria>
- Lista dinâmica funcional em `/configuracoes` com adicionar/remover/renumerar, seguindo o `10-UI-SPEC.md` literalmente em cópia, cores, espaçamento e tipografia
- Três intervalos salvos voltam como três intervalos após recarregar (prova de que `formData.getAll` foi usado)
- Lista vazia e valor menor que 1 são barrados com as mensagens exatas do contrato de UI, sem persistir nada
- Os três campos de dias-parado da Fase 7 continuam salvando pelo mesmo botão, sem regressão
- Nenhum arquivo de schema, action de lead ou componente de exibição foi tocado neste plano
</success_criteria>

<output>
Criar `.planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-03-SUMMARY.md` ao terminar, registrando: a comprovação comportamental do `safeParse` da Task 1 (3 entradas → `[4,10,20]`), o resultado de cada um dos 8 passos do `<human-check>` (ou o motivo exato caso o navegador não esteja disponível nesta sessão) e qualquer desvio do `10-UI-SPEC.md` com sua justificativa.
</output>
