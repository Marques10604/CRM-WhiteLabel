---
phase: 10-sequ-ncia-de-follow-up-escalonada
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/actions/lead-actions.ts
  - scripts/verify-sequencia-posicao.cjs
  - package.json
autonomous: true
requirements: [SEQ-02]

must_haves:
  truths:
    - "Clicar em 'Abrir WhatsApp' com um template do tipo follow_up avança a posição do lead na sequência em exatamente 1"
    - "Clicar em 'Abrir WhatsApp' com template primeiro_contato ou prova_valor NUNCA altera a posição do lead na sequência"
    - "O avanço da posição acontece também quando a guarda de corrida barra o auto-avanço de etapa (o lead foi movido por drag entre o SELECT e o UPDATE) — nenhuma tentativa de follow-up é perdida"
    - "Mover um lead para a etapa 'novo' (por drag no board ou pelo dropdown do formulário de edição) zera a posição na sequência"
    - "Mover um lead para qualquer etapa que não seja 'novo' preserva a posição atual na sequência"
    - "Nenhum write-path lê `origemTipo` para decidir o avanço — o gate Inbound vive apenas no cálculo da sugestão"
  artifacts:
    - path: "src/actions/lead-actions.ts"
      provides: "Avanço condicional de sequenciaPosicao em registerWhatsAppContact (2 branches) e reset em updateLeadStage/updateLead"
      contains: "sequenciaPosicao"
    - path: "scripts/verify-sequencia-posicao.cjs"
      provides: "Guarda de regressão do avanço/reset: tabela-verdade em :memory: + checagens estruturais no fonte real"
      contains: "sequencia_posicao"
    - path: "package.json"
      provides: "Entrada npm verify:sequencia ao lado das demais verificações"
      contains: "verify:sequencia"
  key_links:
    - from: "src/actions/lead-actions.ts (registerWhatsAppContact)"
      to: "leads.sequenciaPosicao"
      via: "spread condicional dentro do .set() da transação já existente"
      pattern: "sequenciaPosicao: sql`\\$\\{leads\\.sequenciaPosicao\\} \\+ 1`"
    - from: "src/actions/lead-actions.ts (updateLeadStage e updateLead)"
      to: "leads.sequenciaPosicao"
      via: "spread condicional-por-valor-alvo quando stage === 'novo'"
      pattern: "\\{ sequenciaPosicao: 0 \\}"
---

<objective>
Fazer a posição do lead na sequência andar sozinha: avançar um degrau a cada clique real em "Abrir WhatsApp" com template de follow-up (D-01) e voltar ao início quando o lead retorna para a etapa "Novo" (D-02/D-12) — tudo dentro dos write-paths que já existem, sem função nova e sem uma segunda transação.

Purpose: sem isto, `sequenciaPosicao` fica congelado em 0 para sempre e a sugestão calculada no plano 10-01 sempre apontaria o primeiro degrau, nunca escalonando (SEQ-02 vira letra morta).
Output: três pontos de escrita estendidos com o idioma condicional já estabelecido no arquivo, mais uma guarda de regressão permanente.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-CONTEXT.md
@.planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-RESEARCH.md
@.planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-PATTERNS.md

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código atual — não explorar o repositório para redescobrir. -->

`src/db/schema.ts` (após o plano 10-01):
- `leads.sequenciaPosicao` = `integer("sequencia_posicao").notNull().default(0)`, posicionada entre `contactAttempts` e `importBatchId`

`src/actions/lead-actions.ts` (estado atual, arquivo de 327 linhas):
- Imports (linha 10): `import { and, eq, isNotNull, isNull, sql } from "drizzle-orm";` — `sql` JÁ está importado, nenhum import novo é necessário
- `updateLead` (linhas 89-147): `.set({ ...parsed.data, motivoPerda: parsed.data.stage === "perdido" ? parsed.data.motivoPerda : null, ...(stageChanged ? { stageChangedAt: new Date() } : {}) })`
- `updateLeadStage` (linhas 156-192): `.set({ stage: parsed.data.stage, motivoPerda: parsed.data.stage === "perdido" ? parsed.data.motivoPerda : null, ...(stageChanged ? { stageChangedAt: new Date() } : {}) })`
- `registerWhatsAppContact(leadId, tipo, texto)` (linhas 233-287): assinatura posicional; `let advanced = parsed.data.tipo === "primeiro_contato" && current.stage === "novo";` (linha 251); `db.transaction` com DOIS `.set()` de `leads`:
  - branch principal (linhas 255-262): `.set({ contactAttempts: sql\`${leads.contactAttempts} + 1\`, ...(advanced ? { stage: "contatado", stageChangedAt: new Date() } : {}) })` com `.returning({ id: leads.id })`
  - branch de fallback de corrida (linhas 270-273): `.set({ contactAttempts: sql\`${leads.contactAttempts} + 1\` })`, executado quando `advanced && updated.length === 0`
  - insert incondicional em `interacoes` no fim da transação
- `Template["tipo"]` = `"primeiro_contato" | "follow_up" | "prova_valor"`

`scripts/verify-wa-contact-invariant.cjs` (125 linhas) — molde da guarda nova: banco `:memory:` puro (nunca `data/crm.db`), reproduz em miniatura a tabela `leads` e o MESMO idioma de escrita da Server Action real, varre a tabela-verdade completa (3 tipos x 5 etapas) e sai com exit != 0 na primeira violação.

`scripts/test-lead-actions.cjs` (linhas 33-70) — molde das checagens estruturais estáticas: lê o fonte de `lead-actions.ts`, fatia o corpo de cada função por `indexOf("export async function X")` e faz asserções por regex sobre a fatia.

`package.json` §scripts (estado atual): `guard:no-hard-delete`, `verify:schema`, `verify:origem-tipo`, `test:lead-actions`, `test:interacao-actions`, `test:mutation-guard`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Avanço da posição em registerWhatsAppContact (D-01), nos dois branches da transação</name>
  <files>src/actions/lead-actions.ts</files>
  <read_first>
    - src/actions/lead-actions.ts (linhas 194-287 — doc-comment de `registerWhatsAppContact` e o corpo inteiro da função, incluindo os DOIS `.set()` e o insert em `interacoes`)
    - src/db/schema.ts (versão do plano 10-01 — comentário da coluna `sequenciaPosicao`)
    - .planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-PATTERNS.md §`src/actions/lead-actions.ts` (bloco "D-01 avanço pattern")
    - .planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-RESEARCH.md §Pattern 3, §Anti-Patterns to Avoid e §Common Pitfalls 4
  </read_first>
  <action>
Em `registerWhatsAppContact`, declarar, logo depois da linha de `let advanced = ...`, uma variável independente `const avancaSequencia = parsed.data.tipo === "follow_up";`. Ela é independente de `advanced` de propósito: o auto-avanço de etapa e o avanço de sequência são regras diferentes que só por acaso compartilham o mesmo clique.

Acrescentar o spread `...(avancaSequencia ? { sequenciaPosicao: sql\`${leads.sequenciaPosicao} + 1\` } : {})` nos DOIS `.set()` de `leads` dentro de `db.transaction`: no branch principal (o que tem `.returning({ id: leads.id })`) e no branch de fallback de corrida (o que roda quando `advanced && updated.length === 0`). Omitir o fallback perderia o avanço exatamente nos casos em que o lead foi arrastado por outra ação entre o SELECT e o UPDATE — o mesmo motivo pelo qual `contactAttempts` já é repetido nos dois branches hoje. Posicionar o spread imediatamente após `contactAttempts` nos dois casos, antes do spread de `advanced`.

Regras que NÃO podem ser violadas nesta task:
- NÃO capar o incremento pelo tamanho da lista de intervalos. Capar exigiria ler `configuracoes` dentro da transação, e `computeSequenciaSugestao` (plano 10-01) já trata índice fora dos limites como ausência de sugestão (D-10).
- NÃO ler `leads.origemTipo` nem adicionar qualquer condicional de Inbound/Outbound aqui. O gate ORIGEM-03 é de LEITURA e vive num ponto único (`computeSequenciaSugestao`) — duplicá-lo aqui é o Pitfall 4 do RESEARCH. Um lead Inbound que receba um clique de follow_up pode ter a posição incrementada sem consequência, porque esse valor nunca é lido para exibição.
- NÃO criar uma segunda transação nem um `db.update` fora do bloco `db.transaction` existente.
- NÃO alterar o gate `advanced`, o `stageGuard`, o `.returning()`, o insert em `interacoes` nem a lista de `revalidatePath`.

Estender o doc-comment da função com um parágrafo citando D-01/SEQ-02: por que só `follow_up` avança (primeiro contato inaugura a relação e não é reabordagem; prova de valor é material de apoio, não um degrau da cadência), por que a variável é independente de `advanced`, e por que o gate Inbound NÃO mora aqui.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const fs=require('fs');const s=fs.readFileSync('src/actions/lead-actions.ts','utf8');const i=s.indexOf('export async function registerWhatsAppContact');if(i<0){console.error('funcao nao encontrada');process.exit(1)}const body=s.slice(i);const inc=(body.match(/sequenciaPosicao: sql`\\\$\{leads\.sequenciaPosicao\} \+ 1`/g)||[]).length;if(inc!==2){console.error('esperado 2 incrementos de sequenciaPosicao (branch principal + fallback), encontrado '+inc);process.exit(1)}if(!/const avancaSequencia = parsed\.data\.tipo === \"follow_up\"/.test(body)){console.error('falta const avancaSequencia com tipo follow_up');process.exit(1)}if(/origemTipo/.test(body)){console.error('origemTipo lido dentro de registerWhatsAppContact — Pitfall 4 do RESEARCH');process.exit(1)}const tx=(body.match(/db\.transaction\(/g)||[]).length;if(tx!==1){console.error('esperado exatamente 1 db.transaction, encontrado '+tx);process.exit(1)}console.log('OK avanco 10-02 T1');"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` termina com exit 0
    - O corpo de `registerWhatsAppContact` contém exatamente 2 ocorrências de `sequenciaPosicao: sql\`${leads.sequenciaPosicao} + 1\`` (branch principal e branch de fallback)
    - O corpo contém `const avancaSequencia = parsed.data.tipo === "follow_up";` e usa essa variável nos dois spreads
    - O corpo NÃO contém a string `origemTipo` em nenhum lugar
    - Continua existindo exatamente 1 `db.transaction(` na função, e o insert em `interacoes` segue incondicional dentro dela
    - O gate `let advanced = parsed.data.tipo === "primeiro_contato" && current.stage === "novo";` está literalmente inalterado
  </acceptance_criteria>
  <done>Um clique de follow-up avança a posição na sequência dentro da transação já existente, inclusive no caminho de corrida, sem tocar em nenhuma regra pré-existente.</done>
</task>

<task type="auto">
  <name>Task 2: Reset da posição ao voltar para a etapa "novo" (D-02/D-12), em updateLeadStage e updateLead</name>
  <files>src/actions/lead-actions.ts</files>
  <read_first>
    - src/actions/lead-actions.ts (linhas 89-192 — corpos completos de `updateLead` e `updateLeadStage`, com atenção ao contraste entre `motivoPerda` (condicional pelo VALOR-ALVO) e `stageChangedAt` (condicional pela MUDANÇA))
    - .planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-PATTERNS.md §"Value-target-conditional write idiom (not changed-conditional)"
    - .planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-RESEARCH.md §Pattern 3
    - .planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-01-PLAN.md §`<decisoes_do_planner>` (D-12 — o reset vale para as duas funções, decisão já tomada, não reabrir)
  </read_first>
  <action>
Adicionar o spread `...(parsed.data.stage === "novo" ? { sequenciaPosicao: 0 } : {})` dentro do `.set()` de `updateLeadStage` E dentro do `.set()` de `updateLead`, posicionado imediatamente após a linha de `motivoPerda` e antes do spread de `stageChangedAt` nas duas funções.

Usar deliberadamente o idioma condicional-por-VALOR-ALVO (`parsed.data.stage === "novo"`), o mesmo de `motivoPerda` na linha acima — e NÃO o idioma condicional-por-MUDANÇA (`stageChanged ? ... : {}`) usado por `stageChangedAt`. Motivo: resetar para 0 um lead que já está em "novo" é um no-op idempotente e seguro, e amarrar o reset a `stageChanged` acrescentaria um caso de borda (lead salvo em "novo" sem trocar de etapa) sem nenhum ganho.

Em `updateLead`, o spread precisa vir DEPOIS do `...parsed.data` para não ser sobrescrito por ele (`parsed.data` não contém `sequenciaPosicao`, mas a ordem correta é o que garante isso caso `leadSchema` cresça no futuro).

Comentar as duas ocorrências citando D-02/D-12: o ciclo de reabordagem reinicia porque o lead esfriou e voltou ao início do funil; vale tanto para o gesto de arrastar no board quanto para a edição manual da etapa no formulário, porque a justificativa de produto é o DESTINO, não o mecanismo — mesmo tratamento que `motivoPerda` já recebe hoje nas duas funções. Registrar também que NÃO há reset ao fechar/perder (D-02: fora do funil ativo a posição simplesmente para de ser lida).

Não alterar `stageChanged`, os SELECTs frescos, os `try/catch` de FK, nem as listas de `revalidatePath`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const fs=require('fs');const s=fs.readFileSync('src/actions/lead-actions.ts','utf8');const ul=s.indexOf('export async function updateLead');const uls=s.indexOf('export async function updateLeadStage');const rw=s.indexOf('export async function registerWhatsAppContact');if(ul<0||uls<0||rw<0){console.error('funcoes nao encontradas');process.exit(1)}const bodyUpdateLead=s.slice(ul,uls);const bodyUpdateStage=s.slice(uls,rw);const pat=/\.\.\.\(parsed\.data\.stage === \"novo\" \? \{ sequenciaPosicao: 0 \} : \{\}\)/;if(!pat.test(bodyUpdateLead)){console.error('reset ausente em updateLead');process.exit(1)}if(!pat.test(bodyUpdateStage)){console.error('reset ausente em updateLeadStage');process.exit(1)}for(const [n,b] of [['updateLead',bodyUpdateLead],['updateLeadStage',bodyUpdateStage]]){const mp=b.indexOf('motivoPerda:');const sp=b.search(pat);const sc=b.indexOf('stageChangedAt: new Date()');if(!(mp<sp&&sp<sc)){console.error(n+': ordem errada no .set() (esperado motivoPerda -> sequenciaPosicao -> stageChangedAt)');process.exit(1)}if(/stageChanged \? \{ sequenciaPosicao/.test(b)){console.error(n+': reset amarrado a stageChanged (idioma errado)');process.exit(1)}}console.log('OK reset 10-02 T2');"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` termina com exit 0
    - `updateLead` e `updateLeadStage` contêm, cada uma, exatamente uma ocorrência de `...(parsed.data.stage === "novo" ? { sequenciaPosicao: 0 } : {})` dentro do `.set()`
    - Nas duas funções, a ordem dentro do `.set()` é `motivoPerda` → `sequenciaPosicao` → `stageChangedAt`
    - Nenhuma das duas amarra o reset a `stageChanged` (a string `stageChanged ? { sequenciaPosicao` não existe no arquivo)
    - `stageChangedAt` continua condicional por `stageChanged` nas duas funções (comportamento pré-existente intacto)
    - `npm run test:lead-actions` termina com exit 0 (nenhum caso pré-existente quebrado)
  </acceptance_criteria>
  <done>Mover um lead de volta para "Novo", por drag ou por formulário, zera a posição na sequência; qualquer outro destino a preserva.</done>
</task>

<task type="auto">
  <name>Task 3: Guarda de regressão permanente do avanço e do reset</name>
  <files>scripts/verify-sequencia-posicao.cjs, package.json</files>
  <read_first>
    - scripts/verify-wa-contact-invariant.cjs (arquivo inteiro, 125 linhas — molde do harness `:memory:`, do cabeçalho documental e do padrão de asserção/exit)
    - scripts/test-lead-actions.cjs (linhas 22-70 — `check(condition, message)` e `staticCheckFkWiring()` como molde das asserções estruturais sobre o fonte)
    - src/actions/lead-actions.ts (versão resultante das Tasks 1 e 2)
    - package.json (bloco `scripts`, para inserir a entrada nova ao lado das demais verificações)
  </read_first>
  <action>
Criar `scripts/verify-sequencia-posicao.cjs` com `"use strict"` e cabeçalho documental no mesmo tom de `verify-wa-contact-invariant.cjs` (o que prova, por que existe, como rodar, semântica do exit code). Duas partes, ambas somando no mesmo contador de falhas e terminando com `process.exit(failed > 0 ? 1 : 0)`:

**Parte A — tabela-verdade comportamental em `:memory:`** (nunca abrir `data/crm.db`). Criar uma tabela `leads` em miniatura com `id INTEGER PRIMARY KEY, stage TEXT NOT NULL, contact_attempts INTEGER NOT NULL DEFAULT 0, sequencia_posicao INTEGER NOT NULL DEFAULT 0` e reproduzir o MESMO idioma de escrita das Server Actions reais:
- `applyClick(db, leadId, tipo)`: incrementa `contact_attempts` sempre; incrementa `sequencia_posicao` só quando `tipo === "follow_up"`.
- `applyStage(db, leadId, stage)`: grava `stage` e zera `sequencia_posicao` só quando `stage === "novo"`.
Asserções obrigatórias:
1. Os 3 tipos x 5 etapas (15 pares): `sequencia_posicao` incrementa em exatamente 5 dos 15 (os de `follow_up`, em qualquer etapa) e fica intacta nos outros 10 — provando que o avanço é ortogonal à etapa e ao gate de auto-avanço.
2. Três cliques de `follow_up` seguidos levam a posição de 0 a 3 (acumula, não satura).
3. `applyStage(..., "novo")` depois desses 3 cliques devolve a posição a 0; `applyStage` para `contatado`, `negociacao`, `fechado` e `perdido` preserva a posição (4 asserções).
4. `applyStage(..., "novo")` sobre um lead com posição já 0 continua 0 (idempotência do reset).

**Parte B — checagens estruturais no fonte real** `src/actions/lead-actions.ts` (mesmo idioma de `staticCheckFkWiring`, fatiando o arquivo por `indexOf("export async function ...")`). Asserções obrigatórias:
1. O corpo de `registerWhatsAppContact` contém 2 ocorrências do incremento `sequenciaPosicao: sql\`${leads.sequenciaPosicao} + 1\`` (branch principal + fallback de corrida).
2. O corpo de `registerWhatsAppContact` NÃO contém a string `origemTipo` (Pitfall 4: o gate Inbound é de leitura, nunca de escrita).
3. Os corpos de `updateLead` e de `updateLeadStage` contêm, cada um, o spread `...(parsed.data.stage === "novo" ? { sequenciaPosicao: 0 } : {})`.
4. Nenhum dos três corpos contém `stageChanged ? { sequenciaPosicao` (idioma condicional-por-mudança é o errado para este campo).
As regexes precisam ser tolerantes a espaçamento variável (usar `\s*` entre tokens) para não quebrar em reformatação — mesma lição registrada no `STATE.md` sobre a reescrita de `verify-origem-tipo.cjs` (quick task 260807-uit).

Adicionar ao bloco `scripts` do `package.json` a entrada `"verify:sequencia": "node scripts/verify-sequencia-posicao.cjs"`, logo após `"verify:origem-tipo"`. Não alterar nenhuma outra entrada.

Antes de considerar a task pronta, provar a guarda por MUTAÇÃO sem nunca escrever no arquivo-fonte real: copiar `src/actions/lead-actions.ts` para um arquivo temporário em `os.tmpdir()`, remover um dos incrementos de `sequenciaPosicao` na cópia, rodar a Parte B apontada para essa cópia (via variável de ambiente de caminho ou execução ad-hoc equivalente) e confirmar exit != 0. Registrar o resultado no SUMMARY. Este é o mesmo procedimento adotado na Fase 08-03 para `verify-origem-tipo.cjs`, e existe para que uma interrupção no meio do teste nunca deixe `lead-actions.ts` quebrado em disco.
  </action>
  <verify>
    <automated>npm run verify:sequencia && node -e "const p=require('./package.json');if(!p.scripts['verify:sequencia']){console.error('entrada npm verify:sequencia ausente');process.exit(1)}const fs=require('fs');const g=fs.readFileSync('scripts/verify-sequencia-posicao.cjs','utf8');if(/crm\.db/.test(g)){console.error('a guarda referencia data/crm.db — precisa ser :memory: puro');process.exit(1)}if(!/:memory:/.test(g)){console.error('falta banco :memory:');process.exit(1)}if(!/lead-actions\.ts/.test(g)){console.error('falta a parte B (checagem estrutural do fonte real)');process.exit(1)}console.log('OK guarda 10-02 T3');"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify:sequencia` termina com exit 0 e imprime uma linha OK por asserção
    - `scripts/verify-sequencia-posicao.cjs` abre apenas `:memory:` e nunca referencia `data/crm.db`
    - A Parte A cobre os 15 pares tipo×etapa, o acúmulo de 3 cliques, o reset por destino "novo", a preservação nos outros 4 destinos e a idempotência do reset
    - A Parte B falha (exit != 0) quando um dos incrementos de `sequenciaPosicao` é removido — comprovado por teste de mutação numa CÓPIA em `os.tmpdir()`, com o resultado colado no SUMMARY, e `src/actions/lead-actions.ts` idêntico ao commit no fim da task
    - `package.json` contém `"verify:sequencia": "node scripts/verify-sequencia-posicao.cjs"` e nenhuma outra entrada de script foi alterada
  </acceptance_criteria>
  <done>Existe uma guarda permanente que reprova a remoção acidental do avanço ou do reset, e a própria guarda foi provada por mutação sem risco para o arquivo-fonte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Action | `registerWhatsAppContact`/`updateLeadStage`/`updateLead` são endpoints HTTP internos: argumentos podem ser forjados ou reproduzidos via DevTools |
| escrita concorrente → linha de `leads` | Um drag no board e um clique de WhatsApp no mesmo lead podem colidir entre o SELECT fresco e o UPDATE |
| repositório → guarda de convenção | `scripts/verify-sequencia-posicao.cjs` passa a ser a única barreira automatizada contra a remoção silenciosa do avanço/reset |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Tampering | `registerWhatsAppContact` chamado direto com `tipo="follow_up"` repetidamente para inflar `sequenciaPosicao` | accept | Ferramenta single-admin sem autenticação por decisão de produto (`PROJECT.md`); o pior efeito é a própria posição do admin avançar e a sugestão sumir (D-10), sem perda de dado nem envio de mensagem — o `safeParse` de `whatsappContactSchema` já rejeita `tipo` fora do enum e `texto` vazio |
| T-10-07 | Tampering | Perda de escrita sob corrida (drag simultâneo ao clique) | mitigate | O incremento é repetido no branch de fallback da transação (Task 1), exatamente como `contactAttempts` já é hoje — nenhuma tentativa de follow-up deixa de contar quando a guarda de etapa barra o auto-avanço |
| T-10-08 | Elevation of Privilege | Lead Inbound recebendo tratamento de sequência por um write-path | accept | Deliberado (Pitfall 4 do RESEARCH): o gate ORIGEM-03 é de leitura e vive só em `computeSequenciaSugestao`; incrementar a posição de um lead Inbound é inofensivo porque esse valor nunca é lido para exibição. A guarda da Task 3 reprova a introdução de leitura de `origemTipo` aqui |
| T-10-09 | Repudiation | Avanço de posição sem rastro na timeline | mitigate | O insert incondicional em `interacoes` dentro da mesma `db.transaction()` (Fase 9) permanece intocado — todo avanço tem um evento correspondente com data e texto |
| T-10-10 | Tampering | SQL injection via `sql\`${leads.sequenciaPosicao} + 1\`` | mitigate | O template `sql` interpola uma REFERÊNCIA DE COLUNA tipada do Drizzle, nunca string de usuário; nenhuma concatenação livre é introduzida |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo nesta fase (`10-RESEARCH.md` §Package Legitimacy Audit: "não aplicável"); se um `npm install` surgir, o checkpoint humano bloqueante de legitimidade volta a ser obrigatório |
</threat_model>

<verification>
**Host de 4GB RAM: rodar UM COMANDO DE CADA VEZ, sequencialmente, com o `npm run dev` PARADO.** Nunca encadear com `&`.

1. `npx tsc --noEmit` — exit 0, sem saída
2. `npm run verify:sequencia` — exit 0
3. `node scripts/verify-wa-contact-invariant.cjs` — exit 0 (WA-06/07/08 intactos)
4. `npm run test:lead-actions` — exit 0
5. `npm run test:interacao-actions` — exit 0
6. `npm run guard:no-hard-delete` — exit 0
</verification>

<success_criteria>
- `sequenciaPosicao` incrementa em exatamente 1 por clique de `follow_up`, nos dois branches da transação, e nunca nos outros dois tipos de template
- `sequenciaPosicao` volta a 0 quando o destino da etapa é "novo", por drag ou por formulário, e é preservada em qualquer outro destino
- Nenhuma leitura de `origemTipo` foi introduzida em `registerWhatsAppContact`
- `npm run verify:sequencia` existe, passa, e foi provado por mutação numa cópia temporária
- Nenhum arquivo de UI e nenhuma query de leitura foram tocados neste plano
</success_criteria>

<output>
Criar `.planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-02-SUMMARY.md` ao terminar, registrando: os 3 pontos de escrita alterados com o trecho final de cada `.set()`, a saída de `npm run verify:sequencia`, e o resultado do teste de mutação da Parte B (incluindo a confirmação de que o fonte real ficou intacto).
</output>
