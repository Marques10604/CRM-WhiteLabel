---
phase: 10-sequ-ncia-de-follow-up-escalonada
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/lib/validations.ts
  - scripts/test-lead-actions.cjs
  - scripts/migrate-sequencia-followup.cjs
  - scripts/verify-schema.cjs
  - src/db/queries.ts
autonomous: true
requirements: [SEQ-01, SEQ-02, ORIGEM-03]

must_haves:
  truths:
    - "A coluna `leads.sequencia_posicao` existe no banco vivo (data/crm.db) com DEFAULT 0 NOT NULL e 0 valores NULL, sem perder nenhuma linha de leads"
    - "A coluna `configuracoes.sequencia_intervalos_dias` existe no banco vivo com DEFAULT '[4,10,20]' NOT NULL e a linha singleton id=1 contém um array JSON de inteiros positivos"
    - "Um lead com origemTipo='inbound' nunca produz data sugerida, qualquer que seja sua posição na sequência ou sua última interação (ORIGEM-03)"
    - "Um lead cuja posição excede o último degrau configurado não produz data sugerida (D-10) — ausência silenciosa, sem erro nem exceção"
    - "Um lead sem nenhuma interação de WhatsApp registrada na timeline não produz data sugerida (D-09)"
    - "Um lead nas etapas terminais fechado/perdido nunca produz data sugerida"
    - "A validação server-side rejeita uma lista de intervalos vazia e rejeita qualquer intervalo menor que 1 dia"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Colunas leads.sequenciaPosicao (integer) e configuracoes.sequenciaIntervalosDias (text mode json, number[])"
      contains: "sequencia_intervalos_dias"
    - path: "src/lib/validations.ts"
      provides: "sequenciaIntervalosSchema e configuracoesServerSchema (derivado de configuracoesSchema por .extend)"
      contains: "configuracoesServerSchema"
    - path: "scripts/migrate-sequencia-followup.cjs"
      provides: "Migração manual idempotente via better-sqlite3, com backup prévio e verificação pós-migração"
      contains: "PRAGMA table_info"
    - path: "src/db/queries.ts"
      provides: "getUltimaInteracaoWhatsAppPorLead (agregação SQL) e computeSequenciaSugestao (função pura com todos os gates)"
      exports: ["getUltimaInteracaoWhatsAppPorLead", "computeSequenciaSugestao"]
    - path: "scripts/verify-schema.cjs"
      provides: "Gate permanente de presença das duas colunas novas no banco vivo"
      contains: "sequencia_posicao"
  key_links:
    - from: "src/db/queries.ts (computeSequenciaSugestao)"
      to: "leads.origemTipo"
      via: "gate ORIGEM-03 como primeira condição da função"
      pattern: "origemTipo !== \"outbound\""
    - from: "src/db/queries.ts (getUltimaInteracaoWhatsAppPorLead)"
      to: "src/db/schema.ts (interacoes)"
      via: "agregação max(created_at) group by lead_id excluindo nota_manual e soft-deletadas"
      pattern: "groupBy\\(interacoes\\.leadId\\)"
    - from: "scripts/migrate-sequencia-followup.cjs"
      to: "data/crm.db"
      via: "ALTER TABLE guardado por PRAGMA table_info, nunca drizzle-kit push"
      pattern: "ALTER TABLE"
---

<objective>
Construir toda a fundação de dados da sequência de follow-up escalonada: as duas colunas novas no schema Drizzle, os contratos Zod server-side, a aplicação real no banco vivo via script de migração manual commitado ([BLOCKING]), e a camada de leitura que calcula a próxima data sugerida — incluindo todos os gates (ORIGEM-03, D-09, D-10, etapas terminais).

Purpose: nada mais da Fase 10 pode ser construído sem estas colunas existirem de fato no banco. `npx tsc --noEmit` e `npm run build` passam mesmo sem a migração aplicada (os tipos vêm de `schema.ts`, não do banco), então pular a migração cria um falso-positivo de verificação para a fase inteira — este projeto já viveu esse padrão duas vezes (Fases 06-01 e 07-01).
Output: colunas vivas em `data/crm.db`, contratos Zod estendidos, função pura `computeSequenciaSugestao` testável e query de agregação da última interação.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-CONTEXT.md
@.planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-RESEARCH.md
@.planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-PATTERNS.md

<decisoes_do_planner>
<!-- Duas Open Questions do 10-RESEARCH.md foram resolvidas aqui pelo planner. NÃO revisitar durante a execução; registrar as duas no SUMMARY como decisões da fase. -->

**D-11 (resolve Open Question 1 do 10-RESEARCH.md) — o valor semente de `sequenciaIntervalosDias` é `[4,10,20]`, não `[]`.**
Motivo técnico decisivo (não preferência): o `10-UI-SPEC.md` §Copywriting trava a validação de lista vazia como erro bloqueante ("Adicione ao menos um intervalo."). Com semente `[]`, a primeira visita a `/configuracoes` ficaria com a lista vazia e o admin não conseguiria salvar NEM os três campos de dias-parado já existentes (CONFIG-01/02, Fase 7) até adicionar um intervalo — uma regressão silenciosa numa funcionalidade já entregue. `[4,10,20]` é o mesmo exemplo que o próprio `10-CONTEXT.md` §Phase Boundary usa, mantém a validação `min(1)` intacta e a sugestão resultante é puramente informativa (D-06: nunca sobrescreve `followUpDate`, nunca envia nada), então semear valores reais não dispara nenhum efeito colateral destrutivo no dia da migração.

**D-12 (resolve Open Question 2 do 10-RESEARCH.md) — o reset de `sequenciaPosicao` vale para `updateLeadStage` E `updateLead`.**
D-02 descreve o gesto de arrastar, mas descreve o MOTIVO pelo destino ("voltou ao início do funil"), não pelo mecanismo. `motivoPerda` já é tratado assim hoje nas duas funções, sem distinção de mecanismo. Implementar em ambas (plano 10-02).
</decisoes_do_planner>

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código atual — não explorar o repositório para redescobrir. -->

Já existente em `src/db/schema.ts`:
- `leads` tem hoje 18 colunas físicas; `contactAttempts: integer("contact_attempts").notNull().default(0)` é a última coluna do bloco semântico de tracking, logo antes de `importBatchId`
- `configuracoes` tem hoje 5 colunas físicas: `id`, `dias_parado_novo`, `dias_parado_contatado`, `dias_parado_negociacao`, `updated_at`
- `interacoes.tipo` = enum `["primeiro_contato","follow_up","prova_valor","nota_manual"]`; `interacoes.createdAt` é `integer(..., {mode:"timestamp"})` com default `sql\`(unixepoch())\`` (segundos, não milissegundos)
- Índice já existente: `interacoes_lead_id_idx`

Já existente em `src/lib/validations.ts` (linhas 126-138):
- `configuracoesSchema = z.object({ diasParadoNovo, diasParadoContatado, diasParadoNegociacao })`, cada um `z.coerce.number().int().min(1, "Mínimo de 1 dia.")`
- `export type ConfiguracoesFormValues = z.input<typeof configuracoesSchema>;`
- Idioma de derivação em camada já usado no arquivo: `interacaoManualSchema = notaManualTextoSchema.extend({...})`

Já existente em `src/db/queries.ts`:
- Imports linha 1-5: `import { and, asc, eq, isNull, notInArray } from "drizzle-orm"` / `import { addDays, isBefore, isToday, startOfDay } from "date-fns"` / `db` / `{ configuracoes, leads }` / `type { Lead }`
- `export type Configuracoes = typeof configuracoes.$inferSelect;` (linha 57) — absorve a coluna nova automaticamente
- `groupLeadsByUrgency` (linhas 36-55) é o modelo de função pura/testável com doc-comment

Já existente em `scripts/test-lead-actions.cjs` (linhas 100-105):
```
const manualAlters = [
  "ALTER TABLE `leads` ADD `import_batch_id` text;",
  "ALTER TABLE `leads` ADD `contact_attempts` integer DEFAULT 0 NOT NULL;",
  "ALTER TABLE `leads` ADD `origem_tipo` text DEFAULT 'outbound' NOT NULL;",
  "ALTER TABLE `subnichos` ADD `deleted_at` integer;",
];
```
Este array reconstrói no banco temporário as colunas aplicadas por ALTER TABLE manual (nunca viraram arquivo .sql). `countLeads()` faz `db.select().from(leads)` listando TODAS as colunas do schema — sem a entrada nova o harness quebra com "no such column".

Já existente em `scripts/verify-schema.cjs`:
- `requiredTables = ["leads","subnichos","interacoes"]`, `requiredIndexes = [...]`
- Bloco de conjunto ESTRITO de colunas só para `interacoes` (linhas 46-74), com `fail()` listando faltantes/extras
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Colunas Drizzle novas, contratos Zod derivados e bootstrap do harness de leads</name>
  <files>src/db/schema.ts, src/lib/validations.ts, scripts/test-lead-actions.cjs</files>
  <read_first>
    - src/db/schema.ts (arquivo inteiro, 127 linhas — bloco `leads` linhas 32-65 e bloco `configuracoes` linhas 101-126, incluindo o doc-comment de defaults não-simétricos)
    - src/lib/validations.ts (linhas 110-139 — `interacaoManualSchema`/`interacaoManualUpdateSchema` como idioma de derivação por `.extend`, e o bloco `configuracoesSchema` com seu doc-comment)
    - scripts/test-lead-actions.cjs (linhas 85-125 — bloco `manualAlters` e o comentário que explica por que ele existe)
    - .planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-PATTERNS.md §`src/db/schema.ts` e §`src/lib/validations.ts`
    - .planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-RESEARCH.md §Pattern 1 e §Pattern 2
  </read_first>
  <action>
Em `src/db/schema.ts`:

1. Adicionar em `leads`, IMEDIATAMENTE após a linha de `contactAttempts` e antes de `importBatchId`, a coluna `sequenciaPosicao: integer("sequencia_posicao").notNull().default(0)`. Comentário inline no mesmo estilo denso do comentário de `origemTipo`/`contactAttempts`, citando: índice (0-based) do PRÓXIMO degrau da sequência; avança em `registerWhatsAppContact` quando o template é `follow_up` (D-01); reseta para 0 quando o destino da etapa é "novo" (D-02/D-12); nunca é capado no write-path — quem trata "sequência esgotada" é `computeSequenciaSugestao` (D-10); o DEFAULT 0 físico é exigido pelo SQLite no `ALTER TABLE ADD COLUMN NOT NULL` sobre tabela populada.

2. Adicionar em `configuracoes`, entre `diasParadoNegociacao` e `updatedAt`, a coluna `sequenciaIntervalosDias: text("sequencia_intervalos_dias", { mode: "json" }).$type<number[]>().notNull().default(sql\`'[4,10,20]'\`)`. É o primeiro uso de `text(mode:"json")` neste schema — comentar isso, mais: por que uma coluna JSON única e não uma tabela `sequencia_intervalos` (D-08: sequência é global/singleton, editada e salva como unidade única, sem consulta por degrau), e por que a semente é `[4,10,20]` e não `[]` (D-11 do bloco `<decisoes_do_planner>` deste plano — semear vazio travaria o salvamento dos 3 campos de dias-parado já existentes, porque a validação de lista vazia é bloqueante por `10-UI-SPEC.md`). O literal do default precisa ser exatamente `'[4,10,20]'` (sem espaços) para bater byte-a-byte com o que `JSON.stringify([4,10,20])` produz e com o DEFAULT físico que a Task 2 aplica.

3. Estender o doc-comment de bloco de `configuracoes` (linhas 101-119) com um parágrafo sobre a coluna nova; não reescrever o texto existente sobre defaults não-simétricos.

Em `src/lib/validations.ts`, logo abaixo de `configuracoesSchema` e de `ConfiguracoesFormValues`, adicionar DOIS exports novos, sem alterar `configuracoesSchema` nem `ConfiguracoesFormValues` (isto é load-bearing: `configuracoes-form.tsx` usa os dois hoje via `zodResolver`, e acrescentar um campo obrigatório dentro de `configuracoesSchema` faria `form.handleSubmit` reprovar para sempre um campo que o react-hook-form não registra, bloqueando o submit inteiro):
- `export const sequenciaIntervalosSchema = z.array(z.coerce.number().int().min(1, "Mínimo de 1 dia.")).min(1, "Adicione ao menos um intervalo.");` — as duas mensagens são literais do `10-UI-SPEC.md` §Copywriting Contract, não parafrasear.
- `export const configuracoesServerSchema = configuracoesSchema.extend({ sequenciaIntervalosDias: sequenciaIntervalosSchema });` — mesmo idioma de derivação em camada de `interacaoManualSchema`.
Doc-comment acima dos dois explicando: `configuracoesSchema` continua sendo o contrato do formulário (campos escalares, validados no cliente pelo `zodResolver`); `configuracoesServerSchema` é o contrato AUTORITATIVO do servidor (SEQ-01), consumido por `saveConfiguracoes`; `sequenciaIntervalosSchema` é exportado isoladamente porque o formulário precisa validar a lista dinâmica no cliente com exatamente as mesmas regras (plano 10-03), sem duplicar literais.

Em `scripts/test-lead-actions.cjs`: acrescentar ao array `manualAlters` a entrada `"ALTER TABLE \`leads\` ADD \`sequencia_posicao\` integer DEFAULT 0 NOT NULL;"`, mantendo as 4 entradas existentes intactas e na mesma ordem. Sem isso, `npm run test:lead-actions` quebra com "no such column: sequencia_posicao" assim que `schema.ts` ganhar a coluna. Não alterar mais nada neste arquivo neste plano.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const fs=require('fs');const s=fs.readFileSync('src/db/schema.ts','utf8');const v=fs.readFileSync('src/lib/validations.ts','utf8');const h=fs.readFileSync('scripts/test-lead-actions.cjs','utf8');const c=[[/sequenciaPosicao:\s*integer\(\"sequencia_posicao\"\)\.notNull\(\)\.default\(0\)/.test(s),'leads.sequenciaPosicao'],[/sequenciaIntervalosDias:\s*text\(\"sequencia_intervalos_dias\",\s*\{\s*mode:\s*\"json\"\s*\}\)/.test(s),'configuracoes.sequenciaIntervalosDias'],[/\.\\\$type<number\[\]>\(\)/.test(s),'\$type<number[]>'],[s.includes(\"'[4,10,20]'\"),'default [4,10,20]'],[/export const sequenciaIntervalosSchema/.test(v),'sequenciaIntervalosSchema'],[/export const configuracoesServerSchema = configuracoesSchema\.extend\(/.test(v),'configuracoesServerSchema por extend'],[v.includes('Adicione ao menos um intervalo.'),'copy lista vazia'],[/export const configuracoesSchema = z\.object\(\{\s*\n\s*diasParadoNovo/.test(v),'configuracoesSchema intacto'],[h.includes('ADD `sequencia_posicao` integer DEFAULT 0 NOT NULL'),'manualAlters do harness'];let bad=c.filter(x=>!x[0]).map(x=>x[1]);if(bad.length){console.error('FALTANDO: '+bad.join(', '));process.exit(1)}console.log('OK contratos 10-01 T1');"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` termina com exit 0 e sem nenhuma saída
    - `src/db/schema.ts` contém a linha de `sequenciaPosicao` com os literais `"sequencia_posicao"`, `.notNull()` e `.default(0)`, posicionada entre `contactAttempts` e `importBatchId`
    - `src/db/schema.ts` contém `sequenciaIntervalosDias` com os literais `"sequencia_intervalos_dias"`, `{ mode: "json" }`, `.$type<number[]>()`, `.notNull()` e o default `'[4,10,20]'` (exatamente assim, sem espaços dentro do array)
    - `src/lib/validations.ts` exporta `sequenciaIntervalosSchema` e `configuracoesServerSchema`, e `configuracoesServerSchema` é construído por `configuracoesSchema.extend(`
    - `src/lib/validations.ts` contém os literais `"Mínimo de 1 dia."` e `"Adicione ao menos um intervalo."`
    - `configuracoesSchema` continua com exatamente os 3 campos escalares originais e `ConfiguracoesFormValues` continua derivado dele (nenhum campo de array foi adicionado dentro dele)
    - `scripts/test-lead-actions.cjs` tem 5 entradas em `manualAlters`, a nova sendo `ALTER TABLE \`leads\` ADD \`sequencia_posicao\` integer DEFAULT 0 NOT NULL;`
  </acceptance_criteria>
  <done>Schema e contratos Zod compilam; o harness de leads reconstrói a coluna nova no banco temporário e continua verde.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Migração manual das duas colunas no banco vivo + gate permanente de schema</name>
  <files>scripts/migrate-sequencia-followup.cjs, scripts/verify-schema.cjs</files>
  <read_first>
    - scripts/backfill-origem-tipo.cjs (arquivo inteiro, 80 linhas — molde LITERAL: backup com `wal_checkpoint(TRUNCATE)` + `fs.copyFileSync`, guarda de idempotência por `PRAGMA table_info`, verificação pós-migração, `fail()` com `process.exit(1)`)
    - scripts/verify-schema.cjs (arquivo inteiro, 82 linhas — `requiredTables`, `requiredIndexes`, o bloco de conjunto estrito de colunas de `interacoes`, e a mensagem final de sucesso)
    - src/db/schema.ts (versão já com as duas colunas, resultado da Task 1 — as DDL do script precisam bater byte-a-byte com os defaults declarados lá)
    - .planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-RESEARCH.md §Common Pitfalls 1 e 2 (por que `drizzle-kit push` é proibido para estas duas colunas)
    - .planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-PATTERNS.md §`scripts/migrate-sequencia-followup.cjs` (script completo já redigido no idioma do projeto)
    - .planning/STATE.md §Blockers/Concerns e as linhas de decisão das Fases 06-01/07-01/08-01 sobre o landmine do `drizzle-kit push`
  </read_first>
  <action>
Esta task é BLOQUEANTE para a fase inteira: `npx tsc --noEmit` e `npm run build` passam sem as colunas existirem no banco (os tipos vêm de `schema.ts`), então pular este passo transforma a verificação da Fase 10 num falso-positivo. Nenhuma task posterior de nenhum plano desta fase pode ler/escrever `sequencia_posicao` ou `sequencia_intervalos_dias` antes deste passo estar aplicado.

`npx drizzle-kit push` e `npx drizzle-kit generate` são PROIBIDOS para estas duas colunas — `leads` tem 30+ linhas reais e `configuracoes` já está semeada desde a Fase 7; este projeto já sofreu o bug de "data-loss statement"/prompt de TTY duas vezes (06-01, 07-01) exatamente neste padrão. Se algum comando for executado por engano e imprimir "Found data-loss statements" ou "Interactive prompts require a TTY terminal", ABORTAR sem confirmar e restaurar o backup.

1. Criar `scripts/migrate-sequencia-followup.cjs` com `"use strict"`, seguindo o molde de `backfill-origem-tipo.cjs` na ordem: (a) `fail(message)` que loga com prefixo `[migrate-sequencia-followup]` e `process.exit(1)`; (b) resolver `DB_PATH` de `process.env.DB_FILE_NAME ?? path.join(__dirname, "..", "data", "crm.db")`; (c) backup ANTES de qualquer escrita — abrir com `{ fileMustExist: true }`, `pragma("wal_checkpoint(TRUNCATE)")`, fechar, `fs.copyFileSync` para `${DB_PATH}.backup-<ISO com : e . trocados por ->`, logando o caminho; (d) registrar `beforeLeads` = `SELECT count(*) FROM leads`; (e) guarda de idempotência independente por coluna via `PRAGMA table_info(leads)` / `PRAGMA table_info(configuracoes)`, aplicando só se ausente:
   - ``ALTER TABLE `leads` ADD `sequencia_posicao` integer DEFAULT 0 NOT NULL;``
   - ``ALTER TABLE `configuracoes` ADD `sequencia_intervalos_dias` text DEFAULT '[4,10,20]' NOT NULL;``
   Cada branch loga "coluna X adicionada" ou "X já existe — pulando (idempotência)".
   (f) verificação pós-migração, cada falha chamando `fail()`: `afterLeads` idêntico a `beforeLeads`; `SELECT count(*) FROM leads WHERE sequencia_posicao IS NULL` igual a 0; `SELECT count(*) FROM configuracoes WHERE sequencia_intervalos_dias IS NULL` igual a 0; e, para cada linha de `configuracoes`, `JSON.parse` do valor precisa produzir um array cujos itens sejam todos inteiros >= 1 (tolerar 0 linhas — a linha singleton é semeada em `getConfiguracoes()`, não por migração).
   (g) log final com contagem de leads e `db.close()` + `process.exit(0)`.
   O script NÃO faz backfill de dado (as duas colunas nascem preenchidas pelos DEFAULTs físicos) — só DDL guardado + verificação.

2. Com o `npm run dev` PARADO, executar `node scripts/migrate-sequencia-followup.cjs` uma vez e registrar no SUMMARY: caminho do backup, contagem de linhas de `leads` antes/depois, e o valor efetivo de `sequencia_intervalos_dias` na linha `id=1` (se ela existir).

3. Estender `scripts/verify-schema.cjs` com um gate permanente de PRESENÇA (não de conjunto estrito — diferente do bloco de `interacoes`, que continua exatamente como está): uma checagem nova que exige `sequencia_posicao` em `PRAGMA table_info(leads)` e `sequencia_intervalos_dias` em `PRAGMA table_info(configuracoes)`, com `fail()` nomeando a coluna ausente. Escolha deliberada de presença em vez de conjunto estrito: `leads` acumula colunas a cada fase e um conjunto estrito viraria manutenção obrigatória em toda fase futura. Atualizar a mensagem final de sucesso para citar as duas colunas novas. Não remover nem afrouxar nenhuma checagem existente.
  </action>
  <verify>
    <automated>node scripts/migrate-sequencia-followup.cjs && npm run verify:schema && node -e "const D=require('better-sqlite3');const db=new D('./data/crm.db',{fileMustExist:true});const l=db.prepare('PRAGMA table_info(leads)').all();const c=db.prepare('PRAGMA table_info(configuracoes)').all();const sp=l.find(x=>x.name==='sequencia_posicao');const si=c.find(x=>x.name==='sequencia_intervalos_dias');if(!sp){console.error('leads.sequencia_posicao AUSENTE');process.exit(1)}if(!si){console.error('configuracoes.sequencia_intervalos_dias AUSENTE');process.exit(1)}if(sp.notnull!==1||String(sp.dflt_value)!=='0'){console.error('leads.sequencia_posicao com NOT NULL/DEFAULT errados: '+JSON.stringify(sp));process.exit(1)}if(si.notnull!==1||String(si.dflt_value).replace(/\s/g,'')!==\"'[4,10,20]'\"){console.error('configuracoes.sequencia_intervalos_dias com NOT NULL/DEFAULT errados: '+JSON.stringify(si));process.exit(1)}const nulos=db.prepare('SELECT count(*) AS c FROM leads WHERE sequencia_posicao IS NULL').get().c;if(nulos!==0){console.error(nulos+' leads com sequencia_posicao NULL');process.exit(1)}console.log('OK banco vivo: leads com '+l.length+' colunas, configuracoes com '+c.length+' colunas, 0 NULL');"</automated>
  </verify>
  <acceptance_criteria>
    - Existe em disco um arquivo `data/crm.db.backup-*` criado antes da primeira escrita
    - `PRAGMA table_info(leads)` retorna 19 colunas, incluindo `sequencia_posicao` com `notnull=1` e `dflt_value=0`
    - `PRAGMA table_info(configuracoes)` retorna 6 colunas, incluindo `sequencia_intervalos_dias` com `notnull=1` e `dflt_value='[4,10,20]'`
    - `SELECT count(*) FROM leads` é idêntico antes e depois (os dois números registrados no SUMMARY)
    - `SELECT count(*) FROM leads WHERE sequencia_posicao IS NULL` retorna 0
    - Rodar `node scripts/migrate-sequencia-followup.cjs` uma segunda vez termina com exit 0 e loga "já existe — pulando" para as duas colunas, sem alterar nada (idempotência comprovada)
    - `npm run verify:schema` termina com exit 0 e a mensagem final cita `sequencia_posicao` e `sequencia_intervalos_dias`
    - Nenhum comando `drizzle-kit push`/`generate` foi executado (registrar explicitamente no SUMMARY)
  </acceptance_criteria>
  <done>As duas colunas existem no banco real com os DEFAULTs corretos, nenhuma linha de `leads` foi perdida, o script é idempotente e commitado, e o gate permanente de schema cobre as colunas novas.</done>
</task>

<task type="auto">
  <name>Task 3: Camada de leitura — última interação por lead e cálculo puro da data sugerida</name>
  <files>src/db/queries.ts</files>
  <read_first>
    - src/db/queries.ts (arquivo inteiro, 77 linhas — imports linhas 1-5, `getActiveDashboardLeads`, o doc-comment e a forma de `groupLeadsByUrgency` como modelo de função pura, e `getConfiguracoes`)
    - src/db/schema.ts (versão da Task 1 — `interacoes` linhas 82-99, `leads.sequenciaPosicao`, `leads.origemTipo`, `leads.stage`)
    - .planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-RESEARCH.md §Pattern 2, §Don't Hand-Roll e §Common Pitfalls 4 e 5
    - .planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-UI-SPEC.md §Layout > "Condição de exibição (gate completo, todas obrigatórias)" e o item "Nunca mostrado para leads fechados/perdidos"
  </read_first>
  <action>
Estender `src/db/queries.ts` (sem alterar nenhuma função existente):

1. Ampliar os imports: acrescentar `ne` e `sql` ao import de `drizzle-orm`, e `interacoes` ao import de `@/db/schema`. `addDays` e `isNull` já estão importados.

2. Adicionar `export async function getUltimaInteracaoWhatsAppPorLead(): Promise<Map<number, Date>>` — `db.select({ leadId: interacoes.leadId, ultima: sql<number>\`max(${interacoes.createdAt})\` }).from(interacoes).where(and(ne(interacoes.tipo, "nota_manual"), isNull(interacoes.deletedAt))).groupBy(interacoes.leadId)`, devolvendo `new Map(rows.map((r) => [r.leadId, new Date(r.ultima * 1000)]))`. Doc-comment obrigatório explicando: agregação em SQL e não "buscar tudo e reduzir em JS" (a timeline cresce com o histórico total, não com o número de leads ativos — o índice `interacoes_lead_id_idx` já cobre o `GROUP BY`); `ne(tipo, "nota_manual")` porque D-09 manda usar a última interação de WHATSAPP como base, e nota manual não é contato; `* 1000` porque `created_at` é `unixepoch()` em SEGUNDOS.

3. Adicionar `export function computeSequenciaSugestao(lead: Pick<Lead, "origemTipo" | "sequenciaPosicao" | "stage">, ultimaInteracaoWhatsApp: Date | undefined, intervalosDias: number[]): Date | undefined` — função PURA, sem I/O, no mesmo espírito documentado de `groupLeadsByUrgency`. Quatro gates, nesta ordem, cada um com comentário citando sua origem, todos devolvendo `undefined` (nunca lançar exceção, nunca `console.error` — "sequência esgotada" é ausência silenciosa, não erro):
   a. `lead.origemTipo !== "outbound"` → `undefined` (ORIGEM-03; este é o ÚNICO ponto do sistema onde o gate Inbound existe — não duplicar em nenhum write-path, ver Pitfall 4 do RESEARCH)
   b. `lead.stage === "fechado" || lead.stage === "perdido"` → `undefined` (`10-UI-SPEC.md`: "Nunca mostrado para leads fechados/perdidos"; mora aqui, e não só na página, para que o board do pipeline — que lista as 5 etapas — não divirja do dashboard, que já exclui as terminais via `getActiveDashboardLeads`)
   c. `!ultimaInteracaoWhatsApp` → `undefined` (D-09: sem interação-base não há cálculo; nunca usar `createdAt` do lead nem `followUpDate` como fallback)
   d. `intervalosDias[lead.sequenciaPosicao] === undefined` → `undefined` (D-10: sequência esgotada, ou o admin encurtou a lista depois que o lead já tinha avançado — os dois são cenários normais)
   Retorno: `addDays(ultimaInteracaoWhatsApp, intervalo)`.
   Nota de arquitetura para o doc-comment: a DATA nunca é persistida em lugar nenhum (SEQ-02, "cálculo na leitura") — só o índice `sequenciaPosicao` é; persistir a data faria ela congelar quando o admin editasse os intervalos.

O gate (b) amplia deliberadamente a assinatura esboçada no `10-RESEARCH.md` §Pattern 2 (que tinha só `origemTipo | sequenciaPosicao`) para incluir `stage` — é um reforço exigido pelo contrato de UI já aprovado, não uma divergência acidental; registrar isso no SUMMARY.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node --experimental-strip-types -e "1" >/dev/null 2>&1; node -e "const fs=require('fs');const q=fs.readFileSync('src/db/queries.ts','utf8');const c=[[/export async function getUltimaInteracaoWhatsAppPorLead\(\): Promise<Map<number, Date>>/.test(q),'assinatura getUltimaInteracaoWhatsAppPorLead'],[/groupBy\(interacoes\.leadId\)/.test(q),'groupBy leadId'],[/ne\(interacoes\.tipo, \"nota_manual\"\)/.test(q),'exclui nota_manual'],[/isNull\(interacoes\.deletedAt\)/.test(q),'exclui soft-deletadas'],[/max\(\\\$\{interacoes\.createdAt\}\)/.test(q),'agregacao max'],[/r\.ultima \* 1000/.test(q),'segundos->ms'],[/export function computeSequenciaSugestao/.test(q),'computeSequenciaSugestao'],[/origemTipo !== \"outbound\"/.test(q),'gate ORIGEM-03'],[/\"fechado\"/.test(q)&&/\"perdido\"/.test(q),'gate etapas terminais'],[/addDays\(/.test(q),'addDays'],[!/console\.error/.test(q)&&!/throw new Error/.test(q),'sem excecao/log em sequencia esgotada']];let bad=c.filter(x=>!x[0]).map(x=>x[1]);if(bad.length){console.error('FALTANDO: '+bad.join(', '));process.exit(1)}console.log('OK camada de leitura 10-01 T3');"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` termina com exit 0
    - `src/db/queries.ts` exporta `getUltimaInteracaoWhatsAppPorLead` retornando `Promise<Map<number, Date>>` e usando `groupBy(interacoes.leadId)` com `ne(interacoes.tipo, "nota_manual")` e `isNull(interacoes.deletedAt)` no `and(...)`
    - `src/db/queries.ts` exporta `computeSequenciaSugestao` com os 4 gates na ordem: origemTipo, stage terminal, ausência de interação-base, índice fora dos intervalos
    - A função `computeSequenciaSugestao` não contém `console.error`, `console.warn`, `throw` nem qualquer `await` (é pura e síncrona)
    - Nenhuma função pré-existente de `queries.ts` (`getActiveDashboardLeads`, `groupLeadsByUrgency`, `getConfiguracoes`) teve corpo ou assinatura alterados
    - Comprovação comportamental dos gates (rodar e colar o resultado no SUMMARY): um lead `inbound` com interação e intervalos `[4,10,20]` devolve `undefined`; um lead `outbound` em `perdido` devolve `undefined`; um lead `outbound` em `novo` sem interação devolve `undefined`; um lead `outbound` em `novo` com `sequenciaPosicao=3` e intervalos `[4,10,20]` devolve `undefined`; um lead `outbound` em `contatado` com `sequenciaPosicao=0`, interação em 2026-08-01 e intervalos `[4,10,20]` devolve 2026-08-05
  </acceptance_criteria>
  <done>A leitura agregada da última interação e o cálculo puro da data sugerida existem, compilam, e os 4 gates foram comprovados comportamentalmente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aplicação → SQLite (data/crm.db) | Banco com dados reais de produção do admin; toda DDL desta fase escreve nesse arquivo |
| repositório → gate de schema | `scripts/verify-schema.cjs` é a única barreira automatizada contra "schema.ts diverge do banco vivo" |
| cliente → contrato Zod | `configuracoesServerSchema` é a validação autoritativa da lista de intervalos que chega do formulário (consumida no plano 10-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | `ALTER TABLE` sobre `data/crm.db` (Task 2) | mitigate | Backup com `wal_checkpoint(TRUNCATE)` + `copyFileSync` antes da primeira escrita; guarda de idempotência por `PRAGMA table_info`; verificação de contagem de linhas antes/depois; `drizzle-kit push`/`generate` proibidos para estas colunas |
| T-10-02 | Tampering | Payload JSON malformado ou gigante em `sequencia_intervalos_dias` | mitigate | `sequenciaIntervalosSchema` (Zod, array de inteiros >= 1, mínimo 1 item) valida a FORMA antes de qualquer escrita; o script de migração verifica com `JSON.parse` que toda linha existente é um array de inteiros positivos |
| T-10-03 | Information Disclosure | Sugestão de reabordagem calculada para lead que não deveria recebê-la (Inbound) | mitigate | Gate `origemTipo !== "outbound"` como PRIMEIRA condição de `computeSequenciaSugestao` (Task 3), ponto único do sistema — ORIGEM-03 |
| T-10-04 | Denial of Service | Leitura da timeline inteira em memória para achar a última interação | mitigate | Agregação `max(created_at) GROUP BY lead_id` em SQL (Task 3), apoiada pelo índice `interacoes_lead_id_idx` — nunca "buscar tudo e reduzir em JS" |
| T-10-05 | Tampering | Interpolação de valores em SQL cru | accept | Só o query builder parametrizado do Drizzle é usado; o único `sql\`...\`` desta task interpola uma REFERÊNCIA DE COLUNA tipada (`interacoes.createdAt`), nunca input de usuário |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo é instalado nesta fase (`10-RESEARCH.md` §Package Legitimacy Audit: "não aplicável"); se um `npm install` surgir durante a execução, o checkpoint humano bloqueante de legitimidade volta a ser obrigatório antes de instalar |
</threat_model>

<verification>
**Host de 4GB RAM: rodar UM COMANDO DE CADA VEZ, sequencialmente, com o `npm run dev` PARADO.** Nunca encadear com `&`, nunca rodar em paralelo — este host já caiu por OOM duas vezes.

1. `npx tsc --noEmit` — exit 0, sem saída
2. `node scripts/migrate-sequencia-followup.cjs` — exit 0 (na 2ª execução, loga idempotência)
3. `npm run verify:schema` — exit 0, cita as duas colunas novas
4. `npm run test:lead-actions` — exit 0 (nenhum caso pré-existente quebrado pelo bootstrap novo)
5. `npm run guard:no-hard-delete` — exit 0
6. `node scripts/verify-wa-contact-invariant.cjs` — exit 0
</verification>

<success_criteria>
- `leads` com 19 colunas e `configuracoes` com 6 colunas em `data/crm.db`, nenhuma linha perdida, backup em disco
- `scripts/migrate-sequencia-followup.cjs` commitado e idempotente; nenhum `drizzle-kit push`/`generate` executado
- `configuracoesServerSchema` e `sequenciaIntervalosSchema` exportados sem alterar `configuracoesSchema`/`ConfiguracoesFormValues`
- `computeSequenciaSugestao` pura, com os 4 gates comprovados comportamentalmente
- Nenhum arquivo de UI e nenhuma Server Action foram tocados neste plano
</success_criteria>

<output>
Criar `.planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-01-SUMMARY.md` ao terminar, registrando: caminho do backup, contagem de linhas/colunas de `leads` e `configuracoes` antes e depois, valor de `sequencia_intervalos_dias` na linha `id=1`, a saída da comprovação comportamental dos 4 gates, e as decisões D-11 e D-12 do bloco `<decisoes_do_planner>`.
</output>
