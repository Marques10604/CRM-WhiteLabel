---
phase: 10-sequ-ncia-de-follow-up-escalonada
plan: 04
type: execute
wave: 3
depends_on: [10-02, 10-03]
files_modified:
  - src/app/page.tsx
  - src/app/pipeline/page.tsx
  - src/components/pipeline-board.tsx
  - src/components/pipeline-lead-card.tsx
  - src/components/followup-dashboard.tsx
autonomous: true
requirements: [SEQ-02, SEQ-03, ORIGEM-03]

must_haves:
  truths:
    - "Um lead Outbound com interação de WhatsApp registrada e posição válida mostra 'Sugestão: dd/MM' no card do pipeline e na linha do dashboard"
    - "Um lead Inbound nunca mostra o indicador de sugestão, em nenhuma das duas superfícies (ORIGEM-03)"
    - "Um lead que esgotou os intervalos configurados não mostra indicador nenhum — sem placeholder, sem mensagem de erro (D-10)"
    - "A data sugerida é calculada no servidor e chega pronta aos componentes cliente — nenhum componente cliente recalcula data"
    - "O campo followUpDate continua sendo o único dado editável e nunca é sobrescrito pela sugestão (D-06)"
    - "Templates do tipo 'prova_valor' continuam disponíveis para seleção no diálogo de WhatsApp de qualquer lead (SEQ-03)"
  artifacts:
    - path: "src/app/pipeline/page.tsx"
      provides: "Cálculo server-side da lista de sugestões e prop para o board"
      contains: "computeSequenciaSugestao"
    - path: "src/app/page.tsx"
      provides: "Cálculo server-side da lista de sugestões e prop para o dashboard"
      contains: "getUltimaInteracaoWhatsAppPorLead"
    - path: "src/components/pipeline-lead-card.tsx"
      provides: "Indicador CalendarClock 'Sugestão: dd/MM' na linha de metadados"
      contains: "CalendarClock"
    - path: "src/components/followup-dashboard.tsx"
      provides: "Mesmo indicador na linha de metadados de cada lead do dashboard"
      contains: "Sugestão:"
  key_links:
    - from: "src/app/pipeline/page.tsx"
      to: "src/db/queries.ts (computeSequenciaSugestao)"
      via: "map sobre activeLeads usando config.sequenciaIntervalosDias e o Map de última interação"
      pattern: "computeSequenciaSugestao\\("
    - from: "src/components/pipeline-board.tsx"
      to: "src/components/pipeline-lead-card.tsx"
      via: "prop sugestao derivada de um Map montado com useMemo, mesmo padrão de esfriandoSet"
      pattern: "sugestao=\\{"
    - from: "src/components/followup-dashboard.tsx"
      to: "src/app/page.tsx"
      via: "prop sugestaoPorLead serializável (array de objetos), nunca um Map cru"
      pattern: "sugestaoPorLead"
---

<objective>
Fechar a Fase 10: exibir a próxima data de reabordagem sugerida nas duas superfícies onde `followUpDate` já aparece (dashboard de follow-ups e card do pipeline), com o cálculo inteiramente no servidor, e rodar os gates finais da fase.

Purpose: é onde SEQ-02 e ORIGEM-03 viram comportamento observável — até aqui a sequência existe no banco e avança sozinha, mas o admin ainda não vê nada. O indicador é puramente informativo (D-06) e nunca toca `followUpDate`.
Output: sugestão visível nas duas telas, gates estáticos verdes e verificação humana ponta a ponta registrada.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-CONTEXT.md
@.planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-RESEARCH.md
@.planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-PATTERNS.md
@.planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-UI-SPEC.md

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código atual — não explorar o repositório para redescobrir. -->

`src/db/queries.ts` (após o plano 10-01):
- `getUltimaInteracaoWhatsAppPorLead(): Promise<Map<number, Date>>`
- `computeSequenciaSugestao(lead: Pick<Lead,"origemTipo"|"sequenciaPosicao"|"stage">, ultimaInteracaoWhatsApp: Date | undefined, intervalosDias: number[]): Date | undefined`
- `getConfiguracoes(): Promise<Configuracoes>` — `config.sequenciaIntervalosDias` é `number[]`
- `getActiveDashboardLeads(): Promise<Lead[]>` — já exclui soft-deletados e as etapas `fechado`/`perdido`

`src/app/page.tsx` (35 linhas): `Promise.all([getActiveDashboardLeads(), db.select().from(subnichos), db.select().from(templates)])` → `groupLeadsByUrgency(activeLeads)` → `<FollowupDashboard vencidos hoje proximos7Dias subnichos templates />`

`src/app/pipeline/page.tsx` (63 linhas): `Promise.all([db.select().from(leads).where(isNull(leads.deletedAt)).orderBy(asc(leads.followUpDate)), db.select().from(subnichos), db.select().from(templates), getConfiguracoes()])` → monta `limitesPorEtapa` e `esfriandoLeadIds` (array de ids, computado no servidor) → `<PipelineBoard leads subnichos esfriandoLeadIds templates />`

`src/components/pipeline-board.tsx` (251 linhas):
- `type PipelineBoardProps = { leads: Lead[]; subnichos: Subnicho[]; esfriandoLeadIds: number[]; templates: Template[] }`
- `const esfriandoSet = useMemo(() => new Set(esfriandoLeadIds), [esfriandoLeadIds]);` — molde exato de como derivar estrutura no cliente a partir de prop serializável
- Renderiza `<PipelineLeadCard key lead subnichoNome isEsfriando={esfriandoSet.has(lead.id)} onClick onSendWhatsApp onViewHistory />`

`src/components/pipeline-lead-card.tsx` (115 linhas):
- `type PipelineLeadCardProps = { lead: Lead; subnichoNome: string; isEsfriando: boolean; onClick; onSendWhatsApp; onViewHistory }`
- Imports (linha 5): `import { Clock, History, MessageCircle } from "lucide-react";`
- Linha de metadados (linhas 96-112): `<div className="flex items-center gap-1 text-[14px] leading-normal text-muted-foreground">` com `{format(lead.followUpDate, "dd/MM/yyyy")}`, depois `{isEsfriando ? <span className="flex items-center gap-1 text-[#B45309]"><Clock className="size-3.5" /> Esfriando</span> : null}`, depois `{lead.contactAttempts > 0 ? <span className="flex items-center gap-1" aria-label={...}><MessageCircle className="size-3.5" />{lead.contactAttempts}x</span> : null}`

`src/components/followup-dashboard.tsx` (231 linhas):
- `type FollowupDashboardProps = { vencidos: Lead[]; hoje: Lead[]; proximos7Dias: Lead[]; subnichos: Subnicho[]; templates: Template[] }`
- Linha de metadados por lead (linhas 173-180): `<div className="flex items-center gap-2 text-[14px] leading-normal">` com o nome do sub-nicho em `text-muted-foreground` e `{format(lead.followUpDate, "dd/MM/yyyy")}` numa `<span className={section.dateClassName}>`
- Já importa `format` de `date-fns`

`src/components/whatsapp-preview-dialog.tsx` — já lista os 3 tipos de template (`primeiro_contato`/`follow_up`/`prova_valor`) num `<Select>`; NÃO precisa de mudança nesta fase (SEQ-03 já entregue desde a Fase 4, ver `10-RESEARCH.md` §Phase Requirements).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cálculo server-side das sugestões nas duas rotas</name>
  <files>src/app/page.tsx, src/app/pipeline/page.tsx</files>
  <read_first>
    - src/app/pipeline/page.tsx (arquivo inteiro, 63 linhas — doc-comment, `Promise.all`, `limitesPorEtapa` e `esfriandoLeadIds` como molde literal de derivação server-side)
    - src/app/page.tsx (arquivo inteiro, 35 linhas)
    - src/db/queries.ts (versão do plano 10-01 — assinaturas exatas de `getUltimaInteracaoWhatsAppPorLead` e `computeSequenciaSugestao`)
    - .planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-PATTERNS.md §`src/app/page.tsx` e `src/app/pipeline/page.tsx`
    - .planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-UI-SPEC.md §Layout > "Cálculo sempre no servidor"
  </read_first>
  <action>
Nas DUAS rotas, acrescentar `getConfiguracoes()` (já presente em `pipeline/page.tsx`, falta em `page.tsx`) e `getUltimaInteracaoWhatsAppPorLead()` ao `Promise.all` existente — nunca em `await`s sequenciais separados, para não serializar I/O que hoje é paralelo.

Derivar, depois do `Promise.all`, uma lista `sugestaoPorLead` no formato `{ leadId: number; data: Date }[]`: mapear os leads da rota chamando `computeSequenciaSugestao(lead, ultimaInteracaoPorLead.get(lead.id), config.sequenciaIntervalosDias)` e descartar as entradas cujo retorno é `undefined`.

Formato deliberadamente um ARRAY de objetos, não um `Map`: é o mesmo idioma já usado por `esfriandoLeadIds` (array simples cruzando a fronteira servidor→cliente, reconstruído com `useMemo` no componente cliente). Não passar `Map` como prop de Server Component para Client Component nesta fase — o precedente do projeto é array, e manter o idioma evita depender de detalhes de serialização do RSC.

Em `src/app/page.tsx`: usar `activeLeads` (retorno de `getActiveDashboardLeads()`, que já exclui as etapas terminais) como base do map, e passar `sugestaoPorLead` como prop nova de `<FollowupDashboard />` — as props existentes (`vencidos`, `hoje`, `proximos7Dias`, `subnichos`, `templates`) ficam inalteradas. Não recalcular nada dentro de `groupLeadsByUrgency`.

Em `src/app/pipeline/page.tsx`: usar `activeLeads` (que aqui inclui as 5 etapas) como base do map — não filtrar `fechado`/`perdido` aqui, porque `computeSequenciaSugestao` já contém esse gate (plano 10-01, Task 3); duplicar o filtro criaria duas fontes de verdade que podem divergir. Passar `sugestaoPorLead` como prop nova de `<PipelineBoard />`, mantendo `esfriandoLeadIds` e as demais props intactas.

Estender o doc-comment das duas rotas com um parágrafo citando D-05/D-06/SEQ-02: a sugestão é derivada na leitura a cada request, nunca persistida e nunca agendada; ela acompanha `followUpDate` sem substituí-lo.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const fs=require('fs');const h=fs.readFileSync('src/app/page.tsx','utf8');const p=fs.readFileSync('src/app/pipeline/page.tsx','utf8');const c=[];for(const [n,s] of [['page.tsx',h],['pipeline/page.tsx',p]]){c.push([/getUltimaInteracaoWhatsAppPorLead\(\)/.test(s),n+': chama getUltimaInteracaoWhatsAppPorLead']);c.push([/getConfiguracoes\(\)/.test(s),n+': chama getConfiguracoes']);c.push([/computeSequenciaSugestao\(/.test(s),n+': chama computeSequenciaSugestao']);c.push([/sugestaoPorLead/.test(s),n+': monta/passa sugestaoPorLead']);const pa=(s.match(/Promise\.all\(/g)||[]).length;c.push([pa===1,n+': exatamente 1 Promise.all (I/O paralelo preservado)'])}c.push([!/new Map\(/.test(h)&&!/new Map\(/.test(p),'nenhum Map cruzando a fronteira servidor->cliente']);const bad=c.filter(x=>!x[0]).map(x=>x[1]);if(bad.length){console.error('FALTANDO: '+bad.join(' | '));process.exit(1)}console.log('OK calculo server-side 10-04 T1');"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` termina com exit 0
    - As duas rotas chamam `getConfiguracoes()` e `getUltimaInteracaoWhatsAppPorLead()` dentro do `Promise.all` já existente (uma única chamada `Promise.all` por arquivo)
    - As duas rotas montam `sugestaoPorLead` como `{ leadId, data }[]` filtrando os `undefined`, e passam essa prop para `FollowupDashboard`/`PipelineBoard`
    - Nenhuma das duas rotas constrói um `Map` para passar como prop
    - `pipeline/page.tsx` NÃO filtra `fechado`/`perdido` ao montar a lista (o gate mora em `computeSequenciaSugestao`)
    - `esfriandoLeadIds`, `limitesPorEtapa`, `groupLeadsByUrgency` e todas as props pré-existentes continuam idênticas
  </acceptance_criteria>
  <done>As duas rotas calculam a sugestão no servidor a cada request e entregam o resultado pronto como prop serializável.</done>
</task>

<task type="auto">
  <name>Task 2: Indicador "Sugestão: dd/MM" no card do pipeline e na linha do dashboard</name>
  <files>src/components/pipeline-board.tsx, src/components/pipeline-lead-card.tsx, src/components/followup-dashboard.tsx</files>
  <read_first>
    - src/components/pipeline-lead-card.tsx (arquivo inteiro, 115 linhas — tipo de props, imports de lucide e a linha de metadados linhas 96-112 com os indicadores `Clock`/"Esfriando" e `MessageCircle`/tentativas)
    - src/components/pipeline-board.tsx (linhas 23-28 do tipo de props, linha 99 do `esfriandoSet` com `useMemo`, e o bloco de render de `PipelineLeadCard` linhas 186-201)
    - src/components/followup-dashboard.tsx (arquivo inteiro, 231 linhas — tipo de props e a linha de metadados por lead, linhas 169-181)
    - .planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-UI-SPEC.md §Copywriting Contract e §Layout > "Indicador de próxima data sugerida" e §Color (a proibição de teal/âmbar é crítica)
    - .planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-PATTERNS.md §`pipeline-lead-card.tsx` e `followup-dashboard.tsx`
  </read_first>
  <action>
**`pipeline-board.tsx`:** acrescentar `sugestaoPorLead: { leadId: number; data: Date }[]` ao `PipelineBoardProps` e ao destructuring do componente. Derivar `const sugestaoPorLeadId = useMemo(() => new Map(sugestaoPorLead.map((s) => [s.leadId, s.data])), [sugestaoPorLead]);` — mesmo idioma exato do `esfriandoSet` logo acima. Passar `sugestao={sugestaoPorLeadId.get(lead.id)}` para cada `<PipelineLeadCard />`. Não mexer em nenhuma outra prop, no `DndContext`, no `useOptimistic` nem nos diálogos.

**`pipeline-lead-card.tsx`:** acrescentar `sugestao?: Date` ao `PipelineLeadCardProps` e ao destructuring. Acrescentar `CalendarClock` ao import de `lucide-react` (mantendo a ordem alfabética já usada: `CalendarClock, Clock, History, MessageCircle`). Adicionar, como ÚLTIMO filho da linha de metadados existente (depois do contador de tentativas — nunca reordenar os elementos já presentes, nunca criar uma linha nova), um `{sugestao ? (<span ...>) : null}` contendo `<CalendarClock className="size-3.5" />` seguido do texto `Sugestão: {format(sugestao, "dd/MM")}`, com:
- `className="flex items-center gap-1"` — herda `text-muted-foreground` do container pai. É PROIBIDO usar o teal `#0D9488` (reservado a ações) e o âmbar `#B45309`/`#F59E0B` (reservado ao alerta "Esfriando"): a sugestão é neutra e informativa, e dar a ela cor de ação sugeriria erroneamente que é editável (D-06, marcado como crítico no `10-UI-SPEC.md` §Color).
- `aria-label={\`Próxima reabordagem sugerida em ${format(sugestao, "dd/MM/yyyy")}\`}` — ano completo só na acessibilidade, texto visível curto, mesmo padrão do contador de tentativas.
- `title="Sugestão calculada a partir da última interação registrada. Não altera a data de follow-up real — o campo Follow-up continua sendo a fonte oficial."` — literal do contrato de UI.

**`followup-dashboard.tsx`:** acrescentar `sugestaoPorLead: { leadId: number; data: Date }[]` ao `FollowupDashboardProps` e ao destructuring; derivar o mesmo `Map` com `useMemo` (idioma do `subnichoNameById` logo acima); e inserir o MESMO `<span>` do card (ícone + texto + `aria-label` + `title` idênticos, `text-muted-foreground` explícito porque este container não herda a cor) como último filho da linha de metadados de cada lead, depois da `<span>` de `followUpDate`. Importar `CalendarClock` de `lucide-react` (o arquivo ainda não importa nada de lucide).

Nenhum dos três arquivos pode calcular data: `format` sobre uma prop já resolvida é renderização; qualquer `addDays`, leitura de `sequenciaPosicao` ou de `sequenciaIntervalosDias` dentro de componente cliente é violação do contrato "cálculo sempre no servidor" e deve ser rejeitada na revisão.

Nenhum tamanho, espaçamento ou cor fora do inventário do `10-UI-SPEC.md`: ícone `size-3.5`, texto 14px herdado do container, `gap-1` entre ícone e texto.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/components/pipeline-board.tsx src/components/pipeline-lead-card.tsx src/components/followup-dashboard.tsx && node -e "const fs=require('fs');const b=fs.readFileSync('src/components/pipeline-board.tsx','utf8');const k=fs.readFileSync('src/components/pipeline-lead-card.tsx','utf8');const d=fs.readFileSync('src/components/followup-dashboard.tsx','utf8');const c=[[/sugestaoPorLead: \{ leadId: number; data: Date \}\[\]/.test(b),'board: prop tipada'],[/useMemo\(\(\) => new Map\(sugestaoPorLead/.test(b),'board: Map via useMemo'],[/sugestao=\{/.test(b),'board: passa sugestao ao card'],[/sugestao\?: Date/.test(k),'card: prop sugestao opcional'],[/CalendarClock/.test(k)&&/CalendarClock/.test(d),'CalendarClock nos dois componentes'],[/Sugest[aã]o: \{format\(sugestao, \"dd\/MM\"\)\}/.test(k),'card: texto Sugestao dd/MM'],[k.includes('Próxima reabordagem sugerida em')&&d.includes('Próxima reabordagem sugerida em'),'aria-label literal nos dois'],[k.includes('o campo Follow-up continua sendo a fonte oficial.')&&d.includes('o campo Follow-up continua sendo a fonte oficial.'),'title literal nos dois'],[/sugestaoPorLead/.test(d),'dashboard: prop recebida'],[!/text-\[#0D9488\]|text-\[#B45309\]|text-\[#F59E0B\]/.test(k.slice(k.indexOf('CalendarClock className'))),'card: indicador sem teal/ambar'],[!/addDays|sequenciaPosicao|sequenciaIntervalosDias/.test(k)&&!/addDays|sequenciaPosicao|sequenciaIntervalosDias/.test(d)&&!/addDays|sequenciaPosicao|sequenciaIntervalosDias/.test(b),'nenhum calculo de sequencia no cliente']];const bad=c.filter(x=>!x[0]).map(x=>x[1]);if(bad.length){console.error('FALTANDO: '+bad.join(' | '));process.exit(1)}console.log('OK indicador 10-04 T2');"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exit 0 e `npx eslint` exit 0 nos três arquivos (sem novos warnings)
    - `PipelineBoardProps` e `FollowupDashboardProps` ganharam `sugestaoPorLead: { leadId: number; data: Date }[]`; `PipelineLeadCardProps` ganhou `sugestao?: Date`
    - Os dois componentes de exibição contêm o texto visível `Sugestão: {dd/MM}`, o `aria-label` "Próxima reabordagem sugerida em {dd/MM/yyyy}" e o `title` completo do contrato, literalmente
    - O indicador é o ÚLTIMO elemento da linha de metadados nas duas superfícies, sem reordenar `followUpDate`, "Esfriando" nem o contador de tentativas
    - O indicador NÃO usa `#0D9488`, `#B45309` nem `#F59E0B` — é `text-muted-foreground`
    - Nenhum dos três arquivos contém `addDays`, `sequenciaPosicao` ou `sequenciaIntervalosDias` (nenhum cálculo migrou para o cliente)
    - `esfriandoSet`, `subnichoNameById`, `firstContactTemplate` e todo o comportamento pré-existente de drag/diálogos continuam intactos
  </acceptance_criteria>
  <done>O indicador neutro de sugestão aparece nas duas superfícies quando — e somente quando — o cálculo do servidor devolve uma data.</done>
</task>

<task type="auto">
  <name>Task 3: Gates finais da fase e verificação ponta a ponta</name>
  <files>src/app/page.tsx, src/app/pipeline/page.tsx, src/components/pipeline-lead-card.tsx, src/components/followup-dashboard.tsx</files>
  <read_first>
    - .planning/ROADMAP.md §Phase 10 (os 4 Success Criteria — cada um precisa de evidência nesta task)
    - .planning/REQUIREMENTS.md (SEQ-01, SEQ-02, SEQ-03, ORIGEM-03)
    - .planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-UI-SPEC.md §Layout > "Condição de exibição (gate completo, todas obrigatórias)"
    - src/components/whatsapp-preview-dialog.tsx (confirmar, por leitura, que o `<Select>` de tipo de template já oferece `prova_valor` — SEQ-03 não exige mudança de código)
    - .planning/STATE.md §Blockers/Concerns (host de 4GB: nunca rodar build com o dev server ativo)
  </read_first>
  <action>
Nenhuma alteração funcional nova nesta task — é o fechamento da fase. Os arquivos listados em `<files>` só aparecem porque correções pontuais surgidas dos gates abaixo podem cair neles.

1. **Gates estáticos**, um comando de cada vez, com o `npm run dev` PARADO (host de 4GB já caiu por OOM rodando build junto com o dev server):
   - `npx tsc --noEmit`
   - `npx eslint src/app/page.tsx src/app/pipeline/page.tsx src/components/pipeline-board.tsx src/components/pipeline-lead-card.tsx src/components/followup-dashboard.tsx src/components/configuracoes-form.tsx src/actions/lead-actions.ts src/actions/configuracoes-actions.ts src/db/queries.ts` — escopo restrito de propósito: rodar `npx eslint` sem escopo neste repositório expõe 413 problemas pré-existentes e não relacionados (documentado em `deferred-items.md` na Fase 08-03), o que não bloqueia esta fase
   - `npm run verify:schema`
   - `npm run verify:sequencia`
   - `npm run verify:origem-tipo`
   - `npm run guard:no-hard-delete`
   - `node scripts/verify-wa-contact-invariant.cjs`
   - `npm run test:lead-actions`
   - `npm run test:interacao-actions`
   - `npm run build` (por último, dev server parado)

2. **Evidência de SEQ-03 sem mudança de código:** confirmar por leitura que `templates.tipo` inclui `"prova_valor"` no schema e que `whatsapp-preview-dialog.tsx` oferece esse tipo no seletor; registrar no SUMMARY os dois trechos exatos (arquivo + identificador), declarando explicitamente que SEQ-03 foi entregue na Fase 4 e verificado — não reimplementado — nesta fase.

3. **Rastreabilidade dos 4 Success Criteria do ROADMAP**: no SUMMARY, uma linha por critério apontando o artefato e a evidência concreta (comando executado ou passo do `<human-check>` abaixo) que o comprova.

4. Executar os passos de verificação humana do bloco `<human-check>` deste plano e registrar o resultado item a item. Se não houver navegador disponível nesta sessão, registrar isso explicitamente no SUMMARY como pendência de UAT (mesmo tratamento das Fases 4 e 9) em vez de declarar a fase verificada.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run verify:schema && npm run verify:sequencia && npm run guard:no-hard-delete && node scripts/verify-wa-contact-invariant.cjs && node -e "const fs=require('fs');const s=fs.readFileSync('src/db/schema.ts','utf8');const w=fs.readFileSync('src/components/whatsapp-preview-dialog.tsx','utf8');if(!/\"prova_valor\"/.test(s)){console.error('SEQ-03: prova_valor ausente no enum de templates.tipo');process.exit(1)}if(!/prova_valor/.test(w)){console.error('SEQ-03: prova_valor nao aparece em whatsapp-preview-dialog.tsx');process.exit(1)}console.log('OK gates 10-04 T3 (build e eslint rodam separados, um de cada vez)');"</automated>
    <human-check>
Com `npm run dev` rodando (e só ele), no navegador. Preparação: garantir que existe pelo menos um lead Outbound com uma interação de WhatsApp registrada e um lead Inbound (usar o formulário de lead para ajustar o "Tipo de origem" se necessário).

1. `/configuracoes` — confirmar que a sequência está salva com valores conhecidos (ex.: 4, 10, 20).
2. `/pipeline` — no card de um lead **Outbound** que já tenha clique de WhatsApp registrado, aparece "Sugestão: dd/MM" com ícone de calendário/relógio, em cinza neutro, DEPOIS da data de follow-up e do contador de tentativas. Passar o mouse mostra o tooltip explicando que não altera a data de follow-up.
3. `/` (dashboard) — o MESMO lead mostra o mesmo indicador na linha dele.
4. Card de um lead **Inbound**: nenhum indicador de sugestão aparece, em nenhuma das duas telas (ORIGEM-03). **Se aparecer, é falha bloqueante da fase.**
5. Lead Outbound SEM nenhuma interação de WhatsApp: nenhum indicador aparece (D-09).
6. Abrir WhatsApp num lead Outbound com o template de **Follow-up** e enviar → voltar ao pipeline e recarregar: a data sugerida MUDA (passou a usar o próximo degrau da sequência e a interação nova como base). Repetir até esgotar os 3 degraus → depois do 3º, o indicador SOME e não volta (D-10).
7. Abrir WhatsApp com template **Prova de valor** no mesmo lead: o tipo está disponível no seletor (SEQ-03) e a data sugerida NÃO muda (só follow_up avança a posição).
8. Arrastar esse lead de volta para a coluna "Novo" e recarregar: o indicador volta a aparecer com o PRIMEIRO degrau (D-02 — reset).
9. Editar o campo "Follow-up" do lead pelo formulário e salvar: a data de follow-up muda, a sugestão NÃO muda e não sobrescreve nada (D-06).
10. Mover o lead para "Fechado" ou "Perdido": o indicador some (regra de etapa terminal).
</human-check>
  </verify>
  <acceptance_criteria>
    - Todos os 10 comandos do item 1 terminam com exit 0, executados um de cada vez, com a saída registrada no SUMMARY (`npm run build` com o dev server comprovadamente parado)
    - `npx eslint` no escopo listado não introduz nenhum problema novo em relação ao estado anterior à fase
    - O SUMMARY contém uma linha por Success Criterion do ROADMAP §Phase 10, cada uma apontando artefato + evidência
    - O SUMMARY documenta SEQ-03 como verificado (não reimplementado), citando o enum em `src/db/schema.ts` e o seletor em `whatsapp-preview-dialog.tsx`
    - Os 10 passos do `<human-check>` estão registrados com resultado individual, ou explicitamente marcados como pendência de UAT com o motivo
    - Nenhum arquivo de schema, migração ou Server Action foi alterado nesta task (salvo correção pontual originada de um gate, que precisa ser justificada no SUMMARY)
  </acceptance_criteria>
  <done>Todos os gates automatizados da fase passam verdes e a verificação humana ponta a ponta dos 4 Success Criteria está registrada (ou explicitamente pendente).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Component → Client Component | Props atravessam a fronteira de serialização do RSC; tudo que chega ao cliente é dado já resolvido, não lógica |
| dado derivado → interface | A sugestão é exibida ao lado de um campo editável (`followUpDate`); confundir os dois levaria o admin a decisões erradas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-15 | Information Disclosure | Lead Inbound exibindo sugestão de reabordagem (violação de ORIGEM-03) | mitigate | Gate único em `computeSequenciaSugestao` (plano 10-01); os componentes cliente só renderizam quando a prop existe, e é proibido a eles ler `origemTipo`/`sequenciaPosicao`. Passo 4 do `<human-check>` é bloqueante |
| T-10-16 | Tampering | Cálculo migrando para o cliente e divergindo entre dashboard e pipeline | mitigate | Verificação automatizada da Task 2 reprova `addDays`/`sequenciaPosicao`/`sequenciaIntervalosDias` nos três componentes cliente; as duas rotas chamam a MESMA função pura |
| T-10-17 | Repudiation | Admin confundir a sugestão com a data real e perder um follow-up | mitigate | Cor neutra obrigatória (proibido teal/âmbar), rótulo explícito "Sugestão:" e `title` que declara que o campo Follow-up continua sendo a fonte oficial (D-06) |
| T-10-18 | Denial of Service | Agregação da timeline executada a cada request das duas rotas mais quentes | accept | `MAX(created_at) GROUP BY lead_id` sobre `interacoes` apoiado pelo índice `interacoes_lead_id_idx`, em volume solo (dezenas de leads, centenas de interações); a chamada entra no `Promise.all` já existente, sem serializar I/O |
| T-10-19 | Spoofing | Prop `sugestaoPorLead` manipulada no cliente via DevTools | accept | Só afeta a exibição na sessão do próprio admin; nenhuma escrita depende dessa prop, nenhum dado é persistido a partir dela |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo nesta fase; `CalendarClock` já está presente no `lucide-react` instalado (verificado contra `node_modules` no `10-UI-SPEC.md`) |
</threat_model>

<verification>
**Host de 4GB RAM: rodar UM COMANDO DE CADA VEZ, sequencialmente. `npm run build` só com o `npm run dev` PARADO.** Nunca encadear com `&`, nunca rodar dois em paralelo.

1. `npx tsc --noEmit`
2. `npx eslint` no escopo listado na Task 3
3. `npm run verify:schema`
4. `npm run verify:sequencia`
5. `npm run verify:origem-tipo`
6. `npm run guard:no-hard-delete`
7. `node scripts/verify-wa-contact-invariant.cjs`
8. `npm run test:lead-actions`
9. `npm run test:interacao-actions`
10. `npm run build`
11. Bloco `<human-check>` da Task 3 (10 passos), com `npm run dev` e nada mais rodando
</verification>

<success_criteria>
- SC#1 do ROADMAP: sequência configurável entregue (plano 10-03) e confirmada no passo 1 do `<human-check>`
- SC#2: lead Outbound com posição registrada mostra a próxima data sugerida, calculada na leitura, nas duas superfícies (passos 2, 3 e 6)
- SC#3: lead Inbound nunca recebe sugestão (passo 4, bloqueante)
- SC#4: templates de prova de valor disponíveis para reabordagem (passo 7 + evidência documental de SEQ-03)
- Todos os gates automatizados verdes, com a saída registrada
- `followUpDate` continua sendo o único campo editável e nunca é sobrescrito (passo 9)
</success_criteria>

<output>
Criar `.planning/phases/10-sequ-ncia-de-follow-up-escalonada/10-04-SUMMARY.md` ao terminar, registrando: saída de cada um dos 10 gates, a tabela de rastreabilidade dos 4 Success Criteria, a evidência documental de SEQ-03, e o resultado individual dos 10 passos do `<human-check>` (ou a pendência de UAT com o motivo).
</output>
