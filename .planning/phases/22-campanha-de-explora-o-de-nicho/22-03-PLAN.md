---
phase: 22-campanha-de-explora-o-de-nicho
plan: 03
type: execute
wave: 3
depends_on: []
gap_closure: true
files_modified:
  - src/lib/validations.ts
  - src/actions/lead-actions.ts
  - scripts/test-lead-actions.cjs
  - src/components/campanha-combobox.tsx
  - src/components/lead-form-dialog.tsx
  - src/components/lead-table.tsx
  - src/components/followup-dashboard.tsx
  - src/components/pipeline-board.tsx
  - src/app/leads/page.tsx
  - src/app/page.tsx
  - src/app/pipeline/page.tsx
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CAMPANHA-03]

must_haves:
  truths:
    - "Ao editar (ou criar) um lead, o usuário pode vincular esse lead a uma campanha existente por um campo OPCIONAL 'Campanha' no formulário de lead"
    - "Salvar o lead persiste `campanhaId` no banco sem alterar o `nichoId` do lead"
    - "O usuário pode desvincular o lead escolhendo 'Nenhuma campanha' — grava `campanhaId = NULL`"
    - "Criar/editar um lead SEM escolher campanha continua funcionando (campanhaId NULL) — o campo nunca é obrigatório e nenhum fluxo antigo regride"
    - "Só campanhas ativas (deletedAt IS NULL) aparecem para seleção; a campanha já vinculada continua visível mesmo se tiver sido removida"
    - "Um campanhaId forjado/inexistente submetido direto na Server Action é rejeitado antes de qualquer escrita"
  artifacts:
    - path: "src/lib/validations.ts"
      provides: "campanhaId opcional (nullable, FK) em leadBaseSchema, omitido em csvRowSchema"
      contains: "campanhaId"
    - path: "src/actions/lead-actions.ts"
      provides: "persistência de campanhaId em createLead e updateLead + campanhaExists()"
      contains: "campanhaExists"
    - path: "src/components/campanha-combobox.tsx"
      provides: "combobox pesquisável de campanhas ativas com opção 'Nenhuma campanha'"
      min_lines: 40
      exports: ["CampanhaCombobox"]
    - path: "src/components/lead-form-dialog.tsx"
      provides: "campo opcional 'Campanha' na seção Negócio, logo após o campo Nicho"
      contains: "CampanhaCombobox"
    - path: "scripts/test-lead-actions.cjs"
      provides: "casos automatizados de set/trocar/limpar campanhaId + nichoId intacto + id forjado"
      contains: "campanhaId"
    - path: "src/app/leads/page.tsx"
      provides: "query de campanhas passada como prop do Server Component (sem fetch client-side)"
      contains: "campanhas"
  key_links:
    - from: "src/components/lead-form-dialog.tsx"
      to: "src/components/campanha-combobox.tsx"
      via: "Controller name=campanhaId envolvendo CampanhaCombobox"
      pattern: "CampanhaCombobox"
    - from: "src/components/campanha-combobox.tsx"
      to: "FormData do lead-form-dialog"
      via: "input nativo oculto name=campanhaId do primitivo Combobox"
      pattern: "name = \"campanhaId\""
    - from: "src/actions/lead-actions.ts"
      to: "leads.campanhaId (src/db/schema.ts)"
      via: "campanhaId: parsed.data.campanhaId ?? null no insert e no update"
      pattern: "campanhaId: parsed\\.data\\.campanhaId \\?\\? null"
    - from: "src/app/leads/page.tsx"
      to: "src/components/lead-table.tsx"
      via: "prop campanhas alimentada por db.select().from(campanhas)"
      pattern: "from\\(campanhas\\)"
---

<objective>
Fechar o ÚNICO gap bloqueante da Fase 22 (`22-VERIFICATION.md`, truth #3 / SC3 do ROADMAP /
CAMPANHA-03): hoje só a coluna `leads.campanha_id` existe (schema + banco real), mas não há
NENHUMA forma de o usuário vincular um lead a uma campanha — o formulário de lead não tem o campo,
`lead-actions.ts` não lê nem grava `campanhaId`, e `leadBaseSchema` não tem o campo.

Este plano entrega o vínculo lead → campanha de ponta a ponta: contrato Zod, persistência nas
Server Actions com gate de FK forjada, combobox de campanhas ativas com opção de desvincular,
fiação como prop de Server Component nas 3 telas que abrem o formulário de lead, cobertura de
teste automatizada e a atualização de rastreabilidade em `REQUIREMENTS.md`.

Purpose: sem esse vínculo, a Fase 24 (PAINEL-01, "o painel agrega os leads *dela*") não tem de
onde tirar os leads da campanha — o painel depende deste campo já existir.
Output: campo "Campanha" funcional no formulário de lead + `campanhaId` persistido + CAMPANHA-03
marcado como Complete.

Nota sobre `depends_on: []`: os planos 22-01 (fundação de dados) e 22-02 (UI de campanha) já foram
executados e mergeados — todos os artefatos que este plano consome (tabela `campanhas`, coluna
`leads.campanha_id` aplicada em `data/crm.db`, tipo `Campanha`, DDL de campanhas já presente no
harness de teste) estão vivos em `src/` e no banco. Este plano roda sozinho via `--gaps-only`.

FORA DE ESCOPO (não implementar, mesmo se parecer barato): painel da campanha agregando leads
(PAINEL-01, Fase 24); filtro de campanha na toolbar de `/leads` ou coluna de campanha na tabela;
mapeamento de campanha no import CSV; correção dos achados WR-01..WR-04 do `22-REVIEW.md`; UI de
edição de campanha (`updateCampanha`, Fase 24).
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Vencedor/Desktop/crm-leads/.planning/PROJECT.md
@C:/Users/Vencedor/Desktop/crm-leads/.planning/ROADMAP.md
@C:/Users/Vencedor/Desktop/crm-leads/.planning/REQUIREMENTS.md
@C:/Users/Vencedor/Desktop/crm-leads/CLAUDE.md
@C:/Users/Vencedor/Desktop/crm-leads/.planning/phases/22-campanha-de-explora-o-de-nicho/22-VERIFICATION.md
@C:/Users/Vencedor/Desktop/crm-leads/.planning/phases/22-campanha-de-explora-o-de-nicho/22-01-SUMMARY.md
@C:/Users/Vencedor/Desktop/crm-leads/src/lib/validations.ts
@C:/Users/Vencedor/Desktop/crm-leads/src/actions/lead-actions.ts
@C:/Users/Vencedor/Desktop/crm-leads/src/components/nicho-combobox.tsx
@C:/Users/Vencedor/Desktop/crm-leads/src/components/motivo-perda-combobox.tsx
@C:/Users/Vencedor/Desktop/crm-leads/src/components/lead-form-dialog.tsx
@C:/Users/Vencedor/Desktop/crm-leads/src/components/campanha-list.tsx
</context>

<interfaces>
<!-- Contratos JÁ VIVOS em src/ (entregues por 22-01/22-02). Executor NÃO precisa explorar o
     codebase para descobrir isto — está tudo aqui. -->

De src/types/index.ts:
Campanha = { id: number; nichoId: number; oferta: string; metaConversao: string; janelaInicio: Date; janelaFim: Date; estado: "explorando" | "veredito_registrado" | "em_escala" | "abandonada"; deletedAt: Date | null; createdAt: Date; updatedAt: Date }
Nicho = { id: number; nome: string; deletedAt: Date | null; ... }
Lead — já inclui `campanhaId: number | null` (coluna aplicada em data/crm.db, verificada por PRAGMA no 22-VERIFICATION.md)

De src/db/schema.ts:
campanhas (tabela) — declarada ANTES de leads; leads.campanhaId = integer("campanha_id").references(() => campanhas.id, { onDelete: "set null" }) — NULLABLE, sem default. NENHUMA mudança de schema/migração é necessária neste plano.

De src/lib/validations.ts (estado ATUAL, antes deste plano):
leadBaseSchema — objeto NÃO-refinado; último campo declarado é `motivoPerdaId: z.preprocess((v) => (v === "" || v === null || v === undefined ? undefined : v), z.coerce.number().int().positive().optional())`
leadSchema = leadBaseSchema.refine(...)  // refine de motivoPerda obrigatório quando stage === "perdido"
csvRowSchema = leadBaseSchema.omit({ nichoId: true, followUpDate: true, motivoPerdaId: true }).extend({ nichoNome, origemTipo })
LeadFormValues = z.input<typeof leadSchema>

De src/actions/lead-actions.ts (estado ATUAL):
nichoExists(nichoId) e motivoPerdaExists(motivoPerdaId) — helpers de pré-checagem, ambos PROPOSITALMENTE indiferentes a `deletedAt`
isForeignKeyViolation(err) — backstop de SQLITE_CONSTRAINT_FOREIGNKEY
createLead(_prevState, formData) / updateLead(_prevState, formData) — safeParse → pré-checagens → try { insert/update } catch (FK) → revalidatePath

De src/components (idiomas a copiar, não reinventar):
NichoCombobox — combobox NÃO-criável, filtro `deletedAt === null || id === value`, input nativo oculto via prop `name`
MotivoPerdaCombobox — mesmo shell + item-sentinela de ação (`CREATE_VALUE = "__criar__"`) na lista `items`; tipo do item permite `value: number | string`
campanha-list.tsx linha 67 — idioma do RÓTULO de campanha: nome do nicho + " — " + oferta, com fallback de travessão quando o nicho não é encontrado
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contrato Zod + persistencia de campanhaId nas Server Actions + harness</name>
  <files>src/lib/validations.ts, src/actions/lead-actions.ts, scripts/test-lead-actions.cjs</files>
  <read_first>
    src/lib/validations.ts (arquivo a modificar — ler o doc-comment de leadBaseSchema e os campos
    `motivoPerdaId` e `interesse`: são os DOIS precedentes exatos de campo opcional com
    z.preprocess "vazio -> undefined", e a lista de `.omit()` de csvRowSchema logo abaixo);
    src/actions/lead-actions.ts (arquivo a modificar — ler `motivoPerdaExists` e o override
    `interesse: parsed.data.interesse ?? null` em createLead e updateLead: são os dois moldes
    linha-a-linha deste trabalho); scripts/test-lead-actions.cjs (arquivo a modificar — ler o
    array `manualAlters`, que JÁ contém a DDL de `campanhas` e do ALTER de `campanha_id`
    adicionada em 22-01, o helper `makeFormData`, o helper `callToleratingRevalidate` e os Casos
    13/14/14b, que são o molde exato de "campo opcional: set, limpar -> NULL")
  </read_first>
  <behavior>
    Comportamento esperado, exercitado pelos novos casos do harness (contra banco SQLite temporário
    isolado, nunca `data/crm.db`):
    - createLead com `campanhaId` de uma campanha existente: insere 1 linha, `campanha_id` persiste
      com esse id, e `nichoId` da linha permanece EXATAMENTE o nichoId submetido (intacto).
    - createLead SEM a chave `campanhaId` no FormData: insere normalmente, `campanha_id` fica NULL
      (nenhum fluxo antigo regride — este é o caso da esmagadora maioria dos leads).
    - updateLead trocando `campanhaId` da campanha A para a campanha B: a linha passa a apontar
      para B e o `nichoId` continua intacto.
    - updateLead com `campanhaId` string vazia (usuário escolheu "Nenhuma campanha"): coluna vira
      NULL, e o `nichoId` continua intacto.
    - createLead com `campanhaId` inexistente (ex.: "999999"): NÃO insere nenhuma linha e retorna
      `errors.campanhaId` com a mensagem "Selecione uma campanha válida."
    - csvRowSchema continua resolvendo uma linha de import SEM nenhuma noção de campanha
      (`success === true`) — o import CSV não ganha nem aceita o campo.
  </behavior>
  <action>
    Em src/lib/validations.ts: acrescentar o campo `campanhaId` ao objeto `leadBaseSchema`, como
    ÚLTIMO campo, logo depois de `motivoPerdaId`, com a MESMA forma dele — um `z.preprocess` que
    normaliza string vazia, null e undefined para undefined, envolvendo um
    `z.coerce.number().int().positive().optional()`. NÃO adicionar nenhum `.refine` de
    obrigatoriedade: o campo é opcional em qualquer etapa do funil, sem condicional por stage
    (diferente de motivoPerdaId). Estender o doc-comment de `leadBaseSchema` explicando que
    `campanhaId` é o TERCEIRO campo opcional do schema (CAMPANHA-03, Fase 22), FK nullable para
    `campanhas.id`, cujo vazio grava NULL via o mesmo idioma "undefined do Zod -> null explícito"
    da Server Action. Na chamada `.omit()` de `csvRowSchema`, acrescentar `campanhaId: true` à
    lista de campos omitidos, com um comentário curto explicando que o CSV do cowork nunca traz
    campanha e que omitir aqui fecha o vetor de injeção de FK pelo caminho de import (T-22-11) —
    mapeamento de campanha no import CSV está explicitamente fora do escopo desta fase.

    Em src/actions/lead-actions.ts: importar `campanhas` de @/db/schema junto dos imports de tabela
    já existentes. Criar o helper `campanhaExists(campanhaId: number): Promise<boolean>` como cópia
    linha-a-linha de `motivoPerdaExists`, selecionando o id da tabela `campanhas` — com doc-comment
    registrando que ele é PROPOSITALMENTE indiferente a `deletedAt` pelo mesmo motivo dos outros
    dois helpers: editar e salvar um lead cuja campanha foi removida (soft-delete) não pode passar
    a falhar sem o usuário ter mexido em nada no formulário.

    Em `createLead`: DEPOIS do gate de `motivoPerdaExists` e ANTES do bloco try, acrescentar um
    gate que só roda quando `parsed.data.campanhaId` não é nulo nem undefined — se a campanha não
    existir, retornar erro no campo `campanhaId` com a mensagem exata "Selecione uma campanha
    válida.". Dentro do `.values({...})` do insert, acrescentar `campanhaId: parsed.data.campanhaId
    ?? null` posicionado DEPOIS do spread de `parsed.data` (para nunca ser sobrescrito por ele),
    ao lado do override de `interesse` já existente e seguindo o mesmo comentário de idioma.

    Em `updateLead`: replicar o mesmo gate de existência de campanha (mesma posição relativa,
    mesma mensagem) e acrescentar `campanhaId: parsed.data.campanhaId ?? null` no `.set({...})`,
    também depois do spread de `parsed.data`. Comentar que este override é LOAD-BEARING: é o que
    materializa o desvincular — combobox limpo emite string vazia no FormData, o preprocess do Zod
    entrega undefined, e sem este `?? null` explícito o Drizzle omitiria a coluna do UPDATE e o id
    antigo ficaria preso no banco para sempre (mesma causa-raiz do WR-02 da Fase 11, resolvida do
    mesmo jeito). NÃO tocar em `nichoId` em nenhum dos dois caminhos — ele continua vindo do spread
    de `parsed.data` exatamente como hoje.

    Manter o bloco `catch (err)` das duas actions COMO ESTÁ (só o comentário muda): o objeto de
    erro do SQLite não diz qual FK foi violada, então na janela de corrida check-then-write um
    campanhaId apagado seria reportado como erro de nicho. Acrescentar um comentário registrando
    que a pré-checagem `campanhaExists` é a barreira primária e que essa imprecisão de rótulo é
    aceita conscientemente (T-22-14 do threat model). NÃO acrescentar `revalidatePath("/campanhas")`
    — nada em `/campanhas` lê leads nesta fase, e revalidação especulativa é exatamente o tipo de
    código sem consumidor que o `22-REVIEW.md` já apontou (IN-01).

    Em scripts/test-lead-actions.cjs: no bootstrap do banco temporário (logo depois do INSERT do
    nicho "Nutricionista" e ANTES de `setupDb.close()`), semear DUAS campanhas com um prepared
    statement inserindo nicho_id, oferta, meta_conversao, janela_inicio e janela_fim (as duas
    colunas de janela são inteiros unix epoch — usar o segundo atual e o segundo atual mais uma
    janela de dias), guardando os dois lastInsertRowid em variáveis no escopo da função
    (campanhaAId e campanhaBId). Acrescentar, ao final da sequência de casos existentes (numerar a
    partir de Caso 21), os casos descritos no bloco `behavior` acima, cada um usando
    `callToleratingRevalidate` para as chamadas que chegam ao `revalidatePath`, `countLeads()` para
    as asserções de contagem antes/depois, e leitura de volta do banco via `db.select().from(leads)`
    com `eq(leads.id, ...)` ou o mesmo padrão de `.orderBy(leads.id).limit(1).offset(before)` já
    usado nos Casos 13/14. Em TODOS os casos de campanha, além da asserção sobre `campanha_id`,
    incluir uma asserção explícita de que `row.nichoId` continua igual ao nichoId semeado — essa é
    a metade "sem perder o nicho geral do lead" do critério SC3 e precisa falhar ruidosamente se
    alguém regredir. Acrescentar também, dentro de `staticCheckFkWiring`, duas asserções estáticas:
    que o corpo de `createLead` e o corpo de `updateLead` contêm cada um a string
    `campanhaId: parsed.data.campanhaId ?? null` (prova de fiação que nenhum caso de runtime
    detecta se alguém trocar por um spread implícito).
  </action>
  <acceptance_criteria>
    `npm run test:lead-actions` sai com exit 0 e a saída contém as linhas OK dos casos novos de
    campanha; `npx tsc --noEmit` sai com exit 0;
    `grep -c "campanhaId" src/lib/validations.ts` retorna 2 ou mais (campo no leadBaseSchema +
    omit do csvRowSchema);
    `grep -n "campanhaExists" src/actions/lead-actions.ts` retorna 3 ou mais ocorrências
    (declaração + gate no createLead + gate no updateLead);
    `grep -c "campanhaId: parsed.data.campanhaId ?? null" src/actions/lead-actions.ts` retorna 2;
    `grep -c "Selecione uma campanha válida." src/actions/lead-actions.ts` retorna 2;
    `grep -c "nichoId" scripts/test-lead-actions.cjs` aumenta em relação ao valor atual (asserções
    de nicho intacto nos casos novos).
  </acceptance_criteria>
  <verify>
    <automated>npm run test:lead-actions &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>`campanhaId` é opcional em leadBaseSchema, omitido em csvRowSchema, e createLead/updateLead persistem/limpam o vínculo com gate de FK forjada — provado por casos automatizados que também asseguram nichoId intacto.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Combobox de campanha + campo no formulario de lead + fiacao das 3 telas</name>
  <files>src/components/campanha-combobox.tsx, src/components/lead-form-dialog.tsx, src/components/lead-table.tsx, src/components/followup-dashboard.tsx, src/components/pipeline-board.tsx, src/app/leads/page.tsx, src/app/page.tsx, src/app/pipeline/page.tsx</files>
  <read_first>
    src/components/nicho-combobox.tsx (molde PRIMÁRIO: combobox NÃO-criável, filtro
    `deletedAt === null || id === value`, prop `name` alimentando o input nativo oculto);
    src/components/motivo-perda-combobox.tsx (molde do ITEM-SENTINELA na lista `items` e do tipo de
    item com `value: number | string` — copiar essa mecânica, NÃO copiar a criação-na-hora);
    src/components/campanha-list.tsx linhas 29-33 e 60-73 (idioma do mapa nichoNameById via useMemo
    e do RÓTULO "nicho — oferta" com fallback de travessão);
    src/components/lead-form-dialog.tsx (arquivo a modificar — ler o tipo LeadFormDialogProps, o
    bloco `defaultValues` do useForm, o campo `nichoId` na seção "Negócio" e o campo condicional
    `motivoPerdaId`, que é o molde EXATO de Controller + combobox + `?? undefined` no onValueChange);
    src/app/leads/page.tsx (molde da query em Promise.all + threading de props, incluindo o
    comentário que explica por que `nichos`/`motivosPerda` vêm SEM filtro de deletedAt);
    src/components/lead-table.tsx linhas 71-76 e 354-365, src/components/followup-dashboard.tsx
    por volta das linhas 20-30 e 255-268, src/components/pipeline-board.tsx por volta das linhas
    20-30 e 248-259 (os 3 pontos de render do LeadFormDialog e seus tipos de props)
  </read_first>
  <action>
    Criar src/components/campanha-combobox.tsx ("use client"), modelado no
    `nicho-combobox.tsx` (sem criação-na-hora — criar campanha continua sendo só em `/campanhas`).
    Props: `campanhas` (array de Campanha), `nichos` (array de Nicho, usado só para o rótulo),
    `value` (número ou null), `onValueChange` (recebe número ou null), `name` com valor padrão
    "campanhaId", `disabled` e `invalid`. Definir uma constante-sentinela `NONE_VALUE` com o
    literal "__nenhuma__" e um tipo de item local cujo campo `value` aceita número OU string (mesma
    forma do `MotivoPerdaItem`) — o sentinela nunca colide com um id real porque ids são inteiros.
    Computar o mapa nichoNameById via useMemo a partir de `nichos`. Computar a lista base de itens
    via useMemo filtrando as campanhas por `deletedAt === null || id === value` — este filtro É a
    "query das campanhas ativas" exigida pelo gap: sem a exceção `id === value`, abrir o formulário
    de um lead cuja campanha foi removida mostraria o campo vazio e forçaria trocar de campanha
    (mesma causa-raiz já documentada em `.planning/debug/resolved/nicho-combobox-vazio.md`). O
    rótulo de cada item é o nome do nicho da campanha (via nichoNameById, fallback travessão) mais
    " — " mais a oferta, exatamente como `campanha-list.tsx`. Montar a lista final `items` com o
    item-sentinela PRIMEIRO (value NONE_VALUE, label "Nenhuma campanha") seguido dos itens de
    campanha — é ele que materializa a opção "nenhuma / desvincular" exigida pelo gap. O
    `selectedItem` é procurado SÓ entre os itens de campanha (nunca casa com o sentinela, que é
    string), então com `value` nulo o input fica vazio e mostra o placeholder. No handler
    `onValueChange` do Combobox: item nulo ou item cujo value é NONE_VALUE chamam o
    `onValueChange` da prop com null; qualquer outro chama com o value convertido para número.
    Placeholder do ComboboxInput: "Nenhuma campanha". Texto do ComboboxEmpty: "Nenhuma campanha
    encontrada.". Repassar `name`, `disabled` e `invalid` exatamente como o nicho-combobox faz —
    é o input nativo oculto gerado pelo primitivo Combobox que coloca `campanhaId` no FormData
    bruto do submit (e emite string vazia quando nada está selecionado, que é o que o
    `z.preprocess` da Task 1 normaliza para undefined e a action materializa como NULL).

    Em src/components/lead-form-dialog.tsx: acrescentar `Campanha` à lista de tipos importados de
    @/types e importar CampanhaCombobox. Acrescentar ao tipo LeadFormDialogProps a prop OBRIGATÓRIA
    `campanhas` (array de Campanha), com doc-comment no mesmo estilo do de `motivosPerda`
    explicando que ela alimenta o campo opcional de vínculo lead→campanha (CAMPANHA-03) e que a
    lista vem sem filtro de deletedAt de propósito (o filtro de seleção mora no combobox).
    Desestruturar `campanhas` na assinatura do componente. Em `defaultValues` do useForm,
    acrescentar `campanhaId` recebendo o campanhaId do lead em edição com fallback undefined —
    mesma forma exata da linha de `motivoPerdaId` logo acima. Acrescentar um novo `Field` na seção
    "Negócio", posicionado IMEDIATAMENTE DEPOIS do Field de "Nicho" e ANTES do Field de "Interesse"
    (a adjacência é intencional: o critério SC3 é "vincular à campanha SEM perder o nicho geral",
    e os dois campos coexistindo lado a lado tornam isso óbvio na tela). O Field usa FieldLabel
    "Campanha" com htmlFor campanhaId, um Controller com control do form e name campanhaId
    renderizando o CampanhaCombobox — repassando as listas `campanhas` e `nichos` recebidas por
    prop, o valor do campo convertido para número ou null (mesmo cast do campo motivoPerdaId), um
    onValueChange que chama field.onChange com o id ou undefined quando vier null (mesmo idioma do
    motivoPerdaId, para o campo sumir do payload como ausente e não como zero), e `invalid` ligado
    à presença de erro em campanhaId. FieldDescription: "Vincule este lead a uma campanha de
    exploração de nicho. Opcional." Fechar com um FieldError do erro de campanhaId. O campo NÃO é
    condicional a nenhuma etapa — renderiza sempre, tanto no modo criar quanto no modo editar.

    Fiação (todas as listas de campanha vêm do SERVIDOR como prop — proibido fetch client-side):
    em src/app/leads/page.tsx, src/app/page.tsx e src/app/pipeline/page.tsx, importar `campanhas`
    de @/db/schema e acrescentar `db.select().from(campanhas)` ao array do Promise.all já existente
    (junto de uma nova variável desestruturada `allCampanhas`), SEM filtro de deletedAt e com um
    comentário curto apontando o mesmo motivo já documentado ali para nichos/motivosPerda: o array
    serve de mapa id→rótulo e precisa conseguir exibir a campanha de um lead cujo vínculo aponta
    para uma campanha já removida; o filtro de seleção mora no combobox. Passar `allCampanhas` como
    prop `campanhas` para LeadTable, FollowupDashboard e PipelineBoard respectivamente. Em
    src/components/lead-table.tsx, src/components/followup-dashboard.tsx e
    src/components/pipeline-board.tsx: acrescentar `campanhas` (array de Campanha) ao tipo de props
    e à desestruturação, importar o tipo Campanha de @/types, e repassar `campanhas={campanhas}`
    para CADA render de LeadFormDialog nesses arquivos (o pipeline-board renderiza o dialog uma vez;
    conferir o arquivo e cobrir todas as ocorrências). A prop é DELIBERADAMENTE obrigatória, sem
    valor padrão de array vazio: um default silencioso deixaria o campo renderizar vazio para
    sempre se alguma tela ficasse sem fiação — que é exatamente o modo de falha silenciosa que
    produziu este gap.
  </action>
  <acceptance_criteria>
    `npx tsc --noEmit` sai com exit 0 (a prop obrigatória `campanhas` prova, por si só, que as 3
    telas foram fiadas — sem isso o build quebra); `npm run build` sai com exit 0;
    `npm run lint` sai com exit 0;
    `grep -c "CampanhaCombobox" src/components/lead-form-dialog.tsx` retorna 2 ou mais (import +
    uso);
    `grep -n "__nenhuma__" src/components/campanha-combobox.tsx` retorna 1 ou mais ocorrências;
    `grep -n "Nenhuma campanha" src/components/campanha-combobox.tsx` retorna 2 ou mais
    ocorrências (label do sentinela + placeholder/empty);
    `grep -n "deletedAt === null || " src/components/campanha-combobox.tsx` retorna 1 ocorrência;
    `grep -l "from(campanhas)" src/app/leads/page.tsx src/app/page.tsx src/app/pipeline/page.tsx`
    lista os 3 arquivos;
    `grep -c "campanhas={campanhas}" src/components/lead-table.tsx
    src/components/followup-dashboard.tsx src/components/pipeline-board.tsx` retorna 1 ou mais em
    cada um dos 3 arquivos.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <done>O formulário de lead tem o campo opcional "Campanha" (combobox de campanhas ativas + opção "Nenhuma campanha") nas 3 telas que abrem o dialog, alimentado por query de Server Component.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fechar rastreabilidade de CAMPANHA-03 e rodar o gate completo</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/REQUIREMENTS.md (linha 24 = checkbox de CAMPANHA-03; linha 103 = linha da tabela de
    Traceability de CAMPANHA-03; linha 134 = rodapé "Last updated"); .planning/ROADMAP.md seção
    "#### Phase 22" (critério de sucesso 3, que este plano fecha)
  </read_first>
  <action>
    Em .planning/REQUIREMENTS.md: marcar o checkbox de CAMPANHA-03 como concluído (trocar o `[ ]`
    por `[x]` na linha do requisito, preservando o texto do requisito literalmente inalterado). Na
    tabela de Traceability, trocar o Status da linha `| CAMPANHA-03 | Phase 22 |` de `Pending` para
    `Complete`. Atualizar a linha de rodapé "Last updated" mencionando o fechamento de CAMPANHA-03
    pelo plano 22-03 (gap closure). NÃO mexer em nenhum outro requisito, linha de tabela ou seção
    do arquivo.

    Rodar o gate completo do repositório antes de escrever o SUMMARY, e registrar o resultado de
    cada comando nele: o type check, o lint, o build, o harness de lead actions, o gate de schema e
    o guard de hard-delete (comandos exatos no bloco `verify` abaixo). Se qualquer um falhar,
    corrigir dentro do escopo deste plano antes de encerrar — não deixar débito para a
    re-verificação.

    No SUMMARY, registrar explicitamente como PENDENTE DE UAT HUMANO (não-bloqueante, host de 4GB
    sem navegador nesta sessão) os três checks de UI listados no bloco `verification` deste plano —
    o verificador da fase precisa dessa lista para não marcar como não-verificado o que é
    verificável só por browser.
  </action>
  <acceptance_criteria>
    `grep -n "CAMPANHA-03" .planning/REQUIREMENTS.md` mostra a linha do requisito começando com
    `- [x] **CAMPANHA-03**` e a linha de traceability terminando em `| Complete |`;
    `grep -c "| CAMPANHA-03 | Phase 22 | Pending |" .planning/REQUIREMENTS.md` retorna 0;
    todos os 6 comandos do bloco verify saem com exit 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npm run test:lead-actions &amp;&amp; npm run verify:schema &amp;&amp; npm run guard:no-hard-delete</automated>
  </verify>
  <done>CAMPANHA-03 marcado como concluído no checklist e como Complete na Traceability; gate completo do repositório verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (admin) -> lead-form-dialog.tsx -> createLead/updateLead | Server Action é um endpoint HTTP interno: o FormData chega inteiramente sob controle do cliente, incluindo um `campanhaId` que pode nunca ter passado pelo combobox |
| leadBaseSchema -> csvRowSchema -> bulkImportLeads | Todo campo adicionado ao schema base propaga por herança para o caminho de import CSV, a menos que seja explicitamente omitido |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-10 | Tampering | `campanhaId` forjado/inexistente no FormData de createLead/updateLead (mass-assignment de FK) | mitigate | Helper `campanhaExists()` (cópia de `motivoPerdaExists`) executado ANTES de qualquer escrita; `isForeignKeyViolation` no catch como backstop da janela de corrida check-then-write; caso automatizado no harness (Caso com id 999999) prova que nada é inserido |
| T-22-11 | Tampering | Injeção de `campanhaId` pelo caminho de import CSV (`csvRowSchema` herda de `leadBaseSchema`) | mitigate | `campanhaId: true` acrescentado ao `.omit()` de `csvRowSchema` — o campo não existe no contrato de linha de CSV, então nenhuma linha importada pode carregar vínculo de campanha |
| T-22-12 | Tampering | Vincular um lead a uma campanha soft-deletada via id forjado | accept | `campanhaExists` é deliberadamente indiferente a `deletedAt`, mesmo precedente de `nichoExists`/`motivoPerdaExists`: filtrar aqui faria a edição de um lead cuja campanha foi removida falhar sem o usuário ter mexido em nada. Ferramenta solo, single-user; nenhum ganho de privilégio nem exposição de dado de terceiro |
| T-22-13 | Information Disclosure | Enumeração de ids de campanha pelo combobox / pelo campo do formulário | accept | Mesma postura de todo o app (sem autenticação multi-usuário por design, CLAUDE.md); o admin é o dono de 100% dos dados exibidos |
| T-22-14 | Repudiation | Violação de FK na janela de corrida check-then-write é reportada como erro de `nichoId`, não de `campanhaId` | accept | O objeto de erro do SQLite não identifica qual FK falhou; a pré-checagem `campanhaExists` é a barreira primária e a imprecisão só aparece se a campanha for hard-deletada entre o SELECT e o INSERT (hard-delete não acontece no app — `guard:no-hard-delete`). Impacto: rótulo de erro errado, nenhuma escrita indevida |
| T-22-SC | Tampering | Supply chain (npm/pip/cargo) | accept | ZERO dependências novas neste plano — Combobox, Field, react-hook-form, Zod, Drizzle e o harness já estão instalados. Nenhum `npm install` é executado |
</threat_model>

<verification>
1. `npx tsc --noEmit` — exit 0
2. `npm run lint` — exit 0
3. `npm run build` — exit 0
4. `npm run test:lead-actions` — exit 0, com as linhas OK dos casos novos de campanha (set,
   trocar, limpar para NULL, id inexistente rejeitado, nichoId intacto em todos)
5. `npm run verify:schema` — exit 0 (nenhuma mudança de schema esperada; o gate só confirma que
   `campanhas`/`campanha_id` continuam íntegros em `data/crm.db`)
6. `npm run guard:no-hard-delete` — exit 0
7. UAT humano NÃO-BLOQUEANTE (host de 4GB sem navegador nesta sessão) — registrar no SUMMARY como
   pendente, não bloqueia o fechamento:
   a. Abrir `/leads`, clicar numa linha para editar um lead, conferir que a seção "Negócio" mostra
      o campo "Campanha" logo abaixo de "Nicho"; escolher uma campanha e salvar. Esperado: toast
      "Lead salvo com sucesso.", dialog fecha, e reabrir o mesmo lead mostra a campanha escolhida
      e o nicho original inalterado.
   b. Reabrir o mesmo lead, escolher "Nenhuma campanha" e salvar. Esperado: reabrir mostra o campo
      de campanha vazio e o nicho ainda inalterado.
   c. Abrir o dialog de lead pelo `/pipeline` e pelo dashboard `/` (Follow-ups). Esperado: o campo
      "Campanha" aparece nas duas telas com a mesma lista de campanhas.
</verification>

<success_criteria>
O critério de sucesso #3 do ROADMAP da Fase 22 / CAMPANHA-03 está satisfeito por código + dados:
o usuário vincula opcionalmente um lead a uma campanha existente pelo formulário de lead, pode
desvincular, o `nichoId` do lead nunca é afetado, um `campanhaId` forjado é rejeitado antes de
qualquer escrita, e tudo isso é provado por casos automatizados em `npm run test:lead-actions` —
com CAMPANHA-03 marcado como Complete em `.planning/REQUIREMENTS.md`.
</success_criteria>

<output>
Create `.planning/phases/22-campanha-de-explora-o-de-nicho/22-03-SUMMARY.md` when done
</output>
</content>
</invoke>
