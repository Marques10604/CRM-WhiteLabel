---
phase: 22-campanha-de-explora-o-de-nicho
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/components/campanha-form-dialog.tsx
  - src/components/campanha-estado-badge.tsx
  - src/components/campanha-list.tsx
  - src/app/campanhas/page.tsx
  - "src/app/campanhas/[id]/page.tsx"
  - src/components/app-sidebar.tsx
autonomous: true
requirements: [CAMPANHA-01, CAMPANHA-02, CAMPANHA-04]

must_haves:
  truths:
    - "Usuário cria uma campanha escolhendo um nicho da lista existente, definindo oferta (texto livre), janela de tempo (default ~90 dias, editável) e meta de conversão"
    - "A campanha exibe visualmente seu estado (explorando / veredito registrado / em escala / abandonada)"
    - "Usuário lista todas as campanhas já criadas e navega até o detalhe de qualquer uma delas"
    - "Lista vazia mostra estado vazio com texto + CTA, nunca silêncio"
  artifacts:
    - path: "src/components/campanha-form-dialog.tsx"
      provides: "dialog de criação de campanha (nicho via NichoCombobox, oferta, meta, janela início/fim via Popover+Calendar)"
      min_lines: 80
    - path: "src/components/campanha-list.tsx"
      provides: "listagem de campanhas + botão 'Nova campanha' + estado vazio"
      min_lines: 40
    - path: "src/components/campanha-estado-badge.tsx"
      provides: "badge visual dos 4 estados de campanha"
      contains: "ESTADO_LABEL"
    - path: "src/app/campanhas/page.tsx"
      provides: "rota /campanhas"
    - path: "src/app/campanhas/[id]/page.tsx"
      provides: "rota de detalhe /campanhas/[id] (nicho+oferta, estado, meta, janela)"
  key_links:
    - from: "src/components/campanha-list.tsx"
      to: "src/app/campanhas/[id]/page.tsx"
      via: "Link href=/campanhas/{id}"
      pattern: "href=.\\{`/campanhas/\\$\\{"
    - from: "src/components/app-sidebar.tsx"
      to: "src/app/campanhas/page.tsx"
      via: "NAV_ITEMS entry href=/campanhas"
      pattern: "/campanhas"
    - from: "src/components/campanha-form-dialog.tsx"
      to: "src/actions/campanha-actions.ts (createCampanha)"
      via: "useActionState(createCampanha, undefined) + formAction"
      pattern: "createCampanha"
---

<objective>
Construir a interface de criação e listagem de campanhas (CAMPANHA-01/02/04): dialog de criação
(nicho + oferta + janela + meta), listagem em `/campanhas` com estado vazio, página de detalhe
`/campanhas/[id]` (âncora que as Fases 23/24 vão enriquecer com diagnóstico/veredito/painel — sem
retrabalho desta fase) e o item de navegação na sidebar.

Purpose: dar ao usuário uma forma real de criar e ver campanhas, materializando o "objeto de 1ª
classe" que é o diferencial do produto (ver `.planning/DIRECAO-v1.7-2026-09-04.md`).
Output: rota `/campanhas` navegável a partir da sidebar, criação funcional, estado visível.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Vencedor/Desktop/crm-leads/.planning/PROJECT.md
@C:/Users/Vencedor/Desktop/crm-leads/.planning/ROADMAP.md
@C:/Users/Vencedor/Desktop/crm-leads/CLAUDE.md
@C:/Users/Vencedor/Desktop/crm-leads/src/components/tarefa-form-dialog.tsx
@C:/Users/Vencedor/Desktop/crm-leads/src/components/lead-table-toolbar.tsx
@C:/Users/Vencedor/Desktop/crm-leads/src/components/nicho-combobox.tsx
@C:/Users/Vencedor/Desktop/crm-leads/src/components/etapa-badge.tsx
@C:/Users/Vencedor/Desktop/crm-leads/src/components/nicho-manager.tsx
@C:/Users/Vencedor/Desktop/crm-leads/src/app/nichos/page.tsx
@C:/Users/Vencedor/Desktop/crm-leads/src/app/importar/[batchId]/page.tsx
@C:/Users/Vencedor/Desktop/crm-leads/src/components/app-sidebar.tsx
</context>

<interfaces>
<!-- Contratos produzidos pelo plano 22-01 (Wave 1), já vivos em src/ e em data/crm.db. -->

De src/types/index.ts:
Campanha = { id: number; nichoId: number; oferta: string; metaConversao: string; janelaInicio: Date; janelaFim: Date; estado: "explorando" | "veredito_registrado" | "em_escala" | "abandonada"; deletedAt: Date | null; createdAt: Date; updatedAt: Date }

De src/lib/validations.ts:
campanhaSchema (nichoId, oferta, metaConversao, janelaInicio, janelaFim — refine janelaFim > janelaInicio); CampanhaFormValues = z.input<typeof campanhaSchema>

De src/actions/campanha-actions.ts:
createCampanha(_prevState, formData): Promise<{ success: true; campanha?: Campanha } | { errors: Record<string, string[] | undefined> } | undefined>

De src/db/schema.ts:
campanhas (tabela), nichos (tabela, `export const nichos = sqliteTable("subnichos", ...)`)
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Dialog de criacao de campanha + badge de estado</name>
  <files>src/components/campanha-form-dialog.tsx, src/components/campanha-estado-badge.tsx</files>
  <read_first>
    src/components/tarefa-form-dialog.tsx (molde COMPLETO de dialog de criacao: useForm +
    zodResolver + useActionState + DiscardChangesDialog + onSubmit que monta FormData bruto do DOM
    dentro de startTransition); src/components/lead-table-toolbar.tsx (molde EXATO de
    Popover+Calendar+Button trigger formatado dd/MM/yyyy, usar para os 2 campos de janela);
    src/components/nicho-combobox.tsx (componente PRONTO a importar, nao recriar); src/components/etapa-badge.tsx
    (molde EXATO de badge por enum: STAGE_LABEL/STAGE_TOKEN separados, Badge variant=outline +
    border-transparent)
  </read_first>
  <action>
    Criar src/components/campanha-estado-badge.tsx (SEM "use client" — mesmo idioma server-safe de
    etapa-badge.tsx). Exportar type CampanhaEstado = Campanha["estado"]. Definir
    ESTADO_LABEL: Record&lt;CampanhaEstado, string&gt; com explorando: "Explorando",
    veredito_registrado: "Veredito registrado", em_escala: "Em escala",
    abandonada: "Abandonada". Definir ESTADO_TOKEN: Record&lt;CampanhaEstado, string&gt; reaproveitando
    a escala --status-* ja existente (Fase 19): explorando mapeia para bg-status-info
    text-status-info-foreground, veredito_registrado mapeia para bg-status-warning
    text-status-warning-foreground, em_escala mapeia para bg-status-success text-status-success-foreground,
    abandonada mapeia para bg-status-danger text-status-danger-foreground. Exportar o componente
    CampanhaEstadoBadge recebendo a prop estado e renderizando um Badge variant outline com
    className cn("border-transparent shrink-0", ESTADO_TOKEN[estado]) e o texto ESTADO_LABEL[estado]
    dentro.

    Criar src/components/campanha-form-dialog.tsx ("use client"), CRIACAO APENAS (sem modo edicao
    nesta fase — CAMPANHA-01 so pede criar; edicao de campanha nao e requisito desta fase). Props:
    open (boolean), onOpenChange (funcao), nichos (Nicho[]). Usar
    useActionState com createCampanha (import de @/actions/campanha-actions) e estado inicial
    undefined, e useForm tipado com CampanhaFormValues usando zodResolver(campanhaSchema)
    (import campanhaSchema e o tipo CampanhaFormValues de @/lib/validations). Defaults do form:
    nichoId undefined, oferta string vazia, metaConversao string vazia,
    janelaInicio = startOfDay(new Date()), janelaFim = startOfDay(addDays(new Date(), 90))
    (import startOfDay e addDays de date-fns — CAMPANHA-01 pede janela de ~90 dias, editavel).
    Campos, dentro dos primitivos Field/FieldContent/FieldLabel/FieldError de @/components/ui/field:
    (1) "Nicho" — um Controller para o campo nichoId envolvendo o NichoCombobox existente,
    repassando a lista de nichos recebida por prop, o valor atual do campo (ou null), o
    onValueChange chamando field.onChange, e invalid ligado a presenca de erro em nichoId
    (reaproveita o componente ja existente do projeto — consistencia com lead-form-dialog.tsx e
    csv-import-preview-table.tsx, em vez de recriar um Select novo para o mesmo conceito de
    selecao de nicho). (2) "Oferta" — um Textarea (de @/components/ui/textarea) registrado via
    form.register("oferta"), com FieldDescription "O que voce pretende vender nesse nicho.".
    (3) "Meta de conversao" — um Input registrado via form.register("metaConversao"), com
    placeholder "Ex: 3 leads fechados, 10% de resposta...". (4) "Inicio da janela" e (5) "Fim da
    janela" — dois campos Controller independentes, cada um com seu proprio useState de Popover
    aberto/fechado, seguindo EXATAMENTE o padrao ja usado em lead-table-toolbar.tsx: um Popover
    contendo um PopoverTrigger renderizado como Button variant outline mostrando um icone de
    calendario mais a data formatada dd/MM/yyyy (ou o texto "Selecionar" quando vazio), e um
    PopoverContent com o Calendar em modo single cuja selecao atualiza o campo via field.onChange
    (normalizando para inicio do dia) e fecha o popover — acompanhado de um input hidden nomeado
    janelaInicio (e outro janelaFim) com o valor ISO da data selecionada, para alimentar o
    FormData bruto do submit (mesmo contrato de followUpDate/data nos moldes lidos). A funcao
    onSubmit monta um novo FormData a partir do formRef.current e chama formAction dentro de
    startTransition (exigencia do React 19). Um useEffect observando o state do useActionState:
    em caso de sucesso, exibe toast de sucesso "Campanha criada.", reseta o form e fecha o dialog
    via onOpenChange(false); em caso de erro, exibe toast de erro generico. O fechamento do dialog
    passa pelo guard de descarte (DiscardChangesDialog) quando form.formState.isDirty, mesmo padrao
    de closeWithDiscardGuard dos moldes lidos. O DialogFooter tem os botoes "Cancelar" e
    "Salvar" (mostrando "Salvando..." durante pending).
  </action>
  <acceptance_criteria>
    npx tsc --noEmit sai limpo; grep -n "NichoCombobox" src/components/campanha-form-dialog.tsx
    retorna 1 ou mais ocorrencias; grep -n "createCampanha" src/components/campanha-form-dialog.tsx
    retorna 1 ou mais ocorrencias; grep -n "addDays(new Date(), 90)" src/components/campanha-form-dialog.tsx
    retorna 1 ocorrencia (default de 90 dias); grep -c "PopoverTrigger" src/components/campanha-form-dialog.tsx
    retorna 2 (inicio + fim); grep -n "ESTADO_LABEL" src/components/campanha-estado-badge.tsx
    retorna 1 ou mais ocorrencias e grep -c "explorando\|veredito_registrado\|em_escala\|abandonada"
    src/components/campanha-estado-badge.tsx retorna 8 ou mais (4 estados vezes label e token).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Dialog de criacao funcional (nicho/oferta/meta/janela com default 90d) e badge dos 4 estados existem, tsc limpo.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Listagem, detalhe e navegacao</name>
  <files>src/components/campanha-list.tsx, src/app/campanhas/page.tsx, src/app/campanhas/[id]/page.tsx, src/components/app-sidebar.tsx</files>
  <read_first>
    src/app/nichos/page.tsx (molde de pagina server component simples: query + componente
    client); src/app/importar/[batchId]/page.tsx (molde EXATO de rota dinamica Next 16: params
    tipado como Promise de objeto com o parametro, resolvido via await); src/components/lead-table.tsx
    por volta das linhas 177-183 (idioma de estado vazio: titulo + paragrafo, borda tracejada);
    src/components/app-sidebar.tsx (NAV_ITEMS — adicionar entrada nova)
  </read_first>
  <action>
    Criar src/components/campanha-list.tsx ("use client"). Props: campanhas (Campanha[]) e nichos
    (Nicho[]). Estado local dialogOpen via useState. Computar um mapa nichoNameById via useMemo a
    partir da lista de nichos (mesmo idioma de lead-table.tsx/lead-form-dialog.tsx). Renderizar um
    botao "Nova campanha" (icone Plus de lucide-react) que abre o dialog. Se a lista de campanhas
    estiver vazia, renderizar um estado vazio no MESMO idioma visual do trecho de lead-table.tsx
    lido (borda tracejada arredondada, padding generoso, texto centralizado), com titulo "Nenhuma
    campanha criada ainda" e paragrafo "Crie sua primeira campanha para organizar a exploracao de
    um nicho." em tom mutado. Senao, renderizar uma lista de links (um Link do Next por campanha,
    apontando para /campanhas/{id}, key pelo id) estilizados como card (borda, fundo card, hover
    accent) mostrando: um titulo truncado combinando o nome do nicho (via nichoNameById, com
    fallback de travessao) e a oferta da campanha separados por travessao; um subtitulo com a
    janela formatada (data de inicio ate data de fim, formato dd/MM/yyyy, usando format de
    date-fns); e o CampanhaEstadoBadge com o estado da campanha alinhado a direita. Renderizar o
    CampanhaFormDialog no fim do componente, controlado pelo estado dialogOpen e recebendo a lista
    de nichos.

    Criar src/app/campanhas/page.tsx (server component, sem "use client"). Buscar a lista de
    campanhas ativas (filtro isNull em deletedAt, ordenado por createdAt decrescente) e a lista
    completa de nichos SEM filtro de deletedAt (mesmo idioma de leads/page.tsx: o array serve de
    mapa id-para-nome, e filtrar removidos aqui quebraria o nome exibido de uma campanha apontando
    para um nicho ja removido). Renderizar um titulo "Campanhas" e o componente CampanhaList
    recebendo as duas listas buscadas.

    Criar src/app/campanhas/[id]/page.tsx (server component). A funcao de pagina recebe params
    tipado como Promise de um objeto com a chave id (string), resolvido via await no topo da
    funcao (mesmo contrato exato de src/app/importar/[batchId]/page.tsx). Converter o id para
    numero; se nao for um inteiro positivo, chamar notFound() (importado de next/navigation).
    Buscar a campanha por id fazendo um leftJoin com a tabela de nichos para trazer o nome do
    nicho junto (select explicito com dois campos: o objeto campanha inteiro e o nome do nicho).
    Se a busca nao retornar linha, ou se a campanha encontrada tiver deletedAt preenchido, chamar
    notFound() tambem. Renderizar um cabecalho com o titulo combinando nome do nicho (com fallback
    de travessao) e a oferta, ao lado do CampanhaEstadoBadge com o estado da campanha; e um bloco
    com borda mostrando duas linhas rotuladas "Meta de conversao" (valor bruto do campo) e "Janela
    de tempo" (data de inicio ate data de fim, formato dd/MM/yyyy). Este layout e deliberadamente
    minimalista — as Fases 23 (diagnostico) e 24 (veredito/painel) vao ADICIONAR secoes a esta
    mesma pagina em migracoes futuras, nao retrabalhar esta.

    Em src/components/app-sidebar.tsx, adicionar uma nova entrada ao array NAV_ITEMS com href
    "/campanhas", label "Campanhas" e o icone Target importado de lucide-react (adicionar Target
    a lista de imports ja existente do lucide-react no topo do arquivo), posicionada logo apos a
    entrada de href "/pipeline" e antes da entrada de href "/relatorios" (fluxo logico: pipeline
    de leads, depois campanhas de nicho, depois relatorios agregados).
  </action>
  <acceptance_criteria>
    npx tsc --noEmit sai limpo; npm run build sai com exit 0 e gera as rotas /campanhas e
    /campanhas/[id] na saida do Next; grep -n "href: \"/campanhas\"" src/components/app-sidebar.tsx
    retorna 1 ocorrencia; grep -n "CampanhaList" src/app/campanhas/page.tsx retorna 1 ocorrencia;
    grep -n "notFound" src/app/campanhas/[id]/page.tsx retorna 1 ou mais ocorrencias; grep -n
    "Nenhuma campanha criada ainda" src/components/campanha-list.tsx retorna 1 ocorrencia.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>/campanhas lista (com estado vazio) e cria campanhas; /campanhas/[id] mostra o detalhe; item "Campanhas" navegavel na sidebar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser (admin) -> campanha-form-dialog.tsx -> createCampanha | Entrada de formulario client-side, validacao autoritativa ja mora no Server Action (plano 22-01) |
| URL -> /campanhas/[id] | Parametro de rota vindo diretamente da URL — id numerico arbitrario digitavel pelo usuario |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-07 | Tampering | /campanhas/[id] (id nao-numerico ou negativo na URL) | mitigate | checagem de inteiro positivo antes de qualquer query; notFound() do Next para entrada invalida |
| T-22-08 | Information Disclosure | /campanhas/[id] de uma campanha soft-deletada | mitigate | campanha com deletedAt preenchido tambem chama notFound() — campanha removida nao e exibida por id direto |
| T-22-09 | Spoofing / Information Disclosure | Enumeracao sequencial de ids em /campanhas/[id] | accept | Ferramenta solo, single-user, sem dado sensivel de terceiro (mesma postura de todo o resto do app — sem autenticacao multi-usuario por design, CLAUDE.md) |
| T-22-SC | Tampering | Supply chain (npm) | accept | Zero dependencias novas neste plano — todos os primitivos de UI (Popover, Calendar, Combobox, Badge) e date-fns ja instalados |
</threat_model>

<verification>
1. npx tsc --noEmit — exit 0
2. npm run lint — exit 0
3. npm run build — exit 0, rotas /campanhas e /campanhas/[id] presentes na saida
4. Query direta: script Node com better-sqlite3 insere uma linha de teste em campanhas (nicho_id
   valido) e confirma leitura via SELECT, depois remove a linha de teste (housekeeping, nao afeta
   dado real)
5. UAT humano NAO-BLOQUEANTE (host 4GB): abrir /campanhas no navegador, criar uma campanha,
   confirmar navegacao ate o detalhe — registrar como pendente no SUMMARY, nao bloqueia o
   fechamento do plano
</verification>

<success_criteria>
Usuario consegue criar uma campanha pela UI, ve o estado dela, lista todas as campanhas existentes
e navega ate o detalhe de qualquer uma — CAMPANHA-01/02/04 satisfeitos por code+data.
</success_criteria>

<output>
Create `.planning/phases/22-campanha-de-explora-o-de-nicho/22-02-SUMMARY.md` when done
</output>
