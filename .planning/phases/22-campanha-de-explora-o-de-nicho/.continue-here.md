---
context: phase
phase: 22-campanha-de-explora-o-de-nicho
task: 1
total_tasks: 3
status: not_started
last_updated: 2026-09-05T02:09:47.930Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

Nenhuma constraint bloqueante identificada nesta sessão (sessão foi só verificação de progresso, sem execução).

## Critical Anti-Patterns

Nenhum descoberto nesta sessão.

<current_state>
Milestone v1.7 "Exploração de Nicho", Fase 22 de 25 — "Campanha de Exploração de Nicho", a
primeira fase do milestone. Os dois planos da fase já foram escritos numa sessão anterior
(`22-01-PLAN.md` e `22-02-PLAN.md`, ambos untracked pelo git ainda), mas **nenhuma execução
começou** — 0 `SUMMARY.md`, 0 arquivo de `src/` tocado. Esta sessão rodou só `/gsd-progress`
(read-only) pra confirmar a posição e agora está pausando a pedido do usuário.
</current_state>

<completed_work>

Nada executado ainda. Trabalho de planejamento (22-01-PLAN.md, 22-02-PLAN.md) já existia no
disco no início desta sessão — não foi criado agora.
</completed_work>

<remaining_work>

**22-01-PLAN.md** (wave 1, sem dependências):
- Task 1: Contratos — tabela `campanhas` (nicho_id FK restrict, oferta, meta_conversao,
  janela_inicio, janela_fim, estado enum default `explorando`, deleted_at, timestamps) +
  coluna `leads.campanhaId` (nullable, FK `onDelete: set null`) + tipos `Campanha`/`NewCampanha`
  + `campanhaSchema`/`campanhaUpdateSchema` em Zod
- Task 2: [BLOCKING no próprio plano] Migração real e idempotente
  (`scripts/migrate-campanhas.cjs`) rodada contra `data/crm.db` de verdade — não só gerada em
  disco — + extensão do gate `scripts/verify-schema.cjs` pra cobrir a tabela nova
- Task 3: Server Actions `createCampanha` / `updateCampanha` / `softDeleteCampanha`
  (validação Zod `safeParse` antes de qualquer escrita)

**22-02-PLAN.md** (wave 2, depende de 22-01):
- Task 1: `campanha-form-dialog.tsx` (criação via `NichoCombobox`, oferta, meta, janela via
  Popover+Calendar) + `campanha-estado-badge.tsx` (4 estados)
- Task 2: `campanha-list.tsx` (listagem + botão "Nova campanha" + estado vazio) + rota
  `/campanhas` + rota de detalhe `/campanhas/[id]` + link de navegação na sidebar
</remaining_work>

<decisions_made>

Nenhuma decisão nova nesta sessão — só leitura de estado.
</decisions_made>

<blockers>
- STATE.md desatualizado: diz "Plano: — (ainda não planejado)" e sugere
  `/gsd-plan-phase 22`, mas os planos já existem. Não bloqueia — corrigir texto quando a
  fase fechar.
</blockers>

## Required Reading (in order)
1. `.planning/phases/22-campanha-de-explora-o-de-nicho/22-01-PLAN.md` — must_haves, tasks e
   Trust Boundaries/STRIDE (o plano inclui ameaça modelada, ler a seção inteira antes de
   executar a Task 2 que mexe no schema real)
2. `.planning/phases/22-campanha-de-explora-o-de-nicho/22-02-PLAN.md` — must_haves e tasks de UI
3. `.planning/STATE.md` seção "Session Continuity" — contexto do milestone v1.7 e das Fases
   23-25 que dependem desta

## Critical Anti-Patterns (do NOT repeat these)
Nenhum específico desta fase ainda — ver padrões recorrentes já documentados em
`.planning/STATE.md` (ex.: `drizzle-kit push` não lida bem com `DEFAULT 0`/colunas sem
default em SQLite — Fases 06/07/08 tiveram que aplicar `ALTER TABLE` manual; Task 2 desta
fase já prevê migração manual `.cjs` pelo mesmo motivo).

## Infrastructure State
- Host com 4GB RAM — evitar processos paralelos (ver `feedback_4gb_ram_avoid_parallel` na
  memória do usuário). Fechar processos node antes de rodar `npm run build`.
- `data/crm.db` é o banco real (gitignored) — Task 2 do 22-01 escreve nele de verdade.
- Nenhum servidor/watcher ficou rodando em background ao final desta sessão.

<context>
Sessão de check-in rápido: usuário chamou `/clear` e depois pediu pra continuar; rodei
`/gsd-progress` pra situar (Fase 22 tem planos prontos, zero execução, sem débito de UAT
entre fases, 13 todos de backlog PME pendentes fora do milestone). Antes de qualquer execução
o usuário pediu pra salvar o estado e foi dormir. Não houve mudança de código nesta sessão.
</context>

<next_action>
Start with: `/gsd-execute-phase 22` — começa por `22-01-PLAN.md` Task 1 (schema `campanhas` +
FK opcional em `leads.campanhaId` + tipos/Zod). Se preferir confirmar o plano antes de
executar, `/gsd-plan-review-convergence 22` ou uma leitura manual do 22-01-PLAN.md primeiro.
</next_action>
