---
phase: 04-follow-up-dashboard-whatsapp-outreach
plan: 04
type: execute
wave: 4
depends_on: ["04-02", "04-03"]
files_modified:
  - src/actions/lead-actions.ts
  - src/hooks/use-first-contact-trigger.ts
  - src/components/lead-form-dialog.tsx
  - src/app/leads/page.tsx
  - src/components/lead-table.tsx
  - src/components/followup-dashboard.tsx
  - src/components/pipeline-board.tsx
autonomous: true
requirements: [WA-04]
must_haves:
  truths:
    - "Ao salvar um lead NOVO (criação manual), o modal de preview de 1º contato abre automaticamente com o template padrão de primeiro contato"
    - "Fechar o preview auto-aberto sem enviar NÃO desfaz a criação do lead (o lead já está salvo)"
    - "O auto-gatilho dispara em toda criação manual (sem opt-out nesta fase)"
    - "Editar um lead existente NÃO dispara o auto-gatilho"
  artifacts:
    - path: "src/hooks/use-first-contact-trigger.ts"
      provides: "Hook compartilhado reutilizável (D-18) para abrir o preview de 1º contato"
      exports: ["useFirstContactTrigger"]
    - path: "src/actions/lead-actions.ts"
      provides: "createLead retorna o lead criado ({ success: true; lead })"
      contains: ".returning()"
  key_links:
    - from: "src/components/lead-form-dialog.tsx"
      to: "src/hooks/use-first-contact-trigger.ts"
      via: "dispara trigger no sucesso de createLead (não em updateLead)"
      pattern: "useFirstContactTrigger"
    - from: "src/components/lead-form-dialog.tsx"
      to: "src/components/whatsapp-preview-dialog.tsx"
      via: "renderiza o preview auto-aberto de 1º contato"
      pattern: "WhatsAppPreviewDialog"
    - from: "src/actions/lead-actions.ts"
      to: "src/db (leads)"
      via: "db.insert(...).returning() retorna o lead inserido"
      pattern: "\\.returning\\(\\)"
---

<objective>
Fechar o loop de WA-04: ao criar manualmente um lead, sugerir automaticamente o envio do WhatsApp de primeiro contato abrindo o modal de preview já preenchido — sem bloquear a criação. Construído como um hook compartilhado reutilizável (`useFirstContactTrigger`) para que a futura importação CSV (Fase 2) reuse o mesmo mecanismo sem reescrita (D-18/D-19/D-20/D-21, discrição travada).

Purpose: Último corte vertical da fase — conecta criação de lead ao envio de WhatsApp. Reusa o preview do 04-03 e os templates do 04-02.
Output: Preview de 1º contato abre automaticamente após criar lead, em qualquer superfície de criação (/, /leads, /pipeline).
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-CONTEXT.md
@.planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-RESEARCH.md
@.planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-UI-SPEC.md
@.planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-PATTERNS.md

<interfaces>
<!-- Contratos existentes + do 04-02/04-03. -->

src/actions/lead-actions.ts (createLead atual):
  ActionState = { success: true } | { errors: Record<string,string[]|undefined> } | undefined
  createLead(_prevState, formData): faz db.insert(leads).values(parsed.data); revalidatePath("/"); revalidatePath("/pipeline"); return { success: true }
  // MUDANÇA: usar db.insert(leads).values(parsed.data).returning() → return { success: true, lead: inserted }; adicionar revalidatePath("/leads")

src/components/lead-form-dialog.tsx (efeito success atual, linhas 107-116):
  useEffect: if (state && "success" in state && state.success) { toast; form.reset(); onOpenChange(false); }
  // é modo create quando `lead` prop é undefined (isEditMode = Boolean(lead))

Alvo do hook (04-RESEARCH Pattern 2, linhas 269-281):
  useFirstContactTrigger(defaultTemplate: Template | undefined) → { open, template, lead, subnichoNome, trigger(lead, subnichoNome), close() }

WhatsAppPreviewDialog (04-03): props { open, onOpenChange, lead, subnichoNome, templates, defaultTipo }
Template (04-02): tipo "primeiro_contato"|"follow_up"|"prova_valor"; isDefault: boolean
Lead: ver 04-01.

Superfícies que criam lead (todas renderizam LeadFormDialog em modo create):
  src/components/lead-table.tsx (via /leads/page.tsx), src/components/pipeline-board.tsx (via /pipeline/page.tsx), src/components/followup-dashboard.tsx (via /page.tsx)
</interfaces>

Nota: pipeline-board e followup-dashboard já recebem `templates` como prop desde o 04-03; este plano acrescenta a passagem de `templates` também em lead-table (via /leads/page.tsx).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: createLead retorna o lead + hook useFirstContactTrigger</name>
  <files>src/actions/lead-actions.ts, src/hooks/use-first-contact-trigger.ts</files>
  <behavior>
    - createLead com dados válidos retorna { success: true, lead } onde lead tem id gerado (via .returning())
    - createLead com dados inválidos continua retornando { errors } (sem mudança de contrato de erro)
    - useFirstContactTrigger inicia com open=false; após trigger(lead, subnichoNome), open=true e lead/subnichoNome preenchidos; close() volta open=false
  </behavior>
  <read_first>
    - src/actions/lead-actions.ts (createLead/updateLead — não quebrar updateLead)
    - src/components/pipeline-board.tsx (padrão de estado com união discriminada — analog do hook)
    - src/types/index.ts (Lead, Template)
    - .planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-RESEARCH.md (Pattern 2 linhas 253-298; Pitfall 1 linhas 336-340)
  </read_first>
  <action>
    (1) Em `src/actions/lead-actions.ts`: estenda a variante de sucesso do `ActionState` para `{ success: true; lead: Lead }` (import de Lead já existe). Em `createLead`, troque `await db.insert(leads).values(parsed.data)` por `const [inserted] = await db.insert(leads).values(parsed.data).returning();` e `return { success: true, lead: inserted };`. Adicione `revalidatePath("/leads")` junto de `revalidatePath("/")` e `revalidatePath("/pipeline")` (Pitfall 2 — a lista agora vive em /leads). Mantenha o backstop de FK. IMPORTANTE: `updateLead` retorna `{ success: true }` sem `lead` — isso é compatível com a união (lead opcional na narrowing? não: torne `lead` opcional na variante OU deixe updateLead também retornar sem lead e ajuste os consumidores). Para minimizar risco, defina a variante como `{ success: true; lead?: Lead }` para que updateLead continue válido sem alteração. Rode grep de todos os call sites de `createLead(`/`"success" in state`/`state.lead` para confirmar que nenhum consumidor quebra (Pitfall 1); `tsc --noEmit` é a checklist final.
    (2) Crie `src/hooks/use-first-contact-trigger.ts` (`"use client"` — primeiro hook do projeto, criar diretório `src/hooks/`): `export function useFirstContactTrigger(defaultTemplate: Template | undefined)` com estado interno `useState<{ lead: Lead; subnichoNome: string } | null>(null)`, retornando `{ open: target !== null, template: defaultTemplate, lead: target?.lead, subnichoNome: target?.subnichoNome, trigger: (lead, subnichoNome) => setTarget({ lead, subnichoNome }), close: () => setTarget(null) }`. Sem acoplamento ao LeadFormDialog nem à importação — apenas estado reutilizável (D-18: Fase 2 chamará o mesmo trigger por linha importada).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/actions/lead-actions.ts','utf8'); if(!s.includes('.returning()'))throw new Error('createLead nao usa .returning()'); if(!s.includes('revalidatePath(\"/leads\")'))throw new Error('faltou revalidatePath(/leads)'); const h=require('fs').readFileSync('src/hooks/use-first-contact-trigger.ts','utf8'); ['useFirstContactTrigger','trigger','close'].forEach(t=>{if(!h.includes(t))throw new Error('hook faltou '+t)});"</automated>
  </verify>
  <acceptance_criteria>
    - `createLead` usa `.returning()` e retorna `{ success: true, lead }`; adiciona `revalidatePath("/leads")`
    - `ActionState.success` aceita `lead?` de forma que `updateLead` (sem lead) continua type-safe
    - `useFirstContactTrigger` exportado com `trigger`/`close`/`open`
    - `npx tsc --noEmit` limpo (nenhum call site quebrado — Pitfall 1)
  </acceptance_criteria>
  <done>Contrato de retorno do createLead e hook compartilhado prontos; tsc limpo.</done>
</task>

<task type="auto">
  <name>Task 2: Auto-abrir preview de 1º contato após criar lead (todas as superfícies)</name>
  <files>src/components/lead-form-dialog.tsx, src/app/leads/page.tsx, src/components/lead-table.tsx, src/components/followup-dashboard.tsx, src/components/pipeline-board.tsx</files>
  <read_first>
    - src/components/lead-form-dialog.tsx (efeito success linhas 107-116; isEditMode)
    - src/components/whatsapp-preview-dialog.tsx (04-03 — props e defaultTipo)
    - src/hooks/use-first-contact-trigger.ts (Task 1)
    - src/components/lead-table.tsx (renderiza LeadFormDialog; recebe subnichos)
    - src/app/leads/page.tsx (Server Component — fetch a estender)
    - src/components/pipeline-board.tsx e src/components/followup-dashboard.tsx (já recebem `templates` do 04-03)
  </read_first>
  <action>
    (1) Em `src/components/lead-form-dialog.tsx`: adicione prop opcional `firstContactTemplate?: Template` e `templates?: Template[]` (necessários para renderizar o preview). Instancie `const firstContact = useFirstContactTrigger(firstContactTemplate)`. No efeito de sucesso existente, APÓS `onOpenChange(false)`, se `!isEditMode` (criação) E `state.lead` existir, chame `firstContact.trigger(state.lead, subnichoNomeFor(state.lead.subnichoId))` — resolva o nome do sub-nicho a partir da prop `subnichos` já disponível (mapa id→nome). NÃO disparar em modo edição (D-19: só criação). Renderize `<WhatsAppPreviewDialog open={firstContact.open} onOpenChange={(o)=>{ if(!o) firstContact.close(); }} lead={firstContact.lead} subnichoNome={firstContact.subnichoNome} templates={templates ?? []} defaultTipo="primeiro_contato" />` (D-19: tipo pré-selecionado 1º contato). Fechar o preview NÃO desfaz a criação (D-20: o lead já foi persistido pelo createLead). Adapte o subtítulo do preview para o contexto de sugestão se o WhatsAppPreviewDialog suportar (senão, o subtítulo padrão "Mensagem para {nome}" é aceitável — a copy de sugestão do 04-UI-SPEC linha 152 é desejável mas não bloqueante).
    (2) Passe `templates` e `firstContactTemplate` (o template com `tipo === "primeiro_contato" && isDefault`) ao `<LeadFormDialog>` em cada superfície de criação: em `pipeline-board.tsx` e `followup-dashboard.tsx` derive-os da prop `templates` que elas já recebem (04-03); em `lead-table.tsx` adicione prop `templates: Template[]` e passe adiante — e em `src/app/leads/page.tsx` acrescente `db.select().from(templates)` ao fetch, passando ao `<LeadTable templates={...}>`. O `firstContactTemplate` = `templates.find(t => t.tipo === "primeiro_contato" && t.isDefault)` (pode ser undefined se o admin não marcou padrão; nesse caso o preview abre com o primeiro template de 1º contato ou vazio — comportamento já tratado no WhatsAppPreviewDialog do 04-03).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run build</automated>
    <automated>node -e "const d=require('fs').readFileSync('src/components/lead-form-dialog.tsx','utf8'); ['useFirstContactTrigger','WhatsAppPreviewDialog','isEditMode'].forEach(t=>{if(!d.includes(t))throw new Error('lead-form-dialog faltou '+t)}); if(!d.includes('primeiro_contato'))throw new Error('faltou defaultTipo primeiro_contato'); const p=require('fs').readFileSync('src/app/leads/page.tsx','utf8'); if(!p.includes('templates'))throw new Error('/leads/page nao busca templates');"</automated>
    <human-check>`npm run dev`: criar um lead novo (com template padrão de 1º contato cadastrado) por / , /leads e /pipeline — em cada caso o preview de 1º contato deve abrir automaticamente após salvar, com a mensagem preenchida; fechar sem enviar mantém o lead criado; editar um lead existente NÃO abre o preview.</human-check>
  </verify>
  <acceptance_criteria>
    - `lead-form-dialog.tsx` dispara `firstContact.trigger` só quando `!isEditMode` e `state.lead` existe, e renderiza `WhatsAppPreviewDialog` com `defaultTipo="primeiro_contato"`
    - As 3 superfícies de criação passam `templates` + `firstContactTemplate`; `/leads/page.tsx` busca templates
    - Fechar o preview não afeta a criação já persistida (D-20)
    - `npm run build` passa
  </acceptance_criteria>
  <done>WA-04 satisfeito: criação manual de lead sugere automaticamente o WhatsApp de 1º contato, com mecanismo reutilizável para a Fase 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client (form de lead) → Server Action createLead | Input do lead validado por leadSchema (inalterado) |
| createLead → cliente (retorno com lead) | Payload do lead recém-criado atravessa para o cliente abrir o preview |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering | createLead (novo retorno com lead) | mitigate | Validação por `leadSchema` (Zod) permanece antes do insert; `.returning()` devolve apenas a linha persistida, sem input não-validado |
| T-04-10 | Repudiation/Consistency | Preview auto-aberto após criação | accept | Fechar sem enviar não desfaz criação (D-20, comportamento intencional); sem mutação no caminho de envio |
| T-04-11 | Tampering | Reuso do preview (link wa.me) | mitigate | Herda mitigações do 04-03 (normalizePhone + rel=noopener + texto puro) — nenhum novo caminho de URL |
| T-04-SC | Tampering | Supply chain (npm) | accept | Zero instalações neste plano |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo; `npm run build` passa
- Grep confirma `.returning()`, `revalidatePath("/leads")`, `useFirstContactTrigger`, `defaultTipo="primeiro_contato"`
- Preview auto-abre só em criação (não em edição); fechar não desfaz criação
</verification>

<success_criteria>
- WA-04 satisfeito: criação manual sugere WhatsApp de 1º contato automaticamente (D-18/D-19/D-21)
- Mecanismo reutilizável (hook) pronto para a Fase 2 sem reescrita (D-18)
- D-20 respeitado: fechar o preview não desfaz o lead
</success_criteria>

<output>
Criar `.planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-04-SUMMARY.md` ao concluir.
</output>
