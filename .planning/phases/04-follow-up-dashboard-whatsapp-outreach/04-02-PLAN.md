---
phase: 04-follow-up-dashboard-whatsapp-outreach
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/db/schema.ts
  - src/types/index.ts
  - src/lib/validations.ts
  - src/actions/template-actions.ts
  - src/app/templates/page.tsx
  - src/components/template-list.tsx
  - src/components/template-form-dialog.tsx
  - src/components/delete-template-dialog.tsx
  - data/crm.db
autonomous: true
requirements: [WA-01]
must_haves:
  truths:
    - "Admin pode criar um template de WhatsApp com nome, tipo (1º contato/follow-up/prova de valor) e corpo com variáveis {nome}/{subnicho}/{origem} (D-09, D-10)"
    - "Templates são gerenciados em /templates, rota dedicada com item próprio na sidebar (D-11)"
    - "Admin pode editar e excluir (hard delete, com confirmação) um template em /templates (D-13)"
    - "Admin pode marcar exatamente um template como padrão por tipo (marcar outro desmarca o anterior) (D-12)"
    - "A tabela templates existe no banco após drizzle-kit push"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Tabela templates (tipo enum, nome, corpo, isDefault, timestamps)"
      contains: "sqliteTable(\n    \"templates\""
    - path: "src/actions/template-actions.ts"
      provides: "createTemplate/updateTemplate/deleteTemplate/setDefaultTemplate"
      exports: ["createTemplate", "updateTemplate", "deleteTemplate", "setDefaultTemplate"]
    - path: "src/lib/validations.ts"
      provides: "templateSchema (Zod, mensagens PT-BR)"
      contains: "templateSchema"
    - path: "src/app/templates/page.tsx"
      provides: "Rota /templates (Server Component, lista agrupada por tipo)"
      contains: "TemplateList"
    - path: "src/components/template-form-dialog.tsx"
      provides: "Modal criar/editar template (react-hook-form + Zod)"
      min_lines: 40
  key_links:
    - from: "src/actions/template-actions.ts"
      to: "src/db/schema.ts (templates)"
      via: "db.insert/update/delete + db.transaction para default-per-type"
      pattern: "db\\.transaction"
    - from: "src/actions/template-actions.ts"
      to: "revalidatePath"
      via: "revalida /templates, /, /pipeline (Pitfall 2)"
      pattern: "revalidatePath\\(\"/pipeline\"\\)"
    - from: "src/components/template-form-dialog.tsx"
      to: "src/lib/validations.ts (templateSchema)"
      via: "zodResolver(templateSchema)"
      pattern: "zodResolver"
---

<objective>
Construir o sistema de templates de WhatsApp end-to-end: nova tabela `templates`, schema de validação, Server Actions de CRUD (com "um padrão por tipo" via transação), e a tela `/templates` (lista agrupada por tipo + modal de criar/editar + confirmação de exclusão). Corte vertical completo UI→API→DB para WA-01 (D-09/D-10/D-11/D-12/D-13).

Purpose: Sem templates não há mensagem para pré-preencher no envio (04-03) nem no auto-gatilho (04-04). Este plano é a fundação de dados+UX do WhatsApp.
Output: `/templates` funcional com CRUD e marcação de padrão por tipo; tabela `templates` no banco.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-CONTEXT.md
@.planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-RESEARCH.md
@.planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-UI-SPEC.md
@.planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-PATTERNS.md

<interfaces>
<!-- Analogs existentes — replicar convenções exatas. -->

Schema builder (src/db/schema.ts) — convenções confirmadas:
  integer("id").primaryKey({ autoIncrement: true })
  text(col, { enum: [...] }).notNull()
  integer(col, { mode: "boolean" }).notNull().default(false)
  integer(col, { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
  index("nome_idx").on(table.col)   // no 3º argumento (array)

ActionState (padrão re-declarado por arquivo — subnicho-actions.ts / lead-actions.ts):
  { success: true } | { errors: Record<string,string[]|undefined> } | undefined
  Server Action de form: (_prevState: ActionState, formData: FormData): Promise<ActionState>

subnicho-actions.ts (analog CRUD): safeParse → errors; db.insert; revalidatePath("/subnichos"); revalidatePath("/")
updateLeadStage (analog SELECT-then-compare / mutação condicional): src/actions/lead-actions.ts linhas 129-163

Zod (src/lib/validations.ts) — mensagens PT-BR:
  z.string().trim().min(1, "Nome é obrigatório.")
  z.enum([...], { error: "Selecione ..." })

Componentes analog:
  src/components/subnicho-manager.tsx  → lista + estado vazio + toasts + linhas com ações ícone (Pencil/Trash2, h-9 w-9)
  src/components/lead-form-dialog.tsx  → useActionState + react-hook-form + zodResolver + Dialog + Field/Select/Textarea + efeito success/error
  src/components/discard-changes-dialog.tsx → confirm dialog 2 botões (analog para excluir)
  DialogState união discriminada: { mode: "closed" } | { mode: "create" } | { mode: "edit"; template: Template }

Schema ALVO da tabela templates (04-RESEARCH linhas 386-401):
  templates: id (pk autoinc), tipo enum ["primeiro_contato","follow_up","prova_valor"] notNull,
  nome text notNull, corpo text notNull, isDefault integer boolean notNull default false,
  createdAt/updatedAt timestamp default (unixepoch()), index("templates_tipo_idx").on(table.tipo)

Push command: `npx drizzle-kit push` (dialect sqlite, dbCredentials.url ./data/crm.db) — CREATE TABLE aditivo, não-interativo.

Copy PT-BR: 04-UI-SPEC linhas 122-144 (títulos, labels, erros, toasts, confirmação de exclusão).
</interfaces>

Nota de dependência: `depends_on: ["04-01"]` — não há sobreposição de arquivos com o 04-01, mas a execução é serializada de propósito. O host tem 4GB de RAM e já travou por processos paralelos (feedback do usuário); manter waves sequenciais evita spawnar executores em paralelo. O item de nav "Templates" já foi adicionado no 04-01, apontando para a rota que este plano cria.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contratos — tabela templates + tipo + templateSchema</name>
  <files>src/db/schema.ts, src/types/index.ts, src/lib/validations.ts</files>
  <read_first>
    - src/db/schema.ts (convenções de coluna; tabela subnichos e leads como referência)
    - src/types/index.ts (padrão InferSelectModel/InferInsertModel)
    - src/lib/validations.ts (subnichoSchema/leadSchema, mensagens PT-BR)
    - .planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-RESEARCH.md (Code Examples: templates schema, linhas 386-401)
  </read_first>
  <action>
    (1) Em `src/db/schema.ts` adicione `export const templates = sqliteTable("templates", { id: integer("id").primaryKey({ autoIncrement: true }), tipo: text("tipo", { enum: ["primeiro_contato","follow_up","prova_valor"] }).notNull(), nome: text("nome").notNull(), corpo: text("corpo").notNull(), isDefault: integer("is_default", { mode: "boolean" }).notNull().default(false), createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`), updatedAt: integer("updated_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`) }, (table) => [index("templates_tipo_idx").on(table.tipo)])`. NÃO use `uniqueIndex().where(...)` parcial para o invariante "um padrão por tipo" (Pitfall 3 / drizzle-kit sqlite gera WHERE incorreto) — o invariante é aplicado em Server Action via transação (Task 3).
    (2) Em `src/types/index.ts` adicione `export type Template = InferSelectModel<typeof templates>;`, `export type NewTemplate = InferInsertModel<typeof templates>;` e importe `templates` de `@/db/schema`.
    (3) Em `src/lib/validations.ts` adicione `export const templateSchema = z.object({ tipo: z.enum(["primeiro_contato","follow_up","prova_valor"], { error: "Selecione um tipo." }), nome: z.string().trim().min(1, "Nome é obrigatório."), corpo: z.string().trim().min(1, "Mensagem é obrigatória."), isDefault: z.coerce.boolean().default(false) })` e `export type TemplateFormValues = z.input<typeof templateSchema>;`. Mensagens PT-BR exatas conforme 04-UI-SPEC linha 139.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -c "templates_tipo_idx" src/db/schema.ts</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/lib/validations.ts','utf8'); ['primeiro_contato','follow_up','prova_valor','Mensagem é obrigatória.','Selecione um tipo.'].forEach(t=>{if(!s.includes(t))throw new Error('faltou '+t)});"</automated>
  </verify>
  <acceptance_criteria>
    - `templates` definido em schema.ts com enum tipo, isDefault boolean default false, e `index("templates_tipo_idx")`; SEM `uniqueIndex().where(...)`
    - `Template`/`NewTemplate` exportados de src/types/index.ts
    - `templateSchema` exportado com as 3 mensagens PT-BR e `isDefault` coerced boolean
    - `npx tsc --noEmit` limpo
  </acceptance_criteria>
  <done>Contratos de dados/validação prontos; tsc limpo. Ainda sem tabela no banco (Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Aplicar schema no banco (drizzle-kit push)</name>
  <files>data/crm.db</files>
  <read_first>
    - drizzle.config.ts (dialect sqlite, dbCredentials.url ./data/crm.db, schema ./src/db/schema.ts)
    - src/db/schema.ts (deve já conter `templates` do Task 1)
  </read_first>
  <action>
    Rode `npx drizzle-kit push` para aplicar a nova tabela `templates` ao banco `./data/crm.db`. É uma mudança ADITIVA (CREATE TABLE), não-interativa — nenhum dado existente é alterado, nenhum prompt de perda de dados deve aparecer. Se o comando pedir confirmação interativa inesperada (não deveria, para CREATE TABLE), pare e sinalize — não force com flags destrutivas. Esta task é OBRIGATÓRIA: `tsc`/`build` passam mesmo sem push porque os tipos vêm do schema TS, não do banco vivo — sem o push a tabela não existe e o CRUD falha em runtime (falso-positivo de verificação).
  </action>
  <verify>
    <automated>npx drizzle-kit push</automated>
    <automated>node -e "const Database=require('better-sqlite3'); const db=new Database('./data/crm.db'); const row=db.prepare(\"SELECT name FROM sqlite_master WHERE type='table' AND name='templates'\").get(); if(!row) throw new Error('tabela templates NAO existe apos push'); const cols=db.prepare('PRAGMA table_info(templates)').all().map(c=>c.name); ['id','tipo','nome','corpo','is_default','created_at','updated_at'].forEach(c=>{if(!cols.includes(c))throw new Error('faltou coluna '+c)}); console.log('OK templates:',cols.join(','));"</automated>
  </verify>
  <acceptance_criteria>
    - `npx drizzle-kit push` conclui sem erro
    - `sqlite_master` contém a tabela `templates` com colunas id, tipo, nome, corpo, is_default, created_at, updated_at
  </acceptance_criteria>
  <done>Tabela templates existe no banco vivo; CRUD do Task 3 pode operar em runtime.</done>
</task>

<task type="auto">
  <name>Task 3: Server Actions CRUD + tela /templates (lista, form modal, exclusão)</name>
  <files>src/actions/template-actions.ts, src/app/templates/page.tsx, src/components/template-list.tsx, src/components/template-form-dialog.tsx, src/components/delete-template-dialog.tsx</files>
  <read_first>
    - src/actions/subnicho-actions.ts (analog CRUD + ActionState + revalidatePath)
    - src/actions/lead-actions.ts (updateLeadStage: mutação condicional/SELECT; padrão de revalidatePath múltiplo)
    - src/components/subnicho-manager.tsx (lista + estado vazio + linhas com ícones Pencil/Trash2 h-9 w-9)
    - src/components/lead-form-dialog.tsx (useActionState + react-hook-form + Field/Select/Textarea + efeito success/error)
    - src/components/discard-changes-dialog.tsx (confirm dialog 2 botões)
    - src/app/subnichos/page.tsx (analog Server Component da rota)
    - .planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-RESEARCH.md (Pattern 3: setDefaultTemplate via transaction, linhas 300-316)
    - .planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-UI-SPEC.md (Copywriting linhas 128-144)
  </read_first>
  <action>
    (1) `src/actions/template-actions.ts` (`"use server"`): `createTemplate(_prev, formData)` e `updateTemplate(_prev, formData)` seguindo o padrão de subnicho-actions (safeParse com `templateSchema.safeParse(Object.fromEntries(formData))`; em erro retorna `{ errors }`; insert/update; no update ler `id = Number(formData.get("id"))` e validar inteiro positivo). Se `isDefault` for true no create/update, chame a mesma lógica de transação de `setDefaultTemplate` para garantir unicidade. `deleteTemplate(id: number)` → `db.delete(templates).where(eq(templates.id, id))` (hard delete, D-13). `setDefaultTemplate(id: number, tipo: Template["tipo"])` → `db.transaction((tx) => { tx.update(templates).set({ isDefault: false }).where(and(eq(templates.tipo, tipo), eq(templates.isDefault, true))).run(); tx.update(templates).set({ isDefault: true }).where(eq(templates.id, id)).run(); })` (Pattern 3 — desmarca-então-marca atômico; better-sqlite3 transação síncrona). TODA action que muta templates deve chamar `revalidatePath("/templates")`, `revalidatePath("/")` e `revalidatePath("/pipeline")` (Pitfall 2 — botões de envio no dashboard e pipeline dependem do template default). NÃO revalidar "/leads" (sem botão de envio lá, Open Question 1).
    (2) `src/app/templates/page.tsx` (Server Component, analog subnichos/page.tsx): `const items = await db.select().from(templates).orderBy(asc(templates.tipo), asc(templates.nome))`; título `<h1 className="text-[28px] font-semibold leading-tight">Templates</h1>`; renderize `<TemplateList templates={items} />`.
    (3) `src/components/template-list.tsx` (`"use client"`, analog subnicho-manager para container/estado vazio/toasts, mas edição em MODAL como lead-table via DialogState): agrupe as linhas por `tipo` em 3 seções ("1º contato"/"Follow-up"/"Prova de valor", 04-UI-SPEC linha 129). Cada linha: nome (Body) + badge "Padrão" (fundo #0D9488/10%, texto #0D9488, ícone `Check`) quando isDefault; botão ghost "Tornar padrão" (chama `setDefaultTemplate`) nas linhas não-default; ações ícone Pencil (abre form modal em edit) e Trash2 (abre delete dialog), ambos h-9 w-9. Botão accent "Novo template" no topo abre form em create. Estado vazio "Nenhum template cadastrado ainda." + botão "Novo template". Toasts: "Template salvo com sucesso." / "Não foi possível salvar o template. Tente novamente." / "Template excluído." / "Não foi possível excluir o template. Tente novamente."
    (4) `src/components/template-form-dialog.tsx` (`"use client"`, analog lead-form-dialog): react-hook-form + `zodResolver(templateSchema)` + `useActionState(isEditMode ? updateTemplate : createTemplate, undefined)`. Campos: Nome (Input, placeholder "Ex: Primeiro contato — nutricionista"), Tipo (Select via Controller com opções 1º contato/Follow-up/Prova de valor), Corpo (Textarea, FieldDescription "Use {nome}, {subnicho} ou {origem} para personalizar automaticamente."), e um `<input type="checkbox">` nativo registrado via `form.register("isDefault")` com label "Marcar como padrão para este tipo" (NÃO adicionar primitivo shadcn novo — 04-RESEARCH/UI-SPEC: zero novos itens de registry). Título "Novo template"/"Editar template". Botões "Cancelar" (outline) / "Salvar template" (accent). Efeito success fecha o modal + toast; efeito errors → toast de erro. Em edit inclua `<input type="hidden" name="id">`.
    (5) `src/components/delete-template-dialog.tsx` (analog discard-changes-dialog): Título "Excluir template", corpo `Tem certeza que deseja excluir o template "{nome}"? Essa ação não pode ser desfeita.`, botões "Cancelar" (outline) / "Excluir" (`variant="destructive"`, chama `deleteTemplate`).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run build</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/actions/template-actions.ts','utf8'); ['createTemplate','updateTemplate','deleteTemplate','setDefaultTemplate','db.transaction'].forEach(t=>{if(!s.includes(t))throw new Error('faltou '+t)}); const rp=(s.match(/revalidatePath\(\"\/pipeline\"\)/g)||[]).length; if(rp<2)throw new Error('revalidatePath(/pipeline) esperado em multiplas actions, achou '+rp);"</automated>
    <automated>node -e "const D=require('better-sqlite3'); const db=new D('./data/crm.db'); const now=Math.floor(Date.now()/1000); const ins=db.prepare(\"INSERT INTO templates (tipo,nome,corpo,is_default,created_at,updated_at) VALUES (?,?,?,?,?,?)\"); const a=ins.run('follow_up','A','oi {nome}',1,now,now).lastInsertRowid; const b=ins.run('follow_up','B','oi de novo',0,now,now).lastInsertRowid; db.transaction(()=>{db.prepare('UPDATE templates SET is_default=0 WHERE tipo=? AND is_default=1').run('follow_up'); db.prepare('UPDATE templates SET is_default=1 WHERE id=?').run(b);})(); const defs=db.prepare('SELECT COUNT(*) c FROM templates WHERE tipo=? AND is_default=1').get('follow_up').c; db.prepare('DELETE FROM templates WHERE id IN (?,?)').run(a,b); if(defs!==1)throw new Error('esperava 1 default por tipo, achou '+defs); console.log('OK default-per-type');"</automated>
    <human-check>Rodar `npm run dev`, abrir `/templates`: criar um template de cada tipo com {nome}/{subnicho}/{origem} no corpo, marcar um como padrão, criar um segundo do mesmo tipo e marcá-lo padrão (o primeiro deve perder o badge), editar, e excluir com confirmação.</human-check>
  </verify>
  <acceptance_criteria>
    - `template-actions.ts` exporta createTemplate/updateTemplate/deleteTemplate/setDefaultTemplate; setDefaultTemplate usa `db.transaction`; deleteTemplate faz hard delete
    - Cada action mutante revalida /templates, / e /pipeline
    - `/templates` lista templates agrupados por tipo com badge "Padrão" e ações editar/excluir/tornar padrão
    - Teste automatizado de "um default por tipo" resulta em contagem 1
    - `npm run build` passa
  </acceptance_criteria>
  <done>CRUD de templates completo e funcional em /templates com invariante de padrão por tipo garantido no servidor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client (form /templates) → Server Action | Input não-confiável do formulário de template cruza aqui |
| Server Action → SQLite | Mutações Drizzle parametrizadas + transação |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | template-actions (create/update) | mitigate | Validar todo input com `templateSchema` (Zod, ASVS V5) antes de qualquer insert/update; tipo restrito a enum |
| T-04-04 | Tampering | setDefaultTemplate (race "dois padrões") | mitigate | `db.transaction()` desmarca-então-marca atômico; não confiar em UI para o invariante (mesmo com 2 abas/import futuro) |
| T-04-05 | Information Disclosure | Corpo do template renderizado na lista | mitigate | Corpo tratado como texto puro (React escapa por padrão); sem `dangerouslySetInnerHTML`, sem engine de template com eval |
| T-04-SC | Tampering | Supply chain (npm) | accept | Zero instalações neste plano (04-RESEARCH: zero novas deps; nenhum item de registry shadcn novo) |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo; `npm run build` passa
- Tabela templates existe no banco (Task 2)
- Teste automatizado confirma exatamente 1 default por tipo após troca
- CRUD completo verificável em /templates
</verification>

<success_criteria>
- WA-01 satisfeito: criar/editar templates com variáveis {nome}/{subnicho}/{origem}
- D-12 satisfeito: exatamente um padrão por tipo, aplicado atomicamente no servidor
- D-13 satisfeito: exclusão direta (hard delete) com confirmação leve
- Zero cor/dependência/registry novo
</success_criteria>

<output>
Criar `.planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-02-SUMMARY.md` ao concluir.
</output>
