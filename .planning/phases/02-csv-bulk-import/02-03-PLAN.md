---
phase: 02-csv-bulk-import
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/app/importar/[batchId]/page.tsx
  - src/components/post-import-lead-list.tsx
  - src/components/csv-import-wizard.tsx
autonomous: false
requirements: [LEAD-05]
user_setup: []

must_haves:
  truths:
    - "Após confirmar uma importação, o admin é levado automaticamente a uma tela dedicada listando só os leads daquele lote"
    - "Cada lead na tela pós-importação tem um botão 'Enviar WhatsApp' que abre o preview com o template de 1º contato já selecionado"
    - "A tela pós-importação nunca abre modais de WhatsApp automaticamente em sequência — cada envio é um clique manual do admin (D-13)"
    - "Os leads importados aparecem normalmente em /leads e no pipeline, como qualquer outro lead (nenhum tratamento especial)"
  artifacts:
    - path: "src/app/importar/[batchId]/page.tsx"
      provides: "Rota dinâmica por lote (Server Component), query por importBatchId"
      contains: "importBatchId"
    - path: "src/components/post-import-lead-list.tsx"
      provides: "Lista pós-importação reaproveitando WhatsAppSendButton + WhatsAppPreviewDialog (defaultTipo=primeiro_contato)"
      contains: "primeiro_contato"
  key_links:
    - from: "src/app/importar/[batchId]/page.tsx"
      to: "src/db/schema.ts (leads.importBatchId)"
      via: "where(eq(leads.importBatchId, batchId) AND isNull(deletedAt))"
      pattern: "importBatchId"
    - from: "src/components/post-import-lead-list.tsx"
      to: "src/components/whatsapp-preview-dialog.tsx"
      via: "defaultTipo=\"primeiro_contato\", sem alteração no componente reaproveitado"
      pattern: "primeiro_contato"
    - from: "src/components/csv-import-wizard.tsx"
      to: "src/app/importar/[batchId]/page.tsx"
      via: "router.push(`/importar/${batchId}`) apos bulkImportLeads bem-sucedido"
      pattern: "router.push"
---

<objective>
Fecha o loop D-13/D-14 da Fase 2: após confirmar um lote de importação, o admin é redirecionado para uma tela dedicada e persistente (`/importar/[batchId]`) que lista só os leads daquele lote, cada um com o botão "Enviar WhatsApp" já reaproveitado da Fase 4 (mesmo `WhatsAppSendButton`/`WhatsAppPreviewDialog`, `defaultTipo="primeiro_contato"`) — nunca um auto-disparo em sequência, sempre clique-por-clique do admin.

Purpose: Sem esta tela, o admin confirmaria um lote de leads e teria que procurá-los manualmente em `/leads` para mandar a primeira mensagem — perdendo exatamente a conveniência que D-14 pede. Fecha também a "revisão em nível de lote" citada no critério de sucesso 4 do ROADMAP (LEAD-05).
Output: Rota `/importar/[batchId]` funcional, lista pós-importação com envio de WhatsApp por linha, wizard redirecionando para lá após confirmar.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-csv-bulk-import/02-CONTEXT.md
@.planning/phases/02-csv-bulk-import/02-RESEARCH.md
@.planning/phases/02-csv-bulk-import/02-PATTERNS.md
@.planning/phases/02-csv-bulk-import/02-UI-SPEC.md

<interfaces>
<!-- Padrão exato a copiar quase verbatim (Fase 4) -->

De src/components/followup-dashboard.tsx (o padrão EXATO a reaproveitar, trocando só defaultTipo):
```typescript
type PreviewState = { open: false } | { open: true; lead: Lead; subnichoNome: string };
const [previewState, setPreviewState] = useState<PreviewState>({ open: false });

<WhatsAppSendButton
  nome={lead.nome}
  disabled={normalizePhone(lead.telefone) === null}
  onClick={() => setPreviewState({ open: true, lead, subnichoNome: subnichoNameById.get(lead.subnichoId) ?? "—" })}
/>

<WhatsAppPreviewDialog
  open={previewState.open}
  onOpenChange={(open) => { if (!open) setPreviewState({ open: false }); }}
  lead={previewState.open ? previewState.lead : undefined}
  subnichoNome={previewState.open ? previewState.subnichoNome : ""}
  templates={templates}
  defaultTipo="follow_up" // <- única mudança: "primeiro_contato" nesta fase
/>
```

De src/components/whatsapp-preview-dialog.tsx / whatsapp-send-button.tsx: props já aceitam exatamente isso, NENHUMA alteração necessária nesses dois arquivos (confirmado por leitura direta em 02-RESEARCH.md/02-PATTERNS.md).

De src/hooks/use-first-contact-trigger.ts: NÃO usar aqui — construído para 1 alvo por vez (auto-gatilho de criação manual única, Fase 4 D-18/D-19). D-13 desta fase proíbe abrir modais em sequência para N leads; o padrão `useState<PreviewState>` acima já é 1-dialog-compartilhado-por-clique-manual, que é exatamente o que D-13/D-14 pedem.

Estado atual — src/app/leads/page.tsx (analog de Server Component, para adaptar à rota dinâmica):
```typescript
export default async function LeadsPage() {
  const [activeLeads, allSubnichos, allTemplates] = await Promise.all([
    db.select().from(leads).where(isNull(leads.deletedAt)).orderBy(asc(leads.followUpDate)),
    db.select().from(subnichos),
    db.select().from(templates),
  ]);
  // ...
}
```

Contrato de retorno de src/actions/import-actions.ts (02-01), já usado pelo wizard (02-02):
```typescript
export type BulkImportResult = { success: true; batchId: string; count: number } | { success: false; message: string };
```

Estado atual — csv-import-wizard.tsx, branch de sucesso do confirmar (produzido por 02-02, Task 3 — ESTENDER, não recriar):
```typescript
if (result.success) {
  toast.success(`${result.count} leads importados com sucesso.`);
  // 02-02: reset do wizard para { step: "upload" } — ESTA fase substitui isso por um redirect
} else {
  toast.error(result.message);
}
```

02-UI-SPEC.md Copywriting Contract (copy literal): título "Importação concluída"; subtítulo "{N} leads importados. Envie a primeira mensagem por WhatsApp quando quiser."; CTA de saída "Ver todos os leads" (outline, -> `/leads`); estado vazio defensivo "Nenhum lead foi importado neste lote."; botão inline reaproveitado `aria-label="Enviar WhatsApp para {nome}"`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rota /importar/[batchId] + lista pós-importação com envio de WhatsApp</name>
  <files>src/app/importar/[batchId]/page.tsx, src/components/post-import-lead-list.tsx</files>
  <read_first>
    - src/app/leads/page.tsx (Server Component analog)
    - src/components/followup-dashboard.tsx (padrão PreviewState/WhatsAppSendButton/WhatsAppPreviewDialog — ver bloco `<interfaces>` acima)
    - src/components/whatsapp-preview-dialog.tsx, src/components/whatsapp-send-button.tsx (props já compatíveis, NÃO modificar)
    - src/lib/phone.ts (normalizePhone, para `disabled` do botão de envio)
    - src/db/schema.ts (leads.importBatchId, de 02-01)
    - .planning/phases/02-csv-bulk-import/02-CONTEXT.md (D-13, D-14)
    - .planning/phases/02-csv-bulk-import/02-PATTERNS.md (seção 9)
    - .planning/phases/02-csv-bulk-import/02-UI-SPEC.md (Copywriting — título/subtítulo/CTA/estado vazio da tela pós-importação)
  </read_first>
  <action>
    Criar `src/app/importar/[batchId]/page.tsx` (Server Component, `params: Promise<{ batchId: string }>` — padrão de rota dinâmica assíncrona do Next.js App Router): `const importedLeads = await db.select().from(leads).where(and(eq(leads.importBatchId, batchId), isNull(leads.deletedAt))).orderBy(asc(leads.nome));` + `subnichos`/`templates` via `Promise.all`. Se `importedLeads.length === 0`, renderizar só a copy defensiva "Nenhum lead foi importado neste lote." (não deveria ocorrer após uma confirmação bem-sucedida, mas cobre acesso direto a um `batchId` inválido/antigo). Caso contrário: heading "Importação concluída" (20px/600) + subtítulo `${importedLeads.length} leads importados. Envie a primeira mensagem por WhatsApp quando quiser.` + `<PostImportLeadList leads={importedLeads} subnichos={allSubnichos} templates={allTemplates} />` + link outline "Ver todos os leads" para `/leads`.

    Criar `src/components/post-import-lead-list.tsx` (`"use client"`) — cópia quase verbatim do padrão `PreviewState` de `followup-dashboard.tsx` (ver bloco `<interfaces>` acima): `subnichoNameById` via `useMemo(() => new Map(subnichos.map(s => [s.id, s.nome])), [subnichos])`; uma linha por lead (nome em Body 16px/400) com `<WhatsAppSendButton nome={lead.nome} disabled={normalizePhone(lead.telefone) === null} onClick={() => setPreviewState({ open: true, lead, subnichoNome: subnichoNameById.get(lead.subnichoId) ?? "—" })} />`; um único `<WhatsAppPreviewDialog ... defaultTipo="primeiro_contato" />` ao final (a ÚNICA diferença semântica em relação ao dashboard). NÃO importar `useFirstContactTrigger` (D-13 — proíbe o auto-disparo em sequência que esse hook foi desenhado para o caso de 1-lead-manual, não N-leads-em-lote).
  </action>
  <verify>
    <automated>cd "C:/Users/Vencedor/Desktop/crm-leads" && npx tsc --noEmit</automated>
    <automated>npm run build</automated>
    <automated>node -e "const fs=require('fs'); const p=fs.readFileSync('src/app/importar/[batchId]/page.tsx','utf8'); ['importBatchId','isNull','Nenhum lead foi importado neste lote.'].forEach(t=>{if(!p.includes(t))throw new Error('faltou '+t)}); const l=fs.readFileSync('src/components/post-import-lead-list.tsx','utf8'); ['WhatsAppSendButton','WhatsAppPreviewDialog','primeiro_contato'].forEach(t=>{if(!l.includes(t))throw new Error('faltou '+t)}); if(l.includes('useFirstContactTrigger')) throw new Error('D-13: nao deve usar useFirstContactTrigger no fluxo de lote'); console.log('OK: Task 1');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/importar/[batchId]/page.tsx` filtra `eq(leads.importBatchId, batchId)` E `isNull(leads.deletedAt)`
    - Estado de lote vazio renderiza a copy literal "Nenhum lead foi importado neste lote."
    - Subtítulo renderiza o template literal com contagem + "Envie a primeira mensagem por WhatsApp quando quiser."
    - `post-import-lead-list.tsx` importa `WhatsAppSendButton` e `WhatsAppPreviewDialog`, usa `defaultTipo="primeiro_contato"`, e NÃO importa `useFirstContactTrigger` (D-13)
    - `npx tsc --noEmit` e `npm run build` passam
  </acceptance_criteria>
  <done>Tela pós-importação dedicada, persistente por lote, reaproveitando 100% do mecanismo de envio de WhatsApp da Fase 4 sem alterar nenhum dos dois componentes reaproveitados.</done>
</task>

<task type="auto">
  <name>Task 2: Wizard redireciona para a tela pós-importação após confirmar</name>
  <files>src/components/csv-import-wizard.tsx</files>
  <read_first>
    - src/components/csv-import-wizard.tsx (handler de confirmação produzido por 02-02, Task 3 — ver bloco `<interfaces>` acima, ESTENDER não recriar)
    - src/app/importar/[batchId]/page.tsx (rota criada na Task 1 deste plano)
    - .planning/phases/02-csv-bulk-import/02-UI-SPEC.md (toast de sucesso "{N} leads importados com sucesso.")
  </read_first>
  <action>
    Em `csv-import-wizard.tsx`, importar `useRouter` de `next/navigation` (`const router = useRouter();` — primeiro uso de navegação client-side no projeto; API nativa do Next.js, não é uma dependência nova). No branch `{ success: true, batchId, count }` do handler de confirmação (de 02-02): manter `toast.success(\`${count} leads importados com sucesso.\`)`; SUBSTITUIR o reset provisório para `{ step: "upload" }` (comportamento interino de 02-02) por `router.push(\`/importar/${batchId}\`)`. O branch `{ success: false, message }` continua inalterado (`toast.error(message)`, permanece no passo de prévia para o admin corrigir e tentar de novo).
  </action>
  <verify>
    <automated>cd "C:/Users/Vencedor/Desktop/crm-leads" && npx tsc --noEmit</automated>
    <automated>npm run build</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/components/csv-import-wizard.tsx','utf8'); if(!s.includes('useRouter')) throw new Error('faltou useRouter'); if(!/router\.push\(\`\/importar\/\$\{/.test(s)) throw new Error('faltou router.push(/importar/${batchId})'); console.log('OK: Task 2');"</automated>
    <human-check>Rodar `npm run dev` (host 4GB — se travar, confiar nos automated acima). Fluxo fim-a-fim: subir um CSV de teste em `/importar`, mapear, revisar a prévia, confirmar — o navegador deve ir automaticamente para `/importar/{batchId}` mostrando só os leads daquele lote, cada um com "Enviar WhatsApp" funcional (abre o preview com template de 1º contato, wa.me abre com telefone/mensagem corretos); clicar "Ver todos os leads" leva a `/leads` e os mesmos leads aparecem lá normalmente.</human-check>
  </verify>
  <acceptance_criteria>
    - `csv-import-wizard.tsx` importa `useRouter` de `next/navigation` e chama `router.push(\`/importar/${batchId}\`)` dentro do branch de sucesso de `bulkImportLeads`
    - O comportamento interino de 02-02 (reset para `{ step: "upload" }` no sucesso) não existe mais nesse branch
    - `npx tsc --noEmit` e `npm run build` passam
  </acceptance_criteria>
  <done>Fluxo IMPORT-01..03 + LEAD-05 + D-13/D-14 completo de ponta a ponta: confirmar uma importação leva direto à lista de envio de WhatsApp do lote recém-criado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parâmetro de rota dinâmica `batchId` (URL) -> query de leads | `batchId` vem da URL, não confiável, mas usado só como filtro de leitura (nenhuma mutação nesta tela) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Information Disclosure | `batchId` adivinhado/alterado manualmente pelo admin na URL | accept | Ferramenta solo sem autenticação (mesma aceitação já registrada em fases anteriores); um `batchId` inexistente ou de outro lote só mostra "Nenhum lead foi importado neste lote." ou os leads daquele lote — nenhum dado além do que já é visível em `/leads` |
| T-02-09 | Tampering | Superfície de mutação desta tela | accept | Nenhuma mutação nova é introduzida — a tela é somente leitura + navegação para o `WhatsAppPreviewDialog` já auditado na Fase 4, reaproveitado sem alteração |
| T-02-SC | Tampering | Dependências npm | accept | Nenhuma instalação neste plano (`useRouter` é API nativa do Next.js) |
</threat_model>

<verification>
- `npx tsc --noEmit` e `npm run build` passam após cada task
- Fluxo fim-a-fim confirmado via human-check (Task 2): confirmar importação -> redirect automático -> envio de WhatsApp por lead -> leads visíveis em /leads
</verification>

<success_criteria>
- LEAD-05 (revisão em nível de lote) satisfeito: rota dedicada por `importBatchId`
- D-13 satisfeito: nenhum auto-disparo de modal em sequência para o lote
- D-14 satisfeito: cada lead importado tem seu próprio botão de envio de WhatsApp, reaproveitando o mecanismo exato da Fase 4
</success_criteria>

<output>
Create `.planning/phases/02-csv-bulk-import/02-03-SUMMARY.md` when done
</output>
