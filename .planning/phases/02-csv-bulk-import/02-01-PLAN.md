---
phase: 02-csv-bulk-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - data/crm.db
  - src/lib/csv-encoding.ts
  - src/lib/csv-import.ts
  - src/lib/validations.ts
  - src/actions/import-actions.ts
autonomous: true
requirements: [LEAD-05, IMPORT-02, IMPORT-03]
user_setup: []

must_haves:
  truths:
    - "A coluna import_batch_id existe no banco real (./data/crm.db) e é nullable — leads criados manualmente continuam funcionando sem ela"
    - "Uma linha de CSV cujo telefone (normalizado) já pertence a um lead ativo existente é reportada como duplicata pela camada de dados, antes de qualquer escrita"
    - "Um nome de sub-nicho do CSV que não existe ainda é detectado como 'novo' na prévia e criado automaticamente na confirmação, sem criar duas vezes o mesmo nome dentro do mesmo lote"
    - "Uma linha de CSV com sub-nicho em branco é rejeitada pela validação server-side — nunca vira um lead com subnichoId nulo/forjado"
    - "Confirmar um lote grava todos os leads confirmados com o mesmo import_batch_id, dentro de uma única transação atômica (tudo ou nada)"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Coluna leads.importBatchId (nullable) + índice leads_import_batch_id_idx"
      contains: "importBatchId"
    - path: "src/lib/csv-encoding.ts"
      provides: "decodeCsvFile(file): sniff de BOM/UTF-8/windows-1252 (IMPORT-03)"
      exports: ["decodeCsvFile"]
    - path: "src/lib/csv-import.ts"
      provides: "Tipos de linha mapeada, defaults de coluna opcional, dedup dentro do lote (IMPORT-02 metade 1)"
      exports: ["mapCsvRows", "detectWithinBatchDuplicatePhones", "CSV_DEFAULTS"]
    - path: "src/lib/validations.ts"
      provides: "csvRowSchema (deriva de leadSchema, sem subnichoId/followUpDate, com subnichoNome obrigatório)"
      contains: "csvRowSchema"
    - path: "src/actions/import-actions.ts"
      provides: "fetchPreviewSupportData (dedup contra DB + sub-nichos desconhecidos) e bulkImportLeads (transação, LEAD-05)"
      exports: ["fetchPreviewSupportData", "bulkImportLeads"]
  key_links:
    - from: "src/lib/csv-import.ts"
      to: "src/lib/phone.ts"
      via: "normalizePhone() reaproveitado, nunca reimplementado (D-06)"
      pattern: "normalizePhone"
    - from: "src/actions/import-actions.ts"
      to: "src/db/schema.ts (leads.importBatchId)"
      via: "insert com importBatchId=batchId dentro de db.transaction"
      pattern: "importBatchId"
    - from: "src/actions/import-actions.ts"
      to: "src/db/schema.ts (subnichos)"
      via: "mesma query lower(trim(nome)) reaproveitada entre prévia e confirmação (Pitfall 6)"
      pattern: "lower\\(trim\\("
---

<objective>
Motor de importação de CSV: coluna aditiva `importBatchId` (LEAD-05) aplicada ao banco real, módulos puros de encoding/mapeamento/dedup client-side (IMPORT-03, metade de IMPORT-02), e as duas Server Actions que sustentam todo o wizard das Fases 02-02/02-03 — `fetchPreviewSupportData` (dedup contra o banco + detecção de sub-nicho desconhecido) e `bulkImportLeads` (transação atômica de inserção em lote com auto-criação de sub-nicho e tag de lote). Este plano não tem UI própria, mas entrega comportamento real e verificável fim-a-fim via script direto contra `./data/crm.db` (mesmo padrão já usado em 01-02/01-03 neste host sem navegador) — não é "fundação" vazia, é o motor de importação funcionando de ponta a ponta.

Purpose: Toda decisão de dados da Fase 2 (D-02 a D-12) vive aqui, num único lugar reaproveitado por prévia E confirmação, para que as duas nunca divirjam (Pitfall 6 do RESEARCH.md). As fases seguintes (02-02 wizard, 02-03 tela pós-importação) só consomem estes contratos.
Output: `leads.importBatchId` vivo no banco; `csv-encoding.ts`, `csv-import.ts`, `csvRowSchema` prontos; `fetchPreviewSupportData`/`bulkImportLeads` funcionais e testados contra o banco real.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-csv-bulk-import/02-CONTEXT.md
@.planning/phases/02-csv-bulk-import/02-RESEARCH.md
@.planning/phases/02-csv-bulk-import/02-PATTERNS.md

<interfaces>
<!-- Estado atual dos arquivos que este plano modifica/consome. Use estes contratos diretamente, não precisa explorar o resto do código. -->

Estado atual — src/db/schema.ts (tabela leads, campos relevantes):
```typescript
motivoPerda: text("motivo_perda"),
stageChangedAt: integer("stage_changed_at", { mode: "timestamp" }),
deletedAt: integer("deleted_at", { mode: "timestamp" }), // nullable = ativo (LEAD-04)
// ...
(table) => [
  index("leads_deleted_at_idx").on(table.deletedAt),
  index("leads_follow_up_date_idx").on(table.followUpDate),
  index("leads_stage_idx").on(table.stage),
  index("leads_subnicho_id_idx").on(table.subnichoId),
]
```

Estado atual — src/lib/validations.ts (leadSchema completo, para derivar csvRowSchema via `.omit()`/`.extend()` — NUNCA reimplementar campo a campo):
```typescript
export const leadSchema = z.object({
  nome: z.string().trim().min(1, "Nome é obrigatório."),
  telefone: z.string().trim().min(1, "Telefone é obrigatório.").transform((v, ctx) => {
    const normalized = normalizePhone(v);
    if (!normalized) { ctx.addIssue({ code: "custom", message: "Telefone inválido. Use DDD + número (ex.: (11) 91234-5678)." }); return z.NEVER; }
    return normalized;
  }),
  canal: z.enum(["instagram", "whatsapp"], { error: "Selecione um canal de contato." }),
  origem: z.string().trim().min(1, "Origem é obrigatória."),
  valorEstimado: z.preprocess((v) => parseBRLToCents(String(v ?? "")), z.number({ error: "Valor estimado é obrigatório." }).int().nonnegative()),
  notas: z.string().trim().min(1, "Notas são obrigatórias."),
  followUpDate: z.coerce.date(),
  subnichoId: z.coerce.number().int().positive("Selecione um sub-nicho."),
  stage: z.enum(["novo", "contatado", "negociacao", "fechado", "perdido"]).default("novo"),
  motivoPerda: z.string().trim().optional(),
});
```

Estado atual — src/lib/phone.ts (reaproveitar tal como é, D-06):
```typescript
export function normalizePhone(input: string): string | null { /* digits-only, DDI 55, 12 ou 13 dígitos */ }
```

Estado atual — src/actions/subnicho-actions.ts (query case-insensitive-trim a REAPROVEITAR, D-09/D-11):
```typescript
const existing = await db.select().from(subnichos)
  .where(sql`lower(trim(${subnichos.nome})) = lower(trim(${nome}))`);
```

Estado atual — src/actions/lead-actions.ts (convenção de revalidatePath após mutação em leads, reaproveitar o mesmo trio):
```typescript
revalidatePath("/");
revalidatePath("/leads");
revalidatePath("/pipeline");
```

Estado atual — src/db/client.ts (driver better-sqlite3 — `db.transaction((tx) => {...})` roda SÍNCRONO, sem `await` dentro do callback; usar `.all()`/`.run()` no lugar de `await` para forçar execução síncrona dentro da transação, exatamente como no exemplo de 02-RESEARCH.md "Code Examples"):
```typescript
export const db = drizzle({ client: sqlite, schema });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Coluna aditiva leads.importBatchId + push no banco real</name>
  <files>src/db/schema.ts, data/crm.db</files>
  <read_first>
    - src/db/schema.ts (tabela leads completa, linhas 30-57 — ver `motivoPerda`/`stageChangedAt` como precedente exato de coluna nullable aditiva)
    - .planning/phases/02-csv-bulk-import/02-RESEARCH.md (seção "Recommended Schema Shape (LEAD-05)")
    - .planning/phases/02-csv-bulk-import/02-PATTERNS.md (seção 2 — schema aditivo)
    - .planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-02-PLAN.md (Task 2 — precedente exato de `npx drizzle-kit push` para mudança aditiva, incluindo o script de verificação via better-sqlite3 direto no banco)
  </read_first>
  <action>
    Em `src/db/schema.ts`, adicionar à tabela `leads` (logo após `stageChangedAt`, antes de `deletedAt`) a coluna `importBatchId: text("import_batch_id"), // nullable = lead criado manualmente (LEAD-05)` — SEM `.notNull()`, SEM `.default(...)`, mesmo formato nullable-sem-default de `motivoPerda`/`stageChangedAt`. Adicionar ao array de índices da tabela: `index("leads_import_batch_id_idx").on(table.importBatchId)`.

    Rodar `npx drizzle-kit push` para aplicar a nova coluna + índice ao banco `./data/crm.db` — mudança puramente aditiva (ADD COLUMN + CREATE INDEX), não deve pedir confirmação interativa de perda de dados. Esta task é BLOQUEANTE: `npx tsc --noEmit`/`npm run build` passam mesmo sem o push (tipos vêm do schema TS, não do banco vivo) — sem o push, `bulkImportLeads` (Task 3) falharia em runtime contra um banco desatualizado, um falso-positivo de verificação.

    Nota: o precedente de 03-01-PLAN.md usou `drizzle-kit generate` + `migrate` (por causa de um backfill custom necessário naquele caso). Aqui não há backfill — `import_batch_id` é legitimamente `NULL` para todo lead já existente/manual — então `drizzle-kit push` (mesmo comando usado em 04-02 para a tabela `templates`) é suficiente e mais simples.
  </action>
  <verify>
    <automated>cd "C:/Users/Vencedor/Desktop/crm-leads" && npx drizzle-kit push</automated>
    <automated>node -e "const D=require('better-sqlite3'); const db=new D('./data/crm.db'); const cols=db.prepare('PRAGMA table_info(leads)').all().map(c=>c.name); if(!cols.includes('import_batch_id')) throw new Error('faltou import_batch_id'); const idx=db.prepare(\"SELECT name FROM sqlite_master WHERE type='index' AND name='leads_import_batch_id_idx'\").get(); if(!idx) throw new Error('faltou indice leads_import_batch_id_idx'); console.log('OK: coluna e indice presentes');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/schema.ts` contém `importBatchId: text("import_batch_id")` sem `.notNull()` e sem `.default(`
    - `src/db/schema.ts` contém `index("leads_import_batch_id_idx").on(table.importBatchId)`
    - `npx drizzle-kit push` conclui sem erro e sem prompt de perda de dados
    - `PRAGMA table_info(leads)` no banco real inclui `import_batch_id`; `sqlite_master` lista o índice `leads_import_batch_id_idx`
  </acceptance_criteria>
  <done>LEAD-05 vivo no banco real — leads manuais continuam com import_batch_id NULL; Task 3 já pode escrever nela.</done>
</task>

<task type="auto">
  <name>Task 2: Módulos puros — sniff de encoding, mapeamento/dedup de linha CSV, csvRowSchema</name>
  <files>src/lib/csv-encoding.ts, src/lib/csv-import.ts, src/lib/validations.ts</files>
  <read_first>
    - src/lib/phone.ts (estilo de módulo puro: doc-comment de contrato no topo, função pura exportada, zero I/O)
    - src/lib/money.ts (mesmo estilo, `parseBRLToCents`/`formatCentsToBRL` a reaproveitar via csvRowSchema herdado)
    - src/lib/validations.ts (leadSchema completo — ver bloco `<interfaces>` acima)
    - .planning/phases/02-csv-bulk-import/02-RESEARCH.md (Pattern 1 — código completo de `decodeCsvFile`; Pitfall 2 — BOM; Pitfall 5 — normalizar telefone ANTES de comparar)
    - .planning/phases/02-csv-bulk-import/02-PATTERNS.md (seções 3 e 4)
    - .planning/phases/02-csv-bulk-import/02-CONTEXT.md (D-02, D-03, D-04, D-06, D-07, D-09, D-11, D-12)
  </read_first>
  <action>
    Criar `src/lib/csv-encoding.ts` com doc-comment de contrato (estilo `phone.ts`, citando IMPORT-03/D-03) exportando `async function decodeCsvFile(file: File): Promise<string>`: (1) checar BOM UTF-8 (`0xEF,0xBB,0xBF`) nos 3 primeiros bytes — se presente, `new TextDecoder("utf-8").decode(buffer)` (TextDecoder remove o BOM automaticamente, evita o quirk do PapaParse #840 no primeiro header); (2) senão, tentar `new TextDecoder("utf-8", { fatal: true }).decode(buffer)`; (3) se lançar, fallback `new TextDecoder("windows-1252").decode(buffer)`. Implementação exata em 02-RESEARCH.md Pattern 1.

    Criar `src/lib/csv-import.ts` (módulo puro, doc-comment de contrato) exportando:
    - `export type CsvFieldKey = "nome" | "telefone" | "subnichoNome" | "canal" | "origem" | "valorEstimado" | "notas";`
    - `export type CsvColumnMapping = Record<CsvFieldKey, string | null>;` (valor = nome do header do CSV mapeado para esse campo; `null` = não mapeado)
    - `export type ParsedCsvRow = Record<string, string>;` (saída crua do `Papa.parse({ header: true })`, consumida pela Fase 02-02)
    - `export type MappedCsvRow = { rowIndex: number; nome: string; telefone: string; telefoneNormalizado: string | null; subnichoNome: string; canal: "instagram" | "whatsapp"; origem: string; valorEstimado: string; notas: string; };` — `telefone`/`valorEstimado` ficam como STRING crua (mesmo texto do CSV); `telefoneNormalizado` é só para cálculo de flag/dedup client-side, nunca é o que se envia ao servidor.
    - `export const CSV_DEFAULTS = { canal: "whatsapp", origem: "Importação CSV", notas: "Importado via CSV.", valorEstimado: "0" } as const;` — valores concretos de default (decisão desta fase, documentada aqui) usados SOMENTE quando a coluna opcional correspondente não foi mapeada ou a célula está em branco/todo-espaço após trim. `nome`/`telefone`/`subnichoNome` NUNCA recebem default (D-04: nome+telefone são obrigatórios; subnichoNome em branco é bloqueado por D-12, não preenchido silenciosamente).
    - `export function mapCsvRows(rows: ParsedCsvRow[], mapping: CsvColumnMapping): MappedCsvRow[]` — para cada linha+índice: lê o valor da coluna mapeada (ou `""` se `mapping[campo] === null` ou a coluna não existir na linha); para `canal`/`origem`/`notas`/`valorEstimado`, se o valor trimado for `""`, usa `CSV_DEFAULTS[campo]`; `telefoneNormalizado = normalizePhone(telefone)` (import de `@/lib/phone`, D-06).
    - `export function detectWithinBatchDuplicatePhones(rows: MappedCsvRow[]): Set<number>` — agrupa por `telefoneNormalizado` (ignora `null`); retorna o conjunto de `rowIndex` de TODAS as linhas que compartilham um `telefoneNormalizado` com pelo menos outra linha do lote (D-07 metade 1) — normalizar ANTES de comparar, nunca comparar a string crua (Pitfall 5).

    Em `src/lib/validations.ts`, adicionar (logo após `leadSchema`):
    ```
    export const csvRowSchema = leadSchema.omit({ subnichoId: true, followUpDate: true }).extend({
      subnichoNome: z.string().trim().min(1, "Sub-nicho é obrigatório."),
    });
    export type CsvRowValues = z.infer<typeof csvRowSchema>;
    ```
    `subnichoId` é omitido porque a linha de CSV carrega o NOME do sub-nicho, resolvido para id só dentro da transação de `bulkImportLeads` (Task 3). `followUpDate` é omitido porque nenhuma coluna de follow-up é coletada do CSV (D-04/UI-SPEC não listam esse campo como mapeável) — o servidor grava `followUpDate = new Date()` (momento da importação) diretamente no insert, não vem do input do admin. `subnichoNome` reaproveita a mesma regra de not-blank de D-12 (bloqueia linha vazia).
  </action>
  <verify>
    <automated>cd "C:/Users/Vencedor/Desktop/crm-leads" && npx tsc --noEmit</automated>
    <automated>node -e "const fs=require('fs'); const enc=fs.readFileSync('src/lib/csv-encoding.ts','utf8'); if(!enc.includes('windows-1252')) throw new Error('faltou fallback windows-1252'); if(!enc.includes('0xEF')||!enc.includes('0xBB')||!enc.includes('0xBF')) throw new Error('faltou checagem de BOM EF BB BF'); const imp=fs.readFileSync('src/lib/csv-import.ts','utf8'); ['mapCsvRows','detectWithinBatchDuplicatePhones','CSV_DEFAULTS','normalizePhone','whatsapp','Importação CSV','Importado via CSV.'].forEach(t=>{if(!imp.includes(t))throw new Error('faltou '+t)}); const val=fs.readFileSync('src/lib/validations.ts','utf8'); if(!val.includes('csvRowSchema')) throw new Error('faltou csvRowSchema'); if(!/csvRowSchema\s*=\s*leadSchema\.omit/.test(val)) throw new Error('csvRowSchema deve derivar de leadSchema.omit'); console.log('OK: libs puras + csvRowSchema presentes');"</automated>
  </verify>
  <acceptance_criteria>
    - `decodeCsvFile` checa os 3 bytes de BOM explicitamente e cai em `windows-1252` no branch catch (grep confirma ambos)
    - `csv-import.ts` exporta `mapCsvRows`, `detectWithinBatchDuplicatePhones`, `CSV_DEFAULTS` com os 4 valores literais exatos (`"whatsapp"`, `"Importação CSV"`, `"Importado via CSV."`, `"0"`)
    - `mapCsvRows`/`detectWithinBatchDuplicatePhones` usam `normalizePhone` de `@/lib/phone` (nunca uma reimplementação)
    - `csvRowSchema` é derivado via `leadSchema.omit({ subnichoId: true, followUpDate: true }).extend(...)` — não uma cópia paralela de campos
    - `npx tsc --noEmit` limpo
  </acceptance_criteria>
  <done>Encoding, mapeamento com defaults concretos, dedup dentro do lote e o schema Zod server-side de linha CSV estão prontos para a Task 3 (backend) e para 02-02 (wizard) consumirem.</done>
</task>

<task type="auto">
  <name>Task 3: Server Actions — fetchPreviewSupportData + bulkImportLeads (transação, D-08/D-09/D-12, LEAD-05)</name>
  <files>src/actions/import-actions.ts</files>
  <read_first>
    - src/actions/lead-actions.ts (createLead/updateLeadStage — padrão ActionState, revalidatePath, backstop de FK)
    - src/actions/subnicho-actions.ts (createSubnicho — query case-insensitive-trim EXATA a reaproveitar, D-09/D-11)
    - src/lib/phone.ts, src/lib/validations.ts (csvRowSchema da Task 2)
    - src/db/schema.ts (leads.importBatchId da Task 1; subnichos)
    - .planning/phases/02-csv-bulk-import/02-RESEARCH.md (seção "Code Examples" — bulkImportLeads completo; Pitfall 3 — nunca `.values([...todasAsLinhas])`; Pitfall 6 — mesma query entre prévia e confirmação)
    - .planning/phases/02-csv-bulk-import/02-PATTERNS.md (seção 5, Padrões A-D)
    - .planning/phases/02-csv-bulk-import/02-CONTEXT.md (D-05, D-07, D-08, D-09, D-10, D-12)
  </read_first>
  <action>
    Criar `src/actions/import-actions.ts` (`"use server"` no topo do arquivo).

    Definir UMA função helper de lookup de sub-nicho reaproveitada em AMBOS os pontos abaixo (Pitfall 6 — nunca duas strings SQL diferentes para a mesma checagem): `function subnichoLookupCondition(nomeTrimado: string) { return sql\`lower(trim(${subnichos.nome})) = lower(trim(${nomeTrimado}))\`; }` (idêntica à condição já usada em `createSubnicho`).

    `export type PreviewSupportData = { duplicatePhones: string[]; unknownSubnichoNames: string[]; };`
    `export async function fetchPreviewSupportData(normalizedPhones: string[], subnichoNamesTrimmed: string[]): Promise<PreviewSupportData>`: deduplicar os dois arrays de entrada; se `normalizedPhones.length > 0`, consultar `db.select({ telefone: leads.telefone }).from(leads).where(and(isNull(leads.deletedAt), inArray(leads.telefone, normalizedPhones)))` (D-05/D-07 metade 2 — só leads ATIVOS contam como duplicata) e devolver os telefones encontrados em `duplicatePhones`; para cada nome único em `subnichoNamesTrimmed`, consultar `db.select({ id: subnichos.id }).from(subnichos).where(subnichoLookupCondition(nome))` — nomes SEM resultado entram em `unknownSubnichoNames` (D-09/D-10).

    `export type ConfirmedImportRow = { nome: string; telefone: string; canal: "instagram" | "whatsapp"; origem: string; valorEstimado: string; notas: string; subnichoNome: string; };` (mesmo formato de campo que `csvRowSchema` espera — nenhum rename necessário no `safeParse`).
    `export type BulkImportResult = { success: true; batchId: string; count: number } | { success: false; message: string };`

    `export async function bulkImportLeads(rows: ConfirmedImportRow[]): Promise<BulkImportResult>`:
    1. Se `rows.length === 0`, retornar `{ success: false, message: "Nenhuma linha para importar." }`.
    2. Revalidar CADA linha com `csvRowSchema.safeParse(row)` (defesa em profundidade, ASVS V5 — nunca confiar só na prévia client-side, mesmo padrão de `createLead`). Se QUALQUER linha falhar, abortar tudo (nenhum insert parcial) e retornar `{ success: false, message: "Não foi possível importar os leads. Tente novamente." }` (copy literal do toast de erro do UI-SPEC).
    3. `const batchId = randomUUID();` (`import { randomUUID } from "node:crypto";` — sem pacote `uuid`, per RESEARCH.md).
    4. `const insertedCount = db.transaction((tx) => { ... return count; })` — callback SÍNCRONO (driver better-sqlite3, sem `await` dentro): (a) construir `const subnichoIdByNome = new Map<string, number>()`; para cada `subnichoNome` único (chave = trim+lowercase) entre as linhas validadas: buscar via `tx.select({ id: subnichos.id }).from(subnichos).where(subnichoLookupCondition(nome)).all()[0]` — se existir, usar seu id; senão, `const [created] = tx.insert(subnichos).values({ nome: nomeOriginalTrimado }).returning();` (usa a primeira grafia/capitalização vista no CSV para esse nome, não força lowercase no nome criado) e guardar `created.id` no Map, ANTES de processar a próxima linha com o mesmo nome (evita criar o mesmo sub-nicho duas vezes no mesmo lote); (b) `for (const row of validatedRows) { tx.insert(leads).values({ nome: row.nome, telefone: row.telefone, canal: row.canal, origem: row.origem, valorEstimado: row.valorEstimado, notas: row.notas, subnichoId: subnichoIdByNome.get(chave)!, stage: "novo", stageChangedAt: new Date(), followUpDate: new Date(), importBatchId: batchId }).run(); }` — SEMPRE um insert por linha dentro do loop, NUNCA `.values([...todasAsLinhas])` num único statement (Pitfall 3 — limite de variáveis do SQLite).
    5. `revalidatePath("/")`, `revalidatePath("/leads")`, `revalidatePath("/pipeline")` (mesmo trio de `createLead`/`softDeleteLead`).
    6. Retornar `{ success: true, batchId, count: insertedCount }`.
  </action>
  <verify>
    <automated>cd "C:/Users/Vencedor/Desktop/crm-leads" && npx tsc --noEmit</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/actions/import-actions.ts','utf8'); ['fetchPreviewSupportData','bulkImportLeads','db.transaction','csvRowSchema.safeParse','randomUUID','importBatchId'].forEach(t=>{if(!s.includes(t))throw new Error('faltou '+t)}); if((s.match(/lower\(trim\(/g)||[]).length<1) throw new Error('faltou condicao lower(trim( compartilhada'); console.log('OK: import-actions.ts contem os contratos esperados');"</automated>
    <human-check>Criar um script temporário na raiz do projeto (ex.: `verify-import-actions.tmp.cjs`, deletado ao final) que: cria um sub-nicho de teste e um lead ativo de teste com telefone conhecido diretamente via better-sqlite3; chama `fetchPreviewSupportData` com um telefone igual ao do lead de teste (esperado: aparece em `duplicatePhones`) e um nome de sub-nicho inexistente (esperado: aparece em `unknownSubnichoNames`); chama `bulkImportLeads` com 2 linhas confirmadas (uma reaproveitando o sub-nicho de teste, outra com o nome de sub-nicho novo) e confirma que ambos os leads criados têm o MESMO `importBatchId`, que o sub-nicho novo foi criado exatamente uma vez, e que uma linha com `subnichoNome: ""` faz `bulkImportLeads` retornar `{ success: false }` sem inserir nada. Ao final, soft-deletar (`deletedAt`) os leads de teste e apagar o sub-nicho de teste recém-criado (ou deixar como registro de teste conhecido), depois apagar o script temporário.</human-check>
  </verify>
  <acceptance_criteria>
    - `import-actions.ts` exporta `fetchPreviewSupportData`, `bulkImportLeads`, `PreviewSupportData`, `ConfirmedImportRow`, `BulkImportResult`
    - `bulkImportLeads` chama `csvRowSchema.safeParse` para cada linha ANTES de qualquer insert
    - `bulkImportLeads` envolve os inserts em `db.transaction(` e nunca passa mais de um objeto de linha por chamada de `.values(` para `leads` (grep não encontra `.values([` seguido de múltiplos objetos de lead na mesma chamada)
    - A condição de lookup de sub-nicho (`lower(trim(`) é definida uma única vez e reaproveitada tanto em `fetchPreviewSupportData` quanto dentro da transação de `bulkImportLeads`
    - Todo lead inserido num mesmo lote recebe o mesmo `importBatchId` (confirmado pelo script temporário do human-check)
    - `revalidatePath` chamado para `"/"`, `"/leads"`, `"/pipeline"` dentro de `bulkImportLeads`
    - `npx tsc --noEmit` limpo
  </acceptance_criteria>
  <done>Motor de importação completo e provado correto contra o banco real: dedup contra o banco, auto-criação de sub-nicho sem duplicação dentro do lote, bloqueio de sub-nicho vazio, e inserção transacional com tag de lote — pronto para 02-02 (wizard) chamar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wizard (browser, construído em 02-02) -> Server Actions (import-actions.ts) | Arrays de telefones/nomes de sub-nicho e linhas confirmadas cruzam para o servidor, não confiáveis |
| Server Action -> SQLite (Drizzle, db.transaction) | Insert em lote de leads + auto-criação de sub-nicho, executado com privilégio total sobre `./data/crm.db` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | bulkImportLeads(rows[]) | mitigate | `csvRowSchema.safeParse` revalida CADA linha no servidor antes de qualquer insert (ASVS V5), nunca confia na prévia client-side; se qualquer linha falhar, a transação inteira é abortada (nenhum insert parcial) |
| T-02-02 | Denial of Service | Insert em lote (limite de variáveis do SQLite) | mitigate | `db.transaction()` com loop de insert linha-a-linha, nunca `.values([...todasAsLinhas])` num único statement — evita `SQLITE_ERROR: too many SQL variables` em lotes de dezenas/centenas de linhas (Pitfall 3, RESEARCH.md) |
| T-02-03 | Tampering / Race | Auto-criação de sub-nicho em lote (D-09) | mitigate | `Map<string,number>` chaveado por nome lowercase-trim dentro da MESMA transação evita criar o mesmo sub-nicho duas vezes no mesmo lote; a query de existência (`lower(trim(`) é compartilhada entre `fetchPreviewSupportData` (prévia) e `bulkImportLeads` (confirmação) para que as duas nunca divirjam (Pitfall 6) |
| T-02-04 | Tampering | Checagem de duplicado (fetchPreviewSupportData) contornável pelo client | accept | A checagem é só informativa (D-05 — o admin decide, não é um controle de bloqueio); mesmo que o client pule a chamada de prévia, `bulkImportLeads` ainda cria o lead normalmente — comportamento intencional de D-08 ("importar mesmo assim" sempre cria um lead novo), nunca um controle de segurança |
| T-02-SC | Tampering | Dependências npm | accept | Nenhuma instalação neste plano (`crypto.randomUUID` é nativo do Node, sem pacote `uuid`) |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo após cada task
- `npx drizzle-kit push` aplicado; `PRAGMA table_info(leads)` confirma `import_batch_id`; índice `leads_import_batch_id_idx` presente
- Script temporário confirma: dedup contra DB funciona, sub-nicho novo criado uma única vez por lote, linha com sub-nicho vazio rejeitada sem insert parcial, todos os leads de um lote compartilham o mesmo `importBatchId`
</verification>

<success_criteria>
- LEAD-05 satisfeito: coluna `import_batch_id` viva no banco, gravada por `bulkImportLeads`
- IMPORT-02 (metade de dados) satisfeito: dedup por telefone normalizado, nas duas direções (contra o banco e dentro do lote)
- IMPORT-03 (metade de dados) satisfeito: encoding auto-detectado via BOM/TextDecoder antes de qualquer parse
- D-08/D-09/D-11/D-12 satisfeitos no backend: import-anyway sempre cria lead novo, sub-nicho desconhecido auto-criado com a mesma regra exata de LEAD-02, sem fuzzy-matching, sub-nicho vazio bloqueia a linha
</success_criteria>

<output>
Create `.planning/phases/02-csv-bulk-import/02-01-SUMMARY.md` when done
</output>
