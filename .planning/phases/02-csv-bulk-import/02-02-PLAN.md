---
phase: 02-csv-bulk-import
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - package.json
  - src/app/importar/page.tsx
  - src/components/csv-import-wizard.tsx
  - src/components/csv-upload-dropzone.tsx
  - src/components/csv-column-mapper.tsx
  - src/components/csv-import-preview-table.tsx
  - src/components/app-sidebar.tsx
autonomous: false
requirements: [IMPORT-01, IMPORT-02, IMPORT-03]
user_setup: []

must_haves:
  truths:
    - "Admin consegue arrastar/selecionar um arquivo .csv real e ver o separador (vírgula/ponto-e-vírgula) e a codificação detectados automaticamente, sem configurar nada manualmente"
    - "Admin consegue mapear colunas do CSV para os campos do lead, com nome e telefone obrigatórios e os demais campos opcionais"
    - "A prévia mostra todas as linhas do arquivo, com flags visuais distintas para duplicado, sub-nicho novo e sub-nicho obrigatório/bloqueado"
    - "O botão 'Confirmar importação' fica desabilitado enquanto existir alguma linha bloqueada sem sub-nicho selecionado"
    - "Confirmar a importação cria leads reais no banco (via bulkImportLeads) e mostra um toast de sucesso"
  artifacts:
    - path: "src/app/importar/page.tsx"
      provides: "Rota /importar (Server Component, busca subnichos+templates)"
      contains: "CsvImportWizard"
    - path: "src/components/csv-import-wizard.tsx"
      provides: "Orquestrador client dos passos upload/mapear/prévia via useState discriminado"
      contains: "WizardState"
    - path: "src/components/csv-upload-dropzone.tsx"
      provides: "Drag-and-drop/seleção de arquivo .csv"
    - path: "src/components/csv-column-mapper.tsx"
      provides: "Mapeamento de colunas CSV -> campos do lead"
    - path: "src/components/csv-import-preview-table.tsx"
      provides: "Tabela de prévia com flags (duplicado/novo sub-nicho/bloqueado) e confirmação"
    - path: "src/components/app-sidebar.tsx"
      provides: "Item de nav 'Importar' -> /importar, logo após 'Leads'"
      contains: "/importar"
  key_links:
    - from: "src/components/csv-import-wizard.tsx"
      to: "src/lib/csv-encoding.ts + src/lib/csv-import.ts"
      via: "decodeCsvFile() antes de Papa.parse(); mapCsvRows()/detectWithinBatchDuplicatePhones() na transição para a prévia"
      pattern: "decodeCsvFile"
    - from: "src/components/csv-import-preview-table.tsx"
      to: "src/actions/import-actions.ts"
      via: "fetchPreviewSupportData() ao entrar na prévia; bulkImportLeads() no confirmar"
      pattern: "bulkImportLeads"
    - from: "src/components/app-sidebar.tsx"
      to: "src/app/importar/page.tsx"
      via: "NAV_ITEMS -> href /importar"
      pattern: "/importar"
---

<objective>
Wizard completo de importação de CSV: upload com drag-and-drop, mapeamento de colunas, e prévia com flags (duplicado/sub-nicho novo/sub-nicho bloqueado) + confirmação — consumindo o motor de dados construído em 02-01 (`decodeCsvFile`, `mapCsvRows`, `detectWithinBatchDuplicatePhones`, `fetchPreviewSupportData`, `bulkImportLeads`). Ao final deste plano, IMPORT-01/IMPORT-02/IMPORT-03 estão inteiramente satisfeitas do ponto de vista do admin: ele consegue subir um CSV real do cowork e ver leads de verdade criados no banco.

Purpose: É o corte vertical visível da fase — sem esta UI, o motor de 02-01 não é alcançável pelo admin. Fecha a experiência ponta a ponta descrita nos critérios de sucesso 1-3 do ROADMAP da Fase 2.
Output: Rota `/importar` funcional com wizard de 3 passos, item de navegação, e leads reais criados a partir de um CSV confirmado.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-csv-bulk-import/02-CONTEXT.md
@.planning/phases/02-csv-bulk-import/02-RESEARCH.md
@.planning/phases/02-csv-bulk-import/02-PATTERNS.md
@.planning/phases/02-csv-bulk-import/02-UI-SPEC.md

<interfaces>
<!-- Contratos produzidos por 02-01 — usar diretamente, não reimplementar. -->

De src/lib/csv-encoding.ts (02-01):
```typescript
export async function decodeCsvFile(file: File): Promise<string>
```

De src/lib/csv-import.ts (02-01):
```typescript
export type CsvFieldKey = "nome" | "telefone" | "subnichoNome" | "canal" | "origem" | "valorEstimado" | "notas";
export type CsvColumnMapping = Record<CsvFieldKey, string | null>;
export type ParsedCsvRow = Record<string, string>;
export type MappedCsvRow = { rowIndex: number; nome: string; telefone: string; telefoneNormalizado: string | null; subnichoNome: string; canal: "instagram" | "whatsapp"; origem: string; valorEstimado: string; notas: string; };
export const CSV_DEFAULTS: { canal: "whatsapp"; origem: "Importação CSV"; notas: "Importado via CSV."; valorEstimado: "0"; };
export function mapCsvRows(rows: ParsedCsvRow[], mapping: CsvColumnMapping): MappedCsvRow[];
export function detectWithinBatchDuplicatePhones(rows: MappedCsvRow[]): Set<number>;
```

De src/actions/import-actions.ts (02-01):
```typescript
export type PreviewSupportData = { duplicatePhones: string[]; unknownSubnichoNames: string[]; };
export async function fetchPreviewSupportData(normalizedPhones: string[], subnichoNamesTrimmed: string[]): Promise<PreviewSupportData>;
export type ConfirmedImportRow = { nome: string; telefone: string; canal: "instagram" | "whatsapp"; origem: string; valorEstimado: string; notas: string; subnichoNome: string; };
export type BulkImportResult = { success: true; batchId: string; count: number } | { success: false; message: string };
export async function bulkImportLeads(rows: ConfirmedImportRow[]): Promise<BulkImportResult>;
```

Estado atual — src/app/leads/page.tsx (analog exato de Server Component a seguir para `/importar/page.tsx`):
```typescript
export default async function LeadsPage() {
  const [activeLeads, allSubnichos, allTemplates] = await Promise.all([
    db.select().from(leads).where(isNull(leads.deletedAt)).orderBy(asc(leads.followUpDate)),
    db.select().from(subnichos),
    db.select().from(templates),
  ]);
  return (
    <div className="flex flex-col gap-6">
      <h1 className="text-[28px] font-semibold leading-tight">Leads</h1>
      <LeadTable leads={activeLeads} subnichos={allSubnichos} templates={allTemplates} />
    </div>
  );
}
```

Estado atual — src/components/app-sidebar.tsx (NAV_ITEMS, adicionar `/importar` logo após `/leads`):
```typescript
const NAV_ITEMS = [
  { href: "/", label: "Follow-ups" },
  { href: "/leads", label: "Leads" },
  { href: "/pipeline", label: "Pipeline" },
  { href: "/templates", label: "Templates" },
  { href: "/subnichos", label: "Sub-nichos" },
  { href: "/lixeira", label: "Lixeira" },
] as const;
```

Estado atual — src/components/subnicho-combobox.tsx (reaproveitar sem modificação para o combobox inline de linha bloqueada, D-12):
```typescript
export function SubnichoCombobox({ subnichos, value, onValueChange, name, disabled, invalid }: SubnichoComboboxProps)
```

Estado atual — src/components/lead-table-columns.tsx (padrão `declare module "@tanstack/react-table" { interface TableMeta<TData> {...} }` para canais de callback via `meta`, e `useReactTable({ data, columns, getCoreRowModel: getCoreRowModel() })` mínimo sem sort/filtro/paginação — a prévia desta fase só precisa disso).

02-UI-SPEC.md Copywriting Contract (copy literal a usar — não parafrasear): título da página "Importar leads"; dropzone vazio "Arraste o CSV aqui ou clique para selecionar" / "Aceita arquivos .csv. Detectamos automaticamente o separador (vírgula ou ponto-e-vírgula) e a codificação do arquivo."; erro de leitura "Não foi possível ler este arquivo" / "Verifique se é um arquivo .csv exportado do Google Sheets ou Excel e tente novamente." / botão "Tentar novamente"; zero linhas válidas "Nenhuma linha válida encontrada" / "O arquivo foi lido, mas nenhuma linha tem nome e telefone preenchidos. Confira o arquivo e tente novamente."; passo 2 título "Mapeie as colunas", ajuda "Nome e telefone são obrigatórios. Os demais campos são opcionais — deixe em branco se o arquivo não tiver essa coluna.", indicador "Detectado: separador {vírgula/ponto-e-vírgula}, codificação {UTF-8/Windows-1252}"; CTA "Ver prévia"; passo 3 título "Revise antes de importar", resumo "{N} leads no arquivo · {D} duplicados · {S} sub-nichos novos · {B} bloqueados"; badges "Duplicado"/"Novo sub-nicho"/"Sub-nicho obrigatório"; toggle linha duplicada "Importar mesmo assim"; aviso sub-nichos novos "{N} sub-nichos novos serão criados: {lista}"; CTA confirmar "Confirmar importação" (disabled + title "Selecione um sub-nicho para todas as linhas bloqueadas"); CTA voltar "Voltar ao mapeamento"; toast sucesso "{N} leads importados com sucesso."; toast erro "Não foi possível importar os leads. Tente novamente."
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING] Verificar legitimidade de @types/papaparse antes de instalar</name>
  <files>package.json (gate pre-instalacao — nenhum arquivo e modificado nesta task)</files>
  <read_first>
    - .planning/phases/02-csv-bulk-import/02-RESEARCH.md (secao "Package Legitimacy Audit" e "Assumptions Log" A4)
  </read_first>
  <action>Gate humano bloqueante de supply-chain ANTES de qualquer `npm install`. O executor NAO instala nada nesta task — apenas apresenta ao humano os itens de checagem abaixo para `@types/papaparse` (o unico pacote desta fase marcado `[ASSUMED]`; `papaparse` em si ja esta `[OK]`/Aprovado no Package Legitimacy Audit do RESEARCH.md e NAO passa por este gate) e aguarda o humano digitar "approved".</action>
  <what-built>Nada ainda — este e o gate de legitimidade de pacote ANTES da instalacao (Package Legitimacy Gate). `papaparse` ja esta `[OK]`/Aprovado no Package Legitimacy Audit de 02-RESEARCH.md (slopcheck rodado, sem postinstall). Ja `@types/papaparse` esta marcado `[ASSUMED]` (Assumption A4 do RESEARCH.md) — nao foi slopchecado nesta sessao de pesquisa, so assumido como um pacote DefinitelyTyped legitimo.</what-built>
  <how-to-verify>
    Visitar https://www.npmjs.com/package/@types/papaparse e confirmar: (1) o pacote esta publicado sob o escopo `@types` (mantido pelo projeto DefinitelyTyped); (2) o link "Repository" aponta para `github.com/DefinitelyTyped/DefinitelyTyped`; (3) o volume de downloads semanais e compativel com um pacote de tipos amplamente usado (classe de milhoes/semana, coerente com a popularidade do proprio `papaparse`); (4) nenhuma mudanca recente e suspeita de mantenedor/ownership. Opcionalmente, se o CLI `slopcheck` estiver disponivel localmente, rodar `python -m slopcheck install @types/papaparse` e conferir o veredito.
  </how-to-verify>
  <resume-signal>Responda "approved" para prosseguir com a instalacao de `@types/papaparse` como devDependency, ou "reject" com o motivo para interromper (nesse caso, usar um stub `.d.ts` local em vez do pacote DefinitelyTyped).</resume-signal>
  <verify>
    <human-check>Os 4 itens objetivos (escopo @types, repositorio DefinitelyTyped, volume de downloads, ausencia de mudanca suspeita de mantenedor) confirmados em npmjs.com antes de liberar a instalacao.</human-check>
  </verify>
  <acceptance_criteria>
    - "approved" so e dado quando os 4 itens conferem
    - Este checkpoint NAO e auto-aprovavel (gate de legitimidade de pacote — workflow.auto_advance ignorado)
    - Nenhum `npm install` roda antes da aprovacao humana explicita
  </acceptance_criteria>
  <done>Humano digitou "approved" apos confirmar os 4 itens objetivos para @types/papaparse.</done>
</task>

<task type="auto">
  <name>Task 2: Upload + mapeamento de colunas (passos 1-2 do wizard)</name>
  <files>package.json, src/app/importar/page.tsx, src/components/csv-import-wizard.tsx, src/components/csv-upload-dropzone.tsx, src/components/csv-column-mapper.tsx</files>
  <read_first>
    - src/app/leads/page.tsx (analog exato de Server Component — ver bloco `<interfaces>` acima)
    - src/lib/csv-encoding.ts, src/lib/csv-import.ts (contratos de 02-01 — ver bloco `<interfaces>` acima)
    - src/components/lead-table.tsx (padrão de state machine local via `useState` com union discriminada — DialogState — NUNCA `useReducer`)
    - src/components/lead-form-dialog.tsx (padrão Field/FieldLabel/Select para os selects de mapeamento)
    - .planning/phases/02-csv-bulk-import/02-RESEARCH.md (Architecture Patterns — diagrama completo; Pattern 2 — config exata do Papa.parse; Pitfall 1 e 2 — encoding/BOM)
    - .planning/phases/02-csv-bulk-import/02-PATTERNS.md (seções 6 e 8, e "Convenções transversais" seção 10)
    - .planning/phases/02-csv-bulk-import/02-UI-SPEC.md (Copywriting Contract completo, Spacing — dropzone `min-h-48`)
  </read_first>
  <action>
    Rodar `npm install papaparse` e `npm install --save-dev @types/papaparse` (só depois da Task 1 aprovada).

    Criar `src/app/importar/page.tsx` (Server Component, análogo a `leads/page.tsx`): buscar `subnichos` e `templates` via `Promise.all` (sem buscar `leads` — o CSV é a fonte de dados do wizard); renderizar `<h1 className="text-[28px] font-semibold leading-tight">Importar leads</h1>` + `<CsvImportWizard subnichos={allSubnichos} templates={allTemplates} />`.

    Criar `src/components/csv-import-wizard.tsx` (`"use client"`) com estado via `useState<WizardState>` (união discriminada, SEM `useReducer` — convenção do projeto, ver 02-PATTERNS.md seção 10):
    ```typescript
    type WizardState =
      | { step: "upload" }
      | { step: "mapping"; fileName: string; parsedRows: ParsedCsvRow[]; detectedDelimiter: string; mapping: CsvColumnMapping }
      | { step: "preview"; fileName: string; mappedRows: MappedCsvRow[] };
    ```
    Handler de seleção/drop de arquivo: (1) se `file.size` exceder ~10MB, mostrar aviso e não prosseguir (guarda leve de tamanho, RESEARCH.md V12 — realista para um lote de leads solo-admin); (2) `const decoded = await decodeCsvFile(file);` (SEMPRE antes do parse, nunca passar o `File`/buffer bruto pro Papa.parse); (3) `Papa.parse<ParsedCsvRow>(decoded, { header: true, skipEmptyLines: true, delimiter: "", transformHeader: (h) => h.trim() })`; (4) se `result.data.length === 0` ou nenhuma linha tem nome/telefone preenchidos após um primeiro mapeamento automático por nome de coluna, mostrar o estado de erro "Nenhuma linha válida encontrada" (copy literal do UI-SPEC) e permanecer/retornar ao passo `upload`; se o arquivo não pôde ser lido (exceção no parse), mostrar "Não foi possível ler este arquivo" com botão "Tentar novamente" que volta a `{ step: "upload" }`; (5) caso contrário, transicionar para `{ step: "mapping", fileName: file.name, parsedRows: result.data, detectedDelimiter: result.meta.delimiter, mapping: <inicial, todos os campos null> }`.

    Criar `src/components/csv-upload-dropzone.tsx`: área de drag-and-drop + `<input type="file" accept=".csv">` oculto, `min-h-48`, estado vazio com heading "Arraste o CSV aqui ou clique para selecionar" e texto de ajuda "Aceita arquivos .csv. Detectamos automaticamente o separador (vírgula ou ponto-e-vírgula) e a codificação do arquivo." (copy literal); ao selecionar, mostra o nome do arquivo + botão outline "Trocar arquivo" que reseta o wizard para `{ step: "upload" }`.

    Criar `src/components/csv-column-mapper.tsx`: um `<Select>` por `CsvFieldKey` (rótulos exatos do UI-SPEC: "Nome *", "Telefone *", "Sub-nicho", "Canal", "Origem", "Valor estimado", "Notas") cujas opções são os headers de `Object.keys(parsedRows[0] ?? {})`; campos opcionais (todos exceto nome/telefone) têm também a opção "— nenhuma —" (mapeia para `null`, D-04); texto auxiliar `text-muted-foreground` "Detectado: separador {vírgula/ponto-e-vírgula}, codificação {UTF-8/Windows-1252}" (mapear `detectedDelimiter` "," -> "vírgula", ";" -> "ponto-e-vírgula" para exibição); heading "Mapeie as colunas", ajuda "Nome e telefone são obrigatórios. Os demais campos são opcionais — deixe em branco se o arquivo não tiver essa coluna." (copy literal); CTA primário "Ver prévia" (`disabled` até nome E telefone estarem mapeados) que chama `mapCsvRows(parsedRows, mapping)` e transiciona para `{ step: "preview", fileName, mappedRows }` (a computação de flags/dedup contra o banco fica para a Task 3, próxima task do MESMO plano).
  </action>
  <verify>
    <automated>cd "C:/Users/Vencedor/Desktop/crm-leads" && npx tsc --noEmit</automated>
    <automated>npm run build</automated>
    <automated>node -e "const fs=require('fs'); const pkg=JSON.parse(fs.readFileSync('package.json','utf8')); if(!pkg.dependencies || !pkg.dependencies.papaparse) throw new Error('faltou papaparse em dependencies'); if(!pkg.devDependencies || !pkg.devDependencies['@types/papaparse']) throw new Error('faltou @types/papaparse em devDependencies'); const dz=fs.readFileSync('src/components/csv-upload-dropzone.tsx','utf8'); ['Arraste o CSV aqui ou clique para selecionar','Trocar arquivo'].forEach(t=>{if(!dz.includes(t))throw new Error('faltou copy: '+t)}); const wiz=fs.readFileSync('src/components/csv-import-wizard.tsx','utf8'); ['decodeCsvFile','Papa.parse','useState'].forEach(t=>{if(!wiz.includes(t))throw new Error('faltou '+t)}); if(wiz.includes('useReducer')) throw new Error('useReducer nao deve ser usado (convencao do projeto)'); console.log('OK: Task 2');"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` lista `papaparse` em `dependencies` e `@types/papaparse` em `devDependencies`
    - `csv-upload-dropzone.tsx` contém a copy literal "Arraste o CSV aqui ou clique para selecionar" e o botão "Trocar arquivo"
    - `csv-import-wizard.tsx` chama `decodeCsvFile` ANTES de `Papa.parse` no handler de arquivo, usa `useState` (união discriminada) e NÃO usa `useReducer`
    - Estados de erro mostram a copy literal "Não foi possível ler este arquivo" e "Nenhuma linha válida encontrada", ambos retornando ao passo `upload`
    - `csv-column-mapper.tsx` desabilita "Ver prévia" até nome+telefone mapeados; campos opcionais oferecem uma opção "não mapeado" (null)
    - `npx tsc --noEmit` e `npm run build` passam
  </acceptance_criteria>
  <done>Admin consegue selecionar/soltar um CSV real, ver o separador/codificação detectados automaticamente, mapear colunas para os campos do lead, e avançar para uma lista de linhas mapeadas — pronto para a Task 3 computar flags e confirmar.</done>
</task>

<task type="auto">
  <name>Task 3: Prévia com flags + confirmação + item de navegação</name>
  <files>src/components/csv-import-preview-table.tsx, src/components/csv-import-wizard.tsx, src/components/app-sidebar.tsx</files>
  <read_first>
    - src/components/csv-import-wizard.tsx (estado `{ step: "preview" }` produzido pela Task 2 — ESTENDER, não recriar)
    - src/actions/import-actions.ts (fetchPreviewSupportData, bulkImportLeads — contratos de 02-01, ver bloco `<interfaces>` acima)
    - src/components/lead-table-columns.tsx (padrão `declare module "@tanstack/react-table" { interface TableMeta<TData> {...} }` + `useReactTable({ getCoreRowModel: getCoreRowModel() })` mínimo)
    - src/components/subnicho-combobox.tsx (reaproveitar sem modificação, contido a `max-w-[220px]` na célula, D-12)
    - src/components/etapa-badge.tsx (padrão de badge com paleta fixa via `style={{ backgroundColor, color }}`, `<Badge variant="outline" className="border-transparent">`)
    - src/components/app-sidebar.tsx (NAV_ITEMS — ver bloco `<interfaces>` acima)
    - .planning/phases/02-csv-bulk-import/02-UI-SPEC.md (Color — as 3 cores/ícones de flag; Copywriting Contract — resumo, badges, CTAs, toasts)
    - .planning/phases/02-csv-bulk-import/02-PATTERNS.md (seção 8, Padrões A/B/C)
    - .planning/phases/02-csv-bulk-import/02-CONTEXT.md (D-05, D-09, D-10, D-12)
  </read_first>
  <action>
    Em `csv-import-wizard.tsx`, ao entrar em `{ step: "preview" }` (`useEffect` chaveado em `mappedRows`), chamar `fetchPreviewSupportData(telefonesNormalizadosÚnicos, nomesDeSubnichoTrimadosÚnicos)` (Server Action de 02-01) e `detectWithinBatchDuplicatePhones(mappedRows)` (lib de 02-01) para montar, por linha, as flags: `duplicadoDb` (telefone em `supportData.duplicatePhones`), `duplicadoLote` (rowIndex no Set retornado por `detectWithinBatchDuplicatePhones`), `subnichoNovo` (nome trim-lowercase em `supportData.unknownSubnichoNames`), `subnichoBloqueado` (`subnichoNome.trim() === ""`). Manter overrides do admin por linha num `Map<number, { importarMesmoAssim: boolean; subnichoOverrideId: number | null }>` (default `importarMesmoAssim: false` — D-05; `subnichoOverrideId: null` — D-12).

    Criar `src/components/csv-import-preview-table.tsx` (`@tanstack/react-table`, só `getCoreRowModel()` — sem sort/filtro/paginação, IMPORT-01 exige ver TODA linha): colunas Nome/Telefone/Sub-nicho/Canal/Origem/Valor estimado/Notas + coluna "Status" com os badges EXATAMENTE conforme UI-SPEC — "Duplicado" (`#FEF3C7`/`#B45309`, ícone `TriangleAlert`), "Novo sub-nicho" (`#DBEAFE`/`#1D4ED8`, ícone `Sparkles`), "Sub-nicho obrigatório" (`#FEE2E2`/`#B91C1C`, ícone `CircleAlert`) + `border-l-4 border-l-[#DC2626]` na `<TableRow>` inteira quando `subnichoBloqueado`; coluna "Ação" com checkbox "Importar mesmo assim" (linhas duplicadas) e `<SubnichoCombobox>` inline `max-w-[220px]` (linhas bloqueadas, escreve em `subnichoOverrideId` via callback `meta`). Resumo acima da tabela: `"{N} leads no arquivo · {D} duplicados · {S} sub-nichos novos · {B} bloqueados"` (copy literal, contadores reais); quando `S > 0`, aviso adicional `"{N} sub-nichos novos serão criados: {lista separada por vírgula}"` (D-10).

    Botão "Confirmar importação": `disabled` enquanto existir alguma linha com `subnichoBloqueado && subnichoOverrideId === null`; quando `disabled`, `title="Selecione um sub-nicho para todas as linhas bloqueadas"`. Ao clicar: filtrar as linhas a enviar — excluir linhas com `duplicadoDb || duplicadoLote` cujo `importarMesmoAssim` seja `false`; para linhas bloqueadas, resolver `subnichoNome` a partir do `subnichoOverrideId` (procurar o nome no array `subnichos` recebido via props); montar `ConfirmedImportRow[]` e chamar `bulkImportLeads(...)`. Em `{ success: true, count }`: `toast.success(\`${count} leads importados com sucesso.\`)` — o redirecionamento para a tela pós-importação (D-14) é responsabilidade do plano 02-03 (próxima wave); por ENQUANTO, resetar o wizard para `{ step: "upload" }` após o toast. Em `{ success: false, message }`: `toast.error(message)`, permanecer no passo de prévia (para o admin poder corrigir e tentar de novo). Botão secundário "Voltar ao mapeamento" retorna a `{ step: "mapping", ... }` preservando `parsedRows`/`detectedDelimiter`.

    Em `src/components/app-sidebar.tsx`, adicionar `{ href: "/importar", label: "Importar" }` a `NAV_ITEMS` IMEDIATAMENTE após o item `{ href: "/leads", label: "Leads" }`.
  </action>
  <verify>
    <automated>cd "C:/Users/Vencedor/Desktop/crm-leads" && npx tsc --noEmit</automated>
    <automated>npm run build</automated>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('src/components/csv-import-preview-table.tsx','utf8'); ['#FEF3C7','#B45309','TriangleAlert','#DBEAFE','#1D4ED8','Sparkles','#FEE2E2','#B91C1C','CircleAlert','border-l-[#DC2626]','subnichoBloqueado'].forEach(x=>{if(!t.includes(x))throw new Error('faltou '+x)}); const sb=fs.readFileSync('src/components/app-sidebar.tsx','utf8'); const leadsIdx=sb.indexOf('/leads'); const importarIdx=sb.indexOf('/importar'); if(importarIdx<0) throw new Error('faltou /importar em NAV_ITEMS'); if(!(importarIdx>leadsIdx && importarIdx<leadsIdx+120)) throw new Error('/importar deve vir logo apos /leads'); console.log('OK: Task 3');"</automated>
    <human-check>
      Rodar `npm run dev` (host de 4GB — se travar, pular e confiar nos automated acima). Fluxo completo: (1) criar um arquivo .csv de teste com cabeçalhos "Nome,Telefone,SubNicho" e 4 linhas — uma limpa, uma com telefone de um lead ativo já existente no banco (duplicata), uma com um nome de sub-nicho que não existe ainda, uma com a célula de sub-nicho vazia; (2) em `/importar`, subir o arquivo, confirmar detecção de vírgula/UTF-8; (3) mapear Nome/Telefone/SubNicho; (4) na prévia, confirmar as 3 flags aparecem nas linhas certas, "Confirmar importação" está desabilitado; (5) selecionar um sub-nicho para a linha bloqueada — botão habilita; (6) marcar "Importar mesmo assim" na linha duplicada; (7) confirmar — toast de sucesso, os leads aparecem em /leads com o sub-nicho novo criado em /subnichos.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `csv-import-preview-table.tsx` renderiza as 3 cores/ícones de flag exatos do UI-SPEC e aplica `border-l-[#DC2626]` na linha bloqueada
    - "Confirmar importação" tem uma expressão `disabled` que referencia `subnichoBloqueado` (linhas ainda sem sub-nicho resolvido bloqueiam o botão)
    - `bulkImportLeads` só é chamado com o conjunto de linhas já filtrado/resolvido (nunca o `mappedRows` bruto)
    - `app-sidebar.tsx` tem `{ href: "/importar", label: "Importar" }` logo após o item `/leads`
    - `npx tsc --noEmit` e `npm run build` passam
  </acceptance_criteria>
  <done>IMPORT-01/IMPORT-02/IMPORT-03 completos e navegáveis: admin sobe um CSV real, mapeia colunas, revê uma prévia com flags corretas, resolve linhas bloqueadas e confirma — leads reais são criados no banco com o import_batch_id do lote.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Arquivo CSV (upload do admin) -> Parsing client-side (decodeCsvFile + PapaParse) | Conteúdo do CSV é totalmente não confiável — encoding, delimitador, tamanho, conteúdo de célula, número de colunas |
| Wizard (client) -> Server Actions de prévia/confirmação (import-actions.ts, de 02-01) | Arrays de telefones/nomes de sub-nicho e linhas confirmadas cruzam para o servidor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Denial of Service | Arquivo CSV enorme/malformado travando a aba do navegador | mitigate | Guarda leve de tamanho de arquivo (~10MB) antes de decodificar/parsear (RESEARCH.md V12); todo o parse acontece client-side, o arquivo bruto nunca é enviado ao servidor |
| T-02-06 | Tampering | Encoding/delimitador ambíguo ou forjado | mitigate | `decodeCsvFile` (BOM + TextDecoder fatal probe, de 02-01) + `Papa.parse({ delimiter: "" })` cobrem os 2 casos reais documentados (D-01/D-02/D-03); o delimitador/codificação detectados são exibidos ao admin para conferência antes de mapear |
| T-02-07 | Tampering | CSV injection em nome/notas (ex.: `=cmd\|'/c calc'!A1`) | accept | Não explorável aqui — valores só são armazenados como texto SQLite e renderizados como texto React (nunca reexportados para planilha nem usados em fórmula/eval); risco de CSV injection só se materializaria numa futura feature de exportação, fora de escopo desta fase |
| T-02-SC | Tampering | Supply chain (papaparse + @types/papaparse) | mitigate | `papaparse` já `[OK]`/Aprovado (02-RESEARCH.md); `@types/papaparse` é `[ASSUMED]` — checkpoint humano bloqueante (Task 1) antes da instalação, verificando `npmjs.com/package/@types/papaparse` |
</threat_model>

<verification>
- `npx tsc --noEmit` e `npm run build` passam após cada task
- Fluxo completo de upload -> mapeamento -> prévia com flags -> confirmação testado via human-check (Task 3)
- `app-sidebar.tsx` navega corretamente para `/importar`
</verification>

<success_criteria>
- IMPORT-01 satisfeito: upload, mapeamento de colunas e prévia antes de qualquer escrita
- IMPORT-02 (visível) satisfeito: duplicatas flaggeadas na prévia, decisão por linha (D-05)
- IMPORT-03 (visível) satisfeito: separador e codificação detectados e exibidos ao admin
- D-09/D-10/D-12 satisfeitos na UI: sub-nicho novo destacado, sub-nicho vazio bloqueia confirmação até resolvido
</success_criteria>

<output>
Create `.planning/phases/02-csv-bulk-import/02-02-SUMMARY.md` when done
</output>
