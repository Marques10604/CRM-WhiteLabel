---
phase: 23-diagn-stico-de-ia-da-campanha
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - src/db/schema.ts
  - scripts/migrate-diagnosticos.cjs
  - scripts/verify-schema.cjs
autonomous: true
requirements: [DIAGNOSTICO-01, DIAGNOSTICO-10]

must_haves:
  truths:
    - "A tabela `diagnosticos` existe no banco real `data/crm.db` com o conjunto exato de 11 colunas"
    - "Rodar a migração duas vezes seguidas não altera nem uma linha das tabelas existentes"
    - "O gate `npm run verify:schema` falha se a tabela `diagnosticos` não existir ou divergir"
    - "Cada geração de diagnóstico é uma linha nova — a tabela é append-only, sem `deletedAt` e sem coluna de cache"
    - "Falha fatal e aviso não-fatal vivem em colunas separadas: `erro` só é preenchida quando `status` é `falhou`"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Tabela Drizzle `diagnosticos` com colunas JSON e FK restrict para campanhas"
      contains: "export const diagnosticos"
    - path: "scripts/migrate-diagnosticos.cjs"
      provides: "Migração manual idempotente com backup, testemunha e verificação pós"
      contains: "migrate-diagnosticos"
      min_lines: 100
    - path: "scripts/verify-schema.cjs"
      provides: "Gate estrito de colunas de `diagnosticos`"
      contains: "REQUIRED_DIAGNOSTICOS_COLUMNS"
  key_links:
    - from: "src/db/schema.ts"
      to: "src/lib/ai/diagnostico-schema.ts"
      via: "import type { Diagnostico } para o .$type<> da coluna payload"
      pattern: "diagnostico-schema"
    - from: "scripts/migrate-diagnosticos.cjs"
      to: "data/crm.db"
      via: "better-sqlite3 CREATE TABLE idempotente"
      pattern: "sqlite_master"
    - from: "scripts/verify-schema.cjs"
      to: "diagnosticos"
      via: "PRAGMA table_info conjunto estrito"
      pattern: "REQUIRED_DIAGNOSTICOS_COLUMNS"
---

<objective>
[BLOCKING] Criar a tabela `diagnosticos` — no schema Drizzle, no banco real `data/crm.db` via
migração `.cjs` manual, e no gate `verify:schema`.

Purpose: build e type-check passam SEM a migração (os tipos vêm de `schema.ts`, não do banco vivo).
Sem esta task a fase inteira entra em falso-positivo de verificação: o código compila, mas todo
INSERT de diagnóstico explode em runtime. A migração TEM de rodar de verdade contra `data/crm.db`.
Output: tabela `diagnosticos` viva, com 11 colunas, 3 índices e FK para `campanhas`, coberta por
gate estrito.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-diagn-stico-de-ia-da-campanha/23-RESEARCH.md
@.planning/phases/23-diagn-stico-de-ia-da-campanha/23-PATTERNS.md
@src/db/schema.ts
@scripts/migrate-campanhas.cjs
@scripts/verify-schema.cjs

<interfaces>
<!-- Contratos consumidos deste plano pelos planos 23-04 (Server Action) e 23-07 (leitura na UI) -->

De `src/db/schema.ts`:

- `export const diagnosticos` — `sqliteTable("diagnosticos", ...)` com os campos TS:
  `id` (number), `campanhaId` (number, FK `campanhas.id`, `onDelete: "restrict"`),
  `payload` (`Diagnostico | null`, coluna JSON), `fontes` (`{ url: string; title?: string }[] | null`, JSON),
  `buscas` (`string[] | null`, JSON), `status` (`"ok" | "falhou"`), `erro` (`string | null`),
  `aviso` (`string | null`), `inputTokens` (`number | null`), `outputTokens` (`number | null`),
  `criadoEm` (`Date`, `integer({ mode: "timestamp" })` com default `(unixepoch())`).

Semântica travada de `erro` × `aviso` (D-23-07): `erro` é preenchida SOMENTE quando `status` é
`"falhou"` (mensagem da falha fatal, `payload` NULL). `aviso` é preenchida em gerações
BEM-SUCEDIDAS (`status` `"ok"`) que passaram com ressalva — hoje o `avisoCrossCheck` devolvido por
`gerarDiagnostico()` (plano 23-03). As duas nunca são preenchidas ao mesmo tempo.

Índices: `diagnosticos_campanha_id_idx`, `diagnosticos_criado_em_idx`, `diagnosticos_status_idx`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tabela `diagnosticos` no schema Drizzle</name>
  <files>src/db/schema.ts</files>
  <read_first>
    - `src/db/schema.ts` linhas 76-133 (tabela `campanhas`, doc-comment + colunas + índices) e linhas 308-311 (`configuracoes.sequenciaIntervalosDias`, idioma de coluna JSON)
    - `src/lib/ai/diagnostico-schema.ts` (plano 23-01) — o tipo `Diagnostico`
    - `.planning/phases/23-diagn-stico-de-ia-da-campanha/23-PATTERNS.md` seção "src/db/schema.ts" (linhas 197-241)
  </read_first>
  <action>
Acrescentar a tabela `diagnosticos` em `src/db/schema.ts`, declarada IMEDIATAMENTE APÓS a tabela
`campanhas` (que termina na linha 133) e ANTES de `leads`.

Import de tipo no topo do arquivo: `import type { Diagnostico } from "@/lib/ai/diagnostico-schema";`
— obrigatoriamente `import type` (não import de valor), para não arrastar efeito colateral para
`schema.ts`, que é importado por todo o app.

Colunas, com estes nomes físicos exatos (o `verify-schema.cjs` da Task 3 usa o conjunto estrito):
- `id`: `integer("id").primaryKey({ autoIncrement: true })`
- `campanhaId`: `integer("campanha_id").notNull().references(() => campanhas.id, { onDelete: "restrict" })`
- `payload`: `text("payload", { mode: "json" }).$type<Diagnostico>()` — NULLABLE (é NULL quando `status` é `"falhou"`)
- `fontes`: `text("fontes", { mode: "json" }).$type<{ url: string; title?: string }[]>()` — nullable
- `buscas`: `text("buscas", { mode: "json" }).$type<string[]>()` — nullable
- `status`: `text("status", { enum: ["ok", "falhou"] }).notNull()`
- `erro`: `text("erro")` — nullable
- `aviso`: `text("aviso")` — nullable
- `inputTokens`: `integer("input_tokens")` — nullable
- `outputTokens`: `integer("output_tokens")` — nullable
- `criadoEm`: `integer("criado_em", { mode: "timestamp" }).notNull().default(sql\`(unixepoch())\`)`

DECISÃO TRAVADA D-23-01: `criado_em` é `integer` timestamp com default `(unixepoch())`, NÃO ISO
string. O 23-AI-SPEC §4 diz "ISO string, padrão do projeto" e está ERRADO — todo timestamp do
`schema.ts` usa `integer({ mode: "timestamp" })` (ver `campanhas.createdAt` linha 125,
`tarefas` linha 260), e o `format()` do date-fns na UI espera `Date`/número.

DECISÃO TRAVADA D-23-07: `erro` e `aviso` são colunas SEPARADAS, com semânticas que nunca se
misturam. `erro` só é preenchida quando `status` é `"falhou"` — é a mensagem da falha fatal, e nesse
caso `payload` é NULL. `aviso` só é preenchida quando `status` é `"ok"` — guarda um aviso NÃO-FATAL
de uma geração que passou com ressalva (hoje, o `avisoCrossCheck` de `gerarDiagnostico()`: contagem
de URLs citadas dentro do objeto que não constam em `res.sources`). Reusar `erro` para os dois
tornaria impossível distinguir por SQL "falhou" de "passou com ressalva", e faria o bloco de erro da
UI (plano 23-07) disparar em cima de uma geração válida.

Índices no segundo argumento do `sqliteTable` (array), idioma das linhas 128-132:
`index("diagnosticos_campanha_id_idx").on(table.campanhaId)`,
`index("diagnosticos_criado_em_idx").on(table.criadoEm)`,
`index("diagnosticos_status_idx").on(table.status)`.

Doc-comment em PT-BR no estilo dos vizinhos (linhas 76-107), registrando: tabela append-only, 1
linha por geração (DIAGNOSTICO-10 — sem cache, sem checagem "já existe"), SEM `deletedAt` (nunca
há `DELETE FROM diagnosticos`, por isso NÃO entra na ALLOWLIST de `guard-no-hard-delete.cjs`),
FK `onDelete: "restrict"` porque campanha nunca é hard-deletada (mesmo raciocínio de
`interacoes.leadId`), `payload` NULL quando `status` é `"falhou"` (falha é evento visível, nunca
um resultado falso), a separação `erro` × `aviso` de D-23-07 escrita explicitamente coluna a
coluna, e migração SEMPRE via `scripts/migrate-diagnosticos.cjs`, NUNCA `drizzle-kit push`/`generate`.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` sai com código 0
    - `grep -c "export const diagnosticos" src/db/schema.ts` retorna 1
    - `grep "import type { Diagnostico }" src/db/schema.ts` retorna 1 linha
    - `grep -c "deletedAt" ` no bloco da tabela `diagnosticos` retorna 0 (a tabela não tem soft-delete)
    - O bloco da tabela contém as duas colunas separadas `text("erro")` e `text("aviso")`
    - O doc-comment explica a semântica distinta de `erro` (falha fatal) e `aviso` (ressalva não-fatal), citando D-23-07
    - O bloco contém `mode: "timestamp"` e `unixepoch()` para `criado_em`, e NÃO contém a string `toISOString`
    - `npm run guard:no-hard-delete` sai com código 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run guard:no-hard-delete</automated>
  </verify>
  <done>`diagnosticos` declarada em `src/db/schema.ts` com 11 colunas, 3 índices, FK restrict e doc-comment registrando D-23-01 e D-23-07; tsc limpo.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Migração `.cjs` idempotente rodada contra `data/crm.db`</name>
  <files>scripts/migrate-diagnosticos.cjs</files>
  <read_first>
    - `scripts/migrate-campanhas.cjs` (arquivo inteiro, 175 linhas) — molde BLOCO A BLOCO
    - `scripts/migrate-tarefas.cjs` — variante "tabela nova sem ALTER em `leads`"
    - `src/db/schema.ts` (bloco `diagnosticos` da Task 1) — colunas que a DDL precisa espelhar
    - `src/db/client.ts` — confirma `journal_mode=WAL` (motivo do `wal_checkpoint` antes do backup)
    - `.planning/phases/23-diagn-stico-de-ia-da-campanha/23-PATTERNS.md` seção "scripts/migrate-diagnosticos.cjs" (linhas 135-193)
  </read_first>
  <action>
Criar `scripts/migrate-diagnosticos.cjs` copiando bloco a bloco `scripts/migrate-campanhas.cjs`,
trocando só o prefixo de log para `[migrate-diagnosticos]` e a DDL.

PROIBIDO usar `drizzle-kit push` ou `drizzle-kit generate` em qualquer momento — dois incidentes
destrutivos documentados (Fases 06-01 e 07-01) e o snapshot do drizzle-kit está divergente do
banco real desde a Fase 4.

Blocos obrigatórios, na ordem do molde:
1. `"use strict"`, requires de `node:path` / `node:fs` / `better-sqlite3`,
   `DB_PATH = process.env.DB_FILE_NAME ?? path.join(__dirname, "..", "data", "crm.db")`,
   helper `fail(message)` que imprime `[migrate-diagnosticos] FALHOU: ...` e chama `process.exit(1)`.
2. BACKUP antes de qualquer escrita: abrir o DB com `{ fileMustExist: true }`, rodar
   `pragma("wal_checkpoint(TRUNCATE)")`, fechar, e `fs.copyFileSync` para
   `${DB_PATH}.backup-<ISO com : e . trocados por ->`.
3. Contagem-testemunha de não-regressão ANTES da DDL: `SELECT count(*) AS c FROM campanhas` e
   `SELECT count(*) AS c FROM leads`, guardadas em variáveis.
4. CREATE TABLE idempotente, guardado por
   `SELECT name FROM sqlite_master WHERE type='table' AND name='diagnosticos'`. A DDL cria as 11
   colunas na ordem: `id INTEGER PRIMARY KEY AUTOINCREMENT`,
   `campanha_id INTEGER NOT NULL REFERENCES campanhas(id)`, `payload TEXT`, `fontes TEXT`,
   `buscas TEXT`, `status TEXT NOT NULL`, `erro TEXT`, `aviso TEXT`, `input_tokens INTEGER`,
   `output_tokens INTEGER`, `criado_em INTEGER NOT NULL DEFAULT (unixepoch())`.
   Dentro da mesma guarda, criar os 3 índices: `diagnosticos_campanha_id_idx` em `(campanha_id)`,
   `diagnosticos_criado_em_idx` em `(criado_em)`, `diagnosticos_status_idx` em `(status)`.
5. VERIFICAÇÃO PÓS-MIGRAÇÃO (molde `migrate-campanhas.cjs` linhas 103-171):
   - as duas contagens-testemunha continuam idênticas às de antes; senão `fail()`
   - `PRAGMA table_info(diagnosticos)` bate com o conjunto ESTRITO das 11 colunas — nenhuma
     faltando E nenhuma extra; senão `fail()`
   - os 3 índices existem em `sqlite_master`; senão `fail()`
   - `PRAGMA foreign_key_list(diagnosticos)` contém uma FK de `campanha_id` para `campanhas`;
     senão `fail()`
   - `db.close()` e `process.exit(0)` com log de sucesso

Depois de escrever o arquivo, EXECUTAR a migração DE VERDADE contra `data/crm.db` duas vezes
seguidas com `npm run migrate:diagnosticos` (o script npm já existe desde o plano 23-01). A
segunda execução prova a idempotência: precisa sair com código 0 e não recriar nada. Registrar no
SUMMARY as contagens-testemunha de `campanhas` e `leads` antes e depois — precisam ser idênticas.
  </action>
  <acceptance_criteria>
    - `npm run migrate:diagnosticos` sai com código 0 na 1ª execução
    - `npm run migrate:diagnosticos` sai com código 0 na 2ª execução consecutiva (idempotência)
    - `node -e "const D=require('better-sqlite3');const db=new D('data/crm.db');const c=db.prepare(\"SELECT name FROM sqlite_master WHERE type='table' AND name='diagnosticos'\").get();if(!c)process.exit(1)"` sai com código 0
    - `node -e "const D=require('better-sqlite3');const db=new D('data/crm.db');const cols=db.prepare('PRAGMA table_info(diagnosticos)').all().map(c=>c.name).sort().join(',');const exp=['aviso','buscas','campanha_id','criado_em','erro','fontes','id','input_tokens','output_tokens','payload','status'].join(',');if(cols!==exp){console.error(cols);process.exit(1)}"` sai com código 0
    - A contagem de linhas de `campanhas` e de `leads` é idêntica antes e depois das duas execuções
    - Existe pelo menos um arquivo `data/crm.db.backup-*` criado pela execução
    - `grep -v '^\s*//' scripts/migrate-diagnosticos.cjs | grep -c "drizzle-kit"` retorna 0
  </acceptance_criteria>
  <verify>
    <automated>npm run migrate:diagnosticos && npm run migrate:diagnosticos</automated>
  </verify>
  <done>Tabela `diagnosticos` criada em `data/crm.db` com 11 colunas, 3 índices e FK; migração idempotente provada por 2 execuções; nenhuma linha de `campanhas`/`leads` alterada.</done>
</task>

<task type="auto">
  <name>Task 3: Gate estrito de `diagnosticos` em `verify-schema.cjs`, com mutação provada</name>
  <files>scripts/verify-schema.cjs</files>
  <read_first>
    - `scripts/verify-schema.cjs` (arquivo inteiro; em especial linhas 29-40 de `requiredTables`/`requiredIndexes`, linhas 114-148 do bloco `REQUIRED_CAMPANHAS_COLUMNS`, e o `console.log` final em torno das linhas 180-183)
    - `.planning/phases/23-diagn-stico-de-ia-da-campanha/23-PATTERNS.md` seção "scripts/verify-schema.cjs" (linhas 245-274)
  </read_first>
  <action>
Estender `scripts/verify-schema.cjs` com 4 edições, copiando o molde EXATO do bloco de `campanhas`
(linhas 114-148):

1. Em `requiredTables`, acrescentar a string `"diagnosticos"`.
2. Em `requiredIndexes`, acrescentar `"diagnosticos_campanha_id_idx"`,
   `"diagnosticos_criado_em_idx"` e `"diagnosticos_status_idx"`.
3. Novo bloco `REQUIRED_DIAGNOSTICOS_COLUMNS` com o conjunto ESTRITO das 11 colunas, na ordem
   física: `id`, `campanha_id`, `payload`, `fontes`, `buscas`, `status`, `erro`, `aviso`,
   `input_tokens`, `output_tokens`, `criado_em`. Guardado por
   `if (tableNames.has("diagnosticos"))`, lendo `PRAGMA table_info(diagnosticos)`, calculando
   `missingColumns` e `extraColumns` e chamando `fail()` se qualquer um dos dois for não-vazio.
   Conjunto estrito (missing E extra) é seguro aqui — tabela nova não acumula colunas por fase,
   mesmo precedente da decisão 12-01.
4. Atualizar a mensagem do `console.log` final para incluir `diagnosticos`.

MUTAÇÃO PROVADA obrigatória (precedente 12-01): provar que o gate realmente pega a ausência.
Fazer isso SEM tocar `data/crm.db` de produção — copiar `data/crm.db` para um arquivo temporário,
rodar `DROP TABLE diagnosticos` nessa cópia, e executar
`DB_FILE_NAME=<copia> node scripts/verify-schema.cjs` confirmando exit code 1 e a mensagem de
falha. Depois apagar a cópia. Registrar a saída da mutação no SUMMARY.
  </action>
  <acceptance_criteria>
    - `npm run verify:schema` sai com código 0 contra `data/crm.db`
    - A saída de `npm run verify:schema` menciona `diagnosticos`
    - `grep -c "REQUIRED_DIAGNOSTICOS_COLUMNS" scripts/verify-schema.cjs` retorna valor >= 2
    - `grep -c "diagnosticos_campanha_id_idx" scripts/verify-schema.cjs` retorna 1
    - O bloco `REQUIRED_DIAGNOSTICOS_COLUMNS` lista 11 nomes, incluindo `erro` E `aviso`
    - Mutação: com uma CÓPIA de `data/crm.db` onde `diagnosticos` foi dropada, `DB_FILE_NAME=<copia> node scripts/verify-schema.cjs` sai com código 1
    - A cópia temporária foi removida ao fim; `git status` não mostra arquivo `.db` novo
  </acceptance_criteria>
  <verify>
    <automated>npm run verify:schema</automated>
  </verify>
  <done>`verify:schema` cobre `diagnosticos` com conjunto estrito de 11 colunas e 3 índices; mutação (tabela dropada numa cópia) faz o gate falhar com exit 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script de migração → `data/crm.db` | O banco contém os dados reais do usuário (44+ leads, campanhas). Escrita destrutiva acidental é o risco central. |
| LLM → banco | O `payload` gravado vem do modelo. Já validado por Zod na Server Action, mas a coluna aceita JSON arbitrário fisicamente. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-10 | Denial of Service / Tampering | `scripts/migrate-diagnosticos.cjs` | mitigate | Backup com `wal_checkpoint(TRUNCATE)` + `copyFileSync` ANTES de qualquer escrita; contagem-testemunha de `campanhas` e `leads` antes/depois com `fail()` na divergência; `CREATE TABLE` guardado por `sqlite_master` (idempotente). Proibido `drizzle-kit` (2 incidentes destrutivos documentados). |
| T-23-11 | Tampering | Drift schema Drizzle × banco físico | mitigate | `verify-schema.cjs` com conjunto ESTRITO (missing E extra) das 11 colunas + os 3 índices + mutação provada por DROP numa cópia do banco. |
| T-23-07 | Tampering | coluna `diagnosticos.payload` | mitigate | `payload` é NULL quando `status` é `"falhou"` — nunca um resultado parcial gravado como válido. A re-validação com `diagnosticoSchema` na leitura (plano 23-07) é a segunda barreira (fronteira de confiança do DB). |
| T-23-12 | Repudiation | histórico de gerações | mitigate | Tabela append-only, sem `deletedAt`, fora da ALLOWLIST de `guard-no-hard-delete.cjs` — nenhuma geração (nem falha) pode ser apagada, o que sustenta DIAGNOSTICO-10 (custo sempre visível). |
| T-23-16 | Repudiation | ambiguidade `erro` × `aviso` | mitigate | D-23-07: colunas separadas. `erro` só com `status` `"falhou"`; `aviso` só com `status` `"ok"`. Sem isso, uma geração válida com ressalva seria indistinguível de uma falha por SQL e cairia no bloco de erro da UI. |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo
- `npm run lint` sai com código 0
- `npm run verify:schema` verde e mencionando `diagnosticos`
- `npm run guard:no-hard-delete` verde
- `npm run migrate:diagnosticos` executado 2x com exit 0
- Contagens-testemunha de `campanhas` e `leads` idênticas antes/depois (registradas no SUMMARY)
</verification>

<success_criteria>
- A tabela `diagnosticos` existe fisicamente em `data/crm.db`, não só em `schema.ts`
- Nenhum dado pré-existente foi alterado
- O gate de schema falha quando a tabela some (mutação provada)
- `erro` e `aviso` têm semânticas separadas e documentadas (D-23-07)
</success_criteria>

<output>
Create `.planning/phases/23-diagn-stico-de-ia-da-campanha/23-02-SUMMARY.md` when done
</output>
