---
phase: 23-diagn-stico-de-ia-da-campanha
plan: 05
type: execute
wave: 4
depends_on: ["23-04"]
files_modified:
  - src/components/achado-tipo-badge.tsx
  - src/components/veredito-sugerido-chip.tsx
  - src/app/campanhas/[id]/_components/rascunho-mensagem.tsx
  - src/components/diagnostico-resultado.tsx
autonomous: true
requirements: [DIAGNOSTICO-03, DIAGNOSTICO-04, DIAGNOSTICO-05, DIAGNOSTICO-06, DIAGNOSTICO-07, DIAGNOSTICO-08, DIAGNOSTICO-09]

must_haves:
  truths:
    - "Um achado de dado quantificável e uma alegação de marketing nunca aparecem com o mesmo peso visual"
    - "O gatilho de dor mais forte é o único elemento com borda de acento na seção"
    - "O rascunho de 1ª mensagem é editável, copiável e não tem nenhum caminho de envio"
    - "Nenhuma URL vinda do modelo vira href sem passar pela allowlist de esquema"
    - "O veredito da IA aparece sempre rotulado como sugestão não vinculante"
  artifacts:
    - path: "src/components/achado-tipo-badge.tsx"
      provides: "Badge de tipo de achado com 3 eixos de distinção (cor, ícone, rótulo)"
      contains: "alegacao_marketing"
    - path: "src/components/veredito-sugerido-chip.tsx"
      provides: "Chip do veredito sugerido com token de status por decisão"
      contains: "mudar_angulo"
    - path: "src/app/campanhas/[id]/_components/rascunho-mensagem.tsx"
      provides: "Textarea editável de valor vivo + copiar para a área de transferência"
      contains: "use client"
    - path: "src/components/diagnostico-resultado.tsx"
      provides: "Render completo do objeto de diagnóstico validado"
      contains: "urlSegura"
      min_lines: 100
  key_links:
    - from: "src/components/diagnostico-resultado.tsx"
      to: "src/lib/ai/diagnostico-schema.ts"
      via: "urlSegura(url) antes de qualquer href"
      pattern: "urlSegura"
    - from: "src/components/diagnostico-resultado.tsx"
      to: "src/components/achado-tipo-badge.tsx"
      via: "render de cada item de achados"
      pattern: "AchadoTipoBadge"
---

<objective>
Construir os componentes de apresentação do diagnóstico: os dois badges semânticos, o rascunho
editável de 1ª mensagem, e o componente que renderiza o objeto validado inteiro.

Purpose: DIAGNOSTICO-07 (dado x alegação nunca misturados sem distinção) é um dos 5 modos de falha
críticos da fase e é resolvido AQUI, no nível visual. Estes componentes são puros em relação ao
banco — recebem o objeto já validado como prop — o que permite construí-los e revisá-los sem
depender da query nem do botão.
Output: 4 componentes novos, todos no idioma de badge/card já em produção no projeto.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-diagn-stico-de-ia-da-campanha/23-UI-SPEC.md
@.planning/phases/23-diagn-stico-de-ia-da-campanha/23-PATTERNS.md
@src/components/campanha-estado-badge.tsx
@src/components/whatsapp-preview-dialog.tsx
@src/app/campanhas/[id]/page.tsx
@src/lib/ai/diagnostico-schema.ts

<interfaces>
<!-- Contratos consumidos pelo plano 23-07 -->

- `export function AchadoTipoBadge({ tipo }: { tipo: Diagnostico["achados"][number]["tipo"] })` — Server Component
- `export function VereditoSugeridoChip({ decisao }: { decisao: Diagnostico["veredito_sugerido"]["decisao"] })` — Server Component
- `export function RascunhoMensagem({ rascunho }: { rascunho: string })` — Client Component
- `export function DiagnosticoResultado(props: { diagnostico: Diagnostico; fontes: { url: string; title?: string }[]; criadoEm: Date; inputTokens: number | null; outputTokens: number | null; buscas: string[] | null })` — Server Component
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Badges semânticos — tipo de achado e veredito sugerido</name>
  <files>src/components/achado-tipo-badge.tsx, src/components/veredito-sugerido-chip.tsx</files>
  <read_first>
    - `src/components/campanha-estado-badge.tsx` (arquivo inteiro, 41 linhas) — molde EXATO do idioma `Record<K, string>` para LABEL e TOKEN separados, e do `<Badge variant="outline" className={cn("border-transparent shrink-0", TOKEN[k])}>`
    - `src/components/etapa-badge.tsx` — segundo exemplo do mesmo idioma
    - `.planning/phases/23-diagn-stico-de-ia-da-campanha/23-UI-SPEC.md` §Color, tabelas das linhas 121-132 — os tokens e rótulos exatos
    - `src/lib/ai/diagnostico-schema.ts` — o tipo `Diagnostico` de onde derivar as chaves
  </read_first>
  <action>
Criar `src/components/achado-tipo-badge.tsx` copiando o idioma de `campanha-estado-badge.tsx`.
Server Component (sem `"use client"`).

Derivar o tipo da chave do próprio schema:
`type AchadoTipo = Diagnostico["achados"][number]["tipo"]` — mesmo idioma de
`export type CampanhaEstado = Campanha["estado"]` na linha 5 do analog. Import de tipo a partir de
`@/lib/ai/diagnostico-schema`.

TRÊS mapas `Record<AchadoTipo, ...>` separados (não acoplar rótulo, cor e ícone num objeto só —
mesma razão do analog):
- `TIPO_LABEL`: `dado_quantificavel` para `"Dado quantificável"`, `alegacao_marketing` para
  `"Alegação de concorrente"` (cópia VERBATIM do 23-UI-SPEC linha 123-124)
- `TIPO_TOKEN`: `dado_quantificavel` para `"bg-status-info text-status-info-foreground"`,
  `alegacao_marketing` para `"bg-status-warning text-status-warning-foreground"`
- `TIPO_ICON`: `dado_quantificavel` para o ícone `Hash` do `lucide-react`, `alegacao_marketing`
  para `Megaphone`

Renderizar `<Badge variant="outline" className={cn("border-transparent shrink-0", TIPO_TOKEN[tipo])}>`
com o ícone antes do rótulo, separados por `gap-1` (4px — grade de espaçamento do 23-UI-SPEC).
Ícone com `aria-hidden` e tamanho compatível com a altura `h-5` do primitivo `Badge`.

Criar `src/components/veredito-sugerido-chip.tsx` no mesmo molde. Server Component.
`type VereditoDecisao = Diagnostico["veredito_sugerido"]["decisao"]`.
- `VEREDITO_LABEL`: `aprofundar` para `"Aprofundar"`, `mudar_angulo` para `"Mudar o ângulo"`,
  `abandonar` para `"Abandonar"`
- `VEREDITO_TOKEN`: `aprofundar` para `"bg-status-success text-status-success-foreground"`,
  `mudar_angulo` para `"bg-status-warning text-status-warning-foreground"`, `abandonar` para
  `"bg-status-danger text-status-danger-foreground"`
O chip em si é só o `<Badge>` — o rótulo "Sugestão da IA (não vinculante)" que o envolve é
responsabilidade do `DiagnosticoResultado` (Task 3), não deste componente.

REGRA DURA DE COR (gate `verify:brand`): somente tokens `bg-status-*` / `text-status-*-foreground`.
Nenhum hex, nenhuma classe de cor literal do Tailwind (`bg-amber-500` e afins), nenhum token de
marca (`bg-primary`) nestes dois arquivos.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` sai com código 0
    - `npm run verify:brand` sai com código 0
    - `npm run check:contrast` sai com código 0
    - `grep -cE "#[0-9a-fA-F]{3,8}" src/components/achado-tipo-badge.tsx src/components/veredito-sugerido-chip.tsx` retorna 0 para os dois arquivos
    - `grep -c "use client" src/components/achado-tipo-badge.tsx` retorna 0 e o mesmo para `veredito-sugerido-chip.tsx` (são Server Components)
    - `achado-tipo-badge.tsx` contém as strings `Hash`, `Megaphone`, `bg-status-info`, `bg-status-warning` e `Alegação de concorrente`
    - `veredito-sugerido-chip.tsx` contém `bg-status-success`, `bg-status-warning`, `bg-status-danger` e `Mudar o ângulo`
    - `npm run lint` sai com código 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run verify:brand && npm run check:contrast</automated>
  </verify>
  <done>Dois badges Server criados no idioma LABEL/TOKEN/ICON, só com tokens `--status-*`, cobrindo os 2 tipos de achado e as 3 decisões de veredito.</done>
</task>

<task type="auto">
  <name>Task 2: Rascunho de 1ª mensagem — editável, copiável, sem caminho de envio</name>
  <files>src/app/campanhas/[id]/_components/rascunho-mensagem.tsx</files>
  <read_first>
    - `src/components/whatsapp-preview-dialog.tsx` linhas 70-71, 118-124 e 162-167 — o idioma de textarea com valor VIVO no state, e a regra de sempre ler o state, nunca o valor original memoizado
    - `.planning/phases/23-diagn-stico-de-ia-da-campanha/23-UI-SPEC.md` §Estados regra 7 (linha 232) e §Copywriting linhas 167-169 — cópia VERBATIM
    - `src/components/ui/textarea.tsx` e `src/components/ui/button.tsx` — primitivos já no repo
  </read_first>
  <action>
Criar `src/app/campanhas/[id]/_components/rascunho-mensagem.tsx` com `"use client"` na primeira
linha. Props: `{ rascunho: string }`.

Estado local `texto` inicializado com a prop `rascunho`, e `<Textarea value={texto}
onChange={...} className="min-h-32">` — textarea CONTROLADO, mesmo idioma do analog
`whatsapp-preview-dialog.tsx`. As edições vivem só no state: não há Server Action de salvar, não
há persistência (DIAGNOSTICO-08 — "as edições não são salvas").

Estado local `copiado` (boolean). Botão `variant="outline"` cujo handler faz
`await navigator.clipboard.writeText(texto)` — lendo o state VIVO, nunca a prop original — seta
`copiado` para true e agenda um `setTimeout` de 2000ms devolvendo para false. Rótulo do botão:
`"Copiar mensagem"` normalmente e `"Copiado"` enquanto `copiado` for true (cópia VERBATIM do
23-UI-SPEC linha 169). Limpar o timeout no cleanup para não setar state em componente desmontado.

Nota fixa abaixo do textarea, em `text-xs text-muted-foreground`, com o texto VERBATIM do
23-UI-SPEC linha 168: "Rascunho editável. Nunca enviado pelo sistema — copie e use à mão. As
edições não são salvas."

PROIBIDO neste arquivo: qualquer botão "Salvar", qualquer chamada de Server Action, qualquer link
`wa.me` ou `mailto:`, qualquer import de `@/actions/*` ou `@/lib/ai/gerar-diagnostico`. O rascunho
NUNCA é enviado pelo sistema (DIAGNOSTICO-08) — é isso que este componente prova.

Se o React Compiler acusar falso-positivo de `react-hooks` (precedente documentado no STATE.md,
decisão 07-02), usar `eslint-disable-next-line` com comentário explicando, no mesmo padrão já
aceito em `whatsapp-preview-dialog.tsx`.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` sai com código 0
    - `npm run lint` sai com código 0
    - A primeira linha do arquivo é `"use client";`
    - `grep -cE "wa\.me|mailto:|@/actions/|gerar-diagnostico" src/app/campanhas/\[id\]/_components/rascunho-mensagem.tsx` retorna 0
    - `grep -c "Salvar" src/app/campanhas/\[id\]/_components/rascunho-mensagem.tsx` retorna 0
    - O arquivo contém a string literal `As edições não são salvas.`
    - O arquivo contém `navigator.clipboard.writeText` e o handler passa a variável de state, não a prop
    - `npm run verify:brand` sai com código 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run verify:brand</automated>
    <human-check>Abrir uma campanha com diagnóstico gerado, editar o texto do rascunho, clicar "Copiar mensagem", colar em outro lugar e confirmar que veio o texto EDITADO (não o original). Confirmar que o botão vira "Copiado" e volta em ~2s, e que não existe nenhum botão de salvar ou enviar.</human-check>
  </verify>
  <done>`RascunhoMensagem` renderiza textarea controlado com valor vivo, botão de copiar que lê o state, nota fixa de "nunca enviado" e zero caminhos de envio ou persistência.</done>
</task>

<task type="auto">
  <name>Task 3: `DiagnosticoResultado` — render do objeto validado com fontes seguras</name>
  <files>src/components/diagnostico-resultado.tsx</files>
  <read_first>
    - `.planning/phases/23-diagn-stico-de-ia-da-campanha/23-UI-SPEC.md` §Layout e Estrutura da Seção (linhas 181-218), §Typography (linhas 80-95), §Copywriting (linhas 138-177) — TODA a cópia é VERBATIM daqui
    - `src/app/campanhas/[id]/page.tsx` linhas 52-64 — idioma do bloco `rounded-lg border bg-card p-4` com `<dl>` e pares `dt`/`dd` em `gap-0.5`
    - `src/components/whatsapp-preview-dialog.tsx` linhas 196-201 — idioma de link externo com `target="_blank" rel="noopener noreferrer"`
    - `src/lib/ai/diagnostico-schema.ts` — o tipo `Diagnostico` e o helper `urlSegura`
    - `src/components/achado-tipo-badge.tsx` e `src/components/veredito-sugerido-chip.tsx` (Task 1)
    - `src/app/campanhas/[id]/_components/rascunho-mensagem.tsx` (Task 2)
  </read_first>
  <action>
Criar `src/components/diagnostico-resultado.tsx` — Server Component (sem `"use client"`), com as
props do bloco `<interfaces>`. Renderiza o objeto JÁ validado; não faz query, não valida.

Estrutura, na ordem do 23-UI-SPEC §Layout (container `flex flex-col gap-4`):

1. Linha de metadados, `text-xs text-muted-foreground font-mono`, no formato exato
   `gerado em {dd/MM/yyyy HH:mm} · {n} fontes · {inputTokens}+{outputTokens} tokens · {n} buscas`
   — usar `format` do `date-fns` sobre `criadoEm`. Quando `inputTokens`/`outputTokens`/`buscas`
   forem null, exibir `—` no lugar do número, nunca omitir o segmento (DIAGNOSTICO-10: o custo é
   sempre visível).
2. Bloco Saturação, `rounded-lg border bg-card p-4`: o número `indice_saturacao.concorrentes_diretos`
   em `text-xl font-mono tabular-nums font-semibold`, com o rótulo "Concorrentes diretos
   encontrados" e a `leitura` em `text-sm` ao lado/abaixo. Este é o ÚNICO número-herói da seção —
   nenhum outro bloco pode competir em peso visual. O número NÃO recebe cor semântica (é
   `text-foreground` neutro): contagem não é veredito.
3. Bloco "Gatilhos de dor" (`text-base font-semibold` no título, cards em `gap-3`): 1 a 3 cards.
   O card cujo `mais_forte` é true recebe `border-primary` e um `<Badge>` com o rótulo
   "Mais forte" usando `bg-status-neutral text-status-neutral-foreground`. Esta é a ÚNICA borda de
   acento da seção inteira. Cada card mostra o texto do gatilho e um link para `observavel_em`.
4. Bloco "Objeções esperadas": 2 a 3 pares — a `objecao` em `font-semibold` e, abaixo, o prefixo
   literal "Resposta sugerida:" seguido de `resposta_sugerida` em `text-sm`.
5. Bloco "Ticket médio estimado", `rounded-lg border bg-card p-4`: `valor_estimado_brl` formatado
   como moeda BRL em `font-mono tabular-nums`, o prefixo literal "Base:" seguido de `base`, e o
   link para `fonte_url`.
6. Bloco "Achados", `flex flex-col gap-2`: cada linha é `<AchadoTipoBadge tipo={...} />` seguido da
   `afirmacao` e do link para `fonte_url`. DISTINÇÃO OBRIGATÓRIA EM 3 EIXOS (DIAGNOSTICO-07): além
   da cor e do ícone que vêm do badge, o TEXTO da afirmação de um `alegacao_marketing` renderiza em
   `italic text-muted-foreground`, enquanto o de um `dado_quantificavel` renderiza em
   `text-sm text-foreground` (peso e cor plenos). Nunca o mesmo tratamento para os dois.
7. Bloco "Rascunho de 1ª mensagem": renderizar `<RascunhoMensagem rascunho={diagnostico.rascunho_primeira_mensagem} />`.
8. Bloco Veredito: o rótulo literal "Sugestão da IA (não vinculante)" acima do
   `<VereditoSugeridoChip decisao={...} />`, a `justificativa` em `text-sm`, e a caption literal
   "É insumo para a sua decisão. O veredito final é registrado por você." em
   `text-xs text-muted-foreground`.
9. Bloco "Fontes", `flex flex-col gap-2`: lista de todas as entradas da prop `fontes` (que vem de
   `res.sources`, não das URLs escritas dentro do objeto). Cada item exibe o DOMÍNIO da URL mais
   o ícone `ExternalLink`.

REGRA DURA DE SEGURANÇA (mitigação T-23-02): TODA URL renderizada como `href` — nos gatilhos, nos
achados, no ticket, na saturação e no bloco Fontes — passa antes por `urlSegura(url)` importado de
`@/lib/ai/diagnostico-schema`. Se `urlSegura` retornar false, renderizar o texto da URL SEM
`<a href>` (texto inerte em `text-muted-foreground`). `z.string().url()` do Zod aceita
`javascript:` e não basta. Todo `<a>` leva `target="_blank" rel="noopener noreferrer"`.

REGRA DURA DE COR: apenas tokens (`bg-card`, `text-muted-foreground`, `text-primary`,
`border-primary`, `bg-status-*`). Nenhum hex. O acento `--primary` é reservado a exatamente 2 usos
aqui: os links de fonte (`text-primary underline-offset-4 hover:underline`) e a `border-primary`
do gatilho mais forte. Transições apenas `transition-colors`, nunca `transition: all`.

Toda a cópia visível em PT-BR, VERBATIM do 23-UI-SPEC §Copywriting. Sem "!", sem emoji, sem hype.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` sai com código 0
    - `npm run lint` sai com código 0
    - `npm run verify:brand` sai com código 0
    - `npm run check:contrast` sai com código 0
    - `grep -cE "#[0-9a-fA-F]{3,8}" src/components/diagnostico-resultado.tsx` retorna 0
    - `grep -c "urlSegura" src/components/diagnostico-resultado.tsx` retorna valor >= 1
    - Toda ocorrência de `href=` no arquivo está dentro de um ramo guardado por `urlSegura` — verificar lendo cada ocorrência
    - `grep -c 'rel="noopener noreferrer"' src/components/diagnostico-resultado.tsx` retorna valor >= 1 e é igual ao número de ocorrências de `target="_blank"`
    - `grep -c "italic" src/components/diagnostico-resultado.tsx` retorna valor >= 1 (o tratamento distinto de `alegacao_marketing`)
    - `grep -c "border-primary" src/components/diagnostico-resultado.tsx` retorna 1 (única borda de acento da seção)
    - O arquivo contém as strings literais `Concorrentes diretos encontrados`, `Gatilhos de dor`, `Objeções esperadas`, `Resposta sugerida:`, `Ticket médio estimado`, `Base:`, `Achados`, `Rascunho de 1ª mensagem`, `Sugestão da IA (não vinculante)`, `Fontes`, `Mais forte`
    - `grep -c "transition: all\|transition-all" src/components/diagnostico-resultado.tsx` retorna 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run verify:brand && npm run check:contrast</automated>
    <human-check>Com um diagnóstico real gerado, conferir na tela: (1) um achado "Dado quantificável" e um "Alegação de concorrente" são distinguíveis por cor, ícone E tratamento do texto (itálico/muted na alegação); (2) só um gatilho tem borda de acento e o badge "Mais forte"; (3) todo link de fonte abre em nova aba; (4) repetir a conferência com o tema escuro ativado.</human-check>
  </verify>
  <done>`DiagnosticoResultado` renderiza os 9 blocos do 23-UI-SPEC, com distinção de 3 eixos entre dado e alegação, um único número-herói, uma única borda de acento e nenhuma URL não-allowlistada virando href.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| objeto gerado pelo LLM → DOM | Strings e URLs autoradas pelo modelo são renderizadas na página do usuário. |
| área de transferência | O rascunho copiado sai do sistema e é colado pelo usuário em outro app. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02 | Tampering / Elevation | `diagnostico-resultado.tsx` | mitigate | `urlSegura(url)` (allowlist `http:`/`https:`) obrigatória antes de qualquer `href`; URL reprovada vira texto inerte. `z.string().url()` do Zod aceita `javascript:` e NÃO é suficiente. Todo `<a>` com `rel="noopener noreferrer"`. |
| T-23-14 | Information Disclosure | texto do diagnóstico no DOM | accept | O JSX do React auto-escapa todo texto interpolado; não há `dangerouslySetInnerHTML` em nenhum componente desta fase. Conteúdo do diagnóstico é público (citações de páginas abertas), sem PII de terceiros. |
| T-23-03 | Tampering | conteúdo de concorrente exibido como achado | mitigate | Copy de concorrente é obrigatoriamente rotulada `alegacao_marketing` com cor, ícone E itálico/muted distintos, para nunca ser lida como dado de mercado (DIAGNOSTICO-07, modo de falha crítico 4). |
| T-23-15 | Tampering | rascunho de 1ª mensagem | mitigate | Nenhum caminho de envio no componente: proibidos `wa.me`, `mailto:`, import de `@/actions/*` e botão "Salvar" — verificado por grep. O envio é sempre manual, fora do sistema (DIAGNOSTICO-08). |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo
- `npm run lint` sai com código 0
- `npm run verify:brand` e `npm run check:contrast` verdes
- `npm run build` sai com código 0
</verification>

<success_criteria>
- Um leitor distingue instantaneamente dado quantificável de alegação de concorrente
- Nenhuma URL do modelo pode virar href com esquema perigoso
- O rascunho é editável e copiável e não tem nenhuma superfície de envio
</success_criteria>

<output>
Create `.planning/phases/23-diagn-stico-de-ia-da-campanha/23-05-SUMMARY.md` when done
</output>
