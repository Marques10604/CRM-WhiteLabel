---
phase: 23-diagn-stico-de-ia-da-campanha
plan: 04
type: execute
wave: 3
depends_on: ["23-02", "23-03"]
files_modified:
  - src/actions/diagnostico-actions.ts
  - scripts/spike-modelo-diagnostico.mjs
  - src/lib/ai/gerar-diagnostico.ts
autonomous: false
requirements: [DIAGNOSTICO-01, DIAGNOSTICO-02, DIAGNOSTICO-10]

user_setup:
  - service: anthropic
    why: "Geração do diagnóstico de IA com busca na web (DIAGNOSTICO-01/02)"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Console da Anthropic (console.anthropic.com) -> API keys -> Create key. Colar em .env.local na raiz do projeto (o .gitignore já cobre .env*)"
    dashboard_config:
      - task: "Habilitar a tool 'Web Search' nas configurações da ORGANIZAÇÃO"
        location: "Console da Anthropic -> Settings da organização -> Tools / Web Search. Sem isso a chamada volta HTTP 400."

must_haves:
  truths:
    - "Um clique explícito é a única forma de disparar uma geração — nada roda automaticamente"
    - "Toda geração vira uma linha nova na tabela `diagnosticos`, inclusive quando falha"
    - "Uma falha nunca é renderizada como resultado: grava `status` falhou com a mensagem e devolve erro"
    - "A configuração real do modelo Sonnet 5 (effort, maxOutputTokens, temperature) foi medida numa chamada real, não assumida"
  artifacts:
    - path: "src/actions/diagnostico-actions.ts"
      provides: "Server Action gerarDiagnosticoAction: valida, checa campanha, gera, persiste, revalida"
      contains: "use server"
      min_lines: 70
    - path: "scripts/spike-modelo-diagnostico.mjs"
      provides: "Spike de medição da config do modelo com log completo de usage/finishReason/steps"
      min_lines: 40
  key_links:
    - from: "src/actions/diagnostico-actions.ts"
      to: "src/lib/ai/gerar-diagnostico.ts"
      via: "await gerarDiagnostico(...)"
      pattern: "gerarDiagnostico"
    - from: "src/actions/diagnostico-actions.ts"
      to: "tabela diagnosticos"
      via: "db.insert(diagnosticos).values(...)"
      pattern: "db\\.insert\\(diagnosticos\\)"
    - from: "src/actions/diagnostico-actions.ts"
      to: "/campanhas/[id]"
      via: "revalidatePath no sucesso E na falha"
      pattern: "revalidatePath"
---

<objective>
Ligar o núcleo de IA ao banco: a Server Action que valida a entrada, confirma que a campanha
existe, chama `gerarDiagnostico()`, persiste UMA linha por geração (ok ou falhou) e revalida a
rota. Mais o spike obrigatório que mede a configuração real do Claude Sonnet 5.

Purpose: fecha a Open Question 1 da pesquisa de fase — `effort`, `maxOutputTokens` e `temperature`
com o adaptive thinking sempre ligado do Sonnet 5 são MEDIUM confidence e precisam de uma chamada
real medida antes de a UI depender deles. Também estabelece que falha é evento persistido e
visível, nunca um resultado silencioso.
Output: `src/actions/diagnostico-actions.ts`, `scripts/spike-modelo-diagnostico.mjs`, config do
modelo ajustada e documentada.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-diagn-stico-de-ia-da-campanha/23-RESEARCH.md
@.planning/phases/23-diagn-stico-de-ia-da-campanha/23-PATTERNS.md
@src/actions/campanha-actions.ts
@src/db/schema.ts
@src/lib/ai/gerar-diagnostico.ts

<interfaces>
<!-- Contrato consumido pelo plano 23-07 (botão client) -->

De `src/actions/diagnostico-actions.ts`:

- `export type DiagnosticoActionState = { success: true; diagnosticoId: number } | { success: false; erro: string } | undefined;`
- `export async function gerarDiagnosticoAction(_prev: DiagnosticoActionState, formData: FormData): Promise<DiagnosticoActionState>`
  — lê o campo `campanhaId` do `FormData`.

DECISÃO D-23-02 (fecha a Open Question 3 do 23-RESEARCH.md): o arquivo mora em
`src/actions/diagnostico-actions.ts`, NÃO co-locado em `src/app/campanhas/[id]/actions.ts` como o
23-UI-SPEC sugeriu. Motivo: consistência com os outros 6 arquivos de actions do projeto e com o
padrão dos harnesses `test-*-actions.cjs`.

DECISÃO D-23-03: o shape de retorno é `{ success: false; erro: string }` (erro operacional único),
divergindo conscientemente do `{ errors: Record<string, string[]> }` do projeto — que existe para
erros POR CAMPO, e esta action não tem campos editáveis, só um `campanhaId`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server Action `gerarDiagnosticoAction` — validar, gerar, persistir, revalidar</name>
  <files>src/actions/diagnostico-actions.ts</files>
  <read_first>
    - `src/actions/campanha-actions.ts` (arquivo inteiro, 131 linhas) — molde EXATO: cabeçalho `"use server"`, helper `isForeignKeyViolation` duplicado (linhas 30-37), `nichoExists` (45-48), `safeParse` antes de qualquer query (59-62), backstop de FK no catch do insert (69-79), helper de `revalidatePath` (50-53)
    - `src/db/schema.ts` — a tabela `diagnosticos` (plano 23-02) e a tabela `campanhas`
    - `src/lib/ai/gerar-diagnostico.ts` (plano 23-03) — assinatura e classes de erro
    - `src/app/campanhas/[id]/page.tsx` linhas 31-41 — idioma do `leftJoin` com `nichos` para obter `nichoNome`
    - `.planning/phases/23-diagn-stico-de-ia-da-campanha/23-PATTERNS.md` seção "src/actions/diagnostico-actions.ts" (linhas 38-131)
  </read_first>
  <action>
Criar `src/actions/diagnostico-actions.ts` com `"use server"` na primeira linha.

Duplicar VERBATIM o helper `isForeignKeyViolation` de `campanha-actions.ts` linhas 30-37 (checa
`err.code === "SQLITE_CONSTRAINT_FOREIGNKEY"`). O projeto duplica esse helper em cada arquivo de
actions em vez de importar — precedente aceito, manter.

Definir e exportar `DiagnosticoActionState` conforme o bloco `<interfaces>`.

Definir um schema Zod local de entrada: `z.object({ campanhaId: z.coerce.number().int().positive() })`,
mesmo idioma de coerção de `src/lib/validations.ts` linhas 110-113.

`gerarDiagnosticoAction(_prev, formData)`:
1. `safeParse(Object.fromEntries(formData))` ANTES de qualquer acesso ao banco. Se falhar,
   devolver `{ success: false, erro: "Campanha inválida." }`.
2. SELECT da campanha com `leftJoin` em `nichos` para trazer `nichos.nome`, filtrando por
   `eq(campanhas.id, campanhaId)`. Selecionar `campanhas.oferta`, `campanhas.metaConversao`,
   `campanhas.janelaInicio`, `campanhas.janelaFim` e o `nichoNome`. Se não houver linha OU
   `campanha.deletedAt` estiver preenchido, devolver
   `{ success: false, erro: "Campanha não encontrada." }` — guarda de FK forjada, mesma técnica de
   `nichoExists`.
3. Derivar `janelaDias` a partir de `janelaInicio`/`janelaFim` com `differenceInCalendarDays` do
   `date-fns` (já é dep do projeto).
4. Dentro de um `try`: `await gerarDiagnostico({ nicho: nichoNome ?? "", oferta, janelaDias, meta: metaConversao })`.
   No sucesso, `db.insert(diagnosticos).values({ campanhaId, payload: r.diagnostico, fontes: r.fontes,
   buscas: r.buscas, status: "ok", erro: r.avisoCrossCheck ?? null, inputTokens: r.uso.inputTokens,
   outputTokens: r.uso.outputTokens }).returning({ id: diagnosticos.id })`. Passar os objetos JS
   direto — Drizzle serializa por causa do `mode: "json"`. Depois `revalidatePath` de
   `/campanhas/${campanhaId}` e retornar `{ success: true, diagnosticoId: row.id }`.
5. No `catch`: se `isForeignKeyViolation(err)`, devolver `{ success: false, erro: "Campanha não encontrada." }`.
   Caso contrário, derivar `erro` de `err instanceof Error ? err.message : "Falha desconhecida."`,
   INSERIR a linha `{ campanhaId, status: "falhou", erro }` ANTES de retornar (DIAGNOSTICO-10 —
   falha também é evento visível no histórico; `payload` fica NULL), `revalidatePath` da mesma
   rota, e retornar `{ success: false, erro }`.

NUNCA devolver um resultado parcial ou genérico em caso de erro — DIAGNOSTICO-02 e o modo de falha
crítico 3 do 23-AI-SPEC. A UI mostra o erro, não um diagnóstico.

NUNCA fazer checagem "já existe diagnóstico para esta campanha" antes de chamar a API — sem cache
de nenhum tipo, é requisito explícito (DIAGNOSTICO-10). Cada invocação é uma chamada nova.

A mensagem de erro devolvida ao usuário não pode conter stack trace nem qualquer conteúdo de
`process.env` (mitigação T-23-08).
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` sai com código 0
    - A primeira linha do arquivo é `"use server";`
    - `grep -c "isForeignKeyViolation" src/actions/diagnostico-actions.ts` retorna valor >= 2
    - `grep -c "safeParse" src/actions/diagnostico-actions.ts` retorna valor >= 1
    - `grep -c "db.insert(diagnosticos)" src/actions/diagnostico-actions.ts` retorna 2 (um insert de sucesso e um de falha)
    - `grep -c "revalidatePath" src/actions/diagnostico-actions.ts` retorna valor >= 2
    - O arquivo NÃO contém as strings `cache`, `jaExiste` nem qualquer SELECT em `diagnosticos` anterior à chamada de `gerarDiagnostico` (prova de ausência de cache — DIAGNOSTICO-10)
    - `npm run lint` sai com código 0
    - `npm run guard:no-hard-delete` sai com código 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run guard:no-hard-delete</automated>
  </verify>
  <done>Server Action criada em `src/actions/diagnostico-actions.ts`, com validação Zod primeiro, guarda de campanha, insert de sucesso E de falha, e revalidação em ambos os caminhos.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [SETUP] Chave de API e Web Search habilitada no Console da Anthropic</name>
  <files>.env.local</files>
  <read_first>
    - `.planning/phases/23-diagn-stico-de-ia-da-campanha/23-RESEARCH.md` §Environment Availability (linhas 681-699) — as duas dependências sem fallback
    - `.gitignore` — confirmar que `.env*` está coberto (linha 34)
  </read_first>
  <action>
PARAR e apresentar as duas instruções de setup ao usuário. Não tentar criar `.env.local` nem
adivinhar a chave — nenhuma das duas configurações é automatizável (uma exige uma chave secreta do
usuário, a outra é um toggle no Console da organização). Depois da resposta, confirmar por código
que `.env.local` existe, contém `ANTHROPIC_API_KEY` com valor não vazio, e NÃO está rastreado pelo
git. Registrar a confirmação no SUMMARY. Se `.env.local` não existir, não seguir para a Task 3 —
ela faz uma chamada real e falharia com erro obscuro de autenticação.
  </action>
  <what-built>
Todo o código server-side do diagnóstico está pronto e compila. Nenhuma chamada real ao modelo foi
feita ainda. Estas duas configurações vivem fora do repositório e não podem ser feitas por código.
  </what-built>
  <how-to-verify>
1. Criar o arquivo `.env.local` na RAIZ do projeto (mesmo nível de `package.json`) com uma única
   linha: `ANTHROPIC_API_KEY=sk-ant-...` usando uma chave gerada em
   console.anthropic.com -> API keys -> Create key. O `.gitignore` já cobre `.env*`, então o
   arquivo não será commitado.
2. No Console da Anthropic, entrar nas configurações da ORGANIZAÇÃO (não do projeto) e habilitar a
   tool **Web Search**. Sem isso toda chamada volta HTTP 400, e nenhuma quantidade de código
   corrige.
3. Confirmar que o arquivo existe rodando, no terminal do projeto:
   `node -e "require('node:fs').accessSync('.env.local')"` — deve sair sem erro.
4. Responder `pronto` quando as duas coisas estiverem feitas.

Custo esperado a partir daqui: cerca de US$0,15 a US$0,25 por diagnóstico gerado (Claude Sonnet 5
a US$2/US$10 por 1M de tokens, mais US$10 por 1.000 buscas web, com teto de 6 buscas por geração).
  </how-to-verify>
  <acceptance_criteria>
    - `node -e "require('node:fs').accessSync('.env.local')"` sai com código 0
    - `node -e "const s=require('node:fs').readFileSync('.env.local','utf8');if(!/ANTHROPIC_API_KEY=\S+/.test(s))process.exit(1)"` sai com código 0
    - `git status --porcelain .env.local` não retorna nada (arquivo ignorado, não rastreado)
    - Confirmação humana explícita de que a Web Search foi habilitada nas configurações da organização
  </acceptance_criteria>
  <resume-signal>Digite "pronto" quando `.env.local` existir e a Web Search estiver habilitada no Console</resume-signal>
  <verify>
    <automated>node -e "const fs=require('node:fs');fs.accessSync('.env.local');const s=fs.readFileSync('.env.local','utf8');if(!/ANTHROPIC_API_KEY=\S+/.test(s)){console.error('ANTHROPIC_API_KEY ausente em .env.local');process.exit(1)}console.log('OK .env.local com ANTHROPIC_API_KEY')"</automated>
  </verify>
  <done>`.env.local` existe com `ANTHROPIC_API_KEY` e não está rastreado pelo git; humano confirmou a Web Search habilitada na organização.</done>
</task>

<task type="auto">
  <name>Task 3: Spike de configuração do Claude Sonnet 5 e ajuste medido de `gerar-diagnostico.ts`</name>
  <files>scripts/spike-modelo-diagnostico.mjs, src/lib/ai/gerar-diagnostico.ts</files>
  <read_first>
    - `.planning/phases/23-diagn-stico-de-ia-da-campanha/23-RESEARCH.md` Pitfall 9 (linhas 491-498) e Open Questions 1 (linhas 657-660) — o que exatamente está em dúvida
    - `.planning/phases/23-diagn-stico-de-ia-da-campanha/23-RESEARCH.md` Assumptions Log A2/A3/A5 (linhas 645-648)
    - `src/lib/ai/gerar-diagnostico.ts` (plano 23-03) — a config a medir e ajustar
    - `scripts/ts-alias-loader.mjs` — como o `.mjs` resolve `@/`
  </read_first>
  <action>
Criar `scripts/spike-modelo-diagnostico.mjs`: um script ESM de medição, executado UMA vez à mão
(`node scripts/spike-modelo-diagnostico.mjs`), que faz UMA chamada real ao Claude Sonnet 5 e
imprime tudo que a pesquisa de fase deixou em aberto.

Bootstrap: `register("./ts-alias-loader.mjs", pathToFileURL(...))` no topo, igual aos harnesses
`.cjs` do repo mas em sintaxe ESM. Carregar `.env.local` — o Next carrega sozinho em runtime, mas
um script solto não; ler o arquivo e popular `process.env.ANTHROPIC_API_KEY` manualmente, ou usar
`node --env-file=.env.local`. Documentar no cabeçalho do script qual das duas formas foi usada.

O script chama `gerarDiagnostico` importada de `@/lib/ai/gerar-diagnostico` com um nicho fixo de
teste (usar "costureira sob medida" — é um dos 3 nichos-gold) e mede/imprime:
1. Duração total em segundos (`Date.now()` antes e depois)
2. `inputTokens` e `outputTokens` retornados
3. O `finishReason` — se vier `"length"`, o `maxOutputTokens` está apertado
4. Quantas buscas o array `buscas` capturou, e o CONTEÚDO de cada uma. Se o array vier vazio, o
   nome do campo da query dentro de `step.toolCalls[].input` é diferente de `query` (assumption A5)
   — nesse caso, imprimir o JSON bruto do primeiro `toolCall` para descobrir o nome real
5. Quantas fontes o gate coletou de `res.sources`
6. Se o objeto validou de primeira ou precisou da 2ª tentativa

Para os itens 3, 4 e 6 o script precisa de visibilidade dentro da chamada. Adicionar em
`gerar-diagnostico.ts` um parâmetro opcional de diagnóstico/telemetria (por exemplo um segundo
argumento `opcoes?: { onTelemetria?: (dados: unknown) => void }`) que, quando fornecido, recebe
`finishReason`, `usage`, a contagem de steps e o `toolCalls` bruto do primeiro step. Esse
parâmetro é opcional e não usado pela Server Action — existe para o spike e para o eval do plano
23-06. NÃO logar nada dele por padrão.

Depois de rodar o spike UMA vez, AJUSTAR `src/lib/ai/gerar-diagnostico.ts` a partir do medido:
- Se `finishReason` veio `"length"` ou `outputTokens` passou de 12000, subir `maxOutputTokens`
- Se `outputTokens` ficou bem abaixo do teto e a latência ficou sob 60s, manter `16000`
- Confirmar se `providerOptions: { anthropic: { effort: "low" } }` foi aceito sem erro pelo
  provider `@4.0.49`. Se o provider rejeitar a opção, REMOVER a linha e registrar isso como
  decisão da fase (assumption A2 falhou)
- Confirmar se `temperature: 0.3` foi aceito. Se a API rejeitar por causa do adaptive thinking,
  remover o parâmetro e registrar (assumption A3 falhou)
- Se o array `buscas` veio vazio, corrigir a extração da query em `onStepFinish` usando o nome de
  campo real observado no JSON bruto (assumption A5)

Registrar no SUMMARY, como DECISÃO DE FASE D-23-04, os valores finais de `maxOutputTokens`,
`effort` e `temperature` COM os números medidos que os justificam (tokens, duração,
`finishReason`). Não deixar o executor de planos futuros re-escolher esses valores.

O script fica no repo (é o instrumento de re-medição quando o snapshot do modelo mudar) mas NÃO
ganha entrada em `package.json` — não é CI, custa dinheiro e precisa da chave.
  </action>
  <acceptance_criteria>
    - `node scripts/spike-modelo-diagnostico.mjs` executa e sai com código 0, imprimindo duração, `inputTokens`, `outputTokens`, `finishReason`, número de buscas com o texto de cada uma, e número de fontes
    - O `finishReason` impresso NÃO é `"length"` na configuração final (se for, `maxOutputTokens` foi ajustado e o spike re-rodado até deixar de ser)
    - O número de fontes coletadas é >= 1 (prova de que a Web Search está de fato habilitada na organização)
    - O SUMMARY registra D-23-04 com os valores finais de `maxOutputTokens`, `effort` e `temperature` e os números medidos que os sustentam
    - `node -e "const p=require('./package.json');if(Object.values(p.scripts).some(s=>s.includes('spike-modelo-diagnostico')))process.exit(1)"` sai com código 0 (spike NÃO virou script npm)
    - `npx tsc --noEmit` e `npm run lint` saem com código 0 após o ajuste em `gerar-diagnostico.ts`
    - `npm run test:diagnostico-estrutural` continua verde
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run test:diagnostico-estrutural</automated>
    <human-check>Rodar `node scripts/spike-modelo-diagnostico.mjs` sozinho (host 4GB — fechar outros processos node antes) e conferir na saída: pelo menos 1 fonte coletada, `finishReason` diferente de `length`, e as queries de busca listadas em português e específicas do nicho "costureira sob medida" (não genéricas).</human-check>
  </verify>
  <done>Spike executado contra a API real; `maxOutputTokens`/`effort`/`temperature` fixados a partir de medição e registrados como D-23-04; extração das queries de busca confirmada ou corrigida.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → Server Action | `campanhaId` chega em `FormData` e é totalmente controlável pelo cliente. Server Action é endpoint HTTP interno. |
| aplicação → API da Anthropic | Sai a chave de API; volta conteúdo não confiável. |
| LLM → banco | O `payload` gravado é conteúdo gerado por modelo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05 | Tampering | `gerarDiagnosticoAction` | mitigate | `z.coerce.number().int().positive()` no `campanhaId` ANTES de qualquer query; SELECT da campanha com checagem de `deletedAt` (guarda de FK forjada); `isForeignKeyViolation` como backstop no catch do insert, cobrindo a janela check-then-write. |
| T-23-01 | Information Disclosure | `.env.local` | mitigate | Chave só em `.env.local`, coberto pelo `.gitignore` (linha 34); verificado por `git status --porcelain .env.local` vazio na Task 2. Lida apenas server-side pelo provider. |
| T-23-04 | Denial of Service | custo por clique | mitigate | Sem cache é requisito, então o teto é estrutural: 1 usuário, botão manual, `maxUses: 6`, `maxOutputTokens` medido, `MAX_TENTATIVAS = 2`. Tokens e nº de buscas persistidos em toda linha para o custo ficar auditável (DIAGNOSTICO-10). |
| T-23-07 | Tampering / Repudiation | persistência de falha | mitigate | Erro grava linha com `status` `"falhou"` e `payload` NULL antes de retornar; a action nunca devolve resultado parcial. |
| T-23-08 | Information Disclosure | mensagem de erro ao usuário | mitigate | O campo `erro` recebe `err.message`, nunca stack trace nem `process.env`; o spike imprime `usage`/`finishReason`/`toolCalls`, jamais a chave. |
| T-23-13 | Denial of Service | latência de 30-90s da Server Action | mitigate | `maxDuration = 120` na rota (plano 23-07); localmente `next dev`/`next start` não impõem limite. Deploy em Vercel Hobby cortaria em 60s — registrado como restrição conhecida, não aplicável ao uso local atual. |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo
- `npm run lint` sai com código 0
- `npm run verify:schema` verde
- `npm run guard:no-hard-delete` verde
- `npm run test:diagnostico-estrutural` verde
- `node scripts/spike-modelo-diagnostico.mjs` executado com sucesso, com pelo menos 1 fonte e `finishReason` diferente de `length`
</verification>

<success_criteria>
- Uma geração real ponta a ponta (sem UI) produziu objeto validado e fontes reais
- A configuração do modelo está fixada por medição, não por suposição
- Falha de geração é persistida e devolvida como erro, nunca como resultado
</success_criteria>

<output>
Create `.planning/phases/23-diagn-stico-de-ia-da-campanha/23-04-SUMMARY.md` when done
</output>
