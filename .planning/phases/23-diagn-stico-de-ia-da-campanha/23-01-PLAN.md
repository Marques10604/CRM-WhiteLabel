---
phase: 23-diagn-stico-de-ia-da-campanha
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ai/diagnostico-schema.ts
  - test/fixtures/diagnostico/ok-costureira.json
  - test/fixtures/diagnostico/ok-motoboy.json
  - test/fixtures/diagnostico/ok-estetica.json
  - test/fixtures/diagnostico/adversarial-so-marketing.json
  - test/fixtures/diagnostico/bad-zero-mais-forte.json
  - test/fixtures/diagnostico/bad-dois-mais-forte.json
  - test/fixtures/diagnostico/bad-enum-veredito.json
  - test/fixtures/diagnostico/bad-achados-sem-tipo.json
  - test/fixtures/diagnostico/bad-truncado.json
  - scripts/test-diagnostico-estrutural.cjs
  - package.json
autonomous: true
requirements: [DIAGNOSTICO-02, DIAGNOSTICO-03, DIAGNOSTICO-04, DIAGNOSTICO-05, DIAGNOSTICO-06, DIAGNOSTICO-07, DIAGNOSTICO-08, DIAGNOSTICO-09]

must_haves:
  truths:
    - "O contrato de saída do diagnóstico existe como schema Zod e rejeita objeto fora da forma anti-genérico"
    - "Um conjunto de fontes vazio é rejeitado por um helper puro, sem depender do SDK de IA"
    - "URL com esquema perigoso (javascript:/data:) é reprovada antes de virar href"
    - "O harness estrutural roda em segundos, sem chave de API, sem banco, e falha em fixture ruim"
  artifacts:
    - path: "src/lib/ai/diagnostico-schema.ts"
      provides: "diagnosticoSchema + type Diagnostico + filtrarFontes/assertTemFonte/urlSegura"
      contains: "export const diagnosticoSchema"
      min_lines: 60
    - path: "scripts/test-diagnostico-estrutural.cjs"
      provides: "Harness estrutural anti-genérico sobre fixtures"
      contains: "test-diagnostico-estrutural"
      min_lines: 60
    - path: "test/fixtures/diagnostico/ok-costureira.json"
      provides: "Fixture gold válida com _sources simulado"
    - path: "package.json"
      provides: "Scripts npm test:diagnostico-estrutural e migrate:diagnosticos"
      contains: "test:diagnostico-estrutural"
  key_links:
    - from: "scripts/test-diagnostico-estrutural.cjs"
      to: "src/lib/ai/diagnostico-schema.ts"
      via: "import dinâmico via ts-alias-loader.mjs"
      pattern: "@/lib/ai/diagnostico-schema"
    - from: "scripts/test-diagnostico-estrutural.cjs"
      to: "test/fixtures/diagnostico"
      via: "readdirSync das fixtures"
      pattern: "fixtures.*diagnostico"
---

<objective>
Criar o **contrato** do diagnóstico de IA — o schema Zod que materializa as regras anti-genérico
dos requisitos DIAGNOSTICO-02..09 — junto com as fixtures de referência e o harness estrutural
que prova o contrato sem chamar o Claude e sem tocar o banco.

Purpose: o schema Zod é o contrato anti-genérico da fase inteira (23-AI-SPEC §4b). Ele nasce
antes de qualquer código de IA para que os planos seguintes implementem *contra* um contrato já
provado, e para que o harness (sensor Nyquist) exista antes da lógica que ele mede.
Output: `src/lib/ai/diagnostico-schema.ts`, 9 fixtures JSON, `scripts/test-diagnostico-estrutural.cjs`,
2 scripts npm.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-diagn-stico-de-ia-da-campanha/23-AI-SPEC.md
@.planning/phases/23-diagn-stico-de-ia-da-campanha/23-RESEARCH.md
@.planning/phases/23-diagn-stico-de-ia-da-campanha/23-PATTERNS.md
@src/lib/validations.ts
@scripts/test-tarefa-actions.cjs
@scripts/ts-alias-loader.mjs
@package.json

<interfaces>
<!-- Contratos que os planos seguintes consomem deste plano. Definidos AQUI, não descobertos. -->

De `src/lib/ai/diagnostico-schema.ts` (a criar nesta onda):

- `export const diagnosticoSchema` — objeto Zod com as chaves de topo:
  `indice_saturacao`, `gatilhos_dor`, `objecoes`, `ticket_medio`, `achados`,
  `rascunho_primeira_mensagem`, `veredito_sugerido`.
- `export type Diagnostico = z.infer<typeof diagnosticoSchema>` — consumido por
  `src/db/schema.ts` (plano 23-02) como `.$type<Diagnostico>()` e pelos componentes de UI.
- `export function filtrarFontes(sources): { url: string; title?: string }[]`
- `export function assertTemFonte(fontes): void` — lança `Error` quando o array está vazio.
- `export function urlSegura(u: string): boolean` — true só para `http:` e `https:`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contrato Zod do diagnóstico + helpers de gate puros</name>
  <files>src/lib/ai/diagnostico-schema.ts</files>
  <read_first>
    - `.planning/phases/23-diagn-stico-de-ia-da-campanha/23-AI-SPEC.md` §4b "Structured Outputs with Zod" (linhas 286-338) — corpo do schema, VERBATIM
    - `.planning/phases/23-diagn-stico-de-ia-da-campanha/23-RESEARCH.md` linhas 593-613 — helpers `filtrarFontes` / `urlSegura`
    - `.planning/phases/23-diagn-stico-de-ia-da-campanha/23-PATTERNS.md` seção "src/lib/ai/diagnostico-schema.ts" — restrição dura de imports
    - `src/lib/validations.ts` — idioma Zod do projeto (mensagens PT-BR literais, `export type` logo abaixo do schema, constantes de mensagem em `const NOME_MSG`)
  </read_first>
  <action>
Criar `src/lib/ai/diagnostico-schema.ts` com o schema Zod que é o contrato de saída do LLM.

Copiar o corpo do schema VERBATIM de `23-AI-SPEC.md` §4b. Estrutura obrigatória, com estes nomes
de chave exatos (snake_case — é o que o modelo emite):
- `indice_saturacao`: objeto com `concorrentes_diretos` (`z.number().int().nonnegative()`),
  `leitura` (`z.string().min(10).max(300)`), `fontes` (`z.array(z.string().url()).min(1)`) — DIAGNOSTICO-03
- `gatilhos_dor`: `z.array(...).min(1).max(3)` de `{ gatilho: string 10..300, observavel_em: z.string().url(), mais_forte: z.boolean() }` — DIAGNOSTICO-04
- `objecoes`: `z.array(...).min(2).max(3)` de `{ objecao: string 5..300, resposta_sugerida: string 10..600 }` — DIAGNOSTICO-05
- `ticket_medio`: `{ valor_estimado_brl: z.number().positive(), base: string 10..400, fonte_url: z.string().url() }` — DIAGNOSTICO-06
- `achados`: `z.array(...).min(3)` de `{ afirmacao: string 10..400, tipo: z.enum(["dado_quantificavel","alegacao_marketing"]), fonte_url: z.string().url() }` — DIAGNOSTICO-07
- `rascunho_primeira_mensagem`: `z.string().min(40).max(1200)` — DIAGNOSTICO-08
- `veredito_sugerido`: `{ decisao: z.enum(["aprofundar","mudar_angulo","abandonar"]), justificativa: string 20..800 }` — DIAGNOSTICO-09
- `.refine(...)` no objeto: exatamente 1 item de `gatilhos_dor` com `mais_forte === true`, mensagem
  literal `"Exatamente um gatilho deve ser marcado como mais_forte"`.

Extrair essa mensagem para uma constante nomeada no topo (idioma de `CAMPANHA_JANELA_INVALIDA_MSG`
em `src/lib/validations.ts`) para que o harness da Task 3 possa asserir a mensagem sem duplicar
string literal.

Exportar `export type Diagnostico = z.infer<typeof diagnosticoSchema>` logo abaixo do schema.

Adicionar os 3 helpers puros:
- `filtrarFontes(sources)` — recebe `{ sourceType: string; url?: string; title?: string }[]`,
  devolve só os itens com `sourceType === "url"` e `url` string, mapeados para `{ url, title }`.
- `assertTemFonte(fontes)` — lança `Error` com mensagem literal
  `"Diagnóstico sem nenhuma fonte da web — rejeitado."` quando `fontes.length === 0`. Retorna void.
- `urlSegura(u)` — `new URL(u)` em try/catch; true só se `protocol` for `"https:"` ou `"http:"`.
  Documentar no comentário que `z.string().url()` NÃO basta (aceita `javascript:`) — este helper é o
  controle de XSS T-23-02.

RESTRIÇÃO DURA (Pitfall 10 do 23-RESEARCH.md): este arquivo pode importar SOMENTE `zod`.
Proibido, direta ou transitivamente: `server-only`, `ai`, `@ai-sdk/anthropic`, `@/db/client`,
`@/db/schema`, qualquer componente React. O harness `.cjs` importa este arquivo fora do bundler do
Next — qualquer um desses imports faz o harness explodir.

Doc-comment de cabeçalho em PT-BR explicando: é o contrato anti-genérico da Fase 23, cada bloco
mapeia para um DIAGNOSTICO-XX, e a restrição de imports acima.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` sai com código 0
    - `grep -c "^import" src/lib/ai/diagnostico-schema.ts` retorna 1 e a única linha de import contém `from "zod"`
    - `grep -E "server-only|@ai-sdk|from \"ai\"|@/db/" src/lib/ai/diagnostico-schema.ts` não retorna nenhuma linha
    - O arquivo contém as 7 chaves de topo: `indice_saturacao`, `gatilhos_dor`, `objecoes`, `ticket_medio`, `achados`, `rascunho_primeira_mensagem`, `veredito_sugerido`
    - O arquivo contém os identificadores exportados `diagnosticoSchema`, `Diagnostico`, `filtrarFontes`, `assertTemFonte`, `urlSegura`
    - `node -e "require('node:module').register('./scripts/ts-alias-loader.mjs', require('node:url').pathToFileURL('./scripts/')); import('@/lib/ai/diagnostico-schema').then(m => { if (!m.diagnosticoSchema) process.exit(1); })"` sai com código 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>`src/lib/ai/diagnostico-schema.ts` existe, exporta os 5 identificadores, importa apenas `zod`, e `tsc --noEmit` passa.</done>
</task>

<task type="auto">
  <name>Task 2: Fixtures de referência do diagnóstico (3 gold + 1 adversarial + 5 ruins)</name>
  <files>test/fixtures/diagnostico/ok-costureira.json, test/fixtures/diagnostico/ok-motoboy.json, test/fixtures/diagnostico/ok-estetica.json, test/fixtures/diagnostico/adversarial-so-marketing.json, test/fixtures/diagnostico/bad-zero-mais-forte.json, test/fixtures/diagnostico/bad-dois-mais-forte.json, test/fixtures/diagnostico/bad-enum-veredito.json, test/fixtures/diagnostico/bad-achados-sem-tipo.json, test/fixtures/diagnostico/bad-truncado.json</files>
  <read_first>
    - `src/lib/ai/diagnostico-schema.ts` (criado na Task 1) — a forma exata de cada fixture
    - `.planning/phases/23-diagn-stico-de-ia-da-campanha/23-AI-SPEC.md` §5 "Reference Dataset" (linhas 471-481) — os 3 nichos-gold e seus vereditos esperados
    - `.planning/phases/23-diagn-stico-de-ia-da-campanha/23-PATTERNS.md` seção "test/fixtures/diagnostico/*.json"
  </read_first>
  <action>
Criar o diretório `test/fixtures/diagnostico/` (não existe hoje — não há `test/` no repo) com 9
arquivos JSON. Cada fixture é um objeto no formato de `diagnosticoSchema` MAIS um campo extra
`_sources` — array de `{ "sourceType": "url", "url": "...", "title": "..." }` simulando
`result.sources` do AI SDK, usado pelo cross-check da Task 3. Zod descarta chaves desconhecidas,
então `_sources` não invalida as fixtures `ok-*`.

Conteúdo em PT-BR, com números e URLs plausíveis (domínios brasileiros reais como
`instagram.com`, `reclameaqui.com.br`, `olx.com.br`, blogs de setor). Não precisa que a URL exista
de fato — a fixture não faz rede.

Prefixo `ok-` = DEVE validar contra `diagnosticoSchema`:
- `ok-costureira.json` — nicho costureira/ateliê sob medida, `veredito_sugerido.decisao` = `"mudar_angulo"`
- `ok-motoboy.json` — nicho motoboy com clientes particulares, decisao = `"aprofundar"`
- `ok-estetica.json` — nicho estética/beleza, decisao = `"abandonar"`
Cada uma com: `indice_saturacao.concorrentes_diretos` inteiro plausível (ex. 3, 7, 22), exatamente
1 gatilho com `mais_forte: true` entre 2-3 gatilhos, 2 ou 3 objeções, `ticket_medio.valor_estimado_brl`
positivo, `achados` com pelo menos 3 itens misturando os 2 valores de `tipo`,
`rascunho_primeira_mensagem` entre 40 e 1200 caracteres, e TODA `fonte_url`/`observavel_em`/
`indice_saturacao.fontes[]` também presente em `_sources`.

- `adversarial-so-marketing.json` — também prefixo de validação: renomear para
  `ok-adversarial-so-marketing.json` NÃO; manter o nome `adversarial-so-marketing.json` e tratá-lo
  como caso especial no harness (Task 3). Deve validar contra o schema E ter pelo menos 2 achados
  `alegacao_marketing` e pelo menos 1 `dado_quantificavel` (exercita a dimensão 6 do 23-AI-SPEC §5:
  copy de concorrente marcada como alegação, nunca como dado).

Prefixo `bad-` = DEVE ser rejeitado por `diagnosticoSchema`:
- `bad-zero-mais-forte.json` — 3 gatilhos, nenhum com `mais_forte: true` (viola o `.refine`)
- `bad-dois-mais-forte.json` — 3 gatilhos, dois com `mais_forte: true` (viola o `.refine`)
- `bad-enum-veredito.json` — `veredito_sugerido.decisao` = `"talvez"` (fora do enum)
- `bad-achados-sem-tipo.json` — um item de `achados` sem a chave `tipo`
- `bad-truncado.json` — JSON sintaticamente válido mas com a chave `veredito_sugerido` AUSENTE e
  `rascunho_primeira_mensagem` cortado em menos de 40 caracteres, simulando `finishReason: "length"`

Todas as 9 fixtures precisam ser JSON parseável — nenhuma pode ter JSON quebrado, senão o harness
falha no `JSON.parse` antes de chegar ao `safeParse`.
  </action>
  <acceptance_criteria>
    - `ls test/fixtures/diagnostico/*.json | wc -l` retorna 9
    - `node -e "const fs=require('fs');for(const f of fs.readdirSync('test/fixtures/diagnostico')) JSON.parse(fs.readFileSync('test/fixtures/diagnostico/'+f,'utf8'))"` sai com código 0
    - Os 3 arquivos `ok-*.json` contêm respectivamente as strings `"mudar_angulo"`, `"aprofundar"` e `"abandonar"` no campo `veredito_sugerido.decisao`
    - `grep -l "_sources" test/fixtures/diagnostico/*.json` lista os 9 arquivos
    - `grep -c "alegacao_marketing" test/fixtures/diagnostico/adversarial-so-marketing.json` retorna valor >= 2
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('node:fs');const d='test/fixtures/diagnostico';const a=fs.readdirSync(d).filter(f=>f.endsWith('.json'));if(a.length!==9){console.error('esperado 9 fixtures, achei '+a.length);process.exit(1)}for(const f of a){JSON.parse(fs.readFileSync(d+'/'+f,'utf8'))}console.log('OK 9 fixtures parseaveis')"</automated>
  </verify>
  <done>9 fixtures JSON existem em `test/fixtures/diagnostico/`, todas parseáveis, com `_sources` simulado, e os 3 gold carregam os 3 vereditos discriminados.</done>
</task>

<task type="auto">
  <name>Task 3: Harness estrutural anti-genérico + scripts npm</name>
  <files>scripts/test-diagnostico-estrutural.cjs, package.json</files>
  <read_first>
    - `scripts/test-tarefa-actions.cjs` (linhas 36-59 do bootstrap e 304-318 do encerramento) — molde exato de `register()`/`check()`/`process.exit`
    - `scripts/ts-alias-loader.mjs` — como o alias `@/` é resolvido
    - `src/lib/ai/diagnostico-schema.ts` (Task 1) — o que importar
    - `test/fixtures/diagnostico/` (Task 2) — as fixtures a percorrer
    - `package.json` (linhas 5-31) — idioma das entradas de script existentes
    - `.planning/phases/23-diagn-stico-de-ia-da-campanha/23-RESEARCH.md` §Validation Architecture (linhas 716-725) — as 5 dimensões automatizáveis
  </read_first>
  <action>
Criar `scripts/test-diagnostico-estrutural.cjs` no molde de `scripts/test-tarefa-actions.cjs`.

Bootstrap idêntico ao molde: `"use strict"`, `require("node:module").register("./ts-alias-loader.mjs", pathToFileURL(__dirname + "/"))`,
contador `let failed = 0`, função `check(condition, message)` que imprime `OK <msg>` ou
`FAIL <msg>` e incrementa `failed`, e encerramento
`main().then(() => process.exit(failed > 0 ? 1 : 0)).catch(...)`.

DIFERENÇAS CRÍTICAS em relação ao molde (23-PATTERNS.md, seção do harness): NÃO registrar o
`next-cache-stub-loader.mjs` (não se importa nenhum módulo `"use server"`), NÃO setar
`DB_FILE_NAME`, NÃO criar temp DB, NÃO executar `DELETE FROM` — este harness não toca banco nenhum.

Dentro de `main()`, importar dinamicamente `@/lib/ai/diagnostico-schema` (`diagnosticoSchema`,
`filtrarFontes`, `assertTemFonte`, `urlSegura`) e rodar os grupos de asserção abaixo.

Grupo A — Dimensão 1 (conformidade estrutural), varredura das fixtures:
percorrer `test/fixtures/diagnostico/*.json`; para cada arquivo com prefixo `ok-` asserir
`diagnosticoSchema.safeParse(data).success === true`; para cada arquivo com prefixo `bad-` asserir
`success === false`; `adversarial-so-marketing.json` é tratado como caso válido (deve passar).

Grupo B — Asserções finas sobre CADA fixture que valida (os 3 `ok-` + a adversarial), lendo do
`safeParse().data`:
- `indice_saturacao.concorrentes_diretos` é inteiro e `>= 0` (DIAGNOSTICO-03)
- `gatilhos_dor.length` entre 1 e 3 E exatamente 1 item com `mais_forte === true` (DIAGNOSTICO-04)
- `objecoes.length` entre 2 e 3 e toda objeção tem `resposta_sugerida` não vazia (DIAGNOSTICO-05)
- `ticket_medio.valor_estimado_brl > 0` e `ticket_medio.fonte_url` presente (DIAGNOSTICO-06)
- `achados.length >= 3` e todo achado tem `tipo` em `["dado_quantificavel","alegacao_marketing"]` (DIAGNOSTICO-07)
- `rascunho_primeira_mensagem.length` entre 40 e 1200 (DIAGNOSTICO-08)
- `veredito_sugerido.decisao` em `["aprofundar","mudar_angulo","abandonar"]` (DIAGNOSTICO-09)

Grupo C — Dimensão 6 (mistura dado x marketing): na fixture `adversarial-so-marketing.json`,
asserir que existe pelo menos 1 achado `alegacao_marketing` E pelo menos 1 `dado_quantificavel`
(o mix tem que estar presente e distinguível).

Grupo D — Dimensão 2 (gate de fontes + cross-check), sem rede:
- `filtrarFontes([])` devolve array de comprimento 0
- `filtrarFontes([{sourceType:"document"},{sourceType:"url",url:"https://a.com",title:"A"}])` devolve exatamente 1 item com `url === "https://a.com"`
- `assertTemFonte([])` lança (usar try/catch, asserir que caiu no catch)
- `assertTemFonte([{url:"https://a.com"}])` não lança
- cross-check: para cada fixture que valida, montar o `Set` das URLs de `_sources` e asserir que
  TODA `fonte_url` de `achados[]`, a `ticket_medio.fonte_url`, todo `observavel_em` de
  `gatilhos_dor[]` e toda entrada de `indice_saturacao.fontes[]` constam nesse Set

Grupo E — Dimensão 2 (XSS, T-23-02):
- `urlSegura("https://exemplo.com.br")` é true
- `urlSegura("http://exemplo.com.br")` é true
- `urlSegura("javascript:alert(1)")` é false
- `urlSegura("data:text/html,<script>")` é false
- `urlSegura("nao-e-url")` é false

Grupo F — Dimensão 9 (limiares de custo, sobre dados simulados no próprio arquivo, sem DB):
declarar no harness um par de linhas simuladas de `diagnosticos` (objetos JS literais com
`buscas`, `inputTokens`, `outputTokens`, `finishReason`) e asserir `buscas.length <= 6` (o
`maxUses` travado), `inputTokens + outputTokens` acima de 0, e `finishReason !== "length"`.

Em `package.json`, acrescentar DUAS entradas na seção `scripts`, seguindo a ordem/estilo das
existentes:
- `"migrate:diagnosticos": "node scripts/migrate-diagnosticos.cjs"` (o arquivo alvo é criado no
  plano 23-02 — a entrada nasce aqui de propósito, para que `package.json` seja tocado por um
  único plano por onda)
- `"test:diagnostico-estrutural": "node scripts/test-diagnostico-estrutural.cjs"`
  </action>
  <acceptance_criteria>
    - `npm run test:diagnostico-estrutural` sai com código 0 e imprime pelo menos 40 linhas iniciadas por `OK `
    - A saída NÃO contém nenhuma linha iniciada por `FAIL `
    - `node -e "const p=require('./package.json');if(!p.scripts['test:diagnostico-estrutural']||!p.scripts['migrate:diagnosticos'])process.exit(1)"` sai com código 0
    - Prova de mutação: renomear temporariamente `test/fixtures/diagnostico/bad-dois-mais-forte.json` para `ok-dois-mais-forte.json`, rodar `npm run test:diagnostico-estrutural`, confirmar exit code 1, e desfazer o rename
    - `grep -v '^\s*//' scripts/test-diagnostico-estrutural.cjs | grep -c "next-cache-stub-loader"` retorna 0
    - `grep -v '^\s*//' scripts/test-diagnostico-estrutural.cjs | grep -c "DELETE FROM"` retorna 0
  </acceptance_criteria>
  <verify>
    <automated>npm run test:diagnostico-estrutural</automated>
  </verify>
  <done>`npm run test:diagnostico-estrutural` verde, cobrindo as dimensões 1/2/5/6/9 do 23-AI-SPEC §5; falha comprovada quando uma fixture ruim é reclassificada como boa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → aplicação | A saída do modelo é entrada NÃO CONFIÁVEL. Todo objeto passa por `diagnosticoSchema` antes de qualquer uso. |
| LLM → DOM (via href) | URLs autoradas pelo modelo viram `href` na UI (plano 23-05). O esquema precisa ser allowlistado ANTES. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02 | Tampering / Elevation | `urlSegura()` em `src/lib/ai/diagnostico-schema.ts` | mitigate | Allowlist de esquema (`http:`/`https:`) — `z.string().url()` do Zod aceita `javascript:` e NÃO basta. Grupo E do harness prova a rejeição de `javascript:` e `data:`. |
| T-23-06 | Repudiation | `filtrarFontes` / `assertTemFonte` | mitigate | O gate de fontes lê apenas o array de `sources` da tool de busca; cross-check do Grupo D prova que toda URL citada dentro do objeto consta em `_sources`. |
| T-23-07 | Tampering | `diagnosticoSchema` | mitigate | Contrato estrito (enums, min/max, `.refine` de exatamente 1 `mais_forte`) rejeita objeto malformado antes de virar resultado renderizável. Grupo A prova com fixtures `bad-*`. |
| T-23-01 | Information Disclosure | `src/lib/ai/diagnostico-schema.ts` | mitigate | Restrição dura de imports (só `zod`): o arquivo do contrato nunca puxa o SDK nem `server-only`, então não há caminho de import que arraste a chave de API para um bundle de cliente. Verificado por grep na acceptance criteria da Task 1. |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo
- `npm run lint` sai com código 0
- `npm run test:diagnostico-estrutural` verde, sem nenhuma linha `FAIL`
- Prova de mutação do harness executada e desfeita
</verification>

<success_criteria>
- `diagnosticoSchema` encoda todos os DIAGNOSTICO-02..09 em asserções verificáveis
- O harness roda em menos de 30 segundos, sem `ANTHROPIC_API_KEY` e sem tocar `data/crm.db`
- `type Diagnostico` está exportado e pronto para `.$type<Diagnostico>()` no plano 23-02
</success_criteria>

<output>
Create `.planning/phases/23-diagn-stico-de-ia-da-campanha/23-01-SUMMARY.md` when done
</output>
