---
status: planned
phase: 23
slug: diagn-stico-de-ia-da-campanha
stopped_at: "planejamento CONCLUÍDO — 7 planos verificados, pronto para /gsd-execute-phase 23"
saved: 2026-09-09T00:00:00Z
saved_reason: "fim do /gsd-plan-phase 23"
---

# Fase 23 — Planejamento CONCLUÍDO (2026-09-09)

`/gsd-plan-phase 23` rodou do início ao fim:

1. Usou os 5 artefatos pré-planner já prontos (RESEARCH, PATTERNS, AI-SPEC, UI-SPEC, VALIDATION) — sem CONTEXT.md (decisão da sessão anterior).
2. **Planner (opus)** → 7 planos em 5 ondas — commit `344d24b`.
3. **Plan-checker (sonnet)**, 1ª passada → 0 bloqueadores, 5 avisos.
4. **Revisão dirigida (planner opus)** → 5/5 avisos tratados — commit `2e26c8e`.
5. **Re-checagem (sonnet)** → **VERIFICATION PASSED** (2 avisos menores aceitos como dívida).
6. Gate de cobertura de requisitos: DIAGNOSTICO-01..10 todos cobertos.
7. STATE.md + ROADMAP.md atualizados à mão (shim `gsd-sdk` global quebrado — ver STATE.md Blockers).

## Planos (5 ondas)

| Onda | Planos | Autônomo |
|------|--------|----------|
| 1 | 23-01 (contrato Zod + fixtures + harness anti-genérico) | sim |
| 2 | 23-02 [BLOCKING] (tabela `diagnosticos` + migração `.cjs`), 23-03 (núcleo IA + `npm i`) | sim / **não** (portão de pacote) |
| 3 | 23-04 (Server Action + setup + spike Sonnet 5) | **não** (setup do usuário) |
| 4 | 23-05 (componentes de apresentação), 23-06 (eval gold-set) | sim / **não** (aprovação gold-set) |
| 5 | 23-07 (seção UI em `/campanhas/[id]`) | sim |

## Decisões aprovadas pelo usuário (não re-perguntar)

- **D-23-01** — `criado_em` = `integer({mode:"timestamp"})` + `unixepoch()` (corrige AI-SPEC §4)
- **D-23-02** — Server Action em `src/actions/diagnostico-actions.ts` (não co-locada)
- **D-23-06** — sem "busca ao vivo": spinner + aviso de custo no pending; consultas reais
  (`diagnosticos.buscas`) renderizadas só após a geração (23-05 Task 3, bloco 2)
- **D-23-07** — coluna `aviso` TEXT separada de `erro` no schema `diagnosticos` (11 colunas)

## Dívida menor aceita (não bloqueia execução)

- 23-05 `must_haves.truths` não captura o bloco "Ressalva:" (acceptance criterion já cobre)
- 23-05 `depends_on: ["23-04"]` poderia ser `["23-02"]` (otimização de paralelismo)

## Próximo passo

`/clear` e então:

```
/gsd-execute-phase 23
```

**Setup do usuário ANTES da Onda 3** (Ondas 1–2, exceto o `npm i` da 23-03, rodam sem isso):
- `.env.local` na raiz: `ANTHROPIC_API_KEY=sk-ant-...`
- Habilitar "Web Search" tool nas configs da ORGANIZAÇÃO no Console da Anthropic (senão HTTP 400)

## Milestone v1.7 — progresso

Fase 22 ✅ · Fase 23 ✅ planejada · Fases 24 (Veredito + Mapa de Nichos), 25 (Tour React Joyride) pendentes.
