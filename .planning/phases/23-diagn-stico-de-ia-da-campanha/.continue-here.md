---
status: in_progress
phase: 23
slug: diagn-stico-de-ia-da-campanha
stopped_at: "planejamento da Fase 23 — meio do /gsd-plan-phase, pattern-mapper em voo"
saved: 2026-09-05T23:22:00Z
saved_reason: "usuário foi dormir"
---

# Onde paramos — Fase 23 (Diagnóstico de IA da Campanha)

## Resumo da sessão (2026-09-05, noite)

1. **Fase 22 fechada** — `/close-phase 22`: learnings extraídos (`3cecc73`), verificação já `passed` (4/4), **push direto na `main`** (sem PR, padrão v1.5/v1.6). Marcadores reconciliados.
2. **`/gsd-ai-integration-phase 23`** completo → `23-AI-SPEC.md` (`2b80cc8`): framework **Vercel AI SDK** (`ai@7.0.93` + `@ai-sdk/anthropic@4.0.49`), `generateText` + `Output.object` + web search tool do Claude, harness estrutural anti-genérico, gold-set dos 3 nichos.
3. **`/gsd-ui-phase 23`** completo → `23-UI-SPEC.md` (`5c8e7cb` + revisão `90be328`, **6/6 dimensões PASS** após 1 revisão de tipografia). Seção nova em `/campanhas/[id]`, 6 componentes novos.
4. **`/gsd-plan-phase 23`** — EM ANDAMENTO. Estado:
   - ✅ `23-RESEARCH.md` (`e636a0e`) — pesquisa de fase feita. Correções pro planner: `claude-sonnet-5` já é snapshot fixo; preço US$2/US$10 por 1M; **Sonnet 5 tem adaptive thinking sempre ligado** → `maxOutputTokens: 6000` da AI-SPEC é apertado demais, usar ~16000 + `effort: 'low'`, validar com task de spike na Wave 1; `diagnostico-schema.ts` precisa ter ZERO imports de efeito colateral.
   - ✅ `23-VALIDATION.md` (`11c8b90`) — estratégia Nyquist (esqueleto; planner preenche o Per-Task Map).
   - 🔄 **gsd-pattern-mapper estava rodando quando salvamos** — se `23-PATTERNS.md` NÃO existe na pasta, o resultado se perdeu; re-rodar.

## Próximo passo ao acordar

Rodar de novo:

```
/gsd-plan-phase 23
```

Ele é resumível: detecta `23-RESEARCH.md` e `23-VALIDATION.md` (pula pesquisa + Nyquist), detecta `23-PATTERNS.md` se existir (pula pattern-mapper), e vai direto pro **gsd-planner** (opus) → **gsd-plan-checker** (loop de revisão até 3x) → gate de cobertura de requisitos → commit dos PLAN.md.

**Se `23-PATTERNS.md` não existir:** o `/gsd-plan-phase 23` re-spawna o pattern-mapper automaticamente (não-bloqueante) antes do planner. Sem ação manual.

Decisões já dadas nesta sessão (não re-perguntar): sem CONTEXT.md (seguir sem — AI-SPEC+UI-SPEC+roadmap são os contratos); modo COMPLETO (com pesquisa de fase, já feita); UI-SPEC gerada primeiro (feito).

## Forma de plano sugerida pela pesquisa

- **Wave 0:** `migrate-diagnosticos.cjs` (molde `migrate-campanhas.cjs`) + tabela `diagnosticos` no `schema.ts` + `verify-schema.cjs` estendido + fixtures `test/fixtures/diagnostico/*.json` + esqueleto do `test-diagnostico-estrutural.cjs`
- **Wave 1:** `src/lib/ai/{diagnostico-schema,diagnostico-prompt,gerar-diagnostico}.ts` + Server Action + harness estrutural verde + **task de spike de config do modelo** (effort/maxOutputTokens/temperature com o adaptive thinking do Sonnet 5)
- **Wave 2:** seção de UI em `/campanhas/[id]` (6 componentes) + `scripts/eval-diagnostico.mjs`

## Setup do usuário ANTES de `/gsd-execute-phase 23` (não bloqueia o planejamento)

- Criar `.env.local` na raiz: `ANTHROPIC_API_KEY=sk-ant-...`
- Habilitar a "Web Search" tool nas configurações da **organização** no Console da Anthropic (senão HTTP 400)
- Wave 0 e a maior parte da Wave 1 rodam SEM isso; só a geração real + `eval-diagnostico.mjs` + UAT dependem.

## Milestone v1.7 — progresso

Fase 22 ✅ fechada · Fase 23 🔄 planejando · Fases 24 (Veredito + Mapa de Nichos), 25 (Tour React Joyride) pendentes.
