---
phase: 17-limpeza-de-lint-do-repo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - eslint.config.mjs
  - src/components/nicho-manager.tsx
  - src/components/periodo-selector.tsx
  - src/components/template-form-dialog.tsx
  - src/components/configuracoes-form.tsx
  - src/components/csv-import-wizard.tsx
  - src/components/lixeira-table.tsx
  - .planning/phases/08-origem-governada-separa-o-inbound-outbound/deferred-items.md
  - .planning/PROJECT.md
  - .planning/STATE.md
autonomous: true
requirements: [LINT-01]

must_haves:
  truths:
    - "`npm run lint` executado da raiz do repo, sem passar arquivos, termina com exit code 0"
    - "`.claude/**` (incluindo `.claude/get-shit-done` e `.claude/worktrees/**`) está fora do escopo do ESLint via `globalIgnores` no `eslint.config.mjs` — nenhum arquivo de `.claude/` foi deletado para isso"
    - "O worktree órfão `agent-ab2be3f82c3c9c30d` não aparece mais em `git worktree list`"
    - "`scripts/**/*.cjs` passa no lint via bloco de override que desliga `@typescript-eslint/no-require-imports` com comentário explicando que `require()` é o idioma CommonJS deliberado desses harnesses"
    - "Os 4 erros reais de `src/` (`react-hooks/set-state-in-effect` em 2 efeitos de `nicho-manager.tsx` e 1 de `periodo-selector.tsx`; `react-hooks/refs` em `template-form-dialog.tsx`) têm `eslint-disable-next-line` com comentário citando o falso-positivo do React Compiler e o precedente do projeto"
    - "Nenhuma regra do ESLint foi desligada de forma global que mascare um erro real em `src/` — `git diff eslint.config.mjs` é pequeno e todo bloco novo tem comentário"
    - "`deferred-items.md` da Fase 8 e a seção Context do `PROJECT.md` não listam mais `npm run lint` global saindo 1 como débito aberto"
  artifacts:
    - path: "eslint.config.mjs"
      provides: "`.claude/**` no globalIgnores + override escopado de `scripts/**/*.cjs` para `no-require-imports`"
      contains: ".claude/**"
    - path: "src/components/nicho-manager.tsx"
      provides: "2 `eslint-disable-next-line react-hooks/set-state-in-effect` comentados (um por efeito)"
      contains: "eslint-disable-next-line react-hooks/set-state-in-effect"
    - path: "src/components/periodo-selector.tsx"
      provides: "1 `eslint-disable-next-line react-hooks/set-state-in-effect` comentado no efeito de ressincronização"
      contains: "eslint-disable-next-line react-hooks/set-state-in-effect"
    - path: "src/components/template-form-dialog.tsx"
      provides: "1 `eslint-disable-next-line react-hooks/refs` comentado entre `ref={formRef}` e `onSubmit`"
      contains: "eslint-disable-next-line react-hooks/refs"
    - path: ".planning/phases/08-origem-governada-separa-o-inbound-outbound/deferred-items.md"
      provides: "item do lint marcado como resolvido na Fase 17"
      contains: "Fase 17"
    - path: ".planning/PROJECT.md"
      provides: "seção Context sem o bullet `npm run lint` global sai com exit 1 como débito aberto"
      contains: "auditado, polido"
  key_links:
    - from: "eslint.config.mjs (globalIgnores)"
      to: "árvore .claude/**"
      via: "ignore pattern"
      pattern: "\\.claude/\\*\\*"
    - from: "eslint.config.mjs (bloco de override)"
      to: "scripts/**/*.cjs"
      via: "files: [\"scripts/**/*.cjs\"] com no-require-imports off"
      pattern: "scripts/\\*\\*/\\*\\.cjs"
    - from: "package.json script `lint` (= `eslint`)"
      to: "repo inteiro"
      via: "npm run lint da raiz"
      pattern: "\"lint\": \"eslint\""
---

<objective>
Quitar LINT-01: `npm run lint` rodado da raiz do repo, sem escopo de arquivos, volta a
sair com exit code `0`. A dívida acumulada desde a Fase 8 (documentada como "457 erros
pré-existentes") é triada e fechada sem esconder nenhum erro real de `src/`.

Propósito: hoje `npm run lint` (script = `eslint`, sem args) sai `1`. O diagnóstico real
(`npx eslint .` em 2026-09-01, e `npx eslint . --ignore-pattern '.claude/**'` reconfirmado
nesta sessão) mostra que a esmagadora maioria dos erros vem de ferramental que não é
código do produto:
- `.claude/**` (GSD instalado + worktree órfão, **não trackeado no git**) → ~377 err + ~57 warn, incluindo TODOS os `no-explicit-any` / `ban-ts-comment` / `no-empty-object-type` (vêm de um `.next/types/validator.ts` gerado dentro do worktree órfão)
- `scripts/**/*.cjs` → 76 err, todos `@typescript-eslint/no-require-imports` (CommonJS deliberado — harnesses de teste/migração Node puro)
- `src/` → apenas **4 erros reais** (`react-hooks/set-state-in-effect` x3, `react-hooks/refs` x1), todos falsos-positivos já conhecidos do React Compiler que o projeto **já suprime com `eslint-disable` comentado** noutros arquivos (STATE.md decisões 07-02 / 09-03 / 09-04)

Saída: `eslint.config.mjs` com `.claude/**` no `globalIgnores` e um override escopado de
`scripts/**/*.cjs`; worktree órfão removido; `eslint-disable-next-line` documentado nos 4
erros reais de `src/`; diretivas `eslint-disable` obsoletas de `src/` removidas; docs de
débito (`deferred-items.md`, `PROJECT.md`, `STATE.md`) atualizadas.

**Zero código de runtime tocado.** Só config de tooling e comentários de supressão.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/17-limpeza-de-lint-do-repo/17-CONTEXT.md
@eslint.config.mjs
@.planning/phases/08-origem-governada-separa-o-inbound-outbound/deferred-items.md
</context>

<notes>
- **Nyquist / VALIDATION.md:** N/A nesta fase de limpeza. A rede de validação é o SC#1
  do ROADMAP — `npm run lint` da raiz em exit 0 — mais `npx tsc --noEmit` exit 0 como
  garantia de que as supressões não quebraram nada (são comentários, não código).
- **Host de 4GB:** rodar tudo em série, nada de processos paralelos, fechar processos
  `node` órfãos antes de qualquer comando pesado.
- **`npm run build` NÃO é gate desta fase:** as mudanças são config de lint + comentários
  `eslint-disable`. Nenhuma linha de código executável muda. `npx tsc --noEmit` cobre a
  ausência de regressão de tipos a custo baixo.
- **Zero dependência nova** — nenhum `npm install` em nenhuma task. Gate de legitimidade
  de pacote não se aplica.
- **Diagnóstico atualizado nesta sessão:** `npx eslint . --ignore-pattern '.claude/**'`
  → 88 problemas (80 erros, 8 warnings). 80 erros = 76 `no-require-imports` (todos em
  `scripts/*.cjs`) + 3 `set-state-in-effect` + 1 `refs` (os 4 de `src/`). 8 warnings = 4
  `react-hooks/incompatible-library` (deferidos — D: deixar como estão) + 4 diretivas
  `eslint-disable` obsoletas em `src/`. Warnings **não** afetam o exit code.
- **`.claude` não está no git** (`git ls-files .claude` → 0). Ignorar por config é a
  forma certa (D-01), não deletar arquivos.
</notes>

<tasks>

<task type="auto">
  <name>Task 1: eslint.config.mjs — ignore de `.claude/**` + override de `scripts/**/*.cjs`, e remoção do worktree órfão</name>
  <files>eslint.config.mjs</files>
  <read_first>
    - `eslint.config.mjs` — a config inteira (18 linhas): `defineConfig([...])` com
      `...nextVitals`, `...nextTs` e o `globalIgnores([...])` que hoje re-adiciona só os
      ignores default do `eslint-config-next` (`.next/**`, `out/**`, `build/**`,
      `next-env.d.ts`) e **esqueceu** `.claude/`.
    - `.planning/phases/17-limpeza-de-lint-do-repo/17-CONTEXT.md` §D-01, §D-02, §D-03,
      §D-06 — decisões travadas (patterns exatos, override, texto de comentário fica a
      critério).
    - `package.json` §scripts linha `"lint": "eslint"` — confirma que o script roda sem
      args, da raiz, recursivo. **Não editar.**
    - Saída de `git worktree list` — deve mostrar
      `.claude/worktrees/agent-ab2be3f82c3c9c30d  dc9dc98 [worktree-agent-ab2be3f82c3c9c30d]`.
  </read_first>
  <action>
    Três mudanças, todas de tooling:

    (1) **Ignore de `.claude/**` (D-01).** Adicionar a string `".claude/**"` ao array
    passado para `globalIgnores(...)` em `eslint.config.mjs`, junto dos ignores default
    já re-listados. Ordem dentro do array é livre (Claude's Discretion). Um comentário
    curto na linha nova explicando que `.claude/**` é ferramental do GSD + worktrees,
    não código do produto, e não está trackeado no git. Isso tira do escopo ~377 err +
    ~57 warn de `.claude/get-shit-done`, `.claude/hooks` e do worktree órfão — inclusive
    os `no-explicit-any` (22), `ban-ts-comment` (4) e `no-empty-object-type` (3), que só
    aparecem no `.next/types/validator.ts` gerado dentro do worktree órfão (D-06).

    (2) **Override de `scripts/**/*.cjs` (D-03).** Acrescentar ao array do `defineConfig`
    um objeto de config novo com `files: ["scripts/**/*.cjs"]` e um bloco `rules` que
    seta `"@typescript-eslint/no-require-imports": "off"`. Comentário no objeto (ou na
    regra) explicando: são harnesses de teste/migração em Node puro (`.cjs`), `require()`
    é o idioma CommonJS correto e deliberado desses arquivos — migrar para ESM é dívida
    de padrão separada, explicitamente deferida no 17-CONTEXT.md §Deferred Ideas.
    Todos os 23 `.cjs` do repo estão sob `scripts/` (confirmado: `git ls-files '*.cjs'`),
    então esse único glob cobre os 76 erros `no-require-imports`. Colocar o objeto de
    override **depois** dos spreads `...nextVitals` / `...nextTs` para que ganhe deles.
    NÃO adicionar `no-require-imports` a `src/**` nem a nenhum outro glob. NÃO desligar
    nenhuma outra regra. NÃO mexer nos `.mjs` de `scripts/` (já são ESM, sem erro).

    (3) **Remover o worktree órfão (D-02).** Rodar, da raiz do repo:
    `git worktree remove .claude/worktrees/agent-ab2be3f82c3c9c30d --force` e em seguida
    `git worktree prune`. O `--force` lida com o `.next/` gerado / working tree sujo
    dentro do worktree. O ignore de (1) já cobriria esses arquivos, mas o worktree é
    cruft real de execução antiga (branch `worktree-agent-ab2be3f82c3c9c30d` @ `dc9dc98`,
    um commit da Fase 03) e limpar é o certo. Deletar a branch órfã
    (`git branch -D worktree-agent-ab2be3f82c3c9c30d`) é opcional — faça se quiser deixar
    `git branch` limpo; não é exigido por D-02.

    Não rodar `--fix` do eslint nesta task (as correções ficam na Task 2, controladas).
  </action>
  <acceptance_criteria>
    - `grep -c '\.claude/\*\*' eslint.config.mjs` retorna ≥ 1.
    - `eslint.config.mjs` contém a substring literal `scripts/**/*.cjs` e a substring
      `"@typescript-eslint/no-require-imports"` com valor `"off"` no mesmo objeto de config.
    - `node -e "import('./eslint.config.mjs').then(m => { if (!Array.isArray(m.default)) process.exit(1) })"` sai com exit 0 (a config continua um array válido de flat config).
    - `git worktree list` **não** contém `agent-ab2be3f82c3c9c30d`.
    - `npx eslint scripts` sai com exit 0 (os 76 `no-require-imports` sumiram).
    - `npx eslint . 2>&1 | tail -3` reporta **4 erros** (só os de `src/`) — prova que o
      ignore de `.claude/**` e o override de `scripts/` funcionaram e nada mais foi
      mascarado.
  </acceptance_criteria>
  <verify>
    <automated>git worktree list; npx eslint scripts; echo "scripts exit=$?"; npx eslint . 2>&1 | tail -3</automated>
  </verify>
  <done>`.claude/**` no globalIgnores; override de `scripts/**/*.cjs` comentado; worktree órfão fora de `git worktree list`; `npx eslint .` cai para 4 erros, todos em `src/`.</done>
</task>

<task type="auto">
  <name>Task 2: eslint-disable documentado nos 4 erros reais de `src/` + remoção das diretivas obsoletas</name>
  <files>src/components/nicho-manager.tsx, src/components/periodo-selector.tsx, src/components/template-form-dialog.tsx, src/components/configuracoes-form.tsx, src/components/csv-import-wizard.tsx, src/components/lixeira-table.tsx</files>
  <read_first>
    - `src/components/nicho-manager.tsx` — os DOIS `useEffect`: `NichoRow` (~linhas 26-31,
      `setIsEditing(false)` na linha ~29) e `NichoManager` (~linhas 119-125,
      `formRef.current?.reset()` + `setIsAdding(false)` na linha ~123). Erros reportados
      em `29:7` e `123:7` (`react-hooks/set-state-in-effect`, um por efeito).
    - `src/components/periodo-selector.tsx` — o `useEffect` de ressincronização (~linhas
      101-105) com `setCustomMode` / `setDataInicio` / `setDataFim`. Erro reportado em
      `102:5` (um único erro para o efeito inteiro). O doc-comment do componente (~19-40)
      já explica por que o efeito existe (navegação soft do Next, CR-01/WR-04).
    - `src/components/template-form-dialog.tsx` — o `<form>` (~linhas 115-119):
      `ref={formRef}` na 116, `onSubmit={form.handleSubmit(onSubmit)}` na 117. Erro em
      `117:39` (`react-hooks/refs`).
    - **Precedente do projeto (copiar o estilo verbatim):**
      - `src/components/configuracoes-form.tsx` ~linhas 161-166 — bloco
        `/* eslint-disable-next-line react-hooks/refs -- mesmo padrão de TemplateFormDialog: ... */`
        colocado ENTRE `ref={formRef}` e `onSubmit` dentro do JSX do `<form>`.
      - `src/components/tarefa-form-dialog.tsx` ~linhas 172-178 — mesmo bloco.
      - `src/components/motivo-perda-manager.tsx` ~linhas 39 e 138 —
        `// eslint-disable-next-line react-hooks/set-state-in-effect -- <razão>` acima do
        `setState` dentro do efeito.
      - `src/components/whatsapp-preview-dialog.tsx` ~linha 85 e
        `src/components/lead-timeline-dialog.tsx` ~linha 82 — mesmo idioma para
        `set-state-in-effect`.
    - **Diretivas obsoletas a remover** (ESLint 9 + eslint-config-next 16.2 não reportam
      mais `react-hooks/exhaustive-deps` nesses pontos — ESLint marca cada uma como
      "Unused eslint-disable directive"):
      - `src/components/configuracoes-form.tsx:119` — `// eslint-disable-next-line react-hooks/exhaustive-deps` acima de `}, [state]);`
      - `src/components/csv-import-wizard.tsx:154` — acima de `}, [mappedRows]);`
      - `src/components/csv-import-wizard.tsx:190` — acima de `}, [mappedRows, previewSupportData, ...]);`
      - `src/components/lixeira-table.tsx:112` — acima de `[isPending, restoringId]`
  </read_first>
  <action>
    Duas frentes, ambas só de comentário — **nenhuma linha executável muda**.

    (1) **Suprimir os 4 erros reais** (D-04). Para cada um, inserir um `eslint-disable`
    de UMA linha (nunca desligar a regra para o arquivo inteiro), com comentário curto
    citando: (a) que é falso-positivo já conhecido do React Compiler, (b) o precedente do
    projeto (STATE.md decisões 07-02 / 09-03 / 09-04). Texto exato do comentário fica a
    critério (Claude's Discretion), mas siga o idioma dos analogs:

    - `nicho-manager.tsx`, efeito de `NichoRow`: `// eslint-disable-next-line react-hooks/set-state-in-effect -- <razão>` imediatamente acima da linha `setIsEditing(false);`. O `toast.success(...)` da linha anterior não é flagado; a diretiva vai colada no `setState`.
    - `nicho-manager.tsx`, efeito de `NichoManager`: mesma diretiva imediatamente acima da linha `setIsAdding(false);`.
    - `periodo-selector.tsx`: a diretiva imediatamente acima da primeira linha de `setState` do efeito (`setCustomMode(value === "custom");`) — a regra reporta uma vez para o efeito, então uma diretiva basta.
    - `template-form-dialog.tsx`: o bloco `/* eslint-disable-next-line react-hooks/refs -- ... */` ENTRE `ref={formRef}` e `onSubmit={form.handleSubmit(onSubmit)}`, idêntico em forma ao que já existe em `configuracoes-form.tsx` e `tarefa-form-dialog.tsx` (que hoje citam `template-form-dialog.tsx` como "o analog" — agora ele ganha a sua própria supressão).

    (2) **Remover as 4 diretivas `eslint-disable` obsoletas** de `src/` (D-05, na sua
    forma real: as diretivas obsoletas ocuparam o lugar dos antigos `no-unused-vars`).
    Deletar apenas a linha do comentário `// eslint-disable-next-line react-hooks/exhaustive-deps`
    nos 4 pontos listados em `read_first`. NÃO tocar as demais diretivas
    `exhaustive-deps` do projeto (ex.: `lead-form-dialog.tsx:162`, `template-form-dialog.tsx:91`,
    `whatsapp-preview-dialog.tsx:94`, `tarefa-form-dialog.tsx:96`, `lead-timeline-dialog.tsx:98`)
    — essas ainda são usadas (o efeito tem deps faltando de propósito) e ESLint **não** as
    marca como obsoletas. Só remova o que ESLint reportar como "Unused eslint-disable directive".

    Restrições travadas:
    - NÃO tocar os 4 warnings `react-hooks/incompatible-library` (`csv-import-preview-table.tsx`,
      `lead-form-dialog.tsx`, `lead-table.tsx`, `lixeira-table.tsx`) — deferidos no
      17-CONTEXT.md, são warnings, não bloqueiam o exit 0.
    - NÃO mudar nenhuma lista de dependências de `useEffect`/`useMemo`/`useCallback`.
    - NÃO desligar `react-hooks/set-state-in-effect` nem `react-hooks/refs` globalmente
      nem por arquivo (só `-next-line`).
  </action>
  <acceptance_criteria>
    - `npx eslint src` sai com exit 0.
    - `npx eslint src 2>&1 | grep -c "Unused eslint-disable directive"` retorna 0.
    - `grep -c "eslint-disable-next-line react-hooks/set-state-in-effect" src/components/nicho-manager.tsx` retorna 2.
    - `grep -c "eslint-disable-next-line react-hooks/set-state-in-effect" src/components/periodo-selector.tsx` retorna 1.
    - `grep -c "eslint-disable-next-line react-hooks/refs" src/components/template-form-dialog.tsx` retorna 1.
    - `git diff -U0 src/components/ | grep -E '^\+' | grep -v '^\+\+\+' | grep -vE 'eslint-disable|--|React Compiler|falso-positivo|STATE|padrão|/\*|\*/'` não retorna nenhuma linha (toda adição é comentário de supressão).
    - `npx tsc --noEmit` sai com exit 0.
  </acceptance_criteria>
  <verify>
    <automated>npx eslint src; echo "src exit=$?"; npx eslint src 2>&1 | grep -c "Unused eslint-disable directive" || true; grep -c "set-state-in-effect" src/components/nicho-manager.tsx; npx tsc --noEmit; echo "tsc exit=$?"</automated>
  </verify>
  <done>`npx eslint src` exit 0; 4 supressões de uma linha comentadas nos 4 erros reais; 4 diretivas obsoletas removidas; `tsc --noEmit` exit 0; nenhuma linha executável alterada.</done>
</task>

<task type="auto">
  <name>Task 3: Atualizar docs de débito + gate final `npm run lint` exit 0</name>
  <files>.planning/phases/08-origem-governada-separa-o-inbound-outbound/deferred-items.md, .planning/PROJECT.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/phases/08-origem-governada-separa-o-inbound-outbound/deferred-items.md`
      — o arquivo inteiro (61 linhas): o item "`npx eslint` — 413 problemas ..." e a
      seção **Status:** na linha ~59 ("pendente, não relacionado a ORIGEM-01/02").
    - `.planning/PROJECT.md` — seção `## Context`: o bullet da linha ~111
      ("**`npm run lint` global sai com exit 1** desde a Fase 8 — 457 erros
      pré-existentes ..."). Também a linha ~10 (bullet de escopo do milestone) e ~78
      (resumo do milestone) que descrevem a Fase 17 — essas continuam **corretas** como
      descrição do trabalho; não apagá-las.
    - `.planning/STATE.md` — rodar `grep -n -i lint .planning/STATE.md`; os pontos com
      "sai 1" / "457 erros pré-existentes" / "repo-inteiro sai 1" (ex.: linhas ~303,
      ~315, ~334 na seção de gates / Session Continuity).
    - `.planning/phases/17-limpeza-de-lint-do-repo/17-CONTEXT.md` §D-07 — o que atualizar.
  </read_first>
  <action>
    Fechar o débito na documentação e provar o SC#1. Nenhuma dessas edições toca código.

    (1) **`deferred-items.md` da Fase 8** — marcar o item do lint como resolvido. Editar a
    linha **Status:** para algo como "RESOLVIDO na Fase 17 (LINT-01, 2026-09-01): `.claude/**`
    no `globalIgnores`, override de `scripts/**/*.cjs`, worktree órfão removido,
    `eslint-disable` documentado nos 4 falsos-positivos de `src/`. `npm run lint` da raiz
    sai 0." Manter o resto do texto histórico do item (é registro de quando/como foi
    descoberto). Não deletar o arquivo.

    (2) **`PROJECT.md` §Context** — remover (ou reescrever como resolvido) o bullet
    "**`npm run lint` global sai com exit 1** desde a Fase 8 ...". Se reescrever, dizer que
    foi quitado na Fase 17 e como. Não mexer nos bullets de escopo do milestone (linhas
    ~10 / ~78) — descrevem a fase, seguem válidos.

    (3) **`STATE.md`** — nos pontos que afirmam "`npm run lint` ... sai 1" / "457 erros
    pré-existentes" como estado atual, anexar/anotar que foi resolvido na Fase 17
    (LINT-01). Não reescrever a história das seções Session Continuity de fases passadas —
    só anotar o estado novo onde a frase estiver no presente ("segue saindo 1",
    "repo-inteiro sai 1"). O `/close-phase` da Fase 17 vai fazer o refresh completo do
    STATE.md depois; aqui é só tirar a afirmação falsa.

    (4) **Gate final (SC#1)** — rodar `npm run lint` da raiz, **sem argumentos** (não
    `npx eslint src`), e confirmar exit code 0. Rodar também `npx tsc --noEmit` (exit 0).
    Fechar processos `node` órfãos antes se a RAM estiver apertada.
  </action>
  <acceptance_criteria>
    - `npm run lint` executado da raiz, sem args, sai com exit code 0 (`echo $?` → 0).
    - `npx tsc --noEmit` sai com exit 0.
    - `grep -c "sai com exit 1" .planning/PROJECT.md` retorna 0 (o bullet de débito aberto sumiu ou virou "resolvido").
    - `grep -c "Fase 17" .planning/phases/08-origem-governada-separa-o-inbound-outbound/deferred-items.md` retorna ≥ 1.
    - `grep -niE "npm run lint.*(sai|sae|segue).*1|repo-inteiro sai 1" .planning/STATE.md` não retorna nenhuma linha que descreva o estado como presente/atual.
    - `git status --porcelain` mostra só arquivos modificados (nenhum novo, nenhum removido) além do `17-01-SUMMARY.md`.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint; echo "lint exit=$?"; npx tsc --noEmit; echo "tsc exit=$?"; grep -c "sai com exit 1" .planning/PROJECT.md || true; grep -c "Fase 17" .planning/phases/08-origem-governada-separa-o-inbound-outbound/deferred-items.md</automated>
  </verify>
  <done>`npm run lint` da raiz exit 0; `tsc --noEmit` exit 0; `deferred-items.md` (Fase 8), `PROJECT.md` §Context e `STATE.md` não listam mais o lint global como débito aberto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma) | Fase code-only / config-only de tooling. Nenhuma superfície de entrada, sem auth, sem rede, sem schema, sem código de runtime tocado. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | `globalIgnores([".claude/**"])` em `eslint.config.mjs` | mitigate | `.claude/**` não é código do produto (não trackeado no git — `git ls-files .claude` → 0); ignorá-lo não pode mascarar erro de `src/`. Provado pela acceptance da Task 1: `npx eslint .` continua reportando os 4 erros reais de `src/` antes das supressões da Task 2. |
| T-17-02 | Tampering | Override `files: ["scripts/**/*.cjs"]` desligando `no-require-imports` | mitigate | Escopo do override é o glob `scripts/**/*.cjs` e a regra é `no-require-imports` apenas; `src/**` e qualquer `.ts`/`.tsx` ficam intocados. `require()` é o idioma CommonJS correto desses harnesses. Provado por `npx eslint src` continuar reportando exatamente os mesmos erros que antes do override. |
| T-17-03 | Tampering | `eslint-disable-next-line` nos 4 erros de `src/` | accept | Supressões pontuais por linha, cada uma comentada; não desligam a regra para o arquivo nem globalmente. Mesmo padrão de falso-positivo do React Compiler já auditado e aceito no projeto (STATE.md 07-02 / 09-03 / 09-04). Os `react-hooks/*` continuam ativos para todo o resto de `src/`. |
| T-17-04 | Repudiation | `git worktree remove --force` do worktree órfão | accept | Worktree é cruft de execução antiga (branch `worktree-agent-ab2be3f82c3c9c30d` @ `dc9dc98`, um commit da Fase 03, fora do `main`). Conteúdo já reproduzível pelo git; `--force` descarta só `.next/` gerado. Sem impacto em código ativo. |
| T-17-SC | Tampering | instalações npm/pip/cargo | accept | N/A nesta fase — zero dependência nova, nenhum `npm install` em nenhuma task. O gate de legitimidade de pacote não se aplica. |
</threat_model>

<verification>
1. `npm run lint` executado da raiz, **sem argumentos** → exit code 0 (SC#1).
2. `grep '\.claude/\*\*' eslint.config.mjs` → presente no `globalIgnores`; nenhum arquivo de `.claude/` deletado (`git status` não mostra deleções em `.claude/`) (SC#2).
3. `git worktree list` → sem `agent-ab2be3f82c3c9c30d` (SC#2).
4. `eslint.config.mjs` → bloco `files: ["scripts/**/*.cjs"]` com `no-require-imports: "off"` comentado; `npx eslint scripts` exit 0 (SC#3).
5. `npx eslint src` exit 0; os 4 `eslint-disable-next-line` de `src/` têm comentário cada (SC#3).
6. `git diff eslint.config.mjs` → pequeno, revisável, todo bloco novo comentado; nenhuma regra desligada globalmente (SC#4).
7. `git diff -U0 src/components/` → só linhas de comentário `eslint-disable` adicionadas e linhas de diretiva obsoleta removidas; nenhuma linha executável (SC#4).
8. `grep "sai com exit 1" .planning/PROJECT.md` → 0; `deferred-items.md` da Fase 8 marca o item resolvido na Fase 17 (SC#5).
9. `npx tsc --noEmit` → exit 0 (garantia de não-regressão; as mudanças são comentários).
</verification>

<success_criteria>
- SC#1: `npm run lint` da raiz, sem escopo, exit 0.
- SC#2: `.claude/get-shit-done` e o worktree órfão fora do ESLint pela config (ignore pattern), não por deleção; worktree removido de `git worktree list`.
- SC#3: `scripts/**/*.cjs` passa via override documentado de `no-require-imports`; os falsos-positivos `react-hooks/*` de `src/` têm `eslint-disable` comentado, um por ocorrência.
- SC#4: nenhuma regra desativada globalmente que mascare erro real de `src/`; diff da config pequeno, revisável, comentado; diff de `src/` só de comentário.
- SC#5: `deferred-items.md` (Fase 8) e `PROJECT.md` §Context não listam mais `npm run lint` global saindo 1 como débito aberto.
- Zero código de runtime tocado.
</success_criteria>

<output>
Criar `.planning/phases/17-limpeza-de-lint-do-repo/17-01-SUMMARY.md` ao terminar.
</output>
