---
phase: 16-corre-es-de-code-review-da-fase-15
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/validations.ts
  - scripts/test-lead-actions.cjs
  - src/lib/csv-import.ts
  - src/components/csv-column-mapper.tsx
autonomous: true
requirements: [FIX-01, FIX-03]

must_haves:
  truths:
    - "Criar um lead com o campo Interesse contendo só espaços em branco persiste a linha com interesse = NULL (nunca string vazia)"
    - "Limpar o Interesse de um lead que já tinha valor, enviando só espaços em branco, grava NULL na coluna"
    - "npm run test:lead-actions sai com exit 0 cobrindo os dois casos novos de whitespace-only"
    - "O truncamento defensivo em 500 de mapCsvRows corta por code point, sem partir par surrogate"
    - "Os comentários de csv-column-mapper.tsx falam em 8 campos fixos, não 7"
    - "D-01: o trim de `interesse` acontece dentro do `z.preprocess` em `src/lib/validations.ts` (não na Server Action), seguindo o snippet do 15-REVIEW §WR-01"
    - "D-02: `test-lead-actions.cjs` ganha dois casos novos — `createLead` e `updateLead` limpando um valor existente, ambos com `interesse` só-espaços gravando NULL"
    - "D-03: os testes novos ficam limitados ao valor `\"   \"` — sem whitespace exótico (tab/newline) e sem novos casos de truncamento CSV"
  artifacts:
    - path: "src/lib/validations.ts"
      provides: "preprocess de `interesse` que faz trim ANTES de decidir vazio -> undefined"
      contains: "typeof v === \"string\" ? v.trim() : v"
    - path: "scripts/test-lead-actions.cjs"
      provides: "2 casos novos de whitespace-only (create + update) sobre leads.interesse"
      contains: "\"   \""
    - path: "src/lib/csv-import.ts"
      provides: "corte de 500 por code point em mapCsvRows"
      contains: "Array.from"
    - path: "src/components/csv-column-mapper.tsx"
      provides: "comentários atualizados para 8 campos fixos"
      contains: "8 campos fixos"
  key_links:
    - from: "src/lib/validations.ts"
      to: "src/actions/lead-actions.ts"
      via: "parsed.data.interesse ?? null (createLead :101 / updateLead :179)"
      pattern: "interesse: parsed\\.data\\.interesse \\?\\? null"
    - from: "scripts/test-lead-actions.cjs"
      to: "leads.interesse"
      via: "leitura direta do banco após a Server Action"
      pattern: "interesse === null"
    - from: "src/lib/validations.ts"
      to: "csvRowSchema"
      via: "leadBaseSchema.omit() — csvRowSchema herda o preprocess corrigido"
      pattern: "leadBaseSchema"
---

<objective>
Fechar FIX-01 (achado WR-01 do `15-REVIEW.md`) e a parte "CSV" do FIX-03 (achados
IN-01 e IN-02): o campo `interesse` passa a honrar o contrato D-04 da Fase 15
("vazio grava NULL, nunca `''`") também quando a entrada é só espaço em branco, e o
truncamento defensivo da importação CSV corta por code point.

Propósito: hoje `" "` no formulário manual escapa do `v === ""` do `z.preprocess`,
vira `""` depois do `z.string().trim()` interno, e o `?? null` da Server Action não
o captura — o banco recebe string vazia, contradizendo um contrato repetido ~4× no
próprio código. Nenhum teste cobre esse caminho.

Saída: `validations.ts` com o trim dentro do preprocess, dois casos novos no harness
`test-lead-actions.cjs` provando create e update, corte por code point em
`csv-import.ts` e comentários honestos em `csv-column-mapper.tsx`.

**Zero feature funcional nova.** `interesse` continua texto livre nullable — não
renomear, não governar, não indexar.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/16-corre-es-de-code-review-da-fase-15/16-CONTEXT.md
@.planning/phases/16-corre-es-de-code-review-da-fase-15/16-PATTERNS.md
@.planning/phases/15-campo-interesse-servi-o-desejado-no-lead/15-REVIEW.md
@.planning/phases/15-campo-interesse-servi-o-desejado-no-lead/15-CONTEXT.md

@src/lib/validations.ts
@src/lib/csv-import.ts
@src/components/csv-column-mapper.tsx
@scripts/test-lead-actions.cjs
</context>

<notes>
- **Nyquist / VALIDATION.md:** intencionalmente N/A nesta fase de limpeza (decisão do
  usuário). A rede de validação é o SC#5 do ROADMAP — `tsc --noEmit`, `npm run build`
  e `npm run test:lead-actions` em exit 0 — verificado no plano 16-02, Task 3.
- **Host de 4GB:** rodar tudo em série. Nada de processos em paralelo, fechar
  processos `node` órfãos antes do `npm run build`.
- **Zero dependência nova** nesta fase — nenhum `npm install`, nenhum gate de
  legitimidade de pacote se aplica.
</notes>

<tasks>

<task type="auto">
  <name>Task 1: Trim dentro do z.preprocess de `interesse` (WR-01 / D-01)</name>
  <files>src/lib/validations.ts</files>
  <read_first>
    - `src/lib/validations.ts` — o `z.preprocess` de `interesse` em `leadBaseSchema`
      (~linhas 71-74), o comentário de contrato do bloco (~linhas 33-40), o
      `z.preprocess` de `motivoPerdaId` (~linhas 80-83, o molde do qual `interesse`
      foi derivado) e a definição de `csvRowSchema` (~linha 116, deriva por `.omit()`).
    - `.planning/phases/15-campo-interesse-servi-o-desejado-no-lead/15-REVIEW.md`
      §WR-01 — traz o snippet de correção pronto; seguir **verbatim**.
    - `.planning/phases/16-corre-es-de-code-review-da-fase-15/16-PATTERNS.md`
      §`src/lib/validations.ts` — bloco "antes", bloco "depois" e o molde `motivoPerdaId`.
    - `.planning/phases/15-campo-interesse-servi-o-desejado-no-lead/15-CONTEXT.md`
      §D-04 — o contrato "vazio grava NULL, nunca string vazia".
    - `src/actions/lead-actions.ts` linhas ~98-101 e ~177-179 — os consumidores
      (`interesse: parsed.data.interesse ?? null`). **Não editar**, só entender.
  </read_first>
  <action>
    Substituir a função de preprocess de `interesse` em `leadBaseSchema` pela versão do
    `15-REVIEW.md` §WR-01, seguindo o snippet como escrito: a arrow function passa a ter
    corpo em bloco, declara `const s` recebendo `v.trim()` quando `typeof v === "string"`
    (senão o próprio `v`), e retorna `undefined` quando `s` é string vazia, `null` ou
    `undefined` — caso contrário retorna `s`. O segundo argumento do `z.preprocess`
    (`z.string().trim().max(500, "O interesse deve ter no máximo 500 caracteres.").optional()`)
    fica **intacto**, mensagem PT-BR inclusa.

    Restrições travadas:
    - D-09: NÃO alterar o `.max(500)` de `interesse` (nem em `leadBaseSchema` nem em
      `csvRowSchema`) — continua contando code units UTF-16.
    - NÃO adicionar trim ao preprocess de `motivoPerdaId` (é numérico; a divergência é
      intencional e está documentada no 16-PATTERNS.md).
    - NÃO tocar `src/actions/lead-actions.ts` — o `?? null` já é load-bearing e correto;
      com o trim, `"   "` chega lá como `undefined` e vira `null`.
    - `csvRowSchema` herda a mudança automaticamente pelo `.omit()`; o caminho do CSV
      não muda de comportamento porque `mapCsvRows` já pré-trima.

    Se o comentário de contrato do bloco (~33-40) for tocado, manter a coerência com o
    novo trim — mas reescrevê-lo não é obrigatório.
  </action>
  <acceptance_criteria>
    - `src/lib/validations.ts` contém, dentro do preprocess de `interesse`, a expressão
      `typeof v === "string" ? v.trim() : v` atribuída a um `const s`.
    - O preprocess retorna `undefined` para `s === ""`, `s === null` e `s === undefined`.
    - A string literal `"O interesse deve ter no máximo 500 caracteres."` continua
      presente no arquivo, e `.max(500` continua presente (D-09 respeitado).
    - `grep -c "motivoPerdaId" src/lib/validations.ts` continua ≥ 1 e o preprocess de
      `motivoPerdaId` segue sem `.trim()`.
    - `npx tsc --noEmit` sai com exit 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit; echo "tsc exit=$?"; grep -c 'v.trim() : v' src/lib/validations.ts; grep -c '.max(500' src/lib/validations.ts</automated>
  </verify>
  <done>`tsc --noEmit` exit 0; o preprocess de `interesse` faz trim antes de testar vazio; `.max(500)` e a mensagem PT-BR intactos.</done>
</task>

<task type="auto">
  <name>Task 2: Dois casos de whitespace-only em test-lead-actions.cjs (FIX-01 / D-02)</name>
  <files>scripts/test-lead-actions.cjs</files>
  <read_first>
    - `scripts/test-lead-actions.cjs` — Casos 13 e 14 (~linhas 418-453, o molde exato a
      copiar), os helpers `check` (~23), `makeFormData` (~147), `countLeads` (~167),
      `callToleratingRevalidate` (~176), e o Caso 19 (~516-532, truncamento CSV — deve
      continuar passando).
    - `.planning/phases/16-corre-es-de-code-review-da-fase-15/16-PATTERNS.md`
      §`scripts/test-lead-actions.cjs` — molde verbatim dos Casos 13/14, as duas opções
      de sequenciamento e a recomendação (caminho "a": criar um lead novo com `interesse`
      preenchido antes do update).
    - `.planning/phases/16-corre-es-de-code-review-da-fase-15/16-CONTEXT.md` §D-02 e §D-03.
    - `src/actions/lead-actions.ts` ~177-179 — o `interesse: parsed.data.interesse ?? null`
      do `updateLead` que o 2º caso prova.
  </read_first>
  <action>
    Acrescentar **dois** casos novos ao harness, logo após o Caso 14, no mesmo formato
    bloco `{ ... }` + `check(condicao, mensagem)` já usado no arquivo (numerar como
    "Caso 14a / 14b" ou renumerar os seguintes — indiferente, o harness só imprime a
    string do `check`).

    Caso A — create com só espaços: copiar a estrutura do Caso 13. `before = await countLeads()`,
    `await callToleratingRevalidate(createLead, makeFormData({ interesse: "   " }))`,
    `after = await countLeads()`, ler a linha nova com
    `db.select().from(leads).orderBy(leads.id).limit(1).offset(before)`. Dois `check`:
    (1) `after === before + 1` — só-espaços ainda cria o lead; (2) `row?.interesse === null`
    — o campo vira NULL, nunca `""`. Mensagens em PT-BR, incluindo o valor observado via
    `JSON.stringify(row?.interesse)` como os casos vizinhos fazem.

    Caso B — update limpando com só espaços: **não** reusar o `interesseLeadId` do Caso 13
    (o Caso 14 já o zerou). Criar primeiro um lead novo com `interesse` preenchido
    (`makeFormData({ interesse: <texto qualquer> })`), capturar o id pelo mesmo idioma de
    `offset(before)`, e só então chamar
    `callToleratingRevalidate(updateLead, makeFormData({ id: String(id), interesse: "   " }))`.
    `check` final: `row?.interesse === null`, lendo com
    `db.select().from(leads).where(eq(leads.id, id))`. Antes do update, opcionalmente um
    `check` de que o valor inicial não era nulo (prova que o update de fato limpou).

    Manter o `console.log` de cortesia sobre `revalidatePath` lançar fora do contexto Next,
    igual aos Casos 13/14.

    Restrições travadas (D-03): NÃO adicionar casos de whitespace exótico (`\t`, `\n`,
    espaço não-quebrável) nem casos novos de truncamento CSV. Exatamente os dois casos
    de `"   "`. Não converter o harness para Vitest/Jest.
  </action>
  <acceptance_criteria>
    - `npm run test:lead-actions` sai com exit 0 e a contagem total de casos aumenta em
      relação aos 19 anteriores (o rodapé do harness imprime o total).
    - A saída do harness contém as duas asserções novas em PT-BR mencionando `interesse`
      e NULL, ambas passando.
    - `grep -c '"   "' scripts/test-lead-actions.cjs` ≥ 2.
    - O Caso 19 (truncamento de 600 chars → `interesse.length === 500`) continua passando.
    - Nenhuma ocorrência de `\t` ou `\n` como valor de `interesse` nos casos novos (D-03).
  </acceptance_criteria>
  <verify>
    <automated>npm run test:lead-actions; echo "test exit=$?"; grep -c '"   "' scripts/test-lead-actions.cjs</automated>
  </verify>
  <done>`npm run test:lead-actions` exit 0 com os 2 casos novos verdes; create e update com `"   "` provados gravando NULL.</done>
</task>

<task type="auto">
  <name>Task 3: Corte por code point no CSV + comentários "8 campos fixos" (IN-02 / D-08, IN-01 / D-07)</name>
  <files>src/lib/csv-import.ts, src/components/csv-column-mapper.tsx</files>
  <read_first>
    - `src/lib/csv-import.ts` ~linhas 144-147 — o bloco do comentário D-10 e a linha
      `const interesse = readMapped(row, "interesse").slice(0, 500);`; as linhas vizinhas
      de `mapCsvRows` (`canal`, `origem`, `valorEstimado`) e o tipo `MappedCsvRow` (~43-49).
    - `src/components/csv-column-mapper.tsx` — `FIELD_CONFIGS` (~17-26, hoje com 8
      entradas) e os comentários das linhas ~64-66 e ~73-76.
    - `.planning/phases/16-corre-es-de-code-review-da-fase-15/16-PATTERNS.md`
      §`src/lib/csv-import.ts` e §`src/components/csv-column-mapper.tsx` — blocos "antes"
      e "depois" exatos.
    - `.planning/phases/15-campo-interesse-servi-o-desejado-no-lead/15-REVIEW.md`
      §IN-01 e §IN-02.
  </read_first>
  <action>
    Duas edições pontuais, ambas sem mudança de comportamento observável fora do caso
    de borda:

    (1) `src/lib/csv-import.ts`, dentro de `mapCsvRows`: trocar o corte por code unit
    pelo corte por code point — envolver `readMapped(row, "interesse")` em `Array.from(...)`,
    aplicar `.slice(0, 500)` sobre o array de code points e fechar com `.join("")`. O
    comentário D-10 imediatamente acima permanece válido e deve ser preservado; se
    reescrito, manter a referência a D-10/D-11. O tipo `MappedCsvRow.interesse` continua
    `string` — sem mudança de tipo. **Só neste arquivo** (D-08) e **sem** mexer no
    `.max(500)` do `csvRowSchema` (D-09) — a assimetria code-point × code-unit é aceita e
    já está documentada.

    (2) `src/components/csv-column-mapper.tsx`: procurar toda ocorrência literal do número
    sete nos comentários que descrevem os campos fixos ("7 campos fixos", "nenhum dos 7
    campos fixos", "dos 7 campos") e trocar por **8**. `FIELD_CONFIGS` já tem 8 entradas e
    o código deriva dinamicamente de `Object.values(mapping)` — a mudança é 100% de texto
    de comentário, zero lógica. Manter o estilo de prefixo (`// D-0x:` / `// WR-0x:`) já
    usado no arquivo.
  </action>
  <acceptance_criteria>
    - `src/lib/csv-import.ts` contém `Array.from(readMapped(row, "interesse"))` seguido de
      `.slice(0, 500)` e `.join("")` na atribuição de `const interesse`.
    - `grep -c '\.slice(0, 500)' src/lib/csv-import.ts` continua ≥ 1 e nenhuma outra
      atribuição do arquivo foi alterada.
    - `csvRowSchema` em `src/lib/validations.ts` permanece byte-idêntico ao estado após a
      Task 1 (nenhuma edição de `.max(500)` — D-09).
    - `src/components/csv-column-mapper.tsx`: `grep -c "7 campos fixos" src/components/csv-column-mapper.tsx`
      retorna 0, e `grep -c "8 campos fixos" src/components/csv-column-mapper.tsx` retorna ≥ 1.
    - Nenhuma linha de código (não-comentário) de `csv-column-mapper.tsx` mudou:
      `git diff --stat src/components/csv-column-mapper.tsx` mostra só linhas de comentário.
    - `npx tsc --noEmit` exit 0 e `npm run test:lead-actions` exit 0 (o Caso 19, com 600
      chars ASCII, continua devolvendo `interesse.length === 500`).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit; echo "tsc exit=$?"; grep -c 'Array.from' src/lib/csv-import.ts; grep -c "7 campos fixos" src/components/csv-column-mapper.tsx || true; grep -c "8 campos fixos" src/components/csv-column-mapper.tsx; npm run test:lead-actions; echo "test exit=$?"</automated>
  </verify>
  <done>Corte de 500 por code point em `mapCsvRows`; zero ocorrências de "7 campos fixos"; `tsc` e `test:lead-actions` em exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador do admin → Server Action (`createLead`/`updateLead`) | `FormData` não confiável atravessa aqui; `leadBaseSchema` é o portão |
| arquivo CSV do parceiro → `mapCsvRows` → `csvRowSchema` → `bulkImportLeads` | conteúdo de terceiro (cowork) atravessa aqui; truncar-antes-de-validar é a defesa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | `z.preprocess` de `interesse` em `src/lib/validations.ts` | mitigate | O trim novo só normaliza a borda vazia; `.max(500)` e `z.object` (strip) ficam intactos, então mass-assignment via `FormData` forjado (`id`, `deletedAt`, `createdAt`) continua descartado como o `15-REVIEW.md` auditou. Provado pelos 2 casos novos da Task 2. |
| T-16-02 | Tampering | `Array.from(...).slice(0,500)` em `mapCsvRows` (`src/lib/csv-import.ts`) | mitigate | Corte por code point elimina o surrogate solto na fronteira 499/500. Defesa em profundidade server-side (`csvRowSchema.max(500)`) permanece para chamadas RPC diretas de `bulkImportLeads` (D-09). |
| T-16-03 | Denial of Service | `Array.from` sobre célula CSV gigante em `mapCsvRows` | accept | `Array.from` materializa um array de code points da célula antes do `.slice` — custo de memória maior que o `.slice` anterior. Aceito: o import é local, single-admin, e o arquivo inteiro já está em memória via PapaParse antes de `mapCsvRows` rodar; não há amplificação remota. Alternativa (pré-cortar em 1000 code units) fica deferida — D-08 trava a forma literal. |
| T-16-04 | Repudiation | Harness `scripts/test-lead-actions.cjs` | accept | Harness de desenvolvimento, roda contra banco de teste local; sem superfície de produção. |
| T-16-SC | Tampering | instalações npm/pip/cargo | accept | N/A nesta fase — zero dependência nova, nenhum `npm install` em nenhuma task. O gate de legitimidade de pacote não se aplica. |
</threat_model>

<verification>
1. `npx tsc --noEmit` → exit 0.
2. `npm run test:lead-actions` → exit 0, com os 2 casos novos de `"   "` verdes e o Caso 19 intacto.
3. `grep -c "7 campos fixos" src/components/csv-column-mapper.tsx` → 0.
4. `git diff src/lib/validations.ts` → só o corpo do preprocess de `interesse` mudou; `.max(500)`, mensagem PT-BR e o preprocess de `motivoPerdaId` intocados.
5. `git diff --stat` → exatamente 4 arquivos modificados, nenhum arquivo criado ou removido.
6. `npm run verify:schema` → exit 0 (nenhuma mudança de schema físico nesta fase; confirma ausência de regressão).
</verification>

<success_criteria>
- FIX-01 fechado: `interesse` só-espaços grava `NULL` no create e no update, com cobertura automatizada (ROADMAP SC#1).
- FIX-03 parcial fechado: `.slice(0,500)` corta por code point (SC#4, item 2) e os comentários dizem 8 campos fixos (SC#4, item 1).
- `tsc --noEmit` e `test:lead-actions` em exit 0 (parte do SC#5; `npm run build` fecha no plano 16-02).
- Zero feature funcional nova; `interesse` continua texto livre nullable.
</success_criteria>

<output>
Criar `.planning/phases/16-corre-es-de-code-review-da-fase-15/16-01-SUMMARY.md` ao terminar.
</output>
