---
phase: 16-corre-es-de-code-review-da-fase-15
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/components/csv-import-preview-table.tsx
  - scripts/migrate-interesse.cjs
autonomous: true
requirements: [FIX-02, FIX-03]

must_haves:
  truths:
    - "Ao mapear uma coluna do CSV para Interesse, a prévia da importação mostra uma coluna Interesse com o valor que será gravado"
    - "Quando o valor da prévia foi cortado em 500 caracteres, a célula exibe um aviso visível antes de o admin confirmar"
    - "Rodar migrate:interesse com a coluna já existente não cria arquivo de backup novo em data/"
    - "O cabeçalho de migrate-interesse.cjs documenta explicitamente 'pare a app Next antes de rodar'"
    - "tsc --noEmit, npm run build e npm run test:lead-actions saem todos com exit 0"
  artifacts:
    - path: "src/components/csv-import-preview-table.tsx"
      provides: "coluna Interesse em previewColumns, no molde da coluna notas, com badge de truncamento na célula"
      contains: "accessorKey: \"interesse\""
    - path: "scripts/migrate-interesse.cjs"
      provides: "guard hasColumn antes do backup + doc de premissa operacional"
      contains: "hasColumn"
  key_links:
    - from: "src/components/csv-import-preview-table.tsx"
      to: "MappedCsvRow.interesse (src/lib/csv-import.ts)"
      via: "row.original.interesse no cell da coluna"
      pattern: "row\\.original\\.interesse"
    - from: "src/components/csv-import-preview-table.tsx"
      to: "@/components/ui/badge"
      via: "badge de 'cortado em 500' dentro da célula (fora do RowFlags)"
      pattern: "interesse\\?\\.length === 500|interesse\\.length === 500"
    - from: "scripts/migrate-interesse.cjs"
      to: "data/crm.db"
      via: "PRAGMA table_info(leads) antes de qualquer fs.copyFileSync"
      pattern: "PRAGMA table_info"
---

<objective>
Fechar FIX-02 (achado WR-02 do `15-REVIEW.md`) e o restante do FIX-03 (achado IN-03),
e rodar o gate SC#5 do ROADMAP sobre o conjunto completo das correções da fase.

Propósito: hoje o admin confirma uma importação de CSV sem nunca ver o que será gravado
em `interesse` — é a única coluna mapeável ausente da prévia, e o truncamento em 500
(D-10 da Fase 15) é 100% invisível. Em paralelo, `migrate-interesse.cjs` cria um backup
novo de `data/crm.db` a cada execução, inclusive nas idempotentes (a pasta `data/` já
acumula 13 backups), e depende de uma premissa operacional não documentada.

Saída: coluna "Interesse" na prévia com badge de truncamento, script de migração que só
faz backup quando de fato vai escrever, e os três gates de SC#5 verdes.

**Zero feature funcional nova.** Nenhuma superfície de entrada nova, sem auth, sem rede,
sem schema destrutivo.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/16-corre-es-de-code-review-da-fase-15/16-CONTEXT.md
@.planning/phases/16-corre-es-de-code-review-da-fase-15/16-PATTERNS.md
@.planning/phases/15-campo-interesse-servi-o-desejado-no-lead/15-REVIEW.md
@.planning/phases/15-campo-interesse-servi-o-desejado-no-lead/15-CONTEXT.md
@.planning/phases/16-corre-es-de-code-review-da-fase-15/16-01-SUMMARY.md

@src/components/csv-import-preview-table.tsx
@scripts/migrate-interesse.cjs
</context>

<notes>
- **Nyquist / VALIDATION.md:** intencionalmente N/A nesta fase de limpeza (decisão do
  usuário). A rede de validação é o SC#5 do ROADMAP, executado na Task 3 deste plano.
- **Host de 4GB:** execução em série. Fechar processos `node` órfãos antes do
  `npm run build` (o Next 16.2 usa Turbopack e já passou limpo desde 2026-08-29, mas a
  pressão de RAM é real).
- **`human_verify_mode: end-of-phase`** — o check visual da prévia entra como
  `<human-check>`, não como checkpoint bloqueante.
- **Zero dependência nova** nesta fase — nenhum `npm install`.
</notes>

<tasks>

<task type="auto">
  <name>Task 1: Coluna "Interesse" na prévia do CSV + badge de truncamento (WR-02 / D-04, D-05, D-06)</name>
  <files>src/components/csv-import-preview-table.tsx</files>
  <read_first>
    - `src/components/csv-import-preview-table.tsx` — a coluna `notas` em `previewColumns`
      (~linhas 121-132, o molde exato mandado por D-04), o `StatusBadges` e o uso do
      `<Badge>` do shadcn (~63-108, o padrão visual dos avisos), o tipo `RowFlags`
      (~31-37, **não** estender), a docstring do componente (~194-200), `handleConfirm`
      (~266) e o corpo genérico da tabela (~331-342).
    - `.planning/phases/16-corre-es-de-code-review-da-fase-15/16-PATTERNS.md`
      §`src/components/csv-import-preview-table.tsx` — molde da coluna `notas`, molde do
      badge (`variant="outline"`, `className="w-fit gap-1 border-transparent"`, `style`
      com `#FEF3C7`/`#B45309`, ícone `lucide-react` `size-3`) e a estrutura ilustrativa
      da célula.
    - `.planning/phases/15-campo-interesse-servi-o-desejado-no-lead/15-REVIEW.md` §WR-02.
    - `src/lib/csv-import.ts` — tipo `MappedCsvRow` (~43-49): `interesse` é `string`,
      nunca `null` neste caminho (`""` quando não mapeado).
  </read_first>
  <action>
    Adicionar **uma** entrada nova ao array `previewColumns`, posicionada logo após a
    coluna `notas`, com `accessorKey: "interesse"` e `header: "Interesse"`.

    O `cell` renderiza duas coisas empilhadas num wrapper `flex flex-col gap-1`:
    (1) o valor, no mesmo idioma da coluna `notas` — um `<span>` com
    `className="block max-w-xs whitespace-pre-line"` exibindo `row.original.interesse || "—"`
    (o `|| "—"` é o que D-04 pede para célula vazia);
    (2) condicionalmente, quando `row.original.interesse?.length === 500`, um `<Badge>`
    do shadcn com o mesmo esquema visual dos avisos amarelos já usados no arquivo
    (`variant="outline"`, `className="w-fit gap-1 border-transparent"`, `style` com
    `backgroundColor: "#FEF3C7"` e `color: "#B45309"`, ícone `lucide-react` em `size-3`),
    com copy PT-BR no espírito de "cortado em 500 caracteres" — texto exato é discrição
    do executor, mantendo o tom dos outros badges (`Duplicado`, `Nicho novo`, etc.).

    Reusar os imports que já existem no topo do arquivo (`Badge`, ícones `lucide-react`);
    só adicionar import novo se o ícone escolhido ainda não estiver importado.

    Restrições travadas:
    - D-06: o badge mora **dentro do `cell` da coluna `interesse`**. NÃO adicionar ao
      tipo `RowFlags`, NÃO tocar `StatusBadges`, NÃO entrar no sistema de flags amarelas
      por linha.
    - NÃO tocar `handleConfirm` (já passa `interesse: r.interesse`).
    - NÃO tocar o resumo textual da prévia (~291-315) — D-06 mantém o diff pequeno.
    - NÃO mudar o corpo do componente: a tabela renderiza colunas genericamente, então
      acrescentar a entrada ao array `previewColumns` basta.
  </action>
  <acceptance_criteria>
    - `grep -c 'accessorKey: "interesse"' src/components/csv-import-preview-table.tsx` = 1.
    - `grep -c 'header: "Interesse"' src/components/csv-import-preview-table.tsx` = 1.
    - O arquivo contém `row.original.interesse || "—"` e uma condição sobre
      `row.original.interesse` com `length === 500` guardando a renderização do badge.
    - `grep -c 'whitespace-pre-line' src/components/csv-import-preview-table.tsx` ≥ 2
      (a de `notas` mais a nova).
    - O tipo `RowFlags` e o componente `StatusBadges` estão byte-idênticos ao estado
      anterior: `git diff src/components/csv-import-preview-table.tsx` não mostra
      alteração nessas regiões, nem em `handleConfirm`, nem no resumo textual.
    - `npx tsc --noEmit` exit 0.
    - `npx eslint src/components/csv-import-preview-table.tsx` exit 0 (gate escopado ao
      arquivo — o lint global do repo é dívida da Fase 17, não bloqueia aqui).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit; echo "tsc exit=$?"; grep -c 'accessorKey: "interesse"' src/components/csv-import-preview-table.tsx; grep -c 'header: "Interesse"' src/components/csv-import-preview-table.tsx; npx eslint src/components/csv-import-preview-table.tsx; echo "eslint exit=$?"</automated>
    <human-check>Em `/leads` → importar CSV: subir um CSV com uma coluna de texto livre, mapeá-la para "Interesse" e conferir no passo de prévia que (a) existe uma coluna "Interesse" com o valor de cada linha, (b) linha sem valor mostra "—", (c) uma célula com mais de 500 caracteres mostra o valor cortado E o badge amarelo de "cortado em 500 caracteres". Registrar no UAT de fim de fase.</human-check>
  </verify>
  <done>A prévia da importação exibe a coluna "Interesse" no padrão de `notas`, com badge de truncamento na própria célula; `RowFlags`/`StatusBadges`/`handleConfirm` intocados; `tsc` e `eslint` escopado em exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: migrate-interesse.cjs — doc "pare a app" + sem backup em execução idempotente (IN-03 / D-10, D-11)</name>
  <files>scripts/migrate-interesse.cjs</files>
  <read_first>
    - `scripts/migrate-interesse.cjs` inteiro (89 linhas) — o header doc (1-20, incl. a
      nota `[BLOCKING]`), o bloco de backup (34-46), a abertura do `db` (48), a contagem
      de referência (51), o guard `hasColumn` (56-66) e a verificação pós-migração (68-88).
    - `.planning/phases/16-corre-es-de-code-review-da-fase-15/16-PATTERNS.md`
      §`scripts/migrate-interesse.cjs` — bloco "antes", a sequência-alvo em 3 passos e o
      alerta sobre não abrir duas conexões SQLite simultâneas.
    - `.planning/phases/15-campo-interesse-servi-o-desejado-no-lead/15-REVIEW.md` §IN-03.
    - `scripts/migrate-motivos-perda.cjs` (~40-52) — o bloco de backup irmão, **só como
      referência**; D-11 proíbe editá-lo.
  </read_first>
  <action>
    Duas mudanças no script, ambas restritas a este arquivo (D-11):

    (1) Header doc: acrescentar uma linha explícita de premissa operacional — "pare a app
      Next antes de rodar" — justificando com o WAL ativo (`journal_mode=WAL` no
      `src/db/client.ts`) que pode deixar escritas concorrentes fora da cópia do arquivo
      principal. Texto exato é discrição do executor, em PT-BR, no estilo `//` das linhas
      existentes; ponto de inserção sugerido: logo após a linha que já fala de backup +
      checkpoint WAL, ou junto do bloco `[BLOCKING]`.

    (2) Reordenar para o backup só acontecer quando o script for de fato escrever.
      Sequência-alvo:
      a. Abrir **uma** conexão `Database(DB_PATH, { fileMustExist: true })` e avaliar
         `hasColumn` via `PRAGMA table_info(leads)` procurando a coluna `interesse`.
      b. Se `hasColumn` for falso: fazer o checkpoint (`pragma("wal_checkpoint(TRUNCATE)")`),
         fechar a conexão, `fs.copyFileSync(DB_PATH, backupPath)` dentro do mesmo
         `try/catch` com `fail(...)` já existente, logar o caminho do backup, reabrir o
         `db` e executar o `ALTER TABLE \`leads\` ADD \`interesse\` text;`.
      c. Se `hasColumn` for verdadeiro: logar que a coluna já existe, que nada há a migrar
         e que **nenhum backup foi criado** (idempotência) — sem nenhum `fs.copyFileSync`.
      Cuidar para nunca manter duas conexões abertas ao mesmo arquivo: reaproveitar uma
      única conexão ou fechar antes de reabrir, como o script já faz com `dbForCheckpoint`.

    Preservar integralmente o resto do comportamento: a contagem de referência
    `beforeLeads` antes de qualquer escrita, e **toda** a verificação pós-migração
    (contagem `afterLeads` igual, tipo `TEXT`, `notnull === 0`, o `SELECT interesse ... LIMIT 1`,
    o log final de OK, `db.close()` e `process.exit(0)`). A verificação roda nos dois
    caminhos — na execução idempotente ela é justamente a prova de que está tudo certo.

    Restrições travadas:
    - D-11: NÃO replicar a correção para `migrate-motivos-perda.cjs`,
      `migrate-tarefas.cjs`, `migrate-sequencia-followup.cjs` ou qualquer outro
      `migrate-*.cjs`. É dívida de padrão do projeto, deferida.
    - NÃO apagar nenhum dos 13 backups já existentes em `data/`.
    - NÃO introduzir `drizzle-kit push`/`generate` em lugar nenhum.
  </action>
  <acceptance_criteria>
    - `grep -in "pare a app" scripts/migrate-interesse.cjs` retorna ≥ 1 linha, dentro do
      bloco de comentário do topo.
    - No arquivo, a avaliação de `hasColumn` (`PRAGMA table_info`) aparece **antes** da
      primeira ocorrência de `fs.copyFileSync`, e o `copyFileSync` está dentro de um
      bloco condicionado a `!hasColumn`.
    - Comportamento provado (a coluna `interesse` já existe em `data/crm.db`): contar
      `data/crm.db.backup-*` antes de rodar, executar `npm run migrate:interesse`, contar
      de novo — os dois números são **iguais** (hoje: 13) e o comando sai com exit 0.
    - A saída do script na execução idempotente menciona que a coluna já existe e que o
      backup não foi criado, e ainda imprime o log final de OK com a contagem de leads.
    - `data/crm.db` continua com os mesmos 37 leads: `npm run verify:schema` exit 0.
    - `git status --short scripts/` mostra apenas `migrate-interesse.cjs` modificado —
      nenhum outro `migrate-*.cjs` tocado (D-11).
  </acceptance_criteria>
  <verify>
    <automated>B=$(ls data/crm.db.backup-* 2>/dev/null | wc -l); npm run migrate:interesse; echo "migrate exit=$?"; A=$(ls data/crm.db.backup-* 2>/dev/null | wc -l); echo "backups antes=$B depois=$A"; test "$B" = "$A" && echo "OK sem backup novo" || echo "FALHOU backup acumulou"; npm run verify:schema; echo "schema exit=$?"; grep -in "pare a app" scripts/migrate-interesse.cjs</automated>
  </verify>
  <done>`npm run migrate:interesse` roda idempotente em exit 0 sem criar backup novo; header documenta "pare a app Next antes de rodar"; nenhum outro `migrate-*.cjs` tocado.</done>
</task>

<task type="auto">
  <name>Task 3: Gate SC#5 — tsc + build + test:lead-actions em exit 0 sobre a fase inteira</name>
  <files>(nenhum — task de verificação; só corrige regressão se algum gate falhar)</files>
  <read_first>
    - `.planning/ROADMAP.md` §"Phase 16" — os 5 Success Criteria, em especial o SC#5.
    - `.planning/phases/16-corre-es-de-code-review-da-fase-15/16-01-SUMMARY.md` — o que o
      plano 16-01 entregou (validations.ts, test-lead-actions.cjs, csv-import.ts,
      csv-column-mapper.tsx).
    - `package.json` §scripts — `build`, `test:lead-actions`, `verify:schema`,
      `guard:no-hard-delete` e os demais gates `verify:*` do projeto.
    - `.planning/STATE.md` §Blockers/Concerns — a nota de 2026-08-29 sobre `npm run build`
      passar limpo com Turbopack no host de 4GB (fechar processos `node` antes ajuda).
  </read_first>
  <action>
    Rodar, **em série** (host de 4GB, nunca em paralelo), os três gates do SC#5 sobre o
    estado final da fase, mais os guards de regressão baratos do projeto:
    `npx tsc --noEmit`, `npm run build`, `npm run test:lead-actions`, `npm run verify:schema`
    e `npm run guard:no-hard-delete`. Antes do `build`, encerrar processos `node` órfãos
    para aliviar a RAM.

    Registrar os exit codes de cada comando no SUMMARY. Se algum gate falhar, corrigir a
    regressão introduzida por esta fase no arquivo responsável — **sem** alargar escopo:
    não corrigir dívida pré-existente aqui (o `npm run lint` do repo inteiro sai 1 com
    457 erros pré-existentes desde a Fase 8, e isso é trabalho da Fase 17 — não é falha
    deste gate).

    Confirmar também, por leitura do `git diff --stat` acumulado da fase, que exatamente
    6 arquivos foram modificados (`src/lib/validations.ts`, `src/lib/csv-import.ts`,
    `src/components/csv-import-preview-table.tsx`, `src/components/csv-column-mapper.tsx`,
    `scripts/migrate-interesse.cjs`, `scripts/test-lead-actions.cjs`), nenhum criado nem
    removido, e que nenhuma migração de schema nova foi introduzida.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` → exit 0.
    - `npm run build` → exit 0.
    - `npm run test:lead-actions` → exit 0, com contagem de casos maior que os 19 da
      Fase 15 e zero falhas.
    - `npm run verify:schema` → exit 0.
    - `npm run guard:no-hard-delete` → exit 0.
    - `git diff --stat` da fase lista exatamente os 6 arquivos acima, com 0 arquivos
      criados e 0 removidos.
    - Os exit codes dos 5 comandos estão registrados textualmente no
      `16-02-SUMMARY.md`.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit; echo "tsc=$?"; npm run build; echo "build=$?"; npm run test:lead-actions; echo "test=$?"; npm run verify:schema; echo "schema=$?"; npm run guard:no-hard-delete; echo "guard=$?"</automated>
  </verify>
  <done>Os 3 gates do SC#5 (`tsc --noEmit`, `npm run build`, `npm run test:lead-actions`) mais `verify:schema` e `guard:no-hard-delete` saem todos em exit 0, com os códigos registrados no SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arquivo CSV do parceiro → prévia renderizada no navegador do admin | conteúdo de terceiro é exibido aqui; React escapa por padrão |
| script `.cjs` local → `data/crm.db` (arquivo de produção com PII de leads) | integridade do banco real atravessa aqui; backup é a rede de segurança |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Information Disclosure | coluna `Interesse` em `previewColumns` (`csv-import-preview-table.tsx`) | accept | O dado exibido é o próprio CSV que o admin acabou de subir, na sessão dele, em ferramenta solo sem multi-usuário. Nenhum dado de outro contexto entra na célula. |
| T-16-06 | Tampering | render do valor de CSV na célula da prévia | mitigate | Renderizado como texto via JSX (`{row.original.interesse \|\| "—"}`), com escape automático do React; proibido `dangerouslySetInnerHTML` na célula nova. O molde copiado (coluna `notas`) já segue essa regra. |
| T-16-07 | Denial of Service | célula gigante renderizada na prévia | mitigate | `mapCsvRows` já entrega `interesse` cortado em 500 code points antes da prévia; `max-w-xs` + `whitespace-pre-line` contêm o layout. |
| T-16-08 | Tampering / integridade de dados | pular `fs.copyFileSync` em `scripts/migrate-interesse.cjs` (D-10) | mitigate | O backup só é pulado no ramo `hasColumn === true`, que **não executa nenhum DDL nem escrita** — apenas `PRAGMA`/`SELECT` de verificação. O ramo que escreve (`ALTER TABLE`) mantém checkpoint WAL + `copyFileSync` + `try/catch` com `fail()` intactos. Os 13 backups históricos em `data/` não são apagados. |
| T-16-09 | Repudiation | premissa operacional "app parada" durante a migração | mitigate | Documentada explicitamente no header do script (D-10 item 1); o script não consegue impedir escrita concorrente no `-wal`, então a mitigação é documental e o ramo idempotente (o único que roda hoje) não escreve nada. |
| T-16-SC | Tampering | instalações npm/pip/cargo | accept | N/A nesta fase — zero dependência nova, nenhum `npm install` em nenhuma task. O gate de legitimidade de pacote não se aplica. |
</threat_model>

<verification>
1. `npx tsc --noEmit` → exit 0.
2. `npm run build` → exit 0 (Turbopack; fechar processos `node` antes no host de 4GB).
3. `npm run test:lead-actions` → exit 0, > 19 casos, zero falhas.
4. `npm run verify:schema` → exit 0 e `npm run guard:no-hard-delete` → exit 0.
5. `grep -c 'accessorKey: "interesse"' src/components/csv-import-preview-table.tsx` → 1.
6. Contagem de `data/crm.db.backup-*` idêntica antes e depois de `npm run migrate:interesse`.
7. `git status --short scripts/` → só `migrate-interesse.cjs` e `test-lead-actions.cjs` modificados (D-11 respeitado).
8. `git diff --stat` da fase → exatamente 6 arquivos modificados, 0 criados, 0 removidos.
</verification>

<success_criteria>
- FIX-02 fechado: a prévia exibe "Interesse" no padrão de `notas` (ROADMAP SC#2) e o
  truncamento em 500 é observável pelo badge na célula (ROADMAP SC#3, via "aviso visível
  na prévia" — o corte por code point veio no 16-01).
- FIX-03 fechado por completo: IN-01 e IN-02 no 16-01; IN-03 aqui — doc "pare a app" +
  sem backup em execução idempotente (ROADMAP SC#4).
- ROADMAP SC#5 verde: `tsc --noEmit`, `npm run build` e `npm run test:lead-actions` em
  exit 0 com os casos novos incluídos.
- Zero feature funcional nova; `interesse` continua texto livre nullable; nenhum outro
  `migrate-*.cjs` tocado.
</success_criteria>

<output>
Criar `.planning/phases/16-corre-es-de-code-review-da-fase-15/16-02-SUMMARY.md` ao terminar,
registrando os exit codes dos 5 comandos do gate SC#5.
</output>
