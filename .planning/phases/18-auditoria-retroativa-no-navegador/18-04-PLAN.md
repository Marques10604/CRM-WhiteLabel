---
phase: 18-auditoria-retroativa-no-navegador
plan: 04
type: execute
wave: 4
depends_on: [18-03]
files_modified:
  - .planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-HUMAN-UAT.md
  - .planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-VERIFICATION.md
autonomous: true
requirements: [AUDIT-03]
must_haves:
  truths:
    - "Os 7 cenários de 04-HUMAN-UAT.md foram executados ao vivo no navegador com result + evidence gravados"
    - "O dashboard por urgência (Vencidos/Hoje/Próximos 7 dias), o CRUD de templates com 'um padrão por tipo', o preview de WhatsApp editável e o auto-gatilho de 1º contato nas 3 superfícies foram observados"
    - "Os fixes de review re-testados: boundary de 7 dias (CR-01), race no drag-to-Perdido (CR-02 / quick 260828-gna), stageChangedAt + limpeza de motivoPerda (WR-01/WR-02)"
    - "04-HUMAN-UAT.md está complete e 04-VERIFICATION.md foi para status: passed"
  artifacts:
    - path: ".planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-HUMAN-UAT.md"
      provides: "UAT ao vivo da Fase 4"
      contains: "status: complete"
    - path: ".planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-VERIFICATION.md"
      provides: "Verificação da Fase 4 promovida"
      contains: "status: passed"
  key_links:
    - from: "04-VERIFICATION.md frontmatter human_verification"
      to: "[] (esvaziado) + status: passed"
      via: "cada item do human_verification mapeado a um cenário pass do 04-HUMAN-UAT.md"
      pattern: "human_verification: \\[\\]"
---

<objective>
Executar ao vivo no navegador os 7 cenários já escritos de `04-HUMAN-UAT.md` (Follow-up
Dashboard & WhatsApp Outreach) contra o `data/crm.db` real, gravar os resultados
incrementalmente, promover `04-HUMAN-UAT.md` a `complete` e `04-VERIFICATION.md` de
`human_needed` para `passed` — fechando AUDIT-03.

Purpose: A Fase 4 tem 21/21 must-haves verificados só no nível de código; o
`04-VERIFICATION.md` lista 7 verificações humanas pendentes (frontmatter `human_verification`)
e 4 fixes de review marcados como "requires human verification". Nenhum navegador foi usado.
Output: `04-HUMAN-UAT.md` `complete` + `04-VERIFICATION.md` `passed` (`human_verification: []`).

**Precondição:** Chrome aberto com a extensão Claude conectada. Se as ferramentas retornarem
erro de conexão, levante checkpoint de setup e pare.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-auditoria-retroativa-no-navegador/18-CONTEXT.md
@.planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-HUMAN-UAT.md
@.planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-VERIFICATION.md
</context>

<operational_playbook>
Aprendizados OBRIGATÓRIOS da Fase 16 (host 4GB, SEM paralelo):

1. Subir o dev server: matar node órfãos → `npm run dev` em background → esperar "Ready" →
   abrir aba em http://localhost:3000.
2. `get_page_text` / `read_page` como fonte PRIMÁRIA. Screenshot (toast, migração de coluna
   no board, layout) só com `wait` + retry — dão timeout sob pressão de RAM.
3. Formulários via `computer` `left_click` + `type` (NÃO `form_input`). `<Select>` Base UI:
   `left_click` no combobox + `left_click` na opção. Drag-and-drop no `/pipeline`: usar
   `computer` com sequência de `left_mouse_down` → move → `left_mouse_up` sobre a coluna alvo.
4. **NÃO poluir o banco.** Cenários que precisam de um lead num estado específico: preferir
   editar um lead de teste "UAT18 ..." criado para isso e soft-deletá-lo ao fim; NÃO abrir
   wa.me de verdade para leads reais do admin sem necessidade (a aba pode abrir; fechar logo).
   O clique em "Abrir WhatsApp" incrementa o contador de tentativas e pode auto-avançar a
   etapa — usar leads de teste para esses cenários (ver Fase 6, mas aqui também).
5. Gravar cada cenário em `04-HUMAN-UAT.md` ASSIM QUE executado (result + evidence). Retomada
   = primeiro cenário sem `result` != `[pending]`.
6. Fechar dev server + aba do Chrome ao terminar.
7. Bug não-trivial → `/gsd-quick` separado, `result: fail` + link no cenário e no SUMMARY.
   NÃO consertar inline. AUDIT-03 fecha mesmo com quick task aberta (SC#4); mas o
   `04-VERIFICATION.md` só vai a `passed` se 0 `fail` (D-05).
</operational_playbook>

<tasks>

<task type="auto">
  <name>Task 1: Executar cenários 1-4 de 04-HUMAN-UAT.md (dashboard, templates, preview, auto-gatilho)</name>
  <read_first>
    - .planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-HUMAN-UAT.md (cenários 1-4, com o `expected:` de cada)
    - .planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-VERIFICATION.md (frontmatter human_verification — o detalhe do que observar em cada teste)
    - O <operational_playbook>
  </read_first>
  <files>.planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-HUMAN-UAT.md</files>
  <action>
    Subir o dev server (playbook).
    Cenário 1 (`/`): confirmar as 3 seções por urgência (Vencidos / Hoje / Próximos 7 dias),
    que clicar num item abre o modal de edição, o estado "Tudo em dia!" quando não há
    follow-up (se o banco real tiver follow-ups, descrever o que aparece e marcar como
    observado), e que "Novo lead" abre o LeadFormDialog em modo criação SEM navegar.
    Cenário 2 (`/templates`): criar um template de cada tipo com variáveis `{nome}`/`{nicho}`/`{origem}`,
    marcar um como padrão, criar um segundo do mesmo tipo e marcá-lo padrão → o primeiro
    perde o badge "Padrão"; editar um; excluir com confirmação. Usar nomes "UAT18 Template ...".
    Ao fim, excluir os templates de teste (hard-delete é o comportamento da Fase 4, D-13).
    Cenário 3 (`/` e `/pipeline`): clicar o botão WhatsApp → preview abre com a mensagem
    preenchida; editar a textarea → o link "Abrir WhatsApp" muda ao vivo; lead com telefone
    inválido → botão desabilitado com tooltip; no `/pipeline`, clicar no botão NÃO inicia
    drag nem abre a edição do card, e arrastar o card ainda funciona. Usar um lead de teste
    "UAT18 ..." para o clique que dispara wa.me / contador.
    Cenário 4 (WA-04): criar um lead novo a partir de `/`, de `/leads` e de `/pipeline` — em
    cada um confirmar que o preview de 1º contato abre automaticamente com o subtítulo
    "Sugestão: enviar mensagem de primeiro contato para {nome}."; fechar sem enviar → lead
    permanece salvo; editar um lead existente → o preview NÃO abre. Usar leads "UAT18 WA04 ..."
    e soft-deletá-los ao fim.
    Gravar `result:` + `evidence:` após cada cenário. Atualizar `## Summary`.
  </action>
  <acceptance_criteria>
    - Cenários 1-4 com `result:` != `[pending]` e bloco `evidence:` (screenshot salvo ou trecho de `get_page_text`)
    - Cenário 2 comprova "um padrão por tipo" (primeiro perde o badge ao marcar o segundo)
    - Cenário 4 comprova auto-gatilho nas 3 superfícies e a ausência na edição
    - Templates/leads "UAT18 ..." removidos/soft-deletados ao fim
    - Qualquer `fail` tem quick task em `.planning/quick/` + link
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/Vencedor/Desktop/crm-leads && grep -c 'result: pass\|result: fail\|result: skipped' .planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-HUMAN-UAT.md</automated>
    <manual>Contagem >= 4; cenários 1-4 com evidence</manual>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Executar cenários 5-7 (boundary 7 dias, race drag-to-Perdido, stageChangedAt/motivoPerda) + fechar 04-HUMAN-UAT.md</name>
  <read_first>
    - .planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-HUMAN-UAT.md (cenários 5-7 + ## Summary)
    - .planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-VERIFICATION.md (os 3 últimos itens de human_verification — CR-01, CR-02, WR-01/WR-02)
    - STATE.md (quick 260828-gna — o fix do deadlock do drag para "Perdido"; re-teste manual do drag estava pendente)
  </read_first>
  <files>.planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-HUMAN-UAT.md</files>
  <action>
    Cenário 5 (CR-01): criar/editar um lead de teste "UAT18 Boundary" com follow-up exatamente
    hoje+7 → confirmar que ele aparece na seção "Próximos 7 dias" no dashboard `/` (não some
    de todos os grupos).
    Cenário 6 (CR-02 / quick 260828-gna): no `/pipeline`, arrastar dois leads de teste
    diferentes para "Perdido" em sequência rápida, antes de dispensar o primeiro modal de
    motivo. Confirmar que o modal abre sem freeze, que o card NÃO se move no drop até
    "Salvar motivo", que "Cancelar" descarta (card não move), e que ambas as transições
    persistem após refresh quando os dois motivos são salvos. Usar leads "UAT18 Perdido 1/2".
    Cenário 7 (WR-01/WR-02): criar um lead de teste direto em "Contatado" → confirmar que ele
    fica elegível para o indicador "esfriando" após o prazo (checar `stage_changed_at` via
    query no `data/crm.db` — deve estar preenchido na criação); reativar um lead que estava
    "Perdido" (via form e via drag no pipeline) → confirmar que `motivo_perda` foi limpo no
    banco (query).
    Soft-deletar todos os leads de teste "UAT18 ..." ao fim. Fechar a aba e o dev server.
    Atualizar `## Summary` (total 7). Se 0 `fail` e 0 `pending` → frontmatter da
    `04-HUMAN-UAT.md`: `status: complete`, adicionar
    `method: browser UAT retroativa (Fase 18 / AUDIT-03) — extensão Claude no Chrome contra data/crm.db real`,
    `issues: 0`, `pending: 0`. Se houver `fail` → manter `partial`, `issues: <n>`, `## Gaps`,
    e PARAR antes da Task 3.
  </action>
  <acceptance_criteria>
    - Cenários 5-7 com `result:` != `[pending]` e `evidence:`
    - Cenário 5: evidência mostra o lead em "Próximos 7 dias"
    - Cenário 6: evidência confirma o comportamento do fix da quick 260828-gna (sem freeze, card não move no drop, Cancelar limpa)
    - Cenário 7: evidência = query no `data/crm.db` (stage_changed_at preenchido; motivo_perda limpo ao sair de Perdido)
    - `04-HUMAN-UAT.md` `status: complete` (se 0 fail) com `method:` no frontmatter
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/Vencedor/Desktop/crm-leads && grep -c 'result: \[pending\]' .planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-HUMAN-UAT.md</automated>
    <manual>0 `[pending]`; frontmatter status coerente</manual>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Promover 04-VERIFICATION.md para status: passed</name>
  <read_first>
    - .planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-VERIFICATION.md (frontmatter human_verification — os 7 itens)
    - .planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-HUMAN-UAT.md (resultados finais)
  </read_first>
  <files>.planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-VERIFICATION.md</files>
  <action>
    SÓ executar se `04-HUMAN-UAT.md` está `status: complete` com 0 issues e 0 pending (D-05).
    Editar `04-VERIFICATION.md`:
      - frontmatter: `status: human_needed` → `status: passed`; `human_verification:` → `[]`
        (lista esvaziada); adicionar
        `human_verification_closed_by: "Fase 18 / AUDIT-03 — browser UAT ao vivo, ver 04-HUMAN-UAT.md"`.
      - No corpo, adicionar uma seção "## Human Verification — Fechado (Fase 18)" com uma
        tabela: cada um dos 7 itens antes pendentes → nº do cenário de `04-HUMAN-UAT.md` +
        `result: pass` + resumo de 1 linha da evidência.
      - Atualizar a linha "Status:" e "Gaps Summary" para refletir `passed`. Se alguma quick
        task ficou aberta (cenário `skipped` aceito, não `fail`), listar com nota "não bloqueia
        AUDIT-03 (SC#4)".
    Não mexer no resto do relatório (a parte code-level continua válida).
  </action>
  <acceptance_criteria>
    - `04-VERIFICATION.md` frontmatter `status: passed` e `human_verification: []`
    - Seção nova mapeando os 7 itens de verificação humana a cenários `pass` de `04-HUMAN-UAT.md`
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/Vencedor/Desktop/crm-leads && grep -q 'status: passed' .planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-VERIFICATION.md && grep -q 'human_verification: \[\]' .planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-VERIFICATION.md && grep -q 'status: complete' .planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-HUMAN-UAT.md && echo OK</automated>
    <manual>VERIFICATION passed só com HUMAN-UAT complete</manual>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma) | Verificação de comportamento já shipado. Não altera runtime, sem rede (wa.me abre em aba mas não envia nada), sem auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | `data/crm.db` real (leads/templates de teste + contador de tentativas + auto-avanço de etapa disparados pelo clique em WhatsApp) | mitigate | Todo lead/template de teste com prefixo "UAT18 "; cenários que disparam contador/auto-avanço usam SÓ leads de teste; soft-delete dos leads e hard-delete dos templates de teste ao fim de cada task; registrar no `04-HUMAN-UAT.md` o que foi criado e a limpeza; nunca `DELETE FROM leads`. |
| T-18-SC | Tampering | dependências npm | accept | Nenhum pacote instalado nesta fase. |
</threat_model>

<verification>
- `04-HUMAN-UAT.md`: 7 cenários, 0 `[pending]`, `status: complete` (se 0 fail), `method:` no frontmatter
- `04-VERIFICATION.md`: `status: passed`, `human_verification: []`
- `data/crm.db` sem leads/templates "UAT18 ..." ao fim
- Toda `fail` tem quick task em `.planning/quick/`
- Dev server + aba do Chrome encerrados
</verification>

<success_criteria>
- AUDIT-03 fechado: `04-HUMAN-UAT.md` complete + `04-VERIFICATION.md` passed
- Os 7 cenários (incl. os 3 fixes de review) executados ao vivo com evidência
</success_criteria>

<output>
Criar `.planning/phases/18-auditoria-retroativa-no-navegador/18-04-SUMMARY.md` ao terminar,
com `## Issues Encontradas` (quick tasks abertas, se houver).
</output>
