---
phase: 18-auditoria-retroativa-no-navegador
plan: 03
type: execute
wave: 3
depends_on: [18-02]
files_modified:
  - .planning/phases/02-csv-bulk-import/02-HUMAN-UAT.md
  - .planning/phases/02-csv-bulk-import/02-VERIFICATION.md
autonomous: true
requirements: [AUDIT-02]
must_haves:
  truths:
    - "02-HUMAN-UAT.md existe do zero cobrindo upload (detecção de separador/codificação + estados de erro), mapeamento de colunas, prévia com as 3 flags e import real no banco"
    - "Upload e detecção de separador/codificação verificados ao vivo com CSVs de fixture (vírgula, ponto-e-vírgula, windows-1252)"
    - "As 3 flags da prévia (Duplicado, Nicho novo, Nicho bloqueado) foram exercidas e observadas ao vivo"
    - "Um import REAL mínimo (1-2 linhas UAT18) foi confirmado, redirecionou para /importar/[batchId], gravou import_batch_id compartilhado, e os leads de teste foram soft-deletados depois"
    - "02-VERIFICATION.md criado com status: passed"
  artifacts:
    - path: ".planning/phases/02-csv-bulk-import/02-HUMAN-UAT.md"
      provides: "Roteiro + resultados ao vivo da Fase 2"
      contains: "method:"
    - path: ".planning/phases/02-csv-bulk-import/02-VERIFICATION.md"
      provides: "Verificação da Fase 2 promovida a passed"
      contains: "status: passed"
  key_links:
    - from: "02-HUMAN-UAT.md cenário de import real"
      to: "data/crm.db (import_batch_id, nicho novo criado)"
      via: "query direta pós-confirmação + soft-delete de limpeza"
      pattern: "import_batch_id"
---

<objective>
Autorar `02-HUMAN-UAT.md` do zero e executar ao vivo no navegador toda a Fase 2 (CSV Bulk
Import): upload com detecção de separador/codificação, mapeamento de colunas, prévia com as
flags Duplicado / Nicho novo / Nicho bloqueado, e UM import real mínimo no `data/crm.db`.
Criar `02-VERIFICATION.md` `passed` — fechando AUDIT-02.

Purpose: A Fase 2 nunca teve navegador — os 3 SUMMARYs registram "nenhuma sessão headless
teve acesso a navegador" e o próprio 02-CONTEXT alertava que nenhum CSV real do cowork tinha
sido validado contra as suposições de delimitador/encoding.
Output: `02-HUMAN-UAT.md` `complete` + `02-VERIFICATION.md` `passed`.

**Precondição:** Chrome aberto com a extensão Claude conectada. Se as ferramentas retornarem
erro de conexão, levante checkpoint de setup e pare.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-auditoria-retroativa-no-navegador/18-CONTEXT.md
@.planning/phases/02-csv-bulk-import/02-01-SUMMARY.md
@.planning/phases/02-csv-bulk-import/02-02-SUMMARY.md
@.planning/phases/02-csv-bulk-import/02-03-SUMMARY.md
@.planning/phases/02-csv-bulk-import/02-UI-SPEC.md
@.planning/phases/16-corre-es-de-code-review-da-fase-15/16-HUMAN-UAT.md
</context>

<operational_playbook>
Aprendizados OBRIGATÓRIOS da Fase 16 (host 4GB, SEM paralelo):

1. Subir o dev server: matar node órfãos → `npm run dev` em background → esperar "Ready" →
   abrir aba em http://localhost:3000.
2. `get_page_text` / `read_page` como fonte PRIMÁRIA. Screenshot só com `wait` + retry.
3. **Upload:** `file_upload` FUNCIONA (testado na Fase 16). Achar o `ref` do `<input type=file>`
   via `read_page`; NUNCA clicar no botão de upload (abre picker nativo invisível).
4. `<Select>` de mapeamento (Base UI): `left_click` no combobox + `left_click` na opção.
   Checkbox "Importar mesmo assim" e `SubnichoCombobox` inline: `left_click` direto.
5. **NÃO confirmar imports que poluam o banco** — a prévia já prova o comportamento. EXCEÇÃO:
   o cenário 13 (import real) usa um CSV mínimo (1-2 linhas, nome "UAT18 ImportReal <n>") e
   limpa depois (soft-delete dos leads via `/leads` ou `/lixeira`; o nicho novo "UAT18 ..."
   pode ficar ou ser soft-deletado via `/nichos`). Registrar no cenário o que foi criado e a
   limpeza.
6. Gravar cada cenário em `02-HUMAN-UAT.md` ASSIM QUE executado (result + evidence). Retomada
   = primeiro cenário sem `result`.
7. Fechar dev server + aba do Chrome ao terminar.
8. Bug não-trivial → `/gsd-quick` separado, `result: fail` + link no cenário e no SUMMARY.
   NÃO consertar inline.
</operational_playbook>

<tasks>

<task type="auto">
  <name>Task 1: Autorar 02-HUMAN-UAT.md + preparar os CSVs de fixture</name>
  <read_first>
    - .planning/phases/02-csv-bulk-import/02-01-SUMMARY.md (decodeCsvFile: BOM/UTF-8/windows-1252; csvRowSchema; bulkImportLeads transacional + importBatchId compartilhado)
    - .planning/phases/02-csv-bulk-import/02-02-SUMMARY.md (wizard 3 passos; estados de erro "Não foi possível ler este arquivo" / "Nenhuma linha válida encontrada"; nome+telefone obrigatórios; "— nenhuma —"; 3 badges de flag; border-l vermelho; "Confirmar importação" desabilitado com linha bloqueada)
    - .planning/phases/02-csv-bulk-import/02-03-SUMMARY.md (redirect para /importar/[batchId] após confirmar)
    - .planning/phases/02-csv-bulk-import/02-UI-SPEC.md (cores/ícones exatos das flags, copy literal)
    - .planning/phases/16-corre-es-de-code-review-da-fase-15/16-HUMAN-UAT.md (formato)
  </read_first>
  <files>.planning/phases/02-csv-bulk-import/02-HUMAN-UAT.md</files>
  <action>
    Criar os CSVs de fixture em `.planning/phases/18-auditoria-retroativa-no-navegador/fixtures/`
    (pasta nova; arquivos versionáveis, sem PII real):
      - `uat18-virgula.csv` — 3 linhas, separador `,`, UTF-8, colunas nome/telefone/nicho/notas
      - `uat18-ponto-virgula.csv` — 3 linhas, separador `;`
      - `uat18-win1252.csv` — 2 linhas com acentos (ç, ã, é) salvo em windows-1252
      - `uat18-ilegivel.bin` — alguns bytes binários / não-texto
      - `uat18-sem-linhas.csv` — só o cabeçalho, sem linhas de dados
      - `uat18-flags.csv` — 4 linhas: (a) telefone que JÁ existe no data/crm.db (consultar via
        query antes para pegar um telefone real) → flag Duplicado; (b) telefone repetido
        DUAS vezes dentro do arquivo → Duplicado dentro do lote; (c) nicho inédito
        "UAT18 NichoNovo" → flag Nicho novo; (d) nicho = um nicho soft-deletado (se não
        houver, criar e soft-deletar "UAT18 NichoBloq" antes) → flag Nicho bloqueado
      - `uat18-import-real.csv` — 2 linhas, nomes "UAT18 ImportReal 1" e "UAT18 ImportReal 2",
        telefones válidos novos, nicho "UAT18 ImportRealNicho" (inédito)

    Criar `02-HUMAN-UAT.md` no formato de `16-HUMAN-UAT.md`. Frontmatter: `status: partial`,
    `phase: 02-csv-bulk-import`, `source: [02-01-SUMMARY.md, 02-02-SUMMARY.md, 02-03-SUMMARY.md, 02-UI-SPEC.md]`,
    `method: "browser automation (extensão Claude no Chrome) contra dev server localhost:3000 + data/crm.db real"`,
    `issues: 0`, `pending: 13`.
    `## Tests` com os cenários (cada um `expected:` + `result: [pending]`):
      1. Upload `uat18-virgula.csv` → indicador "Detectado: separador , / codificação UTF-8".
      2. Upload `uat18-ponto-virgula.csv` → detecta separador `;`.
      3. Upload `uat18-win1252.csv` → acentos corretos na prévia (sem mojibake), indicador
         "codificação windows-1252".
      4. Upload `uat18-ilegivel.bin` → "Não foi possível ler este arquivo", volta ao passo upload.
      5. Upload `uat18-sem-linhas.csv` → "Nenhuma linha válida encontrada".
      6. Passo mapeamento: "Ver prévia" fica bloqueado até nome E telefone estarem mapeados;
         campos opcionais têm a opção "— nenhuma —".
      7. Mapear nome/telefone/nicho/notas e avançar para a prévia.
      8. Prévia mostra TODAS as linhas do arquivo (sem paginação), com o resumo/contadores reais.
      9. Flag "Duplicado" (telefone já no banco) → badge com a cor/ícone da UI-SPEC + checkbox
         "Importar mesmo assim".
      10. Flag "Duplicado dentro do lote" (telefone repetido no arquivo) → badge.
      11. Flag "Nicho novo" → badge; a prévia indica que o nicho será criado.
      12. Flag "Nicho bloqueado" (nicho soft-deletado) → badge, `border-l` vermelho,
          `SubnichoCombobox` inline; "Confirmar importação" DESABILITADO enquanto a linha
          bloqueada não for resolvida; resolver via combobox → botão habilita.
      13. Import REAL: `uat18-import-real.csv` → mapear → prévia → "Confirmar importação" →
          redireciona para `/importar/[batchId]` com os 2 leads e o botão "Enviar WhatsApp";
          query no `data/crm.db` confirma os 2 leads com o MESMO `import_batch_id` e o nicho
          "UAT18 ImportRealNicho" criado 1x; DEPOIS soft-deletar os 2 leads.
    Fechar com `## Summary` e `## Gaps`.
  </action>
  <acceptance_criteria>
    - `02-HUMAN-UAT.md` existe com 13 cenários, cada um `expected:` + `result: [pending]`, frontmatter com `method:`
    - Os 7 CSVs de fixture existem em `.planning/phases/18-.../fixtures/`
    - `uat18-flags.csv` usa um telefone real lido do `data/crm.db` para o caso Duplicado
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/Vencedor/Desktop/crm-leads && test -f .planning/phases/02-csv-bulk-import/02-HUMAN-UAT.md && ls .planning/phases/18-auditoria-retroativa-no-navegador/fixtures/ | grep -c csv</automated>
    <manual>13 `result:` no HUMAN-UAT; >=5 CSVs de fixture</manual>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Executar upload + detecção + mapeamento (cenários 1-8)</name>
  <read_first>
    - .planning/phases/02-csv-bulk-import/02-HUMAN-UAT.md (cenários 1-8)
    - .planning/phases/02-csv-bulk-import/02-02-SUMMARY.md
    - O <operational_playbook> (especialmente o item 3 sobre file_upload via ref)
  </read_first>
  <files>.planning/phases/02-csv-bulk-import/02-HUMAN-UAT.md</files>
  <action>
    Subir o dev server (playbook). Abrir `/importar`.
    Executar os cenários 1-8 na ordem. Para cada upload: `read_page` para achar o `ref` do
    `<input type=file>`, depois `file_upload` com o caminho absoluto do fixture. Ler o
    indicador de separador/codificação e os estados de erro via `get_page_text`.
    Cenário 6: no passo de mapeamento, confirmar que o CTA "Ver prévia" está desabilitado
    antes de mapear nome+telefone e habilita depois; confirmar a opção "— nenhuma —" nos
    selects opcionais.
    Gravar `result:` + `evidence:` após cada cenário (incremental). Atualizar `## Summary`.
  </action>
  <acceptance_criteria>
    - Cenários 1-8 com `result:` != `[pending]` e `evidence:` (trecho de `get_page_text` ou screenshot)
    - Cenário 3 comprova acentos sem mojibake (windows-1252)
    - Cenários 4 e 5 comprovam a copy literal de erro e o retorno ao passo upload
    - Qualquer `fail` tem quick task em `.planning/quick/` + link
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/Vencedor/Desktop/crm-leads && grep -c 'result: pass\|result: fail\|result: skipped' .planning/phases/02-csv-bulk-import/02-HUMAN-UAT.md</automated>
    <manual>Contagem >= 8; cenários 1-8 com evidence</manual>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Executar prévia com flags + import real + criar 02-VERIFICATION.md passed</name>
  <read_first>
    - .planning/phases/02-csv-bulk-import/02-HUMAN-UAT.md (cenários 9-13 + ## Summary)
    - .planning/phases/02-csv-bulk-import/02-01-SUMMARY.md (importBatchId compartilhado; subnichoLookupCondition entre prévia e confirm)
    - .planning/phases/02-csv-bulk-import/02-03-SUMMARY.md (redirect /importar/[batchId])
    - .planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-VERIFICATION.md (formato de VERIFICATION)
  </read_first>
  <files>
    .planning/phases/02-csv-bulk-import/02-HUMAN-UAT.md
    .planning/phases/02-csv-bulk-import/02-VERIFICATION.md
  </files>
  <action>
    Com o dev server de pé: executar os cenários 9-12 (flags na prévia) usando
    `uat18-flags.csv`, gravando `result:` + `evidence:` (screenshot da tabela de prévia ou
    trecho de `read_page` mostrando os badges e o estado do botão "Confirmar importação").
    Cenário 13 (import real): subir `uat18-import-real.csv`, mapear, ir à prévia, clicar
    "Confirmar importação". Confirmar o redirect para `/importar/[batchId]`. Rodar
    `node -e` contra `data/crm.db`: `SELECT id, nome, import_batch_id FROM leads WHERE nome LIKE 'UAT18 ImportReal%'`
    (mesmo batch id nos 2) e `SELECT count(*) FROM subnichos WHERE nome = 'UAT18 ImportRealNicho'`
    (== 1). Colar a saída no `evidence:`. DEPOIS: soft-deletar os 2 leads via `/leads`
    (botão excluir + confirmar) OU via `restoreLead`/`softDeleteLead` — registrar no cenário.
    Fechar a aba do Chrome e o dev server.
    Atualizar `## Summary` (total 13). Se 0 `fail` e 0 `pending` → frontmatter
    `status: complete`, `issues: 0`, `pending: 0`. Se houver `fail` → `partial` + `## Gaps` e
    PARAR (não criar o VERIFICATION — D-05).

    Se `02-HUMAN-UAT.md` está `complete`: criar `02-VERIFICATION.md` no molde de
    `04-VERIFICATION.md`. Frontmatter `phase: 02-csv-bulk-import`, `verified: <ISO>`,
    `status: passed`,
    `method: browser UAT retroativa (Fase 18 / AUDIT-02) — extensão Claude no Chrome contra data/crm.db real`,
    `human_verification: []`. Corpo: "Observable Truths" cobrindo IMPORT-01, IMPORT-02
    (dedup dentro do lote + contra o banco), IMPORT-03 (encoding/separador), LEAD-05
    (importBatchId) → cada um mapeado ao(s) cenário(s) `pass`. "Gaps Summary": nenhum, ou as
    quick tasks abertas (não bloqueiam AUDIT-02, SC#4).
  </action>
  <acceptance_criteria>
    - Cenários 9-13 com `result:` != `[pending]` e `evidence:`
    - Cenário 12 comprova o botão "Confirmar importação" desabilitado→habilitado ao resolver a linha bloqueada
    - Cenário 13: evidência = saída de query mostrando mesmo `import_batch_id` nos 2 leads + nicho criado 1x; os 2 leads soft-deletados depois
    - `02-HUMAN-UAT.md` `status: complete` (se 0 fail); `02-VERIFICATION.md` `status: passed` + `human_verification: []`
    - Nenhum lead "UAT18 ..." ativo em `/leads` ao fim
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/Vencedor/Desktop/crm-leads && grep -q 'status: passed' .planning/phases/02-csv-bulk-import/02-VERIFICATION.md && grep -c 'result: \[pending\]' .planning/phases/02-csv-bulk-import/02-HUMAN-UAT.md</automated>
    <manual>0 `[pending]`; VERIFICATION passed só com HUMAN-UAT complete</manual>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arquivo CSV → wizard de importação | Entrada não confiável (arquivo do cowork) atravessa aqui — mas o comportamento já foi shipado e validado por Zod/`csvRowSchema`; este plano só EXERCITA, não altera. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | `data/crm.db` real (import de teste) | mitigate | Só o cenário 13 confirma import real, com CSV mínimo (2 linhas "UAT18 ImportReal"); soft-delete dos leads logo após a verificação da query; nicho de teste "UAT18 ..." soft-deletável via `/nichos`; nunca `DELETE FROM`. Todos os outros cenários param na prévia (não confirmam). |
| T-18-02 | Information Disclosure | fixtures CSV versionados | mitigate | Fixtures contêm só dados fabricados "UAT18 ..."; o único dado real é UM número de telefone lido do banco para o caso Duplicado — usar um telefone que já está no banco (não expõe nada novo) e não commitar nome/PII junto. Se preferir, gerar o caso Duplicado apontando para um lead de teste criado na hora. |
| T-18-SC | Tampering | dependências npm | accept | Nenhum pacote instalado nesta fase. |
</threat_model>

<verification>
- `02-HUMAN-UAT.md`: 13 cenários, 0 `[pending]`, `status: complete` (se 0 fail)
- `02-VERIFICATION.md`: `status: passed`, `human_verification: []`
- `data/crm.db` sem leads "UAT18 ..." ativos; import_batch_id compartilhado comprovado por query
- Toda `fail` tem quick task em `.planning/quick/`
- Dev server + aba do Chrome encerrados
</verification>

<success_criteria>
- AUDIT-02 fechado: `02-HUMAN-UAT.md` complete + `02-VERIFICATION.md` passed
- Upload/detecção, mapeamento, as 3 flags e 1 import real executados ao vivo com evidência
</success_criteria>

<output>
Criar `.planning/phases/18-auditoria-retroativa-no-navegador/18-03-SUMMARY.md` ao terminar,
com `## Issues Encontradas` (quick tasks abertas, se houver).
</output>
