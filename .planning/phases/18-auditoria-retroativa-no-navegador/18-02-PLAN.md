---
phase: 18-auditoria-retroativa-no-navegador
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - .planning/phases/01-lead-sub-nicho-foundation/01-HUMAN-UAT.md
  - .planning/phases/01-lead-sub-nicho-foundation/01-VERIFICATION.md
autonomous: true
requirements: [AUDIT-01]
must_haves:
  truths:
    - "Lista de leads ordenada por follow-up mais próximo verificada ao vivo (sort default + toggle de coluna)"
    - "Filtros da toolbar (nicho, etapa, intervalo de follow-up) verificados isolados e combinados, e o botão Limpar filtros"
    - "Paginação 25/página verificada com o volume real (~44 leads) do data/crm.db"
    - "01-HUMAN-UAT.md está complete (todos os 20 cenários com result, 0 pending, 0 issues)"
    - "01-VERIFICATION.md criado com status: passed"
  artifacts:
    - path: ".planning/phases/01-lead-sub-nicho-foundation/01-VERIFICATION.md"
      provides: "Relatório de verificação da Fase 1 promovido a passed pela UAT ao vivo"
      contains: "status: passed"
    - path: ".planning/phases/01-lead-sub-nicho-foundation/01-HUMAN-UAT.md"
      provides: "UAT completo da Fase 1"
      contains: "status: complete"
  key_links:
    - from: "01-VERIFICATION.md (must-haves)"
      to: "01-HUMAN-UAT.md (cenários pass)"
      via: "status: complete como precondição de passed (D-05)"
      pattern: "01-HUMAN-UAT"
---

<objective>
Terminar a UAT ao vivo da Fase 1: executar no navegador os cenários 13-20 de
`01-HUMAN-UAT.md` (ordenação, filtros da toolbar, paginação) contra o `data/crm.db` real,
fechar o `01-HUMAN-UAT.md` como `complete`, e criar `01-VERIFICATION.md` com `status: passed`
— fechando AUDIT-01.

Purpose: A lista de leads é a superfície central da Fase 1. Os filtros/sort/paginação foram
verificados só por scripts headless `@tanstack/react-table` na época (01-03-SUMMARY) — nunca
renderizados num navegador.
Output: `01-HUMAN-UAT.md` `complete` + `01-VERIFICATION.md` `passed`.

**Precondição:** Chrome aberto com a extensão Claude conectada. Se as ferramentas retornarem
erro de conexão, levante checkpoint de setup e pare. `01-HUMAN-UAT.md` já deve existir com os
cenários 1-12 resolvidos (plano 18-01).
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-auditoria-retroativa-no-navegador/18-CONTEXT.md
@.planning/phases/01-lead-sub-nicho-foundation/01-SPEC.md
@.planning/phases/01-lead-sub-nicho-foundation/01-03-SUMMARY.md
@.planning/phases/01-lead-sub-nicho-foundation/01-HUMAN-UAT.md
@.planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-VERIFICATION.md
</context>

<operational_playbook>
Mesmos aprendizados OBRIGATÓRIOS da Fase 16 (host 4GB, SEM paralelo):

1. Subir o dev server: matar node órfãos → `npm run dev` em background → esperar "Ready" →
   abrir aba em http://localhost:3000.
2. `get_page_text` / `read_page` como fonte PRIMÁRIA. Screenshot só com `wait` + retry.
3. `<Select>` do Base UI: `left_click` no combobox + `left_click` na opção (ref do `read_page`
   ou coordenada). `<Calendar>` do date-picker de intervalo: idem, dentro do `<Popover>`.
4. Gravar cada cenário em `01-HUMAN-UAT.md` ASSIM QUE executado (result + evidence). Retomada =
   primeiro cenário sem `result`.
5. Fechar dev server + aba do Chrome ao terminar.
6. Bug não-trivial → `/gsd-quick` separado, `result: fail` + link no cenário e na seção
   "## Issues Encontradas" do SUMMARY. NÃO consertar inline.

Evidência aceitável: trecho de `get_page_text`/`read_page` mostrando a ordem/contagem de
linhas esperada, OU screenshot salvo, OU query no `data/crm.db`.
</operational_playbook>

<tasks>

<task type="auto">
  <name>Task 1: Executar ordenação + filtros da toolbar (cenários 13-19)</name>
  <read_first>
    - .planning/phases/01-lead-sub-nicho-foundation/01-HUMAN-UAT.md (cenários 13-19)
    - .planning/phases/01-lead-sub-nicho-foundation/01-03-SUMMARY.md (filterFn por coluna: nicho por id, follow-up por intervalo inclusivo com startOfDay/endOfDay; "Limpar filtros")
    - O <operational_playbook>
  </read_first>
  <files>.planning/phases/01-lead-sub-nicho-foundation/01-HUMAN-UAT.md</files>
  <action>
    Subir o dev server (playbook). Abrir `/leads`.
    Cenário 13: sem filtro, capturar as primeiras ~5 linhas via `get_page_text` e confirmar
    que as datas de follow-up estão em ordem crescente (mais próximo no topo).
    Cenário 14: clicar o cabeçalho "Nome" → confirmar reordenação asc; clicar de novo → desc;
    idem para "Follow-up"; confirmar que "Telefone" não reordena.
    Cenário 15: abrir o `<Select>` de nicho na toolbar, escolher um nicho com leads reais →
    a tabela só mostra leads daquele nicho.
    Cenário 16: filtro por etapa (ex.: "Contatado") → só leads nessa etapa.
    Cenário 17: intervalo de follow-up (início/fim) cobrindo parte dos leads → só entram os
    leads dentro do intervalo, inclusive os que caem exatamente na data-início e na data-fim.
    Cenário 18: nicho + etapa juntos → interseção (AND).
    Cenário 19: "Limpar filtros" → todos os leads voltam, sort volta ao default.
    Gravar `result:` + `evidence:` após cada cenário. Atualizar `## Summary`.
  </action>
  <acceptance_criteria>
    - Cenários 13-19 com `result:` != `[pending]` e bloco `evidence:`
    - Cenário 17 comprova inclusividade nas duas pontas do intervalo
    - Cenário 18 comprova AND (não OR)
    - Qualquer `fail` tem quick task em `.planning/quick/` + link no cenário
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/Vencedor/Desktop/crm-leads && grep -c 'result: pass\|result: fail\|result: skipped' .planning/phases/01-lead-sub-nicho-foundation/01-HUMAN-UAT.md</automated>
    <manual>Contagem >= 19; cenários 13-19 com evidence</manual>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Executar paginação (cenário 20) + fechar 01-HUMAN-UAT.md como complete</name>
  <read_first>
    - .planning/phases/01-lead-sub-nicho-foundation/01-HUMAN-UAT.md (cenário 20 + ## Summary)
    - .planning/phases/01-lead-sub-nicho-foundation/01-03-SUMMARY.md (paginação 25/página, "Página X de Y", reset para página 1 ao trocar filtro)
  </read_first>
  <files>.planning/phases/01-lead-sub-nicho-foundation/01-HUMAN-UAT.md</files>
  <action>
    Com o dev server de pé e ~44 leads reais: em `/leads` sem filtro, confirmar 25 linhas,
    indicador "Página 1 de N", "Anterior" desabilitado; clicar "Próximo" → página 2 com o
    restante, "Próximo" desabilitado; aplicar filtro que reduza a <25 resultados → volta para
    a página 1. Gravar `result:` + `evidence:` no cenário 20.
    Fechar a aba do Chrome e o dev server.
    Atualizar `## Summary` (total 20). Se 0 `fail` e 0 `pending`: frontmatter →
    `status: complete`, `issues: 0`, `pending: 0`. Se houver `fail`: manter `status: partial`,
    `issues: <n>`, listar em `## Gaps` e PARAR antes da Task 3 (D-05 — VERIFICATION não
    promove até a quick task fechar). Cenário que exija julgamento humano real (contraste de
    cor etc.) → `result: skipped` + motivo em `## Gaps`.
  </action>
  <acceptance_criteria>
    - Cenário 20 com `result:` != `[pending]` e `evidence:`
    - `01-HUMAN-UAT.md` `status: complete` SE 0 fail e 0 pending; senão `partial` + `## Gaps`
    - `## Summary` com total 20 e contadores batendo
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/Vencedor/Desktop/crm-leads && grep -c 'result: \[pending\]' .planning/phases/01-lead-sub-nicho-foundation/01-HUMAN-UAT.md</automated>
    <manual>Contagem de `[pending]` == 0; frontmatter status coerente com os resultados</manual>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Criar 01-VERIFICATION.md com status passed</name>
  <read_first>
    - .planning/phases/01-lead-sub-nicho-foundation/01-SPEC.md (§Acceptance Criteria — 10 itens = os must-have truths)
    - .planning/phases/01-lead-sub-nicho-foundation/01-HUMAN-UAT.md (resultados finais)
    - .planning/phases/04-follow-up-dashboard-whatsapp-outreach/04-VERIFICATION.md (formato de referência)
  </read_first>
  <files>.planning/phases/01-lead-sub-nicho-foundation/01-VERIFICATION.md</files>
  <action>
    SÓ executar se `01-HUMAN-UAT.md` está `status: complete` com 0 issues e 0 pending (D-05).
    Se não, parar e reportar no SUMMARY que a promoção fica pendente da(s) quick task(s).

    Criar `01-VERIFICATION.md` no molde de `04-VERIFICATION.md`. Frontmatter:
    `phase: 01-lead-sub-nicho-foundation`, `verified: <timestamp ISO>`, `status: passed`,
    `method: browser UAT retroativa (Fase 18 / AUDIT-01) — extensão Claude no Chrome contra data/crm.db real`,
    `human_verification: []`.
    Corpo: tabela "Observable Truths" mapeando os 10 Acceptance Criteria do 01-SPEC ao(s)
    cenário(s) de `01-HUMAN-UAT.md` que os provam (nº do cenário + `result: pass` + resumo da
    evidência). "Requirements Coverage": LEAD-01, LEAD-02, LEAD-03, LEAD-04, REMIND-02 →
    SATISFIED. "Gaps Summary": nenhum, ou lista as quick tasks abertas com nota de que não
    bloqueiam AUDIT-01 (SC#4).
    Não re-verificar código — a base já passou tsc/build/harness na época e nas Fases 16/17.
  </action>
  <acceptance_criteria>
    - `01-VERIFICATION.md` existe com frontmatter `status: passed` e `human_verification: []`
    - Cada um dos 10 Acceptance Criteria do 01-SPEC mapeado a >=1 cenário `pass`
    - Requirements Coverage lista LEAD-01, LEAD-02, LEAD-03, LEAD-04, REMIND-02
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/Vencedor/Desktop/crm-leads && grep -q 'status: passed' .planning/phases/01-lead-sub-nicho-foundation/01-VERIFICATION.md && grep -q 'status: complete' .planning/phases/01-lead-sub-nicho-foundation/01-HUMAN-UAT.md && echo OK</automated>
    <manual>VERIFICATION passed só com HUMAN-UAT complete</manual>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma) | Verificação de comportamento já shipado. Não altera runtime, sem rede, sem auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | `data/crm.db` real | mitigate | Este plano só LÊ da lista de leads (filtros/sort/paginação); não cria dados. Qualquer lead de teste residual do 18-01 já deve estar soft-deletado. |
| T-18-SC | Tampering | dependências npm | accept | Nenhum pacote instalado nesta fase. |
</threat_model>

<verification>
- `01-HUMAN-UAT.md`: 20 cenários, 0 `[pending]`, `status: complete` (se 0 fail)
- `01-VERIFICATION.md`: `status: passed`, `human_verification: []`, 10 ACs mapeados
- Toda `fail` tem quick task em `.planning/quick/`
- Dev server + aba do Chrome encerrados
</verification>

<success_criteria>
- AUDIT-01 fechado: `01-HUMAN-UAT.md` complete + `01-VERIFICATION.md` passed
- Cenários 13-20 executados ao vivo no navegador com evidência
</success_criteria>

<output>
Criar `.planning/phases/18-auditoria-retroativa-no-navegador/18-02-SUMMARY.md` ao terminar,
com a seção `## Issues Encontradas` (quick tasks abertas, se houver).
</output>
