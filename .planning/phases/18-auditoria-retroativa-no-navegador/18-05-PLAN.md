---
phase: 18-auditoria-retroativa-no-navegador
plan: 05
type: execute
wave: 5
depends_on: [18-04]
files_modified:
  - .planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-HUMAN-UAT.md
  - .planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-VERIFICATION.md
autonomous: true
requirements: [AUDIT-04]
must_haves:
  truths:
    - "Os 11 cenários de 06-HUMAN-UAT.md foram executados ao vivo no navegador com result + evidence gravados"
    - "Auto-avanço Novo→Contatado só com template de 1º contato; nunca regride além de Contatado; contador incrementa a todo clique; cancelar não altera nada — todos observados"
    - "As 3 superfícies (/ , /leads , /importar/[batchId]) e os indicadores 'esfriando' + contador na mesma linha foram exercitados"
    - "06-HUMAN-UAT.md está complete e 06-VERIFICATION.md foi para status: passed"
  artifacts:
    - path: ".planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-HUMAN-UAT.md"
      provides: "UAT ao vivo da Fase 6"
      contains: "status: complete"
    - path: ".planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-VERIFICATION.md"
      provides: "Verificação da Fase 6 promovida"
      contains: "status: passed"
  key_links:
    - from: "06-VERIFICATION.md human_verification (11 itens)"
      to: "[] + status: passed"
      via: "cada item mapeado a um cenário pass do 06-HUMAN-UAT.md"
      pattern: "human_verification: \\[\\]"
---

<objective>
Executar ao vivo no navegador os 11 cenários já escritos de `06-HUMAN-UAT.md` (Auto-avanço de
Etapa + Contador de Tentativas) contra o `data/crm.db` real, gravar os resultados
incrementalmente, promover `06-HUMAN-UAT.md` a `complete` e `06-VERIFICATION.md` de
`human_needed` para `passed` — fechando AUDIT-04.

Purpose: A Fase 6 tem 4/4 must-haves só no nível de código; o `06-VERIFICATION.md` lista 11
verificações humanas pendentes (toasts, migração visual de coluna, contador no card, layout
de indicadores). Nenhum navegador foi usado.
Output: `06-HUMAN-UAT.md` `complete` + `06-VERIFICATION.md` `passed` (`human_verification: []`).

**Precondição:** Chrome aberto com a extensão Claude conectada. Se as ferramentas retornarem
erro de conexão, levante checkpoint de setup e pare.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-auditoria-retroativa-no-navegador/18-CONTEXT.md
@.planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-HUMAN-UAT.md
@.planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-VERIFICATION.md
</context>

<operational_playbook>
Aprendizados OBRIGATÓRIOS da Fase 16 (host 4GB, SEM paralelo):

1. Subir o dev server: matar node órfãos → `npm run dev` em background → esperar "Ready" →
   abrir aba em http://localhost:3000.
2. `get_page_text` / `read_page` como fonte PRIMÁRIA. Screenshot (toast, coluna do board,
   contador no card, layout dos indicadores) só com `wait` + retry.
3. `<Select>` de tipo de template no preview (Base UI): `left_click` no combobox +
   `left_click` na opção. Drag no `/pipeline`: `computer` `left_mouse_down` → move →
   `left_mouse_up`.
4. **Usar SÓ leads de teste "UAT18 ..." para os cliques que disparam contador/auto-avanço** —
   não mexer nos contadores dos leads reais do admin. Criar os leads de teste nos estados que
   cada cenário pede (Novo / Contatado / Negociação), executar, e soft-deletar ao fim.
   O clique "Abrir WhatsApp" abre uma aba wa.me — fechar logo, não enviar nada.
5. Gravar cada cenário em `06-HUMAN-UAT.md` ASSIM QUE executado (result + evidence). Retomada
   = primeiro cenário sem `result` != `[pending]`. (11 cenários — a sessão pode cair no
   limite 5h; a retomada é barata.)
6. Fechar dev server + aba do Chrome ao terminar.
7. Bug não-trivial → `/gsd-quick` separado, `result: fail` + link. NÃO consertar inline.
   `06-VERIFICATION.md` só vai a `passed` com 0 `fail` (D-05).
</operational_playbook>

<tasks>

<task type="auto">
  <name>Task 1: Executar cenários 1-6 (pipeline + dashboard: auto-avanço, não-regressão, cancelamento)</name>
  <read_first>
    - .planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-HUMAN-UAT.md (cenários 1-6, com o `expected:` de cada)
    - .planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-VERIFICATION.md (frontmatter human_verification — o detalhe de cada teste)
    - O <operational_playbook>
  </read_first>
  <files>.planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-HUMAN-UAT.md</files>
  <action>
    Subir o dev server (playbook). Criar os leads de teste necessários ("UAT18 F6 Novo",
    "UAT18 F6 Contatado", "UAT18 F6 Negociacao" — este último editando a etapa para
    Negociação). Cadastrar um template padrão do tipo "primeiro contato" e um do tipo
    "follow-up" se ainda não existirem (nomes "UAT18 ...").
    Cenário 1 (`/pipeline`): lead "Novo" → "Enviar WhatsApp" → manter "1º contato" →
    "Abrir WhatsApp" → aba wa.me abre, modal fecha, toast "{Nome} avançou para Contatado.",
    card migra para a coluna Contatado com ícone + "1x".
    Cenário 2: mesmo lead (agora Contatado), repetir com "1º contato" → sem toast de avanço,
    permanece em Contatado, contador "2x".
    Cenário 3: lead em Negociação, "Abrir WhatsApp" com "1º contato" → etapa inalterada
    (nunca regride), contador incrementa.
    Cenário 4: abrir o modal e cancelar (botão Cancelar, Escape, clique fora) → contador e
    etapa inalterados, nenhum toast.
    Cenário 5 (`/`): lead Novo aberto com "Follow-up", trocar para "1º contato" antes de
    enviar → avança com toast (o tipo vivo do Select vale, não o defaultTipo).
    Cenário 6 (`/`): lead Novo, manter "Follow-up", enviar → não avança, sem toast, contador
    incrementa.
    Gravar `result:` + `evidence:` (screenshot do toast/card ou trecho de `get_page_text` +
    query do `contact_attempts`/`stage` no `data/crm.db`) após cada cenário. Atualizar `## Summary`.
  </action>
  <acceptance_criteria>
    - Cenários 1-6 com `result:` != `[pending]` e `evidence:` (visual + query do contador/etapa no db)
    - Cenário 3 comprova não-regressão (Negociação não volta para Contatado)
    - Cenário 5 comprova que o tipo vivo do Select prevalece
    - Leads/templates "UAT18 ..." rastreados para limpeza na Task 2
    - Qualquer `fail` tem quick task em `.planning/quick/` + link
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/Vencedor/Desktop/crm-leads && grep -c 'result: pass\|result: fail\|result: skipped' .planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-HUMAN-UAT.md</automated>
    <manual>Contagem >= 6; cenários 1-6 com evidence</manual>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Executar cenários 7-11 (/leads, auto-gatilho, CSV, indicadores no card) + fechar 06-HUMAN-UAT.md</name>
  <read_first>
    - .planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-HUMAN-UAT.md (cenários 7-11 + ## Summary)
    - .planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-VERIFICATION.md (itens 7-11 de human_verification)
    - .planning/phases/02-csv-bulk-import/02-HUMAN-UAT.md (fixture uat18-import-real.csv — reutilizável para o cenário 9)
  </read_first>
  <files>.planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-HUMAN-UAT.md</files>
  <action>
    Cenário 7 (`/leads`): enviar "1º contato" num lead Novo de teste numa tela com vários
    leads visíveis → toast com o NOME correto do lead, etapa atualizada na linha da tabela.
    Cenário 8: criar um lead novo (auto-gatilho de 1º contato dispara o preview) e fechar sem
    enviar → contador em 0, nada extra no card.
    Cenário 9: importar um CSV mínimo (reusar `fixtures/uat18-import-real.csv` ou um novo de
    1 linha "UAT18 F6 CSV"), na tela `/importar/[batchId]` clicar "Abrir WhatsApp" de um lead
    → aba abre; ao voltar a `/pipeline`, o card mostra "1x" e o lead está em Contatado.
    Soft-deletar o(s) lead(s) importado(s) depois.
    Cenário 10: card com contador 0 → nenhum ícone/número extra aparece (D-06).
    Cenário 11: card simultaneamente "Esfriando" e com contador > 0 → os dois indicadores na
    mesma linha, sem quebra de layout, cores distintas (âmbar vs. neutro). Se não houver lead
    real nesse estado, forçar via query (`stage_changed_at` antigo num lead de teste
    "Contatado" + contador > 0) e recarregar `/pipeline`.
    Soft-deletar todos os leads "UAT18 ..." e excluir os templates "UAT18 ...". Fechar a aba e
    o dev server.
    Atualizar `## Summary` (total 11). Se 0 `fail` e 0 `pending` → frontmatter da
    `06-HUMAN-UAT.md`: `status: complete`,
    `method: browser UAT retroativa (Fase 18 / AUDIT-04) — extensão Claude no Chrome contra data/crm.db real`,
    `issues: 0`, `pending: 0`. Se houver `fail` → `partial` + `## Gaps` + PARAR antes da Task 3.
  </action>
  <acceptance_criteria>
    - Cenários 7-11 com `result:` != `[pending]` e `evidence:`
    - Cenário 9: evidência confirma "1x" + Contatado no card após o clique na tela pós-import
    - Cenário 11: evidência (screenshot) mostra os dois indicadores na mesma linha sem quebra
    - `06-HUMAN-UAT.md` `status: complete` (se 0 fail) com `method:` no frontmatter
    - Nenhum lead/template "UAT18 ..." residual no banco (soft-delete / hard-delete de template)
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/Vencedor/Desktop/crm-leads && grep -c 'result: \[pending\]' .planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-HUMAN-UAT.md</automated>
    <manual>0 `[pending]`; frontmatter status coerente</manual>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Promover 06-VERIFICATION.md para status: passed</name>
  <read_first>
    - .planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-VERIFICATION.md (frontmatter human_verification — 11 itens)
    - .planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-HUMAN-UAT.md (resultados finais)
  </read_first>
  <files>.planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-VERIFICATION.md</files>
  <action>
    SÓ executar se `06-HUMAN-UAT.md` está `status: complete` com 0 issues e 0 pending (D-05).
    Editar `06-VERIFICATION.md`:
      - frontmatter: `status: human_needed` → `status: passed`; `human_verification:` → `[]`;
        adicionar `human_verification_closed_by: "Fase 18 / AUDIT-04 — browser UAT ao vivo, ver 06-HUMAN-UAT.md"`.
      - No corpo, seção "## Human Verification — Fechado (Fase 18)": tabela dos 11 itens →
        nº do cenário de `06-HUMAN-UAT.md` + `result: pass` + resumo de 1 linha da evidência.
      - Atualizar "Status:" e "Gaps Summary" para `passed`. Quick tasks abertas (se houver) →
        listar com nota "não bloqueia AUDIT-04 (SC#4)".
  </action>
  <acceptance_criteria>
    - `06-VERIFICATION.md` frontmatter `status: passed` e `human_verification: []`
    - Seção nova mapeando os 11 itens a cenários `pass` de `06-HUMAN-UAT.md`
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/Vencedor/Desktop/crm-leads && grep -q 'status: passed' .planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-VERIFICATION.md && grep -q 'human_verification: \[\]' .planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-VERIFICATION.md && grep -q 'status: complete' .planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-HUMAN-UAT.md && echo OK</automated>
    <manual>VERIFICATION passed só com HUMAN-UAT complete</manual>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma) | Verificação de comportamento já shipado. Não altera runtime, sem rede (wa.me abre em aba, não envia), sem auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | `data/crm.db` real (contador de tentativas + auto-avanço de etapa + interações gravadas a cada clique em WhatsApp) | mitigate | TODOS os cliques que disparam contador/avanço usam SÓ leads de teste "UAT18 F6 ..."; leads soft-deletados e templates "UAT18 ..." hard-deletados ao fim da Task 2; forçar estado "esfriando" via UPDATE de `stage_changed_at` só em lead de teste; registrar tudo no `06-HUMAN-UAT.md`; nunca `DELETE FROM leads`. |
| T-18-SC | Tampering | dependências npm | accept | Nenhum pacote instalado nesta fase. |
</threat_model>

<verification>
- `06-HUMAN-UAT.md`: 11 cenários, 0 `[pending]`, `status: complete` (se 0 fail), `method:` no frontmatter
- `06-VERIFICATION.md`: `status: passed`, `human_verification: []`
- `data/crm.db` sem leads/templates "UAT18 ..." ao fim
- Toda `fail` tem quick task em `.planning/quick/`
- Dev server + aba do Chrome encerrados
</verification>

<success_criteria>
- AUDIT-04 fechado: `06-HUMAN-UAT.md` complete + `06-VERIFICATION.md` passed
- Os 11 cenários executados ao vivo com evidência visual + query do contador/etapa
</success_criteria>

<output>
Criar `.planning/phases/18-auditoria-retroativa-no-navegador/18-05-SUMMARY.md` ao terminar,
com `## Issues Encontradas` (quick tasks abertas, se houver).
</output>
