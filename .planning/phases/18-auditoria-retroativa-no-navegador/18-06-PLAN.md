---
phase: 18-auditoria-retroativa-no-navegador
plan: 06
type: execute
wave: 6
depends_on: [18-05]
files_modified:
  - .planning/phases/08-origem-governada-separa-o-inbound-outbound/08-HUMAN-UAT.md
  - .planning/phases/08-origem-governada-separa-o-inbound-outbound/08-VERIFICATION.md
  - .planning/STATE.md
autonomous: true
requirements: [AUDIT-05]
must_haves:
  truths:
    - "Os 4 cenários de 08-HUMAN-UAT.md foram executados ao vivo no navegador com result + evidence gravados"
    - "Campo 'Tipo de origem' abaixo de 'Origem' sem pré-seleção na criação; salvamento bloqueado sem escolha com mensagem visível; lead pré-existente vem com 'Outbound' na edição; lote de CSV grava origem_tipo='outbound' — todos observados"
    - "08-HUMAN-UAT.md está complete e 08-VERIFICATION.md foi para status: passed"
    - "STATE.md §Deferred Items não lista mais os uat_gap/verification_gap das Fases 1, 2, 4, 6 e 8 (D-07 / SC#5)"
  artifacts:
    - path: ".planning/phases/08-origem-governada-separa-o-inbound-outbound/08-VERIFICATION.md"
      provides: "Verificação da Fase 8 promovida a passed"
      contains: "status: passed"
    - path: ".planning/STATE.md"
      provides: "§Deferred Items sem as afirmações de gap que deixaram de ser verdade"
      contains: "Deferred Items"
  key_links:
    - from: "STATE.md §Deferred Items"
      to: "5 linhas uat_gap/verification_gap removidas (Fases 1,2,4,6,8)"
      via: "edição pontual, /close-phase faz o refresh completo depois"
      pattern: "uat_gap"
---

<objective>
Fechar a última fase da auditoria: executar ao vivo os 4 cenários de `08-HUMAN-UAT.md`
(Origem Governada Inbound × Outbound), promover `08-HUMAN-UAT.md` a `complete` e
`08-VERIFICATION.md` a `passed`, e limpar do `STATE.md` §Deferred Items as linhas de
`uat_gap`/`verification_gap` das Fases 1, 2, 4, 6 e 8 que deixaram de ser verdade (D-07).
Fecha AUDIT-05 e o SC#5 da fase.

Purpose: A Fase 8 tem 10/15 must-haves automatizados; 5 dependem de clique real (posição do
campo no DOM, `<FieldError>` visível, hidratação do `defaultValues.origemTipo` na edição, e o
caminho de CSV que NUNCA teve prova comportamental — nem browser nem harness, 08-REVIEW WR-03).
Output: `08-HUMAN-UAT.md` `complete` + `08-VERIFICATION.md` `passed` + `STATE.md` §Deferred
Items limpo.

**Precondição:** Chrome aberto com a extensão Claude conectada. Se as ferramentas retornarem
erro de conexão, levante checkpoint de setup e pare.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-auditoria-retroativa-no-navegador/18-CONTEXT.md
@.planning/phases/08-origem-governada-separa-o-inbound-outbound/08-HUMAN-UAT.md
@.planning/phases/08-origem-governada-separa-o-inbound-outbound/08-VERIFICATION.md
</context>

<operational_playbook>
Aprendizados OBRIGATÓRIOS da Fase 16 (host 4GB, SEM paralelo):

1. Subir o dev server: matar node órfãos → `npm run dev` em background → esperar "Ready" →
   abrir aba em http://localhost:3000.
2. `get_page_text` / `read_page` (accessibility tree) como fonte PRIMÁRIA — especialmente
   para confirmar a POSIÇÃO do campo "Tipo de origem" logo abaixo de "Origem" e a presença
   das opções do select. Screenshot só com `wait` + retry.
3. Formulário via `computer` `left_click` + `type` (NÃO `form_input`). `<Select>` "Tipo de
   origem" (Base UI, `type="button"`): `left_click` no combobox + `left_click` na opção.
4. **Upload:** `file_upload` via `ref` do `<input type=file>` (nunca clicar no botão).
5. **NÃO poluir o banco:** o cenário 4 (CSV) exige import real → usar `fixtures/uat18-import-real.csv`
   (do plano 18-03) ou um CSV de 1 linha "UAT18 F8 CSV"; após a query que confirma
   `origem_tipo='outbound'`, soft-deletar o(s) lead(s). Cenários 1-3 não persistem nada
   (ou usam lead de teste "UAT18 F8" soft-deletado depois).
6. Gravar cada cenário em `08-HUMAN-UAT.md` ASSIM QUE executado (result + evidence). Retomada
   = primeiro cenário sem `result` != `[pending]`.
7. Fechar dev server + aba do Chrome ao terminar.
8. Bug não-trivial → `/gsd-quick` separado, `result: fail` + link. NÃO consertar inline.
   `08-VERIFICATION.md` só vai a `passed` com 0 `fail` (D-05).
</operational_playbook>

<tasks>

<task type="auto">
  <name>Task 1: Executar os 4 cenários de 08-HUMAN-UAT.md + fechar o arquivo</name>
  <read_first>
    - .planning/phases/08-origem-governada-separa-o-inbound-outbound/08-HUMAN-UAT.md (os 4 cenários com `expected:`)
    - .planning/phases/08-origem-governada-separa-o-inbound-outbound/08-VERIFICATION.md (frontmatter human_verification — o detalhe e o "why_human" de cada um; WR-03 do 08-REVIEW sobre o caminho de CSV)
    - O <operational_playbook>
  </read_first>
  <files>.planning/phases/08-origem-governada-separa-o-inbound-outbound/08-HUMAN-UAT.md</files>
  <action>
    Subir o dev server (playbook).
    Cenário 1: abrir "Novo lead" (em `/leads` ou `/`). Via `read_page`, confirmar que "Tipo de
    origem" aparece IMEDIATAMENTE abaixo de "Origem", com placeholder "Selecione o tipo de
    origem" (nada pré-selecionado) e exatamente duas opções: Inbound e Outbound.
    Cenário 2: preencher o resto do formulário e clicar "Salvar" SEM escolher "Tipo de
    origem" → confirmar que o salvamento é bloqueado e a mensagem "Selecione o tipo de origem."
    aparece visualmente abaixo do campo (capturar via `get_page_text`).
    Cenário 3: abrir o modal de EDIÇÃO de um lead pré-existente (qualquer lead real do admin,
    backfillado pela plan 08-01) → confirmar que o select "Tipo de origem" já vem com
    "Outbound" selecionado. NÃO salvar (só observar) — ou salvar sem alterar e reverter.
    Cenário 4: importar `fixtures/uat18-import-real.csv` (ou CSV de 1 linha "UAT18 F8 CSV")
    via `/importar` → confirmar; depois `node -e` contra `data/crm.db`:
    `SELECT nome, origem_tipo FROM leads WHERE import_batch_id = '<batchId do lote>'` →
    todas as linhas com `origem_tipo = 'outbound'`. Colar a saída no `evidence:`.
    Soft-deletar o(s) lead(s) de teste importado(s).
    Gravar `result:` + `evidence:` após cada cenário. Fechar a aba e o dev server.
    Atualizar `## Summary` (total 4). Se 0 `fail` e 0 `pending` → frontmatter da
    `08-HUMAN-UAT.md`: `status: complete`,
    `method: browser UAT retroativa (Fase 18 / AUDIT-05) — extensão Claude no Chrome contra data/crm.db real`,
    `issues: 0`, `pending: 0`. Se houver `fail` → `partial` + `## Gaps` + PARAR antes da Task 2.
  </action>
  <acceptance_criteria>
    - Os 4 cenários com `result:` != `[pending]` e `evidence:`
    - Cenário 1: evidência do `read_page` mostrando a ordem Origem → Tipo de origem + as 2 opções
    - Cenário 2: evidência com a mensagem de erro literal visível
    - Cenário 4: evidência = saída de query mostrando `origem_tipo='outbound'` para todas as linhas do lote; lead(s) de teste soft-deletado(s)
    - `08-HUMAN-UAT.md` `status: complete` (se 0 fail) com `method:`
    - Qualquer `fail` tem quick task em `.planning/quick/` + link
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/Vencedor/Desktop/crm-leads && grep -c 'result: \[pending\]' .planning/phases/08-origem-governada-separa-o-inbound-outbound/08-HUMAN-UAT.md</automated>
    <manual>0 `[pending]`; frontmatter status coerente com os resultados</manual>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Promover 08-VERIFICATION.md para passed</name>
  <read_first>
    - .planning/phases/08-origem-governada-separa-o-inbound-outbound/08-VERIFICATION.md (frontmatter human_verification — 4 itens)
    - .planning/phases/08-origem-governada-separa-o-inbound-outbound/08-HUMAN-UAT.md (resultados finais)
  </read_first>
  <files>.planning/phases/08-origem-governada-separa-o-inbound-outbound/08-VERIFICATION.md</files>
  <action>
    SÓ executar se `08-HUMAN-UAT.md` está `status: complete` com 0 issues e 0 pending (D-05).
    Editar `08-VERIFICATION.md`:
      - frontmatter: `status: human_needed` → `status: passed`; `human_verification:` → `[]`;
        adicionar `human_verification_closed_by: "Fase 18 / AUDIT-05 — browser UAT ao vivo, ver 08-HUMAN-UAT.md"`.
      - No corpo, seção "## Human Verification — Fechado (Fase 18)": tabela dos 4 itens → nº do
        cenário + `result: pass` + resumo de 1 linha da evidência. Anotar explicitamente que
        o WR-03 do 08-REVIEW (caminho de CSV sem prova comportamental) está agora coberto pelo
        cenário 4 com query real no `data/crm.db`.
      - Atualizar "Status:" e "Gaps Summary" para `passed`.
  </action>
  <acceptance_criteria>
    - `08-VERIFICATION.md` frontmatter `status: passed` e `human_verification: []`
    - Seção nova mapeando os 4 itens a cenários `pass`, com menção ao WR-03 fechado
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/Vencedor/Desktop/crm-leads && grep -q 'status: passed' .planning/phases/08-origem-governada-separa-o-inbound-outbound/08-VERIFICATION.md && grep -q 'human_verification: \[\]' .planning/phases/08-origem-governada-separa-o-inbound-outbound/08-VERIFICATION.md && grep -q 'status: complete' .planning/phases/08-origem-governada-separa-o-inbound-outbound/08-HUMAN-UAT.md && echo OK</automated>
    <manual>VERIFICATION passed só com HUMAN-UAT complete</manual>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Limpar o §Deferred Items do STATE.md (D-07 / SC#5)</name>
  <read_first>
    - .planning/STATE.md (§Deferred Items — a tabela por volta das linhas 264-283)
    - Os 5 arquivos VERIFICATION agora `passed`: 01, 02, 04, 06, 08 (confirmar antes de editar)
  </read_first>
  <files>.planning/STATE.md</files>
  <action>
    Pré-checar: os 5 VERIFICATION.md (Fases 1, 2, 4, 6, 8) estão `status: passed` E os 5
    HUMAN-UAT.md estão `status: complete`. Se algum não estiver (quick task aberta bloqueando
    D-05), NÃO remover a linha correspondente — deixar com uma nota "aguardando quick task
    <id>" e reportar no SUMMARY.

    Na tabela do §Deferred Items do `STATE.md`, REMOVER as linhas:
      - `uat_gap | Fase 04 (04-HUMAN-UAT.md) — 7 cenários pendentes`
      - `verification_gap | Fase 04 (04-VERIFICATION.md)`
      - `uat_gap | Fase 06 (06-HUMAN-UAT.md) — 11 cenários pendentes`
      - `verification_gap | Fase 06 (06-VERIFICATION.md)`
      - `uat_gap | Fase 08 (08-HUMAN-UAT.md) — 4 cenários pendentes`
      - `verification_gap | Fase 08 (08-VERIFICATION.md)`
      - `verification_gap | Fases 1 e 2 nunca tiveram /gsd-verify-work formal ...`
    MANTER: as linhas de `quick_task (×8)`, `todo`, `todo (×5)`, `seed (×2)`, e a linha
    `uat_gap | Fase 12 Teste 14` (fora do escopo da Fase 18).
    Atualizar o parágrafo introdutório do §Deferred Items para refletir que os gaps de
    UAT/verificação das Fases 1/2/4/6/8 foram fechados na Fase 18 (AUDIT-01..05), citando a
    data (2026-09) e que o `/close-phase` fará o refresh completo do STATE.
    Se alguma quick task foi aberta durante a Fase 18 (cenários `fail`), ADICIONAR uma linha
    `quick_task | <id> — <descrição> (achado do UAT da Fase 18, não bloqueia AUDIT)`.
    NÃO tocar em outras seções do STATE (a §"COMEÇA AQUI" e a Session Continuity são
    responsabilidade do `/close-phase`).
  </action>
  <acceptance_criteria>
    - `STATE.md` §Deferred Items não contém mais nenhuma linha `uat_gap`/`verification_gap` das Fases 1, 2, 4, 6, 8
    - As linhas `quick_task (×8)`, `seed`, `todo` e a `uat_gap` da Fase 12 continuam presentes
    - Parágrafo introdutório do §Deferred Items atualizado com a menção à Fase 18
    - Se houver quick task nova da Fase 18, ela está listada
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/Vencedor/Desktop/crm-leads && sed -n '/## Deferred Items/,/## Session Continuity/p' .planning/STATE.md | grep -c 'Fase 0[468].*HUMAN-UAT\|Fase 0[468].*VERIFICATION\|Fases 1 e 2 nunca tiveram'</automated>
    <manual>Contagem == 0; linhas de quick_task/seed/todo/Fase 12 preservadas</manual>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma) | Verificação de comportamento já shipado + edição de docs de planejamento. Não altera runtime, sem rede, sem auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | `data/crm.db` real (import de teste do cenário 4; possível save no cenário 3) | mitigate | Cenário 4 usa CSV mínimo "UAT18 F8"; leads soft-deletados após a query; cenário 3 só observa (não salva) ou salva-e-reverte um lead real sem alterar `origem_tipo`; nunca `DELETE FROM`. |
| T-18-02 | Tampering | `STATE.md` (edição da tabela §Deferred Items) | mitigate | Só remover as 7 linhas nomeadas explicitamente; pré-checagem de que os 5 VERIFICATION estão `passed` antes de remover cada uma; `git diff` do STATE revisável no commit; `/close-phase` faz o refresh completo depois. |
| T-18-SC | Tampering | dependências npm | accept | Nenhum pacote instalado nesta fase. |
</threat_model>

<verification>
- `08-HUMAN-UAT.md`: 4 cenários, 0 `[pending]`, `status: complete` (se 0 fail), `method:` no frontmatter
- `08-VERIFICATION.md`: `status: passed`, `human_verification: []`, WR-03 anotado como fechado
- `STATE.md` §Deferred Items sem os `uat_gap`/`verification_gap` das Fases 1/2/4/6/8
- Os 5 VERIFICATION (01/02/04/06/08) todos `passed`; os 5 HUMAN-UAT todos `complete`
- `data/crm.db` sem leads "UAT18 ..." ativos
- Toda `fail` da fase inteira tem quick task em `.planning/quick/`
</verification>

<success_criteria>
- AUDIT-05 fechado: `08-HUMAN-UAT.md` complete + `08-VERIFICATION.md` passed
- SC#5 cumprido: `STATE.md` §Deferred Items não lista mais os gaps das Fases 1/2/4/6/8
- A auditoria retroativa da Fase 18 está completa (5/5 requisitos, 5/5 VERIFICATION passed)
</success_criteria>

<output>
Criar `.planning/phases/18-auditoria-retroativa-no-navegador/18-06-SUMMARY.md` ao terminar,
com `## Issues Encontradas` consolidando TODAS as quick tasks abertas ao longo da Fase 18
(planos 18-01 a 18-06) e uma nota de que nenhuma bloqueia o fecho dos requisitos AUDIT (SC#4).
</output>
