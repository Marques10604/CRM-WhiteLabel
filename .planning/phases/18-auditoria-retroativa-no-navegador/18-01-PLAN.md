---
phase: 18-auditoria-retroativa-no-navegador
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-lead-sub-nicho-foundation/01-HUMAN-UAT.md
autonomous: true
requirements: [AUDIT-01]
must_haves:
  truths:
    - "01-HUMAN-UAT.md existe com TODOS os cenários da Fase 1 listados (CRUD de lead, CRUD de nicho + dedupe, lista/toolbar/paginação), cada um com campo result"
    - "Os cenários de CRUD de lead (criar/validar/editar/descartar/soft-delete/restaurar) foram executados no navegador contra data/crm.db com result + evidence gravados"
    - "Os cenários de CRUD de nicho + dedupe case-insensitive + rename + soft-delete/reativação foram executados no navegador com result + evidence gravados"
  artifacts:
    - path: ".planning/phases/01-lead-sub-nicho-foundation/01-HUMAN-UAT.md"
      provides: "Roteiro + resultados ao vivo dos cenários de CRUD da Fase 1"
      contains: "method:"
  key_links:
    - from: "01-HUMAN-UAT.md (cada cenário executado)"
      to: "evidência (trecho de get_page_text / read_page / query no data/crm.db)"
      via: "bloco evidence: por cenário"
      pattern: "evidence:"
---

<objective>
Autorar `01-HUMAN-UAT.md` do zero (não existe hoje) cobrindo toda a Fase 1, e executar ao vivo
no navegador os cenários de **CRUD de lead** e **CRUD de nicho + dedupe case-insensitive**
contra o `data/crm.db` real. A lista/toolbar/paginação e a promoção do `01-VERIFICATION.md`
ficam para o plano 18-02.

Purpose: A Fase 1 (Lead & Nicho Foundation) nunca teve `/gsd-verify-work` formal — os 4
SUMMARYs registram "clique real recomendado antes de uso real" mas nenhuma sessão teve
navegador. AUDIT-01 fecha essa lacuna.
Output: `01-HUMAN-UAT.md` autorado (status `partial`), com os cenários de CRUD já
`result: pass|fail|skipped` + `evidence:`.

**Precondição:** o Chrome precisa estar aberto com a extensão Claude conectada. Se as
ferramentas da extensão retornarem erro de conexão, levante um checkpoint de setup e pare —
não tente contornar.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-auditoria-retroativa-no-navegador/18-CONTEXT.md
@.planning/phases/01-lead-sub-nicho-foundation/01-SPEC.md
@.planning/phases/01-lead-sub-nicho-foundation/01-01-SUMMARY.md
@.planning/phases/01-lead-sub-nicho-foundation/01-02-SUMMARY.md
@.planning/phases/01-lead-sub-nicho-foundation/01-04-SUMMARY.md
@.planning/phases/16-corre-es-de-code-review-da-fase-15/16-HUMAN-UAT.md
</context>

<operational_playbook>
Aprendizados OBRIGATÓRIOS do UAT da Fase 16 (2026-09-01) — host 4GB, SEM processos em paralelo:

1. **Subir o ambiente (uma vez):** em Git Bash, matar node órfãos deste projeto antes
   (`taskkill //F //IM node.exe` só se nenhum outro projeto Node estiver rodando; senão
   filtrar por PID). Rodar `npm run dev` em background (Next 16.2 Turbopack, ~4s). Esperar a
   linha `Ready` / `Local: http://localhost:3000` ANTES de abrir a aba.
2. **Ler estado da página:** `get_page_text` e `read_page` (accessibility tree) são a fonte
   PRIMÁRIA de verificação. Screenshot só quando indispensável, com `wait` + retry — dão
   timeout intermitente sob pressão de RAM.
3. **Preencher formulários:** `form_input` NÃO dispara o `onChange` do react-hook-form neste
   projeto. Usar `computer` → `left_click` no campo + `type`. Para `<Select>` do Base UI
   (`type="button"`): `left_click` no combobox, depois `left_click` na opção (por `ref` do
   `read_page` ou por coordenada de screenshot).
4. **Upload de arquivo:** `file_upload` funciona. Achar o `ref` do `<input type=file>` via
   `read_page`; NUNCA clicar no botão de upload (abre picker nativo invisível).
5. **NÃO confirmar imports que poluam o banco.** A prévia do wizard já prova o comportamento.
6. **Gravar cada cenário no `01-HUMAN-UAT.md` ASSIM QUE executado** (result + evidence), nunca
   bufferizar — o limite de uso 5h pode cortar a sessão. Retomada = primeiro cenário sem
   `result`.
7. **Ao terminar a sessão:** fechar o dev server e a aba do Chrome.

Evidência aceitável por cenário: trecho de `get_page_text`/`read_page` mostrando o estado
esperado, OU screenshot salvo, OU query direta no `data/crm.db`
(`node -e "const db=require('better-sqlite3')('data/crm.db'); console.log(db.prepare('...').all())"`).

Bug não-trivial encontrado → **NÃO consertar inline**. Registrar em `/gsd-quick` (task
separada), anotar no cenário do `01-HUMAN-UAT.md` (`result: fail` + link) e na seção
"## Issues Encontradas" do SUMMARY deste plano. Regressão trivial de 1 linha → consertar e
anotar como deviation.
</operational_playbook>

<tasks>

<task type="auto">
  <name>Task 1: Autorar 01-HUMAN-UAT.md completo (todos os cenários da Fase 1)</name>
  <read_first>
    - .planning/phases/01-lead-sub-nicho-foundation/01-SPEC.md (§Acceptance Criteria — 10 critérios falsificáveis)
    - .planning/phases/01-lead-sub-nicho-foundation/01-02-SUMMARY.md (form de 9 campos, discard-changes, contrato valor/telefone)
    - .planning/phases/01-lead-sub-nicho-foundation/01-04-SUMMARY.md (soft-delete + Lixeira + reativação de nicho por nome)
    - .planning/phases/16-corre-es-de-code-review-da-fase-15/16-HUMAN-UAT.md (formato: frontmatter, ## Tests, result:, evidence:, ## Summary)
  </read_first>
  <files>.planning/phases/01-lead-sub-nicho-foundation/01-HUMAN-UAT.md</files>
  <action>
    Criar `01-HUMAN-UAT.md` seguindo o formato de `16-HUMAN-UAT.md`. Frontmatter:
    `status: partial`, `phase: 01-lead-sub-nicho-foundation`, `source: [01-SPEC.md, 01-02-SUMMARY.md, 01-04-SUMMARY.md]`,
    `method: "browser automation (extensão Claude no Chrome) contra dev server localhost:3000 + data/crm.db real"`,
    `issues: 0`, `pending: <total>`.
    Seção `## Tests` com os cenários abaixo, cada um com `expected:` e `result: [pending]`.
    NÃO inventar comportamento — derivar de 01-SPEC + SUMMARYs.

    CRUD de lead:
      1. Criar lead com os 9 campos preenchidos (nome, telefone, canal, origem, valor, notas,
         follow-up, nicho, etapa) → linha persiste e aparece em `/leads`.
      2. Criar lead com um campo obrigatório vazio → erro inline no campo, submit bloqueado,
         nada persiste.
      3. Editar lead clicando na linha → modal reabre pré-preenchido; salvar altera a linha.
      4. Fechar o modal com alteração não salva → aviso "Descartar alterações?" com
         "Continuar editando" / "Descartar".
      5. Contrato valor/telefone: digitar "1.234,56" e "(11) 91234-5678" → grava 123456
         centavos e "5511912345678" (conferir via query no data/crm.db).
      6. Soft-delete: botão excluir → modal de confirmação nomeado → confirmar → some de
         `/leads`, aparece em `/lixeira` com coluna "Excluído em". Cancelar o modal deixa o
         lead na lista.
      7. Restaurar da Lixeira → volta para `/leads`, some da Lixeira (ação instantânea, sem
         confirmação).

    CRUD de nicho + dedupe:
      8. Criar nicho novo em `/nichos` → aparece na lista e fica selecionável no form de lead.
      9. Dedupe case-insensitive: criar um nicho cujo nome bate (ignorando caixa/espaços) com
         um existente → rejeitado com erro inline (usar um nome que já exista no banco; se
         necessário criar "UAT18 Nicho" e depois tentar "uat18 nicho").
      10. Near-duplicate permitido: criar "UAT18 Nic" quando "UAT18 Nicho" existe → sucesso
          (grafias diferentes não são bloqueadas — SPEC req 3).
      11. Renomear nicho inline (ícone lápis) → nome atualiza na lista.
      12. Soft-delete de nicho + reativação por nome (quick 260725-lai): remover um nicho de
          teste → some das superfícies de seleção (combobox do form + filtro da toolbar);
          recriar com o mesmo nome → reativa o registro (não cria duplicado).

    Lista / toolbar / paginação (executados no plano 18-02, mas LISTADOS aqui já):
      13. Ordenação default: sem filtro, o topo de `/leads` é o follow-up mais próximo.
      14. Clicar cabeçalho (Nome/Nicho/Etapa/Follow-up) alterna asc/desc; Telefone não ordena.
      15. Filtro por nicho (single-select) → só leads daquele nicho.
      16. Filtro por etapa → só leads naquela etapa.
      17. Filtro por intervalo de follow-up (início/fim), inclusivo nas duas pontas.
      18. Filtros combinados (nicho + etapa) → interseção (AND).
      19. "Limpar filtros" → restaura todos os leads + sort default.
      20. Paginação: com >25 leads, "Página 1 de N"; Anterior/Próximo desabilitam nos limites;
          mudar filtro volta para a página 1.

    Fechar com `## Summary` (total / passed / issues / pending / skipped / blocked) e `## Gaps`.
  </action>
  <acceptance_criteria>
    - `01-HUMAN-UAT.md` existe com os 20 cenários acima, cada um com `expected:` e `result: [pending]`
    - Frontmatter tem `method:` no formato do D-04
    - `## Summary` presente com `pending: 20`
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/01-lead-sub-nicho-foundation/01-HUMAN-UAT.md && grep -c 'result:' .planning/phases/01-lead-sub-nicho-foundation/01-HUMAN-UAT.md</automated>
    <manual>Contagem de `result:` == 20; frontmatter contém `method:`</manual>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Executar no navegador os cenários de CRUD de lead (1-7)</name>
  <read_first>
    - .planning/phases/01-lead-sub-nicho-foundation/01-HUMAN-UAT.md (cenários 1-7, criados na Task 1)
    - O <operational_playbook> deste plano (setup do dev server, get_page_text vs screenshot, computer type vs form_input, não poluir o banco, gravar incremental)
  </read_first>
  <files>.planning/phases/01-lead-sub-nicho-foundation/01-HUMAN-UAT.md</files>
  <action>
    Subir o ambiente conforme o playbook (matar node órfãos, `npm run dev` em background,
    esperar "Ready", abrir aba em http://localhost:3000).
    Executar os cenários 1 a 7 na ordem, um de cada vez. Após CADA cenário, editar
    `01-HUMAN-UAT.md` gravando `result: pass|fail|skipped` + um bloco `evidence: |` com o
    trecho de `get_page_text`/`read_page` ou a query no `data/crm.db` que prova o resultado.
    Preencher o form de lead via `computer` `left_click` + `type` (não `form_input`); os
    `<Select>` de canal/etapa e o combobox de nicho via `left_click` no combobox + `left_click`
    na opção; o `<Calendar>` de follow-up idem.
    Cenário 5: após salvar, rodar `node -e` contra `data/crm.db` para ler `valorEstimado` e
    `telefone` do lead criado e colar o resultado no `evidence:`.
    Cenário 6/7: os leads de teste criados aqui ficam soft-deletados ao fim do cenário 6 (é o
    próprio teste); no cenário 7 restaurar e depois soft-deletar de novo para não deixar
    "UAT18" na lista ativa. Registrar no `evidence:` do cenário 7 o estado final (lead de
    teste na Lixeira).
    Atualizar os contadores do `## Summary` conforme avança.
    Se algum cenário falhar: `result: fail`, abrir `/gsd-quick`, anotar o link no cenário e na
    seção "## Issues Encontradas" do SUMMARY. NÃO consertar inline (exceto regressão trivial
    de 1 linha).
  </action>
  <acceptance_criteria>
    - Cenários 1-7 de `01-HUMAN-UAT.md` com `result:` != `[pending]` e bloco `evidence:` preenchido
    - Cenário 5 tem evidência = saída de query no `data/crm.db` (centavos + telefone normalizado)
    - Nenhum lead "UAT18 ..." permanece na lista ativa de `/leads` ao fim (soft-deletado)
    - Qualquer `fail` tem uma quick task registrada em `.planning/quick/` e link no cenário
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/Vencedor/Desktop/crm-leads && sed -n '/CRUD de lead/,/CRUD de nicho/p' .planning/phases/01-lead-sub-nicho-foundation/01-HUMAN-UAT.md | grep -c 'result: pass\|result: fail\|result: skipped'</automated>
    <manual>7 cenários resolvidos; cada um com evidence; leads UAT18 soft-deletados</manual>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Executar no navegador os cenários de CRUD de nicho + dedupe (8-12)</name>
  <read_first>
    - .planning/phases/01-lead-sub-nicho-foundation/01-HUMAN-UAT.md (cenários 8-12)
    - .planning/phases/01-lead-sub-nicho-foundation/01-04-SUMMARY.md (reativação de nicho por nome — quick 260725-lai)
    - O <operational_playbook> deste plano
  </read_first>
  <files>.planning/phases/01-lead-sub-nicho-foundation/01-HUMAN-UAT.md</files>
  <action>
    Com o dev server já de pé, navegar para `/nichos`. Executar os cenários 8 a 12 na ordem.
    Gravar `result:` + `evidence:` após cada um (incremental).
    Cenário 9/10: criar "UAT18 Nicho", tentar "uat18 nicho" (deve rejeitar com erro inline —
    capturar a mensagem literal no evidence), depois criar "UAT18 Nic" (deve aceitar).
    Cenário 11: renomear "UAT18 Nic" → "UAT18 Nic Renomeado"; confirmar via `get_page_text`.
    Cenário 12: soft-deletar "UAT18 Nicho"; abrir o form de "Novo lead" e confirmar que ele
    sumiu do combobox de nicho e do filtro da toolbar em `/leads`; recriar "UAT18 Nicho" e
    confirmar que reativou (mesma linha, sem duplicata — checar via query `data/crm.db` que
    só existe 1 linha com esse nome).
    Ao fim: soft-deletar os nichos de teste "UAT18 ..." que não estejam em uso (se algum
    estiver referenciado por lead de teste soft-deletado, deixar e anotar — o FK
    `onDelete: restrict` impede hard-delete, comportamento conhecido de 02-01).
    Fechar o dev server e a aba do Chrome ao terminar.
    Atualizar `## Summary`. NÃO promover o `01-VERIFICATION.md` aqui (é o plano 18-02).
  </action>
  <acceptance_criteria>
    - Cenários 8-12 com `result:` != `[pending]` e `evidence:` preenchido
    - Cenário 9 tem no evidence a mensagem de erro literal de dedupe case-insensitive
    - Cenário 12 tem no evidence a query provando 1 única linha do nicho reativado
    - `## Summary` com contadores corretos; `pending` reflete só os cenários 13-20 (lista/toolbar/paginação)
    - Dev server e aba do Chrome fechados
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/Vencedor/Desktop/crm-leads && grep -c 'result: pass\|result: fail\|result: skipped' .planning/phases/01-lead-sub-nicho-foundation/01-HUMAN-UAT.md</automated>
    <manual>Contagem >= 12 (cenários 1-12 resolvidos); cenários 13-20 ainda [pending]</manual>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma) | Fase de verificação de comportamento já shipado. Não altera código de runtime, sem rede, sem auth, sem nova superfície de entrada. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | `data/crm.db` real (dados de teste criados no UAT) | mitigate | Prefixo "UAT18 " em todo lead/nicho de teste; soft-delete do lead ao fim de cada cenário; registrar no `01-HUMAN-UAT.md` o que foi criado e o passo de limpeza; nunca `DELETE FROM` (guard-no-hard-delete cobre). |
| T-18-SC | Tampering | dependências npm | accept | Nenhum pacote instalado nesta fase — só execução de UAT + edição de docs de planejamento. |
</threat_model>

<verification>
- `01-HUMAN-UAT.md` existe, frontmatter com `method:`, 20 cenários listados
- Cenários 1-12 (`result:` pass/fail/skipped) com bloco `evidence:` cada
- Nenhum registro "UAT18 ..." ativo em `/leads` ou `/nichos` ao fim (soft-deletados)
- Toda `fail` tem quick task em `.planning/quick/` + link no cenário
- Dev server + aba do Chrome encerrados
</verification>

<success_criteria>
- `01-HUMAN-UAT.md` autorado (status `partial`) com os 20 cenários da Fase 1
- 12 cenários de CRUD (lead + nicho) executados ao vivo no navegador com evidência
- Banco real sem lixo de teste ativo
</success_criteria>

<output>
Criar `.planning/phases/18-auditoria-retroativa-no-navegador/18-01-SUMMARY.md` ao terminar,
incluindo a seção `## Issues Encontradas` (quick tasks abertas, se houver).
</output>
