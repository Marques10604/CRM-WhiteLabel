---
phase: 07-configura-o-de-dias-parado-por-etapa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/lib/validations.ts
  - src/db/queries.ts
  - src/actions/configuracoes-actions.ts
autonomous: true
requirements: [CONFIG-01, CONFIG-02]

must_haves:
  truths:
    - "A tabela `configuracoes` existe de fato no banco vivo `./data/crm.db` (não só no schema TypeScript)"
    - "getConfiguracoes() implementa getOrCreate: seleciona id=1 e, se ausente, insere a linha padrão (Novo=999999, Contatado=5, Negociação=999999) e a retorna — sem depender de SQL de migração (a prova em runtime acontece no 07-02, no primeiro GET de /configuracoes)"
    - "Os defaults do schema honram D-04 na íntegra: só Contatado nasce com 5 (paridade com o hardcode pré-fase); Novo e Negociação nascem com 999999 (≈ nunca esfria), porque o código pré-deploy NUNCA flagava essas duas etapas"
    - "Salvar com 0, negativo ou valor não numérico é rejeitado pelo servidor e nada é persistido"
    - "Salvar valores válidos persiste na linha id=1 e invalida o cache de /configuracoes E de /pipeline"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Tabela singleton `configuracoes` (dias_parado_novo/contatado/negociacao + updated_at), defaults 999999/5/999999 (D-04)"
      contains: "export const configuracoes"
    - path: "src/lib/validations.ts"
      provides: "configuracoesSchema com mínimo de 1 dia por campo (D-03)"
      contains: "configuracoesSchema"
    - path: "src/db/queries.ts"
      provides: "getConfiguracoes() com semeadura idempotente da linha padrão (D-04)"
      contains: "export async function getConfiguracoes"
    - path: "src/actions/configuracoes-actions.ts"
      provides: "Server Action saveConfiguracoes (validação autoritativa + persistência + revalidação)"
      exports: ["saveConfiguracoes"]
  key_links:
    - from: "src/actions/configuracoes-actions.ts"
      to: "src/lib/validations.ts"
      via: "import de configuracoesSchema + safeParse"
      pattern: "configuracoesSchema\\.safeParse"
    - from: "src/actions/configuracoes-actions.ts"
      to: "/pipeline"
      via: "revalidatePath após o UPDATE (Pitfall 4 do RESEARCH)"
      pattern: "revalidatePath\\(\"/pipeline\"\\)"
    - from: "src/db/queries.ts"
      to: "tabela configuracoes"
      via: "select + insert getOrCreate"
      pattern: "configuracoes"
---

<objective>
Construir toda a camada de dados da configuração de dias-parado por etapa: a tabela singleton `configuracoes` (aplicada de fato no banco vivo), o schema Zod de validação, a leitura com semeadura idempotente e a Server Action de escrita.

Purpose: CONFIG-01/CONFIG-02 exigem que o limite de "esfriando" deixe de ser um literal no código e passe a ser um dado persistido e editável. Este plano entrega o contrato de dados que a UI (07-02) e o board do pipeline consomem — nada visual é construído aqui.
Output: `configuracoes` existindo em `./data/crm.db`, `configuracoesSchema`, `getConfiguracoes()` e `saveConfiguracoes()`.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-configura-o-de-dias-parado-por-etapa/07-CONTEXT.md
@.planning/phases/07-configura-o-de-dias-parado-por-etapa/07-RESEARCH.md
@.planning/phases/07-configura-o-de-dias-parado-por-etapa/07-PATTERNS.md

<interfaces>
<!-- Contratos existentes que o executor consome. Extraídos do código real. -->

De `src/db/client.ts`:
```typescript
export const db: BetterSQLite3Database<typeof schema>; // better-sqlite3, arquivo ./data/crm.db, WAL + foreign_keys=ON
```

De `src/actions/template-actions.ts` (padrão de ActionState replicado por toda Server Action do projeto):
```typescript
type ActionState =
  | { success: true }
  | { errors: Record<string, string[] | undefined> }
  | undefined;

export async function updateTemplate(_prevState: ActionState, formData: FormData): Promise<ActionState>;
```

De `src/types/index.ts` (padrão de derivação de tipo a partir do schema Drizzle):
```typescript
export type Template = InferSelectModel<typeof templates>;
```

Contratos NOVOS criados por este plano e consumidos pelo plano 07-02:
```typescript
// src/db/queries.ts
export type Configuracoes = typeof configuracoes.$inferSelect;
// { id: number; diasParadoNovo: number; diasParadoContatado: number; diasParadoNegociacao: number; updatedAt: Date }
export async function getConfiguracoes(): Promise<Configuracoes>;

// src/lib/validations.ts
export const configuracoesSchema; // 3 campos: diasParadoNovo, diasParadoContatado, diasParadoNegociacao
export type ConfiguracoesFormValues = z.input<typeof configuracoesSchema>;

// src/actions/configuracoes-actions.ts
export async function saveConfiguracoes(_prevState: ActionState, formData: FormData): Promise<ActionState>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tabela singleton `configuracoes` no schema + `configuracoesSchema` no Zod</name>
  <files>src/db/schema.ts, src/lib/validations.ts</files>
  <read_first>
    - `src/db/schema.ts` — forma real das tabelas `subnichos`/`templates`/`leads` (colunas tipadas, `sql\`(unixepoch())\`` como default de timestamp, imports já presentes no topo)
    - `src/lib/validations.ts` — padrão dos schemas existentes (`templateSchema`, `subnichoSchema`, `stageUpdateSchema`), mensagens de erro em PT-BR, uso de `z.input<>` para o tipo de form values
    - `.planning/phases/07-configura-o-de-dias-parado-por-etapa/07-PATTERNS.md` — seções `src/db/schema.ts` e `src/lib/validations.ts` (analogs e código-alvo verificados)
    - `.planning/phases/07-configura-o-de-dias-parado-por-etapa/07-RESEARCH.md` — Pattern 1 e Pitfall 3
  </read_first>
  <action>
    Em `src/db/schema.ts`, adicionar ao final do arquivo a tabela `configuracoes` (`sqliteTable("configuracoes", {...})`) com exatamente 5 colunas: `id: integer("id").primaryKey()` SEM `autoIncrement` (singleton, sempre 1); `diasParadoNovo: integer("dias_parado_novo").notNull().default(999999)`; `diasParadoContatado: integer("dias_parado_contatado").notNull().default(5)`; `diasParadoNegociacao: integer("dias_parado_negociacao").notNull().default(999999)`; `updatedAt: integer("updated_at", { mode: "timestamp" }).notNull().default(sql\`(unixepoch())\`)`. Sem índices (tabela de 1 linha), sem `CHECK` constraint (nenhuma tabela do projeto usa CHECK — a invariante "uma linha" é garantida no código de aplicação, mesmo precedente de `applyDefaultTemplate`). Nenhum import novo é necessário — `sql` e `integer` já estão importados no topo do arquivo. Os defaults NÃO são simétricos, e isso é deliberado (D-04 / Success Criteria #3 do ROADMAP): o código pré-deploy desta fase (`src/app/pipeline/page.tsx`) só flaga `stage === "contatado"` com `>= 5` dias — leads parados em Novo ou Negociação NUNCA são destacados como "esfriando", por mais tempo que fiquem parados. Semear Novo/Negociação com 5 faria o board destacar leads reais já no primeiro deploy, antes de qualquer ação do admin (o banco vivo `./data/crm.db` tem hoje leads em `novo` parados há 5+ dias) — exatamente a mudança de comportamento proibida por D-04. Por isso Novo e Negociação nascem com 999999 (≈ nunca esfria; D-03 não impõe teto máximo) e só Contatado nasce com 5. Não igualar os três defaults, não "arredondar" 999999 para um número menor e não inventar coluna de flag/exclusão.

    Adicionar um comentário JSDoc curto acima da tabela explicando que é singleton (`id` fixo = 1), que o default 5 de `dias_parado_contatado` existe para dar paridade com o hardcode pré-fase (D-04/CONFIG-02), que os defaults 999999 de `dias_parado_novo`/`dias_parado_negociacao` existem para que essas etapas continuem nunca esfriando até o admin salvar valores reais (mesma D-04) e que a semeadura da linha acontece em `getConfiguracoes()` e não em SQL de migração.

    Em `src/lib/validations.ts`, adicionar ao final `configuracoesSchema = z.object({...})` com os 3 campos `diasParadoNovo`, `diasParadoContatado`, `diasParadoNegociacao`, cada um `z.coerce.number().int().min(1, "Mínimo de 1 dia.")` — a mensagem é exatamente essa, travada pelo `07-UI-SPEC.md`. Sem teto máximo (D-03). Não usar `z.preprocess` nem `.transform`. Exportar também `export type ConfiguracoesFormValues = z.input<typeof configuracoesSchema>;`, mesmo padrão de `TemplateFormValues`.

    NÃO adicionar o tipo `Configuracoes` em `src/types/index.ts` — ele é exportado de `src/db/queries.ts` (Task 3), conforme decidido no 07-PATTERNS.md.
  </action>
  <verify>
    <automated>cd "C:/Users/Vencedor/Desktop/crm-leads" && npx tsc --noEmit && node -e "const s=require('fs').readFileSync('src/db/schema.ts','utf8');const v=require('fs').readFileSync('src/lib/validations.ts','utf8');const need=['sqliteTable(\"configuracoes\"','dias_parado_novo','dias_parado_contatado','dias_parado_negociacao','updated_at'];for(const n of need){if(!s.includes(n))throw new Error('schema.ts faltando: '+n)}if(/configuracoes[\s\S]*?primaryKey\(\{\s*autoIncrement/.test(s.slice(s.indexOf('configuracoes'))))throw new Error('configuracoes.id nao pode ser autoIncrement');const sn=s.replace(/\s+/g,'');for(const [col,def] of [['dias_parado_novo','999999'],['dias_parado_contatado','5'],['dias_parado_negociacao','999999']]){if(!sn.includes('\"'+col+'\").notNull().default('+def+')'))throw new Error('D-04: default errado em '+col+', esperado .notNull().default('+def+')')}const min=(v.match(/M.nimo de 1 dia\./g)||[]).length;if(min!==3)throw new Error('esperado 3x \'Minimo de 1 dia.\' em validations.ts, achei '+min);if(!v.includes('ConfiguracoesFormValues'))throw new Error('ConfiguracoesFormValues nao exportado');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` termina sem erros
    - `src/db/schema.ts` contém `export const configuracoes = sqliteTable("configuracoes"` e as 4 colunas SQL `dias_parado_novo`, `dias_parado_contatado`, `dias_parado_negociacao`, `updated_at`
    - A definição de `id` da tabela `configuracoes` NÃO contém `autoIncrement`
    - `dias_parado_contatado` tem `.notNull().default(5)` (D-04, paridade com o hardcode pré-fase) e `dias_parado_novo`/`dias_parado_negociacao` têm `.notNull().default(999999)` (essas etapas nunca esfriam antes de o admin salvar)
    - `src/lib/validations.ts` contém exatamente 3 ocorrências da string `"Mínimo de 1 dia."` e exporta `configuracoesSchema` e `ConfiguracoesFormValues`
    - Nenhum `CHECK`/`check(` foi adicionado a `src/db/schema.ts`
  </acceptance_criteria>
  <done>Tabela `configuracoes` definida no schema TypeScript e `configuracoesSchema` disponível para import; nada aplicado ao banco ainda.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Aplicar a tabela `configuracoes` ao banco vivo via drizzle-kit push</name>
  <files>./data/crm.db (banco local, não versionado)</files>
  <read_first>
    - `drizzle.config.ts` — confirma `dialect: "sqlite"`, `schema: "./src/db/schema.ts"`, `dbCredentials.url: "./data/crm.db"`
    - `.planning/phases/07-configura-o-de-dias-parado-por-etapa/07-RESEARCH.md` — Pitfall 1 (por que `generate` está proibido nesta fase) e Pitfall 2 (`push` não semeia dados)
    - `.planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-01-PLAN.md` — precedente do mesmo drift na Fase 6 e o fallback DDL manual usado lá
  </read_first>
  <action>
    Esta task é BLOQUEANTE: sem ela o build e o `tsc` passam (os tipos vêm do schema TypeScript, não do banco), produzindo um falso positivo de verificação — a aplicação quebraria em runtime com `no such table: configuracoes`.

    Rodar `npx drizzle-kit push --verbose` na raiz do projeto e LER o SQL que ele propõe antes de qualquer coisa. O único statement esperado é `CREATE TABLE \`configuracoes\` (...)`. NÃO rodar `npx drizzle-kit generate` nesta fase em hipótese nenhuma — o snapshot `src/db/migrations/meta/0002_snapshot.json` está divergente do banco real desde a Fase 4 (`templates`, `leads.import_batch_id`, `leads.contact_attempts`, `subnichos.deleted_at` existem no banco mas não no snapshot), e `generate` + `migrate` falharia com `duplicate column name`.

    GATE de segurança: se a saída do `push` propuser QUALQUER statement além do `CREATE TABLE configuracoes` — em particular `DROP TABLE`, `DELETE FROM`, `ALTER TABLE leads`, `ALTER TABLE templates`, `ALTER TABLE subnichos` — abortar imediatamente sem aplicar e usar o fallback abaixo. Se o comando ficar parado esperando confirmação interativa, abortar também e usar o fallback (não passar `--force`, que aplicaria statements destrutivos sem revisão).

    Fallback (precedente da Fase 6): aplicar o DDL diretamente com `better-sqlite3` via `node -e`, executando um único `CREATE TABLE configuracoes` equivalente ao que o conversor do drizzle-kit geraria — colunas `id integer PRIMARY KEY NOT NULL`, `dias_parado_novo integer DEFAULT 999999 NOT NULL`, `dias_parado_contatado integer DEFAULT 5 NOT NULL`, `dias_parado_negociacao integer DEFAULT 999999 NOT NULL`, `updated_at integer DEFAULT (unixepoch()) NOT NULL`.

    Não inserir nenhuma linha aqui — a semeadura da linha `id=1` é responsabilidade de `getConfiguracoes()` (Task 3), porque `push` nunca executa `INSERT` (Pitfall 2).

    Registrar no SUMMARY.md da fase, como débito técnico pré-existente (não escopo desta fase): a reconciliação do snapshot de migrações continua pendente desde a Fase 4/6.
  </action>
  <verify>
    <automated>cd "C:/Users/Vencedor/Desktop/crm-leads" && node -e "const D=require('better-sqlite3');const db=new D('./data/crm.db');const t=db.prepare(\"SELECT name FROM sqlite_master WHERE type='table' AND name='configuracoes'\").get();if(!t)throw new Error('tabela configuracoes NAO existe no banco vivo');const cols=db.prepare('PRAGMA table_info(configuracoes)').all().map(c=>c.name).sort();const esperado=['dias_parado_contatado','dias_parado_negociacao','dias_parado_novo','id','updated_at'];if(JSON.stringify(cols)!==JSON.stringify(esperado))throw new Error('colunas divergentes: '+cols.join(','));const defs=Object.fromEntries(db.prepare('PRAGMA table_info(configuracoes)').all().map(c=>[c.name,String(c.dflt_value)]));if(defs.dias_parado_novo!=='999999'||defs.dias_parado_contatado!=='5'||defs.dias_parado_negociacao!=='999999')throw new Error('D-04: defaults aplicados no banco divergem de 999999/5/999999: '+JSON.stringify(defs));const leads=db.prepare('SELECT COUNT(*) c FROM leads').get().c;console.log('OK — tabela criada com defaults corretos, leads preservados:',leads)"</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT name FROM sqlite_master WHERE name='configuracoes'` retorna uma linha no arquivo `./data/crm.db`
    - `PRAGMA table_info(configuracoes)` retorna exatamente as 5 colunas: `id`, `dias_parado_novo`, `dias_parado_contatado`, `dias_parado_negociacao`, `updated_at`
    - `PRAGMA table_info(configuracoes)` mostra `dflt_value` = `999999` para `dias_parado_novo` e `dias_parado_negociacao`, e `5` para `dias_parado_contatado` (D-04)
    - `SELECT COUNT(*) FROM leads` retorna o mesmo número de antes da task (nenhum dado destruído)
    - As tabelas `leads`, `subnichos` e `templates` continuam presentes em `sqlite_master`
    - Nenhum arquivo novo foi criado em `src/db/migrations/` (nenhum `generate` foi executado)
    - A tabela `configuracoes` está vazia neste ponto (`SELECT COUNT(*) FROM configuracoes` = 0) — a semeadura é da Task 3
  </acceptance_criteria>
  <done>A tabela `configuracoes` existe fisicamente em `./data/crm.db` com as 5 colunas corretas e nenhum dado existente foi perdido.</done>
</task>

<task type="auto">
  <name>Task 3: getConfiguracoes() com semeadura idempotente + Server Action saveConfiguracoes</name>
  <files>src/db/queries.ts, src/actions/configuracoes-actions.ts</files>
  <read_first>
    - `src/db/queries.ts` — padrão de query com JSDoc de justificativa (`getActiveDashboardLeads`) e estilo de imports do arquivo
    - `src/actions/template-actions.ts` — padrão completo de Server Action: `"use server"`, `type ActionState`, assinatura `(_prevState, formData)`, `safeParse` + `error.flatten().fieldErrors`, `revalidatePath` antes do `return { success: true }`, ausência deliberada de `try/catch`
    - `src/db/schema.ts` — a tabela `configuracoes` criada na Task 1
    - `src/lib/validations.ts` — `configuracoesSchema` criado na Task 1
    - `.planning/phases/07-configura-o-de-dias-parado-por-etapa/07-RESEARCH.md` — Pattern 2 (getOrCreate) e Pitfall 4 (revalidatePath de /pipeline)
  </read_first>
  <action>
    Em `src/db/queries.ts`, adicionar `export type Configuracoes = typeof configuracoes.$inferSelect;` e `export async function getConfiguracoes(): Promise<Configuracoes>`. A função faz um `db.select().from(configuracoes).where(eq(configuracoes.id, 1))`; se a linha existir, retorna; se não existir, executa `db.insert(configuracoes).values({ id: 1 }).returning()` (as demais colunas usam os defaults do schema: Novo=999999, Contatado=5, Negociação=999999 — nenhum desses números pode ser hardcoded dentro da função) e retorna a linha criada. Ajustar os imports do arquivo: adicionar `eq` ao import de `drizzle-orm` e `configuracoes` ao import de `@/db/schema`. Incluir JSDoc no mesmo tom dos comentários existentes explicando que este getOrCreate é o que satisfaz CONFIG-02/D-04 (Contatado nasce com 5, paridade com o hardcode pré-fase; Novo/Negociação nascem com 999999 e por isso continuam nunca esfriando até o admin salvar) e que a semeadura mora aqui — e não em SQL de migração — porque `drizzle-kit push` não executa INSERTs.

    Criar `src/actions/configuracoes-actions.ts` com `"use server"` no topo, o mesmo `type ActionState` local usado nos outros arquivos de action, e `export async function saveConfiguracoes(_prevState: ActionState, formData: FormData): Promise<ActionState>`. O corpo: `configuracoesSchema.safeParse(Object.fromEntries(formData))`; em falha, `return { errors: parsed.error.flatten().fieldErrors }` sem tocar no banco; em sucesso, `db.update(configuracoes).set({ ...parsed.data, updatedAt: new Date() }).where(eq(configuracoes.id, 1))`, depois `revalidatePath("/configuracoes")` E `revalidatePath("/pipeline")` (omitir o segundo deixa o board com os limites antigos até um F5 manual — Pitfall 4), e `return { success: true }`. Sem `id` vindo do FormData (singleton fixo, sem lookup) e sem `try/catch` (convenção do projeto: erros inesperados de DB propagam para o error boundary do Next.js).

    Ponto de segurança (T-07-01): o `safeParse` desta action é a validação AUTORITATIVA de D-03 (mínimo 1 dia). O `min={1}` do input HTML que o plano 07-02 vai adicionar é apenas UX antecipada e não substitui esta checagem — nunca remover ou relaxar o `safeParse` aqui.
  </action>
  <verify>
    <automated>cd "C:/Users/Vencedor/Desktop/crm-leads" && npx tsc --noEmit && node -e "const a=require('fs').readFileSync('src/actions/configuracoes-actions.ts','utf8');const q=require('fs').readFileSync('src/db/queries.ts','utf8');for(const n of ['\"use server\"','configuracoesSchema.safeParse','revalidatePath(\"/configuracoes\")','revalidatePath(\"/pipeline\")','eq(configuracoes.id, 1)'])if(!a.includes(n))throw new Error('action faltando: '+n);if(a.includes('try {'))throw new Error('try/catch nao segue a convencao do projeto');for(const n of ['export async function getConfiguracoes','export type Configuracoes'])if(!q.includes(n))throw new Error('queries faltando: '+n);const D=require('better-sqlite3');const db=new D('./data/crm.db');db.prepare('DELETE FROM configuracoes').run();console.log('linhas antes do getOrCreate:',db.prepare('SELECT COUNT(*) c FROM configuracoes').get().c);db.close();console.log('OK — estrutura verificada')"</automated>
    <automated>cd "C:/Users/Vencedor/Desktop/crm-leads" && node -e "const q=require('fs').readFileSync('src/db/queries.ts','utf8');const bloco=q.slice(q.indexOf('getConfiguracoes'));for(const n of ['eq(configuracoes.id, 1)','.insert(configuracoes)','.returning()'])if(!bloco.includes(n))throw new Error('getOrCreate faltando: '+n);if(!bloco.replace(/\s+/g,'').includes('values({id:1'))throw new Error('insert deve fixar id: 1');const D=require('better-sqlite3');const db=new D('./data/crm.db');const c=db.prepare('SELECT COUNT(*) c FROM configuracoes').get().c;if(c!==0)throw new Error('tabela deve terminar VAZIA neste plano (a semeadura em runtime e provada no 07-02), tem '+c+' linha(s)');console.log('OK — getOrCreate implementado, tabela vazia pronta para o teste de runtime do 07-02')"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` termina sem erros
    - `src/db/queries.ts` exporta `getConfiguracoes` e o tipo `Configuracoes`
    - O corpo de `getConfiguracoes()` contém `eq(configuracoes.id, 1)`, `.insert(configuracoes)` com `values({ id: 1 })` e `.returning()` — nenhum valor de dias é hardcoded na função (vêm dos defaults do schema — em especial, a função NÃO passa 5 para Novo/Negociação)
    - Ao final desta task a tabela `configuracoes` está VAZIA (`SELECT COUNT(*)` = 0) — a semeadura em runtime e sua idempotência são provadas no 07-02 (Task 2), no primeiro GET real de /configuracoes
    - `src/actions/configuracoes-actions.ts` começa com `"use server"` e contém `configuracoesSchema.safeParse`, `revalidatePath("/configuracoes")` e `revalidatePath("/pipeline")`
    - O arquivo de action NÃO contém `try {` (convenção do projeto)
    - O `db.update(...)` da action é filtrado por `eq(configuracoes.id, 1)` (nunca um update sem `where`)
  </acceptance_criteria>
  <done>A configuração pode ser lida (semeando Novo=999999, Contatado=5, Negociação=999999 no primeiro acesso) e escrita via Server Action com validação server-side de mínimo 1 dia e revalidação de /configuracoes e /pipeline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → Server Action `saveConfiguracoes` | O `FormData` chega de um endpoint HTTP interno e é totalmente não confiável — o `<input type="number" min={1}>` do cliente pode ser burlado por devtools ou por um POST direto |
| aplicação → `./data/crm.db` | Toda escrita passa por Drizzle; nenhum SQL é montado por concatenação de string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `saveConfiguracoes` (`src/actions/configuracoes-actions.ts`) | mitigate | `configuracoesSchema.safeParse(Object.fromEntries(formData))` com `z.coerce.number().int().min(1)` nos 3 campos — rejeita `0`, negativos, decimais e strings não numéricas ANTES de qualquer escrita; validação client-side é só UX (D-03/Pitfall 3) |
| T-07-02 | Tampering | `db.update(configuracoes)` | mitigate | Drizzle emite queries parametrizadas; o `where` é fixo em `eq(configuracoes.id, 1)` e nenhum identificador vem do FormData — não há superfície de SQL injection nem de update em massa |
| T-07-03 | Tampering | `drizzle-kit push` (Task 2) | mitigate | Gate explícito de revisão do SQL emitido; abortar se propuser `DROP TABLE`/`DELETE FROM`/`ALTER TABLE` em `leads`/`templates`/`subnichos`; `--force` proibido; verificação pós-aplicação confere `COUNT(*) FROM leads` inalterado |
| T-07-04 | Denial of Service | valor arbitrariamente grande em qualquer campo de dias | accept | Sem teto por decisão explícita (D-03); um inteiro enorme só faz nenhum lead ser marcado como esfriando — sem consumo de recurso, sem impacto em disponibilidade. Essa mesma propriedade é usada de propósito nos defaults 999999 de Novo/Negociação (D-04) |
| T-07-05 | Elevation of Privilege | rota/action sem autenticação | accept | App single-admin local por decisão de escopo do projeto (`CLAUDE.md`: sem autenticação multi-usuário); `/configuracoes` tem exatamente a mesma exposição de `/pipeline`, `/leads` e todas as outras rotas — esta fase não altera o modelo de acesso |
| T-07-SC | Tampering | instalações npm/pip/cargo | mitigate | Nenhum pacote novo é instalado nesta fase (`07-RESEARCH.md` §Package Legitimacy Audit: nenhum `[ASSUMED]`/`[SUS]`/`[SLOP]`). Se o executor concluir que precisa instalar QUALQUER dependência nova, parar e escalar para checkpoint humano antes de rodar `npm install` |
</threat_model>

<verification>
1. `npx tsc --noEmit` limpo em todo o repositório
2. `node -e` confirma `configuracoes` presente em `sqlite_master` com as 5 colunas esperadas e `leads` intacta
3. `getConfiguracoes()` implementa o getOrCreate (select id=1 → insert `{ id: 1 }` → returning), deixando a tabela vazia ao final deste plano; a prova em runtime de CONFIG-02/D-04 (linha nasce com Contatado=5 e Novo/Negociação=999999) é o primeiro GET real de /configuracoes no 07-02
4. `src/actions/configuracoes-actions.ts` contém as duas chamadas de `revalidatePath` exigidas
5. `npm run build` NÃO deve ser rodado com o dev server ativo neste host de 4GB (precedente de OOM registrado no STATE.md) — se rodar, parar o dev server antes
</verification>

<success_criteria>
- A tabela `configuracoes` existe em `./data/crm.db` com as 5 colunas corretas e nenhum dado pré-existente foi perdido
- Os defaults da tabela são 999999 (Novo), 5 (Contatado) e 999999 (Negociação) — D-04: nenhum lead de Novo/Negociação passa a esfriar no dia do deploy
- `configuracoesSchema` rejeita `0`, negativos e não-numéricos com a mensagem exata "Mínimo de 1 dia."
- `saveConfiguracoes` persiste em `id=1` e revalida `/configuracoes` e `/pipeline`
- Nenhum arquivo novo em `src/db/migrations/`, nenhum `drizzle-kit generate` executado
- `npx tsc --noEmit` limpo
</success_criteria>

<output>
Criar `.planning/phases/07-configura-o-de-dias-parado-por-etapa/07-01-SUMMARY.md` ao concluir, registrando o débito técnico do snapshot de migrações divergente (herdado das Fases 4 e 6, deliberadamente não resolvido aqui).
</output>
