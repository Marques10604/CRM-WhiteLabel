---
phase: 05-notas-enriquecidas-na-importa-o-csv
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/csv-import.ts
  - scripts/verify-notas-concat.cjs
autonomous: true
requirements: [IMPORT-04, IMPORT-05]
must_haves:
  truths:
    - "mapCsvRows produz um campo notas concatenado contendo TODAS as colunas extras marcadas que têm valor na linha, sem perder nenhuma (IMPORT-04)"
    - "Uma linha cuja coluna Notas 1-pra-1 está vazia mas com colunas extras preenchidas NUNCA contém o texto 'Importado via CSV.' (Pitfall 1 do 05-RESEARCH.md)"
    - "Um CSV simples com apenas a coluna Notas mapeada 1-pra-1 produz exatamente o mesmo texto que produz hoje, sem rótulo e sem sufixo (IMPORT-05 / D-11)"
    - "Uma linha sem nenhum conteúdo (Notas vazia e todas as colunas extras vazias) recebe CSV_DEFAULTS.notas (D-11 / Pitfall 5)"
    - "A ordem das colunas no texto final é a ordem das colunas no arquivo CSV, nunca a ordem em que o admin marcou os checkboxes (D-08)"
    - "Uma coluna já usada em qualquer campo fixo do mapping nunca é concatenada uma segunda vez como coluna extra (Pitfall 4)"
    - "D-06: colunas extras são unidas por quebra de linha simples (\\n), sem separador final"
    - "D-07: uma célula vazia numa coluna extra omite a linha inteira daquela coluna — nunca 'header: ' vazio nem '(não informado)'"
    - "D-10: quando a coluna Notas 1-pra-1 E colunas extras estão preenchidas, o valor de Notas entra primeiro sem rótulo, seguido das extras com rótulo, na ordem do CSV (D-08) — nada se perde"
  artifacts:
    - path: "src/lib/csv-import.ts"
      provides: "Tipo CsvExtraNotasColumns, função pura buildNotasText e mapCsvRows com 3º parâmetro opcional"
      exports: ["CsvExtraNotasColumns", "buildNotasText", "mapCsvRows", "CSV_DEFAULTS"]
      contains: "buildNotasText"
    - path: "scripts/verify-notas-concat.cjs"
      provides: "Harness de verificação comportamental (10 cenários D-05..D-11 + Pitfalls 1/4/5) executando o código-fonte real de csv-import.ts"
      contains: "transpileModule"
  key_links:
    - from: "src/lib/csv-import.ts (mapCsvRows)"
      to: "src/lib/csv-import.ts (buildNotasText)"
      via: "fallback CSV_DEFAULTS.notas aplicado SOBRE o resultado final concatenado"
      pattern: "buildNotasText\\([\\s\\S]{0,200}\\|\\| CSV_DEFAULTS\\.notas"
    - from: "scripts/verify-notas-concat.cjs"
      to: "src/lib/csv-import.ts"
      via: "lê e transpila o arquivo real (não uma cópia) via typescript.transpileModule"
      pattern: "src/lib/csv-import\\.ts"
---

<objective>
Estender o motor puro de mapeamento CSV (`src/lib/csv-import.ts`) para concatenar múltiplas colunas de origem em um único campo `notas` formatado, implementando D-05 a D-11 do `05-CONTEXT.md` — com o fallback `CSV_DEFAULTS.notas` aplicado sobre o RESULTADO FINAL concatenado (nunca sobre o valor bruto da coluna "Notas" isolada, que é a regressão central identificada no Pitfall 1 do `05-RESEARCH.md`).

Purpose: Toda a lógica de formatação precisa existir e estar provada ANTES de qualquer UI ser construída — o plano 05-02 (wizard + checkboxes) consome o tipo `CsvExtraNotasColumns` e a assinatura estendida de `mapCsvRows` criados aqui. Preserva o invariante do projeto "o componente de prévia só renderiza, nunca recalcula".
Output: `csv-import.ts` com tipo irmão + `buildNotasText` exportada + `mapCsvRows` de 3 parâmetros; harness de verificação comportamental rodando 10 cenários contra o código-fonte real.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-notas-enriquecidas-na-importa-o-csv/05-CONTEXT.md
@.planning/phases/05-notas-enriquecidas-na-importa-o-csv/05-RESEARCH.md
@.planning/phases/05-notas-enriquecidas-na-importa-o-csv/05-PATTERNS.md

<interfaces>
<!-- Contratos ATUAIS lidos diretamente do código em 2026-07-29. Use estes; não explore o codebase para redescobri-los. -->

src/lib/csv-import.ts (estado atual):
  export type CsvFieldKey = "nome" | "telefone" | "subnichoNome" | "canal" | "origem" | "valorEstimado" | "notas"
  export type CsvColumnMapping = Record<CsvFieldKey, string | null>   // null = coluna não mapeada
  export type ParsedCsvRow = Record<string, string>
  export type MappedCsvRow = { rowIndex, nome, telefone, telefoneNormalizado: string|null, subnichoNome, canal: "instagram"|"whatsapp", origem, valorEstimado, notas }
  export const CSV_DEFAULTS = { canal: "whatsapp", origem: "Importação CSV", notas: "Importado via CSV.", valorEstimado: "0" } as const
  export function mapCsvRows(rows: ParsedCsvRow[], mapping: CsvColumnMapping): MappedCsvRow[]
    // closure interna readMapped(row, field): header null ou ausente em row => ""; senão (row[header] ?? "").trim()
    // linha 68 (A LINHA A SUBSTITUIR): const notas = readMapped(row, "notas") || CSV_DEFAULTS.notas;
  export function detectWithinBatchDuplicatePhones(rows: MappedCsvRow[]): Set<number>
  import { normalizePhone } from "@/lib/phone"   // única linha de import do arquivo

Contrato ALVO a ser criado neste plano (consumido pelo plano 05-02):
  export type CsvExtraNotasColumns = string[]
  export function buildNotasText(row: ParsedCsvRow, mapping: CsvColumnMapping, extraColumns: CsvExtraNotasColumns, csvHeaderOrder: string[]): string   // "" é retorno válido
  export function mapCsvRows(rows: ParsedCsvRow[], mapping: CsvColumnMapping, extraNotasColumns?: CsvExtraNotasColumns): MappedCsvRow[]   // 3º param default []

Consumidor server-side (INALTERADO neste plano):
  src/lib/validations.ts linha 34: notas: z.string().trim().min(1, "Notas são obrigatórias.")
  src/actions/import-actions.ts: bulkImportLeads insere notas como string comum
</interfaces>

Nota de ambiente (host de 4GB, ver CLAUDE.md e STATE.md): rode os comandos de verificação UM DE CADA VEZ, sequencialmente. Não rode `npm run build` neste plano (o dev server está rodando em localhost:3000 e o build já derrubou o worker do Next por falta de memória neste host) — `npm run build` está reservado para o plano 05-02, com o dev server parado.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tipo CsvExtraNotasColumns + buildNotasText + mapCsvRows de 3 parâmetros</name>
  <files>src/lib/csv-import.ts</files>
  <read_first>
    - src/lib/csv-import.ts (arquivo inteiro, 110 linhas — veja o estado real de `readMapped`, `CSV_DEFAULTS` e da linha 68 antes de editar)
    - .planning/phases/05-notas-enriquecidas-na-importa-o-csv/05-RESEARCH.md (Pattern 2, linhas 199-272 — código de referência de `buildNotasText`/`mapCsvRows`; Pitfalls 1 a 5, linhas 325-365)
    - .planning/phases/05-notas-enriquecidas-na-importa-o-csv/05-PATTERNS.md (seção `src/lib/csv-import.ts`, linhas 20-123 — convenção de comentário-cabeçalho citando D-xx e o idioma exato de acesso à linha)
    - .planning/phases/05-notas-enriquecidas-na-importa-o-csv/05-CONTEXT.md (D-05 a D-11 — regras de formatação travadas)
  </read_first>
  <action>
    Edite APENAS `src/lib/csv-import.ts`. Três mudanças, nesta ordem:

    (1) TIPO IRMÃO. Logo abaixo de `export type CsvColumnMapping` adicione `export type CsvExtraNotasColumns = string[]`, com comentário curto em português citando D-01/D-02 ("nomes de header do CSV marcados como coluna extra para notas; vazio por padrão"). NÃO adicione um novo membro em `CsvFieldKey` e NÃO altere a forma de `CsvColumnMapping` — ele continua um `Record` fechado sobre os 7 campos fixos.

    (2) FUNÇÃO PURA EXPORTADA `buildNotasText(row, mapping, extraColumns, csvHeaderOrder)` retornando `string`, colocada entre `CSV_DEFAULTS` e `mapCsvRows`, com comentário-cabeçalho em português citando D-05 a D-11 (mesma convenção dos comentários já existentes no arquivo). Comportamento exato:
      - Acumule pedaços em um array `parts: string[]`.
      - PRIMEIRO pedaço (D-10): se `mapping.notas` não for `null` e o header existir em `row`, leia `(row[mapping.notas] ?? "").trim()` — MESMO idioma de acesso de `readMapped`, não invente um helper novo — e empurre para `parts` SEM rótulo (não prefixe com "Notas:") quando não for string vazia.
      - DEDUP (Pitfall 4): monte `fixedHeaders` = `Set` de todos os valores não-nulos de `Object.values(mapping)` (os 7 campos fixos, não só `notas`), e `extraSet` = `Set` de `extraColumns` filtrado removendo qualquer header presente em `fixedHeaders`. Isso torna o array de estado tolerante a valores órfãos sem lógica de limpeza ativa.
      - PEDAÇOS SEGUINTES (D-08): itere sobre `csvHeaderOrder` (a ordem do arquivo), NUNCA sobre `extraColumns` (a ordem de clique do admin). Para cada header presente em `extraSet`: leia o valor com o mesmo idioma `(row[header] ?? "").trim()`; se for string vazia, pule a coluna inteira (D-07 — nunca emita "header: " vazio nem "(não informado)"); senão empurre a string `` `${header}: ${value}` `` usando o nome EXATO do cabeçalho do CSV como rótulo, sem tradução, sem capitalização (D-05).
      - Retorne `parts.join("\n")` (D-06 — quebra de linha simples entre pedaços, sem separador final). String vazia é um retorno válido e esperado; NÃO aplique nenhum fallback aqui.
      - Sem `throw`, sem I/O, sem dependência de React — função pura, mesmo contrato de `detectWithinBatchDuplicatePhones`.

    (3) `mapCsvRows` PASSA A ACEITAR 3 PARÂMETROS: `mapCsvRows(rows: ParsedCsvRow[], mapping: CsvColumnMapping, extraNotasColumns: CsvExtraNotasColumns = [])`. Dentro dela, ANTES do `rows.map`, derive `const csvHeaderOrder = Object.keys(rows[0] ?? {})` — fonte ÚNICA de ordem de colunas, calculada internamente a partir do próprio parâmetro `rows` (Pitfall 3: não aceite `csvHeaderOrder` como parâmetro externo e não crie uma segunda função de cálculo de headers). Substitua a linha 68 atual (`const notas = readMapped(row, "notas") || CSV_DEFAULTS.notas;`) por duas linhas: primeiro o resultado de `buildNotasText(row, mapping, extraNotasColumns, csvHeaderOrder)` em uma const, e SÓ DEPOIS `|| CSV_DEFAULTS.notas` sobre esse resultado final. A string `readMapped(row, "notas")` deve DESAPARECER do arquivo — sua permanência é a assinatura exata da regressão do Pitfall 1. Nenhuma outra linha de `mapCsvRows` muda (nome, telefone, subnichoNome, canal, origem, valorEstimado e o objeto de retorno ficam idênticos), e `readMapped` continua sendo usada pelos outros 6 campos.

    O default `= []` no 3º parâmetro é obrigatório: mantém qualquer chamador de 2 argumentos compilando sem alteração (Assumption A1 do 05-RESEARCH.md).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>node -e "const fs=require('fs');const raw=fs.readFileSync('src/lib/csv-import.ts','utf8');const code=raw.replace(/\/\*[\s\S]*?\*\//g,'').replace(/^\s*\/\/.*$/gm,'');const need=['export type CsvExtraNotasColumns','export function buildNotasText','extraNotasColumns','Object.keys(rows[0]'];need.forEach(t=>{if(!code.includes(t))throw new Error('faltou no codigo: '+t)});if(code.includes('readMapped(row, \"notas\")'))throw new Error('REGRESSAO Pitfall 1: readMapped(row, \"notas\") ainda existe');if(!/buildNotasText\([\s\S]{0,300}\|\| CSV_DEFAULTS\.notas/.test(code))throw new Error('fallback CSV_DEFAULTS.notas nao esta aplicado sobre o resultado de buildNotasText');console.log('OK asserts de fonte');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/csv-import.ts` exporta `CsvExtraNotasColumns` (alias de `string[]`) e a função `buildNotasText` com a assinatura de 4 parâmetros `(row, mapping, extraColumns, csvHeaderOrder)`
    - `mapCsvRows` tem 3º parâmetro `extraNotasColumns` com default `[]` e deriva `csvHeaderOrder` de `Object.keys(rows[0] ?? {})` internamente
    - A substring `readMapped(row, "notas")` NÃO existe mais no arquivo (fora de comentários)
    - O código contém `buildNotasText(...)` seguido, em até 300 caracteres, de `|| CSV_DEFAULTS.notas` — provando que o fallback é aplicado sobre o resultado final
    - `CsvFieldKey` continua com exatamente 7 membros e `CsvColumnMapping` continua `Record<CsvFieldKey, string | null>`
    - `npx tsc --noEmit` sai com código 0
  </acceptance_criteria>
  <done>Motor de concatenação implementado em `csv-import.ts`, com o fallback na ordem correta e o tipo público que o plano 05-02 vai consumir.</done>
</task>

<task type="auto">
  <name>Task 2: Harness de verificação comportamental (10 cenários) contra o código real</name>
  <files>scripts/verify-notas-concat.cjs</files>
  <read_first>
    - src/lib/csv-import.ts (já modificado na Task 1 — confirme a assinatura exportada real antes de escrever asserções contra ela)
    - scripts/guard-no-hard-delete.cjs (precedente do projeto para script `.cjs` de verificação: estilo, saída no console, `process.exit(1)` em falha)
    - .planning/phases/05-notas-enriquecidas-na-importa-o-csv/05-RESEARCH.md (Pitfalls 1, 4 e 5 — os três cenários de regressão obrigatórios)
    - .planning/phases/05-notas-enriquecidas-na-importa-o-csv/05-CONTEXT.md (D-05 a D-11 — resultados esperados exatos de cada cenário)
  </read_first>
  <action>
    Crie `scripts/verify-notas-concat.cjs` (CommonJS, sem dependência nova — usa apenas `fs` e o pacote `typescript` que já está em devDependencies).

    MECANISMO DE CARGA (não existe test runner neste projeto e nenhum pacote novo pode ser instalado nesta fase): o script lê `src/lib/csv-import.ts` como texto, substitui a ÚNICA linha de import (`import { normalizePhone } from "@/lib/phone";` — o alias `@/` não resolve fora do bundler) por um stub local equivalente que devolve os dígitos do telefone ou `null`, transpila o texto resultante com `require("typescript").transpileModule` usando `module: CommonJS` / `target: ES2020`, e avalia o JS gerado com `new Function("module","exports","require", js)` para obter `buildNotasText`, `mapCsvRows` e `CSV_DEFAULTS`. Isso testa o CÓDIGO-FONTE REAL, não uma cópia — se a regex de substituição do import não casar (arquivo refatorado), o script deve falhar explicitamente com mensagem "import de @/lib/phone nao encontrado", nunca seguir em silêncio.

    FIXTURE: uma `mapping` base com `nome: "nome"`, `telefone: "telefone"` e os demais `null`, e linhas cujas chaves aparecem nesta ordem de arquivo: `nome, score, telefone, sinal_dor, trecho_dor, observacao`.

    CENÁRIOS OBRIGATÓRIOS (cada um com mensagem de erro própria citando o D-xx/Pitfall que ele protege; use igualdade estrita de string, não `includes`, exceto onde indicado):
      S1 (IMPORT-05/D-11): `mapping.notas = "observacao"`, extras `[]`, célula `observacao = "texto livre"` → `notas` === `"texto livre"` (sem rótulo, sem sufixo).
      S2 (D-11): `mapping.notas = null`, extras `[]` → `notas` === `CSV_DEFAULTS.notas`.
      S3 (D-10/D-05/D-06): `mapping.notas = "observacao"` com valor, extras `["score","sinal_dor"]` → `notas` === `"texto livre\nscore: 4\nsinal_dor: dor_confirmada"` (coluna de notas primeiro e sem rótulo; extras com header exato como rótulo; junção por `\n`).
      S4 (Pitfall 1 — REGRESSÃO CRÍTICA): `mapping.notas = "observacao"` com célula VAZIA, extras `["score","sinal_dor"]` com valores → o resultado NÃO contém `CSV_DEFAULTS.notas` em lugar nenhum E é exatamente `"score: 4\nsinal_dor: dor_confirmada"`.
      S5 (D-08): extras passadas na ordem `["observacao","sinal_dor","score"]` (ordem de clique invertida) → a saída sai na ordem do arquivo: `score` antes de `sinal_dor` antes de `observacao`. Compare a string inteira.
      S6 (D-07): extras `["score","sinal_dor","trecho_dor"]` com `sinal_dor` vazio nessa linha → a saída não contém a substring `"sinal_dor"` e não contém `": \n"` nem termina com `": "`.
      S7 (Pitfall 5): extras marcadas globalmente mas TODAS as células dessa linha vazias e `mapping.notas` apontando para célula vazia → `notas` === `CSV_DEFAULTS.notas` (garante que `csvRowSchema.notas.min(1)` nunca é violado).
      S8 (Pitfall 4): header `observacao` usado em `mapping.origem` E também presente em `extraColumns` → `observacao` aparece EXATAMENTE UMA VEZ na saída (conte ocorrências, não use `includes`).
      S9 (Assumption A1): `mapCsvRows(rows, mapping)` chamado com 2 argumentos produz exatamente o mesmo `notas` que o comportamento de hoje (coluna mapeada ou default), sem lançar.
      S10 (D-08 + Pitfall 3): `buildNotasText` chamada diretamente com um `csvHeaderOrder` que inclui um header ausente em `row` não lança e simplesmente ignora esse header.

    SAÍDA: em sucesso, imprimir `OK: 10 cenarios de concatenacao de notas` e sair com 0; em falha, imprimir o nome do cenário + esperado vs. recebido e `process.exit(1)`. Não adicione script novo em `package.json` (o comando é `node scripts/verify-notas-concat.cjs`) — evita tocar em `package.json` nesta fase.
  </action>
  <verify>
    <automated>node scripts/verify-notas-concat.cjs</automated>
    <automated>node -e "const s=require('fs').readFileSync('scripts/verify-notas-concat.cjs','utf8');if(!s.includes('transpileModule'))throw new Error('harness nao usa typescript.transpileModule');if(!s.includes('src/lib/csv-import.ts'))throw new Error('harness nao le o arquivo real');['S1','S4','S5','S7','S8'].forEach(t=>{if(!s.includes(t))throw new Error('faltou cenario '+t)});const pkg=require('fs').readFileSync('package.json','utf8');if(pkg.includes('verify-notas-concat'))throw new Error('package.json foi tocado — fora do escopo desta fase');console.log('OK asserts do harness');"</automated>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-notas-concat.cjs` sai com código 0 e imprime `OK: 10 cenarios de concatenacao de notas`
    - O harness carrega `src/lib/csv-import.ts` real via `typescript.transpileModule` (não redefine a lógica localmente)
    - Os cenários S1, S4, S5, S7 e S8 estão presentes e nomeados no arquivo
    - `package.json` NÃO contém `verify-notas-concat` (nenhuma dependência nem script novo nesta fase)
    - `npm run lint` sai com código 0
    - Rodar o harness com a linha antiga restaurada em `csv-import.ts` faria S4 falhar (comprove mentalmente ao escrever S4: a asserção precisa ser sensível à ordem do fallback, não apenas a `includes`)
  </acceptance_criteria>
  <done>Regressão do Pitfall 1 (e mais 9 regras de D-05 a D-11) provada por comando automatizado repetível, sem instalar nada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Arquivo CSV (fornecido pelo admin) → parser/motor de mapeamento client-side | Conteúdo de célula e nomes de header entram como texto não confiável; mesmo boundary já aceito pela Fase 2 |
| Motor client-side (`mapCsvRows`) → Server Action `bulkImportLeads` | O texto concatenado atravessa como string comum, validado por `csvRowSchema` (inalterado) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering (XSS) | `buildNotasText` — conteúdo de célula CSV concatenado em `notas` | mitigate | A função só produz string (`header: valor` separados por `\n`), nunca HTML. A renderização a jusante é JSX escapado por padrão (`accessorKey: "notas"` em `csv-import-preview-table.tsx`); `grep -r dangerouslySetInnerHTML src/` retorna zero ocorrências hoje e é proibido introduzir uma nesta fase. Nenhum caminho de renderização como HTML existe para `notas` no projeto. |
| T-05-02 | Tampering | Nome de header do CSV usado como rótulo dentro do texto de notas (D-05) | accept | O CSV é fornecido pelo próprio admin (single-user, ferramenta local); headers já são exibidos crus como opções de `SelectItem` desde a Fase 2. Não amplia superfície de ataque além do boundary já aceito. |
| T-05-03 | Denial of Service | Concatenação por linha em CSV com muitas colunas/linhas | accept | Custo O(linhas × colunas extras) sobre um arquivo limitado a 10MB por `MAX_FILE_SIZE_BYTES` (guarda já existente no wizard); volume real ≈ 19 colunas × poucos milhares de linhas, client-side, sem impacto no servidor. |
| T-05-04 | Tampering (bypass de validação) | `notas` vazia driblando `csvRowSchema.notas.min(1)` | mitigate | Fallback `\|\| CSV_DEFAULTS.notas` aplicado sobre o resultado FINAL por linha garante string não-vazia (cenários S7/S4 do harness). A validação server-side em `src/lib/validations.ts` permanece a autoridade final e NÃO é enfraquecida nem alterada por este plano. |
| T-05-SC | Tampering | Supply chain (npm/pip/cargo) | mitigate | Zero instalações neste plano; o harness usa apenas `fs` e o `typescript` já presente em devDependencies. Acceptance criteria da Task 2 falha se `package.json` for tocado. |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo (comando único, sequencial — host de 4GB)
- `node scripts/verify-notas-concat.cjs` verde nos 10 cenários
- `npm run lint` limpo
- Asserção de fonte confirma ausência de `readMapped(row, "notas")` e presença do fallback sobre o resultado de `buildNotasText`
- `git diff --stat` mostra exatamente 2 arquivos: `src/lib/csv-import.ts` e `scripts/verify-notas-concat.cjs` (nenhum componente, nenhum schema, nenhum `package.json`)
</verification>

<success_criteria>
- IMPORT-04 (metade motor): todas as colunas extras marcadas com valor aparecem no texto final, na ordem do arquivo, com rótulo = header exato
- IMPORT-05 (metade motor): CSV simples 1-pra-1 e CSV sem nada mapeado produzem exatamente o comportamento de hoje
- Pitfall 1 impossível de reintroduzir sem quebrar o cenário S4 do harness
- Contrato público (`CsvExtraNotasColumns`, `mapCsvRows` de 3 params) pronto para o plano 05-02 consumir
</success_criteria>

<output>
Criar `.planning/phases/05-notas-enriquecidas-na-importa-o-csv/05-01-SUMMARY.md` ao concluir.
</output>
