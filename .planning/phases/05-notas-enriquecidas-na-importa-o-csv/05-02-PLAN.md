---
phase: 05-notas-enriquecidas-na-importa-o-csv
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/csv-column-mapper.tsx
  - src/components/csv-import-wizard.tsx
autonomous: true
requirements: [IMPORT-04, IMPORT-05]
must_haves:
  truths:
    - "No passo 'Mapeie as colunas', o admin vê uma seção 'Colunas extras para notas (opcional)' com um checkbox por coluna do CSV ainda não usada em nenhum dos 7 campos fixos (SC #1 / D-01/D-02/D-04)"
    - "Marcar/desmarcar checkboxes atualiza ao vivo um texto 'Serão concatenadas: a → b → c' na ordem do arquivo CSV (SC #4 / D-08/D-09)"
    - "Se nenhuma coluna ficar sem mapeamento, a seção inteira não é renderizada — sem mensagem de vazio (D-03)"
    - "Ao clicar em 'Ver prévia', a coluna Notas da tabela de prévia mostra o texto concatenado final por linha (SC #2)"
    - "Voltar ao mapeamento preserva as colunas extras marcadas, exatamente como já preserva o mapping 1-pra-1"
    - "Um CSV simples continua importando sem exigir nenhuma interação com a nova seção: 'Ver prévia' continua habilitado apenas por nome+telefone (SC #3 / IMPORT-05)"
  artifacts:
    - path: "src/components/csv-column-mapper.tsx"
      provides: "Seção de checkboxes de colunas extras + resumo ao vivo, totalmente controlada por props"
      contains: "Colunas extras para notas (opcional)"
    - path: "src/components/csv-import-wizard.tsx"
      provides: "extraNotasColumns no WizardState (mapping e preview), handler e passagem para mapCsvRows"
      contains: "extraNotasColumns"
  key_links:
    - from: "src/components/csv-import-wizard.tsx"
      to: "src/lib/csv-import.ts (mapCsvRows)"
      via: "3º argumento com as colunas extras marcadas ao montar mappedRows"
      pattern: "mapCsvRows\\(state\\.parsedRows, state\\.mapping, state\\.extraNotasColumns\\)"
    - from: "src/components/csv-import-wizard.tsx"
      to: "src/components/csv-column-mapper.tsx"
      via: "props extraNotasColumns + onExtraNotasColumnsChange (mesmo padrão de mapping/onMappingChange)"
      pattern: "onExtraNotasColumnsChange"
    - from: "src/components/csv-column-mapper.tsx"
      to: "ordem do arquivo CSV (prop headers)"
      via: "resumo ao vivo derivado de headers.filter(...), nunca da ordem de clique"
      pattern: "headers\\.filter\\("
---

<objective>
Adicionar ao passo 2 do wizard de importação a seção "Colunas extras para notas (opcional)" (checkboxes das colunas ainda não mapeadas) com resumo ao vivo da ordem de concatenação, e transportar esse estado pelo `WizardState` até a chamada de `mapCsvRows` estendida no plano 05-01 — fechando SC #1, #2, #3 e #4 da Phase 5.

Purpose: O motor de concatenação (05-01) só ganha valor quando o admin consegue escolher as colunas. Esta é a metade de UI de IMPORT-04, mantendo IMPORT-05 intacto (CSV simples não exige nenhuma interação nova).
Output: `/importar` passo 2 com seção de checkboxes + texto "Serão concatenadas: ..."; `csv-import-preview-table.tsx` exibindo o texto final sem nenhuma mudança de código.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-notas-enriquecidas-na-importa-o-csv/05-CONTEXT.md
@.planning/phases/05-notas-enriquecidas-na-importa-o-csv/05-UI-SPEC.md
@.planning/phases/05-notas-enriquecidas-na-importa-o-csv/05-PATTERNS.md
@.planning/phases/05-notas-enriquecidas-na-importa-o-csv/05-RESEARCH.md

<interfaces>
<!-- Contratos reais lidos do código em 2026-07-29 + o contrato criado pelo plano 05-01. Use estes; não explore o codebase para redescobri-los. -->

Do plano 05-01 (@.planning/phases/05-notas-enriquecidas-na-importa-o-csv/05-01-SUMMARY.md):
  export type CsvExtraNotasColumns = string[]                      // de "@/lib/csv-import"
  export function mapCsvRows(rows, mapping, extraNotasColumns?: CsvExtraNotasColumns): MappedCsvRow[]

src/components/csv-column-mapper.tsx (estado atual):
  const NONE_VALUE = "__nenhuma__"
  FIELD_CONFIGS: 7 entradas { key, label, required } — nome*, telefone*, subnichoNome, canal, origem, valorEstimado, notas
  type CsvColumnMapperProps = { headers: string[]; mapping: CsvColumnMapping; onMappingChange: (m: CsvColumnMapping) => void; detectedDelimiter: string; detectedEncoding: "UTF-8"|"Windows-1252"; onContinue: () => void }
  const canContinue = Boolean(mapping.nome && mapping.telefone)      // NÃO alterar
  function handleFieldChange(key, value) { onMappingChange({ ...mapping, [key]: value === NONE_VALUE ? null : value }) }
  Estrutura de render: <div className="flex flex-col gap-6"> → [cabeçalho h2+2 p] → [<div className="flex flex-col gap-4 rounded-lg bg-[#F4F4F5] p-6"> com FIELD_CONFIGS.map] → [<div className="flex justify-end"> com Button "Ver prévia"]

src/components/csv-import-wizard.tsx (estado atual):
  EMPTY_MAPPING: CsvColumnMapping = { nome: null, telefone: null, subnichoNome: null, canal: null, origem: null, valorEstimado: null, notas: null }
  WizardState = { step:"upload"; fileName?; error? } | { step:"mapping"; fileName; parsedRows; detectedDelimiter; detectedEncoding; mapping } | { step:"preview"; ...mesmos campos...; mappedRows }
  handleFileSelected → setState({ step:"mapping", ..., mapping: { ...EMPTY_MAPPING } })
  handleMappingChange(mapping) { if (state.step !== "mapping") return; setState({ ...state, mapping }); }
  handleContinueToPreview() { ... const rows = mapCsvRows(state.parsedRows, state.mapping); ... setState({ ...state, step:"preview", mappedRows: rows }); }
  handleBackToMapping() — NÃO usa spread; lista explicitamente fileName/parsedRows/detectedDelimiter/detectedEncoding/mapping
  Render do passo mapping: <CsvColumnMapper headers={Object.keys(state.parsedRows[0] ?? {})} mapping={...} onMappingChange={...} detectedDelimiter={...} detectedEncoding={...} onContinue={handleContinueToPreview} />

Idioma de checkbox do projeto (precedente exato, csv-import-preview-table.tsx linhas 143-151):
  <label className="flex items-center gap-2 text-sm text-foreground"> com <input type="checkbox" className="size-4 accent-[#0D9488]" checked={...} onChange={...} /> e o texto do rótulo como filho do label.
  NÃO existe src/components/ui/checkbox.tsx e é PROIBIDO rodar `npx shadcn add checkbox` (OOM já registrado com `popover` neste host de 4GB).

src/components/csv-import-preview-table.tsx: SEM MUDANÇA — já renderiza row.notas via { accessorKey: "notas", header: "Notas" }.
</interfaces>

Nota de ambiente (host de 4GB, ver CLAUDE.md/STATE.md): comandos de verificação UM DE CADA VEZ. Antes de `npm run build`, PARE o dev server que está rodando em localhost:3000 (o build concorrente com o dev server já derrubou o worker do Next por falta de memória neste host); depois do build, o dev server pode ser religado para o `<human-check>`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seção de checkboxes + resumo ao vivo no mapper e estado no wizard</name>
  <files>src/components/csv-column-mapper.tsx, src/components/csv-import-wizard.tsx</files>
  <read_first>
    - src/components/csv-column-mapper.tsx (arquivo inteiro, 121 linhas — estrutura de render e handleFieldChange)
    - src/components/csv-import-wizard.tsx (linhas 28-36 EMPTY_MAPPING, 55-73 WizardState, 213-259 handlers, 296-306 render do mapper)
    - src/components/csv-import-preview-table.tsx (linhas 135-155 — idioma exato do checkbox nativo a copiar; e o comentário de invariante nas linhas 28-30, que NÃO pode ser violado)
    - src/lib/csv-import.ts (contrato já estendido pelo plano 05-01: CsvExtraNotasColumns e mapCsvRows de 3 params)
    - .planning/phases/05-notas-enriquecidas-na-importa-o-csv/05-UI-SPEC.md (Spacing Scale, Typography e Copywriting Contract — valores prescritivos exatos)
    - .planning/phases/05-notas-enriquecidas-na-importa-o-csv/05-CONTEXT.md (D-01 a D-04, D-08, D-09)
  </read_first>
  <action>
    Duas edições atômicas (as duas juntas, para que `npx tsc --noEmit` fique verde ao fim da task — props novas obrigatórias no mapper exigem o wizard já passando elas).

    (A) `src/components/csv-column-mapper.tsx`:
      - Import: acrescente `CsvExtraNotasColumns` ao import de tipo já existente de `@/lib/csv-import` (mesma linha, nenhum módulo novo).
      - Props: adicione a `CsvColumnMapperProps` os campos `extraNotasColumns: CsvExtraNotasColumns` e `onExtraNotasColumnsChange: (columns: CsvExtraNotasColumns) => void` (obrigatórios, não opcionais) e destruture-os na assinatura do componente. Componente segue 100% controlado — NÃO introduza `useState` interno.
      - Derivação a cada render (sem `useMemo`, volume pequeno — Pattern 3 do 05-RESEARCH.md): `mappedHeaders` = `Set` dos valores não-nulos de `Object.values(mapping)`; `unmappedHeaders` = `headers.filter(h => !mappedHeaders.has(h))`. Recalcular a cada render é obrigatório (Pitfall 4: se um `Select` fixo passar a apontar para uma coluna já marcada, o checkbox dela precisa sumir na hora).
      - Handler `handleToggleExtraColumn(header: string)`: espelha `handleFieldChange` — se `extraNotasColumns.includes(header)`, chame `onExtraNotasColumnsChange(extraNotasColumns.filter(h => h !== header))`; senão `onExtraNotasColumnsChange([...extraNotasColumns, header])`. O array guarda apenas QUAIS colunas estão marcadas; a ordem dele nunca é usada para exibição nem para concatenação (D-08 / Pitfall 2).
      - Render: insira um `<div>` irmão IMEDIATAMENTE ABAIXO do container `FIELD_CONFIGS` e ACIMA do `<div className="flex justify-end">` do botão (D-01), renderizado apenas quando `unmappedHeaders.length > 0` (D-03 — sem mensagem de vazio, sem placeholder). Classes prescritas pelo 05-UI-SPEC, sem inventar valores novos:
          • container: `flex flex-col gap-4 rounded-lg bg-[#F4F4F5] p-6`
          • rótulo da seção: `<p className="text-sm font-semibold">` com o texto EXATO `Colunas extras para notas (opcional)` (D-04 — sem `h2`/`h3`, sem variação de copy)
          • lista: `<div className="flex flex-col gap-2">` com um `<label key={header} className="flex items-center gap-2 text-sm text-foreground">` por header, contendo `<input type="checkbox" className="size-4 accent-[#0D9488]" checked={extraNotasColumns.includes(header)} onChange={() => handleToggleExtraColumn(header)} />` e o nome do header como texto puro (pass-through, sem capitalizar/traduzir — D-05)
          • resumo ao vivo (D-09), renderizado só quando pelo menos uma coluna está marcada: `<p className="text-sm text-muted-foreground">` com `Serão concatenadas: ` seguido de `headers.filter(h => extraNotasColumns.includes(h)).join(" → ")`. A ordem DEVE vir de `headers.filter(...)` (ordem do arquivo) — usar `extraNotasColumns.join(...)` ou `.map(...)` diretamente é a violação de D-08/Pitfall 2 e é proibido. Separador exatamente `" → "` (espaço, seta, espaço).
      - `canContinue` fica IDÊNTICO (`Boolean(mapping.nome && mapping.telefone)`) — colunas extras nunca bloqueiam nem habilitam "Ver prévia" (SC #3). Copy e estilo do botão "Ver prévia" inalterados.
      - Comentário curto em português citando D-01/D-02/D-03/D-04/D-08/D-09 acima da nova seção, seguindo a convenção do arquivo.

    (B) `src/components/csv-import-wizard.tsx`:
      - Import: acrescente `type CsvExtraNotasColumns` ao bloco de import já existente de `@/lib/csv-import`.
      - Constante `const EMPTY_EXTRA_NOTAS_COLUMNS: CsvExtraNotasColumns = [];` logo abaixo de `EMPTY_MAPPING` (mesma convenção `EMPTY_*`).
      - `WizardState`: adicione `extraNotasColumns: CsvExtraNotasColumns` aos membros `"mapping"` E `"preview"` (não ao `"upload"`).
      - `handleFileSelected`: no `setState({ step: "mapping", ... })` acrescente `extraNotasColumns: [...EMPTY_EXTRA_NOTAS_COLUMNS]` ao lado de `mapping: { ...EMPTY_MAPPING }` (cada upload novo começa sem nenhuma coluna extra marcada — D-11).
      - Novo `handleExtraNotasColumnsChange(extraNotasColumns: CsvExtraNotasColumns)` espelhando `handleMappingChange` byte a byte no formato: guarda `if (state.step !== "mapping") return;` seguida de `setState({ ...state, extraNotasColumns })`.
      - `handleContinueToPreview`: troque a chamada para `mapCsvRows(state.parsedRows, state.mapping, state.extraNotasColumns)`. O restante (checagem `hasAnyValidRow`, reset de `previewSupportData`/`overrides`, `setState({ ...state, step: "preview", mappedRows: rows })`) fica inalterado — o spread já carrega `extraNotasColumns` para o passo preview.
      - `handleBackToMapping`: esta função NÃO usa spread, lista campo a campo — acrescente explicitamente `extraNotasColumns: state.extraNotasColumns` à lista (esquecer isso perde a seleção do admin ao voltar).
      - Render do passo `"mapping"`: acrescente `extraNotasColumns={state.extraNotasColumns}` e `onExtraNotasColumnsChange={handleExtraNotasColumnsChange}` ao `<CsvColumnMapper .../>`. A prop `headers` continua `Object.keys(state.parsedRows[0] ?? {})` — mesma fonte de ordem que `mapCsvRows` deriva internamente de `rows[0]` (Pitfall 3), não introduza uma segunda derivação.

    NÃO toque em `src/components/csv-import-preview-table.tsx`, em `src/lib/validations.ts`, em `src/actions/import-actions.ts` nem em `package.json`. Nenhum componente novo em `src/components/ui/`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>node -e "const fs=require('fs');const strip=s=>s.replace(/\{\/\*[\s\S]*?\*\/\}/g,'').replace(/\/\*[\s\S]*?\*\//g,'').replace(/^\s*\/\/.*$/gm,'');const m=strip(fs.readFileSync('src/components/csv-column-mapper.tsx','utf8'));['Colunas extras para notas (opcional)','Serão concatenadas:','type=\"checkbox\"','accent-[#0D9488]','unmappedHeaders','onExtraNotasColumnsChange','headers.filter('].forEach(t=>{if(!m.includes(t))throw new Error('mapper faltou: '+t)});if(!m.includes('Boolean(mapping.nome && mapping.telefone)'))throw new Error('canContinue foi alterado — SC #3 quebrado');if(/extraNotasColumns\.(join|map)\(/.test(m))throw new Error('D-08/Pitfall 2: resumo usa ordem de clique em vez da ordem do arquivo');if(m.includes('ui/checkbox'))throw new Error('primitivo shadcn checkbox proibido nesta fase');const w=strip(fs.readFileSync('src/components/csv-import-wizard.tsx','utf8'));['EMPTY_EXTRA_NOTAS_COLUMNS','handleExtraNotasColumnsChange','mapCsvRows(state.parsedRows, state.mapping, state.extraNotasColumns)'].forEach(t=>{if(!w.includes(t))throw new Error('wizard faltou: '+t)});const back=w.slice(w.indexOf('function handleBackToMapping'));if(!back.slice(0,600).includes('extraNotasColumns: state.extraNotasColumns'))throw new Error('handleBackToMapping nao preserva extraNotasColumns');console.log('OK asserts de UI/estado');"</automated>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `csv-column-mapper.tsx` contém o texto exato `Colunas extras para notas (opcional)`, o container `rounded-lg bg-[#F4F4F5] p-6`, e um `<input type="checkbox" className="size-4 accent-[#0D9488]">` por header não mapeado
    - O resumo usa `headers.filter(...)` + `" → "`; nenhuma ocorrência de `extraNotasColumns.join(` ou `extraNotasColumns.map(` no arquivo (fora de comentários)
    - A seção só é renderizada quando `unmappedHeaders.length > 0` (D-03) e `canContinue` continua exatamente `Boolean(mapping.nome && mapping.telefone)`
    - `csv-import-wizard.tsx` tem `extraNotasColumns` nos membros `mapping` e `preview` do `WizardState`, chama `mapCsvRows(state.parsedRows, state.mapping, state.extraNotasColumns)` e lista `extraNotasColumns: state.extraNotasColumns` dentro de `handleBackToMapping`
    - Nenhum import de `@/components/ui/checkbox` e nenhum arquivo novo em `src/components/ui/`
    - `npx tsc --noEmit` e `npm run lint` saem com código 0
  </acceptance_criteria>
  <done>Passo 2 do wizard oferece a seleção de colunas extras com resumo ao vivo, e o estado chega íntegro até `mapCsvRows`.</done>
</task>

<task type="auto">
  <name>Task 2: Guardas de regressão de escopo + verificação integrada da fase</name>
  <files>src/components/csv-column-mapper.tsx, src/components/csv-import-wizard.tsx</files>
  <read_first>
    - src/components/csv-import-preview-table.tsx (confirmar que segue intacto e que a coluna "Notas" continua sendo só `accessorKey`)
    - scripts/verify-notas-concat.cjs (harness do plano 05-01 — rodar de novo agora que a UI existe, garantindo que nada no motor regrediu)
    - .planning/ROADMAP.md (Phase 5, Success Criteria #1 a #4 — a checagem humana abaixo é a prova final deles)
  </read_first>
  <action>
    Nenhuma mudança funcional nova. Esta task fecha a fase provando que o escopo não vazou e que o fluxo inteiro funciona:

    (1) Rode o harness do 05-01 novamente (`node scripts/verify-notas-concat.cjs`) — o motor precisa continuar verde depois das mudanças de UI.
    (2) Confirme por `git status --porcelain` / `git diff --name-only HEAD` que os únicos arquivos alterados na fase são `src/lib/csv-import.ts`, `scripts/verify-notas-concat.cjs`, `src/components/csv-column-mapper.tsx` e `src/components/csv-import-wizard.tsx`. Qualquer alteração em `src/components/csv-import-preview-table.tsx`, `src/lib/validations.ts`, `src/actions/import-actions.ts`, `package.json`, `package-lock.json`, `drizzle/` ou `src/db/` é violação de escopo e deve ser revertida.
    (3) PARE o dev server (localhost:3000) antes de rodar `npm run build`; rode o build isolado (host de 4GB — nunca em paralelo com o dev server nem com outro processo pesado). Religue o dev server depois, para a checagem humana.
    (4) Se algum passo falhar, corrija dentro dos 2 arquivos desta fase (`csv-column-mapper.tsx`/`csv-import-wizard.tsx`) — não expanda para arquivos fora da lista acima.
  </action>
  <verify>
    <automated>node scripts/verify-notas-concat.cjs</automated>
    <automated>node -e "const {execSync}=require('child_process');const changed=execSync('git diff --name-only HEAD').toString().split('\n').map(s=>s.trim()).filter(Boolean);const allowed=['src/lib/csv-import.ts','scripts/verify-notas-concat.cjs','src/components/csv-column-mapper.tsx','src/components/csv-import-wizard.tsx'];const leaked=changed.filter(f=>!allowed.includes(f)&&!f.startsWith('.planning/'));if(leaked.length)throw new Error('escopo vazou para: '+leaked.join(', '));console.log('OK escopo: '+changed.join(', '));"</automated>
    <automated>npm run build</automated>
    <human-check>Com o dev server religado, abra http://localhost:3000/importar e faça o percurso completo com um CSV de prospecção do cowork (colunas nome, score, telefone, sinal_dor, trecho_dor, observacao): (1) no passo "Mapeie as colunas", mapeie só Nome e Telefone — a seção "Colunas extras para notas (opcional)" deve listar score, sinal_dor, trecho_dor e observacao como checkboxes [SC #1]; (2) marque score e trecho_dor fora de ordem (trecho_dor primeiro) — o texto "Serão concatenadas: score → trecho_dor" deve aparecer na ordem do arquivo [SC #4]; (3) clique em "Ver prévia" — a coluna Notas deve mostrar "score: N" e "trecho_dor: ..." em linhas separadas, sem "Importado via CSV." misturado [SC #2]; (4) clique em "Voltar ao mapeamento" — os checkboxes marcados devem continuar marcados; (5) mapeie a coluna Notas 1-pra-1 para observacao e confira que observacao SAI da lista de checkboxes e que a prévia mostra o texto de observacao sem rótulo, na primeira linha, seguido das extras; (6) repita com um CSV simples (só nome, telefone, observacao) mapeando Notas para observacao e nenhuma extra marcada — a importação deve funcionar exatamente como antes [SC #3]; (7) se todas as colunas do CSV estiverem mapeadas em campos fixos, a seção inteira deve sumir [D-03].</human-check>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-notas-concat.cjs` continua saindo com código 0 após as mudanças de UI
    - `git diff --name-only HEAD` (fora de `.planning/`) lista no máximo os 4 arquivos permitidos; `csv-import-preview-table.tsx`, `validations.ts`, `import-actions.ts` e `package.json` NÃO aparecem
    - `npm run build` sai com código 0 com o dev server parado, e a rota `/importar` aparece na saída do build
    - `<human-check>` percorrido com os 7 passos aprovados pelo admin (SC #1 a #4 + D-03 + persistência do "Voltar")
  </acceptance_criteria>
  <done>Fase 5 verificada ponta a ponta: motor + UI, escopo contido em 4 arquivos, build limpo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nomes de header do CSV (texto do arquivo do admin) → rótulos renderizados nos checkboxes e no resumo ao vivo | Texto não confiável renderizado na UI do passo de mapeamento |
| Estado client-side do wizard (`extraNotasColumns`) → `mapCsvRows` → Server Action `bulkImportLeads` | Seleção do admin influencia o conteúdo do campo `notas` enviado ao servidor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering (XSS) | Nome de header renderizado como rótulo de checkbox e dentro de "Serão concatenadas: ..." | mitigate | Renderização como texto filho de JSX (`{header}`), escapado automaticamente pelo React; proibido `dangerouslySetInnerHTML` (zero ocorrências em `src/` hoje). Mesmo tratamento que os headers já recebem nos `SelectItem` desde a Fase 2. |
| T-05-06 | Tampering (duplicação de dado) | Coluna usada em campo fixo E marcada como extra | mitigate | Dupla defesa: `unmappedHeaders` recalculado a cada render a partir do `mapping` atual (sem `useMemo`/cache), e filtro por `Object.values(mapping)` dentro de `buildNotasText` (plano 05-01, cenário S8 do harness). Valores órfãos no estado são tolerados sem duplicar saída. |
| T-05-07 | Information Disclosure | Resumo ao vivo exibindo nomes de coluna | accept | Só expõe nomes de header do arquivo que o próprio admin acabou de subir, já visíveis nos `Select`s do mesmo passo; ferramenta single-user local, sem outro observador. |
| T-05-08 | Tampering (bypass de validação) | `notas` chegando vazia ao servidor por seleção do admin | mitigate | Fallback `CSV_DEFAULTS.notas` sobre o resultado final (05-01) + `csvRowSchema.notas.min(1)` em `src/lib/validations.ts` permanece inalterado como autoridade server-side. Este plano não toca em nenhum caminho de validação. |
| T-05-SC | Tampering | Supply chain (npm) | mitigate | Zero instalações; `npx shadcn add checkbox` explicitamente proibido (checkbox HTML nativo, precedente do projeto). A Task 2 falha se `package.json`/`package-lock.json` aparecerem no diff. |
</threat_model>

<verification>
- `npx tsc --noEmit` e `npm run lint` limpos (sequenciais, um por vez)
- Asserções de fonte confirmam copy exata da UI-SPEC, checkbox nativo, ordem derivada de `headers.filter(...)` e `canContinue` intocado
- `node scripts/verify-notas-concat.cjs` verde (motor não regrediu)
- Guarda de escopo: diff limitado a 4 arquivos de código
- `npm run build` limpo com o dev server parado
- `<human-check>` cobrindo SC #1 a #4, D-03 e persistência do "Voltar ao mapeamento"
</verification>

<success_criteria>
- SC #1: seção de checkboxes lista todas as colunas não mapeadas (D-01/D-02/D-04)
- SC #2: prévia mostra o texto concatenado final por linha, sem recomputar nada no componente de prévia
- SC #3: CSV simples importa sem configuração extra; "Ver prévia" segue governado só por nome+telefone
- SC #4: resumo ao vivo mostra as colunas na ordem do arquivo antes de confirmar (D-08/D-09)
- IMPORT-04 e IMPORT-05 completos ao fim deste plano
</success_criteria>

<output>
Criar `.planning/phases/05-notas-enriquecidas-na-importa-o-csv/05-02-SUMMARY.md` ao concluir.
</output>
