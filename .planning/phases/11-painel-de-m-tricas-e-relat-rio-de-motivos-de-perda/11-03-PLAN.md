---
phase: 11-painel-de-m-tricas-e-relat-rio-de-motivos-de-perda
plan: 03
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - src/components/motivo-perda-combobox.tsx
  - src/lib/validations.ts
  - src/db/schema.ts
  - src/actions/lead-actions.ts
  - src/components/lead-form-dialog.tsx
  - src/components/motivo-perda-dialog.tsx
  - src/components/pipeline-board.tsx
  - src/app/pipeline/page.tsx
  - scripts/verify-motivo-perda-obrigatorio.cjs
  - package.json
autonomous: true
requirements: [PERDA-01]

must_haves:
  truths:
    - "Mover um lead para 'Perdido' sem escolher motivo é rejeitado pelo servidor com a mensagem 'Selecione o motivo da perda.'"
    - "O admin escolhe o motivo numa lista pesquisável, nas DUAS superfícies: modal de drag no pipeline e formulário de edição do lead"
    - "Digitar um nome que não existe na lista oferece 'Criar \"{texto}\"' e, ao selecionar, o motivo é criado e já fica selecionado (D-03)"
    - "Cancelar o modal de motivo reverte o drag — o lead volta à etapa anterior e updateLeadStage NÃO é chamado"
    - "Tirar um lead de 'Perdido' limpa o motivo gravado (motivoPerdaId volta a null)"
    - "Editar um lead perdido cujo motivo foi removido continua mostrando o motivo selecionado, sem campo vazio"
  artifacts:
    - path: "src/components/motivo-perda-combobox.tsx"
      provides: "combobox pesquisável com criação-na-hora (primeiro combobox criável do projeto)"
      min_lines: 60
    - path: "src/lib/validations.ts"
      provides: "motivoPerdaId condicional-obrigatório em stageUpdateSchema e leadSchema"
      contains: "motivoPerdaId"
    - path: "src/actions/lead-actions.ts"
      provides: "gravação de motivoPerdaId + motivoPerdaExists + backstop de FK"
      contains: "motivoPerdaExists"
    - path: "scripts/verify-motivo-perda-obrigatorio.cjs"
      provides: "gate permanente de D-04 (obrigatoriedade condicional + ausência da superfície antiga)"
      min_lines: 60
  key_links:
    - from: "src/components/motivo-perda-combobox.tsx"
      to: "src/actions/motivo-perda-actions.ts"
      via: "createMotivoPerda dentro de startTransition, seleção pelo id retornado"
      pattern: "createMotivoPerda"
    - from: "src/lib/validations.ts"
      to: "stage === perdido"
      via: ".refine com path motivoPerdaId"
      pattern: "path: \\[\"motivoPerdaId\"\\]"
    - from: "src/components/pipeline-board.tsx"
      to: "src/actions/lead-actions.ts"
      via: "updateLeadStage(leadId, newStage, motivoPerdaId: number)"
      pattern: "updateLeadStage\\("
    - from: "src/app/pipeline/page.tsx"
      to: "motivosPerda"
      via: "db.select() no Promise.all, passado como prop ao board"
      pattern: "motivosPerda"
---

<objective>
Trocar a captura de motivo de perda de texto livre opcional para seleção obrigatória de uma lista
governada (D-03 + D-04), nas DUAS superfícies reais de captura — o modal de drag no board do pipeline
e o formulário de edição do lead — com a obrigatoriedade reforçada de forma autoritativa no servidor.

Este é o plano que fecha a troca FK iniciada em 11-01: a coluna morta `leads.motivoPerda` (texto) sai
do schema Drizzle e `motivoPerdaId` passa a ser a única fonte de verdade em `src/`.

**Janela intencional de typecheck:** a Task 2 troca os contratos Zod e o servidor; os consumidores de
UI só ficam coerentes ao fim da Task 3. `npx tsc --noEmit` só é exigido verde no fim da Task 3 — mesmo
precedente já registrado em STATE.md para a Fase 09-01. Não interromper o plano entre as duas tasks.

Purpose: sem isto, PERDA-01 nunca coleta dado — o relatório de motivos nasceria permanentemente vazio.
Output: combobox criável, contratos Zod condicionais, servidor autoritativo, 2 superfícies migradas.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-painel-de-m-tricas-e-relat-rio-de-motivos-de-perda/11-CONTEXT.md
@.planning/phases/11-painel-de-m-tricas-e-relat-rio-de-motivos-de-perda/11-RESEARCH.md
@.planning/phases/11-painel-de-m-tricas-e-relat-rio-de-motivos-de-perda/11-PATTERNS.md
@.planning/phases/11-painel-de-m-tricas-e-relat-rio-de-motivos-de-perda/11-UI-SPEC.md
@.planning/debug/resolved/subnicho-combobox-vazio.md
@.planning/phases/11-painel-de-m-tricas-e-relat-rio-de-motivos-de-perda/11-02-SUMMARY.md
</context>

<interfaces>
Contratos disponíveis após 11-01/11-02 (não explorar o repositório):

`src/actions/motivo-perda-actions.ts` (plano 11-02):
`createMotivoPerda(_prevState, formData: FormData)` devolve
`{ success: true; id: number } | { errors: { nome: string[] } } | undefined`.

`src/types/index.ts`: `MotivoPerda` = `{ id: number; nome: string; deletedAt: Date | null; createdAt: Date }`.

Estado ATUAL a ser modificado:

`src/lib/validations.ts`
- `leadSchema` (linhas 10-45) — objeto com `motivoPerda: z.string().trim().optional()` na linha 44
- `csvRowSchema` (linha 64) — derivado de `leadSchema.omit({ subnichoId: true, followUpDate: true }).extend({...})`
- `stageUpdateSchema` (linhas 72-76) — `{ id, stage, motivoPerda: z.string().trim().optional() }`
- `LeadFormValues = z.input<typeof leadSchema>`

`src/actions/lead-actions.ts`
- `subnichoExists(subnichoId)` (linhas 41-47) — SELECT indiferente a `deletedAt`, com doc-comment explicando por quê
- `isForeignKeyViolation(err)` (linhas 22-29) — compara `err.code === "SQLITE_CONSTRAINT_FOREIGNKEY"`
- `updateLead` linha 132: `motivoPerda: parsed.data.stage === "perdido" ? parsed.data.motivoPerda : null`
- `updateLeadStage(id: number, stage: Lead["stage"], motivoPerda?: string)` (linha 169) e a mesma
  atribuição condicional na linha 197
- Idioma condicional-por-VALOR-ALVO já usado 4x no arquivo (linhas 132, 145, 197, 203)

`src/components/lead-form-dialog.tsx`
- props incluem `subnichos: Subnicho[]` (linha 48)
- `defaultValues` (linhas 114-134) inclui `motivoPerda: lead?.motivoPerda ?? ""`
- `<SubnichoCombobox>` via `<Controller control={form.control} name="subnichoId" render={...} />` (linhas 322-333) — molde exato
- bloco condicional `form.watch("stage") === "perdido"` (linhas 372-387) com `<Textarea id="motivoPerda" {...form.register("motivoPerda")} />`

`src/components/motivo-perda-dialog.tsx` (79 linhas): props `{ open, onOpenChange, leadNome, onSkip, onSave: (motivo: string) => void }`,
estado `useState("")`, `<Textarea>`, botões "Pular" (outline) e "Salvar motivo" (teal).

`src/components/pipeline-board.tsx`
- `motivoQueueRef` tipado `{ leadId; leadNome; resolve: (motivo: string | undefined) => void }[]` (linhas 82-84)
- `handleDragEnd` (linhas 123-157): `motivoPerda = await new Promise<string | undefined>(...)`, depois `updateLeadStage(leadId, newStage, motivoPerda)`
- `resolveMotivoPerda(motivo: string | undefined)` (linhas 159-171) — faz `shift()` da fila e avança para o próximo pendente
- `<MotivoPerdaDialog>` (linhas 231-239) com `onOpenChange`/`onSkip` chamando `resolveMotivoPerda(undefined)`
- `useOptimistic` (linhas 86-90) — se `updateLeadStage` nunca é chamado, o estado base não muda e o React reverte o otimista sozinho ao assentar a transição

`src/app/pipeline/page.tsx`: `Promise.all([...])` (linhas 31-42) já busca `db.select().from(subnichos)` como `allSubnichos` e passa `subnichos={allSubnichos}` ao board.

`src/components/subnicho-combobox.tsx` (83 linhas): shell `Combobox`/`ComboboxInput`/`ComboboxContent`/`ComboboxEmpty`/`ComboboxList`/`ComboboxItem`,
`items` memoizado com o filtro `deletedAt === null || id === value`, `selectedItem` derivado, input nativo oculto via prop `name`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: MotivoPerdaCombobox — seleção pesquisável com criação-na-hora (D-03)</name>
  <files>src/components/motivo-perda-combobox.tsx</files>
  <read_first>
    - src/components/subnicho-combobox.tsx (arquivo inteiro, 83 linhas — molde estrutural)
    - src/components/ui/combobox.tsx (API dos primitivos, incluindo o suporte a ícone inline em ComboboxItem)
    - src/actions/motivo-perda-actions.ts (shape do ActionState com id)
    - .planning/debug/resolved/subnicho-combobox-vazio.md (por que o filtro anti-soft-delete precisa da exceção `id === value`)
    - .planning/phases/11-painel-de-m-tricas-e-relat-rio-de-motivos-de-perda/11-UI-SPEC.md (§MotivoPerdaCombobox, linhas 183-190; §Copywriting linhas 115-117)
    - .planning/phases/11-painel-de-m-tricas-e-relat-rio-de-motivos-de-perda/11-PATTERNS.md (seção "src/components/motivo-perda-combobox.tsx")
  </read_first>
  <action>
    Criar `src/components/motivo-perda-combobox.tsx` (`"use client"`), copiando o shell de
    `subnicho-combobox.tsx`. Props: `motivosPerda: MotivoPerda[]`, `value: number | null`,
    `onValueChange: (motivoPerdaId: number | null) => void`, `name?: string` (default `"motivoPerdaId"`),
    `disabled?: boolean`, `invalid?: boolean`.

    COPIAR SEM MUDANÇA o filtro anti-soft-delete: `items` memoizado a partir de
    `motivosPerda.filter((m) => m.deletedAt === null || m.id === value).map((m) => ({ value: m.id, label: m.nome }))`.
    A exceção `m.id === value` é obrigatória — sem ela, editar um lead perdido cujo motivo foi
    removido mostraria o campo vazio (causa-raiz documentada em `.planning/debug/resolved/subnicho-combobox-vazio.md`).
    Copiar também o `selectedItem` derivado e o input nativo oculto via prop `name`, que alimenta o
    FormData de `lead-form-dialog.tsx`.

    Placeholder do input: `Selecione ou digite um motivo...`. `ComboboxEmpty`: `Nenhum motivo encontrado.`

    NOVO (primeiro combobox criável do projeto — sem análogo interno): manter o texto digitado num
    estado local (`query`). Quando `query` tem conteúdo e, comparando por `trim()` + minúsculas, NÃO
    casa com nenhum `item.label`, renderizar como ÚLTIMA entrada da lista um `ComboboxItem` de ação
    com o rótulo `Criar "{query}"`, precedido do ícone `Plus` de `lucide-react` com classe `size-4`, e
    texto em `text-[#0D9488]` (única linha do combobox que é ação, não dado — é a exceção explícita da
    regra "accent nunca em conteúdo de dados" do 11-UI-SPEC.md linha 74). Quando o texto digitado casa
    exatamente com um item existente, o item "Criar" NÃO aparece.

    Ao selecionar a entrada de criação:
    1. montar um `FormData` com o campo `nome` = query e chamar `createMotivoPerda` dentro de
       `startTransition` (via `useTransition`);
    2. enquanto `isPending`, aplicar `disabled` no input do combobox;
    3. no sucesso, chamar `onValueChange(result.id)` usando o `id` devolvido pela action (é para isso
       que o `ActionState` foi ampliado no plano 11-02) e fechar o popup;
    4. no erro, MANTER o popup aberto e exibir a mensagem de `result.errors.nome[0]` numa linha
       `text-sm text-[#DC2626]`, mesmo padrão de erro de campo já usado em `subnicho-manager.tsx`.

    A action `createMotivoPerda` é chamada diretamente (não via `useActionState`) porque o combobox
    precisa do retorno síncrono da promessa para selecionar o id — `useActionState` só expõe o estado
    no render seguinte. Não usar `useEffect` para reagir ao resultado.

    Se algum falso-positivo do React Compiler (`react-hooks/refs` ou `react-hooks/set-state-in-effect`)
    aparecer no `npx eslint`, suprimir com `eslint-disable-next-line` DOCUMENTADO, mesmo padrão já
    aceito no projeto (STATE.md, decisão 07-02) — nunca mudar o comportamento para contornar o lint.
  </action>
  <verify>
    <automated>cd "C:/Users/Vencedor/Desktop/crm-leads" && npx tsc --noEmit && npx eslint src/components/motivo-perda-combobox.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/motivo-perda-combobox.tsx` começa com `"use client"` e exporta `MotivoPerdaCombobox`
    - Contém o filtro literal com `deletedAt === null` E `=== value` na mesma expressão (exceção anti-soft-delete)
    - Contém `createMotivoPerda`, `useTransition` e `Plus` importado de `lucide-react`
    - Contém os literais `Selecione ou digite um motivo...`, `Nenhum motivo encontrado.` e o rótulo de criação com `Criar "`
    - Contém `text-[#0D9488]` aplicado à linha de criação e `text-[#DC2626]` na mensagem de erro inline
    - NÃO contém `useActionState` (a action é chamada diretamente para obter o id de volta)
    - `npx tsc --noEmit` sai com código 0 (o componente ainda não é consumido por ninguém nesta task — adição isolada)
    - `npx eslint src/components/motivo-perda-combobox.tsx` sai com código 0
  </acceptance_criteria>
  <done>Combobox criável pronto e isolado, consumível pelas duas superfícies da Task 2 e da Task 3.</done>
</task>

<task type="auto">
  <name>Task 2: Contratos Zod condicionais (D-04), servidor autoritativo e formulário de lead</name>
  <files>src/lib/validations.ts, src/db/schema.ts, src/actions/lead-actions.ts, src/components/lead-form-dialog.tsx</files>
  <read_first>
    - src/lib/validations.ts (arquivo inteiro, 165 linhas — leadSchema, csvRowSchema, stageUpdateSchema)
    - src/actions/lead-actions.ts (linhas 1-215 — subnichoExists, isForeignKeyViolation, updateLead, updateLeadStage)
    - src/db/schema.ts (coluna morta `motivoPerda` marcada no plano 11-01)
    - src/components/lead-form-dialog.tsx (props linha 48, defaultValues 114-134, Controller de subnicho 322-333, bloco condicional 372-387)
    - .planning/phases/11-painel-de-m-tricas-e-relat-rio-de-motivos-de-perda/11-PATTERNS.md (seções "src/lib/validations.ts", "src/actions/lead-actions.ts", "src/components/lead-form-dialog.tsx")
    - .planning/phases/11-painel-de-m-tricas-e-relat-rio-de-motivos-de-perda/11-UI-SPEC.md (§Copywriting linhas 118-120)
  </read_first>
  <action>
    `src/lib/validations.ts` — refatorar em camada, mesmo idioma DRY já documentado no arquivo
    (`interacaoManualSchema`, `configuracoesServerSchema`):
    - Extrair o objeto atual de `leadSchema` para `leadBaseSchema` (mesmo conteúdo), trocando o campo
      `motivoPerda: z.string().trim().optional()` por `motivoPerdaId: z.coerce.number().int().positive().optional()`.
    - `leadSchema` passa a ser `leadBaseSchema` com um `.refine` condicional D-04: reprova quando
      `stage === "perdido"` e `motivoPerdaId` é nulo/indefinido, com `path: ["motivoPerdaId"]` e a
      mensagem VERBATIM `Selecione o motivo da perda.` (11-UI-SPEC.md linha 120).
    - `csvRowSchema` passa a derivar de `leadBaseSchema` (NÃO de `leadSchema`), com
      `.omit({ subnichoId: true, followUpDate: true, motivoPerdaId: true })` e o `.extend` já existente.
      Motivo do `leadBaseSchema`: `.omit()` só existe em objeto não-refinado, e o CSV nunca traz motivo
      de perda (leads importados nunca nascem em "perdido"). Documentar isso num comentário citando D-04.
    - `stageUpdateSchema`: trocar `motivoPerda` por `motivoPerdaId: z.coerce.number().int().positive().optional()`
      e aplicar o MESMO `.refine` condicional com `path: ["motivoPerdaId"]` e a mesma mensagem.
    - `LeadFormValues` continua sendo `z.input<typeof leadSchema>`.

    `src/db/schema.ts` — remover a coluna morta `motivoPerda: text("motivo_perda")` da declaração de
    `leads`, deixando um comentário curto no lugar registrando que a coluna FÍSICA continua no banco
    (nunca é dropada — 11-RESEARCH.md Open Question 2) mas não é mais lida nem escrita por `src/`.

    `src/actions/lead-actions.ts`:
    - Criar `motivoPerdaExists(motivoPerdaId: number): Promise<boolean>` copiando `subnichoExists`
      linha por linha (SELECT do `id` em `motivosPerda`, PROPOSITALMENTE indiferente a `deletedAt`),
      com doc-comment explicando o mesmo raciocínio: editar/salvar um lead perdido cujo motivo foi
      removido não pode falhar.
    - Em `createLead` e `updateLead`: quando `parsed.data.stage === "perdido"`, chamar
      `motivoPerdaExists` e, se falso, retornar `{ errors: { motivoPerdaId: ["Selecione o motivo da perda."] } }`
      antes de qualquer escrita.
    - Em `updateLead` (linha 132) e `updateLeadStage` (linha 197): trocar a atribuição condicional para
      `motivoPerdaId: parsed.data.stage === "perdido" ? parsed.data.motivoPerdaId : null`, mantendo o
      idioma condicional-por-VALOR-ALVO e a posição DEPOIS do spread de `parsed.data`. Preservar
      integralmente os comentários WR-02 existentes, atualizando o nome do campo neles.
    - Trocar a assinatura de `updateLeadStage` de `motivoPerda?: string` para `motivoPerdaId?: number`.
    - Estender os `catch` de `isForeignKeyViolation` em `createLead`/`updateLead` para também mapear
      violação de FK de `motivo_perda_id`, devolvendo `{ errors: { motivoPerdaId: ["Selecione o motivo da perda."] } }`
      quando o stage é "perdido" (backstop da janela de corrida check-then-write, mesmo raciocínio já
      aplicado a `subnichoId`).
    - Verificar que não sobrou NENHUMA referência a `parsed.data.motivoPerda` ou `leads.motivoPerda`
      no arquivo.

    `src/components/lead-form-dialog.tsx`:
    - Adicionar a prop nova `motivosPerda: MotivoPerda[]`, espelhando como `subnichos` já é recebida.
    - `defaultValues`: trocar `motivoPerda: lead?.motivoPerda ?? ""` por
      `motivoPerdaId: lead?.motivoPerdaId ?? undefined`.
    - No bloco condicional `form.watch("stage") === "perdido"`: substituir o `<Textarea id="motivoPerda">`
      por `<MotivoPerdaCombobox>` dentro de um `<Controller control={form.control} name="motivoPerdaId" ... />`,
      copiando estrutura por estrutura o `Controller` do `<SubnichoCombobox>` (linhas 322-333),
      incluindo o `value` com fallback `?? null` e o `invalid` derivado de `errors.motivoPerdaId`.
    - `<FieldLabel>` continua `Motivo da perda`. Trocar o `<FieldDescription>` de
      "Opcional — por que esse lead foi perdido (ex: ...)" para VERBATIM `Por que esse lead foi perdido.`
      (11-UI-SPEC.md linha 119). `<FieldError>` passa a ler `errors.motivoPerdaId`.
    - Remover o import de `Textarea` se ele ficar sem uso no arquivo.

    NÃO rodar `npx tsc --noEmit` como gate desta task: `pipeline-board.tsx` e `motivo-perda-dialog.tsx`
    ainda passam `string | undefined` para `updateLeadStage`, e a página do pipeline ainda não passa a
    prop `motivosPerda`. A coerência de tipos é restaurada na Task 3 — janela intencional e documentada.
  </action>
  <verify>
    <automated>cd "C:/Users/Vencedor/Desktop/crm-leads" && npx eslint src/lib/validations.ts src/actions/lead-actions.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/validations.ts` contém `leadBaseSchema`, `motivoPerdaId`, `path: ["motivoPerdaId"]` e a mensagem literal `Selecione o motivo da perda.` em DOIS lugares (leadSchema e stageUpdateSchema)
    - `src/lib/validations.ts` NÃO contém mais `motivoPerda: z.string()` e `csvRowSchema` deriva de `leadBaseSchema` com `motivoPerdaId: true` no `.omit`
    - `src/db/schema.ts` NÃO contém mais `text("motivo_perda")`, mas contém `motivo_perda_id`
    - `src/actions/lead-actions.ts` contém `motivoPerdaExists`, e a busca por `parsed.data.motivoPerda` (sem `Id`) no arquivo retorna zero ocorrências
    - `src/actions/lead-actions.ts` contém a assinatura `updateLeadStage(` com um parâmetro `motivoPerdaId?: number`
    - `src/components/lead-form-dialog.tsx` contém `MotivoPerdaCombobox`, `name="motivoPerdaId"` e o literal `Por que esse lead foi perdido.`, e NÃO contém mais `id="motivoPerda"` nem `register("motivoPerda")`
    - Comportamento (provado na Task 3 pelo gate `verify:motivo-perda`): `stageUpdateSchema.safeParse({ id: 1, stage: "perdido" })` sem `motivoPerdaId` retorna `success: false` com a issue no path `motivoPerdaId`
    - `npx eslint` escopado a `validations.ts` e `lead-actions.ts` sai com código 0
  </acceptance_criteria>
  <done>Contratos e servidor autoritativos para D-04; formulário de lead migrado para o combobox.</done>
</task>

<task type="auto">
  <name>Task 3: Modal de drag obrigatório, reversão do drag no Cancelar, e gate permanente de D-04</name>
  <files>src/components/motivo-perda-dialog.tsx, src/components/pipeline-board.tsx, src/app/pipeline/page.tsx, scripts/verify-motivo-perda-obrigatorio.cjs, package.json</files>
  <read_first>
    - src/components/motivo-perda-dialog.tsx (arquivo inteiro, 79 linhas — a ser reescrito)
    - src/components/pipeline-board.tsx (linhas 51-171 e 219-245 — doc-comment, motivoQueueRef, handleDragEnd, resolveMotivoPerda, uso do MotivoPerdaDialog e do LeadFormDialog)
    - src/app/pipeline/page.tsx (arquivo inteiro, 87 linhas — Promise.all e props do board)
    - src/components/delete-subnicho-dialog.tsx (padrão showCloseButton={false} + onOpenChange interceptado + footer de 2 botões)
    - src/components/motivo-perda-combobox.tsx (criado na Task 1)
    - scripts/verify-sequencia-posicao.cjs (Parte B — checagens estáticas no fonte real com fatiamento por indexOf e regex tolerante a espaçamento)
    - .planning/phases/11-painel-de-m-tricas-e-relat-rio-de-motivos-de-perda/11-UI-SPEC.md (§motivo-perda-dialog.tsx, linhas 192-200; §Copywriting linhas 121-123)
  </read_first>
  <action>
    `src/components/motivo-perda-dialog.tsx` — reescrever:
    - REMOVER: o `<Textarea>` e seu import, o botão "Pular", a prop `onSkip`, a palavra "(opcional)"
      da descrição, e o estado `useState("")` de string.
    - Trocar a assinatura de `onSave: (motivo: string) => void` para `onSave: (motivoPerdaId: number) => void`;
      estado local vira `useState<number | null>(null)`.
    - Adicionar a prop `motivosPerda: MotivoPerda[]` e renderizar `<MotivoPerdaCombobox>` no lugar do textarea.
    - Adicionar a prop `onCancel: () => void` e um botão `<Button variant="outline">Cancelar</Button>` que a chama.
    - Botão `Salvar motivo` (mantendo as classes teal atuais) com `disabled` enquanto o estado local for `null`.
    - `<DialogContent showCloseButton={false}>` e `onOpenChange` interceptado (copiar de
      `delete-subnicho-dialog.tsx`) — sem uma decisão explícita (Cancelar ou Salvar) o modal não fecha,
      evitando drag órfão (etapa mudou visualmente mas nunca persistiu nem reverteu).
    - Título continua `Mover para Perdido`; descrição VERBATIM no formato `Por que "{leadNome}" foi perdido?`
      (sem "(opcional)").

    `src/components/pipeline-board.tsx`:
    - Adicionar a prop `motivosPerda: MotivoPerda[]` ao `PipelineBoardProps` e repassá-la tanto ao
      `<MotivoPerdaDialog>` quanto ao `<LeadFormDialog>` (que ganhou essa prop na Task 2).
    - Tipar o resolver da fila `motivoQueueRef` como `(motivoPerdaId: number | null) => void` e a
      variável de `handleDragEnd` como `number | null`, onde `null` significa CANCELADO.
    - Novo comportamento de cancelamento: criar `cancelMotivoPerda()` que faz o mesmo `shift()`/avanço
      de fila de `resolveMotivoPerda` mas resolve a promessa com `null`. Em `handleDragEnd`, quando o
      valor resolvido for `null`, RETORNAR ANTES de chamar `updateLeadStage` — sem chamada ao servidor,
      o estado base nunca muda e o `useOptimistic` reverte o card sozinho ao assentar a transição
      (mecanismo já descrito no doc-comment das linhas 51-61). Nenhum toast de sucesso nesse caminho.
    - `onOpenChange` do `<MotivoPerdaDialog>` (que hoje resolve com `undefined`) e o novo `onCancel`
      passam ambos por `cancelMotivoPerda`; `onSave` passa por `resolveMotivoPerda(motivoPerdaId)`.
    - Atualizar o doc-comment do componente (linhas 51-61) trocando "modal opcional e não-bloqueante"
      pela descrição real: modal obrigatório com Cancelar que reverte o drag (D-04).
    - `updateLeadStage(leadId, newStage, motivoPerdaId)` passa a receber `number`.

    `src/app/pipeline/page.tsx`: adicionar `db.select().from(motivosPerda)` ao `Promise.all` existente
    (espelhando `allSubnichos`) e passar `motivosPerda={allMotivosPerda}` ao `<PipelineBoard>`.

    Criar `scripts/verify-motivo-perda-obrigatorio.cjs` — gate permanente de D-04, molde da Parte B de
    `scripts/verify-sequencia-posicao.cjs` (`fs.readFileSync` do fonte real, fatiamento por
    `indexOf("export async function ...")`, regex tolerante a espaçamento com `\s*`), harness
    `check(condition, message)` + `process.exit(failed > 0 ? 1 : 0)`. Duas partes:

    PARTE A — comportamental, sem banco (Zod puro): setar `DB_FILE_NAME=":memory:"` antes do
    `register("./ts-alias-loader.mjs", ...)`, importar `@/lib/validations` e provar que
    (a) `stageUpdateSchema.safeParse({ id: 1, stage: "perdido" })` reprova com issue no path
    `motivoPerdaId`; (b) `stageUpdateSchema.safeParse({ id: 1, stage: "perdido", motivoPerdaId: 3 })`
    aprova; (c) `stageUpdateSchema.safeParse({ id: 1, stage: "contatado" })` aprova sem motivo;
    (d) a mensagem de erro é exatamente `Selecione o motivo da perda.`; (e) o mesmo trio de asserções
    aplicado a `leadSchema` com um payload de lead válido.

    PARTE B — estática no fonte real: (f) `src/actions/lead-actions.ts` contém, dentro dos corpos de
    `updateLead` e `updateLeadStage`, o idioma condicional-por-valor-alvo atribuindo `motivoPerdaId`
    com `null` no ramo não-perdido; (g) `src/actions/lead-actions.ts` contém `motivoPerdaExists`;
    (h) GREP NEGATIVO em `src/components/motivo-perda-dialog.tsx`: o arquivo NÃO pode conter
    `Textarea`, `onSkip` nem o literal `Pular` (sinal de alerta do Pitfall 1 do 11-RESEARCH.md);
    (i) GREP NEGATIVO em `src/`: nenhuma ocorrência de `.motivoPerda` que não seja seguida de `Id`,
    ignorando linhas de comentário.

    Registrar em `package.json`: `"verify:motivo-perda": "node scripts/verify-motivo-perda-obrigatorio.cjs"`.
  </action>
  <verify>
    <automated>cd "C:/Users/Vencedor/Desktop/crm-leads" && npx tsc --noEmit && npx eslint src/components/motivo-perda-dialog.tsx src/components/pipeline-board.tsx src/app/pipeline/page.tsx src/components/lead-form-dialog.tsx && npm run verify:motivo-perda && npm run test:lead-actions && npm run guard:no-hard-delete</automated>
    <human-check>
      1. Rodar `npm run dev` e abrir http://localhost:3000/pipeline
      2. Arrastar um card para a coluna "Perdido" → o modal abre, tem combobox (não caixa de texto livre), NÃO tem botão "Pular" nem X de fechar, e "Salvar motivo" está desabilitado
      3. Clicar fora do modal / apertar Esc → o modal NÃO fecha
      4. Clicar "Cancelar" → o modal fecha e o card VOLTA para a coluna de origem; nenhum toast de sucesso aparece
      5. Arrastar de novo, escolher "Preço" no combobox → "Salvar motivo" habilita; confirmar → toast "Lead movido para Perdido." e o card fica em Perdido após refresh
      6. Arrastar outro card para Perdido e, no combobox, digitar um nome inexistente (ex: "Sem fit") → aparece a linha `Criar "Sem fit"` com ícone de mais em teal; selecioná-la cria e já seleciona o motivo; salvar
      7. Conferir em /motivos-perda que "Sem fit" aparece na lista
      8. Abrir o lead perdido pelo formulário de edição (/leads) → o campo "Motivo da perda" mostra o combobox com o motivo já selecionado e a ajuda "Por que esse lead foi perdido."
      9. No formulário, trocar a etapa de "Perdido" para "Negociação" e salvar → salva sem pedir motivo; reabrir e voltar para "Perdido" → salvar sem escolher motivo é BLOQUEADO com "Selecione o motivo da perda."
      10. Arrastar DOIS cards seguidos para Perdido antes de decidir o primeiro → o modal atende um lead por vez, sem card órfão
    </human-check>
  </verify>
  <acceptance_criteria>
    - `src/components/motivo-perda-dialog.tsx` NÃO contém `Textarea`, `onSkip` nem o literal `Pular`; contém `showCloseButton={false}`, `MotivoPerdaCombobox`, `onCancel` e `disabled`
    - `src/components/pipeline-board.tsx` contém `cancelMotivoPerda`, `motivosPerda` e um retorno antecipado que impede `updateLeadStage` quando o motivo resolvido é `null`
    - `src/app/pipeline/page.tsx` contém `motivosPerda` dentro do `Promise.all` e passa a prop ao `<PipelineBoard>`
    - `npm run verify:motivo-perda` sai com código 0 e cobre as 9 checagens (a)-(i) descritas na ação
    - Teste de mutação do gate (em cópia temporária dos fontes em `os.tmpdir()`, NUNCA nos arquivos reais): reintroduzir a string `onSkip` na cópia de `motivo-perda-dialog.tsx` faz o script sair com código 1
    - Comportamento: `stageUpdateSchema.safeParse({ id: 1, stage: "perdido" })` sem `motivoPerdaId` retorna `success: false` com issue em `path: ["motivoPerdaId"]` e mensagem `Selecione o motivo da perda.`
    - `npx tsc --noEmit` sai com código 0 (a janela intencional aberta na Task 2 está fechada)
    - `npm run test:lead-actions` sai com código 0 (regressão do CRUD de lead após a troca de coluna)
    - `npm run guard:no-hard-delete` sai com código 0
  </acceptance_criteria>
  <done>As duas superfícies de captura exigem motivo da lista governada; cancelar reverte o drag; gate de D-04 permanente no repositório.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → `updateLeadStage` / `updateLead` | `motivoPerdaId` chega como argumento posicional / FormData de um client component; totalmente controlável pelo cliente |
| combobox → `createMotivoPerda` | nome livre digitado pelo admin cria linha nova na tabela governada |
| `leads.motivoPerdaId` → `motivos_perda.id` | integridade referencial no nível do banco |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-12 | Tampering | bypass de D-04 chamando `updateLeadStage(id, "perdido")` sem motivo direto da action | mitigate | ASVS V5 — `.refine` condicional em `stageUpdateSchema`/`leadSchema` roda no `safeParse` ANTES de qualquer escrita; o `disabled` do botão e o `zodResolver` são só UX antecipada, nunca a autoridade |
| T-11-13 | Tampering | `motivoPerdaId` forjado apontando para id inexistente (mass-assignment de FK) | mitigate | `motivoPerdaExists(id)` consultado antes da escrita quando `stage === "perdido"`, mais o backstop `isForeignKeyViolation` no catch (fecha a janela de corrida check-then-write) e a FK `onDelete: "restrict"` no banco |
| T-11-14 | Tampering | injeção SQL via nome digitado no combobox criável | mitigate | `createMotivoPerda` valida com `motivoPerdaSchema` e usa apenas queries parametrizadas do Drizzle/better-sqlite3 (mitigação herdada do plano 11-02, T-11-06) |
| T-11-15 | Tampering | XSS via nome de motivo renderizado no combobox / modal / formulário | mitigate | React escapa por padrão; proibido `dangerouslySetInnerHTML` nos componentes desta fase |
| T-11-16 | Denial of Service | drag órfão (etapa muda visualmente e nunca persiste nem reverte) travando o board | mitigate | Modal não-dispensável (`showCloseButton={false}` + `onOpenChange` interceptado) e fila `motivoQueueRef` que sempre resolve exatamente uma promessa por decisão; o caminho de cancelamento resolve com `null` e deixa o `useOptimistic` reverter |
| T-11-17 | Information Disclosure | motivo de perda de um lead exposto na URL/querystring | accept | Nenhum id de motivo trafega por URL nesta fase; captura é sempre por Server Action com corpo POST |
| T-11-18 | Elevation of Privilege | ausência de controle de acesso nas Server Actions de lead | accept | Ferramenta solo-admin local sem auth (PROJECT.md/CLAUDE.md); ASVS V2/V3/V4 não se aplicam |
</threat_model>

<verification>
Sequencial, dev server parado durante os gates estáticos (host 4GB RAM):

1. `npx tsc --noEmit`
2. `npx eslint` escopado aos arquivos tocados
3. `npm run verify:motivo-perda`
4. `npm run test:lead-actions`
5. `npm run test:motivo-perda-actions`
6. `npm run verify:sequencia` (regressão da Fase 10 — `updateLeadStage` foi tocado)
7. `npm run guard:no-hard-delete`
8. `npm run verify:motivos-perda-schema`
9. `<human-check>` de 10 passos da Task 3 (rodado no gate de fim de fase)
</verification>

<success_criteria>
- Mover para "Perdido" sem motivo é rejeitado pelo servidor, nas duas superfícies
- Criação-na-hora funciona no combobox e já seleciona o motivo criado (D-03)
- Cancelar o modal reverte o drag sem chamar o servidor
- Zero referências a `.motivoPerda` (sem `Id`) em `src/`
- `npx tsc --noEmit` limpo e os 8 gates automatizados de `<verification>` com exit 0
</success_criteria>

<output>
Criar `.planning/phases/11-painel-de-m-tricas-e-relat-rio-de-motivos-de-perda/11-03-SUMMARY.md` ao concluir.
</output>
