---
phase: 12-agenda-tarefas-soltas
plan: 04
type: execute
wave: 4
depends_on: ["12-03"]
files_modified:
  - src/components/followup-dashboard.tsx
  - src/app/page.tsx
autonomous: true
requirements: [TAREFA-01, TAREFA-02]

must_haves:
  truths:
    - "O botão \"Nova tarefa\" aparece ao lado de \"Novo lead\" no topo do dashboard e abre o TarefaFormDialog em modo criação (D-05)"
    - "Tarefas pendentes aparecem nas 3 seções de urgência do dashboard, na mesma régua dos follow-ups de lead (TAREFA-02)"
    - "Dentro de cada seção, tarefas e follow-ups aparecem intercalados e ordenados por data ascendente, nunca em blocos separados (D-04)"
    - "A contagem \"· N\" do cabeçalho de cada seção soma tarefas + follow-ups"
    - "Tarefa concluída some do dashboard imediatamente (D-02)"
    - "Seção sem nenhum item continua omitida por completo"
    - "O estado vazio só aparece quando não há nem follow-up nem tarefa, e oferece \"Nova tarefa\" entre os CTAs"
  artifacts:
    - path: "src/components/followup-dashboard.tsx"
      provides: "Renderização unificada lead|tarefa por seção de urgência + botão Nova tarefa + TarefaFormDialog"
      contains: "TarefaCard"
    - path: "src/app/page.tsx"
      provides: "Leitura de tarefas pendentes e construção das 3 listas unificadas"
      contains: "getTarefasPendentes"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/db/queries.ts"
      via: "getTarefasPendentes + buildDashboardItems no Promise.all"
      pattern: "buildDashboardItems"
    - from: "src/components/followup-dashboard.tsx"
      to: "src/components/tarefa-card.tsx"
      via: "ramo item.kind === \"tarefa\" do map da seção"
      pattern: "<TarefaCard"
    - from: "src/components/followup-dashboard.tsx"
      to: "src/components/tarefa-form-dialog.tsx"
      via: "estado local tarefaDialogState (criação e edição)"
      pattern: "TarefaFormDialog"
---

<objective>
Integrar a agenda no dashboard `/`: a página passa a ler as tarefas pendentes e montar as 3 listas unificadas (`DashboardItem[]`), e o `FollowupDashboard` passa a renderizar cards de lead e de tarefa INTERCALADOS por data dentro de cada seção de urgência, com o botão "Nova tarefa" no cabeçalho e no estado vazio.

Propósito: fecha TAREFA-02 e os 3 Success Criteria da fase — sem esta integração as tarefas existem no banco mas o admin nunca as vê, e o critério #3 (distinção visual DENTRO do mesmo agrupamento, sem tela separada) não é observável.

Saída: `src/app/page.tsx` e `src/components/followup-dashboard.tsx` atualizados + a suíte completa de gates da fase verde.

Restrição de host: 4GB RAM. Rodar cada gate em SEQUÊNCIA, com o `npm run dev` PARADO antes de `npm run build`. `npm run build` voltou a ser gate normal desde 2026-08-29 (Next 16.2 usa Turbopack).
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-agenda-tarefas-soltas/12-CONTEXT.md
@.planning/phases/12-agenda-tarefas-soltas/12-UI-SPEC.md
@.planning/phases/12-agenda-tarefas-soltas/12-PATTERNS.md
@.planning/phases/12-agenda-tarefas-soltas/12-03-SUMMARY.md

@src/components/followup-dashboard.tsx
@src/app/page.tsx
@src/db/queries.ts
</context>

<interfaces>
<!-- Contratos já criados pelos planos 12-02 e 12-03. O executor NÃO precisa explorar o codebase. -->

De `@/db/queries` (plano 12-02):
- `getTarefasPendentes(): Promise<Tarefa[]>` — já filtra `concluidaEm IS NULL` e ordena por `data` asc
- `buildDashboardItems(activeLeads: Lead[], tarefasPendentes: Tarefa[], now?: Date): DashboardItemsByUrgency` — pura, devolve `{ vencidos, hoje, proximos7Dias }` de `DashboardItem[]`, cada bucket já ordenado por `date` ascendente
- `type DashboardItem = { kind: "lead"; date: Date; lead: Lead } | { kind: "tarefa"; date: Date; tarefa: Tarefa }`

De `@/components/tarefa-card` (plano 12-03):
- `TarefaCard({ tarefa, dateClassName, onEdit })`

De `@/components/tarefa-form-dialog` (plano 12-03):
- `TarefaFormDialog({ open, onOpenChange, tarefa? })` — `tarefa` ausente = modo criação

Estado atual de `followup-dashboard.tsx` que MUDA:
- `FollowupDashboardProps.vencidos/hoje/proximos7Dias: Lead[]` → `DashboardItem[]`
- `type UrgencySection` campo `leads: Lead[]` → `items: DashboardItem[]`
- `totalCount` (L78), `section.leads.length` (L141, L161) → `section.items.length`
- o `.map` da L165 → `switch`/ternário por `item.kind`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Unificar lead+tarefa no dashboard (`followup-dashboard.tsx` + `page.tsx`)</name>
  <files>src/components/followup-dashboard.tsx, src/app/page.tsx</files>
  <read_first>
    - `src/components/followup-dashboard.tsx` (arquivo a modificar, arquivo inteiro L1-255) — em especial: props L15-23, `DialogState` L25-28, `UrgencySection` L34-41, `totalCount` L78, `sections[]` L80-105, cabeçalho L111-118, estado vazio L120-137, filtro de seção vazia L141, contagem `· N` L161, `.map` do card de lead L165-223, render do `LeadFormDialog` L230-241
    - `src/app/page.tsx` (arquivo a modificar, arquivo inteiro L1-68) — imports L1-9, `Promise.all` L25-39, chamada de `groupLeadsByUrgency` L41, props do `FollowupDashboard` L57-65
    - `src/db/queries.ts` — `getTarefasPendentes`, `buildDashboardItems`, `DashboardItem` (criados no plano 12-02)
    - `src/components/tarefa-card.tsx` e `src/components/tarefa-form-dialog.tsx` (criados no plano 12-03 — props exatas)
    - `.planning/phases/12-agenda-tarefas-soltas/12-UI-SPEC.md` §Layout §Cabeçalho do dashboard, §Estado vazio, §Intercalação por data; §Copywriting Contract (linhas do estado vazio e do botão)
    - `.planning/phases/12-agenda-tarefas-soltas/12-PATTERNS.md` §`src/components/followup-dashboard.tsx`, §`src/app/page.tsx`
  </read_first>
  <action>
    Em `src/app/page.tsx`: importar `getTarefasPendentes` e `buildDashboardItems` de `@/db/queries` (substituindo a importação de `groupLeadsByUrgency` NESTE call-site — a função continua existindo para outros consumidores). Adicionar `getTarefasPendentes()` ao `Promise.all` existente, como mais um elemento do array desestruturado. Trocar a linha `const { vencidos, hoje, proximos7Dias } = groupLeadsByUrgency(activeLeads)` por `buildDashboardItems(activeLeads, tarefasPendentes)`. Passar as 3 listas de `DashboardItem` para o `FollowupDashboard` com os mesmos nomes de prop de hoje (`vencidos`, `hoje`, `proximos7Dias`). NÃO escrever SQL inline na página — a leitura mora em `queries.ts` (mesmo motivo já documentado em `getActiveDashboardLeads`). O cálculo de `sugestaoPorLead` continua operando sobre `activeLeads` e sai inalterado.

    Em `src/components/followup-dashboard.tsx`, aplicar as 7 mudanças abaixo:

    1. Tipos: trocar `vencidos`/`hoje`/`proximos7Dias` de `Lead[]` para `DashboardItem[]` nas props, e o campo `leads` de `UrgencySection` para `items: DashboardItem[]`. Importar `DashboardItem` de `@/db/queries` e `Tarefa` de `@/types`.

    2. Estado local novo: `tarefaDialogState`, união discriminada no mesmo idioma do `DialogState` existente — fechado, criação, ou edição com a tarefa. NÃO reaproveitar o `DialogState` de lead; são dois dialogs independentes.

    3. `sections[]`: renomear o campo `leads` para `items` nas 3 entradas; `totalCount` e o filtro de seção vazia (L141) e a contagem `· N` (L161) passam a usar `section.items.length`. Isso faz a contagem do cabeçalho somar tarefas + follow-ups automaticamente, conforme o 12-UI-SPEC.md §Copywriting.

    4. `.map` da seção: iterar `section.items` e ramificar por `item.kind`. No ramo `"lead"`, mover o bloco existente do card de lead (L168-221) SEM NENHUMA alteração de conteúdo ou classe, apenas trocando as referências de `lead` para `item.lead` e ajustando a `key` para algo estável e único entre os dois tipos (ex.: prefixar por `kind`, já que ids de lead e de tarefa podem colidir). No ramo `"tarefa"`, renderizar `<TarefaCard tarefa={item.tarefa} dateClassName={section.dateClassName} onEdit={(t) => setTarefaDialogState(...modo edição...)} />`. A ORDEM já vem pronta do servidor (`buildDashboardItems` ordena cada bucket por data ascendente) — NÃO reordenar, NÃO separar em blocos por tipo, NÃO agrupar tarefas no fim da seção (D-04).

    5. Cabeçalho (L111-118): transformar o `<div className="flex items-center justify-between">` num cluster `<div className="flex items-center gap-2">` contendo, NESTA ordem, o botão "Novo lead" existente (`bg-[#0D9488] text-white hover:bg-[#0D9488]/90`, intocado) e um botão novo "Nova tarefa" (`<Button variant="outline">`) que abre o `TarefaFormDialog` em modo criação. "Nova tarefa" é secundário — NÃO usar o accent teal nele (D-05 / 12-UI-SPEC.md §Color).

    6. Estado vazio (L120-137): trocar o body de "Nenhum follow-up pendente." para "Nenhum follow-up ou tarefa pendente." (verbatim), e acrescentar ao cluster de CTAs um terceiro botão, ficando a ordem: link "Ver todos os leads" (`buttonVariants({ variant: "outline" })`), botão "Nova tarefa" (`variant="outline"`) e botão "Novo lead" (teal, por último). O estado vazio só aparece quando `totalCount === 0`, que agora já conta tarefas.

    7. Render do `<TarefaFormDialog>` como irmão do `<LeadFormDialog>` (não aninhado), no mesmo padrão de `key`/`open`/`onOpenChange` usado na L230-241 — `key` distinta entre criação e edição para forçar o remount e limpar o formulário entre aberturas.

    Não tocar em nenhuma outra parte do dashboard: cores de seção, `headerBg`/`headerText`/`dateClassName`, cópia dos cabeçalhos, o `WhatsAppPreviewDialog` e o comportamento do card de lead permanecem exatamente como estão.

    Se o ESLint apontar `react-hooks/set-state-in-effect` ou `react-hooks/refs`, suprimir com `eslint-disable-next-line` DOCUMENTADO — mesmo falso-positivo do React Compiler já aceito no projeto (`STATE.md`, Fases 07-02 e 09-03/09-04). Nunca alterar comportamento para agradar o linter.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npx eslint src/components/followup-dashboard.tsx src/app/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sai exit 0.
    - `npx eslint src/components/followup-dashboard.tsx src/app/page.tsx` sai exit 0.
    - Contratos de integração presentes: `node -e "const d=require('fs').readFileSync('src/components/followup-dashboard.tsx','utf8'); const p=require('fs').readFileSync('src/app/page.tsx','utf8'); for(const t of ['TarefaCard','TarefaFormDialog','DashboardItem','section.items','Nova tarefa','Nenhum follow-up ou tarefa pendente.']) if(!d.includes(t)) { console.error('dashboard faltando',t); process.exit(1); } for(const t of ['getTarefasPendentes','buildDashboardItems']) if(!p.includes(t)) { console.error('page faltando',t); process.exit(1); } if(d.includes('section.leads')) { console.error('section.leads deve virar section.items'); process.exit(1); } if(p.includes('groupLeadsByUrgency')) { console.error('page.tsx deve usar buildDashboardItems'); process.exit(1); } console.log('OK')"` imprime `OK`.
    - O card de lead não regrediu: `node -e "const d=require('fs').readFileSync('src/components/followup-dashboard.tsx','utf8'); for(const t of ['EtapaBadge','WhatsAppSendButton','Sugestão:','stopPropagation']) if(!d.includes(t)) { console.error('regressao no card de lead:',t,'sumiu'); process.exit(1); } console.log('OK')"` imprime `OK`.
    - `page.tsx` não contém SQL inline: o arquivo NÃO importa nada de `drizzle-orm` para ler tarefas (a leitura mora em `queries.ts`).
    - `npm run build` sai exit 0 (com o dev server parado) e a rota `/` continua sendo gerada.
  </acceptance_criteria>
  <done>O dashboard `/` renderiza follow-ups de lead e tarefas pendentes intercalados por data dentro das 3 seções de urgência, com contagem somada, botão "Nova tarefa" no cabeçalho e no estado vazio, e o `TarefaFormDialog` montado. Nenhum comportamento do card de lead alterado.</done>
</task>

<task type="auto">
  <name>Task 2: Suíte completa de gates da fase + verificação humana ponta a ponta</name>
  <files>(nenhum arquivo de produção — task de verificação; correções pontuais são permitidas se algum gate falhar)</files>
  <read_first>
    - `.planning/ROADMAP.md` §"Phase 12: Agenda / Tarefas Soltas" — os 3 Success Criteria que precisam ser verdade
    - `.planning/phases/12-agenda-tarefas-soltas/12-01-SUMMARY.md`, `12-02-SUMMARY.md`, `12-03-SUMMARY.md` — o que cada onda entregou e quais desvios foram registrados
    - `package.json` — as entradas de script disponíveis (`verify:*`, `test:*`, `guard:*`, `migrate:*`)
    - `.planning/STATE.md` §Blockers/Concerns — a nota do host de 4GB e a resolução de 2026-08-29 sobre `npm run build`
    - `.planning/phases/12-agenda-tarefas-soltas/12-UI-SPEC.md` §Layout (referência visual para a verificação humana)
  </read_first>
  <action>
    Rodar a suíte completa de gates da fase, UM DE CADA VEZ (host de 4GB RAM — nunca processos pesados em paralelo; garantir que nenhum processo `node`/`next dev` esteja rodando antes de `npm run build`):

    1. `npx tsc --noEmit`
    2. `npx eslint` escopado aos 6 arquivos tocados pela fase (`src/db/schema.ts`, `src/db/queries.ts`, `src/lib/validations.ts`, `src/types/index.ts`, `src/actions/tarefa-actions.ts`, `src/components/followup-dashboard.tsx`, `src/app/page.tsx`, `src/components/tarefa-card.tsx`, `src/components/tarefa-form-dialog.tsx`, `src/components/delete-tarefa-dialog.tsx`) — NÃO rodar `npx eslint` sem escopo: o repositório tem 413 problemas pré-existentes não relacionados, documentados em `deferred-items.md`
    3. `npm run verify:schema`
    4. `npm run guard:no-hard-delete`
    5. `npm run test:tarefa-actions`
    6. `npm run test:group-by-urgency`
    7. `npm run test:compute-sequencia` (regressão da generalização de `groupByUrgency`)
    8. `npm run test:lead-actions` e `npm run test:relatorios` (regressão geral — nenhuma tabela existente foi tocada, devem continuar verdes)
    9. `npm run build` (dev server parado)

    Se algum gate falhar, corrigir a causa raiz no arquivo correspondente e reexecutar — não relaxar o gate.

    Depois dos gates verdes, subir `npm run dev` e conduzir a verificação humana ponta a ponta descrita no bloco `<human-check>` abaixo, registrando o resultado de cada item.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run verify:schema && npm run guard:no-hard-delete && npm run test:tarefa-actions && npm run test:group-by-urgency && npm run test:compute-sequencia && npm run build</automated>
    <human-check>
      Com `npm run dev` rodando (porta 3000 ou 3001), no navegador:
      1. Em `/`, confirmar que existe um botão "Nova tarefa" com borda (secundário) imediatamente à direita do botão teal "Novo lead".
      2. Clicar em "Nova tarefa": o dialog abre com o título "Nova tarefa" e exatamente 2 campos — Descrição e Data. Nenhum campo de lead (telefone, canal, sub-nicho, valor, etapa) está presente.
      3. Salvar sem preencher a descrição: aparece a mensagem "Descreva a tarefa." e o dialog NÃO fecha.
      4. Criar uma tarefa com descrição "Ligar pro cowork sobre o CSV de agosto" e data de HOJE: aparece o toast "Tarefa criada." e o card aparece na seção "Hoje".
      5. Criar uma segunda tarefa com data de ONTEM: ela aparece na seção "Vencidos", com a data em vermelho — a MESMA cor da data dos follow-ups de lead vencidos ao lado.
      6. Conferir a distinção visual (Success Criterion #3): o card de tarefa tem um ícone de lista à esquerda e mostra APENAS descrição e data — sem sub-nicho, sem badge de etapa, sem botão de WhatsApp — enquanto os cards de lead da mesma seção continuam com todos os seus adereços.
      7. Conferir a intercalação (D-04): dentro de uma seção que tenha lead E tarefa, os itens estão ordenados por data, misturados — não há bloco separado de tarefas.
      8. Conferir a contagem "· N" no cabeçalho da seção: ela soma tarefas + follow-ups.
      9. Passar o mouse sobre o botão-ícone à direita do card de tarefa: o círculo cinza vira um círculo com check em teal. Navegar até ele por Tab e confirmar que o foco produz o mesmo efeito visual.
      10. Clicar nesse botão: o card SOME do dashboard na hora, aparece o toast "Tarefa concluída." com um botão "Desfazer", e o clique no card NÃO abriu o dialog de edição.
      11. Clicar em "Desfazer": aparece "Tarefa reaberta." e o card volta à seção de urgência correta.
      12. Clicar no CORPO de um card de tarefa: abre o dialog "Editar tarefa" com descrição e data preenchidas e o rodapé com Excluir / Cancelar / Concluir / Salvar.
      13. Alterar a data para daqui a 3 dias e Salvar: toast "Tarefa salva." e o card migra para a seção "Próximos 7 dias".
      14. Reabrir a tarefa e clicar em "Excluir": aparece o dialog "Excluir tarefa" com o aviso "Essa ação não pode ser desfeita.", e o X de fechar NÃO existe. Confirmar: toast "Tarefa excluída." e o card some.
      15. Confirmar que a tarefa excluída sumiu MESMO do banco: `node -e "const D=require('better-sqlite3');const db=new D('data/crm.db');console.log(db.prepare('SELECT id, descricao, concluida_em FROM tarefas').all());"` não lista a linha excluída (prova do hard-delete, D-08).
      16. Concluir/excluir todas as tarefas e leads pendentes (ou usar um banco com o dashboard vazio) e confirmar o estado vazio: heading "Tudo em dia!", body "Nenhum follow-up ou tarefa pendente." e 3 CTAs na ordem "Ver todos os leads" · "Nova tarefa" · "Novo lead".
      17. Confirmar que nenhuma rota nova apareceu no menu lateral — a agenda vive dentro de `/` (Success Criterion #3 exige "sem tela/rota separada").
    </human-check>
  </verify>
  <acceptance_criteria>
    - Os 9 gates automatizados listados no `<action>` saem todos com exit 0, registrados no SUMMARY com a saída resumida de cada um.
    - `npm run build` sai exit 0 com o dev server parado, e o log lista a rota `/`.
    - Os 3 Success Criteria da Fase 12 (ROADMAP.md) têm evidência explícita no SUMMARY: (1) criação de tarefa avulsa sem vínculo a lead, (2) tarefas nas 3 seções de urgência pela mesma régua, (3) distinção visual dentro do mesmo agrupamento, sem tela/rota separada.
    - As decisões D-01 a D-08 têm rastro no SUMMARY apontando o arquivo/comportamento que as implementa.
    - Cada item de 1 a 17 da verificação humana registrado como `pass` ou `issue` com descrição; itens com `issue` viram gaps para `/gsd-plan-phase 12 --gaps`.
  </acceptance_criteria>
  <done>Suíte completa verde (tsc, eslint escopado, verify:schema, guard:no-hard-delete, 5 harnesses de teste, build) e verificação humana ponta a ponta conduzida com resultado registrado item a item.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Component `/` → cliente | `page.tsx` serializa leads e tarefas para o `FollowupDashboard` (client component) |
| descrição de tarefa → DOM do dashboard | Texto arbitrário do usuário renderizado dentro de uma seção compartilhada com dados de lead |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-19 | Information Disclosure | payload serializado para o cliente | mitigate | `getTarefasPendentes` seleciona só a tabela `tarefas`, que não contém PII de terceiros (nem telefone, nem e-mail) — é texto do próprio admin. A tabela é desacoplada de `leads`, então nenhum dado de lead vaza por essa rota nova |
| T-12-20 | Cross-Site Scripting | descrição renderizada no dashboard | mitigate | Renderização exclusivamente via filho JSX / `title` / `aria-label` no `TarefaCard` (plano 12-03); `dangerouslySetInnerHTML` proibido em toda a fase |
| T-12-21 | Tampering | perda de comportamento do card de lead na refatoração para `DashboardItem` | mitigate | O bloco do card de lead é MOVIDO sem alteração de conteúdo; gate automatizado na acceptance criteria verifica que `EtapaBadge`, `WhatsAppSendButton`, `Sugestão:` e `stopPropagation` continuam presentes no arquivo |
| T-12-22 | Denial of Service | falso-positivo de "pronto" com o banco fora de sincronia | mitigate | `npm run verify:schema` roda na suíte e falha se a tabela `tarefas` ou suas colunas sumirem do banco real — `tsc`/`build` sozinhos passariam mesmo com o banco desatualizado |
| T-12-23 | Elevation of Privilege | ausência de authz no dashboard | accept | Aplicação solo, `localhost`, sem contas — mesma disposição de T-12-13. Revisitar na migração para o VPS (gate de senha no middleware) |
| T-12-SC | Tampering | supply chain | accept | Nenhuma dependência npm nova e nenhum bloco novo do registry em toda a Fase 12 |
</threat_model>

<verification>
Ver a Task 2 — ela É a verificação da fase. Ordem obrigatória, um gate por vez, dev server parado antes do `npm run build`.
</verification>

<success_criteria>
- `/` renderiza tarefas pendentes junto dos follow-ups nas 3 seções de urgência
- Dentro de cada seção, os itens saem ordenados por data com tipos misturados (D-04)
- Contagem `· N` do cabeçalho soma tarefas + follow-ups
- Botão "Nova tarefa" presente no cabeçalho e no estado vazio, em `variant="outline"` (D-05)
- Estado vazio com a cópia atualizada e 3 CTAs na ordem especificada
- Seção sem itens continua omitida por completo
- Card de lead com comportamento e visual idênticos ao de antes da fase
- Nenhuma rota nova criada — a agenda vive em `/`
- Suíte completa de gates verde, incluindo `npm run build`
</success_criteria>

<output>
Criar `.planning/phases/12-agenda-tarefas-soltas/12-04-SUMMARY.md` ao terminar, com: tabela de gates (comando → exit code), evidência dos 3 Success Criteria do ROADMAP, rastro de D-01 a D-08, e o resultado item a item (1-17) da verificação humana.
</output>
