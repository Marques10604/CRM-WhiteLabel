---
phase: 12-agenda-tarefas-soltas
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/actions/tarefa-actions.ts
  - src/db/queries.ts
  - scripts/test-tarefa-actions.cjs
  - scripts/test-group-by-urgency.cjs
  - package.json
autonomous: true
requirements: [TAREFA-01, TAREFA-02]

must_haves:
  truths:
    - "`createTarefa` persiste uma tarefa com descrição + data e devolve a linha inserida com `concluidaEm` NULL"
    - "`createTarefa` rejeita descrição vazia/só-espaços e data ausente SEM gravar nada no banco"
    - "`concluirTarefa(id)` preenche `concluidaEm` e é idempotente (2ª chamada não sobrescreve o carimbo original)"
    - "`concluirTarefa(id, { desfazer: true })` volta `concluidaEm` para NULL e a tarefa reaparece como pendente"
    - "`deleteTarefa(id)` remove a linha de fato do banco (hard-delete, D-08)"
    - "`groupByUrgency` bucketiza qualquer item por uma data extraída, com `now` injetável e resultado determinístico"
    - "`buildDashboardItems` produz 3 listas de itens lead+tarefa ordenadas por data ascendente (D-04)"
  artifacts:
    - path: "src/actions/tarefa-actions.ts"
      provides: "Server Actions createTarefa/updateTarefa/concluirTarefa/deleteTarefa"
      exports: ["createTarefa", "updateTarefa", "concluirTarefa", "deleteTarefa"]
    - path: "src/db/queries.ts"
      provides: "groupByUrgency genérico + getTarefasPendentes + buildDashboardItems + tipo DashboardItem"
      contains: "export function groupByUrgency"
    - path: "scripts/test-tarefa-actions.cjs"
      provides: "Harness de comportamento das 4 Server Actions em banco temporário"
      min_lines: 120
    - path: "scripts/test-group-by-urgency.cjs"
      provides: "Teste da função pura de urgência com `now` fixo"
      min_lines: 60
  key_links:
    - from: "src/actions/tarefa-actions.ts"
      to: "src/lib/validations.ts"
      via: "tarefaSchema.safeParse / tarefaUpdateSchema.safeParse"
      pattern: "tarefaSchema|tarefaUpdateSchema"
    - from: "src/actions/tarefa-actions.ts"
      to: "revalidatePath('/')"
      via: "helper revalidateTarefaRoutes chamado após cada mutação"
      pattern: "revalidatePath\\(\"/\"\\)"
    - from: "src/db/queries.ts"
      to: "tabela tarefas"
      via: "getTarefasPendentes filtrando isNull(tarefas.concluidaEm)"
      pattern: "isNull\\(tarefas.concluidaEm\\)"
    - from: "src/db/queries.ts"
      to: "groupLeadsByUrgency (call-sites existentes)"
      via: "wrapper fino preservado sobre groupByUrgency"
      pattern: "export function groupLeadsByUrgency"
---

<objective>
Construir a camada de servidor da agenda: as 4 Server Actions de tarefa (`createTarefa`, `updateTarefa`, `concluirTarefa`, `deleteTarefa`), a generalização da função pura de urgência (`groupByUrgency<T>`) preservando `groupLeadsByUrgency` como wrapper, a leitura `getTarefasPendentes` e a fusão ordenada lead+tarefa (`buildDashboardItems`, D-04) — tudo coberto por dois harnesses `.cjs` automatizados.

Propósito: TAREFA-01 (persistir tarefa sem lead) e a metade de servidor de TAREFA-02 (mesma régua de urgência dos follow-ups). A UI do plano 12-03/12-04 consome estes contratos sem precisar explorar o codebase.

Observação importante: o `12-CONTEXT.md` afirma que `groupLeadsByUrgency` "está testada" — está INCORRETO (confirmado pelo pattern-mapper: não existe nenhum teste dessa função no repositório). O teste da função de urgência é trabalho NOVO desta fase, não uma extensão.

Saída: `src/actions/tarefa-actions.ts`, `src/db/queries.ts` estendido, 2 scripts de teste registrados em `package.json`.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-agenda-tarefas-soltas/12-CONTEXT.md
@.planning/phases/12-agenda-tarefas-soltas/12-PATTERNS.md
@.planning/phases/12-agenda-tarefas-soltas/12-01-SUMMARY.md

@src/actions/motivo-perda-actions.ts
@src/db/queries.ts
@scripts/test-motivo-perda-actions.cjs
@scripts/test-compute-sequencia-sugestao.cjs
</context>

<interfaces>
<!-- Contratos que este plano CRIA. Os planos 12-03 e 12-04 consomem exatamente estes shapes. -->

`src/actions/tarefa-actions.ts`:
- `type ActionState = { success: true; tarefa?: Tarefa } | { errors: Record<string, string[] | undefined> } | undefined`
- `createTarefa(_prevState: ActionState, formData: FormData): Promise<ActionState>` — ligada a `useActionState`
- `updateTarefa(_prevState: ActionState, formData: FormData): Promise<ActionState>` — ligada a `useActionState`
- `concluirTarefa(id: number, opts?: { desfazer?: boolean }): Promise<ActionState>` — argumentos posicionais, fire-and-forget
- `deleteTarefa(id: number): Promise<ActionState>` — argumentos posicionais, HARD-DELETE

`src/db/queries.ts`:
- `export function groupByUrgency<T>(items: T[], getDate: (item: T) => Date, now?: Date): { vencidos: T[]; hoje: T[]; proximos7Dias: T[] }`
- `export function groupLeadsByUrgency(activeLeads: Lead[], now?: Date): LeadsByUrgency` — wrapper fino preservado (call-sites atuais intocados)
- `export async function getTarefasPendentes(): Promise<Tarefa[]>`
- `export type DashboardItem = { kind: "lead"; date: Date; lead: Lead } | { kind: "tarefa"; date: Date; tarefa: Tarefa }`
- `export type DashboardItemsByUrgency = { vencidos: DashboardItem[]; hoje: DashboardItem[]; proximos7Dias: DashboardItem[] }`
- `export function buildDashboardItems(activeLeads: Lead[], tarefasPendentes: Tarefa[], now?: Date): DashboardItemsByUrgency` — pura, sem I/O, cada bucket ordenado por `date` ascendente

Régua de urgência (inalterada, herdada de `groupLeadsByUrgency`): `vencidos` = `isBefore(d, startOfDay(now))`; `hoje` = `isToday(d)`; `proximos7Dias` = `isBefore(d, addDays(startOfDay(now), 8))`. Itens 8+ dias no futuro não caem em nenhum bucket.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar `src/actions/tarefa-actions.ts` com as 4 Server Actions</name>
  <files>src/actions/tarefa-actions.ts</files>
  <read_first>
    - `src/actions/motivo-perda-actions.ts` (molde principal) — imports L1-7, doc-comment de política L9-21, `ActionState` L22-25, helper `revalidateMotivoPerdaRoutes` L31-36, padrão `safeParse` + `flatten().fieldErrors` L42-45, idempotência via `isNull` no WHERE L99-104, `sql\`(unixepoch())\`` L103
    - `src/actions/lead-actions.ts` — `ActionState` L17-20, `createLead` L65-123 (safeParse → insert `.returning()`), `updateLead` L125-210 (SELECT-then-write), `updateLeadStage` L129-131 (validação inline de id: `Number.isInteger(id) && id > 0`), `softDeleteLead` L396-407 (idioma de exclusão idempotente), comentário de convenção de soft-delete L3-8
    - `src/lib/validations.ts` — `tarefaSchema` / `tarefaUpdateSchema` (criados no plano 12-01)
    - `src/db/schema.ts` — tabela `tarefas` (criada no plano 12-01)
    - `scripts/guard-no-hard-delete.cjs` — a `ALLOWLIST` que já contém este arquivo (plano 12-01)
    - `.planning/phases/12-agenda-tarefas-soltas/12-PATTERNS.md` §`src/actions/tarefa-actions.ts`
  </read_first>
  <behavior>
    - `createTarefa` com descrição "Ligar pro cowork" e data válida → `{ success: true, tarefa }` com `concluidaEm === null`, 1 linha nova no banco
    - `createTarefa` com descrição "" ou "   " → `{ errors: { descricao: [...] } }`, 0 linhas gravadas
    - `createTarefa` com data ausente ou string não-parseável → `{ errors: { data: [...] } }`, 0 linhas gravadas
    - `updateTarefa` com id existente → descrição e data novas persistidas, `updated_at` reescrito
    - `updateTarefa` com id ausente/inválido → `{ errors: { id: [...] } }`, nada alterado
    - `concluirTarefa(id)` → `concluida_em` deixa de ser NULL; chamar 2x NÃO sobrescreve o carimbo original
    - `concluirTarefa(id, { desfazer: true })` → `concluida_em` volta a NULL
    - `concluirTarefa(0)` / `concluirTarefa(-1)` / `concluirTarefa(1.5)` → `{ errors: { id: [...] } }`, nada alterado
    - `deleteTarefa(id)` → a linha SOME do banco (contagem decrementa, busca por id devolve undefined)
    - `deleteTarefa(0)` → `{ errors: { id: [...] } }`, nada removido
  </behavior>
  <action>
    Criar `src/actions/tarefa-actions.ts` com `"use server"` no topo. Imports: `and`, `eq`, `isNull`, `isNotNull`, `sql` de `drizzle-orm`; `revalidatePath` de `next/cache`; `db` de `@/db/client`; `tarefas` de `@/db/schema`; `tarefaSchema` e `tarefaUpdateSchema` de `@/lib/validations`; tipo `Tarefa` de `@/types`.

    Escrever um doc-comment de cabeçalho que declara a DIVERGÊNCIA DE POLÍTICA em relação ao resto do projeto: exclusão de tarefa é HARD-DELETE por D-08 (tarefa é descartável por natureza — lembrete cumprido ou cancelado), `tarefas` não tem `deletedAt`, e este arquivo é a única entrada da `ALLOWLIST` de `scripts/guard-no-hard-delete.cjs`. Isso é o espelho invertido do comentário de convenção de `src/actions/lead-actions.ts` L3-8.

    Declarar o `ActionState` exatamente como no bloco `<interfaces>` (união de `{ success: true; tarefa?: Tarefa }`, `{ errors: Record<string, string[] | undefined> }` e `undefined`) — shape homogêneo para as 4 funções, compatível com `useActionState`. Declarar o helper `revalidateTarefaRoutes()` chamando apenas `revalidatePath("/")` (a tarefa só aparece no dashboard raiz), invocado após CADA mutação bem-sucedida — molde de `revalidateMotivoPerdaRoutes`.

    `createTarefa(_prevState, formData)`: `tarefaSchema.safeParse(Object.fromEntries(formData))`; em falha, devolver `{ errors: parsed.error.flatten().fieldErrors }` SEM tocar o banco; em sucesso, `db.insert(tarefas).values(parsed.data).returning()`, revalidar e devolver `{ success: true, tarefa: inserted }`.

    `updateTarefa(_prevState, formData)`: `tarefaUpdateSchema.safeParse(Object.fromEntries(formData))`; em falha devolver os `fieldErrors`; em sucesso desestruturar `{ id, ...rest }`, executar `db.update(tarefas).set({ ...rest, updatedAt: sql\`(unixepoch())\` }).where(eq(tarefas.id, id))`, revalidar, devolver `{ success: true }`. NÃO filtrar por `isNull(concluidaEm)` no WHERE — o dialog de edição pode editar uma tarefa já concluída (D-02/D-07); o único guard é o id.

    `concluirTarefa(id, opts)`: validação inline `if (!Number.isInteger(id) || id <= 0) return { errors: { id: ["Tarefa inválida."] } }` (mesmo idioma de `updateLeadStage`). No caminho de conclusão, `db.update(tarefas).set({ concluidaEm: sql\`(unixepoch())\` }).where(and(eq(tarefas.id, id), isNull(tarefas.concluidaEm)))` — o `isNull` no WHERE é o que garante idempotência (2ª chamada é no-op, não sobrescreve o carimbo original), mesmo idioma de `softDeleteMotivoPerda`. No caminho `opts?.desfazer === true`, `.set({ concluidaEm: null }).where(and(eq(tarefas.id, id), isNotNull(tarefas.concluidaEm)))`. Revalidar e devolver `{ success: true }`.

    `deleteTarefa(id)`: mesma validação inline de id; depois `db.delete(tarefas).where(eq(tarefas.id, id))` — esta é a ÚNICA linha `db.delete(...)` legítima de todo o `src/`, permitida só porque este arquivo está na `ALLOWLIST` do guard. Revalidar e devolver `{ success: true }`. Comentário inline citando D-08 na linha do delete.

    NUNCA montar SQL por template string interpolada com input do usuário — toda condição usa os helpers parametrizados do Drizzle (`eq`, `and`, `isNull`, `isNotNull`); `sql\`(unixepoch())\`` é literal fixo sem interpolação de dado.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>MISSING — a cobertura comportamental é criada na Task 2 deste plano (scripts/test-tarefa-actions.cjs)</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sai exit 0.
    - `npm run guard:no-hard-delete` sai exit 0 mesmo com a linha `db.delete(tarefas)` presente (prova de que a `ALLOWLIST` do plano 12-01 funciona).
    - `node -e "const s=require('fs').readFileSync('src/actions/tarefa-actions.ts','utf8'); for(const t of ['export async function createTarefa','export async function updateTarefa','export async function concluirTarefa','export async function deleteTarefa','tarefaSchema.safeParse','tarefaUpdateSchema.safeParse','revalidatePath(\"/\")','db.delete(tarefas)','D-08']) if(!s.includes(t)) { console.error('faltando',t); process.exit(1); } console.log('OK')"` imprime `OK`.
    - Nenhuma query com template string interpolando dado de usuário: `node -e "const s=require('fs').readFileSync('src/actions/tarefa-actions.ts','utf8'); const bad=s.match(/sql\`[^\`]*\\\$\{[^}]*\}/g); if(bad){console.error('SQL interpolado:',bad);process.exit(1);} console.log('OK')"` imprime `OK`.
    - Toda mutação é seguida de revalidação: o arquivo contém pelo menos 4 chamadas a `revalidateTarefaRoutes()`.
  </acceptance_criteria>
  <done>As 4 Server Actions existem, validam todo input com Zod ou guard inteiro antes de tocar o banco, revalidam `/` após cada mutação, e `deleteTarefa` faz hard-delete real passando pelo guard via ALLOWLIST.</done>
</task>

<task type="auto">
  <name>Task 2: Harness `scripts/test-tarefa-actions.cjs` cobrindo os 4 comportamentos</name>
  <files>scripts/test-tarefa-actions.cjs, package.json</files>
  <read_first>
    - `scripts/test-motivo-perda-actions.cjs` (molde estrutural exato — copiar a casca inteira: cabeçalho `"use strict"` + os dois `register(...)` na ordem invertida L16-28, helper `check(condition, message)` + contador `failed` L30-39, `tmpDb` em `os.tmpdir()` com `process.env.DB_FILE_NAME` setado ANTES de qualquer `await import("@/...")` L42-46, setup de DDL cru L48-60, import dinâmico das actions L62-65, helpers `raw`/`rowById`/`countAll`/`snapshot`/`fd(fields)` L67-89, cleanup de `-shm`/`-wal` L245-253, bloco `main().then().catch()` com `process.exit` L256-270)
    - `scripts/test-support/next-cache-stub-loader.mjs` e `scripts/test-support/ts-alias-loader.mjs` (loaders já existentes — reutilizar sem alterar)
    - `scripts/migrate-tarefas.cjs` (criado no plano 12-01 — a DDL crua do harness deve ser IDÊNTICA à da migração)
    - `src/actions/tarefa-actions.ts` (arquivo sob teste, criado na Task 1)
    - `package.json` L5-21 (bloco `scripts`)
    - `.planning/phases/12-agenda-tarefas-soltas/12-PATTERNS.md` §`scripts/test-tarefa-actions.cjs`
  </read_first>
  <action>
    Criar `scripts/test-tarefa-actions.cjs` copiando a casca de `scripts/test-motivo-perda-actions.cjs`. Pontos obrigatórios da casca: os dois `register(...)` na ordem invertida (o stub de `next/cache` registrado por último para executar primeiro), `process.env.DB_FILE_NAME` apontando para um arquivo em `os.tmpdir()` ANTES de qualquer `await import("@/actions/tarefa-actions")`, DDL crua criando `tarefas` com exatamente as mesmas colunas e os 2 índices da migração, helper `check(condition, message)` acumulando falhas, e cleanup dos arquivos `tmpDb`, `-shm` e `-wal` no fim.

    Implementar os 7 cenários (numerar as mensagens de `check` como "Caso N: ..."):
    1. `createTarefa` com descrição e data ISO válidas → resultado tem `success: true`, a linha existe no banco, `concluida_em` é NULL, `descricao` e `data` batem com o enviado.
    2. `createTarefa` com descrição `"   "` (só espaços) → resultado tem `errors.descricao`, `countAll()` não mudou.
    3. `createTarefa` sem o campo `data` (ou com `"nao-e-data"`) → resultado tem `errors.data`, `countAll()` não mudou.
    4. `updateTarefa` alterando descrição e data de uma tarefa existente → os dois campos persistem e `updated_at` é maior ou igual ao valor anterior.
    5. `concluirTarefa(id)` → `concluida_em` deixa de ser NULL. Depois forçar por SQL direto um valor sentinela conhecido em `concluida_em` (ex.: `111111`) e chamar `concluirTarefa(id)` de novo → o sentinela PERMANECE (prova de idempotência via `isNull` no WHERE), molde do Caso 3 do análogo.
    6. `concluirTarefa(id, { desfazer: true })` → `concluida_em` volta a ser NULL.
    7. `deleteTarefa(id)` → `countAll()` decrementa em 1 E `rowById(id)` devolve `undefined` — este é o teste que PROVA o hard-delete de D-08. Cobrir também `deleteTarefa(0)` devolvendo `errors` e não removendo nada.

    NÃO usar `DELETE FROM` para limpar estado entre cenários — o guard varre `scripts/` e este arquivo NÃO está na allowlist. Usar descrições/ids únicos por cenário (precedente registrado no `STATE.md` para as Fases 10-02 e 11).

    Em `package.json`, adicionar `"test:tarefa-actions": "node scripts/test-tarefa-actions.cjs"` junto às demais entradas `test:*`.

    Rodar o harness e o guard em SEQUÊNCIA (host de 4GB).
  </action>
  <verify>
    <automated>npm run test:tarefa-actions</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:tarefa-actions` sai exit 0 e imprime os 7 casos como aprovados, sem nenhuma falha acumulada.
    - `npm run guard:no-hard-delete` sai exit 0 (prova de que o harness não introduziu `DELETE FROM` em `scripts/`).
    - Teste de mutação do Caso 7 (executado uma vez, manualmente, e registrado no SUMMARY): trocar temporariamente `db.delete(tarefas)` por um `update` no-op em `src/actions/tarefa-actions.ts` faz `npm run test:tarefa-actions` sair com exit 1 na asserção do Caso 7; reverter a alteração em seguida. Se preferir não editar o arquivo-fonte real, aplicar a mutação numa cópia em `os.tmpdir()` (precedente do `test-mutation-guard.cjs` da Fase 8).
    - `data/crm.db` NÃO é tocado pelo harness: nenhum arquivo `data/crm.db.backup-*` novo e a contagem de linhas de `tarefas` em `data/crm.db` permanece a mesma antes e depois da execução.
    - `package.json` contém a chave `test:tarefa-actions`.
  </acceptance_criteria>
  <done>Os 4 comportamentos das Server Actions (criar/validar, editar, concluir/desfazer idempotente, hard-delete) têm cobertura automatizada rodando contra banco temporário, com o hard-delete provado por asserção de ausência da linha.</done>
</task>

<task type="auto">
  <name>Task 3: Generalizar a urgência (`groupByUrgency`), ler tarefas pendentes, fundir lead+tarefa por data — com teste</name>
  <files>src/db/queries.ts, scripts/test-group-by-urgency.cjs, package.json</files>
  <read_first>
    - `src/db/queries.ts` (arquivo a modificar) — imports L1-2, `getActiveDashboardLeads` L14-20 (molde de leitura), tipo `LeadsByUrgency` L22-26, doc-comment + corpo de `groupLeadsByUrgency` L28-55 (a função a generalizar), `computeSequenciaSugestao` L135-146 e `resolvePeriodRange` L182-186 (idioma "função pura, `now` injetável, sem I/O")
    - `scripts/test-compute-sequencia-sugestao.cjs` (molde estrutural exato — `"use strict"` + `process.env.DB_FILE_NAME = ":memory:"` ANTES do `register("./ts-alias-loader.mjs", ...)` L28-33, helper `check` L37-44, helper `sameDate` L46-48, IIFE `(async () => {...})().catch(...)` com `process.exit` L50 / L205-208)
    - `src/db/schema.ts` — tabela `tarefas` (plano 12-01)
    - `src/types/index.ts` — tipos `Lead` e `Tarefa`
    - `.planning/phases/12-agenda-tarefas-soltas/12-PATTERNS.md` §`src/db/queries.ts`, §`scripts/test-group-by-urgency.cjs`, §Sem Análogo Exato
    - `.planning/phases/12-agenda-tarefas-soltas/12-UI-SPEC.md` §Layout §Intercalação por data (D-04)
  </read_first>
  <action>
    Em `src/db/queries.ts`, adicionar `tarefas` ao import de `@/db/schema` e o tipo `Tarefa` ao import de `@/types`.

    Extrair a régua de urgência para `export function groupByUrgency<T>(items: T[], getDate: (item: T) => Date, now?: Date)`, com o mesmo corpo de `groupLeadsByUrgency` (normaliza `startOfDay(now ?? new Date())`, limite superior exclusivo `addDays(today, 8)`, três buckets pela ordem `isBefore(d, today)` → `isToday(d)` → `isBefore(d, in7Days)`). Reescrever `groupLeadsByUrgency` como wrapper fino de UMA linha delegando para `groupByUrgency(activeLeads, (l) => l.followUpDate, now)` — a assinatura pública e o tipo `LeadsByUrgency` permanecem intocados para não quebrar nenhum call-site atual. Mover/duplicar o doc-comment existente (a explicação do `addDays(today, 8)` como limite exclusivo) para a função genérica.

    Adicionar `export async function getTarefasPendentes(): Promise<Tarefa[]>` — molde de `getActiveDashboardLeads`: `db.select().from(tarefas).where(isNull(tarefas.concluidaEm)).orderBy(asc(tarefas.data))`. O filtro `concluidaEm IS NULL` é o que implementa D-02 (tarefa concluída some do dashboard na hora). Encapsular aqui, e não inline em `page.tsx`, pelo mesmo motivo já documentado em `getActiveDashboardLeads`: não vazar SQL para a página nem deixar o escopo divergir entre superfícies.

    Adicionar o tipo `DashboardItem` (união discriminada por `kind` com `date` normalizado) e `DashboardItemsByUrgency` conforme o bloco `<interfaces>`, mais `export function buildDashboardItems(activeLeads, tarefasPendentes, now?)`: mapear cada lead para `{ kind: "lead", date: lead.followUpDate, lead }` e cada tarefa para `{ kind: "tarefa", date: tarefa.data, tarefa }`, concatenar as duas listas, chamar `groupByUrgency(todos, (i) => i.date, now)` e ordenar CADA bucket por `date` ascendente antes de devolver. É essa ordenação que materializa D-04 (tarefas e follow-ups intercalados por data dentro da mesma seção, nunca em blocos separados). A função deve ser PURA — nenhum I/O, `now` injetável — para ser testável no harness abaixo.

    Criar `scripts/test-group-by-urgency.cjs` seguindo a casca de `scripts/test-compute-sequencia-sugestao.cjs`. Obrigatório: `process.env.DB_FILE_NAME = ":memory:"` ANTES do `register("./ts-alias-loader.mjs", ...)`, porque `queries.ts` importa `@/db/client` no nível de módulo e sem isso o teste abriria `data/crm.db`. Injetar um `now` FIXO (ex.: `new Date("2026-08-15T12:00:00Z")`) em todas as chamadas para o teste ser determinístico.

    Casos de `groupByUrgency`: item com data `today - 1` → `vencidos`; item com data igual a `today` mas com hora diferente de meia-noite → `hoje` (exercita `isToday`); item com data `today + 7` → `proximos7Dias` (borda inferior do limite `addDays(today, 8)`); item com data `today + 8` → NENHUM bucket; lista vazia → os 3 buckets vazios.
    Casos de `buildDashboardItems`: lista mista de leads e tarefas com datas embaralhadas → cada item cai no bucket certo pelo seu próprio campo de data, cada bucket sai ordenado por `date` ascendente, e uma tarefa com data anterior à de um lead aparece ANTES dele no array (asserção direta de D-04 — verificar `kind` na posição esperada).
    Caso de regressão: `groupLeadsByUrgency` com a mesma lista de leads devolve exatamente os mesmos leads nos mesmos buckets que antes da generalização.

    Em `package.json`, adicionar `"test:group-by-urgency": "node scripts/test-group-by-urgency.cjs"` junto às demais entradas `test:*`.
  </action>
  <verify>
    <automated>npm run test:group-by-urgency</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:group-by-urgency` sai exit 0, com pelo menos um caso explícito para cada uma das 4 fronteiras (`today-1`, `today` com hora ≠ 00:00, `today+7`, `today+8`) e pelo menos um caso de intercalação provando ordem por data com `kind` misturado.
    - `npx tsc --noEmit` sai exit 0.
    - `node -e "const s=require('fs').readFileSync('src/db/queries.ts','utf8'); for(const t of ['export function groupByUrgency','export function groupLeadsByUrgency','export async function getTarefasPendentes','export function buildDashboardItems','isNull(tarefas.concluidaEm)']) if(!s.includes(t)) { console.error('faltando',t); process.exit(1); } console.log('OK')"` imprime `OK`.
    - `npm run test:compute-sequencia` continua exit 0 (nenhuma regressão nas outras funções puras de `queries.ts`).
    - O teste NÃO cria nem modifica `data/crm.db` (usa `:memory:`).
    - `package.json` contém a chave `test:group-by-urgency`.
  </acceptance_criteria>
  <done>`groupByUrgency<T>` é genérica, pura e com `now` injetável; `groupLeadsByUrgency` segue funcionando como wrapper; `getTarefasPendentes` filtra `concluidaEm IS NULL`; `buildDashboardItems` devolve 3 listas intercaladas e ordenadas por data. Todos cobertos por teste automatizado — a lacuna de cobertura histórica da função de urgência está fechada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| formulário do navegador → Server Action | `FormData` não confiável cruza aqui (`createTarefa`, `updateTarefa`) |
| componente cliente → Server Action posicional | `id: number` vindo do cliente cruza aqui (`concluirTarefa`, `deleteTarefa`) |
| Server Action → SQLite | Toda escrita, incluindo o único hard-delete legítimo do projeto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Tampering | `createTarefa` / `updateTarefa` (input de formulário) | mitigate | `tarefaSchema` / `tarefaUpdateSchema` via `safeParse(Object.fromEntries(formData))` ANTES de qualquer acesso ao banco; retorno de `fieldErrors` sem escrita. Casos 2 e 3 de `test-tarefa-actions.cjs` provam que nada é gravado em input inválido |
| T-12-08 | Tampering | `concluirTarefa` / `deleteTarefa` (id posicional) | mitigate | Guard inteiro `Number.isInteger(id) && id > 0` antes do banco (idioma de `updateLeadStage`); ids fracionários, zero e negativos rejeitados — coberto por asserção no harness |
| T-12-09 | Tampering (SQL injection) | todas as queries de `tarefa-actions.ts` e `getTarefasPendentes` | mitigate | Somente helpers parametrizados do Drizzle (`eq`, `and`, `isNull`, `isNotNull`); nenhum `sql` template com interpolação de dado de usuário — gate automatizado na acceptance criteria da Task 1 |
| T-12-10 | Denial of Service (perda de dado) | `deleteTarefa` (hard-delete) | mitigate | `db.delete(tarefas)` sempre com `.where(eq(tarefas.id, id))` — nunca sem WHERE; `guard-no-hard-delete` continua bloqueando `.delete(leads)`/`.delete(subnichos)`/`.delete(interacoes)`/`.delete(motivosPerda)` inclusive dentro deste arquivo, porque a `ALLOWLIST` isenta o caminho mas os padrões de outras tabelas não foram removidos |
| T-12-11 | Tampering | dupla-chamada de `concluirTarefa` sobrescrevendo o carimbo original | mitigate | `isNull(tarefas.concluidaEm)` no WHERE torna a operação idempotente; provado pelo Caso 5 com valor sentinela |
| T-12-12 | Information Disclosure | mensagens de erro das actions | mitigate | Apenas `fieldErrors` do Zod e strings fixas em português; nenhum `err.message`/stack de banco é devolvido ao cliente |
| T-12-13 | Elevation of Privilege | ausência de authz — qualquer requisição pode concluir/excluir qualquer tarefa | accept | Aplicação solo de usuário único por design (`PROJECT.md` §Constraints); não há multi-tenant nem contas. Revisitar quando o CRM migrar para o VPS público (gate de senha no middleware, ver `STATE.md` §Direção de infraestrutura) |
| T-12-SC | Tampering | supply chain | accept | Nenhuma dependência npm nova nesta fase; nenhum comando de instalação executado |
</threat_model>

<verification>
Rodar em SEQUÊNCIA (host de 4GB RAM, dev server parado):

1. `npx tsc --noEmit` → exit 0
2. `npm run test:tarefa-actions` → exit 0, 7 casos aprovados
3. `npm run test:group-by-urgency` → exit 0
4. `npm run test:compute-sequencia` → exit 0 (regressão)
5. `npm run guard:no-hard-delete` → exit 0
6. `npm run verify:schema` → exit 0
</verification>

<success_criteria>
- As 4 Server Actions existem com o `ActionState` homogêneo e revalidam `/` após cada mutação
- Input inválido (descrição vazia, data inválida, id não-inteiro/não-positivo) nunca chega ao banco — provado por asserção de contagem inalterada
- `concluirTarefa` é idempotente e reversível via `{ desfazer: true }`
- `deleteTarefa` remove a linha de fato, provado por ausência da linha no banco temporário
- `groupByUrgency<T>` é genérica, pura, com `now` injetável e testada nas 4 fronteiras de data
- `buildDashboardItems` devolve buckets com leads e tarefas intercalados e ordenados por data ascendente (D-04), provado por asserção de ordem com `kind` misturado
- `groupLeadsByUrgency` preservado como wrapper — nenhum call-site existente quebrado
</success_criteria>

<output>
Criar `.planning/phases/12-agenda-tarefas-soltas/12-02-SUMMARY.md` ao terminar, registrando a saída dos 3 harnesses e o resultado do teste de mutação do Caso 7 (hard-delete).
</output>
