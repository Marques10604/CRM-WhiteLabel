---
phase: 12-agenda-tarefas-soltas
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - src/components/delete-tarefa-dialog.tsx
  - src/components/tarefa-form-dialog.tsx
  - src/components/tarefa-card.tsx
autonomous: true
requirements: [TAREFA-01, TAREFA-02]

must_haves:
  truths:
    - "O admin abre um dialog próprio de tarefa com exatamente 2 campos — Descrição e Data — sem nenhum campo de lead (D-06)"
    - "No modo edição o rodapé oferece Excluir, Cancelar, Concluir e Salvar (12-UI-SPEC §Copywriting)"
    - "Excluir pede confirmação num dialog não-dispensável antes do hard-delete (D-08)"
    - "O card de tarefa mostra só ícone + descrição + data — sem sub-nicho, sem badge de etapa, sem botão de WhatsApp (D-03)"
    - "O botão-ícone de concluir no card não abre o dialog de edição (stopPropagation, D-07)"
    - "O toast de conclusão oferece Desfazer, que reabre a tarefa"
  artifacts:
    - path: "src/components/delete-tarefa-dialog.tsx"
      provides: "Confirmação não-dispensável de exclusão (hard-delete)"
      contains: "Excluir tarefa"
    - path: "src/components/tarefa-form-dialog.tsx"
      provides: "Form dialog de criação/edição de tarefa (react-hook-form + zodResolver + Calendar)"
      contains: "zodResolver(tarefaSchema)"
      min_lines: 120
    - path: "src/components/tarefa-card.tsx"
      provides: "Card enxuto de tarefa com botão-ícone de concluir"
      contains: "ListTodo"
  key_links:
    - from: "src/components/tarefa-form-dialog.tsx"
      to: "src/actions/tarefa-actions.ts"
      via: "useActionState(isEditMode ? updateTarefa : createTarefa)"
      pattern: "useActionState"
    - from: "src/components/tarefa-form-dialog.tsx"
      to: "src/components/delete-tarefa-dialog.tsx"
      via: "botão Excluir do rodapé em modo edição"
      pattern: "DeleteTarefaDialog"
    - from: "src/components/tarefa-card.tsx"
      to: "src/actions/tarefa-actions.ts"
      via: "concluirTarefa dentro de startTransition"
      pattern: "concluirTarefa"
    - from: "src/components/tarefa-card.tsx"
      to: "toast Desfazer"
      via: "concluirTarefa(id, { desfazer: true })"
      pattern: "desfazer"
---

<objective>
Construir os três componentes de UI da tarefa, isolados do dashboard: o dialog de confirmação de exclusão, o form dialog de criação/edição e o card enxuto com ação rápida de concluir. Nenhum deles é montado ainda — a integração no dashboard é o plano 12-04.

Propósito: TAREFA-01 exige uma superfície de criação (D-05/D-06); D-07 exige que clicar no card abra edição e que exista uma ação rápida de concluir no próprio card; D-03 exige que o card seja distinguível do card de lead por SUBTRAÇÃO (sem sub-nicho, sem badge, sem WhatsApp) mais um ícone; D-08 exige confirmação antes do hard-delete.

Saída: `delete-tarefa-dialog.tsx`, `tarefa-form-dialog.tsx`, `tarefa-card.tsx`.

Restrição de host: NÃO rodar `npx shadcn add checkbox` nem qualquer comando do registry — já falhou por memória neste host de 4GB, e o `12-UI-SPEC.md` materializa deliberadamente o "checkbox concluir" como `Button variant="ghost" size="icon-lg"`. Nenhum bloco novo do shadcn e nenhuma dependência npm entram nesta fase.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-agenda-tarefas-soltas/12-CONTEXT.md
@.planning/phases/12-agenda-tarefas-soltas/12-UI-SPEC.md
@.planning/phases/12-agenda-tarefas-soltas/12-PATTERNS.md
@.planning/phases/12-agenda-tarefas-soltas/12-02-SUMMARY.md

@src/components/lead-form-dialog.tsx
@src/components/delete-motivo-perda-dialog.tsx
@src/components/whatsapp-send-button.tsx
@src/components/followup-dashboard.tsx
@src/actions/tarefa-actions.ts
</context>

<interfaces>
<!-- Contratos consumidos (criados no plano 12-02) e produzidos (consumidos pelo plano 12-04). -->

Consumidos de `@/actions/tarefa-actions`:
- `createTarefa(_prevState, formData)` / `updateTarefa(_prevState, formData)` — para `useActionState`
- `concluirTarefa(id: number, opts?: { desfazer?: boolean })` — posicional, fire-and-forget
- `deleteTarefa(id: number)` — posicional, hard-delete

Consumidos de `@/lib/validations`: `tarefaSchema`, `type TarefaFormValues`
Consumido de `@/types`: `type Tarefa` (campos: `id`, `descricao`, `data`, `concluidaEm`, `createdAt`, `updatedAt`)

Produzidos por este plano (props exatas que o plano 12-04 vai usar):
- `DeleteTarefaDialog({ open, onOpenChange, descricao, onConfirm })`
- `TarefaFormDialog({ open, onOpenChange, tarefa? })` — `tarefa` ausente = modo criação
- `TarefaCard({ tarefa, dateClassName, onEdit })` — `dateClassName` vem de `section.dateClassName` do dashboard; `onEdit(tarefa)` abre o `TarefaFormDialog` em modo edição no componente pai

Primitivos de UI já instalados (NÃO instalar nada): `Dialog`, `Button`, `Field`/`FieldLabel`/`FieldContent`/`FieldDescription`/`FieldError`, `Input`, `Calendar`, `sonner`. Ícones lucide já presentes no pacote: `ListTodo`, `Circle`, `CircleCheck`, `Trash2`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: `DeleteTarefaDialog` + `TarefaFormDialog`</name>
  <files>src/components/delete-tarefa-dialog.tsx, src/components/tarefa-form-dialog.tsx</files>
  <read_first>
    - `src/components/delete-motivo-perda-dialog.tsx` (molde 1:1 do dialog de exclusão — arquivo inteiro L1-53, incluindo `<DialogContent showCloseButton={false}>` e o `DialogFooter` L42-49)
    - `src/components/lead-form-dialog.tsx` (molde do form dialog) — imports L1-44, `useActionState` + modo criar/editar L101-105, `useForm` com `defaultValues` L116-139 (atenção ao Pitfall do default de data documentado em L127-134), `useEffect` de toast pós-submit L141-159, `closeWithDiscardGuard`/`handleDiscard` L161-177, `onSubmit` com `FormData` bruto do DOM + `startTransition` L179-191, `DialogContent className="max-w-lg"` L198, `<form ... noValidate>` + input hidden de `id` L203-210, `Field` de "Nome" L215-221 (molde do campo de texto), `Controller` + `Calendar` + input hidden do campo `followUpDate` L427-457, `DialogFooter` L460-482
    - `src/components/discard-changes-dialog.tsx` (guarda de alterações não salvas reaproveitada)
    - `src/actions/tarefa-actions.ts` (plano 12-02 — assinaturas exatas das 4 actions)
    - `src/lib/validations.ts` — `tarefaSchema`, `TarefaFormValues`
    - `.planning/phases/12-agenda-tarefas-soltas/12-UI-SPEC.md` §Copywriting Contract (TODAS as strings são verbatim de lá) e §Layout §`TarefaFormDialog` / §`DeleteTarefaDialog`
    - `.planning/phases/12-agenda-tarefas-soltas/12-PATTERNS.md` §`src/components/tarefa-form-dialog.tsx`, §`src/components/delete-tarefa-dialog.tsx`
  </read_first>
  <action>
    Criar `src/components/delete-tarefa-dialog.tsx` como cópia estrutural de `delete-motivo-perda-dialog.tsx`, trocando a prop `motivoPerdaNome` por `descricao` e mantendo `open` / `onOpenChange` / `onConfirm`. Manter `<DialogContent showCloseButton={false}>` (não-dispensável pelo X, D-08). `DialogTitle` = "Excluir tarefa". `DialogDescription` = "Tem certeza que deseja excluir a tarefa \"{descricao}\"? Essa ação não pode ser desfeita." `DialogFooter` com "Cancelar" (`variant="outline"`, chama `onOpenChange(false)`) e "Excluir" (`variant="destructive"`, chama `onConfirm`). Todas as strings VERBATIM do 12-UI-SPEC.md §Copywriting.

    Criar `src/components/tarefa-form-dialog.tsx` copiando a casca de `lead-form-dialog.tsx` e podando tudo que não se aplica. Props: `open`, `onOpenChange`, `tarefa?` (ausente = modo criação). Estrutura obrigatória:

    - `"use client"`; `const isEditMode = Boolean(tarefa)`; `const [state, formAction, pending] = useActionState(isEditMode ? updateTarefa : createTarefa, undefined)`.
    - `useForm<TarefaFormValues>({ resolver: zodResolver(tarefaSchema), defaultValues: { descricao: tarefa?.descricao ?? "", data: tarefa?.data ?? startOfDay(new Date()) } })`. O default de `data` PRECISA ser um `Date` real no modo criação — este é o Pitfall documentado em `lead-form-dialog.tsx` L127-134: o `Calendar` só destaca "hoje" visualmente, não é seleção, e sem o default o `zodResolver` barra o submit silenciosamente.
    - `onSubmit` monta `new FormData(formRef.current)` do DOM bruto e envolve `formAction(formData)` em `startTransition` — copiar o idioma de `lead-form-dialog.tsx` L179-191. O React 19 exige a transição; sem ela aparece o warning "called outside of a transition" (regressão já corrigida no quick task 260808-h5i).
    - `<form ref={formRef} onSubmit={form.handleSubmit(onSubmit)} className="flex flex-col gap-6" noValidate>` e, em modo edição, `<input type="hidden" name="id" value={tarefa.id} readOnly />`. O `noValidate` é obrigatório para o `zodResolver` assumir a validação em vez do HTML5 nativo (regressão já corrigida no quick task 260801-ij4).
    - Campo Descrição: `<Field>` + `<FieldLabel htmlFor="descricao">Descrição</FieldLabel>` + `<FieldContent>` com `<Input id="descricao" placeholder="Ex: Ligar pro cowork sobre o CSV de agosto" {...form.register("descricao")} />`, `<FieldDescription>O que precisa ser feito. Serve de título da tarefa.</FieldDescription>` e `<FieldError>`. `Input` de LINHA ÚNICA, nunca `Textarea` — o `truncate` do card assume single-line (D-06).
    - Campo Data: `<Field>` + `<FieldLabel>Data</FieldLabel>` + `<Controller name="data">` renderizando `<Calendar mode="single" selected={...} onSelect={(date) => field.onChange(date ? startOfDay(date) : undefined)} />` seguido de `<input type="hidden" name="data" value={selected ? selected.toISOString() : ""} readOnly />` — copiar o idioma exato do campo `followUpDate` de `lead-form-dialog.tsx` L427-457, incluindo a normalização `startOfDay` (Pitfall de fuso: sem ela a tarefa "pula" de dia perto da virada). `FieldDescription` = "Dia em que essa tarefa precisa acontecer."
    - Os dois `<Field>` ficam soltos dentro do `flex flex-col gap-6`. NÃO envolver em seção `bg-[#F4F4F5] p-6` (o form de lead agrupa 3 seções; a tarefa tem 2 campos).
    - `DialogContent className="max-w-lg"`; `DialogTitle` = "Nova tarefa" no modo criação e "Editar tarefa" no modo edição.
    - `DialogFooter` — modo criar: "Cancelar" (`variant="outline"`) e "Salvar" (`type="submit"`, `className="bg-[#0D9488] text-white hover:bg-[#0D9488]/90"`, texto "Salvando..." e `disabled` enquanto `pending`). Modo editar: "Excluir" (`variant="destructive"`, `type="button"`, abre o `DeleteTarefaDialog`, posicionado à ESQUERDA e separado do grupo à direita — mesmo lugar do "Ver histórico" em `lead-form-dialog.tsx` L461-471), "Cancelar" (`variant="outline"`), "Concluir" (`variant="outline"`, `type="button"`, com `<CircleCheck className="size-4" />` à esquerda do texto) e "Salvar".
    - "Concluir" chama `concluirTarefa(tarefa.id)` dentro de `startTransition`, fecha o dialog e dispara `toast.success("Tarefa concluída.", { action: { label: "Desfazer", onClick: ... } })`, onde o `onClick` chama `concluirTarefa(tarefa.id, { desfazer: true })` dentro de `startTransition` e dispara `toast.success("Tarefa reaberta.")`. O botão "Desfazer" é REQUISITO confirmado pelo usuário, não discricionário.
    - `onConfirm` do `DeleteTarefaDialog` chama `deleteTarefa(tarefa.id)` dentro de `startTransition`, fecha os DOIS dialogs e dispara `toast.success("Tarefa excluída.")`.
    - `useEffect` de toast pós-submit (molde L141-159): sucesso → `toast.success("Tarefa criada.")` no modo criação e `toast.success("Tarefa salva.")` no modo edição, fechando o dialog; erro → `toast.error("Não foi possível salvar a tarefa. Tente novamente.")`. Nunca exibir stack trace.
    - Guarda de alterações não salvas: reaproveitar `DiscardChangesDialog` via `form.formState.isDirty` no `onOpenChange`, mesmo idioma de `closeWithDiscardGuard` em `lead-form-dialog.tsx`. Se optar por omitir (permitido pelo 12-UI-SPEC.md §Layout para um form de 2 campos), o `onOpenChange` fecha direto — registrar a escolha no SUMMARY.

    Toda cor, tamanho e espaçamento vem do 12-UI-SPEC.md — não inventar tokens. O accent `#0D9488` só aparece no botão "Salvar" e nos anéis de foco; "Nova tarefa" e "Concluir" são `variant="outline"`.

    Se o ESLint apontar `react-hooks/refs` no par `onSubmit={form.handleSubmit(onSubmit)}` + leitura de `formRef.current`, ou `react-hooks/set-state-in-effect` no efeito de reset, suprimir com `eslint-disable-next-line` DOCUMENTADO — é o mesmo falso-positivo do React Compiler já aceito em `template-form-dialog.tsx`, `configuracoes-form.tsx` e `lead-timeline-dialog.tsx` (decisão registrada no `STATE.md` para as Fases 07-02 e 09-03). Não alterar o comportamento para contornar o linter.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npx eslint src/components/tarefa-form-dialog.tsx src/components/delete-tarefa-dialog.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sai exit 0.
    - `npx eslint src/components/tarefa-form-dialog.tsx src/components/delete-tarefa-dialog.tsx` sai exit 0 (escopado aos 2 arquivos — o repositório tem 413 problemas pré-existentes fora deste escopo, documentados em `deferred-items.md`).
    - Todas as strings de cópia batem verbatim com o 12-UI-SPEC.md: `node -e "const s=require('fs').readFileSync('src/components/tarefa-form-dialog.tsx','utf8')+require('fs').readFileSync('src/components/delete-tarefa-dialog.tsx','utf8'); for(const t of ['Nova tarefa','Editar tarefa','Descrição','Ex: Ligar pro cowork sobre o CSV de agosto','O que precisa ser feito. Serve de título da tarefa.','Dia em que essa tarefa precisa acontecer.','Tarefa criada.','Tarefa salva.','Tarefa concluída.','Desfazer','Tarefa reaberta.','Tarefa excluída.','Não foi possível salvar a tarefa. Tente novamente.','Excluir tarefa','Essa ação não pode ser desfeita.','Salvando...']) if(!s.includes(t)) { console.error('copy faltando:',t); process.exit(1); } console.log('OK')"` imprime `OK`.
    - Contratos estruturais presentes: `node -e "const s=require('fs').readFileSync('src/components/tarefa-form-dialog.tsx','utf8'); for(const t of ['zodResolver(tarefaSchema)','useActionState','startTransition','noValidate','startOfDay','type=\"hidden\"','DeleteTarefaDialog','concluirTarefa','deleteTarefa','desfazer']) if(!s.includes(t)) { console.error('faltando',t); process.exit(1); } if(s.includes('Textarea')) { console.error('descricao deve ser Input de linha unica (D-06)'); process.exit(1); } console.log('OK')"` imprime `OK`.
    - `delete-tarefa-dialog.tsx` usa `showCloseButton={false}` (não-dispensável, D-08).
    - `package.json` NÃO ganhou nenhuma dependência nova e `src/components/ui/` NÃO ganhou nenhum arquivo novo: `git status --porcelain src/components/ui package.json package-lock.json` não lista nada.
  </acceptance_criteria>
  <done>Os dois dialogs existem, compilam, passam no lint escopado, usam exclusivamente primitivos já instalados e reproduzem a cópia do 12-UI-SPEC.md verbatim. O fluxo Excluir pede confirmação; Concluir oferece Desfazer.</done>
</task>

<task type="auto">
  <name>Task 2: `TarefaCard` — card enxuto com botão-ícone de concluir</name>
  <files>src/components/tarefa-card.tsx</files>
  <read_first>
    - `src/components/followup-dashboard.tsx` — casca clicável do card de lead L168-180 (classes do container, `role="button"`, `tabIndex`, `onKeyDown` Enter/Space com `preventDefault`) e o wrapper de `stopPropagation` do botão de WhatsApp L206-219
    - `src/components/whatsapp-send-button.tsx` (arquivo inteiro — molde do botão-ícone ghost de 36px)
    - `src/components/pipeline-lead-card.tsx` L67-73 (`<span>` com `min-w-0 truncate` + `title` no hover) e L6 (`import { cn } from "@/lib/utils"`)
    - `src/components/ui/button.tsx` L33 (confirmação de que `size="icon-lg"` = 36px)
    - `src/actions/tarefa-actions.ts` (plano 12-02 — assinatura de `concluirTarefa`)
    - `.planning/phases/12-agenda-tarefas-soltas/12-UI-SPEC.md` §Layout §Card de tarefa e §Controle rápido de "concluir"; §Color (a data reaproveita `section.dateClassName`, NUNCA uma paleta nova)
    - `.planning/phases/12-agenda-tarefas-soltas/12-PATTERNS.md` §`src/components/tarefa-card.tsx`
  </read_first>
  <action>
    Criar `src/components/tarefa-card.tsx` (`"use client"`) exportando `TarefaCard({ tarefa, dateClassName, onEdit })`.

    Container: copiar VERBATIM as classes do card de lead do dashboard (`followup-dashboard.tsx` L179) — `flex cursor-pointer items-center justify-between gap-4 rounded-lg border border-zinc-200 bg-white p-4 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#0D9488]` — com `role="button"`, `tabIndex={0}`, `onClick` chamando `onEdit(tarefa)` e `onKeyDown` que trata Enter e Space com `event.preventDefault()` antes de chamar `onEdit(tarefa)`. É o MESMO container do card de lead de propósito: a distinção vem da subtração, não de cor, borda ou fundo diferente (D-03).

    Cluster esquerdo (`flex items-center gap-2 min-w-0`): `<ListTodo className="size-4 shrink-0 text-muted-foreground" aria-hidden />` seguido de um `<div className="flex flex-col gap-1 min-w-0">` com (a) a descrição num `<span className="min-w-0 truncate text-[16px] leading-normal font-normal text-foreground" title={tarefa.descricao}>` e (b) a data num `<span className={cn("text-[14px] leading-normal", dateClassName)}>` formatada com `format(tarefa.data, "dd/MM/yyyy")` de `date-fns`. SÓ a data — proibido renderizar sub-nicho, `EtapaBadge`, botão de WhatsApp, contador de tentativas ou "Sugestão: dd/MM" (D-03). O `dateClassName` chega por prop e é repassado de `section.dateClassName` pelo dashboard, garantindo que uma tarefa vencida e um follow-up vencido tenham a data na MESMA cor (D-04).

    Cluster direito: um `<div onClick={(e) => e.stopPropagation()} onKeyDown={(e) => e.stopPropagation()}>` envolvendo APENAS o botão de concluir. O `stopPropagation` é o que impede a ação rápida de abrir o dialog de edição (D-07, mesmo idioma do wrapper do `WhatsAppSendButton`).

    Botão de concluir: `<Button type="button" variant="ghost" size="icon-lg" aria-label={"Concluir tarefa: " + tarefa.descricao} title="Concluir tarefa">`. Contrato de ícone: em REPOUSO mostra `Circle` em `text-muted-foreground`; em HOVER e em `focus-visible` mostra `CircleCheck` em `text-[#0D9488]`. A implementação é livre — dois ícones empilhados com `group` + `group-hover:` + `group-focus-visible:`, ou estado de hover em `useState` — desde que o contrato de repouso/hover seja respeitado e o foco por teclado produza o mesmo efeito visual do mouse.

    Ao clicar: chamar `concluirTarefa(tarefa.id)` dentro de `startTransition` (fire-and-forget, mesmo idioma do `registerWhatsAppContact` no anchor de WhatsApp) e disparar `toast.success("Tarefa concluída.", { action: { label: "Desfazer", onClick: ... } })`, com o `onClick` do Desfazer chamando `concluirTarefa(tarefa.id, { desfazer: true })` dentro de `startTransition` e disparando `toast.success("Tarefa reaberta.")`. SEM dialog de confirmação — conclusão é reversível pelo toast; só a exclusão confirma (D-08). O `revalidatePath("/")` das actions é o que faz a tarefa sumir/reaparecer no dashboard na hora (D-02).

    Se o toast e o Desfazer forem centralizados num helper compartilhado com o `TarefaFormDialog`, extraí-lo para dentro deste arquivo e reutilizá-lo — não duplicar as strings em dois lugares.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npx eslint src/components/tarefa-card.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sai exit 0.
    - `npx eslint src/components/tarefa-card.tsx` sai exit 0.
    - Ausência de adereços de lead comprovada por asserção de fonte: `node -e "const s=require('fs').readFileSync('src/components/tarefa-card.tsx','utf8'); for(const p of ['EtapaBadge','WhatsAppSendButton','subnicho','Subnicho','normalizePhone','CalendarClock','Sugestão']) if(s.includes(p)) { console.error('card de tarefa nao pode conter',p,'(D-03)'); process.exit(1); } console.log('OK')"` imprime `OK`.
    - Contratos presentes: `node -e "const s=require('fs').readFileSync('src/components/tarefa-card.tsx','utf8'); for(const t of ['ListTodo','Circle','CircleCheck','stopPropagation','role=\"button\"','tabIndex={0}','concluirTarefa','startTransition','Concluir tarefa','Desfazer','size=\"icon-lg\"','truncate']) if(!s.includes(t)) { console.error('faltando',t); process.exit(1); } console.log('OK')"` imprime `OK`.
    - O container do card usa exatamente as mesmas classes do card de lead do dashboard (comparar com `followup-dashboard.tsx` L179): o arquivo contém a substring `rounded-lg border border-zinc-200 bg-white p-4`.
    - Nenhum arquivo novo em `src/components/ui/` e nenhuma dependência npm adicionada.
  </acceptance_criteria>
  <done>`TarefaCard` renderiza ícone + descrição truncada + data na cor da urgência da seção, com botão-ícone ghost de 36px que conclui sem abrir o dialog e oferece Desfazer no toast. Nenhum adereço de lead presente no arquivo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| formulário do navegador → Server Action | `TarefaFormDialog` submete `FormData` bruto do DOM; a validação autoritativa está no servidor (plano 12-02), a do cliente é só UX |
| clique no card → Server Action | `TarefaCard` chama `concluirTarefa(id)` com o id vindo do render |
| descrição de tarefa → DOM | Texto arbitrário do usuário renderizado no card, no `title`, no `aria-label` e no corpo do dialog de exclusão |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-14 | Tampering | validação só no cliente | mitigate | O `zodResolver` do `react-hook-form` é conveniência de UX; a barreira real é o `safeParse` das Server Actions (plano 12-02), que roda mesmo se o cliente for contornado. Nenhum campo é gravado sem passar pelo schema no servidor |
| T-12-15 | Cross-Site Scripting | descrição renderizada no card / dialogs | mitigate | Todo texto entra como filho JSX ou como valor de `title`/`aria-label` — o React escapa por construção. PROIBIDO usar `dangerouslySetInnerHTML` em qualquer um dos 3 componentes (verificável por leitura de fonte) |
| T-12-16 | Denial of Service (perda de dado) | exclusão acidental (hard-delete, D-08) | mitigate | `DeleteTarefaDialog` com `showCloseButton={false}` exige clique explícito em "Excluir"; a cópia avisa "Essa ação não pode ser desfeita." O botão fica em `variant="destructive"` e separado do grupo à direita |
| T-12-17 | Tampering | conclusão acidental pelo botão-ícone do card (sem confirmação) | mitigate | Ação reversível por design: o toast oferece "Desfazer" chamando `concluirTarefa(id, { desfazer: true })`; a idempotência do lado servidor garante que o carimbo original não é sobrescrito |
| T-12-18 | Information Disclosure | mensagens de erro na UI | mitigate | Toasts com cópia fixa em português definida no 12-UI-SPEC.md; nenhum `err.message` ou stack é exibido |
| T-12-SC | Tampering | supply chain (registry shadcn / npm) | mitigate | Nenhum bloco novo do registry e nenhuma dependência npm entram nesta fase — o `checkbox` foi deliberadamente substituído por `Button variant="ghost"`. Gate: `git status --porcelain package.json package-lock.json src/components/ui` deve sair vazio ao fim de cada task |
</threat_model>

<verification>
Rodar em SEQUÊNCIA (host de 4GB RAM, dev server parado):

1. `npx tsc --noEmit` → exit 0
2. `npx eslint src/components/tarefa-form-dialog.tsx src/components/delete-tarefa-dialog.tsx src/components/tarefa-card.tsx` → exit 0
3. Asserções de fonte das acceptance criteria (cópia verbatim, ausência de adereços de lead, ausência de `dangerouslySetInnerHTML`)
4. `git status --porcelain package.json package-lock.json src/components/ui` → saída vazia
</verification>

<success_criteria>
- Os 3 componentes existem, compilam e passam no lint escopado
- Toda a cópia visível bate verbatim com o 12-UI-SPEC.md §Copywriting Contract
- O card de tarefa não contém nenhuma referência a `EtapaBadge`, WhatsApp, sub-nicho ou sugestão de sequência (D-03), e usa o mesmo container do card de lead
- O botão de concluir está dentro de um wrapper com `stopPropagation` (D-07)
- O toast de conclusão tem ação "Desfazer" ligada a `concluirTarefa(id, { desfazer: true })`
- Exclusão passa por dialog não-dispensável antes do hard-delete (D-08)
- Nenhuma dependência npm nova, nenhum bloco novo do registry shadcn, nenhum arquivo novo em `src/components/ui/`
</success_criteria>

<output>
Criar `.planning/phases/12-agenda-tarefas-soltas/12-03-SUMMARY.md` ao terminar, registrando: a técnica escolhida para o estado hover/foco do ícone de concluir, se a guarda `DiscardChangesDialog` foi mantida ou omitida, e quaisquer `eslint-disable` documentados.
</output>
