---
phase: 12-agenda-tarefas-soltas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/types/index.ts
  - src/lib/validations.ts
  - scripts/migrate-tarefas.cjs
  - scripts/guard-no-hard-delete.cjs
  - scripts/verify-schema.cjs
  - package.json
autonomous: true
requirements: [TAREFA-01]

must_haves:
  truths:
    - "A tabela `tarefas` existe fisicamente em `data/crm.db` com as 6 colunas exatas (id, descricao, data, concluida_em, created_at, updated_at)"
    - "`concluida_em` nasce NULL (pendente) e não tem DEFAULT físico — D-01"
    - "`npm run verify:schema` falha se a tabela `tarefas` ou seus índices sumirem do banco real"
    - "`npm run guard:no-hard-delete` continua exit 0 e passa a permitir `DELETE FROM tarefas` em `src/actions/tarefa-actions.ts` (D-08), sem afrouxar leads/subnichos/interacoes/motivos_perda"
    - "`tarefaSchema` rejeita descrição vazia com a mensagem verbatim do 12-UI-SPEC.md"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Declaração Drizzle da tabela `tarefas` (sem FK, sem deletedAt)"
      contains: "export const tarefas"
    - path: "scripts/migrate-tarefas.cjs"
      provides: "Migração manual idempotente via better-sqlite3 (backup + wal_checkpoint + CREATE TABLE guardado + verificação)"
      min_lines: 60
    - path: "src/types/index.ts"
      provides: "Tipos `Tarefa` / `NewTarefa`"
      contains: "export type Tarefa"
    - path: "src/lib/validations.ts"
      provides: "`tarefaSchema` e `tarefaUpdateSchema`"
      contains: "export const tarefaSchema"
    - path: "scripts/guard-no-hard-delete.cjs"
      provides: "ALLOWLIST com a exceção documentada de `tarefas` (D-08)"
      contains: "tarefa-actions.ts"
    - path: "scripts/verify-schema.cjs"
      provides: "Gate de conjunto estrito de colunas de `tarefas`"
      contains: "tarefas"
  key_links:
    - from: "src/types/index.ts"
      to: "src/db/schema.ts"
      via: "InferSelectModel<typeof tarefas>"
      pattern: "InferSelectModel<typeof tarefas>"
    - from: "scripts/migrate-tarefas.cjs"
      to: "data/crm.db"
      via: "better-sqlite3 CREATE TABLE guardado por sqlite_master"
      pattern: "sqlite_master"
    - from: "scripts/verify-schema.cjs"
      to: "tabela tarefas no banco real"
      via: "PRAGMA table_info(tarefas)"
      pattern: "PRAGMA table_info\\(tarefas\\)"
---

<objective>
Criar a fundação de dados da agenda de tarefas soltas: a tabela `tarefas` (totalmente desacoplada de `leads` — sem FK, sem `deletedAt`), sua migração manual `[BLOCKING]`, os tipos TypeScript, o schema Zod de validação e os dois gates automáticos (`verify:schema` e `guard:no-hard-delete`) que passam a cobrir/permitir a nova tabela.

Propósito: TAREFA-01 exige persistir uma tarefa com data + descrição sem vínculo a lead. Nada da UI da fase pode ser construído antes de a tabela existir de fato no banco real — `tsc` e `next build` passam usando só os tipos derivados de `schema.ts`, criando um falso-positivo se a migração não rodar.

Saída: tabela `tarefas` viva em `data/crm.db`, tipos `Tarefa`/`NewTarefa`, `tarefaSchema`/`tarefaUpdateSchema`, e os gates de regressão verdes.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-agenda-tarefas-soltas/12-CONTEXT.md
@.planning/phases/12-agenda-tarefas-soltas/12-PATTERNS.md

@src/db/schema.ts
@scripts/migrate-motivos-perda.cjs
@scripts/guard-no-hard-delete.cjs
@scripts/verify-schema.cjs
</context>

<interfaces>
<!-- Contratos que este plano CRIA e que os planos 12-02/12-03/12-04 consomem. -->
<!-- O executor NÃO precisa explorar o codebase: estes são os shapes finais. -->

Forma-alvo da tabela (`src/db/schema.ts`):
- `tarefas` / nome físico `"tarefas"`
- `id`: integer PK autoIncrement
- `descricao`: text("descricao").notNull() — serve de título (D-06)
- `data`: integer("data", { mode: "timestamp" }).notNull() — só o dia
- `concluidaEm`: integer("concluida_em", { mode: "timestamp" }) — NULLABLE, SEM default; NULL = pendente (D-01)
- `createdAt`: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
- `updatedAt`: integer("updated_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
- Índices: `tarefas_concluida_em_idx` em `concluidaEm`, `tarefas_data_idx` em `data`
- SEM `deletedAt` (D-08 = hard-delete), SEM FK, SEM `uniqueIndex`

Tipos (`src/types/index.ts`):
- `export type Tarefa = InferSelectModel<typeof tarefas>`
- `export type NewTarefa = InferInsertModel<typeof tarefas>`

Schemas Zod (`src/lib/validations.ts`):
- `tarefaSchema` — objeto com `descricao` (string trim min 1) e `data` (coerce date)
- `tarefaUpdateSchema` — `tarefaSchema.extend({ id })` com id inteiro positivo coercido
- `export type TarefaFormValues = z.input<typeof tarefaSchema>`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Declarar a tabela `tarefas` no schema, os tipos e o `tarefaSchema`</name>
  <files>src/db/schema.ts, src/types/index.ts, src/lib/validations.ts</files>
  <read_first>
    - `src/db/schema.ts` (arquivo a modificar) — molde de declaração: `motivosPerda` linhas 49-61 (esqueleto id/nome/índices), `interacoes` linhas 119-136 (par createdAt/updatedAt + doc-comment), `templates.updatedAt` linha 27 (updatedAt notNull com default `unixepoch()`), `leads.stageChangedAt` linha 86 (idioma "timestamp nullable, sem default")
    - `src/types/index.ts` (arquivo a modificar) — linhas 1-17, molde das linhas de `Lead`/`MotivoPerda`
    - `src/lib/validations.ts` (arquivo a modificar) — `subnichoSchema` linha 6, `motivoPerdaSchema` linha 14, `leadBaseSchema.followUpDate` linha 62 (`z.coerce.date()`), `interacaoManualUpdateSchema` linha 170 (padrão `.extend({ id })`), `configuracoesServerSchema` linha 213 (último export do arquivo — âncora de posição)
    - `.planning/phases/12-agenda-tarefas-soltas/12-PATTERNS.md` §`src/db/schema.ts`, §`src/lib/validations.ts`, §`src/types/index.ts`
    - `.planning/phases/12-agenda-tarefas-soltas/12-UI-SPEC.md` §Copywriting Contract (mensagens de erro verbatim)
  </read_first>
  <action>
    Em `src/db/schema.ts`, declarar `export const tarefas = sqliteTable("tarefas", {...}, (table) => [...])` imediatamente APÓS a declaração de `interacoes` e ANTES de `configuracoes` (afinidade de domínio; não há FK, então a posição é livre). Colunas exatamente conforme o bloco `<interfaces>` acima: `id`, `descricao`, `data`, `concluidaEm`, `createdAt`, `updatedAt`. Índices `index("tarefas_concluida_em_idx").on(table.concluidaEm)` e `index("tarefas_data_idx").on(table.data)`. NÃO adicionar `deletedAt`, NÃO adicionar `uniqueIndex`, NÃO adicionar `references(...)`. Nenhum import novo é necessário (linhas 1-2 já trazem `sql`, `sqliteTable`, `integer`, `text`, `index`).

    Escrever um doc-comment JSDoc acima da declaração, no mesmo tom dos doc-comments de `motivosPerda`/`interacoes`, dizendo: (a) tabela desacoplada de `leads` por design — TAREFA-01 pede tarefa sem vínculo; (b) `concluidaEm` NULL = pendente, é o filtro que alimenta o dashboard (D-01/D-02); (c) exclusão é hard-delete por D-08, por isso NÃO existe `deletedAt` e `tarefas` é a exceção documentada em `scripts/guard-no-hard-delete.cjs`; (d) a migração vive em `scripts/migrate-tarefas.cjs`, NUNCA `drizzle-kit push`/`generate` (snapshot divergente do banco real desde a Fase 4).

    Em `src/types/index.ts`, adicionar `tarefas` ao import de `@/db/schema` (linha 2) e exportar `Tarefa` / `NewTarefa` via `InferSelectModel` / `InferInsertModel`, no mesmo formato das linhas 4-17.

    Em `src/lib/validations.ts`, ao final do arquivo (após `configuracoesServerSchema`), adicionar `tarefaSchema` com `descricao: z.string().trim().min(1, "Descreva a tarefa.")` e `data: z.coerce.date({ error: "Escolha uma data." })` — mensagens VERBATIM do 12-UI-SPEC.md §Copywriting Contract. Usar `z.coerce.date()` (não `z.date()`) porque o valor chega como string ISO do `<input type="hidden">`, mesmo idioma de `leadBaseSchema.followUpDate`. Adicionar também `export const tarefaUpdateSchema = tarefaSchema.extend({ id: z.coerce.number().int().positive() })` — `.extend` sobre o schema base, NUNCA cópia paralela de campos (regra DRY já documentada em `interacaoManualSchema`). Exportar `export type TarefaFormValues = z.input<typeof tarefaSchema>`. Doc-comment curto citando D-06 (só 2 campos: descrição + data, sem hora, sem título separado).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sai com exit 0.
    - `node -e "const s=require('fs').readFileSync('src/db/schema.ts','utf8'); const m=s.match(/export const tarefas[\s\S]*?\n\);/); if(!m) process.exit(1); for(const c of ['\"tarefas\"','descricao','\"data\"','concluida_em','created_at','updated_at','tarefas_concluida_em_idx','tarefas_data_idx']) if(!m[0].includes(c)) { console.error('faltando',c); process.exit(1); } if(m[0].includes('deletedAt')||m[0].includes('references(')) { console.error('tarefas nao pode ter deletedAt nem FK'); process.exit(1); } console.log('OK')"` imprime `OK` e sai 0.
    - `node -e "const v=require('fs').readFileSync('src/lib/validations.ts','utf8'); for(const t of ['export const tarefaSchema','export const tarefaUpdateSchema','Descreva a tarefa.','Escolha uma data.','z.coerce.date','TarefaFormValues']) if(!v.includes(t)) { console.error('faltando',t); process.exit(1); } console.log('OK')"` imprime `OK` e sai 0.
    - `src/types/index.ts` exporta `Tarefa` e `NewTarefa` (verificável por `tsc` — qualquer import de `@/types` com `Tarefa` compila).
  </acceptance_criteria>
  <done>Tabela `tarefas` declarada em Drizzle sem FK e sem `deletedAt`, tipos `Tarefa`/`NewTarefa` exportados, `tarefaSchema`/`tarefaUpdateSchema` disponíveis com as mensagens de erro verbatim do UI-SPEC. `tsc` limpo.</done>
</task>

<task type="auto">
  <name>Task 2 [BLOCKING]: Criar e RODAR `scripts/migrate-tarefas.cjs` contra o banco real</name>
  <files>scripts/migrate-tarefas.cjs, package.json</files>
  <read_first>
    - `scripts/migrate-motivos-perda.cjs` (molde exato — copiar bloco a bloco: cabeçalho `"use strict"` + requires + `DB_PATH` L17-23, helper `fail` L35-38, backup com `wal_checkpoint(TRUNCATE)` L40-52, contagem de referência L56-57, CREATE TABLE guardado por `sqlite_master` L59-75, verificação pós-migração L109-128)
    - `scripts/migrate-sequencia-followup.cjs` L1-12 (comentário-âncora da regra dura: nunca `drizzle-kit`)
    - `src/db/schema.ts` (recém-modificado na Task 1 — a DDL crua deve espelhar exatamente as colunas declaradas lá)
    - `package.json` L5-21 (bloco `scripts`, molde das entradas `verify:*` / `test:*`)
    - `.planning/phases/12-agenda-tarefas-soltas/12-PATTERNS.md` §`scripts/migrate-tarefas.cjs`
  </read_first>
  <action>
    REGRA DURA DO PROJETO: NUNCA usar `drizzle-kit push` nem `drizzle-kit generate`. O snapshot do drizzle-kit está divergente do banco real desde a Fase 4 e já causou dois incidentes destrutivos (Fases 06-01 e 07-01). Toda DDL é escrita à mão e aplicada via `better-sqlite3`.

    Criar `scripts/migrate-tarefas.cjs` copiando a estrutura de `scripts/migrate-motivos-perda.cjs`, nesta ordem: (1) doc-comment de cabeçalho explicando por que a migração é manual e citando D-08 (`tarefas` nasce sem `deletedAt`); (2) `"use strict"`, requires de `node:path`/`node:fs`/`better-sqlite3`, `DB_PATH` lendo `process.env.DB_FILE_NAME` com fallback para `data/crm.db`; (3) helper `fail(message)` que imprime com prefixo `[migrate-tarefas]` e faz `process.exit(1)`; (4) backup ANTES de qualquer escrita — abrir o banco com `fileMustExist: true`, `pragma("wal_checkpoint(TRUNCATE)")`, fechar, e `fs.copyFileSync` para `${DB_PATH}.backup-<ISO timestamp sanitizado>`; (5) contagem de referência `SELECT count(*) AS c FROM leads` guardada em `beforeLeads` como testemunha de "não mexi em nada"; (6) CREATE TABLE idempotente guardado por consulta a `sqlite_master` (`type='table' AND name='tarefas'`) — se ausente, executar o `CREATE TABLE tarefas` com as colunas `id INTEGER PRIMARY KEY AUTOINCREMENT`, `descricao TEXT NOT NULL`, `data INTEGER NOT NULL`, `concluida_em INTEGER` (sem NOT NULL, sem DEFAULT), `created_at INTEGER NOT NULL DEFAULT (unixepoch())`, `updated_at INTEGER NOT NULL DEFAULT (unixepoch())`, seguido de `CREATE INDEX tarefas_concluida_em_idx ON tarefas (concluida_em)` e `CREATE INDEX tarefas_data_idx ON tarefas (data)`; se já existir, logar que está pulando por idempotência; (7) verificação pós-migração: reconferir `SELECT count(*) FROM leads` contra `beforeLeads` e falhar se mudou, confirmar a tabela em `sqlite_master`, e conferir via `PRAGMA table_info(tarefas)` que as 6 colunas esperadas estão presentes; (8) `db.close()` e `process.exit(0)`.

    NÃO copiar os blocos de SEED nem de `ALTER TABLE` de `migrate-motivos-perda.cjs` — `tarefas` nasce vazia e sem FK.

    Em `package.json`, adicionar a entrada `"migrate:tarefas": "node scripts/migrate-tarefas.cjs"` no bloco `scripts`, logo antes das entradas `verify:*`.

    DEPOIS de escrever o script, EXECUTÁ-LO de fato contra `data/crm.db` com `npm run migrate:tarefas`. Esta task é `[BLOCKING]`: nenhum plano posterior pode ser executado sem a tabela viva no banco, porque `tsc`/`next build` derivam tipos de `schema.ts` e passariam mesmo com o banco desatualizado (falso-positivo). Confirmar no log a linha de criação e guardar o caminho do arquivo de backup gerado para registrar no SUMMARY.
  </action>
  <verify>
    <automated>npm run migrate:tarefas</automated>
  </verify>
  <acceptance_criteria>
    - `npm run migrate:tarefas` sai com exit 0 e imprime a linha de criação da tabela (primeira execução) ou a linha de idempotência (execuções seguintes).
    - Rodar `npm run migrate:tarefas` uma SEGUNDA vez também sai exit 0 e imprime a mensagem de "já existe — pulando CREATE" (prova de idempotência).
    - `node -e "const D=require('better-sqlite3'); const db=new D('data/crm.db',{fileMustExist:true}); const cols=db.prepare('PRAGMA table_info(tarefas)').all().map(c=>c.name); const want=['id','descricao','data','concluida_em','created_at','updated_at']; const extra=cols.filter(c=>!want.includes(c)); const miss=want.filter(c=>!cols.includes(c)); if(miss.length||extra.length){console.error('faltando',miss,'extras',extra);process.exit(1);} const idx=db.prepare(\"SELECT name FROM sqlite_master WHERE type='index' AND tbl_name='tarefas'\").all().map(r=>r.name); for(const i of ['tarefas_concluida_em_idx','tarefas_data_idx']) if(!idx.includes(i)){console.error('indice ausente',i);process.exit(1);} console.log('OK');"` imprime `OK` e sai 0.
    - Existe pelo menos um arquivo `data/crm.db.backup-*` novo em disco após a primeira execução.
    - A contagem de linhas de `leads` no banco é idêntica antes e depois (o próprio script falha se mudou).
    - `package.json` contém a chave `migrate:tarefas`.
  </acceptance_criteria>
  <done>Tabela `tarefas` existe fisicamente em `data/crm.db` com as 6 colunas e 2 índices, backup datado gerado, script idempotente e registrado em `package.json`. Nenhuma linha de `leads` foi tocada.</done>
</task>

<task type="auto">
  <name>Task 3: Estender `guard-no-hard-delete.cjs` (exceção D-08) e `verify-schema.cjs` (gate de `tarefas`)</name>
  <files>scripts/guard-no-hard-delete.cjs, scripts/verify-schema.cjs</files>
  <read_first>
    - `scripts/guard-no-hard-delete.cjs` (arquivo a modificar) — doc-comment L4-31, `ALLOWLIST` L47, `CODE_PATTERNS` L55-60, `CODE_SQL_PATTERNS` L67-76, mensagem de sucesso L159-161
    - `scripts/verify-schema.cjs` (arquivo a modificar) — `requiredTables` L29, `requiredIndexes` L30-35, bloco de conjunto ESTRITO de colunas de `interacoes` L47-75 (molde), bloco de PRESENÇA de `leads` L83-91 (contraste), linha de sucesso L101-104
    - `.planning/phases/12-agenda-tarefas-soltas/12-CONTEXT.md` §Decisions D-08
    - `.planning/phases/12-agenda-tarefas-soltas/12-PATTERNS.md` §`scripts/guard-no-hard-delete.cjs`, §`scripts/verify-schema.cjs`
  </read_first>
  <action>
    Em `scripts/guard-no-hard-delete.cjs`: adicionar `path.join("src", "actions", "tarefa-actions.ts")` à constante `ALLOWLIST`, precedido de um comentário de bloco explícito citando D-08 e dizendo que `tarefas` é descartável por natureza (lembrete cumprido ou cancelado), que o hard-delete é intencional — sem `deletedAt`, sem Lixeira — e que esta é a ÚNICA tabela do projeto onde `DELETE FROM` / `.delete()` é permitido. Atenção: `tarefa-actions.ts` ainda não existe em disco neste plano; a entrada na ALLOWLIST é um caminho relativo e nunca falha por ausência de arquivo.

    NÃO adicionar nenhum padrão de `tarefas` a `CODE_PATTERNS` nem a `CODE_SQL_PATTERNS` — a ausência é deliberada (esta é a primeira PERMISSÃO do guard, o oposto das extensões de bloqueio das Fases 9 e 11).

    Atualizar o doc-comment do topo do arquivo e a mensagem final de sucesso para explicitar que `tarefas` está FORA do escopo protegido por decisão D-08 (o texto de sucesso hoje lista "leads, subnichos, interacoes, motivos_perda" — mantê-lo e acrescentar a nota de exceção).

    Em `scripts/verify-schema.cjs`: adicionar `"tarefas"` a `requiredTables`; adicionar `"tarefas_concluida_em_idx"` e `"tarefas_data_idx"` a `requiredIndexes`; criar um bloco novo de conjunto ESTRITO de colunas para `tarefas` (molde do bloco de `interacoes` L47-75, não o de `leads`) com a constante das 6 colunas esperadas `id`, `descricao`, `data`, `concluida_em`, `created_at`, `updated_at`, falhando tanto por coluna faltando quanto por coluna extra — conjunto estrito é seguro aqui porque a tabela é nova e não acumula colunas por fase. Atualizar a linha de sucesso final para mencionar `tarefas`.

    Rodar os dois gates em SEQUÊNCIA (host de 4GB — nunca em paralelo).
  </action>
  <verify>
    <automated>npm run verify:schema && npm run guard:no-hard-delete</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify:schema` sai exit 0 e a saída menciona `tarefas`.
    - `npm run guard:no-hard-delete` sai exit 0.
    - Teste de mutação do gate de schema (sem tocar o banco real): `node -e "const{execFileSync}=require('child_process');const D=require('better-sqlite3');const os=require('os'),p=require('path');const f=p.join(os.tmpdir(),'vs-'+Date.now()+'.db');const db=new D(f);db.exec(\"CREATE TABLE leads(id INTEGER PRIMARY KEY, sequencia_posicao INTEGER, motivo_perda_id INTEGER); CREATE TABLE subnichos(id INTEGER PRIMARY KEY); CREATE TABLE interacoes(id INTEGER PRIMARY KEY, lead_id INTEGER, tipo TEXT, texto TEXT, created_at INTEGER, updated_at INTEGER, deleted_at INTEGER); CREATE TABLE motivos_perda(id INTEGER PRIMARY KEY); CREATE UNIQUE INDEX subnicho_nome_unique_idx ON subnichos(id); CREATE INDEX interacoes_lead_id_idx ON interacoes(lead_id); CREATE INDEX interacoes_deleted_at_idx ON interacoes(deleted_at); CREATE UNIQUE INDEX motivo_perda_nome_unique_idx ON motivos_perda(id);\");db.close();let code=0;try{execFileSync(process.execPath,['scripts/verify-schema.cjs'],{env:{...process.env,DB_FILE_NAME:f},stdio:'pipe'});}catch(e){code=e.status;}require('fs').unlinkSync(f);if(code===0){console.error('MUTACAO NAO DETECTADA: verify-schema passou sem a tabela tarefas');process.exit(1);}console.log('OK: gate detecta ausencia de tarefas');"` imprime `OK: gate detecta ausencia de tarefas`.
    - `scripts/guard-no-hard-delete.cjs` contém a string `tarefa-actions.ts` dentro do array `ALLOWLIST` e NÃO contém nenhum literal `tarefas` dentro de `CODE_PATTERNS` ou `CODE_SQL_PATTERNS`: `node -e "const s=require('fs').readFileSync('scripts/guard-no-hard-delete.cjs','utf8');const al=s.match(/const ALLOWLIST = \[[\s\S]*?\];/)[0];if(!al.includes('tarefa-actions.ts'))process.exit(1);const cp=s.match(/const CODE_PATTERNS = \[[\s\S]*?\];/)[0]+s.match(/const CODE_SQL_PATTERNS = \[[\s\S]*?\];/)[0];if(/tarefas/.test(cp)){console.error('tarefas nao pode estar nos padroes de bloqueio');process.exit(1);}console.log('OK');"` imprime `OK`.
  </acceptance_criteria>
  <done>`verify:schema` passa a proteger a tabela `tarefas` com conjunto estrito de colunas (provado por teste de mutação), e `guard:no-hard-delete` documenta e permite o hard-delete de `tarefas` exclusivamente em `src/actions/tarefa-actions.ts`, sem afrouxar nenhuma outra tabela.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script `.cjs` → `data/crm.db` | DDL manual escreve direto no banco de produção local; nenhuma camada de ORM medeia |
| schema Drizzle → banco físico | Divergência silenciosa entre `schema.ts` e o banco real é o risco histórico documentado do projeto (Fases 4/6/7) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | `scripts/migrate-tarefas.cjs` (DDL contra banco de produção local) | mitigate | Backup com `wal_checkpoint(TRUNCATE)` + `copyFileSync` ANTES de qualquer escrita; contagem de referência de `leads` verificada antes/depois; `CREATE TABLE` guardado por `sqlite_master` (idempotente, nunca `DROP`/`ALTER` de tabela existente) |
| T-12-02 | Tampering | `drizzle-kit push` acidental | mitigate | Proibição explícita em doc-comment no schema e no script de migração; nenhum comando `drizzle-kit` aparece em `package.json` |
| T-12-03 | Denial of Service (perda de dado) | `ALLOWLIST` do `guard-no-hard-delete.cjs` | mitigate | Exceção escopada a UM caminho de arquivo (`src/actions/tarefa-actions.ts`), nunca a um diretório nem a um padrão; `CODE_PATTERNS`/`CODE_SQL_PATTERNS` permanecem intocados, então `DELETE FROM leads` em qualquer arquivo continua bloqueado — inclusive dentro do próprio `tarefa-actions.ts` |
| T-12-04 | Tampering | `tarefaSchema` (validação de entrada) | mitigate | `z.string().trim().min(1)` e `z.coerce.date()` — nenhum campo aceita passthrough; o schema é o contrato único consumido pelas Server Actions do plano 12-02 |
| T-12-05 | Repudiation | ausência de auditoria de quem criou/alterou tarefa | accept | Aplicação solo de usuário único por design (`PROJECT.md`); não há identidade a registrar |
| T-12-06 | Elevation of Privilege | ausência de authz/tenant na tabela | accept | Sem multi-usuário por design; a superfície é `localhost` sem exposição pública (ver `STATE.md` §Direção de infraestrutura — gate de senha no middleware só quando o CRM for para o VPS) |
| T-12-SC | Tampering | supply chain (npm/pip/cargo) | accept | Nenhuma dependência npm nova nesta fase (confirmado no `12-UI-SPEC.md` §Design System e §Registry Safety); nenhum comando de instalação é executado, então o Package Legitimacy Gate não se aplica |
</threat_model>

<verification>
Rodar em SEQUÊNCIA (host de 4GB RAM — nunca processos pesados em paralelo, e fechar o `npm run dev` antes):

1. `npx tsc --noEmit` → exit 0
2. `npm run migrate:tarefas` → exit 0 (rodar 2x para provar idempotência)
3. `npm run verify:schema` → exit 0, menciona `tarefas`
4. `npm run guard:no-hard-delete` → exit 0
5. Teste de mutação do `verify-schema` (ver acceptance criteria da Task 3) → detecta a ausência de `tarefas`
</verification>

<success_criteria>
- Tabela `tarefas` existe em `data/crm.db` com exatamente 6 colunas (`id`, `descricao`, `data`, `concluida_em`, `created_at`, `updated_at`) e 2 índices, verificável por `PRAGMA table_info`
- `concluida_em` é nullable e sem DEFAULT (NULL = pendente, D-01)
- Nenhuma coluna `deleted_at` e nenhuma FK na tabela `tarefas` (D-08 + desacoplamento)
- Backup datado de `data/crm.db` gerado antes da DDL
- Contagem de linhas de `leads` inalterada
- `Tarefa`/`NewTarefa` exportados de `@/types`; `tarefaSchema`/`tarefaUpdateSchema` exportados de `@/lib/validations` com as mensagens verbatim do 12-UI-SPEC.md
- `npm run verify:schema` e `npm run guard:no-hard-delete` verdes, e o gate de schema comprovadamente detecta a ausência da tabela (teste de mutação)
</success_criteria>

<output>
Criar `.planning/phases/12-agenda-tarefas-soltas/12-01-SUMMARY.md` ao terminar, registrando: caminho do arquivo de backup gerado, saída da 1ª e 2ª execução de `migrate:tarefas`, e a confirmação de que nenhum comando `drizzle-kit` foi usado.
</output>
