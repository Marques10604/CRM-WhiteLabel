---
phase: 01-lead-sub-nicho-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - components.json
  - .gitignore
  - drizzle.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/components/app-sidebar.tsx
  - src/db/schema.ts
  - src/db/client.ts
  - src/db/migrations/
  - src/types/index.ts
  - src/lib/validations.ts
  - src/actions/subnicho-actions.ts
  - src/components/subnicho-manager.tsx
  - src/app/subnichos/page.tsx
autonomous: false
requirements: [LEAD-02]
user_setup: []

must_haves:
  truths:
    - "O admin consegue rodar o app localmente com `npm run dev` e abrir o navegador numa UI com sidebar fixa (Leads / Sub-nichos / Lixeira)"
    - "O admin consegue criar um sub-nicho na tela Sub-nichos e vê-lo aparecer na lista"
    - "O admin consegue renomear um sub-nicho existente inline"
    - "Criar um sub-nicho com nome duplicado exato (case-insensitive, ignorando espaços de borda) é bloqueado com erro inline; near-duplicates de grafia diferente (ex.: 'nutri' vs 'nutricionista') NÃO são bloqueados (SPEC req 3 — só duplicata exata normalizada)"
    - "Os dados de sub-nicho persistem no arquivo SQLite entre reinícios do servidor"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Tabelas leads e subnichos com FK, enum stage (etapa única, sem split Fechado/Perdido — D-10), soft-delete e índice único case-insensitive"
      contains: "export const leads"
    - path: "src/db/client.ts"
      provides: "Instância Drizzle sobre better-sqlite3 apontando para ./data/crm.db"
      exports: ["db"]
    - path: "src/actions/subnicho-actions.ts"
      provides: "Server Actions createSubnicho e renameSubnicho com dedupe case-insensitive"
      exports: ["createSubnicho", "renameSubnicho"]
    - path: "src/app/subnichos/page.tsx"
      provides: "Tela de gestão de sub-nichos dedicada, separada do combobox de criação inline (Server Component + SubnichoManager) — D-15"
    - path: "src/components/subnicho-manager.tsx"
      provides: "Edição inline via ícone de lápis e adição via '+ Adicionar', sem modal separado — D-16"
    - path: "src/components/app-sidebar.tsx"
      provides: "Sidebar fixa de navegação (D-18)"
    - path: "drizzle.config.ts"
      provides: "Config drizzle-kit (dialect sqlite, out src/db/migrations)"
      contains: "dialect"
  key_links:
    - from: "src/components/subnicho-manager.tsx"
      to: "src/actions/subnicho-actions.ts"
      via: "chamada de Server Action no submit do formulário"
      pattern: "createSubnicho|renameSubnicho"
    - from: "src/actions/subnicho-actions.ts"
      to: "src/db/schema.ts (subnichos)"
      via: "insert/update via Drizzle"
      pattern: "db\\.(insert|update)\\(subnichos\\)"
    - from: "src/app/subnichos/page.tsx"
      to: "src/db/client.ts"
      via: "query de listagem no Server Component"
      pattern: "db\\.select\\(\\)"
---

## Phase Goal

**As a** admin do CRM, **I want to** gerenciar leads e a taxonomia extensível de sub-nichos manualmente numa UI de navegador com banco de dados real, **so that** eu substituo a planilha do Google Sheets como camada de dados base do meu funil de vendas.

> História derivada da linha `**Goal:**` do ROADMAP.md da Fase 1 + Core Value do `PROJECT.md`/`CLAUDE.md` — não é escopo inventado.

<objective>
Este plano é o **Walking Skeleton** da Fase 1: faz o scaffold completo do projeto greenfield (Next.js 16 App Router + TypeScript 5.9.3 + Drizzle + better-sqlite3 + Tailwind v4 + shadcn/ui) e entrega o primeiro corte vertical fim-a-fim — o admin cria e renomeia sub-nichos numa tela real, com escrita e leitura reais no SQLite. Cobre LEAD-02.

Purpose: Provar que o stack inteiro funciona conectado (UI -> Server Action -> Zod -> Drizzle -> SQLite -> revalidatePath -> re-render -> toast) antes de expandir para leads. Sub-nicho vem primeiro porque `leads` referencia `subnichos` por FK.
Output: App executável (`npm run dev`), banco `./data/crm.db` com as tabelas `leads` e `subnichos` criadas, sidebar de navegação, e a tela Sub-nichos totalmente funcional.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-lead-sub-nicho-foundation/01-SPEC.md
@.planning/phases/01-lead-sub-nicho-foundation/01-RESEARCH.md
@.planning/phases/01-lead-sub-nicho-foundation/01-PATTERNS.md
@.planning/phases/01-lead-sub-nicho-foundation/01-UI-SPEC.md
@.planning/phases/01-lead-sub-nicho-foundation/SKELETON.md
@CLAUDE.md

<interfaces>
<!-- Contratos-chave que este plano CRIA. Executores dos planos 02/03/04 consomem estes exports. -->

De src/db/schema.ts (a criar — trecho literal em 01-PATTERNS.md linhas 70-114):
- export const subnichos: colunas id (integer PK autoincrement), nome (text notNull), createdAt (integer timestamp). uniqueIndex "subnicho_nome_unique_idx" sobre lower(trim(nome)).
- export const leads: colunas id, nome, telefone, canal (enum instagram/whatsapp), origem, valorEstimado (integer, centavos), notas, followUpDate (integer timestamp), subnichoId (FK subnichos.id, onDelete restrict), stage (enum novo/contatado/negociacao/fechado_perdido, default novo), deletedAt (integer timestamp nullable), createdAt, updatedAt. Índices em deletedAt/followUpDate/stage/subnichoId.

De src/db/client.ts:
- export const db (drizzle sobre better-sqlite3, schema completo).

De src/actions/subnicho-actions.ts:
- export async function createSubnicho(prevState, formData) retorna { success } ou { errors: { nome: string[] } }
- export async function renameSubnicho(prevState, formData) mesmo formato (formData inclui id + nome).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verificar legitimidade de pacotes não auditados (sonner, date-fns)</name>
  <files>package.json (gate pré-instalação — nenhum arquivo é modificado nesta task)</files>
  <read_first>
    - .planning/phases/01-lead-sub-nicho-foundation/01-RESEARCH.md (seção "## Package Legitimacy Audit", linhas 163-190)
    - CLAUDE.md (tabela "Supporting Libraries" — versões travadas de sonner e date-fns)
  </read_first>
  <action>Gate humano bloqueante de supply-chain ANTES de qualquer `npm install`. O executor NÃO instala nada nesta task — apenas apresenta ao humano os 4 itens de checagem FINITA abaixo para `sonner` e `date-fns` (os dois pacotes que o slopcheck do pesquisador não conseguiu auditar por timeout) e aguarda o humano digitar "approved". Este gate NÃO é uma auditoria aberta/subjetiva de supply-chain: "aprovado" significa exatamente que os 4 itens do `<acceptance_criteria>` conferem — nada além disso é exigido do admin, e nenhum critério é aberto ("parece confiável" NÃO é um critério). Todos os outros pacotes centrais (next, react, react-dom, typescript, drizzle-orm, drizzle-kit, better-sqlite3, zod, react-hook-form, @hookform/resolvers, @tanstack/react-table, tailwindcss, shadcn, lucide-react) já estão APROVADOS na auditoria do RESEARCH e não passam por este gate.</action>
  <what-built>
    Nada foi automatizado ainda — este é um gate de segurança de supply-chain ANTES da instalação de dependências (Task 2). O `slopcheck` do pesquisador auditou 14 dos 16 pacotes centrais como OK, mas o scan de `sonner` e `date-fns` foi interrompido por timeout e ficou "não verificado". A verificação abaixo é uma checagem finita de 4 itens objetivos, não uma auditoria aberta.
  </what-built>
  <how-to-verify>
    Confirmar, um a um, os 4 itens objetivos abaixo (todos verificáveis em ~2 minutos):
    1. NOME EXATO (anti-typosquat): o pacote é literalmente `sonner` (NÃO `soner`, `sonnerr`, `sonner-toast`, `@sonner/*`) e literalmente `date-fns` (NÃO `datefns`, `date-fn`, `date_fns`, `date-fns-tz` como se fosse o principal).
    2. REPOSITÓRIO OFICIAL: abrir https://www.npmjs.com/package/sonner e confirmar que "Repository" aponta para github.com/emilkowalski/sonner; abrir https://www.npmjs.com/package/date-fns e confirmar que "Repository" aponta para github.com/date-fns/date-fns.
    3. VERSÃO TRAVADA: a versão que a Task 2 vai pinar no package.json é exatamente `sonner@2.0.7` e `date-fns@4.4.0` (as versões da tabela travada no CLAUDE.md) — NÃO `@latest`.
    4. VOLUME DE DOWNLOADS coerente com pacote estabelecido: sonner na casa dos milhões/semana; date-fns na casa das dezenas de milhões/semana; sem sinal de "publicado uma única vez há poucos dias por autor desconhecido".
    Se os 4 itens conferem -> aprovar. Se QUALQUER item falhar (repositório ausente, nome diferente do esperado, versão não bate, downloads irrisórios) -> NÃO aprovar e reportar qual item falhou.
  </how-to-verify>
  <resume-signal>Digite "approved" (que significa: os 4 itens acima conferem) para liberar a instalação de sonner@2.0.7 e date-fns@4.4.0 na Task 2, ou descreva qual item falhou.</resume-signal>
  <verify>
    <human-check>Os 4 itens objetivos (nome exato, repositório oficial, versão travada `sonner@2.0.7`/`date-fns@4.4.0`, volume de downloads) confirmados em npmjs.com antes de liberar a instalação.</human-check>
  </verify>
  <acceptance_criteria>
    - "approved" só é dado quando os 4 itens conferem: (1) nome exato `sonner`/`date-fns` sem lookalike/typosquat; (2) repositório oficial github.com/emilkowalski/sonner e github.com/date-fns/date-fns; (3) versão a ser pinada = `sonner@2.0.7` e `date-fns@4.4.0` (bate com CLAUDE.md, NÃO `@latest`); (4) volume de downloads de pacote estabelecido
    - "approved" NÃO exige nenhuma checagem subjetiva/aberta além desses 4 itens — o gate não pode travar em critério indefinido
    - Aprovação humana explícita registrada antes de qualquer `npm install sonner` ou `npm install date-fns`
    - Este checkpoint NÃO é auto-aprovável (gate de legitimidade de pacote — `workflow.auto_advance` ignorado)
  </acceptance_criteria>
  <done>Humano digitou "approved" após confirmar os 4 itens objetivos (nome exato, repositório oficial, versão travada, volume de downloads) para sonner e date-fns.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold do projeto Next.js 16 + dependências + shadcn/ui + layout com sidebar</name>
  <files>package.json, tsconfig.json, next.config.ts, components.json, src/app/globals.css, src/app/layout.tsx, src/app/page.tsx, src/app/subnichos/page.tsx, src/app/lixeira/page.tsx, src/components/app-sidebar.tsx, src/components/ui/</files>
  <read_first>
    - .planning/phases/01-lead-sub-nicho-foundation/01-RESEARCH.md (seção "## Standard Stack" -> "Installation", linhas 139-161; Pitfall 2 linhas 438-444; Pitfall 4 linhas 467-473)
    - .planning/phases/01-lead-sub-nicho-foundation/01-UI-SPEC.md (Design System linhas 19-27; Spacing linha 47; Color linhas 72-84; Typography linhas 52-64)
    - .planning/phases/01-lead-sub-nicho-foundation/01-CONTEXT.md (D-14, D-18)
    - CLAUDE.md (seção Technology Stack / Installation)
  </read_first>
  <action>
    Rodar `npx create-next-app@latest . --typescript --tailwind --app --eslint --src-dir --import-alias "@/*"` na raiz do repo (já contém .git/.planning/CLAUDE.md — aceitar prosseguir em diretório não-vazio). Pinar TypeScript exato com `npm install -D typescript@5.9.3` (NÃO usar `typescript@latest`, que é 7.0.2). Instalar a camada de dados: `npm install drizzle-orm@0.45.2 better-sqlite3@12.11.1` e `npm install -D drizzle-kit@0.31.10 @types/better-sqlite3@7.6.13` — confirmar que o install de `better-sqlite3` termina em segundos sem menção a `node-gyp`/`MSBuild` (binário pré-compilado para Node 24; Pitfall 4). Instalar validação/form: `npm install zod@4.4.3 react-hook-form@7.82.0 @hookform/resolvers` (sem pinar versão antiga do resolver — Pitfall 5). Rodar `npx shadcn@latest init` com estilo "New York"; ao terminar, inspecionar `components.json` e confirmar se a lib de primitivos é Base UI (padrão jul/2026) — registrar no SUMMARY qual foi escolhida (Open Question 1). Instalar UI restante: `npm install @tanstack/react-table@8.21.3 lucide-react date-fns@4.4.0` e `npm install sonner@2.0.7` (date-fns@4.4.0/sonner@2.0.7 só após aprovação da Task 1, nas versões exatas travadas no CLAUDE.md). Adicionar componentes: `npx shadcn@latest add table form dialog select combobox calendar badge button input textarea` e `npx shadcn@latest add sonner`. Criar `src/components/app-sidebar.tsx`: sidebar fixa de 240px, fundo `#F4F4F5`, com links `next/link` para `/` ("Leads"), `/subnichos` ("Sub-nichos") e `/lixeira` ("Lixeira"), indicador de link ativo na cor accent `#0D9488` via `usePathname` (D-18). Editar `src/app/layout.tsx` para renderizar `<AppSidebar />` à esquerda + `<main>` do conteúdo, montar o `<Toaster />` do sonner, mantendo a fonte Geist Sans padrão. Deixar `src/app/page.tsx` como placeholder mínimo (título "Leads" com Display 28px/600 — substituído em 01-02). Criar rotas-casca `src/app/subnichos/page.tsx` e `src/app/lixeira/page.tsx` com apenas o título de página. NÃO criar nenhuma rota `app/api/*` (decisão travada: Server Actions).
  </action>
  <verify>
    <automated>cd "C:/Users/Vencedor/Desktop/crm-leads" && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` lista next@16.2.10, react@19.2.7, typescript@5.9.3 (NÃO 7.x), drizzle-orm@0.45.2, drizzle-kit@0.31.10, better-sqlite3@12.11.1, zod@4.4.3, react-hook-form@7.82.0, @tanstack/react-table@8.21.3, lucide-react, sonner@2.0.7, date-fns@4.4.0
    - `components.json` existe; o SUMMARY registra se a lib de primitivos é Base UI ou Radix
    - `src/components/ui/` contém: table, form, dialog, select, combobox, calendar, badge, button, input, textarea, sonner
    - `npx tsc --noEmit` sem erros e `npm run build` conclui com sucesso
    - Não existe diretório `src/app/api/`
    - `src/components/app-sidebar.tsx` contém os três links (`/`, `/subnichos`, `/lixeira`) e usa `usePathname`
  </acceptance_criteria>
  <done>App Next.js 16 scaffolded, dependências nas versões travadas, componentes shadcn gerados, sidebar fixa e layout raiz renderizando; `tsc --noEmit` e `build` passam.</done>
</task>

<task type="auto">
  <name>Task 3: Camada de dados (schema, config, client, tipos) + [BLOCKING] sync de schema no banco</name>
  <files>src/db/schema.ts, src/db/client.ts, drizzle.config.ts, src/db/migrations/, src/types/index.ts, .gitignore, data/</files>
  <read_first>
    - .planning/phases/01-lead-sub-nicho-foundation/01-PATTERNS.md (schema src/db/schema.ts linhas 65-119; drizzle.config.ts linhas 43-61; db/client.ts linhas 123-137)
    - .planning/phases/01-lead-sub-nicho-foundation/01-RESEARCH.md (Pattern 2 linhas 330-384; Pitfall 3 linhas 446-465; Pitfall 4 linhas 467-473)
    - src/db/schema.ts (se já existir de tentativa anterior)
  </read_first>
  <action>
    Criar `src/db/schema.ts` com AMBAS as tabelas exatamente como o analog literal de 01-PATTERNS.md (linhas 70-114): tabela `subnichos` (id, nome, createdAt) com `uniqueIndex("subnicho_nome_unique_idx")` sobre lower(trim(nome)); tabela `leads` com id, nome, telefone, canal (enum ["instagram","whatsapp"]), origem, valorEstimado (integer em centavos, coluna "valor_estimado_centavos"), notas, followUpDate (integer timestamp), subnichoId (FK `.references(() => subnichos.id, { onDelete: "restrict" })`), stage (enum ["novo","contatado","negociacao","fechado_perdido"], default "novo"), deletedAt (integer timestamp nullable), createdAt, updatedAt, mais os índices em deletedAt/followUpDate/stage/subnichoId. Criar `drizzle.config.ts` com a API atual `defineConfig({ out: "./src/db/migrations", schema: "./src/db/schema.ts", dialect: "sqlite", dbCredentials: { url: "./data/crm.db" } })` — NÃO usar `driver: 'better-sqlite'` (Pitfall 3). Criar `src/db/client.ts` como o analog literal (drizzle sobre `new Database(process.env.DB_FILE_NAME ?? "./data/crm.db")`, `pragma("journal_mode = WAL")`, `export const db`). Criar `src/types/index.ts` derivando tipos via `InferSelectModel`/`InferInsertModel` a partir de `leads` e `subnichos` (`Lead`, `NewLead`, `Subnicho`, `NewSubnicho`). Adicionar `data/` e `./data/*.db*` ao `.gitignore` (só `src/db/migrations/` é versionado). Criar a pasta `data/` se necessário. [BLOCKING] Rodar o sync de schema: `npx drizzle-kit generate` (gera a migração SQL versionada em `src/db/migrations/`) seguido de `npx drizzle-kit migrate` (aplica ao `./data/crm.db`). APÓS o `generate`, ABRIR o arquivo `.sql` gerado e CONFIRMAR que ele contém o índice de expressão `CREATE UNIQUE INDEX ... ON \`subnichos\` (lower(trim(\`nome\`)))`; se o drizzle-kit NÃO emitir a expressão (limitação conhecida de índices de expressão em SQLite com drizzle-kit), ADICIONAR manualmente a linha `CREATE UNIQUE INDEX \`subnicho_nome_unique_idx\` ON \`subnichos\` (lower(trim(\`nome\`)));` no arquivo de migração ANTES de rodar `migrate` — o índice único case-insensitive é a garantia atômica de dedupe (Pitfall 1) e NÃO pode ficar de fora do banco. NOTA: usa-se generate+migrate em vez de `drizzle-kit push` puro por decisão travada em CLAUDE.md/01-RESEARCH.md ("migrations are just generated SQL files you can read and review"); isto satisfaz o gate obrigatório de sync de schema — o banco real DEVE existir e conter as tabelas antes da verificação, pois build/tsc passam sem banco vivo (falso-positivo).
  </action>
  <verify>
    <automated>cd "C:/Users/Vencedor/Desktop/crm-leads" && npx tsc --noEmit && node ./scripts/verify-schema.cjs</automated>
    <automated-fallback>Se ./scripts/verify-schema.cjs não existir, criar um script .cjs que abre ./data/crm.db via better-sqlite3 e faz assert de que sqlite_master contém as tabelas 'leads' e 'subnichos' e o índice 'subnicho_nome_unique_idx', saindo com código != 0 se faltar algo.</automated-fallback>
  </verify>
  <acceptance_criteria>
    - `src/db/schema.ts` exporta `leads` e `subnichos`; `leads` contém `onDelete: "restrict"`, coluna `deletedAt`, e `valorEstimado` como integer (centavos)
    - `drizzle.config.ts` usa `dialect: "sqlite"` (NÃO a chave `driver`)
    - `src/db/migrations/` contém ao menos um arquivo `.sql` gerado
    - O `.sql` de migração contém um índice único de expressão sobre `lower(trim(nome))` (emitido pelo drizzle-kit OU adicionado manualmente se o drizzle-kit não o gerar)
    - O arquivo `./data/crm.db` existe e `sqlite_master` contém as tabelas `leads` e `subnichos` e o índice `subnicho_nome_unique_idx`
    - `./data/crm.db` NÃO está trackeado pelo git (`git status --porcelain data/` vazio)
    - `npx tsc --noEmit` sem erros
  </acceptance_criteria>
  <done>Schema das duas tabelas definido, migração versionada gerada e aplicada (com índice único de expressão garantido no banco); banco `./data/crm.db` existe com tabelas e índice único; tipos derivados disponíveis para os planos seguintes.</done>
</task>

<task type="auto">
  <name>Task 4: Corte vertical de sub-nicho (validations + Server Actions + tela de gestão inline)</name>
  <files>src/lib/validations.ts, src/actions/subnicho-actions.ts, src/components/subnicho-manager.tsx, src/app/subnichos/page.tsx</files>
  <read_first>
    - .planning/phases/01-lead-sub-nicho-foundation/01-PATTERNS.md (subnicho-actions linhas 217-255; validations linhas 141-163; Shared Patterns linhas 356-392; No Analog Found linha 404)
    - .planning/phases/01-lead-sub-nicho-foundation/01-RESEARCH.md (Pattern 2 linhas 330-384; Pitfall 1 linhas 431-437)
    - .planning/phases/01-lead-sub-nicho-foundation/01-UI-SPEC.md (Copywriting Contract linhas 105-123; Color accent "+ Adicionar" linha 81)
    - .planning/phases/01-lead-sub-nicho-foundation/01-CONTEXT.md (D-15, D-16)
    - src/db/schema.ts
    - src/db/client.ts
    - src/app/subnichos/page.tsx (casca criada na Task 2)
  </read_first>
  <action>
    Criar `src/lib/validations.ts` com `subnichoSchema = z.object({ nome: z.string().trim().min(1, "Nome é obrigatório.") })` (mensagem literal do Copywriting Contract). Criar `src/actions/subnicho-actions.ts` (`"use server"`) com `createSubnicho(prevState, formData)` e `renameSubnicho(prevState, formData)` seguindo o analog de 01-PATTERNS.md linhas 225-255: validar com `subnichoSchema`, fazer `SELECT` prévio com `sql\`lower(trim(...)) = lower(trim(...))\`` e retornar `{ errors: { nome: ["Esse sub-nicho já existe."] } }` em duplicata EXATA normalizada (near-duplicates de grafia diferente — ex.: "nutri" vs "nutricionista" — NÃO são bloqueados; só duplicata exata normalizada, per SPEC req 3); envolver o `insert`/`update` em try/catch e converter a violação de constraint do `uniqueIndex` na MESMA mensagem inline (rede de segurança contra duplo-clique — Pitfall 1); chamar `revalidatePath("/subnichos")` e `revalidatePath("/")` (o combobox do plano 01-02 depende dessa lista) e retornar `{ success: true }`. `renameSubnicho` recebe `id` + `nome` no formData e usa `db.update(subnichos).set({ nome }).where(eq(subnichos.id, id))`. Criar `src/components/subnicho-manager.tsx` (Client Component, D-16): lista os sub-nichos recebidos por prop; cada linha tem um ícone de lápis (lucide-react) que transforma o nome em `<input>` editável submetendo `renameSubnicho`; um botão "+ Adicionar" (cor accent `#0D9488`) adiciona uma nova linha/campo submetendo `createSubnicho`; sem modal separado; usar `useActionState` (NUNCA `useFormState`) e disparar toast sonner "Sub-nicho criado." / "Sub-nicho renomeado." no sucesso e erro inline "Esse sub-nicho já existe." no campo. Substituir a casca `src/app/subnichos/page.tsx` por um Server Component que faz `db.select().from(subnichos).orderBy(asc(subnichos.nome))` e passa o resultado para `<SubnichoManager>`, com título de página "Sub-nichos" (Display 28px/600). NÃO adicionar controle de excluir/desativar sub-nicho (fora de escopo — v2).
  </action>
  <verify>
    <automated>cd "C:/Users/Vencedor/Desktop/crm-leads" && npx tsc --noEmit && npm run build</automated>
    <human-check>Rodar `npm run dev`, abrir /subnichos, criar "Nutricionista" (aparece na lista + toast), tentar criar "nutricionista" (erro inline "Esse sub-nicho já existe."), criar "Nutri" (SUCESSO — near-duplicate não é bloqueado), renomear inline via ícone de lápis (toast de renomeado). Reiniciar o servidor e confirmar que o sub-nicho persiste.</human-check>
  </verify>
  <acceptance_criteria>
    - `createSubnicho` rejeita duplicata EXATA case-insensitive com `{ errors: { nome: ["Esse sub-nicho já existe."] } }` e tem try/catch cobrindo a violação de constraint do `uniqueIndex`
    - `createSubnicho` NÃO bloqueia near-duplicates de grafia diferente ("Nutri" quando existe "nutricionista" -> sucesso; SPEC req 3)
    - `subnicho-actions.ts` chama `revalidatePath("/subnichos")` e `revalidatePath("/")`
    - `subnicho-manager.tsx` usa `useActionState` (grep NÃO encontra `useFormState`)
    - Nenhum controle de excluir/desativar sub-nicho existe na tela (`src/app/subnichos/page.tsx` + `subnicho-manager.tsx`)
    - `src/app/subnichos/page.tsx` importa `db` e faz `db.select().from(subnichos)`
    - Textos de toast/erro batem literalmente com o Copywriting Contract ("Sub-nicho criado.", "Sub-nicho renomeado.", "Esse sub-nicho já existe.")
    - `npx tsc --noEmit` e `npm run build` passam
  </acceptance_criteria>
  <done>Corte vertical de sub-nicho completo: criar/renomear com dedupe exato case-insensitive (sem bloquear near-duplicates), persistência real, e feedback via toast/erro inline — walking skeleton fim-a-fim provado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (Client Component) -> Server Action | Input do formulário de sub-nicho cruza para o servidor; deve ser revalidado com Zod no server, nunca só no client |
| Server Action -> SQLite (Drizzle) | Toda escrita passa por query parametrizada do Drizzle; nenhum `sql.raw()` com input de usuário |
| npm registry -> ambiente de build | Instalação de dependências (supply chain) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | createSubnicho / renameSubnicho (input do formulário) | mitigate | Validar com `subnichoSchema` (`safeParse`) no server antes de tocar o Drizzle; `parsed.data` só contém `nome`, ignorando campos extras do FormData (inclui `$ACTION_*`) — barra mass-assignment |
| T-01-02 | Tampering | Índice único de sub-nicho (race de duplo-clique) | mitigate | `uniqueIndex("subnicho_nome_unique_idx")` sobre lower(trim(nome)) no schema + try/catch da violação de constraint na Server Action (Pitfall 1); Task 3 garante que o índice de expressão foi realmente escrito no `.sql` de migração |
| T-01-03 | Tampering | SQL injection via Drizzle | mitigate | Usar apenas o query builder parametrizado do Drizzle; `sql\`lower(trim(${subnichos.nome}))\`` interpola só identificador de coluna, nunca valor de usuário cru |
| T-01-04 | Information Disclosure | Mensagens de erro da Server Action ao client | mitigate | Retornar apenas mensagens de validação de campo (Zod), nunca stack trace ou payload completo; não logar payloads em produção |
| T-01-SC | Tampering | Instalação de pacotes npm (supply chain) | mitigate | slopcheck aprovou 14/16 pacotes centrais (RESEARCH Package Legitimacy Audit); Task 1 é checkpoint humano bloqueante com critério FINITO de 4 itens para `sonner`/`date-fns` (não auditados por timeout) antes do install; versões pinadas exatas (`sonner@2.0.7`/`date-fns@4.4.0`) evitam pull de "latest" comprometido |
</threat_model>

<verification>
- `npm run build` e `npx tsc --noEmit` passam após cada task de código
- Banco `./data/crm.db` existe com tabelas `leads`, `subnichos` e índice `subnicho_nome_unique_idx` (índice de expressão confirmado no `.sql`)
- Fluxo manual de sub-nicho (criar/duplicar/near-duplicate/renomear/persistir) confirmado no `human-check` da Task 4
- Nenhuma rota `app/api/*` criada; nenhum `useFormState`; nenhum controle de excluir sub-nicho
</verification>

<success_criteria>
- App roda localmente (`npm run dev`) com sidebar fixa e três rotas navegáveis
- Tela Sub-nichos cria e renomeia sub-nichos, com bloqueio de duplicata EXATA case-insensitive e sem bloquear near-duplicates (LEAD-02)
- Dados persistem no SQLite entre reinícios
- Stack completo provado fim-a-fim (UI -> Server Action -> Zod -> Drizzle -> SQLite -> re-render -> toast)
</success_criteria>

<output>
Create `.planning/phases/01-lead-sub-nicho-foundation/01-01-SUMMARY.md` when done. Registrar explicitamente: (1) qual lib de primitivos o `shadcn init` escolheu (Base UI vs Radix), (2) confirmação de que `better-sqlite3` instalou binário pré-compilado sem `node-gyp`, (3) nome do arquivo de migração gerado e se o índice de expressão foi emitido pelo drizzle-kit ou adicionado manualmente.
</output>
