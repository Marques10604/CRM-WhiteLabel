---
phase: 1
reviewers: [codex]
reviewed_at: 2026-07-19T20:09:00Z
cycles:
  - cycle: 1
    reviewed_at: 2026-07-19T20:09:00Z
    high_concerns: 4
  - cycle: 2
    reviewed_at: 2026-07-19T21:00:00Z
    high_concerns: 2
plans_reviewed: [01-01-PLAN.md, 01-02-PLAN.md, 01-03-PLAN.md, 01-04-PLAN.md]
---

# Cross-AI Plan Review — Phase 1: Lead & Sub-nicho Foundation

**Note:** `claude` was skipped as a reviewer (self — this review was run from inside Claude Code, per the workflow's self-exclusion rule for independence). No other CLIs (`gemini`, `coderabbit`, `opencode`, `qwen`, `cursor`, local model servers) were detected/requested on this system. Only `codex` was invoked, per the `--codex` flag, in both cycle 1 and cycle 2.

## Cycle 1 — Codex Review (Initial)

## Codex Review

### Overall Summary

The four-plan sequence is strong: it builds the risky infrastructure first, proves one full vertical slice before leads, then layers lead CRUD, table interactivity, and soft-delete in a sensible dependency order. The plans are mostly well-scoped for Phase 1 and directly map to LEAD-01 through LEAD-04 and REMIND-02. Main risks are around version-sensitive tooling, incomplete treatment of date/phone/value normalization, client/server action error contracts, and a few UI edge cases that could create subtle regressions if not specified before implementation.

---

#### PLAN 01-01 — Walking Skeleton

**Strengths**
- Good first slice: sub-nicho CRUD proves UI → Server Action → Zod → Drizzle → SQLite → revalidation.
- Correctly prioritizes DB-level case-insensitive uniqueness instead of relying only on app checks.
- Explicit live DB migration verification is valuable; it avoids "build passes but schema is wrong."
- Creating both `leads` and `subnichos` upfront is reasonable because the FK relationship shapes the data model.
- Strong constraint discipline: no `app/api/*`, no auth, no premature CRM features.

**Concerns**
- **HIGH:** The "human checkpoint" for `sonner` and `date-fns` may block basic scaffolding unless the acceptance criteria are explicit. Define what counts as approved.
- **MEDIUM:** "Near-duplicate" prevention is claimed, but the plan only guarantees normalized duplicate blocking. It will not catch `nutri` vs `nutricionista`, and that is explicitly out of scope. The wording should be tightened.
- **MEDIUM:** SQLite expression unique index syntax and Drizzle migration generation can be finicky. The plan should specify whether the index is generated by Drizzle schema or custom SQL migration.
- **LOW:** Sub-nicho rename can break user expectations if whitespace/case-only changes are silently normalized or rejected without clear messaging.

**Suggestions**
- Define normalization exactly: likely `trim`, collapse repeated internal whitespace, and lowercase for uniqueness comparison.
- Add a seed or manual verification path for one sub-nicho surviving server restart.
- Add a small action-level test for duplicate create and duplicate rename, not just UI verification.
- Document the package manager and lockfile expectations from the scaffold step.

**Risk Assessment:** MEDIUM. The architecture is sound, but greenfield scaffolding with very current versions, shadcn changes, Drizzle migrations, and better-sqlite3 on Windows makes the first wave the riskiest operationally.

---

#### PLAN 01-02 — Lead Create/Edit Vertical Slice

**Strengths**
- Correctly delivers the core business object early.
- The 9-field modal matches the product decisions and avoids feature sprawl.
- Reusing one create/edit dialog is appropriate for this scope.
- Good call using `useActionState` and validating through Zod on Server Actions.
- Explicit hard-delete prohibition is a useful safety rail before soft-delete exists.

**Concerns**
- **HIGH:** Value handling needs sharper specification. `valorEstimado` as integer cents is right, but the plan should define pt-BR input parsing, empty value behavior, rounding, and display formatting.
- **HIGH:** Phone handling is underspecified. Since WhatsApp is a later phase, bad phone normalization now can create cleanup work later.
- **MEDIUM:** `followUpDate` local-midnight normalization is mentioned, but the storage type is "UTC timestamp." The conversion contract should be exact: what is sent from the client, what is persisted, and how it is rendered back.
- **MEDIUM:** Lead update should validate that `subnichoId` exists and is active/valid, not only that the field is shaped correctly.
- **MEDIUM:** The behavior test examples are too thin for this phase. Empty name and valid payload do not cover the risky fields.
- **LOW:** "Base non-interactive list" plus row-click edit is already interactive; make sure keyboard accessibility and action affordances are not postponed indefinitely.

**Suggestions**
- Add tests for invalid `subnichoId`, invalid/empty phone, malformed date, invalid stage, and value parsing.
- Define allowed enum values for `canal` and `stage` in one shared source used by schema, UI, and badge.
- Specify form behavior for optional fields: `origem`, `valorEstimado`, `notas`, and `followUpDate`.
- Add an "at least one sub-nicho required before creating a lead" empty-state path or disabled save state.

**Risk Assessment:** MEDIUM. The vertical slice is well chosen, but this is where most domain data quality problems enter the system. Tightening normalization and error contracts would lower the risk.

---

#### PLAN 01-03 — Filter/Sort/Paginate

**Strengths**
- Correctly keeps filtering client-side for the expected data size.
- Date-range filtering explicitly addresses the off-by-one pitfall.
- Default sort by follow-up date aligns with the product promise: never miss follow-ups.
- Always-visible toolbar matches the user decision and keeps the workflow direct.
- TanStack Table is the right tool for this phase.

**Concerns**
- **MEDIUM:** Null/empty `followUpDate` sorting and filtering behavior is not specified. These leads need deterministic placement.
- **MEDIUM:** Pagination plus filters can create confusing empty pages if the current page index is not reset when filters change.
- **MEDIUM:** Date filters need a clear inclusive range contract.
- **LOW:** "Limpar filtros" should also reset date picker UI state, table column filters, and possibly sorting depending on intended behavior.
- **LOW:** Client-side filtering is acceptable now, but the plan should state a rough migration threshold more concretely.

**Suggestions**
- Define null follow-up behavior: for example, show after dated leads in ascending sort and exclude from date-range filters unless no date filter is active.
- Reset pagination to page 0 whenever any filter changes.
- Add visible active-filter state or a simple result count: "X leads encontrados".
- Add test data with mixed stages, sub-nichos, null dates, past dates, and future dates.

**Risk Assessment:** LOW to MEDIUM. This plan is straightforward, but table state edge cases can make the UI feel broken if not specified.

---

#### PLAN 01-04 — Soft-delete + Lixeira

**Strengths**
- Directly satisfies the "never permanently lost" promise.
- Confirmation dialog with the lead name is the right UX safeguard.
- `stopPropagation` on row actions is explicitly called out, which avoids a common table bug.
- Revalidating both `/` and `/lixeira` is correct.
- Instant restore matches the product decision and keeps the trash workflow lightweight.

**Concerns**
- **HIGH:** "Recoverable, never permanently lost" is not fully protected by grepping only `src/actions/` for `db.delete(`. Hard deletes could appear elsewhere, in scripts, migrations, or future utilities.
- **MEDIUM:** Soft-deleted leads should probably be excluded from edit/update paths unless intentionally restored first.
- **MEDIUM:** The plan does not specify whether deleting an already-deleted lead or restoring an active lead is idempotent.
- **MEDIUM:** Trash table reuse may accidentally preserve active-list row-click edit behavior unless explicitly removed.
- **LOW:** Lixeira should show `deletedAt` so the admin has context for what was removed and when.

**Suggestions**
- Add action-level tests for soft delete, restore, double delete, double restore, and active/trash query separation.
- Add a code convention: application actions must not call `db.delete()` for leads; if future cleanup scripts exist, they must be isolated and named clearly.
- Prevent editing deleted leads from normal edit routes/actions, or have update fail with a clear error.
- Include `deletedAt` in Lixeira sorting, probably newest deleted first.

**Risk Assessment:** LOW to MEDIUM. The feature is simple and well scoped. The main risk is overconfidence in the grep-based hard-delete guard and missing idempotency behavior.

---

#### Cross-Plan Risks

- **Version/tooling risk:** Next 16, React 19, shadcn Base UI, Drizzle, and better-sqlite3 are all version-sensitive. The plans correctly notice this, but implementation should verify early and record deviations.
- **Data normalization risk:** Sub-nicho names, phone numbers, currency, and dates need precise contracts before the database accumulates messy data.
- **Server Action contract risk:** Plans should define a consistent return shape for field errors, form errors, success payloads, and toast messages.
- **Testing gap:** The plans mention useful tests, but coverage is uneven. Most risk sits in actions and normalization, not visual rendering.
- **Accessibility risk:** Modal, combobox, date picker, icon buttons, and row-click editing all need keyboard/focus behavior checked, especially because shadcn primitive behavior may differ by version.

#### Final Assessment (Codex)

Overall risk: **MEDIUM**.

The phase is well decomposed and should achieve the five Phase 1 success criteria if implemented faithfully. The biggest improvements would be to make normalization rules explicit, strengthen action-level tests, define edge-case behavior for dates/nulls/idempotency, and avoid overstating "near duplicate" prevention when the actual Phase 1 mechanism is normalized exact duplicate blocking.

---

## Consensus Summary

Only one external reviewer (`codex`) was invoked for this cycle, so there is no cross-model agreement/disagreement to synthesize — the findings below are Codex's alone, carried forward as-is.

### Agreed Strengths
(Single reviewer — not applicable. See Codex's per-plan "Strengths" above.)

### Agreed Concerns (all HIGH, single reviewer)
- **01-01:** The `sonner`/`date-fns` human supply-chain checkpoint has no explicit "what counts as approved" criteria — risks stalling the walking skeleton on ambiguity rather than an actual finding.
- **01-02:** `valorEstimado` (estimated value) input/parsing/rounding/display contract is underspecified — the pt-BR currency input path, empty-value handling, and cents-rounding rule are not pinned down before implementation.
- **01-02:** `telefone` (phone) normalization is underspecified — deferring this to Phase 4 (WhatsApp `wa.me` links) risks accumulating bad phone data now that has to be cleaned up later.
- **01-04:** The "leads are never permanently lost" guarantee is verified only by a `grep` for `db.delete(` inside `src/actions/` — this doesn't protect against a hard-delete introduced elsewhere (a script, a future utility, a different module), so the acceptance criterion is narrower than the actual requirement (LEAD-04).

### Divergent Views
Not applicable — single reviewer this cycle.

---

## Cycle 2 — Codex Review (Re-review after fixes)

**Context:** Cycle 1's codex review found 4 HIGH concerns. Commit `3fd40d9` ("docs(01): replan phase incorporating cross-AI review feedback (codex cycle 1)") revised all 4 plans to address them. This cycle re-reviews the updated 01-01 through 01-04 PLAN.md files, explicitly verifying resolution status of each prior HIGH and checking for new issues.

**Reviewed at:** 2026-07-19 (cycle 2) · **Reviewer:** codex (only external CLI available/requested; `claude` self-excluded per workflow rule)

### Summary

The revised plan set is materially stronger than Cycle 1. The 4 prior HIGH concerns are addressed with concrete behavior, test cases, and verification hooks rather than vague intent. Codex would still tighten a few implementation-risk areas before execution, especially around nullable follow-up dates, the hard-delete guard's Windows portability, and the money parser being more permissive than the tests imply. Overall the plans still cover the 5 Phase 1 success criteria.

### Cycle 1 Resolution Verdicts

1. **HIGH #1 — sonner/date-fns human checkpoint ambiguity: FULLY RESOLVED**
   `01-01` now defines a finite 4-item checklist: exact package name (typosquat check), official repo, exact pinned version, and download volume, plus an explicit non-approval path. Wired into a blocking human checkpoint before install.

2. **HIGH #2 — valorEstimado contract underspecified: FULLY RESOLVED**
   `01-02` now specifies `parseBRLToCents`/`formatCentsToBRL`, pt-BR parsing, empty-value rejection, explicit zero handling, cent rounding (round-half-up), and 9 test cases. Wired into `scripts/test-money.cjs` plus action-level tests.

3. **HIGH #3 — telefone normalization underspecified: FULLY RESOLVED**
   `01-02` now gives a clear `normalizePhone` contract: digits-only, BR DDI `55`, 10/11-digit local numbers, 12/13-digit already-prefixed numbers, invalid lengths rejected, with explicit tests. Closes the Phase 4 `wa.me` data-quality risk.

4. **HIGH #4 — hard-delete grep too narrow (only `src/actions/`): PARTIALLY RESOLVED**
   The intent is fixed: `guard:no-hard-delete` now scans all of `src/` (not just `src/actions/`), and the verify block also checks for destructive SQL in migrations. Remaining gap: the proposed `package.json` script uses POSIX shell syntax (`! grep -rEn ...`), which may behave inconsistently across Git Bash / PowerShell / `npm.cmd` on this project's Windows environment. Codex recommends implementing the guard as a Node script (`scripts/guard-no-hard-delete.cjs`) instead of inline shell syntax for reliable cross-shell behavior.

### New Concerns

- **HIGH — Follow-up date is specified as both required (DB/Zod) and nullable (list behavior) across plans, with no reachable path to the nullable case.**
  `01-01`'s schema analog declares `followUpDate: integer(...).notNull()` and `01-02`'s `leadSchema` uses `followUpDate: z.coerce.date()` with no `.nullable()/.optional()` — consistent with `01-SPEC.md`'s locked constraint "All 9 fields are required at save time — no partial/draft leads are persisted." Yet `01-03-PLAN.md` Task 1 specifies real behavior and acceptance criteria for "leads sem data de follow-up" (`sortUndefined: 'last'`, a `filterFn` that excludes null-date leads from the date-range filter), and per its own acceptance criteria this must be implemented and grep-verified. Given the DB column is `NOT NULL` and the form always requires the field, this code path is unreachable through the app's own UI in Phase 1 — either the requiredness should be relaxed (nullable follow-up date), or `01-03`'s null-handling spec should be removed/marked as forward-looking-only (e.g., anticipating Phase 2 CSV import rows that might arrive without a date). This should be resolved explicitly before execution, not left as an implicit contradiction between plans.

- **MEDIUM — `guard:no-hard-delete` may be brittle on Windows.**
  Suggested fix: `scripts/guard-no-hard-delete.cjs` (recursive file scan + regex) invoked via `"guard:no-hard-delete": "node scripts/guard-no-hard-delete.cjs"`, avoiding Git Bash/PowerShell/npm-script shell differences.

- **MEDIUM — Money parser may accept malformed input by silently stripping unexpected characters.**
  The spec strips everything that isn't a digit, `.`, or `,` before parsing, so inputs like `"abc1"` reduce to `"1"` rather than being rejected. If intentional (to tolerate a leading "R$ " prefix), tighten with a pre-validation regex that allows only currency symbols/whitespace/digits/separators and rejects other embedded characters, rather than silently discarding arbitrary junk.

- **MEDIUM — `subnichoId` existence check has a check-then-write race window.**
  The plan validates `subnichoId` existence via a `SELECT` before insert/update, which is good for inline UX, but doesn't catch the case where the sub-nicho is deleted between the check and the write. Recommend also catching the FK constraint failure (from `onDelete: "restrict"`) and mapping it to the same `"Selecione um sub-nicho."` inline error as a defense-in-depth backstop.

- **LOW — Hard-delete guard scope (`src/`) still excludes `scripts/`.**
  The original Cycle 1 concern explicitly mentioned "a script, a future utility" as an unprotected path. `01-04`'s guard scans `src/` but the repo already has a `scripts/` directory (e.g., `scripts/test-*.cjs`, `scripts/verify-schema.cjs`) sitting outside that scan. Consider scanning `src/` + `scripts/` + migration folders together (with an explicit allowlist for the schema-creation migration itself).

### Suggestions

- Replace the shell-based `guard:no-hard-delete` grep with a small Node script for reliable behavior across Windows shells; wire it into the same verify step already planned.
- Resolve the follow-up-date required/nullable contract explicitly before implementation — either drop `01-03`'s null-handling behavior (documenting it as N/A for Phase 1) or relax the field to nullable end-to-end (schema, Zod, form) and update `01-SPEC.md`'s "all 9 fields required" acceptance criterion accordingly. Do not leave the two plans implicitly disagreeing.
- Add an FK-constraint-violation catch in `createLead`/`updateLead` as a backstop behind the existing `subnichoId` existence pre-check.
- Tighten `parseBRLToCents` to reject strings with disallowed characters instead of stripping them silently.
- Add an explicit action-level test asserting soft-delete idempotency preserves the original `deletedAt` timestamp on a second delete call.

### Risk Assessment (Codex, cycle 2)

**Overall risk: MEDIUM.** The four original HIGH issues are mostly closed — three fully, one partially (Windows shell portability). The main remaining risk is not conceptual scope; it is implementation ambiguity in follow-up-date nullability (a genuine unresolved contradiction between `01-02` and `01-03`) and verification-script brittleness on Windows. Resolving those two before execution would bring the phase close to LOW risk.

---

## Cycle 2 Consensus Summary

Single reviewer (`codex`) again this cycle — no cross-model divergence to synthesize.

### Cumulative Status of Cycle 1's 4 HIGH Concerns
| # | Concern | Status |
|---|---------|--------|
| 1 | 01-01 sonner/date-fns checkpoint ambiguity | ✅ Fully resolved |
| 2 | 01-02 valorEstimado contract underspecified | ✅ Fully resolved |
| 3 | 01-02 telefone normalization underspecified | ✅ Fully resolved |
| 4 | 01-04 hard-delete grep scoped too narrow | ⚠️ Partially resolved (scope fixed; Windows shell portability of the guard script is unverified) |

### New HIGH Raised This Cycle
- Follow-up date is `NOT NULL`/required end-to-end in `01-01`/`01-02`, but `01-03` specifies real, grep-verified behavior for leads with a null follow-up date — an unreachable code path per the current locked schema. Needs an explicit decision (relax to nullable, or drop `01-03`'s null-handling spec) before execution.

### Open MEDIUM/LOW Items
- `guard:no-hard-delete` shell portability on Windows (MEDIUM)
- Money parser silently strips unexpected characters instead of rejecting them (MEDIUM)
- `subnichoId` check-then-write race has no FK-violation backstop (MEDIUM)
- Hard-delete guard scope excludes `scripts/` (LOW)
