---
phase: 01-lead-sub-nicho-foundation
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - src/components/lead-table.tsx
  - src/components/lead-table-columns.tsx
  - src/components/lead-table-toolbar.tsx
autonomous: true
requirements: [REMIND-02]
user_setup: []

must_haves:
  truths:
    - "O admin consegue filtrar a lista por sub-nicho (single-select) e ver só os leads daquele sub-nicho"
    - "O admin consegue filtrar por etapa (single-select) e ver só os leads daquela etapa"
    - "O admin consegue filtrar por intervalo de data de follow-up (início/fim) e ver só os leads dentro do intervalo"
    - "O admin consegue ordenar as colunas clicando no cabeçalho"
    - "A lista pagina de 25 em 25 com controles anterior/próximo"
    - "Sem filtros aplicados, o topo da lista é o lead com follow-up mais próximo"
  artifacts:
    - path: "src/components/lead-table.tsx"
      provides: "Tabela com sort/filtro/paginação client-side via @tanstack/react-table"
      contains: "getFilteredRowModel"
    - path: "src/components/lead-table-toolbar.tsx"
      provides: "Toolbar fixa de filtros (sub-nicho, etapa, intervalo de follow-up) — D-11"
  key_links:
    - from: "src/components/lead-table-toolbar.tsx"
      to: "src/components/lead-table.tsx"
      via: "column.setFilterValue via instância da table"
      pattern: "setFilterValue"
    - from: "src/components/lead-table.tsx"
      to: "@tanstack/react-table"
      via: "row models de sort/filtro/paginação"
      pattern: "getSortedRowModel|getPaginationRowModel"
---

## Phase Goal

**As a** admin do CRM, **I want to** filtrar e ordenar toda a lista de leads por sub-nicho, etapa e data de follow-up, **so that** eu enxergo de relance quem precisa de atenção, sem rolar uma planilha inteira.

> História derivada da linha `**Goal:**` do ROADMAP.md da Fase 1 — não é escopo inventado.

<objective>
Refina o corte de lista do plano 01-02 adicionando interatividade: ordenação de colunas, filtros single-select (sub-nicho, etapa), filtro por intervalo de data de follow-up, e paginação clássica de 25/página. Cobre REMIND-02.

Purpose: A lista base já existe; esta é a refinação MVP que a torna útil para operar o funil — o admin encontra qualquer lead sem esforço.
Output: `lead-table` totalmente interativo (client-side) + toolbar de filtros fixa acima da tabela.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-lead-sub-nicho-foundation/01-SPEC.md
@.planning/phases/01-lead-sub-nicho-foundation/01-RESEARCH.md
@.planning/phases/01-lead-sub-nicho-foundation/01-PATTERNS.md
@.planning/phases/01-lead-sub-nicho-foundation/01-UI-SPEC.md
@.planning/phases/01-lead-sub-nicho-foundation/01-02-SUMMARY.md

<interfaces>
Consome de 01-02:
- src/components/lead-table.tsx: `<LeadTable leads subnichos />` com `useReactTable` + `getCoreRowModel` (ESTENDER — adicionar row models e state).
- src/components/lead-table-columns.tsx: colunas D-06 (ESTENDER — habilitar sorting e filtros por coluna).
- src/types/index.ts: `Lead`, `Subnicho`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sorting + paginação (25/página) na lead-table</name>
  <files>src/components/lead-table.tsx, src/components/lead-table-columns.tsx</files>
  <read_first>
    - .planning/phases/01-lead-sub-nicho-foundation/01-PATTERNS.md (lead-table linhas 314-342; Pattern 3 config)
    - .planning/phases/01-lead-sub-nicho-foundation/01-RESEARCH.md (Pattern 3 linhas 386-407)
    - .planning/phases/01-lead-sub-nicho-foundation/01-CONTEXT.md (D-12)
    - src/components/lead-table.tsx (criado em 01-02, com getCoreRowModel)
    - src/components/lead-table-columns.tsx
  </read_first>
  <action>
    Estender `useReactTable` em `src/components/lead-table.tsx` adicionando `getSortedRowModel()` e `getPaginationRowModel()`, com `initialState: { pagination: { pageSize: 25 } }` (D-12) e `initialState.sorting` em `followUpDate` ascendente (garante que "sem filtros" mostra follow-up mais próximo primeiro mesmo com sort client-side). Adicionar os states `sorting` (`useState`) e handlers `onSortingChange`. Renderizar controles de paginação clássicos (botões "Anterior"/"Próximo" + indicador de página, texto auxiliar Label 14px) usando `table.previousPage()`/`table.nextPage()`/`table.getCanPreviousPage()`. Em `src/components/lead-table-columns.tsx`, habilitar `enableSorting` nas colunas ordenáveis (Nome, Sub-nicho, Etapa, Follow-up) e tornar os cabeçalhos clicáveis para alternar a direção do sort.
  </action>
  <verify>
    <automated>cd "C:/Users/Vencedor/Desktop/crm-leads" && npx tsc --noEmit && npm run build</automated>
    <human-check>Com >25 leads (ou pageSize temporariamente reduzido para testar), confirmar paginação anterior/próximo; clicar no cabeçalho Nome ordena asc/desc; sem interação, o topo é o follow-up mais próximo.</human-check>
  </verify>
  <acceptance_criteria>
    - `lead-table.tsx` contém `getSortedRowModel` e `getPaginationRowModel` e `pageSize: 25`
    - `initialState.sorting` referencia `followUpDate` ascendente
    - Controles "Anterior"/"Próximo" presentes e ligados a `table.previousPage`/`table.nextPage`
    - Cabeçalhos de coluna ordenáveis alternam a direção do sort
    - `npx tsc --noEmit` e `npm run build` passam
  </acceptance_criteria>
  <done>Tabela ordenável por coluna e paginada de 25 em 25, com default por follow-up mais próximo.</done>
</task>

<task type="auto">
  <name>Task 2: Toolbar fixa de filtros (sub-nicho, etapa, intervalo de follow-up)</name>
  <files>src/components/lead-table.tsx, src/components/lead-table-columns.tsx, src/components/lead-table-toolbar.tsx</files>
  <read_first>
    - .planning/phases/01-lead-sub-nicho-foundation/01-PATTERNS.md (lead-table filtros linhas 333-342; Shared Patterns normalização de fuso linhas 386-388)
    - .planning/phases/01-lead-sub-nicho-foundation/01-RESEARCH.md (Pitfall 6 linhas 483-489; Pattern 3 filtros)
    - .planning/phases/01-lead-sub-nicho-foundation/01-UI-SPEC.md (Spacing toolbar; Color accent focus)
    - .planning/phases/01-lead-sub-nicho-foundation/01-CONTEXT.md (D-11)
    - src/components/lead-table.tsx (agora com sort/paginação)
  </read_first>
  <action>
    Adicionar `getFilteredRowModel()` e o state `columnFilters` (`useState` + `onColumnFiltersChange`) ao `useReactTable` em `src/components/lead-table.tsx`. Criar `src/components/lead-table-toolbar.tsx` (Client Component, D-11): toolbar SEMPRE visível acima da tabela (não colapsável) com três controles single-select/range que chamam `column.setFilterValue`: (1) `<Select>` de sub-nicho populado dos `subnichos` (single-select); (2) `<Select>` de etapa com os 4 valores fixos (Novo/Contatado/Negociação/Fechado-Perdido); (3) filtro de intervalo de data de follow-up com dois `<Calendar>`/date-picker (início/fim) que setam `column.setFilterValue([inicio, fim])`. Implementar uma `filterFn` customizada na coluna `followUpDate` que compara usando date-fns `startOfDay(inicio)` e `endOfDay(fim)` no fuso local antes de comparar com o timestamp do lead (Pitfall 6 — evita lead sumir/aparecer perto da meia-noite). Incluir um botão "Limpar filtros" que reseta `columnFilters`. Os três filtros funcionam independentemente e combinados. Anel de foco na cor accent `#0D9488`.
  </action>
  <verify>
    <automated>cd "C:/Users/Vencedor/Desktop/crm-leads" && npx tsc --noEmit && npm run build</automated>
    <human-check>Rodar `npm run dev`, aplicar filtro de sub-nicho (só leads daquele sub-nicho aparecem), depois etapa, depois intervalo de follow-up; combinar dois filtros; testar um follow-up marcado "hoje" perto da virada do dia (não some do filtro do dia); "Limpar filtros" volta à lista completa.</human-check>
  </verify>
  <acceptance_criteria>
    - `lead-table.tsx` contém `getFilteredRowModel` e state `columnFilters`
    - `lead-table-toolbar.tsx` tem 3 controles: sub-nicho (single-select), etapa (single-select), intervalo de follow-up (range) — todos via `setFilterValue`
    - A `filterFn` de `followUpDate` usa date-fns `startOfDay`/`endOfDay` (grep encontra `startOfDay` e `endOfDay`)
    - Toolbar é sempre visível (não há painel colapsável para os filtros)
    - Botão "Limpar filtros" reseta os filtros
    - `npx tsc --noEmit` e `npm run build` passam
  </acceptance_criteria>
  <done>Toolbar fixa filtra a lista por sub-nicho, etapa e intervalo de follow-up (independente e combinado), com normalização de fuso — REMIND-02 completo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (toolbar/table) -> nenhum servidor | Filtro/sort/paginação rodam inteiramente client-side sobre dados já carregados; não há novo cruzamento de confiança nesta fase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Tampering | filterFn de intervalo de data (bug de fuso) | mitigate | Normalizar com date-fns `startOfDay`/`endOfDay` no fuso local antes de comparar timestamps (Pitfall 6) — evita resultado incorreto de filtro, ameaça de integridade de exibição, não de segurança |
| T-01-10 | Information Disclosure | Dados de leads no client | accept | Toda a lista de leads ativos já é carregada no client para filtro/sort (padrão para o volume esperado, RESEARCH Pattern 3); app single-user local, sem outro usuário de quem esconder dado — risco aceito |
| T-01-SC | Tampering | Dependências npm | accept | Nenhuma instalação nova neste plano (date-fns/@tanstack já auditados em 01-01) |
</threat_model>

<verification>
- `npx tsc --noEmit` e `npm run build` passam após cada task
- Filtros (sub-nicho/etapa/intervalo), sort e paginação confirmados nos `human-check`
- Default sort = follow-up mais próximo; filtro de data resistente à virada do dia
</verification>

<success_criteria>
- Lista filtrável por sub-nicho, etapa e intervalo de follow-up, independente e combinada (REMIND-02)
- Colunas ordenáveis; paginação 25/página
- Default sem filtros = follow-up mais próximo primeiro
</success_criteria>

<output>
Create `.planning/phases/01-lead-sub-nicho-foundation/01-03-SUMMARY.md` when done.
</output>
