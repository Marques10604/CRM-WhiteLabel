---
phase: 15-campo-interesse-servi-o-desejado-no-lead
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - src/lib/csv-import.ts
  - src/components/csv-column-mapper.tsx
  - src/components/csv-import-wizard.tsx
  - src/actions/import-actions.ts
  - src/components/csv-import-preview-table.tsx
  - scripts/test-lead-actions.cjs
autonomous: true
requirements: [LEAD-06]

must_haves:
  truths:
    - "O passo de mapeamento do wizard de CSV lista Interesse como campo opcional (com opção '— nenhuma —')"
    - "Importar um CSV com uma coluna mapeada para Interesse cria os leads com esse valor preenchido"
    - "Importar um CSV sem mapear Interesse funciona exatamente como antes, com interesse nulo nos leads"
    - "Uma célula de Interesse maior que 500 caracteres no CSV não reprova a linha — o lead importa com o valor truncado em 500"
  artifacts:
    - path: "src/lib/csv-import.ts"
      provides: "interesse em CsvFieldKey e MappedCsvRow; truncamento em mapCsvRows antes da validação"
      contains: "slice(0, 500)"
    - path: "src/components/csv-column-mapper.tsx"
      provides: "entrada Interesse (required: false) em FIELD_CONFIGS"
      contains: "key: \"interesse\""
    - path: "src/components/csv-import-wizard.tsx"
      provides: "chave interesse no objeto-literal EMPTY_MAPPING"
      contains: "interesse: null"
    - path: "src/actions/import-actions.ts"
      provides: "interesse em ConfirmedImportRow e no insert de bulkImportLeads"
      contains: "interesse: row.interesse ?? null"
    - path: "src/components/csv-import-preview-table.tsx"
      provides: "interesse no ConfirmedImportRow montado em handleConfirm"
      contains: "interesse: r.interesse"
  key_links:
    - from: "src/components/csv-column-mapper.tsx"
      to: "src/lib/csv-import.ts"
      via: "FIELD_CONFIGS -> mapping -> readMapped(row, 'interesse')"
      pattern: "readMapped\\(row, \"interesse\"\\)"
    - from: "src/lib/csv-import.ts"
      to: "src/actions/import-actions.ts"
      via: "MappedCsvRow.interesse -> ConfirmedImportRow -> bulkImportLeads insert"
      pattern: "interesse: row.interesse \\?\\? null"
    - from: "src/actions/import-actions.ts"
      to: "data/crm.db"
      via: "tx.insert(leads).values inclui interesse"
      pattern: "tx.insert\\(leads\\)"
---

<objective>
Adicionar `interesse` como campo mapeável opcional no wizard de importação CSV: novo
`CsvFieldKey`, entrada no column-mapper, propagação por `mapCsvRows` (com truncamento em
500), e carregamento até o insert de `bulkImportLeads`.

Purpose: fecha a metade "CSV" de LEAD-06 e encerra a milestone v1.4 — o lead que entra
em lote do cowork carrega o que ele quer.
Output: campo Interesse no passo de mapeamento + valor importado no lead + cobertura
automatizada. Depende do plano 15-01 (coluna no banco + `interesse` em `leadBaseSchema`/
`csvRowSchema`).
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-campo-interesse-servi-o-desejado-no-lead/15-SPEC.md
@.planning/phases/15-campo-interesse-servi-o-desejado-no-lead/15-CONTEXT.md
@.planning/phases/15-campo-interesse-servi-o-desejado-no-lead/15-PATTERNS.md
@.planning/phases/15-campo-interesse-servi-o-desejado-no-lead/15-01-PLAN.md

@src/lib/csv-import.ts
@src/components/csv-column-mapper.tsx
@src/components/csv-import-wizard.tsx
@src/actions/import-actions.ts
@src/components/csv-import-preview-table.tsx
@scripts/test-lead-actions.cjs

<interfaces>
<!-- Contratos existentes. `interesse` no leadBaseSchema/csvRowSchema já foi entregue pelo plano 15-01. -->

CsvFieldKey (src/lib/csv-import.ts:13-20) — união de string literais; `notas` é o último.
CsvColumnMapping = `Record<CsvFieldKey, string | null>` (deriva sozinho).
MappedCsvRow (src/lib/csv-import.ts:32-42) — todos os campos `string` (nunca `string | null`;
default aplicado no map). `notas: string` é o último.
CSV_DEFAULTS (src/lib/csv-import.ts:62-68) — NÃO ganha `interesse` (D-11).

mapCsvRows (src/lib/csv-import.ts:111-150):
```ts
function readMapped(row: ParsedCsvRow, field: CsvFieldKey): string {
  const header = mapping[field];
  if (header === null || !(header in row)) return "";
  return (row[header] ?? "").trim();
}
// ... return { rowIndex, nome, telefone, telefoneNormalizado, nichoNome, canal, origem, valorEstimado, notas };
```

FIELD_CONFIGS (src/components/csv-column-mapper.tsx:17-25) — `{ key, label, required }[]`;
o `.map` (:99-132) já renderiza `<Select>` com "— nenhuma —" para todo `required: false`.

EMPTY_MAPPING (src/components/csv-import-wizard.tsx:30-38) — objeto-literal escrito à mão,
tipado `CsvColumnMapping`; `tsc` exige uma chave por `CsvFieldKey`.

ConfirmedImportRow (src/actions/import-actions.ts:67-75) — tipo hand-written; `nichoNome` é
o último. bulkImportLeads valida cada linha com `csvRowSchema.safeParse` (:91) e insere
campo-a-campo em `tx.insert(leads).values({...})` (:143-157) — NÃO usa spread.

csv-import-preview-table.tsx `handleConfirm` (:230-268) — monta `confirmedRows.push({ nome,
telefone, canal, origem, valorEstimado, notas, nichoNome })` a partir de `r` (`MappedCsvRow`).

scripts/test-lead-actions.cjs — imports dinâmicos :133-138; `manualAlters` já contém o
`ALTER TABLE leads ADD interesse` (plano 15-01 Task 3). `makeImportRow` (:356-367) monta
`ConfirmedImportRow` via spread de overrides. Casos 11-12 (:369-409) toleram o `throw` de
`revalidatePath` fora do runtime Next.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: interesse como campo mapeável (tipos + column-mapper + wizard)</name>
  <files>src/lib/csv-import.ts, src/components/csv-column-mapper.tsx, src/components/csv-import-wizard.tsx</files>
  <read_first>
    - src/lib/csv-import.ts (:13-20 `CsvFieldKey`; :32-42 `MappedCsvRow`; :44-68 `CSV_DEFAULTS`; :111-150 `mapCsvRows` + `readMapped`; :82-109 `buildNotasText` — NÃO tocar)
    - src/components/csv-column-mapper.tsx (:14-25 `FieldConfig` + `FIELD_CONFIGS`; :99-132 render do `.map`)
    - src/components/csv-import-wizard.tsx (:30-38 `EMPTY_MAPPING`)
    - .planning/phases/15-campo-interesse-servi-o-desejado-no-lead/15-PATTERNS.md (seções "src/lib/csv-import.ts", "src/components/csv-column-mapper.tsx", e "Padrões Compartilhados > Campo CSV mapeável")
    - .planning/phases/15-campo-interesse-servi-o-desejado-no-lead/15-CONTEXT.md (D-08, D-09, D-10, D-11)
  </read_first>
  <behavior>
    - `mapCsvRows` com `mapping.interesse` apontando para um header cria linhas com `interesse` = valor trimado daquela célula
    - `mapCsvRows` com `mapping.interesse = null` cria linhas com `interesse === ""`
    - célula de `interesse` com 600 caracteres → `result[i].interesse.length === 500`
    - `MappedCsvRow` e `CsvColumnMapping` compilam com a nova chave; `EMPTY_MAPPING` continua tipando
  </behavior>
  <action>
    Em `src/lib/csv-import.ts`: adicionar `| "interesse"` ao fim da união `CsvFieldKey`
    (após `"notas"`, :20). Adicionar `interesse: string;` a `MappedCsvRow` (após
    `notas: string;`, :41). Dentro do `.map` de `mapCsvRows`, calcular
    `const interesse = readMapped(row, "interesse").slice(0, 500);` — o `.slice(0, 500)`
    acontece ANTES de qualquer validação (D-10: célula gigante nunca reprova a linha, o
    lead importa truncado) — e incluir `interesse` no objeto retornado (junto de `notas`).
    NÃO adicionar entrada em `CSV_DEFAULTS` (D-11: vazio/não-mapeado = `""` → vira `null` na
    Server Action). NÃO tocar `buildNotasText`, `extraNotasColumns` nem
    `detectWithinBatchDuplicatePhones`.

    Em `src/components/csv-column-mapper.tsx`: adicionar `{ key: "interesse", label:
    "Interesse", required: false }` a `FIELD_CONFIGS`, imediatamente após a entrada `notas`
    (:24). Nenhuma outra mudança — o `.map` já gera o `<Select>` com "— nenhuma —" para
    todo campo `required: false` (D-09).

    Em `src/components/csv-import-wizard.tsx`: adicionar `interesse: null` ao objeto-literal
    `EMPTY_MAPPING` (:30-38) — `CsvColumnMapping` é `Record<CsvFieldKey, ...>` mas o literal
    é escrito à mão, então `tsc` exige a chave nova.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exit 0
    - `grep -n '"interesse"' src/lib/csv-import.ts` casa dentro de `CsvFieldKey`; `grep -n 'interesse: string' src/lib/csv-import.ts` casa em `MappedCsvRow`
    - `grep -n 'readMapped(row, "interesse").slice(0, 500)' src/lib/csv-import.ts` casa
    - `grep -n 'interesse' src/lib/csv-import.ts` NÃO casa dentro do bloco `CSV_DEFAULTS` (:62-68)
    - `grep -n 'key: "interesse"' src/components/csv-column-mapper.tsx` casa com `required: false`
    - `grep -n 'interesse: null' src/components/csv-import-wizard.tsx` casa no `EMPTY_MAPPING`
  </acceptance_criteria>
  <done>interesse é CsvFieldKey opcional, aparece no column-mapper, e mapCsvRows produz o valor truncado em 500; tsc limpo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: interesse carregado até o insert do import + cobertura</name>
  <files>src/actions/import-actions.ts, src/components/csv-import-preview-table.tsx, scripts/test-lead-actions.cjs</files>
  <read_first>
    - src/actions/import-actions.ts (:67-75 `ConfirmedImportRow`; :81-101 loop de `csvRowSchema.safeParse`; :138-160 loop de insert linha-a-linha `tx.insert(leads).values`)
    - src/components/csv-import-preview-table.tsx (:227-268 `handleConfirm` + `confirmedRows.push`; import de `MappedCsvRow` :25)
    - scripts/test-lead-actions.cjs (:132-138 imports dinâmicos; :352-409 `makeImportRow` + Casos 11-12; `manualAlters` :101-114)
    - .planning/phases/15-campo-interesse-servi-o-desejado-no-lead/15-PATTERNS.md (seção "lead-actions.ts" parágrafo de `bulkImportLeads`; "Arquivos adjacentes")
    - .planning/phases/15-campo-interesse-servi-o-desejado-no-lead/15-CONTEXT.md (D-04, D-10)
  </read_first>
  <behavior>
    - `bulkImportLeads([makeImportRow({ interesse: "quer site institucional" })])` → `SELECT interesse` da linha inserida = `"quer site institucional"`
    - `bulkImportLeads([makeImportRow()])` (sem chave `interesse`) → `SELECT interesse` da linha inserida = `NULL`
    - `mapCsvRows` com uma célula de 600 chars mapeada para `interesse` → `result[0].interesse.length === 500`
  </behavior>
  <action>
    Em `src/actions/import-actions.ts`: adicionar `interesse: string;` ao tipo
    `ConfirmedImportRow` (após `notas: string;`). No loop de insert de `bulkImportLeads`
    (`tx.insert(leads).values({...})`, após `notas: row.notas,`), adicionar
    `interesse: row.interesse ?? null` (idioma "undefined/'' do Zod → null explícito"; `row`
    aqui é `CsvRowValues`, que herda `interesse` opcional do `leadBaseSchema` via o plano
    15-01). O `.max(500)` do `csvRowSchema.safeParse` (:91) nunca reprova porque `mapCsvRows`
    já truncou (D-10).

    Em `src/components/csv-import-preview-table.tsx`: no objeto passado a
    `confirmedRows.push({...})` (:259-267), adicionar `interesse: r.interesse` (`r` é
    `MappedCsvRow`, que ganhou `interesse` na Task 1).

    Em `scripts/test-lead-actions.cjs`: adicionar `const { mapCsvRows } = await
    import("@/lib/csv-import");` junto dos outros imports dinâmicos (:133-138). Confirmar
    que `makeImportRow` repassa `interesse` via spread de overrides (já faz). Adicionar
    3 casos novos após os Casos 11-12: (a) `bulkImportLeads([makeImportRow({ interesse:
    "quer site institucional" })])` → ler a linha inserida do banco temporário e conferir
    `interesse === "quer site institucional"`; (b) `bulkImportLeads([makeImportRow()])` →
    linha inserida com `interesse` `null`; (c) `mapCsvRows([{ Interesse: "z".repeat(600) }],
    { <CsvColumnMapping com interesse: "Interesse", nome/telefone/etc: null> })` →
    `result[0].interesse.length === 500`. Tolerar o `throw` de `revalidatePath` fora do
    runtime Next igual aos Casos 11-12.
  </action>
  <verify>
    <automated>npm run test:lead-actions</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exit 0
    - `npm run test:lead-actions` exit 0 com as 3 novas linhas `OK ...` (persistência via import, null via import, truncamento de mapCsvRows)
    - `grep -n 'interesse: string' src/actions/import-actions.ts` casa em `ConfirmedImportRow`; `grep -n 'interesse: row.interesse ?? null' src/actions/import-actions.ts` casa no `.values`
    - `grep -n 'interesse: r.interesse' src/components/csv-import-preview-table.tsx` casa
    - `npm run build` exit 0
    - `npm run lint` exit 0 (ou só falsos-positivos já suprimidos)
  </acceptance_criteria>
  <done>Valor mapeado de interesse chega ao lead importado; import sem mapear continua igual; 3 casos automatizados verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arquivo CSV do cowork → parse client-side (PapaParse) → `bulkImportLeads` | conteúdo de arquivo de terceiro (partner) cruza aqui; a coluna mapeada para `interesse` é texto arbitrário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | Denial of Service (célula gigante no CSV) | coluna mapeada para `interesse` em `mapCsvRows` | mitigate | `mapCsvRows` faz `readMapped(...).slice(0, 500)` ANTES da validação (D-10) — a linha nunca reprova, o valor entra truncado em 500 |
| T-15-08 | Tampering (Stored XSS via célula de CSV) | `interesse` importado, renderizado depois no `<Input>` do form de edição | mitigate | mesma defesa do T-15-01 (React escapa; `<Input>` controlado; sem `dangerouslySetInnerHTML`); `csvRowSchema` herda `.trim().max(500)` do `leadBaseSchema` |
| T-15-09 | Tampering (campo extra em `ConfirmedImportRow` vindo do client) | `bulkImportLeads(rows)` | mitigate | `csvRowSchema.safeParse` por linha (defesa em profundidade, allowlist) antes de qualquer insert; insert campo-a-campo com `interesse: row.interesse ?? null` explícito, nunca spread cru |
| T-15-10 | Tampering (SQL injection via valor de `interesse` do CSV) | `tx.insert(leads).values(...)` | mitigate | Drizzle parametriza todo valor; nenhum SQL construído por concatenação |
| T-15-SC | Tampering (supply chain) | instalações npm/pip/cargo | accept | zero dependências novas nesta fase; `## Package Legitimacy Audit` não requerido |

Nenhuma ameaça permanece `high` após mitigação (`security_block_on: high`). Sem pacotes `[ASSUMED]`/`[SUS]`.
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm run lint`, `npm run build` — exit 0
- `npm run test:lead-actions` — exit 0 com os 3 casos novos de CSV
- Human-check (fim de fase, `human_verify_mode: end-of-phase`): em `/importar`, subir um CSV que tenha uma coluna livre (ex: "servico") → no passo de mapeamento, confirmar que "Interesse" aparece na lista com opção "— nenhuma —" → mapear "servico" → "Interesse" → confirmar a importação → abrir um lead importado e ver o valor em "Interesse". Repetir a importação sem mapear "Interesse" → leads criados sem interesse, nenhuma regressão no fluxo.
</verification>

<success_criteria>
- Critério 4 do ROADMAP (wizard de importação CSV permite mapear uma coluna para "Interesse", e o valor entra no lead importado) — satisfeito
- Column-mapper lista "Interesse" como campo opcional (não `required`)
- Importar sem mapear "Interesse" não tem regressão (leads com `interesse` nulo)
- Célula de CSV > 500 chars importa truncada, sem reprovar a linha
</success_criteria>

<output>
Criar `.planning/phases/15-campo-interesse-servi-o-desejado-no-lead/15-02-SUMMARY.md` ao concluir.
</output>
