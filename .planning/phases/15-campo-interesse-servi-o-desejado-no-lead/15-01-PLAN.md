---
phase: 15-campo-interesse-servi-o-desejado-no-lead
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/lib/validations.ts
  - scripts/migrate-interesse.cjs
  - package.json
  - scripts/verify-schema.cjs
  - src/components/lead-form-dialog.tsx
  - src/actions/lead-actions.ts
  - scripts/test-lead-actions.cjs
autonomous: true
requirements: [LEAD-06]

must_haves:
  truths:
    - "Criar um lead com o campo Interesse preenchido salva o valor e ele reaparece ao reabrir o lead para edição"
    - "Criar ou editar um lead com Interesse vazio conclui sem erro de validação"
    - "Interesse acima de 500 caracteres no formulário é rejeitado com mensagem em PT-BR"
    - "Leads criados antes da migração abrem e editam sem erro, com interesse nulo"
    - "A tabela leads tem a coluna interesse TEXT nullable no banco real (data/crm.db)"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "coluna interesse (nullable, sem default, sem índice) na tabela leads"
      contains: "interesse: text(\"interesse\")"
    - path: "src/lib/validations.ts"
      provides: "campo interesse opcional (trim, max 500, preprocess vazio->undefined) em leadBaseSchema"
      contains: "interesse"
    - path: "scripts/migrate-interesse.cjs"
      provides: "migração aditiva idempotente ALTER TABLE leads ADD COLUMN interesse, com backup e verificação"
      contains: "ADD `interesse`"
    - path: "src/components/lead-form-dialog.tsx"
      provides: "Input de linha única Interesse abaixo do campo Nicho, opcional"
      contains: "id=\"interesse\""
    - path: "src/actions/lead-actions.ts"
      provides: "persistência de interesse em createLead e updateLead (undefined -> null explícito)"
      contains: "interesse: parsed.data.interesse ?? null"
    - path: "scripts/test-lead-actions.cjs"
      provides: "cobertura automatizada de persistência, limpeza e limite de 500 de interesse"
      contains: "interesse"
  key_links:
    - from: "src/components/lead-form-dialog.tsx"
      to: "src/actions/lead-actions.ts"
      via: "FormData name=interesse -> leadSchema.safeParse -> insert/update"
      pattern: "form.register\\(\"interesse\"\\)"
    - from: "src/lib/validations.ts"
      to: "src/lib/validations.ts"
      via: "interesse fica FORA do .omit() de csvRowSchema, então flui para o import"
      pattern: "omit\\(\\{ nichoId: true, followUpDate: true, motivoPerdaId: true \\}\\)"
    - from: "scripts/migrate-interesse.cjs"
      to: "data/crm.db"
      via: "ALTER TABLE executado sob guarda de PRAGMA table_info"
      pattern: "PRAGMA table_info\\(leads\\)"
---

<objective>
Adicionar a coluna `interesse` (texto livre, opcional, nullable) à tabela `leads` e
ao contrato Zod, migrar o banco real via script `.cjs` custom, e expor o campo no
formulário de criar/editar lead com persistência nas Server Actions.

Purpose: fecha a metade "formulário + banco" de LEAD-06 — o admin registra o que
cada lead quer antes de abordar, sem atrito no cadastro.
Output: schema + Zod + migração aplicada + input no form + persistência + cobertura
automatizada. O plano 15-02 (CSV) depende deste.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-campo-interesse-servi-o-desejado-no-lead/15-SPEC.md
@.planning/phases/15-campo-interesse-servi-o-desejado-no-lead/15-CONTEXT.md
@.planning/phases/15-campo-interesse-servi-o-desejado-no-lead/15-PATTERNS.md

@src/db/schema.ts
@src/lib/validations.ts
@src/components/lead-form-dialog.tsx
@src/actions/lead-actions.ts
@scripts/backfill-origem-tipo.cjs
@scripts/migrate-motivos-perda.cjs
@scripts/verify-schema.cjs
@scripts/test-lead-actions.cjs

<interfaces>
<!-- Contratos existentes que o executor consome. Extraídos do código; não explorar. -->

Tabela `leads` (src/db/schema.ts:76-115) — colunas de texto opcional recentes seguem
`stageChangedAt`/`motivoPerdaId`: nullable, sem default físico, obrigatoriedade (quando
existe) mora no Zod. `nichoId` está na linha 90. O tipo `Lead` deriva de
`InferSelectModel<typeof leads>` (src/types/index.ts) — ganha `interesse: string | null`
sozinho.

leadBaseSchema (src/lib/validations.ts:33-71) — objeto Zod NÃO refinado (tem `.omit()`):
```ts
const leadBaseSchema = z.object({
  nome: z.string().trim().min(1, "Nome é obrigatório."),
  // ...
  origem: z.string().trim().min(1, "Origem é obrigatória."),
  notas: z.string().trim().min(1, "Notas são obrigatórias."),
  motivoPerdaId: z.preprocess(
    (v) => (v === "" || v === null || v === undefined ? undefined : v),
    z.coerce.number().int().positive().optional()
  ),
});
export const leadSchema = leadBaseSchema.refine(/* motivoPerda condicional */);
export const csvRowSchema = leadBaseSchema
  .omit({ nichoId: true, followUpDate: true, motivoPerdaId: true })
  .extend({ nichoNome: /* ... */, origemTipo: /* ... */ });
```

createLead / updateLead (src/actions/lead-actions.ts) — ambas:
`const parsed = leadSchema.safeParse(Object.fromEntries(formData))`, depois
`db.insert(leads).values({ ...parsed.data, motivoPerdaId: ... ?? null, ... })` /
`db.update(leads).set({ ...parsed.data, motivoPerdaId: ... ?? null, ... })`.
Idioma "undefined do Zod -> null explícito": `motivoPerdaId: ... ?? null` (:101, :178).

Campo "Origem" do form (src/components/lead-form-dialog.tsx:270-279) — molde EXATO:
`<Field data-invalid>` + `<FieldLabel htmlFor>` + `<Input id {...form.register("origem")}>`
+ `<FieldDescription>` + `<FieldError errors={[errors.origem]} />`. `defaultValues.origem:
lead?.origem ?? ""` (:122). Campo "Nicho" = bloco que fecha na linha 343, dentro da
`<div>` da seção "Negócio" (abre :320). `onSubmit` monta `new FormData(formRef.current)`
do DOM bruto — um `<Input name="interesse">` entra sozinho.

scripts/test-lead-actions.cjs — bootstrap em banco temporário: aplica migrations
`0000`/`0001` e depois um array `manualAlters` de `ALTER TABLE` que espelha o banco real
(:101-114). Imports dinâmicos em :133-138. `makeFormData` monta o FormData dos casos.
`makeImportRow` (:356-367) monta `ConfirmedImportRow` via spread de overrides.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Coluna interesse no schema Drizzle + campo no contrato Zod</name>
  <files>src/db/schema.ts, src/lib/validations.ts</files>
  <read_first>
    - src/db/schema.ts (tabela `leads` :76-115; doc-comments de `origemTipo` :84-86 e `motivoPerdaId` :98-99; `nichoId` :90)
    - src/lib/validations.ts (`leadBaseSchema` :33-71; doc-comment :18-32; preprocess de `motivoPerdaId` :67-70; `origem` :53; `notas` :61; `csvRowSchema` :103-108)
    - .planning/phases/15-campo-interesse-servi-o-desejado-no-lead/15-PATTERNS.md (seções "src/db/schema.ts" e "src/lib/validations.ts")
    - .planning/phases/15-campo-interesse-servi-o-desejado-no-lead/15-CONTEXT.md (D-04, D-05, D-07)
  </read_first>
  <behavior>
    - `leadSchema.parse` de um lead válido SEM `interesse` resolve sem lançar
    - `leadSchema.parse` de um lead válido com `interesse: ""` resolve e produz `interesse === undefined`
    - `leadSchema.parse` com `interesse` de 501 caracteres lança ZodError cuja mensagem contém "500"
    - `csvRowSchema.parse` de uma linha CSV válida SEM `interesse` continua resolvendo (o campo flui, não quebra imports sem a coluna)
  </behavior>
  <action>
    Em `src/db/schema.ts`, dentro do objeto de colunas de `leads`, logo após `nichoId`
    (linha 90), declarar a coluna `interesse` como `text("interesse")` — SEM `.notNull()`,
    SEM `.default()`, e sem qualquer entrada na lista de índices da tabela. Adicionar um
    bloco de doc-comment `/** ... */` imediatamente acima explicando: nullable de propósito
    (texto livre "serviço desejado", LEAD-06), sem default porque é coluna aditiva e leads
    pré-migração ficam NULL (D-06), sem índice porque não há filtro/busca por `interesse`
    (fora de escopo da Fase 15), citando D-07. Não tocar nos doc-comments de `nichos`/
    `motivosPerda` que proíbem `drizzle-kit push`/`generate`. Não editar `src/types/index.ts`.

    Em `src/lib/validations.ts`, adicionar ao objeto `leadBaseSchema` (perto de `notas`,
    linha 61) o campo `interesse` usando o mesmo idioma de `motivoPerdaId`: um `z.preprocess`
    enxuto que mapeia `"" | null | undefined` para `undefined`, encadeado a
    `z.string().trim().max(500, "<mensagem PT-BR>").optional()`. A mensagem do limite fica
    no espírito de "O interesse deve ter no máximo 500 caracteres." (copy exata é
    discricionária dentro de D-05, manter PT-BR e o tom do projeto). NÃO adicionar
    `interesse` à lista de `.omit()` de `csvRowSchema` (linha 104) — o campo tem que fluir
    automaticamente para `csvRowSchema` e `leadSchema`. Estender o doc-comment de
    `leadBaseSchema` (:18-32) mencionando `interesse` como novo campo opcional, com a mesma
    justificativa (o input do form emite `""` quando vazio; D-04 = vazio grava NULL).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` termina com exit 0
    - `grep -n 'interesse: text("interesse")' src/db/schema.ts` casa, e a mesma linha NÃO contém `.notNull(` nem `.default(`
    - `node -e` carregando `@/lib/validations` (via scripts/ts-alias-loader): `leadSchema.safeParse(<lead válido sem interesse>).success === true`; `leadSchema.safeParse({...<lead válido>, interesse: "x".repeat(501)}).success === false` com mensagem contendo `500`; `leadSchema.safeParse({...<lead válido>, interesse: ""})` -> `.data.interesse === undefined`
    - `csvRowSchema.safeParse(<linha CSV válida sem chave interesse>).success === true`
  </acceptance_criteria>
  <done>Coluna declarada no schema Drizzle e campo opcional propagando para leadSchema e csvRowSchema; tsc limpo.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: [BLOCKING] Script de migração aditiva + gate de schema</name>
  <files>scripts/migrate-interesse.cjs, package.json, scripts/verify-schema.cjs</files>
  <read_first>
    - scripts/backfill-origem-tipo.cjs (backup + `wal_checkpoint(TRUNCATE)` + ADD COLUMN idempotente via `PRAGMA table_info` + verificação de contagem antes/depois)
    - scripts/migrate-motivos-perda.cjs (bloco de verificação pós-migração, `fail()`, `beforeLeads`/`afterLeads`)
    - scripts/verify-schema.cjs (:112-125 bloco `if (tableNames.has("leads"))` com checagem de presença de coluna)
    - package.json (:5-24 bloco `scripts`, entrada `migrate:tarefas`)
    - .planning/phases/15-campo-interesse-servi-o-desejado-no-lead/15-PATTERNS.md (seção "scripts/migrate-interesse.cjs")
    - .planning/phases/15-campo-interesse-servi-o-desejado-no-lead/15-CONTEXT.md (D-06)
  </read_first>
  <action>
    Criar `scripts/migrate-interesse.cjs` (`"use strict"`) compondo os dois análogos:
    cabeçalho comentado explicando por que script custom via `better-sqlite3` e NUNCA
    `drizzle-kit push`/`generate` (D-06, precedentes destrutivos das Fases 06-01/07-01);
    `DB_PATH` de `process.env.DB_FILE_NAME ?? path.join(__dirname, "..", "data", "crm.db")`;
    `fail(msg)` que faz `console.error` com prefixo `[migrate-interesse]` e `process.exit(1)`.
    Antes de qualquer escrita: abrir o banco, `pragma("wal_checkpoint(TRUNCATE)")`, fechar,
    `fs.copyFileSync` para `${DB_PATH}.backup-<ISO timestamp com : e . trocados por ->`;
    qualquer erro chama `fail`. Capturar `beforeLeads` = `SELECT count(*) FROM leads`.
    Guarda de idempotência: `PRAGMA table_info(leads)`; se NÃO houver coluna `interesse`,
    executar `ALTER TABLE \`leads\` ADD \`interesse\` text;` — SEM `DEFAULT`, SEM `NOT NULL`,
    e SEM qualquer `UPDATE leads SET interesse ...` de backfill (D-06: a migração não toca
    dado existente, leads antigos ficam NULL). Se a coluna já existe, logar que está pulando
    o ALTER. Verificação pós-migração: `afterLeads` deve igualar `beforeLeads` (senão `fail`);
    `PRAGMA table_info(leads)` deve listar `interesse` com `type` TEXT (case-insensitive) e
    `notnull === 0` (senão `fail`); `SELECT interesse FROM leads LIMIT 1` deve rodar sem
    lançar. Logar OK e `process.exit(0)`.

    Em `package.json`, adicionar em `scripts`, junto de `"migrate:tarefas"`, a entrada
    `"migrate:interesse": "node scripts/migrate-interesse.cjs"`.

    Em `scripts/verify-schema.cjs`, dentro do bloco `if (tableNames.has("leads")) {`
    (:117-125), adicionar `if (!leadsColumns.has("interesse")) fail("coluna ausente:
    leads.interesse (Fase 15, LEAD-06)");` seguindo o idioma exato das checagens de
    `sequencia_posicao`/`motivo_perda_id`. Se o `console.log` final enumera colunas,
    incluir `interesse` na lista.

    Rodar `node scripts/migrate-interesse.cjs` contra `data/crm.db` (autônomo, sem prompt
    interativo).
  </action>
  <verify>
    <automated>npm run verify:schema</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/migrate-interesse.cjs` termina com exit 0 e cria um arquivo `data/crm.db.backup-*`
    - rodar `node scripts/migrate-interesse.cjs` uma 2ª vez termina com exit 0 e loga o caminho de idempotência (sem erro "duplicate column name")
    - `node -e` com better-sqlite3: `PRAGMA table_info(leads)` lista `interesse` com `type` `TEXT` e `notnull` `0`; `SELECT interesse FROM leads` roda sem erro
    - `npm run verify:schema` termina com exit 0
    - `grep -n '"migrate:interesse"' package.json` casa
    - contagem de linhas de `leads` idêntica antes/depois (o script chama `fail()` se divergir)
  </acceptance_criteria>
  <done>Coluna interesse presente no banco real com backup datado; verify:schema cobre a nova coluna; script idempotente.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Input Interesse no form + persistência nas Server Actions + cobertura</name>
  <files>src/components/lead-form-dialog.tsx, src/actions/lead-actions.ts, scripts/test-lead-actions.cjs</files>
  <read_first>
    - src/components/lead-form-dialog.tsx (:116-139 `defaultValues`; :270-279 campo "Origem"; :323-343 campo "Nicho"; :320 abertura da seção "Negócio"; :179-191 `onSubmit`)
    - src/actions/lead-actions.ts (:65-123 `createLead`; :125-210 `updateLead`; :94-104 `.values`; :168-195 `.set`; idioma `motivoPerdaId ?? null` :101/:178-179)
    - scripts/test-lead-actions.cjs (:76-138 bootstrap + `manualAlters` :101-114 + imports dinâmicos :133-138; :319-350 Casos 9-10; :356-367 `makeImportRow`)
    - .planning/phases/15-campo-interesse-servi-o-desejado-no-lead/15-PATTERNS.md (seções "lead-form-dialog.tsx" e "lead-actions.ts")
    - .planning/phases/15-campo-interesse-servi-o-desejado-no-lead/15-CONTEXT.md (D-01, D-02, D-03, D-04)
  </read_first>
  <behavior>
    - `createLead` com `interesse` preenchido: `SELECT interesse` da linha inserida retorna o valor
    - `updateLead` do mesmo lead com `interesse` vazio (`""`): `SELECT interesse` retorna `NULL`
    - `leadSchema.safeParse` com `interesse` de 501 chars: `success === false`, mensagem contém "500"
    - `csvRowSchema.safeParse(makeImportRow())` (sem `interesse`): `success === true`
  </behavior>
  <action>
    Em `src/components/lead-form-dialog.tsx`: adicionar `interesse: lead?.interesse ?? ""`
    ao objeto `defaultValues` (junto de `nichoId`/`notas`, D-03). Adicionar um novo `<Field>`
    logo abaixo do `</Field>` que fecha o bloco `nichoId` (linha 343), ainda dentro da `<div>`
    da seção "Negócio" (D-01): `<FieldLabel htmlFor="interesse">Interesse</FieldLabel>`, um
    `<Input id="interesse" placeholder="Ex: quer site, automação de WhatsApp...">` de linha
    única registrado com `{...form.register("interesse")}` e `aria-invalid={!!errors.interesse}`,
    um `<FieldDescription>` curto ("O que esse lead quer — serviço ou ajuda que procura.") e
    `<FieldError errors={[errors.interesse]} />`. É `<Input>`, NUNCA `<Textarea>` (D-02); sem
    `Controller` (texto puro, igual "Origem"). Texto exato do placeholder e da descrição é
    discricionário (PT-BR, tom do projeto, exemplos do mundo do usuário). Não mexer em
    `onSubmit` nem no `FormData`.

    Em `src/actions/lead-actions.ts`: no `.values({...})` de `createLead` (após o spread
    `...parsed.data`), adicionar `interesse: parsed.data.interesse ?? null`. No `.set({...})`
    de `updateLead` (após o spread `...parsed.data`), adicionar
    `interesse: parsed.data.interesse ?? null` — isso materializa "editar apagando o texto ->
    `interesse = NULL`" (`<Input>` vazio -> FormData `""` -> preprocess Zod -> `undefined` ->
    `?? null`). NÃO adicionar gate de existência nem try/catch novo (texto livre, sem FK).

    Em `scripts/test-lead-actions.cjs`: adicionar `"ALTER TABLE \`leads\` ADD \`interesse\`
    text;"` ao array `manualAlters` (espelha o banco real pós-Task 2; o bloco já tolera
    "duplicate column name"). Se `makeFormData` filtra chaves conhecidas, incluir `interesse`.
    Adicionar 4 casos novos após os Casos 9-10: (a) `createLead` com `interesse` preenchido
    -> ler `SELECT interesse` de volta = valor; (b) `updateLead` do mesmo lead com
    `interesse: ""` -> `SELECT interesse` = `NULL`; (c) `leadSchema.safeParse` com `interesse`
    de 501 chars -> `success === false` e a mensagem inclui "500"; (d)
    `csvRowSchema.safeParse(makeImportRow())` -> `success === true`. Tolerar o `throw` de
    `revalidatePath` fora do runtime Next igual aos Casos 10/12 existentes.
  </action>
  <verify>
    <automated>npm run test:lead-actions</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exit 0
    - `npm run lint` exit 0 (ou só os falsos-positivos já suprimidos no projeto)
    - `npm run test:lead-actions` exit 0 com as 4 novas linhas `OK ...` (persistência, limpeza, limite 500, csvRowSchema sem interesse)
    - `grep -n 'id="interesse"' src/components/lead-form-dialog.tsx` casa; `grep -n '<Textarea id="interesse"' src/components/lead-form-dialog.tsx` NÃO casa
    - `grep -c 'interesse: parsed.data.interesse ?? null' src/actions/lead-actions.ts` retorna `2`
    - `npm run build` exit 0
  </acceptance_criteria>
  <done>Campo Interesse visível e opcional no form de criar/editar; createLead/updateLead persistem e limpam o valor; 4 casos automatizados verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador (form de lead) → Server Action `createLead`/`updateLead` | input não confiável cruza aqui: texto livre `interesse` no `FormData` |
| `scripts/migrate-interesse.cjs` → arquivo `data/crm.db` | DDL sobre banco de produção real (dados de leads reais) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering (Stored XSS via texto livre) | `interesse` renderizado depois no `<Input>` do form de edição | mitigate | React escapa por padrão; valor sempre via `form.register`/`defaultValues` de um `<Input>` controlado, nunca `dangerouslySetInnerHTML`; `leadBaseSchema.interesse` faz `.trim().max(500)` |
| T-15-02 | Denial of Service (payload gigante no campo) | campo `interesse` do form → `createLead`/`updateLead` | mitigate | `z.string().max(500)` no `leadSchema` rejeita no servidor (`safeParse`) antes de qualquer insert/update |
| T-15-03 | Tampering (mass assignment / chave forjada no FormData) | `Object.fromEntries(formData)` em `createLead`/`updateLead` | mitigate | `leadSchema.safeParse` é allowlist (só campos declarados); gravação explícita `interesse: parsed.data.interesse ?? null`, nunca spread cru de `formData` no `.values`/`.set` |
| T-15-04 | Tampering (SQL injection no DDL ou no valor) | `scripts/migrate-interesse.cjs` + queries Drizzle de lead | mitigate | DDL é string literal estática sem interpolação; Drizzle parametriza todo valor de `interesse` |
| T-15-05 | Denial of Service (migração corrompe/perde dados) | `ALTER TABLE` sobre `data/crm.db` real | mitigate | backup com `wal_checkpoint(TRUNCATE)` + `copyFileSync` ANTES de qualquer escrita; verificação de contagem de linhas antes/depois com `fail()`; idempotência via `PRAGMA table_info` |
| T-15-06 | Information disclosure (PII do lead) | coluna `interesse` | accept | ferramenta solo de um único admin sem autenticação por constraint explícita do PROJECT.md; `interesse` não é exposto em nenhuma rota/endpoint novo (só no form já existente) |
| T-15-SC | Tampering (supply chain) | instalações npm/pip/cargo | accept | zero dependências novas nesta fase — a única mudança em `package.json` é uma linha no bloco `scripts`; `## Package Legitimacy Audit` não requerido (nenhum install) |

Nenhuma ameaça permanece `high` após mitigação (`security_block_on: high`). Sem pacotes `[ASSUMED]`/`[SUS]` — nenhum checkpoint de legitimidade necessário.
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm run lint`, `npm run build` — exit 0
- `npm run verify:schema` — exit 0 (cobre `leads.interesse`)
- `npm run test:lead-actions` — exit 0 com os 4 casos novos
- `data/crm.db` tem coluna `interesse` TEXT nullable (`PRAGMA table_info(leads)`); backup datado presente em `data/`
- Human-check (fim de fase, `human_verify_mode: end-of-phase`): abrir `/leads` → "Novo lead" → preencher "Interesse" → salvar → reabrir o lead em edição e confirmar que o valor aparece; criar outro lead deixando "Interesse" vazio → salva sem erro; editar um lead apagando o "Interesse" → salva e o campo volta vazio.
</verification>

<success_criteria>
- Critério 1 do ROADMAP (form tem campo opcional "Interesse" que salva e persiste) — satisfeito
- Critério 2 do ROADMAP (campo aparece na tela de detalhe/edição com o valor salvo) — satisfeito (o `lead-form-dialog.tsx` é a tela de edição)
- Critério 3 do ROADMAP (criar/editar sem preencher funciona, nunca bloqueia submit) — satisfeito
- `leads.interesse` TEXT nullable no schema Drizzle e no banco real
- Interesse > 500 chars rejeitado com mensagem PT-BR no caminho do formulário
- Leads pré-migração abrem e editam com `interesse` nulo, sem erro
</success_criteria>

<output>
Criar `.planning/phases/15-campo-interesse-servi-o-desejado-no-lead/15-01-SUMMARY.md` ao concluir.
</output>
