---
phase: 25-tour-guiado-do-crm
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - src/components/tour-guiado.tsx
  - src/components/app-sidebar.tsx
  - src/app/layout.tsx
  - src/components/reiniciar-tour-button.tsx
  - src/app/configuracoes/page.tsx
autonomous: true
requirements: [TUTORIAL-01, TUTORIAL-02, TUTORIAL-03, TUTORIAL-05]
user_setup: []

must_haves:
  truths:
    - "Na primeira visita (sem a flag em localStorage), o tour abre sozinho em qualquer rota e apresenta as 5 telas principais"
    - "O usuário fecha ou pula o tour em qualquer passo, sem ser obrigado a terminar"
    - "Depois de concluído OU pulado, o tour não reaparece sozinho no próximo acesso"
    - "O usuário reinicia o tour quando quiser por um botão fixo em /configuracoes"
    - "O tooltip do tour usa os tokens OKLCH da marca e fica legível nos temas claro e escuro"
  artifacts:
    - path: "src/components/tour-guiado.tsx"
      provides: "Wrapper cliente do Joyride, dono do estado run e da leitura/gravação da flag"
      exports: ["TourGuiado"]
      min_lines: 40
    - path: "src/components/reiniciar-tour-button.tsx"
      provides: "Ponto de acesso fixo para reiniciar o tour (TUTORIAL-03)"
      exports: ["ReiniciarTourButton"]
    - path: "src/components/app-sidebar.tsx"
      provides: "Os 5 alvos data-tour nos itens de navegação"
      contains: "data-tour"
    - path: "src/app/layout.tsx"
      provides: "Montagem única do TourGuiado, irmão de {children}"
      contains: "TourGuiado"
    - path: "src/app/configuracoes/page.tsx"
      provides: "Renderização do botão de reinício"
      contains: "ReiniciarTourButton"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/tour-guiado.tsx"
      via: "montagem como irmão de <AppSidebar /> e {children}, dentro do ThemeProvider"
      pattern: "<TourGuiado\\s*/>"
    - from: "src/components/tour-guiado.tsx"
      to: "react-joyride"
      via: "render real do componente do Joyride com steps/run/callback ligados (nome de export confirmado no 25-01-SUMMARY)"
      pattern: "<Joyride"
    - from: "src/components/tour-guiado.tsx"
      to: "react-joyride (options.buttons)"
      via: "token de pular declarado explicitamente na lista de botões — mecanismo de TUTORIAL-02"
      pattern: "\"skip\""
    - from: "src/components/tour-guiado.tsx"
      to: "src/lib/tour-steps.ts"
      via: "prop steps do Joyride"
      pattern: "TOUR_STEPS"
    - from: "src/components/tour-guiado.tsx"
      to: "src/lib/tour-persistence.ts"
      via: "deveGravarComoVisto / lerTourVisto / gravarTourVisto com window.localStorage"
      pattern: "tour-persistence"
    - from: "src/lib/tour-steps.ts"
      to: "src/components/app-sidebar.tsx"
      via: "seletor [data-tour=\"nav-*\"] resolvido no DOM da sidebar"
      pattern: "data-tour=\\{item.tourId\\}"
    - from: "src/components/reiniciar-tour-button.tsx"
      to: "src/lib/tour-persistence.ts"
      via: "limparTourVisto + reload"
      pattern: "limparTourVisto"
---

<objective>
Ligar o tour na interface real: montar o `<Joyride>` uma única vez no layout raiz, marcar os 5 itens da sidebar como alvos, e entregar o ponto de acesso fixo de reinício em `/configuracoes`. Com este plano a fase fica observável de ponta a ponta — TUTORIAL-01 (aparece na 1ª visita, 5 telas), TUTORIAL-02 (pula/fecha em qualquer passo), TUTORIAL-03 (reinicia quando quiser) e TUTORIAL-05 (persiste e não reaparece sozinho).

Purpose: toda a lógica pura e toda a incerteza de API já foram fechadas no plano 25-01. Aqui é só composição — wrapper cliente, atributos estáticos e wiring — com o menor risco possível de retrabalho.

Output: `TourGuiado` montado no layout, 5 `data-tour` na sidebar, `ReiniciarTourButton` em `/configuracoes`.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-tour-guiado-do-crm/25-RESEARCH.md
@.planning/phases/25-tour-guiado-do-crm/25-UI-SPEC.md
@.planning/phases/25-tour-guiado-do-crm/25-PATTERNS.md
@.planning/phases/25-tour-guiado-do-crm/25-01-SUMMARY.md
@src/components/theme-toggle.tsx
@src/components/app-sidebar.tsx
@src/app/layout.tsx
@src/app/configuracoes/page.tsx
</context>

<interfaces>
<!-- Contratos JÁ criados pelo plano 25-01 — use direto, não reexplore o codebase. -->

De `src/lib/tour-persistence.ts`:
- `TOUR_STORAGE_KEY: string` (`"tourVisto"`)
- `deveGravarComoVisto(status: string): boolean`
- `lerTourVisto(storage: Pick<Storage, "getItem">): boolean`
- `gravarTourVisto(storage: Pick<Storage, "setItem">): void`
- `limparTourVisto(storage: Pick<Storage, "removeItem">): void`

De `src/lib/tour-steps.ts`:
- `TOUR_STEPS` — 5 passos, alvos `[data-tour="nav-dashboard"]`, `[data-tour="nav-leads"]`, `[data-tour="nav-pipeline"]`, `[data-tour="nav-campanhas"]`, `[data-tour="nav-relatorios"]`

De `react-joyride@3.2.0` — **os nomes reais de export/prop/tipo/status estão registrados na seção de decisões de `25-01-SUMMARY.md`, lidos do `.d.ts` publicado**. Use os nomes de lá, não os de exemplos v2 de treinamento. Se o SUMMARY divergir do que o código exigir, o `.d.ts` em `node_modules/react-joyride` é a autoridade final.

De `src/components/ui/button.tsx` (já no repo):
- `Button` com `variant="outline"` disponível (`bg-background hover:bg-muted ...`)

Padrão de guard de montagem (copiar de `src/components/theme-toggle.tsx:13-24`):
- `const [mounted, setMounted] = useState(false)` + `useEffect(() => setMounted(true), [])` precedido de `// eslint-disable-next-line react-hooks/set-state-in-effect` com comentário citando a decisão 07-02 do STATE.md
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Componente TourGuiado (wrapper cliente do Joyride)</name>
  <files>src/components/tour-guiado.tsx</files>
  <read_first>
    - .planning/phases/25-tour-guiado-do-crm/25-01-SUMMARY.md (seção de decisões: nomes reais de export, prop de callback, tipo do payload, constantes de status, shape de `options` — lidos do `.d.ts`)
    - node_modules/react-joyride/dist/index.d.ts (ou o caminho apontado pelo campo `types`/`exports` do pacote) — consultar sempre que o SUMMARY não cobrir um campo
    - src/components/theme-toggle.tsx (linhas 1-30: guard `mounted` + `useEffect` + o `eslint-disable-next-line react-hooks/set-state-in-effect` com o comentário que cita a decisão 07-02)
    - src/lib/tour-steps.ts (criado no 25-01)
    - src/lib/tour-persistence.ts (criado no 25-01)
    - .planning/phases/25-tour-guiado-do-crm/25-UI-SPEC.md (seções `## Color` e `## Estados e Interação` — mapeamento token→prop e as 10 regras de contrato)
    - .planning/phases/25-tour-guiado-do-crm/25-PATTERNS.md (seção `src/components/tour-guiado.tsx`)
  </read_first>
  <action>
    Criar `src/components/tour-guiado.tsx` com a diretiva `"use client"` no topo, exportando `TourGuiado` (componente sem props, folha — não envolve `children`).

    Estado e ciclo de vida: dois `useState` — `mounted` (boolean) e `run` (boolean, inicial `false`, porque na v3 o `run` do Joyride nasce `false` e precisa ser controlado explicitamente pela app, Pitfall 2 do RESEARCH). Um único `useEffect` de montagem que seta `mounted` e, se `lerTourVisto(window.localStorage)` for `false`, seta `run` para `true`. Precedê-lo de `// eslint-disable-next-line react-hooks/set-state-in-effect` com o mesmo comentário-justificativa de `theme-toggle.tsx` citando a decisão 07-02 do STATE.md — é o falso-positivo do React Compiler já aceito neste projeto. Enquanto `!mounted`, retornar `null` (nunca um esqueleto visível: o tour não pode existir no SSR nem na 1ª pintura, senão dá hydration mismatch ao ler `localStorage`).

    Handler de evento: função que recebe o payload do callback (tipo real confirmado no 25-01-SUMMARY) e, se `deveGravarComoVisto(payload.status)` for verdadeiro, chama `gravarTourVisto(window.localStorage)` e seta `run` para `false`. Este é o ponto exato de TUTORIAL-02 + TUTORIAL-05: os DOIS caminhos de saída (concluir e pular/fechar) passam por `deveGravarComoVisto`, que já os cobre e já está testado pelo harness do 25-01 — não duplicar a comparação de status literal aqui dentro.

    Render: o componente do Joyride (nome de export confirmado no SUMMARY), REALMENTE renderizado no JSX de retorno — não basta importar —, recebendo `steps={TOUR_STEPS}`, `run={run}` (nomear o estado exatamente `run`, para o gate de grep abaixo casar), modo contínuo ligado (passo a passo com botão de avanço, não beacons isolados), e o prop de callback (nome confirmado no SUMMARY — na v3 é `onEvent`, não `callback`) apontando para o handler.

    Configuração visual — todos os valores vêm da tabela `## Color` do 25-UI-SPEC.md, **sempre como `var(--token)` ou `color-mix`, nunca hex/rgb literal** (regra dura do projeto; o gate `npm run verify:brand` reprova hardcode de cor):
    - cor primária (botão de avanço) → `var(--primary)`
    - fundo do tooltip e da seta → `var(--popover)`
    - texto do tooltip → `var(--popover-foreground)`
    - overlay → `color-mix(in oklch, var(--foreground) 10%, transparent)` — 10%, paridade com o `bg-foreground/10` já usado no overlay de `dialog.tsx`; NÃO os 40% do rascunho do RESEARCH
    - raio do tooltip → equivalente a `rounded-lg` (10)
    - botões: incluir explicitamente o token de pular na lista `options.buttons` (o default da v3 é só voltar/fechar/primário — Pitfall 4). **Este é o mecanismo que satisfaz TUTORIAL-02**, o requisito que define a fase: sem o `"skip"` declarado, o usuário não tem botão de pular e o tour passa a empurrar para a conclusão. Rótulos travados pelo `## Copywriting Contract`: "Próximo", "Concluir" no último passo, "Voltar", "Pular tour", contador "Passo {n} de 5". Se a v3 não expuser um prop de locale/rótulos com esses nomes exatos, usar o que o `.d.ts` oferecer e registrar a divergência como decisão D-25-XX no SUMMARY — não inventar prop, e não abrir mão do botão de pular.
    - beacon: garantir que o tooltip abra direto ao chegar em cada passo (Pitfall 3). Se `disableBeacon` por passo existiu e foi aplicado no 25-01, nada a fazer aqui; se não existir na v3, usar o controle global de beacon disponível em `options` (`beaconTrigger`/`skipBeacon` conforme o `.d.ts`).
    - NÃO usar `portalElement` customizado nesta fase (regra 8 do UI-SPEC: os 5 alvos são itens da sidebar, nenhum dentro de `Dialog`/`Popover`, então não há colisão com o `isolate z-50` do Base UI).

    Não adicionar transição customizada nenhuma (regra 9 do UI-SPEC) e não reimplementar foco/teclado (regra 6: o Joyride já entrega focus trap, navegação por teclado e ARIA).
  </action>
  <acceptance_criteria>
    - `grep -c '"use client"' src/components/tour-guiado.tsx` retorna 1 e a diretiva é a primeira linha do arquivo.
    - `grep -c 'if (!mounted) return null' src/components/tour-guiado.tsx` retorna 1 — guard de hidratação presente.
    - `grep -c 'react-hooks/set-state-in-effect' src/components/tour-guiado.tsx` retorna 1 — supressão documentada, mesmo precedente de `theme-toggle.tsx`.
    - `grep -c 'deveGravarComoVisto' src/components/tour-guiado.tsx` retorna ≥1 e `grep -c "=== \"finished\"\|=== \"skipped\"" src/components/tour-guiado.tsx` retorna 0 — a decisão de status mora na função pura testada, não duplicada no componente.
    - `grep -c 'TOUR_STEPS' src/components/tour-guiado.tsx` retorna ≥1.

    **Prova mecânica de que o Joyride está de fato RENDERIZADO e LIGADO (não só importado ou citado em comentário) — TUTORIAL-01/02/05:**
    - `grep -c '<Joyride' src/components/tour-guiado.tsx` retorna ≥1 — o componente do Joyride aparece no JSX. `Joyride` é o nome de export esperado na v3; se o `25-01-SUMMARY.md` registrou outro nome lido do `.d.ts`, substitua `Joyride` por esse nome real NESTE grep e nos demais abaixo, e registre a substituição no `25-02-SUMMARY.md`.
    - `grep -F -c 'run={run}' src/components/tour-guiado.tsx` retorna ≥1 — o prop `run` está ligado ao estado local `run` (que só vira `true` depois do guard de montagem e da leitura da flag). Sem isso o tour nunca abre: na v3 o `run` nasce `false` (Pitfall 2).
    - `grep -F -c -e 'onEvent={' -e 'callback={' src/components/tour-guiado.tsx` retorna ≥1 — o prop de callback de eventos confirmado no `25-01-SUMMARY.md` (`onEvent` na v3, `callback` na v2) está ligado ao handler. Sem isso a flag nunca é gravada e TUTORIAL-05 quebra silenciosamente, sem erro de build.
    - `grep -F -c '"skip"' src/components/tour-guiado.tsx` retorna ≥1 **e** `grep -Ec 'buttons *: *\[' src/components/tour-guiado.tsx` retorna ≥1 — o token de pular está declarado e existe uma lista `buttons` para ele morar. Este é o mecanismo EXATO de TUTORIAL-02 ("pula/fecha em qualquer passo, nada obriga a concluir"): o default da v3 não inclui `skip` (Pitfall 4), então a ausência deste literal é a fase falhando no requisito que a define. Se o `.d.ts` nomear o token ou a chave de outra forma, use o literal real do `25-01-SUMMARY.md`, ajuste o grep e registre a troca no `25-02-SUMMARY.md`.
    - Confirmar por leitura do arquivo após a edição que o `"skip"` acima está DENTRO do array `buttons` do objeto `options` (o grep prova presença; a leitura prova o aninhamento) e que vale para os 5 passos, não só para o primeiro.
    - `grep -F -c 'Pular tour' src/components/tour-guiado.tsx` retorna exatamente 1 — o rótulo travado pelo `## Copywriting Contract` do UI-SPEC, literal e uma única vez (não "Pular", não "Sair", não duplicado).

    **Cor, segurança e gates:**
    - `grep -c 'var(--primary)\|var(--popover)\|var(--popover-foreground)\|var(--foreground)' src/components/tour-guiado.tsx` retorna ≥4 — os 4 papéis de cor do UI-SPEC estão tokenizados.
    - `grep -Ec '#[0-9a-fA-F]{3,8}\b|rgba?\(' src/components/tour-guiado.tsx` retorna 0 — zero cor literal.
    - `grep -c 'dangerouslySetInnerHTML' src/components/tour-guiado.tsx` retorna 0 (T-25-01).
    - `npm run verify:brand` sai 0 (gate de marca do projeto não regride).
    - `npx tsc --noEmit` sai 0 e `npm run lint` sai 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run verify:brand && grep -c '<Joyride' src/components/tour-guiado.tsx && grep -F -c 'run={run}' src/components/tour-guiado.tsx && grep -F -c -e 'onEvent={' -e 'callback={' src/components/tour-guiado.tsx && grep -F -c '"skip"' src/components/tour-guiado.tsx && grep -Ec 'buttons *: *\[' src/components/tour-guiado.tsx && grep -F -c 'Pular tour' src/components/tour-guiado.tsx</automated>
  </verify>
  <done>`TourGuiado` existe, é cliente, tem guard de montagem, RENDERIZA o Joyride com `steps`/`run`/callback ligados, declara o token de pular em `options.buttons` (TUTORIAL-02), decide `run` a partir da flag persistida, grava a flag nos dois caminhos de saída via a função pura do 25-01, e configura o Joyride 100% com tokens OKLCH.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Alvos data-tour na sidebar + montagem no layout raiz</name>
  <files>src/components/app-sidebar.tsx, src/app/layout.tsx</files>
  <read_first>
    - src/components/app-sidebar.tsx (linhas 22-35: `NAV_ITEMS` `as const`; linhas 61-75: o `<Link>` renderizado dentro do `map`)
    - src/app/layout.tsx (linhas 42-53: `ThemeProvider` envolvendo `AppSidebar`, `<main>{children}</main>` e `Toaster`)
    - src/lib/tour-steps.ts (os 5 seletores que precisam casar exatamente com os `data-tour` escritos aqui)
    - .planning/phases/25-tour-guiado-do-crm/25-PATTERNS.md (seções `src/components/app-sidebar.tsx` e `src/app/layout.tsx` — diffs exatos já mapeados)
  </read_first>
  <action>
    Em `src/components/app-sidebar.tsx`: adicionar um campo `tourId` a CINCO entradas de `NAV_ITEMS`, sem renomear nem remover nenhum campo existente (`href`/`label`/`icon` ficam intactos) e sem reordenar a lista: `/` → `"nav-dashboard"`, `/leads` → `"nav-leads"`, `/pipeline` → `"nav-pipeline"`, `/campanhas` → `"nav-campanhas"`, `/relatorios` → `"nav-relatorios"`. As outras sete entradas (Importar, Mapa de Nichos, Templates, Nichos, Motivos de Perda, Lixeira, Configurações) NÃO ganham `tourId` — o escopo travado do ROADMAP é 5 telas. O array segue `as const`.

    No `<Link>` do `map`, adicionar `data-tour={item.tourId}` de forma incondicional: para os itens sem `tourId` o valor é `undefined` e o React simplesmente não emite o atributo — mais simples e mais seguro que um spread condicional. Nenhuma outra mudança no arquivo: nada de className novo, nada de handler novo, nada de mudança na lógica de `isActive`.

    Em `src/app/layout.tsx`: importar `TourGuiado` de `@/components/tour-guiado`, ao lado dos imports já existentes de `AppSidebar`/`ThemeProvider`, e renderizar `<TourGuiado />` como irmão direto de `<AppSidebar />` e do `<main>`, DENTRO do `ThemeProvider` (para que os tokens `var(--primary)` etc. já estejam resolvidos no contexto de tema correto) e FORA de `{children}` (para montar uma única vez e sobreviver a toda navegação — é a razão arquitetural inteira de o tour ancorado na sidebar não precisar de `router.push`). Colocar entre `<AppSidebar />` e `<main>`. Não mexer em `metadata`, fontes, skip-link, `Toaster` ou nas props do `ThemeProvider`.
  </action>
  <acceptance_criteria>
    - `grep -c 'tourId:' src/components/app-sidebar.tsx` retorna exatamente 5.
    - `grep -c 'nav-dashboard\|nav-leads\|nav-pipeline\|nav-campanhas\|nav-relatorios' src/components/app-sidebar.tsx` retorna 5.
    - `grep -c 'data-tour={item.tourId}' src/components/app-sidebar.tsx` retorna exatamente 1 (um único ponto de emissão, no `<Link>` do map).
    - `grep -c 'href: "/importar"\|href: "/mapa-de-nichos"\|href: "/templates"\|href: "/nichos"\|href: "/motivos-perda"\|href: "/lixeira"\|href: "/configuracoes"' src/components/app-sidebar.tsx` retorna 7 — as 7 entradas fora do tour continuam na lista, intactas.
    - `grep -c '<TourGuiado />' src/app/layout.tsx` retorna 1 e `grep -c 'from "@/components/tour-guiado"' src/app/layout.tsx` retorna 1.
    - O `<TourGuiado />` aparece depois de `<AppSidebar />` e antes de `<main`, dentro do bloco `<ThemeProvider ...>` — confirmar por leitura do arquivo após a edição.
    - Os 5 seletores de `src/lib/tour-steps.ts` casam 1:1 com os 5 `tourId` da sidebar (comparação literal dos slugs, sem typo).
    - `npx tsc --noEmit` sai 0 e `npm run lint` sai 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && grep -c 'tourId:' src/components/app-sidebar.tsx && grep -c '<TourGuiado />' src/app/layout.tsx</automated>
  </verify>
  <done>Os 5 itens de navegação emitem `data-tour` com os slugs exatos esperados por `TOUR_STEPS`, e `TourGuiado` está montado uma única vez no layout raiz, irmão de `{children}`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Botão fixo de reinício do tour em /configuracoes</name>
  <files>src/components/reiniciar-tour-button.tsx, src/app/configuracoes/page.tsx</files>
  <read_first>
    - src/app/configuracoes/page.tsx (arquivo inteiro, 13 linhas: container `flex flex-col gap-6`, H1 e `<ConfiguracoesForm config={config} />`)
    - src/components/configuracoes-form.tsx (linha ~9 e ~291: import e uso do `Button` com `variant="outline"` e `type="button"`)
    - src/components/ui/button.tsx (linhas 10-21: variantes disponíveis, confirmar `outline`)
    - src/lib/tour-persistence.ts (assinatura de `limparTourVisto`)
    - .planning/phases/25-tour-guiado-do-crm/25-UI-SPEC.md (`## Copywriting Contract` — cópia travada do botão e da legenda; `## Typography` — `text-xs text-muted-foreground` para a legenda)
    - .planning/phases/25-tour-guiado-do-crm/25-PATTERNS.md (seção `src/components/reiniciar-tour-button.tsx` — opção (b) recomendada: irmão do form, sem tocar na máquina de RHF/useActionState)
  </read_first>
  <action>
    Criar `src/components/reiniciar-tour-button.tsx` com `"use client"` no topo, exportando `ReiniciarTourButton` (sem props). Um handler de clique que chama `limparTourVisto(window.localStorage)` e em seguida `window.location.reload()` — o reload é a estratégia aprovada pelo 25-RESEARCH.md (Open Question 3, resolvida) e pela regra 4 do UI-SPEC: como `TourGuiado` mora no layout raiz e o botão mora em `/configuracoes`, remontar a árvore é mais barato e mais robusto do que introduzir um Context global só para sincronizar um booleano. É uma ação rara e intencional; perder posição de scroll é aceitável.

    Render: um `<div className="flex flex-col gap-1">` contendo o `Button` do projeto (`@/components/ui/button`) com `type="button"` e `variant="outline"`, rótulo **"Rever tour do CRM"** (cópia travada — não é "Reiniciar tour", não é "Ver tour de novo"), e abaixo um `<p className="text-xs text-muted-foreground">` com a legenda travada **"Reapresenta as 5 telas principais do CRM. Você pode pular a qualquer momento."**. Sem ícone (o UI-SPEC não pede nenhum), sem toast, sem modal de confirmação — a regra "Destrutiva: N/A" do UI-SPEC é explícita: a ação não apaga dado nenhum, só limpa uma flag de UI.

    Em `src/app/configuracoes/page.tsx`: importar `ReiniciarTourButton` de `@/components/reiniciar-tour-button` e renderizá-lo como irmão de `<ConfiguracoesForm config={config} />`, dentro do mesmo container `flex flex-col gap-6`, logo depois do form. A página continua Server Component — o botão é uma folha `"use client"` pequena, exatamente como `ThemeToggle` dentro da sidebar. NÃO tocar em `configuracoes-form.tsx` (opção (a) do PATTERNS.md foi descartada: acoplaria a mudança à máquina de `useActionState`/react-hook-form desse arquivo sem nenhum ganho).
  </action>
  <acceptance_criteria>
    - `grep -c '"use client"' src/components/reiniciar-tour-button.tsx` retorna 1 na primeira linha.
    - `grep -F -c 'Rever tour do CRM' src/components/reiniciar-tour-button.tsx` retorna 1 (cópia travada, literal).
    - `grep -F -c 'Reapresenta as 5 telas principais do CRM. Você pode pular a qualquer momento.' src/components/reiniciar-tour-button.tsx` retorna 1.
    - `grep -c 'variant="outline"' src/components/reiniciar-tour-button.tsx` retorna 1 e `grep -c 'type="button"' src/components/reiniciar-tour-button.tsx` retorna 1.
    - `grep -c 'limparTourVisto' src/components/reiniciar-tour-button.tsx` retorna 1 e `grep -c 'window.location.reload()' src/components/reiniciar-tour-button.tsx` retorna 1.
    - `grep -c 'text-xs text-muted-foreground' src/components/reiniciar-tour-button.tsx` retorna 1.
    - `grep -c 'ReiniciarTourButton' src/app/configuracoes/page.tsx` retorna 2 (import + uso).
    - `git diff --name-only` NÃO lista `src/components/configuracoes-form.tsx`.
    - `npx tsc --noEmit` sai 0, `npm run lint` sai 0, `npm run verify:brand` sai 0.
    - `npm run build` sai 0 com o host livre de outros processos pesados (gate de suíte completa do projeto).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run verify:brand && npm run build</automated>
    <human-check>
      UAT visual (fim de fase, não-bloqueante — `human_verify_mode: end-of-phase`, mesmo padrão das Fases 22-24). Os gates de grep da Task 1 já provam mecanicamente que o botão de pular EXISTE na configuração; este UAT confirma a experiência, não substitui aquela prova. Rodar `npm run dev` e:
      1. Limpar a chave `tourVisto` no DevTools (Application → Local Storage) e recarregar `/` → o tour abre sozinho no passo 1 ("Follow-ups"), sem exigir clique numa bolinha, e o realce cai sobre o item certo da sidebar.
      2. Avançar os 5 passos com "Próximo" e confirmar a ordem Follow-ups → Leads → Pipeline → Campanhas de nicho → Relatórios, com o último botão escrito "Concluir".
      3. Recarregar: o tour NÃO reaparece.
      4. Limpar a flag de novo, abrir o tour, clicar "Pular tour" no passo 2, recarregar: o tour NÃO reaparece (este é o caminho que o Pitfall 4 do RESEARCH costuma quebrar).
      5. Ir em `/configuracoes`, clicar "Rever tour do CRM" → a página recarrega e o tour reinicia do passo 1.
      6. Repetir os passos 1-2 nos dois temas (claro e escuro, pelo toggle no rodapé da sidebar) e confirmar contraste legível do tooltip, botão de avanço na cor da marca e overlay suave (não um preto chapado).
    </human-check>
  </verify>
  <done>`/configuracoes` tem o botão "Rever tour do CRM" com a legenda travada; clicar limpa a flag e reinicia o tour; `configuracoes-form.tsx` não foi tocado; build verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador do usuário (localStorage) → componente cliente | `TourGuiado` e `ReiniciarTourButton` leem/escrevem uma preferência que o próprio usuário pode editar no DevTools |
| dados estáticos do tour → DOM (portal do Joyride) | O texto dos passos é renderizado num portal fora da árvore normal da página |
| biblioteca de terceiro → DOM da aplicação | O Joyride monta overlay/spotlight/tooltip sobre toda a UI |

Nenhuma fronteira de rede, sessão ou banco é atravessada: zero Server Action nova, zero rota nova, zero coluna nova, zero input validado — confirmado pela seção `## Security Domain` do 25-RESEARCH.md.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering / Information Disclosure | `src/components/tour-guiado.tsx` (renderização do conteúdo dos passos) | mitigate | O componente só repassa `TOUR_STEPS` (strings literais estáticas do 25-01) para o Joyride; proibido `dangerouslySetInnerHTML` e proibido interpolar dado de lead/campanha/URL/formulário no conteúdo dos passos. Checado por `grep -c 'dangerouslySetInnerHTML' src/components/tour-guiado.tsx` == 0 nas acceptance criteria da Task 1 |
| T-25-02 | Tampering | flag `tourVisto` em `localStorage`, lida por `TourGuiado` e limpa por `ReiniciarTourButton` | accept | Ferramenta solo, sem autenticação nem multiusuário por design (CLAUDE.md). A flag não porta autorização nem dado de negócio; adulterá-la só faz o tour aparecer ou não para o próprio dono da máquina. Invariante a preservar: nunca usar essa chave como gate de acesso a rota, dado ou funcionalidade |
| T-25-04 | Denial of Service (UI) | overlay/spotlight do Joyride montado no layout raiz | accept | Um alvo `data-tour` ausente poderia, na teoria, deixar um overlay cobrindo a UI. Risco residual mínimo: os 5 alvos são itens fixos da sidebar, montada uma única vez no layout e presente em toda rota (não há "target not found" no escopo desta fase, regra `Error state: N/A` do UI-SPEC), e TUTORIAL-02 garante que o usuário sempre tem botão de fechar/pular disponível em qualquer passo — presença do `"skip"` em `options.buttons` provada por grep na Task 1 |
| T-25-SC | Tampering | `react-joyride` (cadeia de suprimentos) | mitigate | Já mitigado no plano 25-01 (checkpoint humano bloqueante antes do install + versão pinada exata `3.2.0`). Este plano não instala nem atualiza pacote nenhum — `package.json` e `package-lock.json` não estão em `files_modified` |
</threat_model>

<verification>
Com o host livre de outros processos pesados (4GB de RAM — precedente das Fases 18-20):

1. `npx tsc --noEmit` → sai 0
2. `npm run lint` → sai 0
3. `npm run verify:brand` → sai 0 (nenhuma cor hardcoded introduzida pelo tour)
4. `npm run test:tour-persistence` → sai 0 (não regrediu o que o 25-01 entregou)
5. `npm run build` → sai 0, com todas as rotas existentes ainda compilando
6. `npm ls react-joyride` → única biblioteca de tour instalada (TUTORIAL-04)
7. Casamento literal dos 5 slugs entre `src/lib/tour-steps.ts` e `src/components/app-sidebar.tsx`
8. Cadeia de greps da Task 1 → o Joyride está renderizado (`<Joyride`), com `run={run}`, com o prop de callback ligado, com `"skip"` em `options.buttons` e com o rótulo "Pular tour" — prova mecânica de TUTORIAL-02 sem depender do UAT
9. UAT humano de fim de fase (6 checagens do `<human-check>` da Task 3), claro e escuro — não-bloqueante, mesmo padrão das Fases 22-24
</verification>

<success_criteria>
- TUTORIAL-01: com a flag ausente, o tour abre sozinho e apresenta, na ordem, Follow-ups, Leads, Pipeline, Campanhas de nicho e Relatórios, cada passo explicando o que a tela faz
- TUTORIAL-02: há botão de pular e de fechar em qualquer passo; nada obriga a concluir; nenhum modal de confirmação atrapalha a saída — provado mecanicamente pelos greps de `"skip"`/`buttons`/"Pular tour" da Task 1 e confirmado no UAT
- TUTORIAL-03: `/configuracoes` tem o botão fixo "Rever tour do CRM", que reinicia o tour do passo 1 a qualquer momento
- TUTORIAL-05: concluir OU pular grava a flag; o tour não reaparece sozinho no acesso seguinte, e continua sempre reativável pelo botão
- O tooltip usa `var(--popover)`/`var(--popover-foreground)`/`var(--primary)` e overlay `color-mix` sobre `var(--foreground)` a 10% — legível nos dois temas, zero cor hardcoded
- Gates verdes: `tsc --noEmit`, `lint`, `verify:brand`, `test:tour-persistence`, `build`
</success_criteria>

<output>
Create `.planning/phases/25-tour-guiado-do-crm/25-02-SUMMARY.md` when done
</output>
