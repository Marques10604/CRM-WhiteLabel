---
phase: 25-tour-guiado-do-crm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/tour-steps.ts
  - src/lib/tour-persistence.ts
  - scripts/test-tour-persistence.cjs
autonomous: false
requirements: [TUTORIAL-01, TUTORIAL-02, TUTORIAL-04, TUTORIAL-05]
user_setup: []

must_haves:
  truths:
    - "O projeto tem `react-joyride` 3.2.0 instalado e nenhuma outra biblioteca de tour/onboarding"
    - "A API real da v3 (nome do export do componente, nome do prop de callback, campos aceitos por um passo) está confirmada contra os tipos publicados dentro de node_modules, não presumida de exemplos v2"
    - "Os 5 passos do tour existem como dado estático tipado, com a cópia exata travada no 25-UI-SPEC.md"
    - "A decisão de gravar a flag 'já viu o tour' é uma função pura, testável sem DOM, e responde `true` tanto para o status de conclusão quanto para o de pulo"
    - "Um comando automatizado prova o comportamento de ler/gravar/limpar a flag contra um localStorage mockado"
  artifacts:
    - path: "package.json"
      provides: "Dependência react-joyride pinada + script npm test:tour-persistence"
      contains: "react-joyride"
    - path: "src/lib/tour-steps.ts"
      provides: "Array TOUR_STEPS com os 5 passos e a cópia travada"
      exports: ["TOUR_STEPS"]
      min_lines: 30
    - path: "src/lib/tour-persistence.ts"
      provides: "Funções puras de persistência da flag do tour"
      exports: ["TOUR_STORAGE_KEY", "deveGravarComoVisto", "lerTourVisto", "gravarTourVisto", "limparTourVisto"]
    - path: "scripts/test-tour-persistence.cjs"
      provides: "Harness Node que prova TUTORIAL-02/TUTORIAL-05 sem browser"
      min_lines: 40
  key_links:
    - from: "src/lib/tour-steps.ts"
      to: "react-joyride"
      via: "import type Step (tipagem checada por tsc contra o .d.ts real)"
      pattern: "from \"react-joyride\""
    - from: "scripts/test-tour-persistence.cjs"
      to: "src/lib/tour-persistence.ts"
      via: "import dinâmico via ts-alias-loader.mjs"
      pattern: "@/lib/tour-persistence"
    - from: "src/lib/tour-steps.ts"
      to: "src/components/app-sidebar.tsx"
      via: "seletor de atributo data-tour (contrato de alvo, consumido no plano 25-02)"
      pattern: "data-tour="
---

<objective>
Estabelecer a fundação do tour guiado: instalar a biblioteca mandatada (`react-joyride`, TUTORIAL-04) com portão humano de legitimidade de pacote, **confirmar a API real da v3 contra os tipos publicados no próprio pacote** antes de qualquer componente ser escrito, e materializar as duas peças puras da fase — os 5 passos (dado estático, TUTORIAL-01) e a lógica de persistência da flag "já viu o tour" (TUTORIAL-02/TUTORIAL-05), coberta por harness `.cjs`.

Purpose: o 25-RESEARCH.md classifica a API da v3 como confiança MEDIUM (2 páginas de doc retornaram 404 e a v3 é uma reescrita recente com breaking changes vs. a v2 que domina a internet). Escrever componente antes de confirmar nomes de export/prop é a forma mais provável de queimar contexto do executor em erro de build. Este plano fecha essa incerteza primeiro e entrega, já testado, todo o código que não depende de DOM.

Output: `react-joyride` instalado e pinado, `src/lib/tour-steps.ts`, `src/lib/tour-persistence.ts`, `scripts/test-tour-persistence.cjs`, script `test:tour-persistence` no `package.json`, e a superfície real da API v3 registrada no SUMMARY para o plano 25-02 consumir sem reinvestigar.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-tour-guiado-do-crm/25-RESEARCH.md
@.planning/phases/25-tour-guiado-do-crm/25-UI-SPEC.md
@.planning/phases/25-tour-guiado-do-crm/25-PATTERNS.md
@.planning/phases/25-tour-guiado-do-crm/25-VALIDATION.md
@src/lib/lead-csv-export.ts
@scripts/test-lead-csv-export.cjs
@package.json
</context>

<interfaces>
<!-- Contratos que este plano CRIA e que o plano 25-02 vai consumir direto, sem explorar o codebase. -->

Em `src/lib/tour-persistence.ts` (assinaturas obrigatórias — o plano 25-02 depende delas ipsis litteris):
- `TOUR_STORAGE_KEY: string` — valor `"tourVisto"`
- `deveGravarComoVisto(status: string): boolean` — `true` só para o status de conclusão e o de pulo emitidos pelo Joyride
- `lerTourVisto(storage: Pick<Storage, "getItem">): boolean`
- `gravarTourVisto(storage: Pick<Storage, "setItem">): void`
- `limparTourVisto(storage: Pick<Storage, "removeItem">): void`

Em `src/lib/tour-steps.ts`:
- `TOUR_STEPS` — array tipado com o tipo de passo exportado por `react-joyride`, 5 entradas, `target` apontando para os seletores `[data-tour="nav-dashboard"]`, `[data-tour="nav-leads"]`, `[data-tour="nav-pipeline"]`, `[data-tour="nav-campanhas"]`, `[data-tour="nav-relatorios"]`, nessa ordem.

Contrato de alvo consumido pelo plano 25-02 em `src/components/app-sidebar.tsx`: os 5 `tourId` acima, aplicados como `data-tour` nos `<Link>` de `/`, `/leads`, `/pipeline`, `/campanhas`, `/relatorios`.
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Portão humano de legitimidade do pacote react-joyride</name>
  <read_first>
    - .planning/phases/25-tour-guiado-do-crm/25-RESEARCH.md (seção `## Package Legitimacy Audit` — evidência já coletada e o motivo do veredito `[ASSUMED]`)
    - package.json (confirmar que nenhuma lib de tour já existe hoje)
  </read_first>
  <files>nenhum arquivo é modificado nesta task (portão de aprovação, somente leitura)</files>
  <action>NÃO instalar nada ainda. Coletar a evidência automatizada de legitimidade (`npm view react-joyride version repository.url license scripts.postinstall`), apresentá-la ao humano junto com o resumo do `## Package Legitimacy Audit` do 25-RESEARCH.md, e PARAR, aguardando aprovação explícita. Este portão não é auto-aprovável por `mode: yolo` nem por `workflow.auto_advance` — é um portão de cadeia de suprimentos exigido pela degradação graciosa do `slopcheck` (host sem `pip`). Sem a palavra de aprovação do humano, nenhuma task seguinte deste plano roda.</action>
  <what-built>
    Nada ainda — este passo vem ANTES do `npm install`. O 25-RESEARCH.md marcou `react-joyride` como `[ASSUMED]` (não `[VERIFIED]`) porque `slopcheck` não pôde rodar neste host (`pip`/`pip3` ausentes no PATH). O protocolo de degradação graciosa exige confirmação humana antes de instalar um pacote `[ASSUMED]`. Este checkpoint NÃO é auto-aprovável, mesmo com `mode: yolo`/`auto_advance` — é um portão de cadeia de suprimentos.

    Evidência já levantada na pesquisa (reconfirme o que quiser antes de aprovar):
    - Pacote: `react-joyride`, versão alvo `3.2.0`
    - Primeira publicação em 2015-09-04, versão atual publicada em 2026-07-09 (10 anos de histórico)
    - ~1.002.744 downloads/semana (API oficial do npm, janela 2026-09-05..11)
    - Repositório: `github.com/gilbarbara/react-joyride` (confirmado via `npm view repository.url` e via API do GitHub)
    - Licença MIT; `scripts.postinstall` vazio (sem script de pós-instalação)
    - `peerDependencies`: react/react-dom `16.8 - 19` (compatível com react 19.2.7 do projeto)
    - Dependências transitivas todas conhecidas: `@fastify/deepmerge`, `@floating-ui/react-dom`, `@gilbarbara/*`, `is-lite`, `react-innertext`, `scroll`, `scrollparent`, `use-sync-external-store`
  </what-built>
  <how-to-verify>
    1. Abrir https://www.npmjs.com/package/react-joyride e confirmar: nome exato `react-joyride` (sem typo/caractere trocado), versão mais recente `3.2.0`, licença MIT, link de repositório apontando para `github.com/gilbarbara/react-joyride`.
    2. Confirmar na página do npm que a contagem de downloads semanais está na casa do milhão (não dezenas/centenas — sinal clássico de pacote-imitação recém-publicado).
    3. Abrir https://github.com/gilbarbara/react-joyride e confirmar que o repositório existe, tem histórico longo e é do autor `gilbarbara`.
    4. Confirmar que este é o pacote que TUTORIAL-04 manda usar (o requisito diz "React Joyride — nenhuma biblioteca de tour concorrente, nenhum servidor/SaaS externo").
  </how-to-verify>
  <acceptance_criteria>
    - O humano respondeu explicitamente aprovando ou reprovando o `npm install react-joyride@3.2.0`.
    - Se reprovado: a execução do plano PARA aqui e o motivo é registrado no SUMMARY — nenhuma tarefa seguinte roda, porque TUTORIAL-04 não admite substituto.
  </acceptance_criteria>
  <verify>
    <automated>npm view react-joyride version repository.url license scripts.postinstall</automated>
    <human-check>Resposta humana explícita de aprovação registrada antes da Task 2 rodar.</human-check>
  </verify>
  <done>Aprovação humana (ou recusa) registrada. Só com aprovação a Task 2 executa.</done>
  <resume-signal>Digite "aprovado" para liberar o `npm install react-joyride@3.2.0`, ou descreva o problema encontrado.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Instalar react-joyride 3.2.0, confirmar a API v3 contra o .d.ts e criar os 5 passos</name>
  <files>package.json, package-lock.json, src/lib/tour-steps.ts</files>
  <read_first>
    - node_modules/react-joyride/package.json (campos `version`, `types`/`exports` — para achar o caminho real do arquivo de tipos) — **só existe após o install desta task**
    - node_modules/react-joyride/dist/index.d.ts (ou o caminho que o campo `types`/`exports` apontar) — **fonte de verdade da API**, leia antes de escrever qualquer linha de `tour-steps.ts`
    - .planning/phases/25-tour-guiado-do-crm/25-RESEARCH.md (Pitfalls 1/2/3/4, Assumptions A2/A3, Open Question 1)
    - .planning/phases/25-tour-guiado-do-crm/25-UI-SPEC.md (seção `## Copywriting Contract` — cópia TRAVADA dos 5 passos, não reformule)
    - .planning/phases/25-tour-guiado-do-crm/25-PATTERNS.md (seção `src/lib/tour-steps.ts` — forma do array + convenção `as const` de `NAV_ITEMS`)
    - src/components/app-sidebar.tsx (linhas 22-35 e 61-75: convenção de array tipado do projeto e o `<Link>` que vai receber `data-tour` no plano 25-02)
  </read_first>
  <action>
    Rodar `npm install react-joyride@3.2.0` (versão exata verificada na pesquisa e aprovada na Task 1). Não usar `@latest`, não usar range `^`: pinar `3.2.0` no `package.json`, mesmo idioma dos pins exatos já usados neste projeto para `ai@7.0.93` e `@ai-sdk/anthropic@4.0.49`.

    Imediatamente após o install, ANTES de escrever código: abrir o arquivo de tipos publicado dentro de `node_modules/react-joyride` (resolver o caminho pelo campo `types`/`exports` do `package.json` do pacote) e confirmar, anotando os valores reais:
    1. Como o componente é exportado — export nomeado `Joyride` ou export default (a pesquisa diz nomeado na v3; a v2 era default).
    2. O nome do prop de callback de eventos — `onEvent` (v3) ou `callback` (v2).
    3. O nome do tipo do payload desse callback (a pesquisa supõe `CallBackProps`) e se ele tem campo `status`.
    4. Se existe um export de constantes de status (a pesquisa supõe `STATUS`) e quais são os valores literais para "concluído" e "pulado" (a pesquisa supõe `"finished"` e `"skipped"`).
    5. O nome do tipo de um passo (a pesquisa supõe `Step`) e quais campos ele aceita: `target`, `title`, `content`, `placement`, e — Open Question 1 — se `disableBeacon` existe no nível do passo ou se o controle do beacon só existe globalmente (`options.beaconTrigger`/`skipBeacon`).
    6. O shape aceito por `options` — em especial `buttons`, `primaryColor`, `backgroundColor`, `textColor`, `overlayColor`, `borderRadius`, `zIndex`, `spotlightPadding` — e o nome real do prop de estilos (`options` vs `styles`).
    Registrar essas 6 respostas literalmente na seção de decisões do SUMMARY deste plano, com o caminho do arquivo `.d.ts` consultado — o plano 25-02 vai ler esse SUMMARY em vez de reinvestigar. Qualquer divergência em relação ao que o 25-RESEARCH.md supôs vira uma decisão numerada D-25-01, D-25-02, etc. no SUMMARY.

    Criar `src/lib/tour-steps.ts` exportando `TOUR_STEPS`, tipado com o tipo de passo REAL confirmado acima (importado como `import type` de `"react-joyride"`). Cinco entradas, nesta ordem exata: `[data-tour="nav-dashboard"]` / título "Follow-ups", `[data-tour="nav-leads"]` / "Leads", `[data-tour="nav-pipeline"]` / "Pipeline", `[data-tour="nav-campanhas"]` / "Campanhas de nicho", `[data-tour="nav-relatorios"]` / "Relatórios". O corpo de cada passo é a frase correspondente da tabela `## Copywriting Contract` do 25-UI-SPEC.md, copiada caractere a caractere (inclusive os travessões "—" e a acentuação) — essa cópia está travada, não reescreva, não adicione "!", emoji ou hype. Todos os 5 passos usam `placement: "right"` (a sidebar fica à esquerda). Se `disableBeacon` existir no tipo de passo, setar `true` nos 5; se não existir, omitir e anotar no SUMMARY que o controle do beacon terá de vir por `options` no plano 25-02.

    Cabeçalho JSDoc do módulo no mesmo espírito de `src/lib/lead-csv-export.ts`: explicar que é dado 100% estático, sem DOM/React/interpolação dinâmica — e declarar explicitamente a invariante de segurança T-25-01 (nenhum `title`/`content` pode ser construído a partir de dado de banco/URL/formulário, nunca `dangerouslySetInnerHTML`).
  </action>
  <acceptance_criteria>
    - `npm ls react-joyride` mostra `react-joyride@3.2.0` sem `UNMET`/`invalid`.
    - `grep -c '"react-joyride": "3.2.0"' package.json` retorna 1 (pin exato, sem `^` nem `~`).
    - `grep -c 'data-tour=' src/lib/tour-steps.ts` retorna 5 (um alvo por passo).
    - `grep -c 'nav-dashboard\|nav-leads\|nav-pipeline\|nav-campanhas\|nav-relatorios' src/lib/tour-steps.ts` retorna 5 — os cinco slugs distintos aparecem.
    - `grep -c 'dangerouslySetInnerHTML' src/lib/tour-steps.ts` retorna 0 (T-25-01).
    - `grep -F -c 'o painel que evita esquecer um lead.' src/lib/tour-steps.ts` retorna 1 — prova de que a cópia travada do UI-SPEC foi usada literalmente (mesma checagem vale, por leitura, para as outras 4 frases).
    - `npx tsc --noEmit` sai 0 — o array compila contra o tipo de passo REAL do pacote; se um campo suposto (ex. `disableBeacon`) não existir na v3, isto falha aqui, no arquivo mais barato de corrigir, e não dentro do componente.
    - `npm run lint` sai 0.
    - O SUMMARY contém as 6 respostas de API (export do componente, prop de callback, tipo do payload, constantes de status, tipo/campos de passo, shape de `options`) com o caminho do `.d.ts` consultado.
  </acceptance_criteria>
  <verify>
    <automated>npm ls react-joyride && npx tsc --noEmit && npm run lint && grep -c 'data-tour=' src/lib/tour-steps.ts</automated>
  </verify>
  <done>react-joyride@3.2.0 pinado e instalado; a API v3 real está registrada no SUMMARY; `TOUR_STEPS` existe com os 5 passos, cópia travada do UI-SPEC, tipado contra o pacote e compilando limpo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persistência pura da flag do tour + harness .cjs (Wave 0)</name>
  <files>src/lib/tour-persistence.ts, scripts/test-tour-persistence.cjs, package.json</files>
  <read_first>
    - .planning/phases/25-tour-guiado-do-crm/25-VALIDATION.md (seção `## Wave 0 Requirements` — este é o item que destrava a cobertura automatizada de TUTORIAL-02/05)
    - .planning/phases/25-tour-guiado-do-crm/25-PATTERNS.md (seções `src/lib/tour-persistence.ts` e `scripts/test-tour-persistence.cjs` — assinaturas e boilerplate do harness)
    - src/lib/lead-csv-export.ts (linhas 1-19: cabeçalho JSDoc explicando POR QUE o módulo é puro/sem DOM)
    - scripts/test-lead-csv-export.cjs (linhas 1-41 e as últimas ~10: `register` do ts-alias-loader, helper `check()`, import dinâmico, códigos de saída)
    - package.json (bloco `scripts`, linhas 24-34: convenção de nomes `test:*`)
  </read_first>
  <behavior>
    - `deveGravarComoVisto` com o status de conclusão do Joyride → `true`
    - `deveGravarComoVisto` com o status de pulo do Joyride → `true` (Pitfall 4: esquecer este caminho faz o tour reaparecer do zero, violando TUTORIAL-05)
    - `deveGravarComoVisto("running")` → `false`
    - `deveGravarComoVisto("paused")` → `false`
    - `deveGravarComoVisto("")` e um status desconhecido qualquer → `false`
    - `lerTourVisto` com a chave ausente no storage mockado → `false`
    - `gravarTourVisto` grava a string `"true"` sob `TOUR_STORAGE_KEY`, e `lerTourVisto` passa a retornar `true`
    - `limparTourVisto` remove a chave, e `lerTourVisto` volta a `false` (é o que destrava TUTORIAL-03)
    - `TOUR_STORAGE_KEY` vale exatamente `"tourVisto"`
  </behavior>
  <action>
    Criar `src/lib/tour-persistence.ts` com as cinco exportações do bloco `<interfaces>` deste plano, com as assinaturas exatas ali descritas. Ponto central de desenho: `lerTourVisto`/`gravarTourVisto`/`limparTourVisto` recebem o storage como PARÂMETRO (`Pick<Storage, "getItem">` etc.), nunca leem `window`/`globalThis` por dentro — é isso que permite ao harness `.cjs` passar um objeto em memória e rodar sob Node puro, sem jsdom, sem browser (host de 4GB, sem framework de teste de DOM no projeto). Quem passa `window.localStorage` é o componente cliente, no plano 25-02, depois do guard de montagem.

    Os valores literais de status usados por `deveGravarComoVisto` devem ser os REAIS confirmados na Task 2 (a pesquisa supõe `"finished"` e `"skipped"`). Se a Task 2 achou um export de constantes de status no pacote, preferir comparar contra esses literais confirmados e documentar a origem em comentário — a função continua recebendo `string` (não o tipo do pacote) justamente para o harness poder chamá-la sem importar `react-joyride`.

    Cabeçalho JSDoc no molde de `lead-csv-export.ts`: explicar que o módulo é puro, citar TUTORIAL-02/TUTORIAL-05 e o harness que o cobre, e registrar a disposição de ameaça T-25-02 (a flag em `localStorage` é adulterável pelo próprio usuário — risco ACEITO: ferramenta solo, pior caso é o tour reaparecer ou não para quem já tem acesso total à máquina; nunca usar essa chave como controle de acesso a nada).

    Criar `scripts/test-tour-persistence.cjs` copiando o esqueleto de `scripts/test-lead-csv-export.cjs` (shebang, `"use strict"`, `register("./ts-alias-loader.mjs", ...)`, helper `check()`, import dinâmico de `@/lib/tour-persistence`, saída 1 em falha e 0 em sucesso). Asserções: as 9 do bloco `<behavior>` acima, no mínimo. O storage mockado é um objeto simples com `getItem`/`setItem`/`removeItem` sobre um `Map` — nada de `window`, nada de jsdom.

    Registrar `"test:tour-persistence": "node scripts/test-tour-persistence.cjs"` no bloco `scripts` do `package.json`, na mesma vizinhança dos outros `test:*`.

    Prova de mutação (mesma disciplina já usada nas Fases 8/12/19): depois do harness passar, inverter temporariamente o ramo do status de pulo em `deveGravarComoVisto` e confirmar que o harness FALHA; restaurar em seguida e confirmar que volta a passar. Registrar as duas saídas no SUMMARY. Fazer a mutação sempre no arquivo real e restaurá-la na mesma ação — nunca deixar o arquivo mutado em disco ao terminar.
  </action>
  <acceptance_criteria>
    - `node scripts/test-tour-persistence.cjs` sai 0 e imprime pelo menos 9 linhas começando com `OK `.
    - `npm run test:tour-persistence` sai 0 (script registrado no package.json).
    - `grep -c 'window\.\|globalThis\.' src/lib/tour-persistence.ts` retorna 0 — nenhum acesso direto a browser globals no módulo puro.
    - `grep -c 'export function deveGravarComoVisto' src/lib/tour-persistence.ts` retorna 1, e o mesmo para `lerTourVisto`, `gravarTourVisto`, `limparTourVisto`.
    - `grep -F -c '"tourVisto"' src/lib/tour-persistence.ts` retorna pelo menos 1.
    - `npx tsc --noEmit` sai 0 e `npm run lint` sai 0.
    - Prova de mutação registrada no SUMMARY: com o ramo do status de pulo invertido o harness sai 1; restaurado, sai 0. Arquivo restaurado em disco (confirmado por `git diff` limpo em relação à versão final pretendida).
  </acceptance_criteria>
  <verify>
    <automated>npm run test:tour-persistence && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>`src/lib/tour-persistence.ts` existe com as 5 exportações puras; `scripts/test-tour-persistence.cjs` cobre os 9 comportamentos e está registrado como `test:tour-persistence`; a lacuna de Wave 0 do 25-VALIDATION.md está fechada; mutação provada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registro npm → node_modules do projeto | Código de terceiro entra na árvore de build a partir de um registro público — única superfície de confiança realmente nova nesta fase |
| navegador do usuário (localStorage) → código cliente | Preferência de UI lida de um armazenamento que o próprio usuário pode editar no DevTools |
| arquivos-fonte do tour → DOM renderizado | Texto estático vira conteúdo renderizado num tooltip; vira risco só se algum dia deixar de ser estático |

Fora de escopo nesta fase: nenhuma rota nova, nenhuma Server Action nova, nenhuma coluna/tabela nova, nenhum input de usuário validado — confirmado pela seção `## Security Domain` do 25-RESEARCH.md (ASVS V2/V3/V4/V5/V6 todas "não se aplica").

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-SC | Tampering | `npm install react-joyride` (cadeia de suprimentos) | mitigate | `slopcheck` indisponível (sem `pip` no host) → pacote fica `[ASSUMED]`; Task 1 é um `checkpoint:human-verify` BLOQUEANTE e não auto-aprovável (ignora `mode: yolo`/`auto_advance`), com verificação manual em npmjs.com + GitHub; versão pinada exata `3.2.0` (sem `^`/`~`/`@latest`), mesmo idioma dos pins de `ai`/`@ai-sdk/anthropic` da Fase 23 |
| T-25-01 | Tampering / Information Disclosure | `src/lib/tour-steps.ts` (conteúdo dos passos) | mitigate | Todo `title`/`content` é string literal estática no arquivo-fonte; proibido interpolar dado de banco/URL/formulário e proibido `dangerouslySetInnerHTML`. Invariante declarada no JSDoc do módulo e checada por `grep -c 'dangerouslySetInnerHTML' src/lib/tour-steps.ts` == 0 nas acceptance criteria da Task 2 |
| T-25-02 | Tampering | `src/lib/tour-persistence.ts` (chave `tourVisto` em localStorage) | accept | Ferramenta solo, sem multiusuário e sem autenticação por design (CLAUDE.md). A flag não controla acesso a dado nenhum — pior caso da adulteração é o tour reaparecer ou deixar de aparecer para o próprio usuário, que já tem acesso total à máquina/navegador. Risco aceito e documentado no JSDoc do módulo |
| T-25-03 | Information Disclosure | `src/lib/tour-persistence.ts` (o que é gravado) | mitigate | Só a string literal `"true"` é gravada sob `tourVisto` — nenhum dado de lead, nenhum PII, nada derivado do banco pode ir para `localStorage`. Coberto pelas asserções do harness (a gravação é exatamente `"true"`) |
</threat_model>

<verification>
Antes de declarar o plano completo, com o host livre de outros processos pesados (4GB de RAM — precedente das Fases 18-20):

1. `npm ls react-joyride` → `react-joyride@3.2.0`, sem `UNMET`/`invalid`
2. `npm run test:tour-persistence` → sai 0, ≥9 asserções `OK`
3. `npx tsc --noEmit` → sai 0
4. `npm run lint` → sai 0
5. `npm run build` → sai 0 (gate de suíte completa do projeto; rodar isolado)
6. SUMMARY contém as 6 respostas de API v3 lidas do `.d.ts` real, com o caminho do arquivo consultado
7. SUMMARY contém a prova de mutação do harness (falha com o ramo de "pulado" invertido, passa restaurado)
</verification>

<success_criteria>
- `react-joyride@3.2.0` instalado, pinado exato, aprovado por humano antes do install, e é a única biblioteca de tour/onboarding no `package.json`
- A API real da v3 (export do componente, prop de callback, tipo do payload, constantes de status, campos de passo, shape de `options`) está documentada no SUMMARY — o plano 25-02 não precisa reinvestigar nem adivinhar
- `TOUR_STEPS` tem exatamente 5 passos, na ordem Follow-ups → Leads → Pipeline → Campanhas de nicho → Relatórios, com a cópia literal do 25-UI-SPEC.md e alvos `[data-tour="nav-*"]`
- `deveGravarComoVisto` responde `true` para conclusão E para pulo (os dois caminhos de TUTORIAL-02), provado por harness automatizado
- Gates verdes: `test:tour-persistence`, `tsc --noEmit`, `lint`, `build`
</success_criteria>

<output>
Create `.planning/phases/25-tour-guiado-do-crm/25-01-SUMMARY.md` when done
</output>
