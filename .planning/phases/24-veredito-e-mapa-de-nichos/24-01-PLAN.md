---
phase: 24-veredito-e-mapa-de-nichos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - scripts/migrate-veredito.cjs
  - scripts/verify-schema.cjs
  - scripts/test-veredito-actions.cjs
  - package.json
  - src/lib/validations.ts
  - src/actions/campanha-actions.ts
autonomous: true
requirements: [VEREDITO-01, VEREDITO-02, VEREDITO-03]

must_haves:
  truths:
    - "O banco real (data/crm.db) tem as colunas campanhas.veredito_final e campanhas.veredito_decidido_em, nullable"
    - "Uma decisão do usuário (aprofundar/mudar_angulo/abandonar) é persistida na campanha junto com a data do registro"
    - "Um valor de veredito fora dos 3 permitidos é rejeitado antes de qualquer escrita no banco"
    - "Registrar o veredito não altera nenhuma linha de leads nem de qualquer outra tabela"
  artifacts:
    - path: "scripts/migrate-veredito.cjs"
      provides: "Migração manual idempotente das 2 colunas de veredito em campanhas"
      contains: "ALTER TABLE"
    - path: "src/db/schema.ts"
      provides: "Declaração Drizzle de vereditoFinal e vereditoDecididoEm em campanhas"
      contains: "veredito_decidido_em"
    - path: "src/lib/validations.ts"
      provides: "vereditoSchema (enum de 3 valores + campanhaId)"
      contains: "vereditoSchema"
    - path: "src/actions/campanha-actions.ts"
      provides: "Server Action registrarVeredito"
      contains: "registrarVeredito"
    - path: "scripts/test-veredito-actions.cjs"
      provides: "Harness comportamental de registrarVeredito, incluindo o gate de não-interferência (VEREDITO-03)"
      contains: "VEREDITO-03"
  key_links:
    - from: "src/actions/campanha-actions.ts"
      to: "src/lib/validations.ts"
      via: "import de vereditoSchema, safeParse antes de qualquer acesso ao banco"
      pattern: "vereditoSchema\\.safeParse"
    - from: "scripts/verify-schema.cjs"
      to: "data/crm.db"
      via: "PRAGMA table_info(campanhas) com conjunto estrito de colunas"
      pattern: "veredito_final"
---

<objective>
Criar a fundação de dados do veredito do operador (VEREDITO-01/02/03): duas colunas aditivas
nullable em `campanhas`, o contrato Zod do enum de 3 valores, a Server Action `registrarVeredito`
e o harness que prova que registrar o veredito é **pura memória de decisão** — não toca nenhuma
outra tabela.

Purpose: sem coluna viva no banco real e sem Server Action validada, as telas da Onda 2 (painel da
campanha e Mapa de Nichos) não têm o que ler nem o que escrever.
Output: `data/crm.db` migrado, `registrarVeredito` exportada e coberta por teste automatizado.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Precedentes obrigatórios (ler, não adivinhar):
@scripts/migrate-interesse.cjs
@scripts/verify-schema.cjs
@scripts/test-motivo-perda-actions.cjs
@src/actions/campanha-actions.ts
@src/lib/validations.ts
@src/db/schema.ts

<interfaces>
<!-- Contratos que o executor consome. Extraídos do código real — não explorar o repo atrás deles. -->

De `src/db/schema.ts` (tabela `campanhas`, Fase 22 — colunas físicas atuais):
`id`, `nicho_id` (FK subnichos, restrict), `oferta`, `meta_conversao`, `janela_inicio`,
`janela_fim`, `estado` (enum: explorando | veredito_registrado | em_escala | abandonada,
notNull, default "explorando"), `deleted_at`, `created_at`, `updated_at`.
Índices: `campanhas_nicho_id_idx`, `campanhas_deleted_at_idx`, `campanhas_estado_idx`.

De `src/lib/ai/diagnostico-schema.ts` (Fase 23, DIAGNOSTICO-09) — o enum que o veredito do
operador DEVE espelhar valor a valor:
`veredito_sugerido.decisao: z.enum(["aprofundar", "mudar_angulo", "abandonar"])`

De `src/actions/campanha-actions.ts` (Fase 22) — molde já em uso neste arquivo:
`type ActionState = { success: true; campanha?: Campanha } | { errors: Record<string, string[] | undefined> } | undefined`
`function isForeignKeyViolation(err: unknown): boolean`
`async function nichoExists(nichoId: number): Promise<boolean>`
`function revalidateCampanhaRoutes(id?: number): void`  — revalida `/campanhas` e `/campanhas/{id}`

De `src/lib/validations.ts` (Fase 22): `campanhaBaseSchema` (objeto não-refinado),
`campanhaSchema`, `campanhaUpdateSchema` (= base `.extend({ id: z.coerce.number().int().positive() })`).
</interfaces>
</context>

<decisions>
Decisões travadas desta fase (citar o ID no código e no SUMMARY):

- **D-24-01**: `registrarVeredito` escreve EXATAMENTE 4 colunas da linha de `campanhas` alvo —
  `veredito_final`, `veredito_decidido_em`, `estado` (para `"veredito_registrado"`) e `updated_at`
  — e NENHUMA outra tabela. Mudar `estado` é o cumprimento de CAMPANHA-02 (a Fase 22 deixou essa
  transição explicitamente para a Fase 24, ver doc-comment de `campanhas` em `schema.ts`), não uma
  ação automática colateral: VEREDITO-03 proíbe alterar OUTRAS entidades (leads, interações,
  diagnósticos), não a própria campanha que está recebendo a decisão.
- **D-24-02**: a data da decisão (VEREDITO-02) é gravada pelo SERVIDOR no momento do registro
  (`sql\`(unixepoch())\``), nunca escolhida pelo usuário nem enviada pelo cliente. Re-registrar
  sobrescreve `veredito_final` E `veredito_decidido_em` — a data reflete a decisão vigente; não há
  histórico de vereditos do operador (histórico é o papel da tabela `diagnosticos`, não desta).
- **D-24-07**: nenhum pacote novo é instalado nesta fase inteira — zero `npm install`, zero
  primitivo shadcn novo (o host de 4GB já falhou em `npx shadcn add` antes, ver STATE.md Fase 01).
</decisions>

<tasks>

<task type="auto">
  <name>Task 1: Colunas de veredito em campanhas — schema, migração idempotente e gate</name>
  <files>src/db/schema.ts, scripts/migrate-veredito.cjs, scripts/verify-schema.cjs, package.json</files>
  <read_first>
    - scripts/migrate-interesse.cjs (molde bloco-a-bloco: PRAGMA table_info → backup só quando vai escrever → wal_checkpoint(TRUNCATE) → reabrir com fileMustExist → ALTER em try/catch tolerando "duplicate column name" → verificação pós-migração)
    - src/db/schema.ts linhas 86-135 (doc-comment + declaração de `campanhas`)
    - scripts/verify-schema.cjs linhas 125-160 (bloco `REQUIRED_CAMPANHAS_COLUMNS`, conjunto ESTRITO: falha com coluna faltando E com coluna extra)
    - package.json (bloco `scripts`, para posicionar `migrate:veredito` junto dos outros `migrate:*`)
  </read_first>
  <action>
    Adicionar duas colunas ADITIVAS NULLABLE à tabela `campanhas`, em três lugares, na ordem abaixo.

    (a) `src/db/schema.ts`: declarar, imediatamente APÓS `estado` e ANTES de `deletedAt`:
    `vereditoFinal` mapeando a coluna física `veredito_final` como `text` com
    `{ enum: ["aprofundar", "mudar_angulo", "abandonar"] }` — os MESMOS 3 valores, na mesma ordem,
    de `veredito_sugerido.decisao` em `src/lib/ai/diagnostico-schema.ts` (VEREDITO-01: o veredito do
    operador é comparável ao da IA justamente por compartilhar o vocabulário); e
    `vereditoDecididoEm` mapeando `veredito_decidido_em` como `integer` com
    `{ mode: "timestamp" }`. As duas SEM `.notNull()` e SEM `.default()`: campanha sem veredito
    registrado tem as duas NULL, e NULL nas duas é o estado normal da maioria das campanhas.
    Escrever um doc-comment curto acima das duas citando VEREDITO-01/VEREDITO-02, D-24-01 e D-24-02,
    e registrando que nenhum índice é criado (o filtro por veredito do Mapa de Nichos é client-side,
    plano 24-04 — não há WHERE por `veredito_final` em lugar nenhum).

    (b) `scripts/migrate-veredito.cjs`: cópia estrutural de `scripts/migrate-interesse.cjs`, trocando
    a tabela `leads`/coluna `interesse` por `campanhas`/as duas colunas novas. Prefixo de log
    `[migrate-veredito]`. Diferenças obrigatórias em relação ao molde: o check de idempotência olha
    as DUAS colunas (roda o ALTER de cada uma separadamente, só para a que faltar — SQLite não aceita
    duas colunas num único ADD COLUMN); as duas DDL são `ALTER TABLE \`campanhas\` ADD \`veredito_final\` text;`
    e `ALTER TABLE \`campanhas\` ADD \`veredito_decidido_em\` integer;` (sem NOT NULL, sem DEFAULT —
    coluna nullable dispensa a exigência de DEFAULT do SQLite que forçou `origem_tipo`); a contagem de
    referência antes/depois é feita sobre `campanhas` E sobre `leads` (provar que a migração não toca
    leads); a verificação pós-migração confere `type` TEXT / INTEGER e `notnull === 0` nas duas.
    Registrar no cabeçalho a mesma premissa operacional do molde (parar a app Next antes de rodar) e
    o veto explícito a `drizzle-kit push/generate` (dois incidentes destrutivos documentados nas
    Fases 06-01/07-01).

    (c) `package.json`: adicionar o script `"migrate:veredito": "node scripts/migrate-veredito.cjs"`
    ao lado dos outros `migrate:*`.

    (d) `scripts/verify-schema.cjs`: acrescentar `"veredito_final"` e `"veredito_decidido_em"` ao array
    `REQUIRED_CAMPANHAS_COLUMNS`, na mesma posição relativa da declaração Drizzle (depois de `estado`,
    antes de `deleted_at`). O conjunto é ESTRITO nos dois sentidos — sem esta edição, `verify:schema`
    passa a falhar por "coluna extra" assim que a migração rodar.

    (e) RODAR a migração de verdade contra `data/crm.db` (`npm run migrate:veredito`), DUAS VEZES, para
    provar idempotência, e depois `npm run verify:schema`. Registrar no SUMMARY a contagem de campanhas
    e de leads antes/depois (devem ser idênticas) e o caminho do backup criado.
  </action>
  <verify>
    <automated>npm run migrate:veredito && npm run migrate:veredito && npm run verify:schema && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run migrate:veredito` sai com código 0 nas DUAS execuções; a segunda imprime a mensagem de idempotência e NÃO cria backup novo.
    - `node -e "const D=require('better-sqlite3');const c=new D('data/crm.db').prepare('PRAGMA table_info(campanhas)').all();const f=c.find(x=>x.name==='veredito_final');const d=c.find(x=>x.name==='veredito_decidido_em');if(!f||!d)process.exit(1);if(f.notnull!==0||d.notnull!==0)process.exit(1);if(String(f.type).toUpperCase()!=='TEXT'||String(d.type).toUpperCase()!=='INTEGER')process.exit(1);console.log('ok')"` imprime `ok`.
    - `npm run verify:schema` sai 0 COM as duas colunas na lista estrita (provar a mutação: remover temporariamente `veredito_final` do array faz o gate sair 1 — reverter em seguida).
    - A contagem de linhas de `leads` antes e depois da migração é idêntica (impressa pelo próprio script).
    - `npx tsc --noEmit` sai 0.
    - `grep -n "drizzle-kit" scripts/migrate-veredito.cjs` só encontra a linha de comentário que VETA o uso — nenhuma invocação.
  </acceptance_criteria>
  <done>`data/crm.db` tem `campanhas.veredito_final` (TEXT nullable) e `campanhas.veredito_decidido_em` (INTEGER nullable); schema Drizzle e gate estrito alinhados com o banco real.</done>
</task>

<task type="auto">
  <name>Task 2: Contrato Zod do veredito e Server Action registrarVeredito</name>
  <files>src/lib/validations.ts, src/actions/campanha-actions.ts</files>
  <read_first>
    - src/lib/validations.ts linhas 290-327 (bloco de campanha da Fase 22: `campanhaBaseSchema`, `campanhaSchema`, `campanhaUpdateSchema`, idioma de mensagem VERBATIM compartilhada)
    - src/actions/campanha-actions.ts inteiro (molde: `safeParse` antes do banco, pré-checagem de FK, `isForeignKeyViolation` como backstop, `revalidateCampanhaRoutes`, soft-delete idempotente via `isNull(deletedAt)` no WHERE)
    - src/db/schema.ts (bloco `campanhas` já editado na Task 1)
    - src/lib/ai/diagnostico-schema.ts linhas 118-126 (enum `decisao` que este contrato espelha)
  </read_first>
  <action>
    (a) `src/lib/validations.ts`: exportar, ao final do bloco de campanha, `VEREDITO_VALORES` como
    tupla const dos 3 valores `aprofundar`, `mudar_angulo`, `abandonar` (mesma ordem do enum da IA) e
    `vereditoSchema` = objeto Zod com `campanhaId` (`z.coerce.number().int().positive()`) e
    `vereditoFinal` (`z.enum(VEREDITO_VALORES)` com mensagem de erro
    `"Escolha o veredito da campanha."`). Exportar também o tipo
    `VeredicoFinal`/`VereditoFinal` inferido (`z.infer<typeof vereditoSchema>["vereditoFinal"]`) —
    use o nome `VereditoFinal`. O schema NÃO recebe data nenhuma: D-24-02 manda a data vir do
    servidor; aceitar data do cliente seria aceitar dado não confiável sem necessidade.
    Doc-comment citando VEREDITO-01/VEREDITO-02 e D-24-02.

    (b) `src/actions/campanha-actions.ts`: adicionar `async function campanhaAtivaExists(campanhaId: number): Promise<boolean>`
    (SELECT de `campanhas.id` com `and(eq(campanhas.id, id), isNull(campanhas.deletedAt))`) — documentar
    no doc-comment que, DIFERENTE de `campanhaExists` de `lead-actions.ts` (que é indiferente a
    `deletedAt` de propósito), aqui a campanha removida NÃO pode receber veredito.

    Adicionar `export async function registrarVeredito(_prevState: ActionState, formData: FormData): Promise<ActionState>`
    com o `ActionState` já existente no arquivo, na sequência: `vereditoSchema.safeParse(Object.fromEntries(formData))`
    → em falha, devolver `{ errors: parsed.error.flatten().fieldErrors }` SEM tocar o banco; checar
    `campanhaAtivaExists` → em falha, `{ errors: { campanhaId: ["Campanha inválida."] } }`; então UM
    ÚNICO `db.update(campanhas).set({...}).where(and(eq(campanhas.id, id), isNull(campanhas.deletedAt)))`
    escrevendo EXATAMENTE `vereditoFinal`, `vereditoDecididoEm: sql\`(unixepoch())\``,
    `estado: "veredito_registrado"` e `updatedAt: sql\`(unixepoch())\`` (D-24-01). Fechar com
    `revalidateCampanhaRoutes(campanhaId)` mais `revalidatePath("/mapa-de-nichos")` (a tela do plano
    24-04 lê esta coluna; revalidar uma rota que ainda não existe é no-op seguro — mesmo precedente
    de `revalidatePath("/relatorios")` na Fase 11-02) e `return { success: true }`.

    Doc-comment da função citando VEREDITO-01/02/03, D-24-01 e D-24-02, e afirmando explicitamente:
    nenhum `db.update(leads)`, nenhum `db.insert`, nenhuma leitura/escrita de `diagnosticos`,
    nenhuma mudança de `followUpDate`/`stage` de lead nenhum — a divergência em relação ao veredito
    sugerido pela IA é dado do usuário, não gatilho de automação.

    NÃO usar `isForeignKeyViolation` aqui: esta action não escreve nenhuma FK.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run guard:no-hard-delete</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sai 0 e `npm run lint` sai 0.
    - `grep -c "vereditoSchema.safeParse" src/actions/campanha-actions.ts` retorna 1.
    - `grep -c "db.update(campanhas)" src/actions/campanha-actions.ts` retorna 3 (os 2 da Fase 22 + o novo) e `grep -c "leads" src/actions/campanha-actions.ts` retorna 0 — a action não conhece a tabela de leads.
    - `grep -c "unixepoch" src/actions/campanha-actions.ts` >= 4: a data da decisão vem do servidor, nunca do FormData.
    - `grep -c "vereditoDecididoEm" src/lib/validations.ts` retorna 0 — o cliente não envia data (D-24-02).
    - `npm run guard:no-hard-delete` sai 0.
  </acceptance_criteria>
  <done>`registrarVeredito` exportada, validando entrada antes do banco e escrevendo somente na linha da própria campanha.</done>
</task>

<task type="auto">
  <name>Task 3: Harness comportamental de registrarVeredito e gate de não-interferência</name>
  <files>scripts/test-veredito-actions.cjs, package.json</files>
  <read_first>
    - scripts/test-motivo-perda-actions.cjs (molde completo: `register` do stub de next/cache ANTES do ts-alias-loader, `DB_FILE_NAME` em os.tmpdir() setado antes de qualquer `await import("@/...")`, `check(condition, message)`, `process.exit(failed > 0 ? 1 : 0)`, ids/nomes únicos em vez de DELETE FROM)
    - scripts/test-relatorios-queries.cjs (DDL cru das tabelas `subnichos`/`leads` já escrito neste repo — reaproveitar as mesmas colunas)
    - src/actions/campanha-actions.ts (já editado na Task 2)
    - scripts/migrate-veredito.cjs (DDL exata das colunas novas)
  </read_first>
  <action>
    Criar `scripts/test-veredito-actions.cjs` no molde de `test-motivo-perda-actions.cjs`, com DDL cru
    em banco temporário de `os.tmpdir()` para `subnichos`, `campanhas` (as 10 colunas da Fase 22 MAIS
    `veredito_final text` e `veredito_decidido_em integer`) e `leads` (as colunas usadas pelo repo,
    incluindo `campanha_id integer`), com `PRAGMA foreign_keys = ON`. Semear 1 nicho, 2 campanhas
    (uma ativa "alvo", uma ativa "testemunha") e 2 leads vinculados à campanha alvo.

    Casos obrigatórios (cada um com `check(...)` e mensagem citando o requisito):
    1. VEREDITO-01 — `registrarVeredito` com `vereditoFinal="abandonar"` devolve `{ success: true }` e a
       linha da campanha alvo passa a ter `veredito_final = 'abandonar'`.
    2. VEREDITO-02 — após o caso 1, `veredito_decidido_em` é NÃO-NULO e está dentro de 5 segundos do
       `unixepoch()` do momento do teste.
    3. CAMPANHA-02 / D-24-01 — após o caso 1, `estado` da campanha alvo é `'veredito_registrado'`.
    4. VEREDITO-01 (divergência) — registrar `"aprofundar"` numa campanha cujo diagnóstico sugeriria
       outra coisa é aceito sem nenhuma checagem contra a IA: a action NÃO consulta `diagnosticos`
       (asserir que o teste roda sem a tabela `diagnosticos` existir no banco temporário).
    5. Entrada inválida — `vereditoFinal="vender_mais"` devolve objeto com `errors` e a coluna
       `veredito_final` permanece com o valor anterior (nenhuma escrita).
    6. Entrada inválida — `campanhaId="abc"`, `campanhaId="0"` e `campanhaId="-1"` devolvem `errors`.
    7. Campanha soft-deletada — marcar `deleted_at` na testemunha por SQL cru e verificar que
       `registrarVeredito` devolve `errors` e a linha continua com `veredito_final` NULL.
    8. **VEREDITO-03 (gate de não-interferência)** — capturar `JSON.stringify` de `SELECT * FROM leads ORDER BY id`
       ANTES e DEPOIS de uma chamada bem-sucedida e asserir igualdade EXATA das duas strings; asserir
       também que a linha da campanha TESTEMUNHA está byte a byte idêntica (mesmo `estado`, mesmo
       `updated_at`, `veredito_final` NULL).
    9. D-24-02 (re-registro) — forçar por SQL cru `veredito_decidido_em = 1000000000` na campanha alvo,
       registrar `"mudar_angulo"` de novo, e asserir que `veredito_final` mudou E que
       `veredito_decidido_em` é agora MAIOR que 1000000000 (prova determinística de reescrita, sem
       depender da resolução de 1 segundo do relógio).

    Limpeza final por `fs.unlinkSync` do arquivo `.db` inteiro — NUNCA `DELETE FROM` (o
    `guard:no-hard-delete` varre `scripts/`).

    Adicionar ao `package.json`: `"test:veredito-actions": "node scripts/test-veredito-actions.cjs"`.
  </action>
  <verify>
    <automated>npm run test:veredito-actions && npm run guard:no-hard-delete && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:veredito-actions` sai 0 e imprime pelo menos 12 linhas `OK`, incluindo uma cuja mensagem contém `VEREDITO-03`.
    - Teste de mutação obrigatório: comentar temporariamente a linha `estado: "veredito_registrado"` na action faz o caso 3 FALHAR (exit 1); reverter em seguida e reconfirmar exit 0. Registrar as duas execuções no SUMMARY.
    - `npm run guard:no-hard-delete` sai 0 (nenhum `DELETE FROM` introduzido em `scripts/`).
    - O harness roda sem a tabela `diagnosticos` no banco temporário — prova de que `registrarVeredito` não lê o veredito da IA.
    - `npm run lint` sai 0.
  </acceptance_criteria>
  <done>Cobertura automatizada de `registrarVeredito` verde, com o gate de não-interferência de VEREDITO-03 provado por snapshot antes/depois de `leads`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Server Action (`registrarVeredito`) | `FormData` chega cru do navegador: `campanhaId` e `vereditoFinal` são não confiáveis |
| Aplicação → SQLite (`data/crm.db`) | DDL manual fora do ORM; erro aqui é perda de dado real de produção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | `registrarVeredito` / `vereditoFinal` | mitigate | `z.enum(VEREDITO_VALORES)` no `safeParse` ANTES de qualquer acesso ao banco; valor fora do enum nunca chega ao `db.update` (caso 5 do harness) |
| T-24-02 | Tampering / Elevation | `registrarVeredito` / `campanhaId` | mitigate | `z.coerce.number().int().positive()` + `campanhaAtivaExists` (`isNull(deletedAt)`) + o mesmo predicado repetido no WHERE do UPDATE — id forjado ou campanha removida é no-op (casos 6 e 7) |
| T-24-03 | Tampering | `veredito_decidido_em` | mitigate | Data gerada por `sql\`(unixepoch())\`` no servidor; o schema Zod NÃO aceita campo de data (D-24-02) — cliente não consegue forjar a data da decisão |
| T-24-04 | Tampering | Efeito colateral em `leads` | mitigate | D-24-01 restringe a escrita a 4 colunas de 1 linha; gate automatizado compara snapshot completo de `leads` antes/depois (caso 8 — VEREDITO-03) |
| T-24-05 | Denial of Service | `scripts/migrate-veredito.cjs` contra `data/crm.db` | mitigate | Backup com `wal_checkpoint(TRUNCATE)` antes de escrever, `fileMustExist: true` no reopen, ALTER em try/catch tolerante a "duplicate column name", verificação de contagem de linhas pós-migração; veto explícito a `drizzle-kit push` |
| T-24-SC | Tampering | Cadeia de suprimentos (npm) | accept | Zero instalação de pacote nesta fase (D-24-07) — nenhum `npm install`, nenhum componente shadcn novo; sem superfície nova de supply chain |
</threat_model>

<verification>
1. `npm run migrate:veredito` duas vezes → exit 0 nas duas, backup só na primeira.
2. `npm run verify:schema` → exit 0 com as duas colunas no conjunto estrito.
3. `npm run test:veredito-actions` → exit 0, >= 12 `OK`.
4. `npx tsc --noEmit` → exit 0.
5. `npm run lint` → exit 0.
6. `npm run guard:no-hard-delete` → exit 0.
7. Contagem de `leads` em `data/crm.db` idêntica antes e depois da migração.
</verification>

<success_criteria>
- `campanhas.veredito_final` e `campanhas.veredito_decidido_em` existem, nullable, no banco real e no schema Drizzle, com o gate estrito atualizado.
- `registrarVeredito` persiste a decisão do operador e a data, e é provado por teste que ela não altera `leads` nem qualquer outra campanha (VEREDITO-03).
- Nenhum pacote novo instalado; nenhum comando `drizzle-kit` executado.
</success_criteria>

<output>
Create `.planning/phases/24-veredito-e-mapa-de-nichos/24-01-SUMMARY.md` when done
</output>
