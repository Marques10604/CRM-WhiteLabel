---
phase: 24-veredito-e-mapa-de-nichos
plan: 04
type: execute
wave: 2
depends_on: ["24-01", "24-02"]
files_modified:
  - src/components/mapa-de-nichos-table.tsx
  - src/app/mapa-de-nichos/page.tsx
  - src/components/app-sidebar.tsx
autonomous: true
requirements: [PAINEL-02, PAINEL-03]

must_haves:
  truths:
    - "Existe uma tela 'Mapa de Nichos' acessível pela navegação lateral"
    - "A tela lista todas as campanhas com nicho, veredito sugerido pela IA, veredito final do usuário e resumo do resultado real"
    - "O usuário filtra a lista por veredito e por nicho"
    - "O usuário ordena a lista por veredito e por nicho"
    - "Clicar numa campanha leva ao detalhe dela"
  artifacts:
    - path: "src/app/mapa-de-nichos/page.tsx"
      provides: "Rota /mapa-de-nichos montando as linhas do mapa"
      contains: "getResultadoPorCampanha"
    - path: "src/components/mapa-de-nichos-table.tsx"
      provides: "Tabela client com filtros e ordenação (PAINEL-03)"
      contains: "MapaLinha"
    - path: "src/components/app-sidebar.tsx"
      provides: "Item de navegação para /mapa-de-nichos"
      contains: "mapa-de-nichos"
  key_links:
    - from: "src/app/mapa-de-nichos/page.tsx"
      to: "src/db/queries.ts"
      via: "getResultadoPorCampanha() e getVereditoIAPorCampanha() sem argumento (todas as campanhas)"
      pattern: "getVereditoIAPorCampanha\\(\\)"
    - from: "src/components/mapa-de-nichos-table.tsx"
      to: "/campanhas/[id]"
      via: "Link por linha"
      pattern: "/campanhas/\\$\\{"
---

<objective>
Entregar a tela **Mapa de Nichos** (PAINEL-02/PAINEL-03): a visão consolidada de todas as campanhas
já exploradas — nicho, veredito sugerido pela IA, veredito final do usuário e o resumo do resultado
real — filtrável e ordenável por veredito e por nicho.

Purpose: é a tela que responde "quais nichos eu já testei, o que decidi em cada um e o que deu" —
o ativo de memória que a planilha nunca deu.
Output: rota `/mapa-de-nichos`, componente de tabela com filtros e item na navegação lateral.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-veredito-e-mapa-de-nichos/24-01-SUMMARY.md
@.planning/phases/24-veredito-e-mapa-de-nichos/24-02-SUMMARY.md

Precedentes obrigatórios (ler, não adivinhar):
@src/app/campanhas/page.tsx
@src/components/campanha-list.tsx
@src/components/app-sidebar.tsx
@src/app/relatorios/page.tsx
@src/components/veredito-sugerido-chip.tsx

<interfaces>
<!-- Contratos reais. Não explorar o repo atrás deles. -->

Do plano 24-02 (`src/db/queries.ts`):
`export type ResultadoCampanha = { total: number; fechados: number; perdidos: number; ticketMedioCentavos: number | null }`
`export async function getResultadoPorCampanha(campanhaId?: number): Promise<Map<number, ResultadoCampanha>>`  — sem argumento, devolve todas as campanhas que têm leads vinculados
`export async function getVereditoIAPorCampanha(campanhaId?: number): Promise<Map<number, "aprofundar" | "mudar_angulo" | "abandonar">>`
`export function computeTaxaConversao(row: { total: number; fechados: number }): number`

Do plano 24-02 (`src/lib/utils.ts`): `export function formatarTaxaConversao(taxa: number): string`
De `src/lib/money.ts`: `export function formatCentsToBRL(cents: number): string`

Do plano 24-01 (`src/lib/validations.ts`): `export type VereditoFinal = "aprofundar" | "mudar_angulo" | "abandonar"`
Do plano 24-01 (`src/db/schema.ts`): `campanhas.vereditoFinal` (nullable), `campanhas.vereditoDecididoEm` (Date | null)

De `src/components/veredito-sugerido-chip.tsx` (Fase 23):
`export type VereditoDecisao = "aprofundar" | "mudar_angulo" | "abandonar"`
`export function VereditoSugeridoChip({ decisao }: { decisao: VereditoDecisao })`
Rótulos canônicos: aprofundar → "Aprofundar", mudar_angulo → "Mudar o ângulo", abandonar → "Abandonar"

De `src/components/campanha-estado-badge.tsx` (Fase 22): `export function CampanhaEstadoBadge({ estado })`

De `src/app/campanhas/page.tsx` (Fase 22) — molde de carga de dados:
`db.select().from(campanhas).where(isNull(campanhas.deletedAt)).orderBy(desc(campanhas.createdAt))`
mais `db.select().from(nichos)` SEM filtro de `deletedAt` (serve de mapa id→nome; filtrar quebraria
o nome de campanha cujo nicho foi removido).

De `src/components/app-sidebar.tsx`: array de itens no formato `{ href, label, icon }`, ícones de
`lucide-react`; ativo por `pathname.startsWith(item.href)`. Item de Campanhas: `{ href: "/campanhas", label: "Campanhas", icon: Target }`.

Primitivos disponíveis em `src/components/ui/` (NÃO instalar nenhum novo): `badge`, `button`,
`field`, `select`, `separator`, `table`.
</interfaces>
</context>

<decisions>
- **D-24-06**: o Mapa de Nichos filtra e ordena CLIENT-SIDE, com `useState` + `useMemo` sobre as
  linhas recebidas por prop do Server Component. Sem `@tanstack/react-table`, sem querystring, sem
  paginação: a lista é de dezenas de campanhas, e a alternativa traria colunas dinâmicas e estado de
  tabela que ninguém pediu. Consequência de segurança desejável: nenhum parâmetro de filtro toca SQL.
- **D-24-11**: o filtro "Veredito" opera sobre o veredito FINAL do usuário (é a decisão que importa
  para triagem) e inclui a opção "Sem veredito" — campanha ainda em exploração é justamente o que o
  operador precisa achar. O veredito da IA é exibido em coluna própria, mas não é filtro.
- **D-24-05**: `VereditoSugeridoChip` é reusado para as duas colunas de veredito; o cabeçalho da
  coluna é quem diz de quem é a decisão.
- **D-24-07**: nenhum pacote novo, nenhum primitivo shadcn novo.
</decisions>

<tasks>

<task type="auto">
  <name>Task 1: Tabela client do Mapa de Nichos com filtros e ordenação</name>
  <files>src/components/mapa-de-nichos-table.tsx</files>
  <read_first>
    - src/components/campanha-list.tsx (Client Component com `useMemo`, `Link` por linha para `/campanhas/{id}`, estado vazio com título + texto + CTA)
    - src/app/relatorios/page.tsx (idioma visual de tabela: `Table`/`TableHeader`/`TableBody`, `text-right font-mono tabular-nums`, ênfase por peso, nunca por cor)
    - src/components/veredito-sugerido-chip.tsx (contrato e rótulos canônicos)
    - src/components/lead-table-toolbar.tsx (idioma de barra de filtros já usada no projeto)
  </read_first>
  <action>
    Criar `src/components/mapa-de-nichos-table.tsx` com `"use client"`, exportando o TIPO do contrato
    ANTES do componente (é ele que o `page.tsx` da Task 2 implementa):

    `export type MapaLinha = { campanhaId: number; nichoNome: string; oferta: string; estado: "explorando" | "veredito_registrado" | "em_escala" | "abandonada"; vereditoIA: VereditoDecisao | null; vereditoFinal: VereditoDecisao | null; vereditoDecididoEm: Date | null; total: number; fechados: number; taxa: number; ticketMedioCentavos: number | null }`

    e `export function MapaDeNichosTable({ linhas }: { linhas: MapaLinha[] })`.

    Estado local (D-24-06): `filtroVeredito` (valores `todos` | `aprofundar` | `mudar_angulo` |
    `abandonar` | `sem_veredito`, default `todos`), `filtroNicho` (`todos` ou o nome do nicho,
    default `todos`) e `ordenarPor` (`nicho` | `veredito` | `leads` | `taxa`, default `nicho`).
    As opções de nicho são derivadas das próprias `linhas` com `useMemo` (nomes únicos, ordem
    alfabética via `localeCompare` com locale pt-BR) — nunca uma lista hardcoded.

    `linhasVisiveis` em UM `useMemo` dependente de `[linhas, filtroVeredito, filtroNicho, ordenarPor]`:
    filtra (`sem_veredito` casa com `vereditoFinal === null`) e então ordena sobre uma CÓPIA do array
    (`[...]`, nunca `sort` in-place sobre a prop):
      • `nicho` → `nichoNome.localeCompare(b, "pt-BR")`, desempate por `oferta`
      • `veredito` → ordem fixa aprofundar → mudar_angulo → abandonar → sem veredito (mapa de peso
        numérico, não ordem alfabética), desempate por `nichoNome`
      • `leads` → `total` decrescente, desempate por `nichoNome`
      • `taxa` → `taxa` decrescente, desempate por `total` decrescente

    Barra de filtros com três `Select` rotulados ("Veredito", "Nicho", "Ordenar por") e um contador
    textual "{n} de {m} campanhas" à direita.

    Tabela com as colunas, nesta ordem: Nicho (nome + `oferta` em linha secundária
    `text-muted-foreground`, tudo envolvido num `Link` para `/campanhas/{campanhaId}`), Estado
    (`CampanhaEstadoBadge`), Veredito da IA (`VereditoSugeridoChip` ou "—"), Veredito final (chip
    mais, abaixo, a data `format(vereditoDecididoEm, "dd/MM/yyyy")` quando houver; senão "—"),
    Leads (`total`), Fechados, Conversão (`formatarTaxaConversao(taxa)`), Ticket médio
    (`formatCentsToBRL` ou "—"). Colunas numéricas alinhadas à direita com `font-mono tabular-nums`.

    Dois estados vazios DISTINTOS: `linhas.length === 0` → bloco com borda tracejada dizendo que
    nenhuma campanha foi criada ainda e um `Link` botão para `/campanhas`; `linhas.length > 0 && linhasVisiveis.length === 0`
    → linha única na tabela com "Nenhuma campanha com esses filtros."

    Só tokens de cor (`text-muted-foreground`, `bg-card`, `border`, `text-foreground`) — `verify:brand`
    reprova qualquer cor literal. Nenhum import de `@/db/*` neste arquivo.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run verify:brand</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sai 0, `npm run lint` sai 0, `npm run verify:brand` sai 0.
    - `grep -c "@/db/" src/components/mapa-de-nichos-table.tsx` retorna 0.
    - `grep -c "export type MapaLinha" src/components/mapa-de-nichos-table.tsx` retorna 1.
    - `grep -c "@tanstack/react-table" src/components/mapa-de-nichos-table.tsx` retorna 0 (D-24-06).
    - `grep -c "sem_veredito" src/components/mapa-de-nichos-table.tsx` retorna >= 2 — a opção de filtro existe e é aplicada (D-24-11).
    - `grep -c "linhas.sort(" src/components/mapa-de-nichos-table.tsx` retorna 0 — a prop nunca é ordenada in-place.
  </acceptance_criteria>
  <done>Componente de tabela pronto e tipado, com filtro por veredito e por nicho, 4 ordenações e dois estados vazios.</done>
</task>

<task type="auto">
  <name>Task 2: Rota /mapa-de-nichos e item de navegação</name>
  <files>src/app/mapa-de-nichos/page.tsx, src/components/app-sidebar.tsx</files>
  <read_first>
    - src/app/campanhas/page.tsx (molde exato de carga: `Promise.all`, `isNull(campanhas.deletedAt)`, lista de nichos sem filtro de deletedAt)
    - src/components/mapa-de-nichos-table.tsx (o tipo `MapaLinha` criado na Task 1 — o contrato a implementar)
    - src/db/queries.ts (`getResultadoPorCampanha`, `getVereditoIAPorCampanha`, `computeTaxaConversao`)
    - src/components/app-sidebar.tsx (array de itens e ícones importados de lucide-react)
  </read_first>
  <action>
    (a) Criar `src/app/mapa-de-nichos/page.tsx` — Server Component async, sem `searchParams` (o filtro
    é client-side, D-24-06). Carregar em UM `Promise.all`: campanhas ativas
    (`isNull(campanhas.deletedAt)`, `orderBy(desc(campanhas.createdAt))`), todos os nichos (sem filtro
    de `deletedAt`, como em `/campanhas`), `getResultadoPorCampanha()` e `getVereditoIAPorCampanha()`
    — as duas ÚLTIMAS sem argumento, para trazer o mapa inteiro numa consulta só em vez de uma por
    campanha (nada de loop com await dentro).

    Montar `linhas: MapaLinha[]` mapeando as campanhas: `nichoNome` via `Map` id→nome com fallback
    "—"; `vereditoIA` = `vereditoPorCampanha.get(id) ?? null`; `vereditoFinal` e `vereditoDecididoEm`
    direto da linha; `resultado` = `resultadoPorCampanha.get(id)` com fallback
    `{ total: 0, fechados: 0, perdidos: 0, ticketMedioCentavos: null }`; `taxa` =
    `computeTaxaConversao({ total, fechados })` calculado NO SERVIDOR (o componente client só
    formata). Renderizar `<h1 className="text-[28px] font-semibold leading-tight">Mapa de Nichos</h1>`
    mais um parágrafo curto de contexto e `<MapaDeNichosTable linhas={linhas} />`.

    Doc-comment citando PAINEL-02/PAINEL-03, D-24-06 e D-24-08 (veredito da IA = geração ok mais
    recente, re-validada).

    (b) Em `src/components/app-sidebar.tsx`: adicionar
    `{ href: "/mapa-de-nichos", label: "Mapa de Nichos", icon: Map }` IMEDIATAMENTE após o item
    `/campanhas` (as duas telas são a mesma família), importando `Map` de `lucide-react`. Atenção ao
    conflito de nome: o arquivo pode já usar `Map` como tipo global do JS em outro lugar — se houver
    qualquer ambiguidade, importar como `Map as MapIcon` e usar `MapIcon`. Nenhuma outra mudança no
    componente.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run verify:brand && npm run verify:theme && npm run build</automated>
    <human-check>
      Rodar `npm run dev` e, no tema claro E no escuro:
      1. Clicar em "Mapa de Nichos" na barra lateral → a tela abre e o item fica marcado como ativo.
      2. Conferir que cada campanha criada aparece com o nicho certo e que o resumo (leads, fechados, conversão) bate com o painel da própria campanha em `/campanhas/{id}`.
      3. Filtrar por "Sem veredito" → só campanhas ainda sem decisão registrada.
      4. Filtrar por um nicho específico → só as campanhas daquele nicho.
      5. Trocar "Ordenar por" entre nicho e veredito → a ordem muda de forma coerente.
      6. Clicar numa linha → cai no detalhe da campanha correta.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit`, `npm run lint`, `npm run verify:brand`, `npm run verify:theme` saem 0.
    - `npm run build` sai 0 e o número de rotas compiladas é MAIOR que o do build anterior (registrar os dois números no SUMMARY) — `/mapa-de-nichos` aparece na listagem de rotas.
    - `grep -c "getResultadoPorCampanha()" src/app/mapa-de-nichos/page.tsx` retorna 1 e `grep -c "getVereditoIAPorCampanha()" src/app/mapa-de-nichos/page.tsx` retorna 1 — chamadas sem argumento, uma vez cada, nunca dentro de `.map()`.
    - `grep -c "await" src/app/mapa-de-nichos/page.tsx` retorna 1 — um único `await` (o do `Promise.all`), prova de que não há N+1.
    - `grep -c "mapa-de-nichos" src/components/app-sidebar.tsx` retorna 1.
    - `grep -c "searchParams" src/app/mapa-de-nichos/page.tsx` retorna 0 (D-24-06: filtro não vem da URL).
  </acceptance_criteria>
  <done>`/mapa-de-nichos` existe, é alcançável pela barra lateral e lista todas as campanhas com os dois vereditos e o resultado real.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Banco → render | Nomes de nicho, oferta e payload de diagnóstico chegam à tela |
| Interação do usuário → filtro | Filtros e ordenação existem só na memória do navegador |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-14 | Injection (SQLi) | Filtros do Mapa | mitigate | D-24-06 — filtro/ordenação são `useMemo` sobre um array já em memória; nenhum valor de filtro chega a SQL nem à querystring, não existe superfície de injeção |
| T-24-15 | Information Disclosure | Campanhas removidas no Mapa | mitigate | `isNull(campanhas.deletedAt)` na carga da página, mesmo predicado já usado em `/campanhas` |
| T-24-16 | Tampering | Render do veredito da IA | mitigate | O veredito vem de `getVereditoIAPorCampanha`, que re-valida o payload com `diagnosticoSchema.safeParse` (D-24-08); a tela só recebe uma das 3 strings do enum ou `null` — nunca texto livre vindo do LLM |
| T-24-17 | Denial of Service | Página carregando todas as campanhas e todos os payloads | accept | Uso solo, dezenas de campanhas; agregação por `GROUP BY` em SQL e 4 consultas fixas (sem N+1, asserido pelo gate de um único `await`). Revisar só se a base passar de milhares de linhas |
| T-24-SC | Tampering | Cadeia de suprimentos (npm) | accept | Zero instalação de pacote e zero primitivo shadcn novo (D-24-07) |
</threat_model>

<verification>
1. `npx tsc --noEmit` → 0.
2. `npm run lint` → 0.
3. `npm run verify:brand` e `npm run verify:theme` → 0.
4. `npm run build` → 0, com `/mapa-de-nichos` na listagem de rotas.
5. Checagem humana de fim de fase conforme o bloco `<human-check>` da Task 2.
</verification>

<success_criteria>
- PAINEL-02 observável: uma tela lista TODAS as campanhas com nicho, veredito da IA, veredito final e resumo do resultado real.
- PAINEL-03 observável: a lista filtra por veredito (incluindo "sem veredito") e por nicho, e ordena por veredito e por nicho.
- Nenhuma consulta por linha (N+1) e nenhum pacote novo.
</success_criteria>

<output>
Create `.planning/phases/24-veredito-e-mapa-de-nichos/24-04-SUMMARY.md` when done
</output>
