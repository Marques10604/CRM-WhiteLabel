---
phase: 24-veredito-e-mapa-de-nichos
plan: 03
type: execute
wave: 2
depends_on: ["24-01", "24-02"]
files_modified:
  - src/components/resultado-campanha-painel.tsx
  - src/components/veredito-secao.tsx
  - src/app/campanhas/[id]/_components/veredito-form.tsx
  - src/app/campanhas/[id]/page.tsx
autonomous: true
requirements: [VEREDITO-01, VEREDITO-02, PAINEL-01]

must_haves:
  truths:
    - "Na tela da campanha o usuário vê contagem de leads, fechados, taxa de conversão, ticket médio e motivos de perda só dos leads vinculados àquela campanha"
    - "Na tela da campanha o usuário escolhe entre aprofundar / mudar o ângulo / abandonar e registra sua decisão"
    - "Depois de registrar, a tela mostra a decisão registrada e a data em que foi tomada"
    - "A sugestão da IA e a decisão do usuário aparecem lado a lado, rotuladas de forma distinta, e a divergência entre elas é tratada como normal"
    - "Campanha sem lead vinculado mostra um estado vazio explicativo, nunca uma tabela em branco nem 0% sem contexto"
  artifacts:
    - path: "src/components/resultado-campanha-painel.tsx"
      provides: "Painel de resultado real agregado da campanha (PAINEL-01)"
      contains: "getResultadoPorCampanha"
    - path: "src/components/veredito-secao.tsx"
      provides: "Seção de veredito: sugestão da IA, decisão do usuário, data e formulário"
      contains: "getVereditoIAPorCampanha"
    - path: "src/app/campanhas/[id]/_components/veredito-form.tsx"
      provides: "Formulário client de registro do veredito"
      contains: "registrarVeredito"
  key_links:
    - from: "src/app/campanhas/[id]/page.tsx"
      to: "src/components/resultado-campanha-painel.tsx"
      via: "render de <ResultadoCampanhaPainel campanhaId={campanhaId} />"
      pattern: "ResultadoCampanhaPainel"
    - from: "src/app/campanhas/[id]/_components/veredito-form.tsx"
      to: "src/actions/campanha-actions.ts"
      via: "useActionState sobre registrarVeredito"
      pattern: "useActionState"
    - from: "src/components/veredito-secao.tsx"
      to: "src/components/veredito-sugerido-chip.tsx"
      via: "reuso do chip para os dois vereditos (D-24-05)"
      pattern: "VereditoSugeridoChip"
---

<objective>
Entregar, na página `/campanhas/[id]`, as duas seções que fecham o loop da campanha: o **painel de
resultado real** (PAINEL-01 — só os leads vinculados) e a **seção de veredito** (VEREDITO-01/02 — a
decisão do operador ao lado da sugestão da IA, com a data do registro).

Purpose: é aqui que o usuário efetivamente decide e vê se o nicho deu retorno; sem esta tela, as
colunas e queries das Ondas anteriores não têm superfície.
Output: 3 componentes novos + `page.tsx` estendida, sem retrabalhar o que a Fase 22/23 já colocou lá.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-veredito-e-mapa-de-nichos/24-01-SUMMARY.md
@.planning/phases/24-veredito-e-mapa-de-nichos/24-02-SUMMARY.md

Precedentes obrigatórios (ler, não adivinhar):
@src/app/campanhas/[id]/page.tsx
@src/components/diagnostico-secao.tsx
@src/app/campanhas/[id]/_components/gerar-diagnostico-button.tsx
@src/app/relatorios/page.tsx
@src/components/veredito-sugerido-chip.tsx

<interfaces>
<!-- Contratos reais. O executor NÃO deve explorar o repo atrás deles. -->

Do plano 24-02 (`src/db/queries.ts`):
`export type ResultadoCampanha = { total: number; fechados: number; perdidos: number; ticketMedioCentavos: number | null }`
`export async function getResultadoPorCampanha(campanhaId?: number): Promise<Map<number, ResultadoCampanha>>`  — campanha sem lead vinculado NÃO aparece no Map
`export async function getVereditoIAPorCampanha(campanhaId?: number): Promise<Map<number, "aprofundar" | "mudar_angulo" | "abandonar">>`
`export async function getContagemPorMotivoPerda(range: PeriodRange, campanhaId?: number): Promise<{ motivoPerdaId: number | null; nome: string; total: number }[]>`
`export function resolvePeriodRange(preset: string | undefined, now?: Date): PeriodRange`  — passar `undefined` devolve o range "tudo"
`export function computeTaxaConversao(row: { total: number; fechados: number }): number`

Do plano 24-02 (`src/lib/utils.ts`): `export function formatarTaxaConversao(taxa: number): string`
De `src/lib/money.ts` (Fase 01): `export function formatCentsToBRL(cents: number): string`

Do plano 24-01:
`src/lib/validations.ts` → `export const vereditoSchema`, `export type VereditoFinal = "aprofundar" | "mudar_angulo" | "abandonar"`, `export const VEREDITO_VALORES`
`src/actions/campanha-actions.ts` → `export async function registrarVeredito(_prevState: ActionState, formData: FormData): Promise<ActionState>` — lê `campanhaId` e `vereditoFinal` do FormData
`ActionState = { success: true; campanha?: Campanha } | { errors: Record<string, string[] | undefined> } | undefined`
`campanhas.vereditoFinal` / `campanhas.vereditoDecididoEm` (Date | null) já vêm na linha carregada por `page.tsx`

De `src/components/veredito-sugerido-chip.tsx` (Fase 23):
`export type VereditoDecisao = "aprofundar" | "mudar_angulo" | "abandonar"`
`export function VereditoSugeridoChip({ decisao }: { decisao: VereditoDecisao })` — só o `<Badge>`; o rótulo que dá contexto é do container

Primitivos disponíveis em `src/components/ui/` (NÃO instalar nenhum novo):
`badge`, `button`, `calendar`, `combobox`, `dialog`, `field` (Field, FieldContent, FieldDescription, FieldError, FieldLabel), `input`, `popover`, `select` (Select, SelectContent, SelectItem, SelectTrigger, SelectValue), `separator`, `table` (Table, TableBody, TableCell, TableHead, TableHeader, TableRow), `textarea`

De `src/app/campanhas/[id]/page.tsx` (Fase 22/23): já carrega `row = { campanha, nichoNome }` via
leftJoin, valida `id` como inteiro positivo, chama `notFound()` para inexistente/soft-deletada,
declara `export const maxDuration = 120` e renderiza `<DiagnosticoSecao campanhaId={campanhaId} />`.
</interfaces>
</context>

<decisions>
- **D-24-03**: o `Select` de veredito NUNCA nasce pré-preenchido com a sugestão da IA. Sem veredito
  registrado, ele mostra o placeholder "Escolha o veredito"; com veredito já registrado, ele nasce
  com a decisão DO USUÁRIO. Pré-selecionar a sugestão transformaria um insumo não vinculante
  (DIAGNOSTICO-09) num default silencioso — e a dívida técnica nº 1 da Fase 23 (variância de
  veredito entre execuções idênticas, registrada em STATE.md) torna isso ativamente perigoso.
- **D-24-05**: reusar `VereditoSugeridoChip` para exibir TAMBÉM o veredito final do usuário — mesmos
  3 valores, mesmos tokens `--status-*`. O que distingue "Sugestão da IA (não vinculante)" de "Sua
  decisão" é o rótulo do container, exatamente como o doc-comment do componente já prevê. Sem
  componente novo, sem rename (renomear rippla em `diagnostico-resultado.tsx`).
- **D-24-10**: ordem narrativa das seções em `/campanhas/[id]`: dados da campanha → Resultado real →
  Diagnóstico de IA → Veredito. A decisão fica por último, depois do que a sustenta.
- **D-24-07**: nenhum pacote novo, nenhum primitivo shadcn novo (host de 4GB).
</decisions>

<tasks>

<task type="auto">
  <name>Task 1: Painel de resultado real da campanha</name>
  <files>src/components/resultado-campanha-painel.tsx</files>
  <read_first>
    - src/app/relatorios/page.tsx (idioma visual das seções: `<section className="flex flex-col gap-4 rounded-lg border bg-card p-6">`, `<h2 className="text-[20px] font-semibold leading-tight">`, tabela com `text-right font-mono tabular-nums`, ênfase por PESO e nunca por cor, texto de ajuda sempre visível e nunca tooltip)
    - src/components/diagnostico-secao.tsx (molde de Server Component async que consulta o banco e decide entre estados)
    - src/db/queries.ts (assinaturas criadas no plano 24-02)
    - src/lib/money.ts (formatCentsToBRL)
  </read_first>
  <action>
    Criar `src/components/resultado-campanha-painel.tsx` exportando
    `export async function ResultadoCampanhaPainel({ campanhaId }: { campanhaId: number })` — Server
    Component (sem `"use client"`).

    Carregar em UM `Promise.all`: `getResultadoPorCampanha(campanhaId)` e
    `getContagemPorMotivoPerda(resolvePeriodRange(undefined), campanhaId)` — o `undefined` resolve
    para o range "tudo" (D-24-07: o painel não filtra por período; a janela da campanha é
    informativa). Derivar `resultado` de `map.get(campanhaId)` com fallback explícito
    `{ total: 0, fechados: 0, perdidos: 0, ticketMedioCentavos: null }` (campanha sem lead vinculado
    não aparece no Map).

    Renderizar uma `<section>` no idioma visual de `/relatorios`, com `<h2 id="resultado-campanha-titulo">`
    "Resultado real" e um texto de apoio curto dizendo que são apenas os leads vinculados a esta
    campanha, sem recorte de período.

    Quando `resultado.total === 0`: renderizar SÓ um estado vazio com texto explicando que nenhum
    lead foi vinculado ainda e como vincular (editar um lead e escolher esta campanha no campo
    "Campanha") — nunca tabela vazia, nunca "0%" solto.

    Quando `resultado.total > 0`: quatro métricas em linha (grid responsivo), cada uma com rótulo
    pequeno em `text-muted-foreground` e valor em `font-mono tabular-nums`:
      • "Leads vinculados" = `total`
      • "Fechados" = `fechados`
      • "Taxa de conversão" = `formatarTaxaConversao(computeTaxaConversao({ total, fechados }))`
      • "Ticket médio" = `formatCentsToBRL(ticketMedioCentavos)` quando não-nulo, senão o caractere
        "—" mais a legenda "nenhum lead fechado ainda" (D-24-04 — nunca "R$ 0,00")
    Abaixo, a tabela de motivos de perda (colunas "Motivo da perda" e "Leads perdidos",
    `Table`/`TableHeader`/`TableBody` de `@/components/ui/table`), com o MESMO texto de ajuda sempre
    visível já usado em `/relatorios` adaptado: esta lista considera a data em que o lead foi movido
    para Perdido, então leads perdidos antes de o CRM passar a registrar essa data não aparecem —
    e por isso a contagem aqui pode ser menor que o número de perdidos das métricas acima. Se
    `contagemMotivoPerda.length === 0`, mostrar "Nenhum lead perdido com motivo registrado."

    Cores SOMENTE por token (`text-muted-foreground`, `bg-card`, `border`, `text-foreground`) — o
    gate `verify:brand` falha com qualquer hex/classe de cor literal.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run verify:brand</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sai 0, `npm run lint` sai 0, `npm run verify:brand` sai 0.
    - `grep -c "use client" src/components/resultado-campanha-painel.tsx` retorna 0 — é Server Component.
    - `grep -c "resolvePeriodRange(undefined)" src/components/resultado-campanha-painel.tsx` retorna 1 (D-24-07).
    - `grep -c "R\\$ 0,00" src/components/resultado-campanha-painel.tsx` retorna 0 e o arquivo contém o fallback "—" para ticket nulo.
    - `grep -c "getResultadoPorCampanha\\|getContagemPorMotivoPerda" src/components/resultado-campanha-painel.tsx` retorna 2 — nenhuma query SQL escrita direto no componente.
  </acceptance_criteria>
  <done>Componente de painel pronto, com estado vazio e fallback de ticket, consumindo só as funções de `@/db/queries`.</done>
</task>

<task type="auto">
  <name>Task 2: Formulário client de registro do veredito</name>
  <files>src/app/campanhas/[id]/_components/veredito-form.tsx</files>
  <read_first>
    - src/app/campanhas/[id]/_components/gerar-diagnostico-button.tsx (molde exato: `useActionState`, envio dentro de `startTransition`, `useEffect` reagindo a `state` com toast sonner, Client Component importando SÓ a Server Action)
    - src/components/lead-form-dialog.tsx linhas 255-330 (uso real de Select + Field + FieldError com react-hook-form/Controller — aqui o formulário é mais simples, mas o idioma visual do Select é o mesmo)
    - src/actions/campanha-actions.ts (assinatura de `registrarVeredito` e shape de `ActionState`, criados no plano 24-01)
    - src/lib/validations.ts (VEREDITO_VALORES e o tipo VereditoFinal)
  </read_first>
  <action>
    Criar `src/app/campanhas/[id]/_components/veredito-form.tsx` com `"use client"`, exportando
    `export function VereditoForm({ campanhaId, vereditoAtual }: { campanhaId: number; vereditoAtual: VereditoFinal | null })`.

    Estado: `const [decisao, setDecisao] = useState<string>(vereditoAtual ?? "")` — D-24-03: o valor
    inicial é o veredito DO USUÁRIO ou vazio; a sugestão da IA nunca entra aqui (o componente nem
    recebe essa prop, de propósito — documentar isso no doc-comment).

    `useActionState<ActionState, FormData>(registrarVeredito, undefined)` no molde do
    `GerarDiagnosticoButton`. Submissão por `handleRegistrar` que monta um `FormData` com
    `campanhaId` (String) e `vereditoFinal` (a decisão escolhida) e chama `formAction` dentro de
    `startTransition`. `useEffect` sobre `state`: sucesso → `toast.success("Veredito registrado.")`;
    objeto com `errors` → `toast.error(...)` com a primeira mensagem disponível, caindo para
    "Não foi possível registrar o veredito." quando não houver mensagem.

    UI: um `Field` com `FieldLabel` "Seu veredito", um `Select` controlado
    (`value={decisao}` / `onValueChange={setDecisao}`) com `SelectTrigger` + `SelectValue`
    (placeholder "Escolha o veredito") e três `SelectItem` — value `aprofundar` rótulo "Aprofundar",
    value `mudar_angulo` rótulo "Mudar o ângulo", value `abandonar` rótulo "Abandonar" (os mesmos
    rótulos do `VEREDITO_LABEL` de `veredito-sugerido-chip.tsx`; derive a lista de um array local de
    3 objetos, não de JSX repetido). Um `Button type="button"` rotulado "Registrar veredito" quando
    `vereditoAtual` é null e "Atualizar veredito" quando já existe, desabilitado quando
    `decisao === ""` ou `pending`, mostrando `Loader2` com `animate-spin` durante o pending.
    `FieldError` renderizando `state.errors.vereditoFinal` / `state.errors.campanhaId` quando houver.

    Uma `FieldDescription` sempre visível: registrar o veredito só grava a sua decisão e a data — não
    arquiva leads, não muda etapa de ninguém, não dispara nada (VEREDITO-03, texto para o usuário).

    Não importar nada de `@/db/*` nem de `@/lib/ai/*` neste arquivo (é Client Component).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run verify:brand</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sai 0, `npm run lint` sai 0, `npm run verify:brand` sai 0.
    - `grep -c "use client" src/app/campanhas/\[id\]/_components/veredito-form.tsx` retorna 1.
    - `grep -c "@/db/\\|@/lib/ai/" src/app/campanhas/\[id\]/_components/veredito-form.tsx` retorna 0.
    - `grep -c "vereditoSugerido\\|vereditoIA" src/app/campanhas/\[id\]/_components/veredito-form.tsx` retorna 0 — D-24-03: o formulário não conhece a sugestão da IA.
    - `grep -c "startTransition" src/app/campanhas/\[id\]/_components/veredito-form.tsx` retorna >= 1 (molde do `GerarDiagnosticoButton`).
  </acceptance_criteria>
  <done>Formulário de veredito funcional, controlado, sem pré-preenchimento pela IA e sem acesso ao banco.</done>
</task>

<task type="auto">
  <name>Task 3: Seção de veredito e fiação das duas seções na página da campanha</name>
  <files>src/components/veredito-secao.tsx, src/app/campanhas/[id]/page.tsx</files>
  <read_first>
    - src/app/campanhas/[id]/page.tsx (a página inteira — guard de id, leftJoin, notFound, ordem atual das seções)
    - src/components/diagnostico-secao.tsx (Server Component async que consulta e compõe)
    - src/components/veredito-sugerido-chip.tsx (VEREDITO_LABEL, VereditoDecisao, contrato do chip)
    - src/db/queries.ts (`getVereditoIAPorCampanha`)
    - .planning/STATE.md, seção "Dívida técnica FINAL da Fase 23", item 1 (variância de veredito entre execuções idênticas — é a justificativa da copy de divergência)
  </read_first>
  <action>
    (a) Criar `src/components/veredito-secao.tsx` exportando
    `export async function VereditoSecao({ campanhaId, vereditoFinal, vereditoDecididoEm }: { campanhaId: number; vereditoFinal: VereditoFinal | null; vereditoDecididoEm: Date | null })`
    — Server Component. Buscar `getVereditoIAPorCampanha(campanhaId)` e derivar
    `vereditoIA = map.get(campanhaId)`.

    Renderizar `<section>` no idioma visual das outras (borda + `bg-card` + `p-6`), com
    `<h2 id="veredito-secao-titulo">` "Veredito".

    Duas linhas rotuladas, lado a lado em telas largas:
      • "Sugestão da IA (não vinculante)" → `<VereditoSugeridoChip decisao={vereditoIA} />` quando
        existir; senão o texto "Nenhum diagnóstico gerado ainda." (D-24-08 cobre também o caso de
        payload inválido — o usuário vê o mesmo texto, sem erro na tela).
      • "Sua decisão" → `<VereditoSugeridoChip decisao={vereditoFinal} />` (D-24-05) seguido de
        "registrada em " mais `format(vereditoDecididoEm, "dd/MM/yyyy")` quando ambos existirem;
        senão "Nenhum veredito registrado ainda."

    Quando os DOIS existirem e forem diferentes, renderizar uma linha de apoio em
    `text-muted-foreground` dizendo, em PT-BR, que divergir da sugestão é esperado: a sugestão vem de
    uma única execução de busca e pode variar entre gerações — quem decide é o operador. Sem ícone de
    alerta, sem cor de erro: não é um problema.

    Fechar com `<VereditoForm campanhaId={campanhaId} vereditoAtual={vereditoFinal} />`.

    (b) Em `src/app/campanhas/[id]/page.tsx`: importar e renderizar, na ordem de D-24-10 —
    `<ResultadoCampanhaPainel campanhaId={campanhaId} />` logo após o `<dl>` existente, depois o
    `<DiagnosticoSecao campanhaId={campanhaId} />` que já está lá, e por último
    `<VereditoSecao campanhaId={campanhaId} vereditoFinal={campanha.vereditoFinal} vereditoDecididoEm={campanha.vereditoDecididoEm} />`.
    NENHUMA outra linha da página muda: o guard de `id`, o `leftJoin`, o `notFound()`, o
    `maxDuration` e o cabeçalho ficam exatamente como estão. Atualizar o doc-comment da página para
    registrar que a Fase 24 adicionou as duas seções (o comentário atual já antecipava isso).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run verify:brand && npm run verify:theme && npm run build</automated>
    <human-check>
      Rodar `npm run dev`, abrir `/campanhas/{id}` de uma campanha com leads vinculados e conferir,
      no tema claro E no escuro:
      1. As métricas do painel batem com os leads que aparecem em `/leads` filtrados por essa campanha.
      2. Registrar "Abandonar" → toast de sucesso, chip "Abandonar" em "Sua decisão" e a data de hoje.
      3. Recarregar a página → a decisão e a data continuam lá.
      4. Com um diagnóstico gerado de veredito diferente, a linha de divergência aparece e não parece um erro.
      5. Abrir uma campanha recém-criada sem leads → estado vazio explicativo, sem tabela em branco.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit`, `npm run lint`, `npm run verify:brand`, `npm run verify:theme` e `npm run build` saem 0 (registrar no SUMMARY o número de rotas compiladas pelo build).
    - `grep -c "ResultadoCampanhaPainel\\|VereditoSecao" "src/app/campanhas/[id]/page.tsx"` retorna 4 (2 imports + 2 usos).
    - `grep -c "notFound();" "src/app/campanhas/[id]/page.tsx"` continua retornando 2 — os guards da Fase 22 (id inválido e campanha inexistente/soft-deletada) estão intactos. O ponto-e-vírgula é obrigatório no padrão: o arquivo tem 3 ocorrências de `notFound()`, sendo a terceira uma MENÇÃO dentro do doc-comment de topo (linha 23), que esta task reescreve — um grep sem o `;` seria sensível à redação do comentário.
    - `grep -c "VereditoSugeridoChip" src/components/veredito-secao.tsx` retorna >= 2 — o mesmo chip serve aos dois vereditos (D-24-05), sem componente novo.
    - `ls src/components/veredito-final-chip.tsx 2>/dev/null` não encontra nada — nenhum chip duplicado foi criado.
  </acceptance_criteria>
  <done>`/campanhas/[id]` mostra resultado real, diagnóstico e veredito, nesta ordem, e o registro do veredito persiste e reaparece após recarregar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL → Server Component | `params.id` de `/campanhas/[id]` chega cru |
| Client Component → Server Action | O `vereditoFinal` submetido vem do navegador |
| Banco → render | `payload` de diagnóstico e valores de lead viram texto na tela |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-10 | Tampering / Information Disclosure | `page.tsx` / `params.id` | mitigate | Guard já existente e preservado: `Number.isInteger(campanhaId) && campanhaId > 0` antes de qualquer query, `notFound()` para inexistente ou soft-deletada (T-22-07/T-22-08) — as duas seções novas só recebem o id DEPOIS do guard |
| T-24-11 | Tampering | `VereditoForm` → `registrarVeredito` | mitigate | O formulário é conveniência de UI; a autoridade é o `vereditoSchema.safeParse` + `campanhaAtivaExists` do lado servidor (plano 24-01). Desabilitar o botão nunca é a defesa |
| T-24-12 | Information Disclosure | Painel exibindo leads da Lixeira | mitigate | `isNull(leads.deletedAt)` dentro das queries (plano 24-02, caso 1 do harness); o componente não escreve SQL próprio |
| T-24-13 | Spoofing (confusão de origem) | Chip da IA × chip do usuário | mitigate | Rótulos textuais distintos e obrigatórios ("Sugestão da IA (não vinculante)" × "Sua decisão"); `VereditoForm` não recebe a sugestão (D-24-03), então não há caminho para a sugestão virar decisão registrada sem gesto humano |
| T-24-SC | Tampering | Cadeia de suprimentos (npm) | accept | Zero instalação de pacote e zero primitivo shadcn novo (D-24-07) |
</threat_model>

<verification>
1. `npx tsc --noEmit` → 0.
2. `npm run lint` → 0.
3. `npm run verify:brand` → 0 (nenhuma cor literal nos 3 componentes novos).
4. `npm run verify:theme` → 0.
5. `npm run build` → 0, com as rotas existentes + nenhuma rota nova (esta plano não cria rota).
6. Checagem humana de fim de fase conforme o bloco `<human-check>` da Task 3.
</verification>

<success_criteria>
- PAINEL-01 observável: contagem, conversão, motivos de perda e ticket médio dos leads DAQUELA campanha, reaproveitando as funções de `/relatorios`.
- VEREDITO-01/02 observáveis: o usuário escolhe entre os 3 vereditos, registra, e vê sua decisão com a data — podendo divergir da IA sem atrito.
- Nenhuma regressão em `/campanhas/[id]`: guards, diagnóstico e cabeçalho intactos.
</success_criteria>

<output>
Create `.planning/phases/24-veredito-e-mapa-de-nichos/24-03-SUMMARY.md` when done
</output>
