---
phase: 09-timeline-de-intera-es
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - src/components/lead-timeline-dialog.tsx
  - src/components/delete-nota-dialog.tsx
autonomous: true
requirements: [TIMELINE-01, TIMELINE-02]

must_haves:
  truths:
    - "Ao abrir a superfície de histórico de um lead, o admin vê todas as interações ativas daquele lead, mais recentes primeiro"
    - "Quando não há nenhuma interação, o admin vê um estado vazio explicativo e ainda assim consegue registrar a primeira nota"
    - "O admin registra uma nota manual sem sair da superfície, e ela aparece imediatamente no topo da lista"
    - "O admin edita e exclui uma nota manual sua; entradas automáticas de WhatsApp não oferecem nenhum controle de editar/excluir"
    - "O texto integral de cada entrada é exibido sem truncamento, preservando quebras de linha"
  artifacts:
    - path: "src/components/lead-timeline-dialog.tsx"
      provides: "Superfície dedicada da timeline (D-02): lista cronológica + composer + edição inline"
      min_lines: 150
      exports: ["LeadTimelineDialog"]
    - path: "src/components/delete-nota-dialog.tsx"
      provides: "Confirmação de exclusão de nota manual empilhada sobre a timeline"
      exports: ["DeleteNotaDialog"]
  key_links:
    - from: "src/components/lead-timeline-dialog.tsx"
      to: "src/actions/interacao-actions.ts"
      via: "chamadas imperativas em useEffect/startTransition"
      pattern: "getInteracoesByLead\\("
    - from: "src/components/lead-timeline-dialog.tsx"
      to: "src/lib/validations.ts (notaManualTextoSchema)"
      via: "zodResolver do formulário do composer"
      pattern: "zodResolver\\(notaManualTextoSchema\\)"
---

<objective>
Construir a superfície própria da timeline (D-02): um `Dialog` dedicado por lead que lê o histórico completo em ordem cronológica, permite registrar uma nota manual pelo composer fixo no topo, e oferece editar/excluir SOMENTE para notas manuais (D-06).

Purpose: é a metade visível de TIMELINE-02 e a metade manual de TIMELINE-01. Sem ela, os registros gravados pelo plano 09-02 são invisíveis para o admin.
Output: `LeadTimelineDialog` e `DeleteNotaDialog`, prontos para serem plugados nos 3 pontos de entrada (plano 09-04).
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-timeline-de-intera-es/09-CONTEXT.md
@.planning/phases/09-timeline-de-intera-es/09-UI-SPEC.md
@.planning/phases/09-timeline-de-intera-es/09-PATTERNS.md
@.planning/phases/09-timeline-de-intera-es/09-01-SUMMARY.md
@.planning/phases/09-timeline-de-intera-es/09-02-SUMMARY.md

<interfaces>
<!-- Contratos entregues pelos planos 09-01/09-02 e primitivos já instalados. Não explorar o repositório para redescobrir. -->

De `src/actions/interacao-actions.ts` (09-01), todas chamadas imperativamente (não via `useActionState`):
```
getInteracoesByLead(leadId: number): Promise<Interacao[]>          // ativas, createdAt DESC, id DESC
createInteracaoManual(leadId: number, texto: string): Promise<{ success: true } | { errors: Record<string, string[] | undefined> }>
updateInteracaoManual(id: number, texto: string): Promise<...mesma forma...>
softDeleteInteracaoManual(id: number): Promise<...mesma forma...>
```
O tipo do retorno NÃO é exportado (arquivo `"use server"`): checar o resultado com `"errors" in resultado`.

De `src/types/index.ts` (09-01):
```
type Interacao = { id: number; leadId: number; tipo: "primeiro_contato"|"follow_up"|"prova_valor"|"nota_manual";
                   texto: string; createdAt: Date; updatedAt: Date | null; deletedAt: Date | null }
```

De `src/lib/validations.ts` (09-01):
```
notaManualTextoSchema = z.object({ texto: z.string().trim().min(1, "Nota não pode ficar vazia.") })
type NotaManualFormValues = z.input<typeof notaManualTextoSchema>
```

Primitivos disponíveis em `src/components/ui/` (nenhum novo pode ser instalado): `dialog.tsx` (Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle; `DialogContent` já tem `max-h`/`overflow-y-auto` globais desde a quick task 260720-x41), `button.tsx` (variants: default/outline/ghost/destructive; sizes: default/xs/sm/lg/icon/icon-xs/icon-sm/icon-lg), `textarea.tsx`, `field.tsx` (Field, FieldContent, FieldError, FieldLabel), `badge.tsx` (variants: default/secondary/destructive/outline/ghost/link), `label.tsx`, `sonner.tsx`.
Ícones lucide-react confirmados instalados: `MessageCircle`, `StickyNote`, `Pencil`, `Trash2`, `History`.

Análogo de dialog alimentado por estado imperativo (`src/components/whatsapp-preview-dialog.tsx`):
- Props `{ open, onOpenChange, lead: Lead | undefined, ... }`; instância ÚNICA reutilizada pelo pai, não remontada por item
- `useEffect(() => { if (!open || !lead) return; ...; }, [open, lead?.id])` com `// eslint-disable-next-line react-hooks/exhaustive-deps`
- `<Dialog open={open && !!lead}>` com o conteúdo guardado por `{lead ? (...) : null}` — NUNCA `if (!lead) return null` (WR-04: desmontava a árvore antes da animação de fechamento)

Análogo de formulário (`src/components/template-form-dialog.tsx`): react-hook-form + `zodResolver`, toast de sucesso/erro via `sonner`, `DialogContent className="max-w-lg"`.

Análogo de confirmação destrutiva (`src/components/delete-lead-dialog.tsx`, arquivo inteiro, 52 linhas): `Dialog` com `showCloseButton={false}`, `DialogTitle`/`DialogDescription`, footer com `Button variant="outline"` "Cancelar" + `Button variant="destructive"` "Excluir".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Superfície da timeline — leitura cronológica, lista de entradas e estado vazio</name>
  <files>src/components/lead-timeline-dialog.tsx</files>
  <read_first>
    - src/components/whatsapp-preview-dialog.tsx (arquivo inteiro — props, reset em `[open, lead?.id]`, guarda de conteúdo em vez de `return null`, comentário WR-04)
    - src/components/ui/dialog.tsx (assinatura de `DialogContent`, `showCloseButton`, classes globais de scroll)
    - src/components/ui/badge.tsx (variants disponíveis)
    - src/components/lead-table.tsx (linhas 200-300 — idioma de linha com `border-b`/`last:border-b-0` e de estado vazio)
    - src/actions/interacao-actions.ts (assinaturas exatas das 4 funções, resultado de 09-01)
    - src/types/index.ts (tipo `Interacao`)
    - .planning/phases/09-timeline-de-intera-es/09-UI-SPEC.md §Layout, §Copywriting Contract, §Color, §Typography, §Spacing Scale (contrato vinculante — copy literal)
  </read_first>
  <action>
Criar `src/components/lead-timeline-dialog.tsx` como client component (`"use client"`), exportando `LeadTimelineDialog` com props `{ open: boolean; onOpenChange: (open: boolean) => void; lead: Lead | undefined }`.

Estrutura e estado:
- `const [interacoes, setInteracoes] = useState<Interacao[]>([])` e `const [carregando, setCarregando] = useState(false)`.
- Uma função `recarregar()` que chama `getInteracoesByLead(lead.id)` e popula o estado — reutilizada pelas tasks 2 e 3 após cada mutação. Guardar contra respostas fora de ordem/lead trocado: ignorar o resultado se `lead?.id` mudou no meio do voo (flag `cancelado` no cleanup do efeito, ou comparação do id no retorno).
- `useEffect` disparado em `[open, lead?.id]`: se `!open || !lead`, zera a lista e retorna; senão marca carregando e chama `recarregar()`. Usar `// eslint-disable-next-line react-hooks/exhaustive-deps` como o análogo faz.
- Renderizar `<Dialog open={open && !!lead} onOpenChange={onOpenChange}>` com `<DialogContent className="max-w-lg">` e o conteúdo inteiro guardado por `{lead ? (...) : null}` — nunca `if (!lead) return null` (WR-04).

Cabeçalho: `DialogHeader` com `DialogTitle` = `Histórico de {lead.nome}` (copy literal do UI-SPEC), sem `DialogDescription`.

Lista de entradas (ordem já vem decrescente do servidor — não reordenar no cliente):
- Container `flex flex-col`, cada entrada `flex flex-col gap-1 border-b border-zinc-200 py-4 last:border-b-0`.
- Linha de cabeçalho da entrada: `flex items-center gap-2`, contendo ícone de tipo `size-4` (`MessageCircle` com `className="size-4 text-[#0D9488]"` quando `tipo !== "nota_manual"`; `StickyNote` com `className="size-4 text-muted-foreground"` quando `tipo === "nota_manual"`), `Badge variant="outline"` neutro com o rótulo do tipo, e o timestamp `text-[14px] text-muted-foreground` formatado com `format(interacao.createdAt, "dd/MM/yyyy 'às' HH:mm")` de `date-fns`.
- Mapa de rótulos (constante no módulo, copy literal do UI-SPEC): `primeiro_contato` → "1º contato", `follow_up` → "Follow-up", `prova_valor` → "Prova de valor", `nota_manual` → "Nota manual".
- Corpo da entrada: `<p className="text-[16px] leading-normal whitespace-pre-wrap text-foreground">{interacao.texto}</p>` — renderização por children de JSX, PROIBIDO `dangerouslySetInnerHTML` (é a mitigação de XSS armazenado desta fase); sem truncamento, sem `line-clamp` (D-05).
- Não renderizar nenhum controle de editar/excluir nesta task (entram na Task 3).

Estado vazio (quando `interacoes.length === 0` e não está carregando): bloco `flex flex-col items-center gap-2 py-12 text-center` com heading `text-[16px] font-semibold` = "Nenhuma interação registrada ainda" e body `text-[14px] text-muted-foreground` = "Cliques em \"Abrir WhatsApp\" e notas manuais aparecem aqui, em ordem cronológica." (copy literal do UI-SPEC). O composer da Task 2 fica acima e continua visível nesse estado.

Não adicionar `DialogFooter` com botões de ação — a superfície é consultiva; fechar pelo X do primitivo.
Não usar cor teal (`#0D9488`) em nada além do ícone `MessageCircle` das entradas automáticas nesta task (a lista de usos permitidos do accent está travada no UI-SPEC §Color).
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const s=require('fs').readFileSync('src/components/lead-timeline-dialog.tsx','utf8');const req=[['\"use client\"','diretiva client'],['export function LeadTimelineDialog','export'],['getInteracoesByLead(','leitura'],['Histórico de','titulo'],['whitespace-pre-wrap','texto integral'],['Nenhuma interação registrada ainda','empty heading'],['dd/MM/yyyy','formato de data'],['StickyNote','icone nota'],['MessageCircle','icone whatsapp'],['max-w-lg','largura do dialog']];const faltando=req.filter(r=>!s.includes(r[0])).map(r=>r[1]);if(faltando.length){console.error('faltando: '+faltando.join(', '));process.exit(1)}if(s.includes('dangerouslySetInnerHTML')){console.error('XSS: dangerouslySetInnerHTML proibido');process.exit(1)}if(/if \(!lead\) return null/.test(s)){console.error('WR-04: nao desmontar a arvore do Dialog');process.exit(1)}console.log('OK timeline shell');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/lead-timeline-dialog.tsx` começa com `"use client"` e exporta `LeadTimelineDialog` com as 3 props exatas `open`, `onOpenChange`, `lead`
    - O arquivo NÃO contém `dangerouslySetInnerHTML` nem `if (!lead) return null`
    - `useEffect` tem array de dependências `[open, lead?.id]` e chama `getInteracoesByLead`
    - O corpo de cada entrada usa `whitespace-pre-wrap` e não usa `line-clamp`/`truncate`/`slice(0,`
    - `DialogContent` tem `className="max-w-lg"`; `DialogTitle` renderiza `Histórico de ` concatenado ao nome do lead
    - As 4 strings de rótulo de tipo aparecem literalmente: `1º contato`, `Follow-up`, `Prova de valor`, `Nota manual`
    - As 2 strings do estado vazio aparecem literalmente conforme o UI-SPEC
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Abrir a superfície para um lead lista o histórico ativo em ordem decrescente, com ícone/badge/timestamp por entrada e estado vazio quando não há nada.</done>
</task>

<task type="auto">
  <name>Task 2: Composer de nota manual fixo no topo da timeline</name>
  <files>src/components/lead-timeline-dialog.tsx</files>
  <read_first>
    - src/components/lead-timeline-dialog.tsx (versão da Task 1 — estado, `recarregar()`, estrutura do dialog)
    - src/components/template-form-dialog.tsx (arquivo inteiro — react-hook-form + `zodResolver`, toasts de sucesso/erro, botão com estado pendente "Salvando...")
    - src/components/ui/textarea.tsx e src/components/ui/field.tsx (assinaturas de `Field`/`FieldContent`/`FieldError`)
    - src/lib/validations.ts (`notaManualTextoSchema`, `NotaManualFormValues`)
    - src/actions/interacao-actions.ts (`createInteracaoManual`)
    - .planning/phases/09-timeline-de-intera-es/09-UI-SPEC.md §Layout (âncora visual primária/composer) e §Copywriting Contract
    - .planning/STATE.md (decisão de 07-02 sobre o falso-positivo do `react-hooks/refs` em `form.handleSubmit` + leitura de ref — este composer NÃO usa `formRef`, então não deve precisar de supressão; só suprimir se o lint realmente acusar, com comentário explicando)
  </read_first>
  <action>
Acrescentar ao `LeadTimelineDialog` um composer fixo no topo do dialog, ACIMA da lista (é a âncora visual primária segundo o UI-SPEC — nunca escondido atrás de um botão "Nova nota"), separado da lista por `gap-4`.

- `const form = useForm<NotaManualFormValues>({ resolver: zodResolver(notaManualTextoSchema), defaultValues: { texto: "" } })`.
- `Field` + `FieldContent` + `Textarea` com `placeholder="Registrar uma nota sobre este lead..."` (copy literal) e `FieldError` ligado a `form.formState.errors.texto` (mensagem "Nota não pode ficar vazia." vem do próprio schema).
- Botão primário alinhado à direita abaixo do textarea (`flex justify-end`), copy "Salvar nota", com `className="bg-[#0D9488] text-white hover:bg-[#0D9488]/90"` (mesmo padrão de `TemplateFormDialog`); rótulo "Salvando..." e `disabled` enquanto pendente.
- Submit: `form.handleSubmit(onSubmit)` num `<form>` próprio DENTRO do dialog (não aninhar dentro de nenhum outro `<form>`). `onSubmit` chama `createInteracaoManual(lead.id, values.texto)` dentro de `startTransition` (ou com um `useState` de pendência), e ao resultado: se `"errors" in resultado` → `toast.error("Não foi possível salvar a nota. Tente novamente.")` e manter o texto digitado; senão → `toast.success("Nota registrada.")`, `form.reset({ texto: "" })` e `await recarregar()` para a nova entrada aparecer no topo.
- Como as actions são de argumentos posicionais (não `useActionState`), passar `values.texto` direto — não montar `FormData` (não há conversão de valor envolvida aqui, diferente de `lead-form-dialog.tsx`).
- O composer continua visível e funcional no estado vazio (o admin cria a primeira nota sem sair do dialog).
- Resetar o formulário também quando o dialog abre para outro lead (acrescentar `form.reset({ texto: "" })` ao efeito de `[open, lead?.id]` da Task 1) — a instância do dialog é única e reutilizada.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/components/lead-timeline-dialog.tsx && node -e "const s=require('fs').readFileSync('src/components/lead-timeline-dialog.tsx','utf8');const req=[['zodResolver(notaManualTextoSchema)','resolver'],['createInteracaoManual(','action de criar'],['Registrar uma nota sobre este lead...','placeholder'],['Salvar nota','cta'],['Salvando...','estado pendente'],['Nota registrada.','toast sucesso'],['Não foi possível salvar a nota. Tente novamente.','toast erro'],['form.reset(','reset apos salvar']];const faltando=req.filter(r=>!s.includes(r[0])).map(r=>r[1]);if(faltando.length){console.error('faltando: '+faltando.join(', '));process.exit(1)}console.log('OK composer');"</automated>
  </verify>
  <acceptance_criteria>
    - O composer aparece no JSX ANTES (acima) do bloco da lista/estado vazio no mesmo `DialogContent`
    - `useForm` usa `zodResolver(notaManualTextoSchema)` e o submit chama `createInteracaoManual(lead.id, ...)`
    - Em sucesso: chama `toast.success("Nota registrada.")`, `form.reset(` e recarrega a lista
    - Em erro (`"errors" in resultado`): chama `toast.error("Não foi possível salvar a nota. Tente novamente.")` e NÃO limpa o textarea
    - As strings `Registrar uma nota sobre este lead...`, `Salvar nota` e `Salvando...` aparecem literalmente
    - `npx eslint src/components/lead-timeline-dialog.tsx` exit 0 (qualquer `eslint-disable` presente tem comentário explicando o motivo)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>O admin registra uma nota manual pelo topo da superfície e ela aparece imediatamente no topo da lista, sem recarregar a página.</done>
</task>

<task type="auto">
  <name>Task 3: Edição inline e exclusão confirmada de nota manual (assimetria D-06)</name>
  <files>src/components/delete-nota-dialog.tsx, src/components/lead-timeline-dialog.tsx</files>
  <read_first>
    - src/components/delete-lead-dialog.tsx (arquivo inteiro, 52 linhas — molde exato do dialog de confirmação)
    - src/components/lead-timeline-dialog.tsx (versão das Tasks 1 e 2)
    - src/actions/interacao-actions.ts (`updateInteracaoManual`, `softDeleteInteracaoManual` — inclusive as cláusulas de WHERE que tornam a operação um no-op para eventos de WhatsApp)
    - .planning/phases/09-timeline-de-intera-es/09-UI-SPEC.md §Layout (edição inline, exclusão empilhada) e §Copywriting Contract (copy literal dos toasts e do dialog de exclusão)
    - .planning/phases/09-timeline-de-intera-es/09-CONTEXT.md D-06/D-07
  </read_first>
  <action>
Criar `src/components/delete-nota-dialog.tsx` espelhando `delete-lead-dialog.tsx` estrutura por estrutura: client component, props `{ open, onOpenChange, onConfirm }` (sem nome de entidade — o contexto é o próprio título), `Dialog` com `DialogContent showCloseButton={false}`, `DialogTitle` = "Excluir nota", `DialogDescription` = "Tem certeza que deseja excluir esta nota? Ela deixará de aparecer na timeline.", footer com "Cancelar" (`variant="outline"`) e "Excluir" (`variant="destructive"`) — copy literal do UI-SPEC, que deliberadamente NÃO promete restauração (não existe superfície de recuperação de notas nesta fase).

Em `lead-timeline-dialog.tsx`, acrescentar os controles APENAS para entradas com `tipo === "nota_manual"` (entradas automáticas nunca renderizam nenhum dos dois — a imutabilidade real já vive no WHERE do servidor, isto é o reforço visual):
- Na linha de cabeçalho da entrada, à direita (`ml-auto flex items-center gap-2`): `Button variant="ghost" size="icon-sm"` com `Pencil className="size-4"` e `aria-label="Editar nota"`; e `Button variant="ghost" size="icon-sm"` com `Trash2 className="size-4 text-[#DC2626]"` e `aria-label="Excluir nota"`.
- Edição inline (não um modal aninhado): estado `const [editandoId, setEditandoId] = useState<number | null>(null)` e `const [textoEdicao, setTextoEdicao] = useState("")`. Clicar em `Pencil` seta os dois a partir da entrada. Enquanto `editandoId === interacao.id`, o CORPO da entrada é substituído por um `Textarea` controlado por `textoEdicao` mais dois botões `size="sm"`: "Salvar" (chama `updateInteracaoManual(interacao.id, textoEdicao)`) e "Cancelar" (limpa `editandoId`, descarta a edição sem confirmação). Desabilitar "Salvar" quando `textoEdicao.trim().length === 0` (o servidor também rejeita, via `interacaoManualUpdateSchema`).
- Ao salvar a edição com sucesso: `toast.success("Nota atualizada.")`, `setEditandoId(null)` e `recarregar()`. Em erro: `toast.error("Não foi possível salvar a nota. Tente novamente.")` mantendo o modo de edição aberto.
- Exclusão: estado `const [notaParaExcluir, setNotaParaExcluir] = useState<number | null>(null)`; `Trash2` abre o `DeleteNotaDialog` empilhado sobre a timeline; `onConfirm` chama `softDeleteInteracaoManual(id)` e, em sucesso, `toast.success("Nota removida da timeline.")` + `recarregar()` + fecha o dialog de confirmação; em erro, `toast.error("Não foi possível salvar a nota. Tente novamente.")`.
- Fechar/abrir o dialog para outro lead precisa zerar `editandoId`, `textoEdicao` e `notaParaExcluir` no mesmo efeito de `[open, lead?.id]`.
- Renderizar `<DeleteNotaDialog />` como irmão do `<Dialog>` da timeline (fora do `DialogContent`), no mesmo padrão em que `lead-table.tsx` monta `DeleteLeadDialog` ao lado dos demais dialogs.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/components/lead-timeline-dialog.tsx src/components/delete-nota-dialog.tsx && node -e "const fs=require('fs');const d=fs.readFileSync('src/components/delete-nota-dialog.tsx','utf8');const t=fs.readFileSync('src/components/lead-timeline-dialog.tsx','utf8');const req=[[d.includes('Excluir nota'),'titulo do dialog'],[d.includes('Tem certeza que deseja excluir esta nota? Ela deixará de aparecer na timeline.'),'corpo do dialog'],[d.includes('variant=\"destructive\"'),'botao destrutivo'],[t.includes('updateInteracaoManual('),'action de editar'],[t.includes('softDeleteInteracaoManual('),'action de excluir'],[t.includes('Nota atualizada.'),'toast editar'],[t.includes('Nota removida da timeline.'),'toast excluir'],[t.includes('nota_manual'),'guarda de tipo na UI'],[t.includes('Editar nota')&&t.includes('Excluir nota'),'aria-labels'],[!/dangerouslySetInnerHTML/.test(t),'sem dangerouslySetInnerHTML']];const faltando=req.filter(r=>!r[0]).map(r=>r[1]);if(faltando.length){console.error('faltando: '+faltando.join(', '));process.exit(1)}console.log('OK edicao/exclusao');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/delete-nota-dialog.tsx` exporta `DeleteNotaDialog` e contém as 4 strings literais: "Excluir nota", "Tem certeza que deseja excluir esta nota? Ela deixará de aparecer na timeline.", "Cancelar", "Excluir"
    - Em `lead-timeline-dialog.tsx`, os botões `Pencil`/`Trash2` estão dentro de uma condicional que exige `tipo === "nota_manual"` — nenhuma entrada com outro tipo renderiza esses controles
    - A edição é inline (substitui o corpo da entrada por um `Textarea` + botões `size="sm"`), sem abrir um segundo `Dialog`
    - "Salvar" da edição está `disabled` quando o texto editado está vazio após `trim()`
    - Os toasts usam exatamente "Nota atualizada." e "Nota removida da timeline."
    - `<DeleteNotaDialog />` é renderizado fora do `DialogContent` da timeline
    - `npx eslint` nos dois arquivos exit 0 e `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Notas manuais são editáveis e removíveis pela própria timeline, com confirmação; eventos automáticos de WhatsApp não expõem nenhum controle de mutação.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conteúdo persistido → DOM | `interacoes.texto` guarda texto arbitrário digitado pelo admin ou vindo de template renderizado; é exibido de volta na timeline |
| UI → Server Action | Os botões de editar/excluir são renderizados condicionalmente, mas a Server Action é chamável diretamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-13 | Tampering / Elevation of Privilege | Renderização de `interacoes.texto` na timeline | mitigate | Texto renderizado exclusivamente como children de JSX (`{interacao.texto}`), com escape automático do React; `dangerouslySetInnerHTML` proibido por acceptance criterion verificado por grep nos 2 arquivos |
| T-09-14 | Tampering | Esconder Pencil/Trash2 como única barreira de imutabilidade | mitigate | A condicional de UI é reforço; a garantia real é `eq(interacoes.tipo, "nota_manual")` no WHERE das actions (09-01) e é provada pelo harness `test:interacao-actions` (09-02) |
| T-09-15 | Repudiation | Nota apagada por engano sem superfície de recuperação | accept | D-06/D-07 travam soft-delete sem UI de restauração (09-RESEARCH.md Assumption A3); mitigado pela confirmação obrigatória do `DeleteNotaDialog` e pelo dado continuar no banco (`deletedAt`), recuperável manualmente via Drizzle Studio |
| T-09-16 | Information Disclosure | Timeline exibindo notas de outro lead por corrida de requisição | mitigate | `recarregar()` descarta respostas cujo `lead.id` mudou durante o voo; estado de edição/exclusão zerado no efeito de `[open, lead?.id]` |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote nem bloco de registry novo nesta fase (09-UI-SPEC.md §Registry Safety: todos os primitivos já estão em `src/components/ui/`); qualquer `npx shadcn add` ou `npm install` exige checkpoint humano bloqueante antes de rodar |
</threat_model>

<verification>
**Host de 4GB RAM: um comando por vez, sequencialmente, com `npm run dev` PARADO.** Nunca em paralelo, nunca com `&`.

1. `npx tsc --noEmit` — exit 0
2. `npx eslint src/components/lead-timeline-dialog.tsx src/components/delete-nota-dialog.tsx` — exit 0 (escopar o eslint aos arquivos da fase: o `npx eslint` sem escopo tem 413 problemas pré-existentes documentados em `deferred-items.md`, fora de escopo)
3. `npm run test:interacao-actions` — exit 0
4. `npm run guard:no-hard-delete` — exit 0
</verification>

<success_criteria>
- `LeadTimelineDialog` e `DeleteNotaDialog` existem, compilam e passam no eslint escopado
- Lista cronológica decrescente, texto integral com quebras de linha preservadas, estado vazio conforme o UI-SPEC
- Composer sempre visível no topo, criando nota manual com feedback por toast
- Editar/excluir disponíveis somente para `tipo === "nota_manual"`
- Nenhum primitivo shadcn novo instalado, nenhuma dependência npm nova
- Nenhum ponto de entrada foi ligado ainda (é o plano 09-04)
</success_criteria>

<output>
Criar `.planning/phases/09-timeline-de-intera-es/09-03-SUMMARY.md` ao terminar.
</output>
