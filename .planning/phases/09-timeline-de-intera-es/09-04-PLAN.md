---
phase: 09-timeline-de-intera-es
plan: 04
type: execute
wave: 4
depends_on: ["09-03"]
files_modified:
  - src/components/lead-table.tsx
  - src/components/lead-table-columns.tsx
  - src/components/pipeline-lead-card.tsx
  - src/components/pipeline-board.tsx
  - src/components/lead-form-dialog.tsx
autonomous: true
requirements: [TIMELINE-02]

must_haves:
  truths:
    - "Na lista /leads, o admin abre o histórico de um lead por um ícone dedicado ao lado de Editar/Excluir, sem abrir o modal de edição por baixo"
    - "No board /pipeline, o admin abre o histórico por um ícone no card sem arrastar o card nem abrir a edição"
    - "No modal de editar lead, o admin abre o histórico por um botão 'Ver histórico' no rodapé, visível apenas em modo edição"
    - "Os três pontos de entrada abrem a MESMA superfície de timeline, para o lead correto"
    - "As 3 seções do modal de lead (Contato/Negócio/Acompanhamento) e o campo `notas` continuam exatamente como antes (D-01)"
  artifacts:
    - path: "src/components/lead-table.tsx"
      provides: "Ícone History na linha + estado e montagem do LeadTimelineDialog"
      contains: "LeadTimelineDialog"
    - path: "src/components/pipeline-board.tsx"
      provides: "Estado e montagem do LeadTimelineDialog para o board"
      contains: "LeadTimelineDialog"
    - path: "src/components/lead-form-dialog.tsx"
      provides: "Botão 'Ver histórico' no rodapé em modo edição"
      contains: "Ver histórico"
  key_links:
    - from: "src/components/lead-table.tsx"
      to: "src/components/lead-timeline-dialog.tsx"
      via: "estado TimelineState + render do dialog"
      pattern: "<LeadTimelineDialog"
    - from: "src/components/pipeline-lead-card.tsx"
      to: "src/components/pipeline-board.tsx"
      via: "prop onViewHistory disparada dentro do wrapper de stopPropagation"
      pattern: "onViewHistory"
---

<objective>
Ligar a superfície de timeline aos três pontos de entrada exigidos por D-03 (lista `/leads`, card do `/pipeline`, rodapé do modal de editar lead) e fechar a fase com os gates automatizados rodados sequencialmente no host de 4GB.

Purpose: completa TIMELINE-02 — sem os pontos de entrada, a superfície construída no plano 09-03 é inalcançável pelo admin.
Output: acesso duplo/triplo à mesma timeline + evidência de verificação da fase.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-timeline-de-intera-es/09-CONTEXT.md
@.planning/phases/09-timeline-de-intera-es/09-UI-SPEC.md
@.planning/phases/09-timeline-de-intera-es/09-PATTERNS.md
@.planning/phases/09-timeline-de-intera-es/09-03-SUMMARY.md

<interfaces>
<!-- Contratos entregues pelo plano 09-03 e estruturas atuais dos arquivos a modificar. Não explorar o repositório para redescobrir. -->

De `src/components/lead-timeline-dialog.tsx` (09-03):
```
export function LeadTimelineDialog(props: { open: boolean; onOpenChange: (open: boolean) => void; lead: Lead | undefined })
```
Instância ÚNICA por tela, controlada por estado do pai (mesmo padrão de `WhatsAppPreviewDialog`/`PreviewState`).

`src/components/lead-table.tsx` (estado atual):
- Tipos de estado locais: `DialogState` (closed/create/edit), `DeleteState`, `PreviewState`
- `const COL = { nome, subnicho, etapa, followup, telefone, acoes: "w-[260px] shrink-0 justify-end" }`
- O corpo da lista é markup customizado (não usa `cell` dos column defs); o grupo de ações fica em `<div className={\`flex items-center gap-1 ${COL.acoes}\`} onClick={stopPropagation}>` com: botão "WhatsApp" (`size="lg"`, verde `#22C55E`), `Pencil` (`variant="ghost" size="icon-lg"`, aria-label `Editar {nome}`), `Trash2` (`variant="ghost" size="icon-lg"`, aria-label `Excluir {nome}`, ícone `text-[#DC2626]`)
- Dialogs montados no fim do componente: `LeadFormDialog`, `DeleteLeadDialog`, `WhatsAppPreviewDialog`

`src/components/lead-table-columns.tsx` (estado atual):
- `declare module "@tanstack/react-table" { interface TableMeta<TData> { onEditLead?: (lead: LeadRow) => void; onDeleteLead?: (lead: LeadRow) => void } }`
- Coluna `acoes` com os botões Pencil/Trash2 — mantida como fonte de verdade de sort/filtro, embora o `cell` não seja mais renderizado desde a quick task 260725-gzb

`src/components/pipeline-lead-card.tsx` (estado atual):
- `type PipelineLeadCardProps = { lead, subnichoNome, isEsfriando, onClick, onSendWhatsApp }`
- Wrapper obrigatório de isolamento: `<div onPointerDown={(e) => e.stopPropagation()} onClick={(e) => e.stopPropagation()}>` em volta do `WhatsAppSendButton` — o card externo tem `{...listeners} {...attributes}` de `useDraggable` mais `onClick` de edição

`src/components/pipeline-board.tsx` (estado atual):
- Estados `DialogState`, `MotivoPerdaState`, `PreviewState`; `<PipelineLeadCard>` recebe `onClick` e `onSendWhatsApp` por lead (linhas 182-195); dialogs montados no fim (`LeadFormDialog` ~203, `WhatsAppPreviewDialog` ~225)

`src/components/lead-form-dialog.tsx` (estado atual):
- `const isEditMode = Boolean(lead);`
- `DialogFooter className="mx-0 mb-0 rounded-none border-t-0 bg-transparent p-0"` com "Cancelar" (`variant="outline"`, `onClick={() => closeWithDiscardGuard(false)}`) e "Salvar" (`type="submit"`)
- Já monta dialogs irmãos fora do `<form>`: `DiscardChangesDialog`, `WhatsAppPreviewDialog`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ponto de entrada na lista /leads</name>
  <files>src/components/lead-table.tsx, src/components/lead-table-columns.tsx</files>
  <read_first>
    - src/components/lead-table.tsx (arquivo inteiro, 364 linhas — tipos de estado, `COL`, o grupo de ações nas linhas 250-297, a montagem dos dialogs nas linhas 331-361)
    - src/components/lead-table-columns.tsx (arquivo inteiro, 171 linhas — `TableMeta` nas linhas 17-23 e a coluna `acoes` nas linhas 131-169)
    - src/components/lead-timeline-dialog.tsx (props exatas de `LeadTimelineDialog`, resultado de 09-03)
    - .planning/phases/09-timeline-de-intera-es/09-UI-SPEC.md §Layout (pontos de entrada) e §Copywriting Contract (aria-label/title)
  </read_first>
  <action>
Em `src/components/lead-table.tsx`:
- Importar `History` de `lucide-react` e `LeadTimelineDialog` de `@/components/lead-timeline-dialog`.
- Adicionar o tipo de estado `type TimelineState = { open: false } | { open: true; lead: Lead }` (mesma forma de `PreviewState`) e `const [timelineState, setTimelineState] = useState<TimelineState>({ open: false })`.
- No grupo de ações da linha, inserir um botão entre o botão "WhatsApp" e o `Pencil` (ordem final: WhatsApp, Histórico, Editar, Excluir, conforme o UI-SPEC): `Button type="button" variant="ghost" size="icon-lg"` com `aria-label={\`Ver histórico de ${lead.nome}\`}`, `title="Ver histórico"`, ícone `<History className="size-4" />` em cor neutra (sem teal — o accent é reservado, ver UI-SPEC §Color), e `onClick` que faz `event.stopPropagation()` antes de `setTimelineState({ open: true, lead })` (a linha inteira já tem `onClick` de edição).
- Alterar `COL.acoes` de `"w-[260px] shrink-0 justify-end"` para `"w-[300px] shrink-0 justify-end"` — a coluna passa a comportar 4 controles (1 botão `lg` + 3 `icon-lg` de 36px + gaps); manter `w-[300px]` idêntico no cabeçalho e no corpo (a constante já é compartilhada pelos dois).
- Montar `<LeadTimelineDialog open={timelineState.open} onOpenChange={(open) => { if (!open) setTimelineState({ open: false }); }} lead={timelineState.open ? timelineState.lead : undefined} />` ao lado dos demais dialogs no fim do componente.

Em `src/components/lead-table-columns.tsx` (mantido como fonte de verdade dos contratos de linha, mesmo com o `cell` de `acoes` não sendo renderizado hoje):
- Acrescentar `onViewTimeline?: (lead: LeadRow) => void;` à interface `TableMeta`.
- Acrescentar ao `cell` da coluna `acoes` um botão `History` com a mesma forma dos vizinhos (`variant="ghost"`, `size="icon-lg"`, `aria-label={\`Ver histórico de ${row.original.nome}\`}`, `title="Ver histórico"`, `event.stopPropagation()` antes de `table.options.meta?.onViewTimeline?.(row.original)`), posicionado antes do `Pencil`.
- Registrar `onViewTimeline` no objeto `meta` do `useReactTable` em `lead-table.tsx`, apontando para `setTimelineState({ open: true, lead })`, mantendo `onEditLead`/`onDeleteLead` intactos.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/components/lead-table.tsx src/components/lead-table-columns.tsx && node -e "const fs=require('fs');const t=fs.readFileSync('src/components/lead-table.tsx','utf8');const c=fs.readFileSync('src/components/lead-table-columns.tsx','utf8');const req=[[t.includes('LeadTimelineDialog'),'dialog montado na lista'],[t.includes('Ver histórico de'),'aria-label na linha'],[t.includes('title=\"Ver histórico\"'),'title na linha'],[t.includes('w-[300px]'),'largura da coluna de acoes ajustada'],[!t.includes('w-[260px]'),'largura antiga removida'],[t.includes('onViewTimeline'),'meta registrada'],[c.includes('onViewTimeline?: (lead: LeadRow) => void'),'TableMeta estendida'],[c.includes('History'),'icone no column def'],[/onEditLead\?:/.test(c)&&/onDeleteLead\?:/.test(c),'meta pre-existente intacta']];const faltando=req.filter(r=>!r[0]).map(r=>r[1]);if(faltando.length){console.error('faltando: '+faltando.join(', '));process.exit(1)}console.log('OK entrada /leads');"</automated>
  </verify>
  <acceptance_criteria>
    - O botão `History` da linha aparece entre o botão "WhatsApp" e o botão `Pencil` no JSX de `lead-table.tsx`
    - O `onClick` desse botão chama `event.stopPropagation()` ANTES de setar o estado da timeline
    - `COL.acoes` usa `w-[300px]` e a string `w-[260px]` não existe mais no arquivo
    - `TableMeta` em `lead-table-columns.tsx` declara `onViewTimeline?: (lead: LeadRow) => void` e mantém `onEditLead`/`onDeleteLead`
    - `<LeadTimelineDialog ... />` é montado no fim de `lead-table.tsx`, com `lead` derivado de `timelineState`
    - `npx eslint` nos 2 arquivos exit 0 e `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Na lista `/leads` existe um ícone de histórico por linha que abre a timeline do lead certo, sem disparar o modal de edição.</done>
</task>

<task type="auto">
  <name>Task 2: Pontos de entrada no board do pipeline e no modal de editar lead</name>
  <files>src/components/pipeline-lead-card.tsx, src/components/pipeline-board.tsx, src/components/lead-form-dialog.tsx</files>
  <read_first>
    - src/components/pipeline-lead-card.tsx (arquivo inteiro, 101 linhas — props, `useDraggable`, o wrapper de `stopPropagation` nas linhas 66-77 e o comentário que explica por que ele existe)
    - src/components/pipeline-board.tsx (estados nas linhas 29-62, uso do `PipelineLeadCard` nas linhas 182-195, montagem dos dialogs a partir da linha 203)
    - src/components/lead-form-dialog.tsx (props e `isEditMode` nas linhas 43-95; `DialogFooter` nas linhas 448-460; dialogs irmãos montados após o `</Dialog>`)
    - src/components/lead-timeline-dialog.tsx (props de `LeadTimelineDialog`, resultado de 09-03)
    - .planning/phases/09-timeline-de-intera-es/09-RESEARCH.md §Pitfall 5 (ícone novo no card sem `stopPropagation` vira drag-handle) e §Open Questions #1 (colocação do botão no rodapé, só em modo edição)
    - .planning/phases/09-timeline-de-intera-es/09-CONTEXT.md D-01 (as 3 seções do modal e o campo `notas` ficam intocados)
  </read_first>
  <action>
Em `src/components/pipeline-lead-card.tsx`:
- Acrescentar `onViewHistory: () => void` a `PipelineLeadCardProps`, ao lado de `onSendWhatsApp`, e desestruturá-la.
- Renderizar um `Button type="button" variant="ghost" size="icon-lg"` com `<History className="size-4" />`, `aria-label={\`Ver histórico de ${lead.nome}\`}`, `title="Ver histórico"`, ao lado do `WhatsAppSendButton`, DENTRO do mesmo `<div onPointerDown={stop} onClick={stop}>` já existente (ou num wrapper irmão com exatamente os mesmos dois handlers) — sem isso o ícone vira drag-handle do `useDraggable` ou dispara o `onClick` de edição do card (Pitfall 5). Envolver os dois controles num `flex items-center gap-1` dentro desse wrapper.
- O `onClick` do botão chama `onViewHistory()`; não precisa de `stopPropagation` próprio se estiver dentro do wrapper (o wrapper já intercepta), mas manter o wrapper é obrigatório.

Em `src/components/pipeline-board.tsx`:
- Importar `LeadTimelineDialog`; adicionar `type TimelineState = { open: false } | { open: true; lead: Lead }` e o `useState` correspondente, na mesma forma de `PreviewState`.
- Passar `onViewHistory={() => setTimelineState({ open: true, lead })}` a cada `<PipelineLeadCard>`.
- Montar `<LeadTimelineDialog>` junto dos demais dialogs no fim do componente, com o mesmo padrão de `onOpenChange` usado por `WhatsAppPreviewDialog`.

Em `src/components/lead-form-dialog.tsx`:
- Importar `History` e `LeadTimelineDialog`; adicionar `const [timelineOpen, setTimelineOpen] = useState(false)`.
- No `DialogFooter`, ANTES do botão "Cancelar", inserir `{isEditMode && lead ? (<Button type="button" variant="outline" onClick={() => setTimelineOpen(true)} disabled={pending}><History className="size-4" />Ver histórico</Button>) : null}` — copy literal "Ver histórico", `variant="outline"`, só em modo edição (um lead ainda não criado não tem histórico).
- Montar `<LeadTimelineDialog open={timelineOpen} onOpenChange={setTimelineOpen} lead={lead} />` como irmão dos dialogs já montados fora do `<form>` (ao lado de `DiscardChangesDialog`/`WhatsAppPreviewDialog`), nunca dentro do `<form>`.
- PROIBIDO nesta task: alterar qualquer campo, label, descrição ou ordem das 3 seções existentes (Contato / Negócio / Acompanhamento), e em especial o campo `notas` — D-01 exige que continuem exatamente como estão. A única mudança no arquivo é o botão do rodapé, o import, o estado e a montagem do dialog.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/components/pipeline-lead-card.tsx src/components/pipeline-board.tsx src/components/lead-form-dialog.tsx && node -e "const fs=require('fs');const c=fs.readFileSync('src/components/pipeline-lead-card.tsx','utf8');const b=fs.readFileSync('src/components/pipeline-board.tsx','utf8');const f=fs.readFileSync('src/components/lead-form-dialog.tsx','utf8');const req=[[/onViewHistory/.test(c),'prop no card'],[/onPointerDown=\{\(event\) => event\.stopPropagation\(\)\}/.test(c),'wrapper de stopPropagation preservado'],[(c.match(/stopPropagation/g)||[]).length>=2,'stopPropagation em pointerdown e click'],[/History/.test(c),'icone no card'],[/onViewHistory=\{/.test(b),'card recebe o handler'],[/<LeadTimelineDialog/.test(b),'dialog montado no board'],[/Ver histórico/.test(f),'botao no rodape do modal'],[/isEditMode/.test(f),'gate de modo edicao'],[/<LeadTimelineDialog/.test(f),'dialog montado no modal'],[/notas/.test(f),'campo notas preservado']];const faltando=req.filter(r=>!r[0]).map(r=>r[1]);if(faltando.length){console.error('faltando: '+faltando.join(', '));process.exit(1)}console.log('OK entradas pipeline + modal');"</automated>
  </verify>
  <acceptance_criteria>
    - `PipelineLeadCardProps` declara `onViewHistory: () => void` e o botão `History` está dentro de um `<div>` que tem simultaneamente `onPointerDown` e `onClick` com `stopPropagation`
    - `pipeline-board.tsx` passa `onViewHistory` a todo `<PipelineLeadCard>` e monta `<LeadTimelineDialog>` uma única vez
    - Em `lead-form-dialog.tsx`, o botão "Ver histórico" está dentro do `DialogFooter`, antes de "Cancelar", com `variant="outline"` e condicionado a `isEditMode`
    - `git diff src/components/lead-form-dialog.tsx` não mostra nenhuma alteração dentro das 3 seções do formulário nem no campo `notas` — apenas imports, um `useState`, o botão do rodapé e a montagem do dialog
    - `<LeadTimelineDialog>` em `lead-form-dialog.tsx` está fora do elemento `<form>`
    - `npx eslint` nos 3 arquivos exit 0 e `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Card do pipeline e modal de editar lead abrem a mesma superfície de timeline, sem quebrar drag-and-drop, edição por clique no card, ou o formulário de lead.</done>
</task>

<task type="auto">
  <name>Task 3: Gates finais sequenciais da Fase 9</name>
  <files>(nenhum arquivo de produção — execução de verificação)</files>
  <read_first>
    - package.json (bloco `scripts` — nomes exatos dos gates disponíveis após 09-01/09-02)
    - .planning/phases/09-timeline-de-intera-es/09-01-SUMMARY.md, 09-02-SUMMARY.md, 09-03-SUMMARY.md (o que cada plano já provou; não repetir gate já verde sem necessidade, mas o build completo só roda aqui)
    - .planning/STATE.md §Session Continuity e a nota de 2026-07-29 sobre o worker do Next crashando por falta de memória quando `npm run build` roda com o dev server ativo
  </read_first>
  <action>
Com o `npm run dev` PARADO e nenhum outro processo Node ativo, rodar os gates abaixo UM DE CADA VEZ, na ordem, registrando o exit code e a saída resumida de cada um no SUMMARY. Nunca encadear com `&`, nunca rodar dois em paralelo — este host de 4GB já foi derrubado por OOM duas vezes exatamente nesse cenário (memória do usuário `feedback_4gb_ram_avoid_parallel`).

1. `npx tsc --noEmit`
2. `npx eslint src/components/lead-timeline-dialog.tsx src/components/delete-nota-dialog.tsx src/components/lead-table.tsx src/components/lead-table-columns.tsx src/components/pipeline-lead-card.tsx src/components/pipeline-board.tsx src/components/lead-form-dialog.tsx src/components/whatsapp-preview-dialog.tsx src/actions/interacao-actions.ts src/actions/lead-actions.ts src/db/schema.ts src/lib/validations.ts` — eslint ESCOPADO aos arquivos tocados pela fase; rodar `npx eslint` sem escopo é inútil aqui (413 problemas pré-existentes já documentados em `deferred-items.md`, fora de escopo desta fase)
3. `npm run guard:no-hard-delete`
4. `npm run verify:schema`
5. `npm run test:interacao-actions`
6. `npm run test:lead-actions`
7. `node scripts/verify-wa-contact-invariant.cjs`
8. `npm run verify:origem-tipo`
9. `npm run build`

Se algum gate falhar, corrigir a causa raiz no arquivo correspondente e re-rodar SOMENTE aquele gate mais os posteriores — não reiniciar a bateria inteira sem necessidade (custo de memória).

Registrar no SUMMARY, além dos exit codes: a contagem de linhas de `interacoes` no banco vivo ao final (`SELECT COUNT(*) FROM interacoes` — deve ser 0 se nenhum clique real foi dado, ou o número de cliques feitos durante testes manuais) e a confirmação de que `SELECT COUNT(*) FROM leads` continua igual ao valor registrado no `09-01-SUMMARY.md`.
  </action>
  <verify>
    <automated>npm run guard:no-hard-delete && npm run verify:schema && npm run test:interacao-actions && npm run test:lead-actions && node scripts/verify-wa-contact-invariant.cjs && npm run verify:origem-tipo</automated>
    <human-check>
Com `npm run dev` rodando (e nenhum gate pesado em paralelo), no navegador:
1. `/leads` — clicar no ícone de relógio/histórico de um lead: abre "Histórico de {nome}" com o composer no topo. Digitar uma nota e salvar: toast "Nota registrada." e a nota aparece no topo da lista com badge "Nota manual".
2. Ainda na timeline: editar a nota (lápis) → salvar → toast "Nota atualizada."; excluir (lixeira) → confirmar → toast "Nota removida da timeline." e a entrada some.
3. Fechar a timeline, clicar em "WhatsApp" no mesmo lead, editar o texto da mensagem e clicar "Abrir WhatsApp". Reabrir o histórico: existe uma entrada nova com o badge do tipo escolhido e o TEXTO EDITADO integral, e essa entrada NÃO tem ícones de editar/excluir.
4. Repetir o passo 3 escolhendo "Follow-up" e depois "Prova de valor" — as duas também precisam virar entrada na timeline.
5. `/pipeline` — clicar no ícone de histórico de um card: abre a timeline do lead certo, sem arrastar o card e sem abrir o modal de edição. Arrastar o mesmo card para outra coluna continua funcionando.
6. Abrir o modal de editar lead: o rodapé mostra "Ver histórico" (e NÃO mostra ao criar um lead novo); clicar abre a mesma timeline. As 3 seções e o campo de notas do formulário continuam idênticos.
    </human-check>
  </verify>
  <acceptance_criteria>
    - Os 9 gates listados na ação foram executados sequencialmente e cada um está registrado no SUMMARY com seu exit code
    - Gates 1 a 9 terminam todos com exit 0
    - `npm run build` conclui sem crash de memória (rodado com o dev server parado) e sem erro de tipos/lint bloqueante
    - `SELECT COUNT(*) FROM leads` no banco vivo é igual ao valor registrado em `09-01-SUMMARY.md`
    - Nenhum arquivo de produção foi alterado por esta task, exceto correções de causa raiz de gate reprovado (se houver, listadas no SUMMARY)
  </acceptance_criteria>
  <done>Toda a bateria automatizada da Fase 9 passa verde em execução sequencial, e a checagem humana no navegador está descrita e pronta para o fechamento de fase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gesto do usuário → handler | No board, o mesmo elemento pode ser interpretado como drag, clique de edição ou clique de ação; a desambiguação é responsabilidade do wrapper |
| superfície nova → formulário existente | O modal de lead tem guarda de alterações não salvas; um dialog aninhado pode interferir no fluxo de descarte |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-17 | Tampering | Ícone `History` em `pipeline-lead-card.tsx` | mitigate | Botão obrigatoriamente dentro do wrapper com `onPointerDown` + `onClick` de `stopPropagation` (Pitfall 5); acceptance criterion verifica os dois handlers por grep |
| T-09-18 | Tampering | Regressão do formulário de lead ao inserir o botão de rodapé | mitigate | D-01 protegido por acceptance criterion baseado em `git diff`: nenhuma alteração dentro das 3 seções nem no campo `notas`; `LeadTimelineDialog` montado fora do `<form>` para não interferir no submit nem na guarda de descarte |
| T-09-19 | Information Disclosure | Timeline aberta exibindo o lead errado após trocar de linha/card | mitigate | Estado `TimelineState` discriminado por lead + reset em `[open, lead?.id]` dentro do dialog (09-03); uma única instância montada por tela |
| T-09-20 | Denial of Service | OOM do host ao rodar gates em paralelo com o dev server | mitigate | Bateria explicitamente sequencial, dev server parado, proibição de `&`; `npm run build` isolado como último gate |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo nesta fase (09-RESEARCH.md §Package Legitimacy Audit; 09-UI-SPEC.md §Registry Safety); qualquer install que apareça exige checkpoint humano bloqueante antes de rodar |
</threat_model>

<verification>
**Host de 4GB RAM: todos os comandos abaixo rodam UM DE CADA VEZ, com `npm run dev` PARADO.** Ordem obrigatória, sem `&`, sem paralelismo:

1. `npx tsc --noEmit`
2. `npx eslint <lista escopada de arquivos da fase>`
3. `npm run guard:no-hard-delete`
4. `npm run verify:schema`
5. `npm run test:interacao-actions`
6. `npm run test:lead-actions`
7. `node scripts/verify-wa-contact-invariant.cjs`
8. `npm run verify:origem-tipo`
9. `npm run build`

O `<human-check>` da Task 3 roda depois, com o dev server ligado e nenhum gate pesado ativo (`workflow.human_verify_mode = end-of-phase`).
</verification>

<success_criteria>
- Os 3 pontos de entrada de D-03 existem e abrem a mesma superfície para o lead correto
- Nenhuma regressão de drag-and-drop no board nem no formulário de lead (D-01 preservado)
- Bateria de 9 gates verde, executada sequencialmente
- TIMELINE-01 e TIMELINE-02 verificáveis de ponta a ponta pelo roteiro de `<human-check>`
</success_criteria>

<output>
Criar `.planning/phases/09-timeline-de-intera-es/09-04-SUMMARY.md` ao terminar, com a tabela de gates (comando → exit code), as contagens do banco vivo e o estado de cada item do roteiro de checagem humana.
</output>
