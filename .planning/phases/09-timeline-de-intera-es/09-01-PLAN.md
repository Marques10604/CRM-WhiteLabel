---
phase: 09-timeline-de-intera-es
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/types/index.ts
  - src/lib/validations.ts
  - scripts/guard-no-hard-delete.cjs
  - scripts/verify-schema.cjs
  - package.json
  - src/actions/interacao-actions.ts
autonomous: true
requirements: [TIMELINE-01, TIMELINE-02]

must_haves:
  truths:
    - "A tabela `interacoes` existe no banco vivo (data/crm.db) com as 7 colunas e os 2 índices declarados em schema.ts"
    - "Uma tentativa de soft-delete de uma linha com tipo != 'nota_manual' é um no-op silencioso (0 linhas afetadas), não um erro e não uma remoção"
    - "Uma tentativa de editar uma linha com tipo != 'nota_manual' é um no-op silencioso (0 linhas afetadas)"
    - "A guarda `npm run guard:no-hard-delete` reprova hard-delete/DROP contra `interacoes`, não só contra `leads`/`subnichos`"
    - "A leitura da timeline de um lead devolve apenas linhas ativas (deletedAt nulo), mais recentes primeiro, com ordenação estável"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Tabela interacoes (enum de 4 tipos, FK para leads.id, deletedAt nullable, 2 índices)"
      contains: "export const interacoes = sqliteTable"
    - path: "src/types/index.ts"
      provides: "Tipos Interacao/NewInteracao inferidos do Drizzle"
      contains: "export type Interacao"
    - path: "src/lib/validations.ts"
      provides: "notaManualTextoSchema, interacaoManualSchema, interacaoManualUpdateSchema, whatsappContactSchema com texto"
      contains: "interacaoManualSchema"
    - path: "src/actions/interacao-actions.ts"
      provides: "getInteracoesByLead, createInteracaoManual, updateInteracaoManual, softDeleteInteracaoManual"
      exports: ["getInteracoesByLead", "createInteracaoManual", "updateInteracaoManual", "softDeleteInteracaoManual"]
    - path: "scripts/guard-no-hard-delete.cjs"
      provides: "Cobertura anti hard-delete estendida para interacoes"
      contains: "interacoes"
    - path: "scripts/verify-schema.cjs"
      provides: "Gate de sync de schema cobrindo a tabela interacoes no banco vivo"
      contains: "interacoes"
  key_links:
    - from: "src/actions/interacao-actions.ts"
      to: "src/db/schema.ts (interacoes)"
      via: "db.insert/db.update/db.select com where guardado por tipo"
      pattern: "eq\\(interacoes\\.tipo, \"nota_manual\"\\)"
    - from: "src/db/schema.ts (interacoes.leadId)"
      to: "src/db/schema.ts (leads.id)"
      via: "references onDelete restrict"
      pattern: "references\\(\\(\\) => leads\\.id"
---

<objective>
Criar toda a camada de dados da timeline de interações: a tabela `interacoes` (schema Drizzle + tipos + contratos Zod), a extensão obrigatória da guarda anti hard-delete no MESMO commit que cria a tabela, a aplicação real no banco vivo via `drizzle-kit push`, e as Server Actions de CRUD de nota manual + leitura cronológica.

Purpose: TIMELINE-01/02 dependem de um registro persistente por evento. Sem esta camada nada mais da fase pode ser construído, e sem a extensão da guarda um hard-delete futuro em `interacoes` passaria despercebido (débito já sinalizado em `.planning/STATE.md` §Blockers/Concerns para a Fase 9).
Output: tabela `interacoes` viva no banco, 4 Server Actions com guarda de imutabilidade server-side (D-06), guarda e gate de schema estendidos.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-timeline-de-intera-es/09-CONTEXT.md
@.planning/phases/09-timeline-de-intera-es/09-RESEARCH.md
@.planning/phases/09-timeline-de-intera-es/09-PATTERNS.md

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código atual — não explorar o repositório para redescobrir. -->

Já existentes em `src/db/schema.ts`:
- `leads` (id integer PK autoIncrement, ..., deletedAt nullable timestamp, createdAt/updatedAt notNull default `(unixepoch())`)
- `templates.tipo` = enum `["primeiro_contato", "follow_up", "prova_valor"]`
- Idioma de índice: `index("<tabela>_<coluna>_idx").on(table.<coluna>)`

Já existente em `src/types/index.ts` (única fonte de tipos inferidos do Drizzle):
- `export type Template = InferSelectModel<typeof templates>;`

Já existente em `src/lib/validations.ts`:
- `whatsappContactSchema = z.object({ leadId: z.coerce.number().int().positive(), tipo: z.enum(["primeiro_contato","follow_up","prova_valor"]) })`
- Idioma de derivação de schema: `csvRowSchema = leadSchema.omit({...}).extend({...})`

Já existente em `src/actions/lead-actions.ts` (modelo direto para as novas actions):
- `type ActionState = { success: true; lead?: Lead } | { errors: Record<string, string[] | undefined> } | undefined;` — declarado LOCALMENTE, nunca exportado (arquivo `"use server"` só pode exportar funções async)
- `softDeleteLead(leadId: number)`: `db.update(leads).set({ deletedAt: sql\`(unixepoch())\` }).where(and(eq(leads.id, leadId), isNull(leads.deletedAt)))` + `revalidatePath` de `/`, `/leads`, `/pipeline`, `/lixeira`

Já existente em `scripts/guard-no-hard-delete.cjs`:
- `const CODE_PATTERNS = [/\.delete\(\s*leads\b/, /\.delete\(\s*subnichos\b/];`
- `const CODE_SQL_PATTERNS = [ /\bDELETE\s+FROM\s+[\`"']?leads\b/i, /\bDELETE\s+FROM\s+[\`"']?subnichos\b/i, /\bDROP\s+TABLE\s+[\`"']?leads\b/i, /\bDROP\s+TABLE\s+[\`"']?subnichos\b/i ];`
- `const ALLOWLIST = [path.join("scripts", "guard-no-hard-delete.cjs")];` — o próprio guard é ignorado, então adicionar padrões nele não o auto-acusa

Já existente em `scripts/verify-schema.cjs`:
- `const requiredTables = ["leads", "subnichos"];`
- `const requiredIndexes = ["subnicho_nome_unique_idx"];`
- Abre `process.env.DB_FILE_NAME ?? ./data/crm.db` com `fileMustExist: true`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contratos da tabela interacoes (schema + tipos + Zod) e extensão da guarda anti hard-delete</name>
  <files>src/db/schema.ts, src/types/index.ts, src/lib/validations.ts, scripts/guard-no-hard-delete.cjs</files>
  <read_first>
    - src/db/schema.ts (estado atual: subnichos, templates, leads, configuracoes — copiar o idioma de enum/FK/índice/`sql\`(unixepoch())\``)
    - src/types/index.ts (arquivo inteiro, 12 linhas — única fonte dos tipos inferidos)
    - src/lib/validations.ts (whatsappContactSchema linhas 78-86, templateSchema linhas 88-95, idioma de derivação em csvRowSchema linhas 64-67)
    - scripts/guard-no-hard-delete.cjs (CODE_PATTERNS linha 49, CODE_SQL_PATTERNS linhas 55-60, ALLOWLIST linha 43, comentário de cabeçalho declarando o escopo LEAD-04)
    - .planning/phases/09-timeline-de-intera-es/09-RESEARCH.md §Code Examples (schema Drizzle de interacoes, já redigido de forma project-idiomatic) e §Common Pitfalls 1
    - .planning/phases/09-timeline-de-intera-es/09-PATTERNS.md §src/db/schema.ts e §scripts/guard-no-hard-delete.cjs
  </read_first>
  <action>
Adicionar em `src/db/schema.ts`, depois de `leads` e antes de `configuracoes`, `export const interacoes = sqliteTable("interacoes", {...})` com exatamente estas colunas:
`id` integer primaryKey autoIncrement; `leadId` integer coluna `"lead_id"` notNull com `.references(() => leads.id, { onDelete: "restrict" })`; `tipo` text coluna `"tipo"` com `enum: ["primeiro_contato", "follow_up", "prova_valor", "nota_manual"]` notNull (reaproveita o vocabulário de `templates.tipo` mais o quarto valor `nota_manual` — decisão de coluna única do 09-RESEARCH.md §Alternatives Considered, atende "tipo/resumo" de TIMELINE-01 sem uma segunda dimensão não pedida); `texto` text notNull (D-04/D-05 — texto integral do que estava na caixa de mensagem, sem truncamento e sem `.max()`); `createdAt` integer coluna `"created_at"` mode timestamp notNull default `sql\`(unixepoch())\``; `updatedAt` integer coluna `"updated_at"` mode timestamp NULLABLE e SEM default (só é preenchido ao editar uma nota manual — diferente de `templates.updatedAt`, que é notNull); `deletedAt` integer coluna `"deleted_at"` mode timestamp NULLABLE (D-06/D-07: só relevante para `tipo="nota_manual"`).
Segundo argumento do `sqliteTable`: `[index("interacoes_lead_id_idx").on(table.leadId), index("interacoes_deleted_at_idx").on(table.deletedAt)]`.
Comentar no bloco: por que `updatedAt` é nullable, e que `deletedAt` nunca é escrito para eventos automáticos de WhatsApp (imutáveis por D-06).

Em `src/types/index.ts`: acrescentar `interacoes` ao import de `@/db/schema` e exportar `Interacao` (`InferSelectModel`) e `NewInteracao` (`InferInsertModel`), na mesma forma dos pares já existentes.

Em `src/lib/validations.ts`: acrescentar `texto: z.string().trim().min(1, "Mensagem vazia.")` a `whatsappContactSchema` (D-04/D-05 — sem `.max()`); e adicionar, logo abaixo de `templateSchema`, três contratos derivados em camada (mesmo idioma DRY de `csvRowSchema` derivando de `leadSchema`): `notaManualTextoSchema = z.object({ texto: z.string().trim().min(1, "Nota não pode ficar vazia.") })` (a mensagem de erro é literal do 09-UI-SPEC.md §Copywriting Contract), `interacaoManualSchema = notaManualTextoSchema.extend({ leadId: z.coerce.number().int().positive() })`, `interacaoManualUpdateSchema = notaManualTextoSchema.extend({ id: z.coerce.number().int().positive() })`, mais `export type NotaManualFormValues = z.input<typeof notaManualTextoSchema>;`. Documentar em comentário que `texto` obrigatório em `whatsappContactSchema` significa que um clique em "Abrir WhatsApp" com a caixa de mensagem vazia deixa de registrar tentativa/interação — consequência deliberada de D-04 (mensagem vazia não é contato real), coerente com o comportamento pré-existente de "entrada inválida ⇒ `{ advanced: false }`, sem escrita".

Em `scripts/guard-no-hard-delete.cjs` (MESMO commit da criação da tabela — exigência explícita de `.planning/STATE.md` §Blockers/Concerns e do Pitfall 1 do 09-RESEARCH.md): acrescentar `/\.delete\(\s*interacoes\b/` a `CODE_PATTERNS`, e `/\bDELETE\s+FROM\s+[\`"']?interacoes\b/i` + `/\bDROP\s+TABLE\s+[\`"']?interacoes\b/i` a `CODE_SQL_PATTERNS`. Atualizar o comentário de cabeçalho e o comentário de escopo acima de `CODE_PATTERNS` para citar `leads`/`subnichos`/`interacoes` (hoje dizem só leads/subnichos). NÃO tornar a guarda genérica para qualquer tabela — `templates` continua com hard-delete legítimo (D-13 de 04-01). O soft-delete de nota manual usa `.update(interacoes)`, então não colide com o padrão `.delete(interacoes`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run guard:no-hard-delete && node -e "const fs=require('fs');const s=fs.readFileSync('src/db/schema.ts','utf8');const t=fs.readFileSync('src/types/index.ts','utf8');const v=fs.readFileSync('src/lib/validations.ts','utf8');const g=fs.readFileSync('scripts/guard-no-hard-delete.cjs','utf8');const c=[[/export const interacoes = sqliteTable\(/.test(s),'schema.interacoes'],[/\"nota_manual\"/.test(s),'enum nota_manual'],[/references\(\(\) => leads\.id/.test(s),'FK leads.id'],[/interacoes_lead_id_idx/.test(s)&&/interacoes_deleted_at_idx/.test(s),'indices'],[/export type Interacao\b/.test(t)&&/export type NewInteracao\b/.test(t),'tipos'],[/interacaoManualSchema/.test(v)&&/interacaoManualUpdateSchema/.test(v)&&/notaManualTextoSchema/.test(v),'schemas zod'],[/texto:\s*z\.string\(\)\.trim\(\)\.min\(1, \"Mensagem vazia\.\"\)/.test(v),'whatsappContactSchema.texto'],[/\\\\.delete\\\\\(\\\\s\*interacoes/.test(g)||g.includes('delete(\\\\s*interacoes'),'guard code pattern'],[/DROP\\\\s\+TABLE[^\n]*interacoes/.test(g),'guard drop pattern']];let bad=c.filter(x=>!x[0]).map(x=>x[1]);if(bad.length){console.error('FALTANDO: '+bad.join(', '));process.exit(1)}console.log('OK contratos');"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` termina com exit 0 e sem nenhuma saída de erro
    - `npm run guard:no-hard-delete` termina com exit 0 e imprime "OK: nenhum hard-delete encontrado"
    - `src/db/schema.ts` contém `export const interacoes = sqliteTable(` com os literais `"lead_id"`, `"tipo"`, `"texto"`, `"created_at"`, `"updated_at"`, `"deleted_at"`, `"nota_manual"`, `interacoes_lead_id_idx`, `interacoes_deleted_at_idx` e `references(() => leads.id, { onDelete: "restrict" })`
    - Em `src/db/schema.ts`, a coluna `updatedAt` de `interacoes` NÃO tem `.notNull()` nem `.default(`
    - `src/types/index.ts` exporta `Interacao` e `NewInteracao`
    - `src/lib/validations.ts` exporta `notaManualTextoSchema`, `interacaoManualSchema`, `interacaoManualUpdateSchema`, `NotaManualFormValues`, e `whatsappContactSchema` contém a chave `texto`
    - `scripts/guard-no-hard-delete.cjs` tem 3 entradas mencionando `interacoes` (1 em `CODE_PATTERNS`, 2 em `CODE_SQL_PATTERNS`) e nenhuma remoção das entradas de `leads`/`subnichos` (o arquivo continua com os 2 padrões de código e os 4 de SQL originais)
  </acceptance_criteria>
  <done>Schema, tipos, contratos Zod e guarda estendida existem e compilam; a guarda passa verde e cobre `interacoes` por nome.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Aplicar a tabela no banco vivo via drizzle-kit push e estender o gate de schema</name>
  <files>scripts/verify-schema.cjs, package.json</files>
  <read_first>
    - scripts/verify-schema.cjs (arquivo inteiro — `requiredTables`, `requiredIndexes`, mensagem final)
    - package.json (bloco `scripts` — nota que `verify:origem-tipo`, `test:lead-actions`, `guard:no-hard-delete` já existem; `verify-schema.cjs` ainda não tem entrada npm)
    - drizzle.config.ts (out: ./src/db/migrations, dbCredentials.url: ./data/crm.db)
    - src/db/schema.ts (versão já com `interacoes`, resultado da Task 1)
    - .planning/phases/09-timeline-de-intera-es/09-RESEARCH.md §Pattern 3 e §Pitfall 6 (por que `push` é seguro para CREATE TABLE de tabela nova e vazia, ao contrário de ADD COLUMN DEFAULT em tabela populada)
    - .planning/STATE.md §Blockers/Concerns (backup de data/crm.db antes de qualquer push) e §Accumulated Context linha sobre o landmine de `drizzle-kit push` das Fases 06-01/07-01/08-01
  </read_first>
  <action>
Esta task é BLOQUEANTE: `tsc` e `npm run build` passam sem a tabela existir (os tipos vêm de `schema.ts`, não do banco), então pular este passo cria um falso-positivo de verificação para a fase inteira.

1. Com o dev server PARADO, fazer backup do banco antes de tocar nele: copiar `data/crm.db` para `data/crm.db.bak-fase09` (o diretório `data/` é gitignorado — o backup não entra no commit). Confirmar que o arquivo de backup existe e tem o mesmo tamanho do original antes de prosseguir.
2. Rodar `npx drizzle-kit push --verbose` e LER as instruções SQL impressas antes de qualquer confirmação. Critério de abortar: se qualquer instrução mencionar uma tabela diferente de `interacoes` (`leads`, `subnichos`, `templates`, `configuracoes`) ou contiver `DELETE FROM`, `DROP TABLE`, ou recriação de tabela, ABORTAR imediatamente sem aplicar, restaurar `data/crm.db` a partir do backup se algo já tiver sido escrito, e reportar o SQL proposto no SUMMARY como bloqueio — o snapshot de migrações deste projeto está divergente do banco real desde a Fase 4/6 (débito documentado) e um diff inesperado sobre `leads` é exatamente o landmine das Fases 06-01/07-01/08-01. Nunca usar `--force`. Não substituir o push por DDL manual escrito à mão: `CREATE TABLE interacoes` numa tabela nova e vazia é o cenário seguro (mesmo precedente de `templates` na Fase 04-02).
3. Estender `scripts/verify-schema.cjs`: acrescentar `"interacoes"` a `requiredTables` e `"interacoes_lead_id_idx"`, `"interacoes_deleted_at_idx"` a `requiredIndexes`; adicionar uma checagem nova de colunas via `PRAGMA table_info(interacoes)` exigindo exatamente o conjunto `id, lead_id, tipo, texto, created_at, updated_at, deleted_at` (falha com mensagem listando as colunas faltantes/extras); atualizar a mensagem final de sucesso para citar `interacoes`.
4. Adicionar `"verify:schema": "node scripts/verify-schema.cjs"` ao bloco `scripts` do `package.json`, ao lado das entradas de verificação já existentes.
5. Rodar `npm run verify:schema` (dev server parado) e confirmar exit 0.
  </action>
  <verify>
    <automated>npm run verify:schema && node -e "const D=require('better-sqlite3');const db=new D('./data/crm.db',{fileMustExist:true});const t=db.prepare(\"SELECT name FROM sqlite_master WHERE type='table' AND name='interacoes'\").get();if(!t){console.error('tabela interacoes AUSENTE no banco vivo');process.exit(1)}const cols=db.prepare('PRAGMA table_info(interacoes)').all().map(c=>c.name).sort().join(',');const esperado=['created_at','deleted_at','id','lead_id','texto','tipo','updated_at'].join(',');if(cols!==esperado){console.error('colunas divergentes: '+cols);process.exit(1)}const fks=db.prepare('PRAGMA foreign_key_list(interacoes)').all();if(!fks.some(f=>f.table==='leads')){console.error('FK para leads ausente');process.exit(1)}const idx=db.prepare(\"SELECT name FROM sqlite_master WHERE type='index' AND tbl_name='interacoes'\").all().map(i=>i.name);if(!idx.includes('interacoes_lead_id_idx')||!idx.includes('interacoes_deleted_at_idx')){console.error('indices ausentes: '+idx.join(','));process.exit(1)}const leadsCols=db.prepare('PRAGMA table_info(leads)').all().length;console.log('OK banco vivo: interacoes criada, FK ok, 2 indices ok, leads com '+leadsCols+' colunas intactas');"</automated>
  </verify>
  <acceptance_criteria>
    - `data/crm.db.bak-fase09` existe em disco antes do push
    - `SELECT name FROM sqlite_master WHERE type='table' AND name='interacoes'` no banco vivo retorna uma linha
    - `PRAGMA table_info(interacoes)` retorna exatamente as colunas `id, lead_id, tipo, texto, created_at, updated_at, deleted_at`
    - `PRAGMA foreign_key_list(interacoes)` inclui uma FK cuja tabela alvo é `leads`
    - `sqlite_master` lista os índices `interacoes_lead_id_idx` e `interacoes_deleted_at_idx`
    - `PRAGMA table_info(leads)` continua com 18 colunas e `SELECT COUNT(*) FROM leads` continua igual à contagem registrada antes do push (registrar os dois números no SUMMARY, antes e depois)
    - `npm run verify:schema` termina com exit 0
    - `package.json` contém a entrada `"verify:schema"`
  </acceptance_criteria>
  <done>A tabela `interacoes` existe no banco real com colunas, FK e índices corretos; nenhuma tabela pré-existente foi alterada; o gate permanente `verify:schema` cobre a tabela nova.</done>
</task>

<task type="auto">
  <name>Task 3: Server Actions de interações com guarda de imutabilidade no WHERE</name>
  <files>src/actions/interacao-actions.ts</files>
  <read_first>
    - src/actions/lead-actions.ts (arquivo inteiro — idioma de `"use server"`, `type ActionState` local não-exportado, `softDeleteLead` linhas 258-269, SELECT fresco antes de mutar, fan-out de `revalidatePath`)
    - src/actions/template-actions.ts (arquivo dedicado de entidade secundária: imports, forma de create/update, `applyDefaultTemplate` com `db.transaction`)
    - src/db/queries.ts (`getActiveDashboardLeads` — idioma de leitura com `and(isNull(...))` + `orderBy`)
    - src/db/schema.ts (versão com `interacoes`, resultado da Task 1)
    - src/lib/validations.ts (versão com `interacaoManualSchema`/`interacaoManualUpdateSchema`, resultado da Task 1)
    - .planning/phases/09-timeline-de-intera-es/09-RESEARCH.md §Pattern 2 e §Pitfall 2 (imutabilidade garantida no WHERE, nunca só na UI)
  </read_first>
  <action>
Criar `src/actions/interacao-actions.ts` com `"use server"` no topo e um comentário de cabeçalho documentando D-06/D-07 (assimetria: evento de WhatsApp é imutável, nota manual é editável/soft-deletável) e a convenção LEAD-04 estendida a `interacoes`.

Declarar LOCALMENTE (nunca exportar — arquivo `"use server"` só exporta funções async) `type InteracaoActionState = { success: true } | { errors: Record<string, string[] | undefined> };`.

Exportar exatamente estas 4 funções async de argumentos posicionais (NÃO `(_prevState, formData)` — são chamadas imperativamente de `onClick`/`useEffect`, mesmo idioma de `registerWhatsAppContact`/`softDeleteLead`):

1. `getInteracoesByLead(leadId: number): Promise<Interacao[]>` — `db.select().from(interacoes).where(and(eq(interacoes.leadId, leadId), isNull(interacoes.deletedAt))).orderBy(desc(interacoes.createdAt), desc(interacoes.id))`. O segundo critério `desc(interacoes.id)` é obrigatório: `created_at` tem resolução de 1 segundo (`unixepoch()`), então dois eventos no mesmo segundo teriam ordem indefinida sem o desempate por id. Se `leadId` não for inteiro positivo, devolver `[]` sem consultar o banco.

2. `createInteracaoManual(leadId: number, texto: string): Promise<InteracaoActionState>` — `interacaoManualSchema.safeParse({ leadId, texto })`; em falha devolver `{ errors: parsed.error.flatten().fieldErrors }`. SELECT fresco de `leads` guardado por `isNull(leads.deletedAt)` para confirmar que o lead existe e está ativo (mesmo padrão de `updateLead`); se não existir, devolver `{ errors: { leadId: ["Lead inválido."] } }`. Depois `db.insert(interacoes).values({ leadId, tipo: "nota_manual", texto: parsed.data.texto })`.

3. `updateInteracaoManual(id: number, texto: string): Promise<InteracaoActionState>` — `interacaoManualUpdateSchema.safeParse({ id, texto })`; `db.update(interacoes).set({ texto: parsed.data.texto, updatedAt: new Date() }).where(and(eq(interacoes.id, parsed.data.id), eq(interacoes.tipo, "nota_manual"), isNull(interacoes.deletedAt)))`. A cláusula `eq(interacoes.tipo, "nota_manual")` é obrigatória e load-bearing: sem ela, uma chamada direta com o id de um evento de WhatsApp (replay via DevTools) reescreveria um registro imutável. Nenhuma linha afetada é no-op silencioso (devolve `{ success: true }`, mesmo idioma idempotente de `softDeleteLead`/`restoreLead`).

4. `softDeleteInteracaoManual(id: number): Promise<InteracaoActionState>` — `db.update(interacoes).set({ deletedAt: sql\`(unixepoch())\` }).where(and(eq(interacoes.id, id), eq(interacoes.tipo, "nota_manual"), isNull(interacoes.deletedAt)))`. NUNCA `db.delete(interacoes...)` (a guarda da Task 1 reprova). Mesma tripla de cláusulas do item 3.

As 3 mutações terminam com `revalidatePath("/")`, `revalidatePath("/leads")`, `revalidatePath("/pipeline")` (as 3 superfícies de D-03) e retornam `{ success: true }`. Nunca construir SQL por concatenação/template string com input do usuário — só o query builder do Drizzle (parametrizado).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run guard:no-hard-delete && node -e "const s=require('fs').readFileSync('src/actions/interacao-actions.ts','utf8');const fns=['getInteracoesByLead','createInteracaoManual','updateInteracaoManual','softDeleteInteracaoManual'];const faltando=fns.filter(f=>!new RegExp('export async function '+f+'\\\\b').test(s));if(faltando.length){console.error('funcoes ausentes: '+faltando.join(', '));process.exit(1)}if(!/^\"use server\";/m.test(s)){console.error('falta \"use server\"');process.exit(1)}const guardas=(s.match(/eq\(interacoes\.tipo, \"nota_manual\"\)/g)||[]).length;if(guardas<2){console.error('guarda de tipo presente em '+guardas+' mutacao(oes), esperado >=2 (update e softDelete)');process.exit(1)}if(/\.delete\(/.test(s)){console.error('encontrou .delete( — soft-delete obrigatorio');process.exit(1)}if(!/desc\(interacoes\.createdAt\)/.test(s)||!/desc\(interacoes\.id\)/.test(s)){console.error('ordenacao instavel: falta desc(createdAt) + desc(id)');process.exit(1)}console.log('OK interacao-actions');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/actions/interacao-actions.ts` começa com `"use server";` e exporta exatamente as 4 funções nomeadas acima, todas async, todas com argumentos posicionais
    - `updateInteracaoManual` e `softDeleteInteracaoManual` contêm, cada uma, `eq(interacoes.tipo, "nota_manual")` dentro do `and(...)` do `.where(...)`
    - O arquivo não contém a string `.delete(` em lugar nenhum
    - `getInteracoesByLead` usa `orderBy(desc(interacoes.createdAt), desc(interacoes.id))` e filtra por `isNull(interacoes.deletedAt)`
    - `createInteracaoManual` faz SELECT em `leads` com `isNull(leads.deletedAt)` antes do insert e insere com `tipo: "nota_manual"` literal
    - As 3 mutações chamam `revalidatePath` para `/`, `/leads` e `/pipeline`
    - `npx tsc --noEmit` exit 0 e `npm run guard:no-hard-delete` exit 0
  </acceptance_criteria>
  <done>As 4 Server Actions existem, compilam, e a imutabilidade dos eventos de WhatsApp está garantida no WHERE do servidor (não só na futura UI).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Action | Toda Server Action é um endpoint HTTP interno: argumentos podem ser forjados/reproduzidos via DevTools, independentemente do que a UI oferece |
| aplicação → SQLite (data/crm.db) | Escrita em banco com dados reais de produção do admin; `drizzle-kit push` opera sobre esse mesmo arquivo |
| repositório → guarda de convenção | `scripts/guard-no-hard-delete.cjs` é a única barreira automatizada contra hard-delete; tabelas novas não entram nela sozinhas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | `updateInteracaoManual` / `softDeleteInteracaoManual` | mitigate | `eq(interacoes.tipo, "nota_manual")` obrigatório no `and(...)` do WHERE das duas mutações (Task 3) — chamada direta com id de evento de WhatsApp vira no-op de 0 linhas |
| T-09-02 | Tampering | Queries de `interacoes` | mitigate | Somente query builder parametrizado do Drizzle; proibido `sql.raw`/concatenação com `texto` ou `tipo` vindos do cliente (Task 3) |
| T-09-03 | Tampering | `scripts/guard-no-hard-delete.cjs` | mitigate | 3 padrões novos (`.delete(interacoes`, `DELETE FROM interacoes`, `DROP TABLE interacoes`) no MESMO commit que cria a tabela (Task 1) — sem isso a guarda passa verde por omissão (falso-positivo de segurança, Pitfall 1) |
| T-09-04 | Tampering | `npx drizzle-kit push` sobre `data/crm.db` | mitigate | Backup `data/crm.db.bak-fase09` antes do push; leitura obrigatória do SQL proposto com `--verbose`; abortar se qualquer instrução tocar tabela diferente de `interacoes`; `--force` proibido; contagem de linhas e colunas de `leads` conferida antes/depois (Task 2) |
| T-09-05 | Spoofing | `createInteracaoManual` com `leadId` forjado/soft-deletado | mitigate | SELECT fresco com `isNull(leads.deletedAt)` antes do insert + FK `onDelete: "restrict"` como backstop no banco (Task 3) |
| T-09-06 | Information Disclosure | Coluna `texto` armazena o conteúdo integral das mensagens | accept | Banco local single-admin, sem autenticação por decisão de produto (`PROJECT.md`); D-04/D-05 exigem o texto completo. Nenhum script de guarda/teste imprime conteúdo de `texto` — só contagens agregadas |
| T-09-07 | Denial of Service | `texto` sem limite de tamanho | accept | D-05 proíbe truncamento; volume solo (poucas interações por lead) torna o risco irrelevante — reavaliar só se um lead acumular centenas de registros |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo é instalado nesta fase (09-RESEARCH.md §Package Legitimacy Audit: "não aplicável"); se um `npm install` surgir durante a execução, o checkpoint humano bloqueante de legitimidade volta a ser obrigatório antes de instalar |
</threat_model>

<verification>
**Host de 4GB RAM: rodar os comandos abaixo UM DE CADA VEZ, sequencialmente, com o `npm run dev` PARADO.** Rodar qualquer par destes em paralelo (ou junto com o dev server) já derrubou este host por OOM duas vezes. Nunca encadear com `&`.

1. `npx tsc --noEmit` — exit 0, sem saída
2. `npm run guard:no-hard-delete` — exit 0
3. `npm run verify:schema` — exit 0, cita `interacoes`
4. `node scripts/verify-wa-contact-invariant.cjs` — exit 0 (regressão WA-06/07/08 intacta)
5. `npm run test:lead-actions` — exit 0 (nenhum caso pré-existente quebrado)
</verification>

<success_criteria>
- Tabela `interacoes` presente em `data/crm.db` com 7 colunas, FK para `leads` e 2 índices
- `leads` intacta: mesma contagem de linhas e de colunas antes e depois do push
- 4 Server Actions exportadas de `src/actions/interacao-actions.ts`, com guarda `tipo = "nota_manual"` nas 2 mutações de nota
- `guard-no-hard-delete` e `verify-schema` cobrem `interacoes` e passam verdes
- Nenhum arquivo de UI foi tocado neste plano
</success_criteria>

<output>
Criar `.planning/phases/09-timeline-de-intera-es/09-01-SUMMARY.md` ao terminar, registrando: SQL efetivamente aplicado pelo push, contagem de linhas/colunas de `leads` antes e depois, e o caminho do backup.
</output>
