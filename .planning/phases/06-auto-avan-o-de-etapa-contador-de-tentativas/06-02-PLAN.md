---
phase: 06-auto-avan-o-de-etapa-contador-de-tentativas
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/components/whatsapp-preview-dialog.tsx
  - src/components/pipeline-lead-card.tsx
autonomous: true
requirements: [WA-06, WA-08]

must_haves:
  truths:
    - "Clicar em Abrir WhatsApp dispara a mutação de servidor sem bloquear nem interceptar a navegação para wa.me (a aba abre sempre)"
    - "Quando a mutação retorna advanced=true, aparece o toast '{Nome} avançou para Contatado.'"
    - "O tipo enviado à mutação é o valor vivo do Select no momento do clique, nunca a prop defaultTipo"
    - "Abrir o modal e cancelar (botão Cancelar, Escape ou clique fora) não dispara nenhuma mutação"
    - "O card do pipeline mostra o ícone MessageCircle + '{n}x' quando o contador é maior que 0, e nada quando é 0"
    - "Nenhuma falha da mutação exibe erro ao admin (a aba do WhatsApp já abriu)"
  artifacts:
    - path: "src/components/whatsapp-preview-dialog.tsx"
      provides: "Disparo fire-and-forget de registerWhatsAppContact no onClick do anchor + toast de auto-avanço"
      contains: "registerWhatsAppContact"
    - path: "src/components/pipeline-lead-card.tsx"
      provides: "Indicador condicional do contador de tentativas no card"
      contains: "contactAttempts"
  key_links:
    - from: "src/components/whatsapp-preview-dialog.tsx"
      to: "src/actions/lead-actions.ts (registerWhatsAppContact)"
      via: "chamada no onClick do <a href={waHref}>, sem await e sem preventDefault"
      pattern: "registerWhatsAppContact\\(leadId, tipo\\)"
    - from: "src/components/whatsapp-preview-dialog.tsx"
      to: "sonner (toast)"
      via: ".then((result) => result.advanced && toast.success(...))"
      pattern: "avançou para Contatado\\."
    - from: "src/components/pipeline-lead-card.tsx"
      to: "lead.contactAttempts"
      via: "renderização condicional na linha de metadados do card"
      pattern: "lead\\.contactAttempts > 0"
---

<objective>
Ligar a camada de servidor da Fase 6 à interface, nos dois únicos arquivos de UI que precisam mudar: disparar `registerWhatsAppContact` no clique real em "Abrir WhatsApp" (com o toast de auto-avanço, D-07) e exibir o contador de tentativas no card do pipeline (D-05/D-06).

Propósito: `WhatsAppPreviewDialog` é o único lugar de todo o app que renderiza o link `wa.me` real — as 5 superfícies (pipeline, dashboard, lista de leads, pós-importação e o auto-gatilho de 1º contato pós-criação) só abrem esse modal. Instrumentar esse anchor único entrega WA-06/WA-08 em todas as telas de uma vez, sem tocar nenhum dos 5 chamadores (RESEARCH Pattern 1, Pitfall 1).

Saída: contador e auto-avanço funcionando de qualquer tela, com o indicador visível no board.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-CONTEXT.md
@.planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-RESEARCH.md
@.planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-PATTERNS.md
@.planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-UI-SPEC.md
@.planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-01-SUMMARY.md

<interfaces>
Contratos entregues pelo plano 06-01 (usar diretamente, não explorar o repositório):

De `src/actions/lead-actions.ts`:
- `registerWhatsAppContact(leadId: number, tipo: Template["tipo"]): Promise<{ advanced: boolean }>`
  - sempre incrementa o contador do lead; avança `novo → contatado` só quando `tipo === "primeiro_contato"` e a etapa lida no servidor é `novo`
  - nunca lança para o chamador em caso de id inválido/lead soft-deletado — retorna `{ advanced: false }`
  - já chama `revalidatePath("/")`, `revalidatePath("/pipeline")`, `revalidatePath("/leads")`

De `src/db/schema.ts` / `@/types`:
- `Lead.contactAttempts: number` (NOT NULL, default 0 — nunca `null`, nenhum fallback `?? 0` é necessário)

Estado atual de `src/components/whatsapp-preview-dialog.tsx` (o que já existe e NÃO deve ser alterado):
- `"use client"` no topo; props `{ open, onOpenChange, lead, subnichoNome, templates, defaultTipo, subtitulo? }`
- `const [tipo, setTipo] = useState<Template["tipo"]>(defaultTipo)` (linha 68) — este é o estado VIVO a usar
- `const waHref = lead && tel ? buildWaLink(tel, texto) : undefined` (linha 109) — recomputado a cada render
- anchor nas linhas 165-178: `<a href={waHref} target="_blank" rel="noopener noreferrer" onClick={() => onOpenChange(false)} className={cn(buttonVariants(), "gap-1.5 bg-[#0D9488] text-white hover:bg-[#0D9488]/90")}>` com `<MessageCircle />` + texto "Abrir WhatsApp"
- branch de fallback (linhas 179-184): `<Button type="button" disabled ...>` quando não há telefone válido

Estado atual de `src/components/pipeline-lead-card.tsx`:
- `import { Clock } from "lucide-react";` (linha 5)
- `type PipelineLeadCardProps = { lead: Lead; subnichoNome: string; isEsfriando: boolean; onClick: () => void; onSendWhatsApp: () => void }` — recebe o objeto `Lead` inteiro, nenhuma prop nova é necessária
- linha de metadados (linhas 82-89): `<div className="flex items-center gap-1 text-[14px] leading-normal text-muted-foreground">` com a data de follow-up e, condicionalmente, `<span className="flex items-center gap-1 text-[#B45309]"><Clock className="size-3.5" /> Esfriando</span>`

Precedente de toast (não copiar o texto, só o mecanismo): `src/components/pipeline-board.tsx` linha 141 — `toast.success(\`Lead movido para ${label}.\`)`. `<Toaster />` já está montado em `src/app/layout.tsx` (linha 35), então o toast funciona em todas as rotas.
</interfaces>
</context>

<planner_notes>
## Pontos travados (não reinterpretar)

- **Onde a lógica vive:** exclusivamente dentro do `onClick` do `<a href={waHref}>` em `whatsapp-preview-dialog.tsx`. Nunca em `WhatsAppSendButton`, nunca em `useFirstContactTrigger`, nunca em `pipeline-board.tsx`/`followup-dashboard.tsx`/`lead-table.tsx`/`post-import-lead-list.tsx`/`lead-form-dialog.tsx`, nunca em `onOpenChange`. Abrir/cancelar o modal não conta como tentativa (D-01, Pitfall 1/7).
- **Nada de `preventDefault`/`await`/`window.open`:** `window.open` chamado depois de um `await` sai da pilha síncrona do gesto do usuário e é bloqueado como popup — o admin clicaria e nada aconteceria, sem erro visível (Pitfall 2). O anchor real permanece intacto.
- **Gate no estado vivo `tipo`, nunca em `defaultTipo`** (Pitfall 3 — questão em aberto do CONTEXT já resolvida pelo RESEARCH): um lead aberto do dashboard (`defaultTipo="follow_up"`) cujo tipo o admin troca para "1º contato" antes de enviar DEVE avançar; um lead aberto do pipeline (`defaultTipo="primeiro_contato"`) cujo tipo o admin troca para "Follow-up" NÃO deve avançar.
- **Cor do contador:** neutra, herdada do `<div>` pai (`text-muted-foreground`) — decisão prescritiva do `06-UI-SPEC.md`. NÃO usar o âmbar do "Esfriando" nem o teal de accent: `REQUIREMENTS.md` (Out of Scope) proíbe escalonamento visual/cores no contador, e um contador colorido leria como alerta.
- **Nenhuma prop nova em `PipelineLeadCard`** — `lead: Lead` já carrega `contactAttempts`.
- **Nenhum componente shadcn novo, nenhuma dependência npm nova** (`06-UI-SPEC.md`: Registry Safety "not required").
</planner_notes>

<tasks>

<task type="auto">
  <name>Task 1: Disparo fire-and-forget da mutação + toast de auto-avanço no anchor do dialog</name>
  <files>src/components/whatsapp-preview-dialog.tsx</files>
  <read_first>
    - `src/components/whatsapp-preview-dialog.tsx` — ler o arquivo inteiro antes de editar; especialmente o `useState` de `tipo` (linha 68), o cálculo de `waHref` (linhas 108-109), o anchor (linhas 165-178) e o branch de `Button disabled` (179-184)
    - `src/actions/lead-actions.ts` — a assinatura e o contrato de retorno de `registerWhatsAppContact` (criada em 06-01)
    - `src/components/pipeline-board.tsx` (linhas 130-145) — precedente de `toast.success` a partir do retorno de uma Server Action num componente cliente
    - `.planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-RESEARCH.md` — Pattern 1 e Pitfalls 2, 3, 7
    - `.planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-UI-SPEC.md` — seção Copywriting Contract (texto exato do toast)
  </read_first>
  <action>
    Adicionar dois imports em `src/components/whatsapp-preview-dialog.tsx`: `toast` de `"sonner"` e `registerWhatsAppContact` de `"@/actions/lead-actions"`.

    Substituir o handler atual do anchor (`onClick={() => onOpenChange(false)}`, linha 170) por um handler de bloco síncrono que, nesta ordem:
    1. chama `onOpenChange(false)` como primeira instrução — comportamento atual preservado, o dialog fecha imediatamente;
    2. faz `if (!lead) return;` dentro do próprio handler — necessário porque `lead` é um parâmetro (binding mutável) e o TypeScript descarta o narrowing do `{lead ? ... }` externo dentro de callbacks;
    3. captura em constantes locais `const leadId = lead.id;` e `const nome = lead.nome;` antes de qualquer fronteira assíncrona (o dialog é compartilhado e o pai limpa `lead` ao fechar);
    4. chama `registerWhatsAppContact(leadId, tipo)` — usando o estado vivo `tipo`, JAMAIS a prop `defaultTipo`;
    5. encadeia `.then((result) => { if (result.advanced) { toast.success(...) } })` com o texto exato `` `${nome} avançou para Contatado.` `` (D-07 / Copywriting Contract do UI-SPEC — não alterar a redação, não usar o texto genérico "Lead movido para ...");
    6. encadeia `.catch(() => {})` com um comentário explicando que a falha é silenciosa por design: a aba do WhatsApp já abriu, exibir erro sobre ela confundiria o admin.

    Proibições absolutas nesta task: não tornar o handler `async`; não usar `await`, `preventDefault`, `window.open` nem `startTransition`; não remover ou alterar `href`, `target="_blank"`, `rel="noopener noreferrer"` nem a `className` do anchor; não alterar o branch de fallback `<Button disabled>`; não adicionar guard de "já clicou"/debounce (D-02: cada clique conta separado); não adicionar nenhuma prop nova ao componente; não tocar em `handleTipoChange`, no `useEffect` de reset nem no cálculo de `waHref`.

    Adicionar um comentário de bloco curto acima do anchor citando WA-06/WA-08 e explicando por que a mutação é fire-and-forget (Pitfall 2) e por que o gate real vive no servidor (o cliente não decide se avança). Não escrever o literal `defaultTipo` em nenhum comentário novo.
  </action>
  <acceptance_criteria>
    - O arquivo contém exatamente uma chamada `registerWhatsAppContact(leadId, tipo)`
    - `defaultTipo` continua aparecendo exatamente 5 vezes no arquivo (baseline medido no planejamento) — nenhuma nova referência, nenhuma removida
    - `rel="noopener noreferrer"` continua aparecendo exatamente uma vez
    - Nenhuma ocorrência de `preventDefault`, `window.open` ou `await registerWhatsAppContact` no arquivo
    - O toast usa a string literal `avançou para Contatado.` exatamente uma vez
    - Existe exatamente um `.catch(` associado à chamada da mutação
    - `npx tsc --noEmit` e `npm run lint` limpos
  </acceptance_criteria>
  <verify>
    <automated>
    cd /c/Users/Vencedor/Desktop/crm-leads
    grep -v -E '^\s*(//|\*|/\*)' src/components/whatsapp-preview-dialog.tsx | grep -Fo 'registerWhatsAppContact(leadId, tipo)' | wc -l   # esperado: 1
    grep -v -E '^\s*(//|\*|/\*)' src/components/whatsapp-preview-dialog.tsx | grep -o 'defaultTipo' | wc -l                            # esperado: 5
    grep -o 'rel="noopener noreferrer"' src/components/whatsapp-preview-dialog.tsx | wc -l                                             # esperado: 1
    grep -c 'preventDefault\|window.open\|await registerWhatsAppContact' src/components/whatsapp-preview-dialog.tsx || true            # esperado: 0
    grep -Fo 'avançou para Contatado.' src/components/whatsapp-preview-dialog.tsx | wc -l                                              # esperado: 1
    grep -Fo '.catch(' src/components/whatsapp-preview-dialog.tsx | wc -l                                                              # esperado: 1
    grep -Fo 'import { toast } from "sonner"' src/components/whatsapp-preview-dialog.tsx | wc -l                                       # esperado: 1
    npx tsc --noEmit
    npm run lint
    </automated>
  </verify>
  <done>O clique em "Abrir WhatsApp" abre a aba do wa.me como antes e, em paralelo, dispara `registerWhatsAppContact(leadId, tipo)` com o tipo vivo do Select; quando o servidor responde `advanced: true`, o toast "{Nome} avançou para Contatado." aparece. Cancelar o modal não dispara nada. `tsc`/`lint` limpos.</done>
</task>

<task type="auto">
  <name>Task 2: Indicador do contador de tentativas no card do pipeline</name>
  <files>src/components/pipeline-lead-card.tsx</files>
  <read_first>
    - `src/components/pipeline-lead-card.tsx` — ler o arquivo inteiro; o alvo é a linha de metadados (linhas 82-89) e o padrão do indicador "Esfriando" que ela já contém
    - `.planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-UI-SPEC.md` — Copywriting Contract (formato `{n}x`, `aria-label`), seção Color (contador neutro, `text-muted-foreground`, nunca âmbar/teal) e Spacing (`size-3.5`, `gap-1`)
    - `src/components/whatsapp-send-button.tsx` — precedente de `aria-label` neste projeto
    - `.planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-PATTERNS.md` — seção de `pipeline-lead-card.tsx`
  </read_first>
  <action>
    Adicionar `MessageCircle` ao import existente de `lucide-react` (que hoje traz só `Clock`), mantendo a ordem alfabética do import.

    Dentro do `<div className="flex items-center gap-1 text-[14px] leading-normal text-muted-foreground">` da linha de metadados, imediatamente DEPOIS do bloco condicional do "Esfriando" (o contador fica à direita dele quando ambos aparecem), adicionar um bloco condicional que renderiza apenas quando `lead.contactAttempts > 0` (D-06 — nada é renderizado quando é 0) e, caso contrário, `null`. O elemento renderizado é um `<span>` com:
    - `className="flex items-center gap-1"` — exatamente isso, sem nenhuma classe de cor (a cor neutra `text-muted-foreground` é herdada do `<div>` pai; adicionar cor própria violaria a decisão do UI-SPEC);
    - `aria-label` com o template `{n} tentativas de contato` usando `lead.contactAttempts` (ex.: "3 tentativas de contato");
    - conteúdo: `<MessageCircle className="size-3.5" />` seguido do número e do sufixo `x` (ex.: renderiza "3x"), sem nenhum rótulo por extenso.

    Não alterar `PipelineLeadCardProps` (nenhuma prop nova — `lead` já carrega o campo), não alterar o indicador "Esfriando", a borda âmbar, o `useDraggable`, o `onClick`/`onKeyDown` do card, o bloco do `WhatsAppSendButton` nem os `stopPropagation` existentes. Não usar fallback `?? 0` (a coluna é NOT NULL DEFAULT 0). Não escrever o literal `contactAttempts` em nenhum comentário novo (interfere no gate de contagem).
  </action>
  <acceptance_criteria>
    - O literal `contactAttempts` aparece exatamente 3 vezes em linhas de código do arquivo (condição, `aria-label`, texto renderizado) — nenhuma prop nova foi adicionada ao tipo de props
    - A condição usada é `lead.contactAttempts > 0` (o indicador desaparece quando o contador é 0)
    - `MessageCircle` aparece exatamente 2 vezes (import + JSX) e `size-3.5` exatamente 2 vezes (Clock + MessageCircle)
    - Existe exatamente um `className="flex items-center gap-1"` (o span do contador, sem classe de cor própria)
    - `aria-label` contém o texto `tentativas de contato` exatamente uma vez
    - `text-[#B45309]` continua aparecendo exatamente uma vez (o "Esfriando" não foi alterado nem recolorido)
    - `npx tsc --noEmit` e `npm run lint` limpos
  </acceptance_criteria>
  <verify>
    <automated>
    cd /c/Users/Vencedor/Desktop/crm-leads
    grep -v -E '^\s*(//|\*|/\*)' src/components/pipeline-lead-card.tsx | grep -o 'contactAttempts' | wc -l          # esperado: 3
    grep -Fo 'lead.contactAttempts > 0' src/components/pipeline-lead-card.tsx | wc -l                              # esperado: 1
    grep -o 'MessageCircle' src/components/pipeline-lead-card.tsx | wc -l                                          # esperado: 2
    grep -o 'size-3.5' src/components/pipeline-lead-card.tsx | wc -l                                               # esperado: 2
    grep -Fo 'className="flex items-center gap-1"' src/components/pipeline-lead-card.tsx | wc -l                    # esperado: 1
    grep -Fo 'tentativas de contato' src/components/pipeline-lead-card.tsx | wc -l                                  # esperado: 1
    grep -Fo 'text-[#B45309]' src/components/pipeline-lead-card.tsx | wc -l                                         # esperado: 1
    npx tsc --noEmit
    npm run lint
    </automated>
  </verify>
  <done>Card do pipeline mostra `MessageCircle` + "{n}x" em cor neutra, com `aria-label` descritivo, somente quando o contador é maior que 0; o indicador "Esfriando" e o resto do card permanecem idênticos. `tsc`/`lint` limpos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| página do CRM → aba externa `wa.me` (`target="_blank"`) | O documento de destino é de terceiros; sem `rel` adequado ele obtém referência ao `window` de origem |
| componente cliente → Server Action | `leadId`/`tipo` saem do cliente e são a única entrada da mutação (validação real fica no servidor, 06-01) |
| tela do CRM → observador do ambiente físico | Nome do lead (PII) exibido em toast |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Tampering | anchor `wa.me` com `target="_blank"` (tabnabbing via `window.opener`) | mitigate | `rel="noopener noreferrer"` já presente e preservado; gate de fonte na Task 1 exige exatamente 1 ocorrência após a edição, e a task proíbe alterar os atributos do anchor |
| T-06-10 | Tampering | gate de tipo lido do cliente | mitigate | O cliente envia o estado vivo `tipo` (nunca `defaultTipo`, gate de contagem na Task 1) e o servidor revalida o par (tipo, etapa) por conta própria — o cliente não decide o avanço |
| T-06-11 | Spoofing (parâmetro forjado) | chamada direta de `registerWhatsAppContact` sem passar pela UI | transfer | Mitigado na fronteira do servidor pelo plano 06-01 (Zod + `isNull(deletedAt)`); nenhum controle adicional possível no cliente |
| T-06-12 | Denial of Service (UI) | rejeição não tratada da promise da mutação | mitigate | `.catch(() => {})` obrigatório na Task 1 — falha de rede/servidor nunca gera unhandled rejection nem toast de erro sobre uma aba já aberta |
| T-06-13 | Information Disclosure | nome do lead (PII) no toast e contador no card | accept | Ferramenta solo em máquina do próprio admin, sem acesso multiusuário (constraint travada em `CLAUDE.md`); o nome já aparece no card e no dialog na mesma tela |
| T-06-SC | Tampering | instalações npm/pip/cargo | accept | Zero pacotes novos (`06-UI-SPEC.md`: Registry Safety "not required"; `06-RESEARCH.md`: zero pacotes novos). Nenhuma task executa instalação; se precisar, parar e rodar o gate de legitimidade antes |
</threat_model>

<verification>
Automatizado (com o dev server parado antes do build — host de 4GB, o worker do Next já morreu por falta de memória com o dev server ativo):

1. `npx tsc --noEmit` — limpo
2. `npm run lint` — limpo
3. `node scripts/verify-wa-contact-invariant.cjs` — exit 0 (invariante de 06-01 continua valendo)
4. Todos os gates de `grep` das Tasks 1 e 2 com os valores esperados
5. `npm run build` — completa sem erro (rodar por último, com o dev server parado)

<human-check>
Verificação no navegador (`http://localhost:3000`) — o projeto nunca teve um click-through real executado (débito registrado em `STATE.md`), então esta é a validação final da fase:

1. `/pipeline`: num lead da coluna **Novo**, clicar em "Enviar WhatsApp" no card, manter o tipo "1º contato" e clicar em "Abrir WhatsApp". Esperado: a aba do WhatsApp abre, o modal fecha, o toast "{Nome} avançou para Contatado." aparece, o card migra para a coluna **Contatado** e passa a exibir o ícone de mensagem com "1x".
2. Mesmo lead (agora em Contatado): repetir o clique com "1º contato". Esperado: **nenhum** toast de avanço, o lead continua em Contatado, o contador passa para "2x".
3. Lead em **Negociação**: clicar em "Abrir WhatsApp" com "1º contato". Esperado: etapa inalterada (nunca regride), contador incrementa.
4. Abrir o modal de qualquer lead e fechar com **Cancelar** (e depois com Escape e clique fora). Esperado: contador inalterado, etapa inalterada, nenhum toast.
5. `/` (dashboard): abrir o modal de um lead **Novo** (que abre com "Follow-up"), trocar o tipo para "1º contato" e clicar em "Abrir WhatsApp". Esperado: avança e mostra o toast (o tipo do momento do clique é o que vale).
6. `/` (dashboard): abrir um lead **Novo**, deixar em "Follow-up" e enviar. Esperado: **não** avança, sem toast, mas o contador incrementa.
7. `/leads`: enviar por "1º contato" um lead **Novo**. Esperado: toast com o nome correto do lead (a tela tem vários leads visíveis) e etapa atualizada na tabela.
8. Criar um lead novo pelo botão "Novo lead" (o modal de 1º contato abre automaticamente) e fechar sem enviar. Esperado: contador em 0 (nada no card do pipeline).
9. Importar um CSV e, em `/importar/[batchId]`, clicar em "Abrir WhatsApp" de um lead. Esperado: a aba abre e, ao voltar ao `/pipeline`, o card mostra "1x" e o lead está em Contatado.
10. Card com contador 0: confirmar que nenhum ícone/número extra aparece (D-06).
11. Card simultaneamente "Esfriando" e com contador > 0: confirmar que os dois indicadores aparecem na mesma linha, "Esfriando" em âmbar e o contador em cinza neutro, sem quebra de layout.
</human-check>
</verification>

<success_criteria>
- Clicar em "Abrir WhatsApp" incrementa o contador em qualquer tela e com qualquer tipo de template (WA-08); abrir/cancelar o modal nunca incrementa (D-01)
- Lead em "Novo" + tipo "1º contato" avança para "Contatado" com o toast "{Nome} avançou para Contatado." (WA-06/D-07)
- Nenhum lead fora de "Novo" muda de etapa por esse clique, e nenhum lead re-avança (WA-07)
- A aba do `wa.me` abre em todos os cliques (nenhum `preventDefault`/`window.open`, `rel="noopener noreferrer"` intacto)
- Card do pipeline exibe ícone + "{n}x" em cor neutra só quando o contador é maior que 0, sem prop nova e sem alterar o indicador "Esfriando"
- `tsc --noEmit`, `lint` e `build` limpos; `node scripts/verify-wa-contact-invariant.cjs` exit 0
</success_criteria>

<output>
Criar `.planning/phases/06-auto-avan-o-de-etapa-contador-de-tentativas/06-02-SUMMARY.md` ao terminar, em PT-BR, registrando quais itens do `<human-check>` foram efetivamente executados no navegador e quais ficaram pendentes (o projeto tem débito histórico de click-through nunca executado — não declarar como verificado o que não foi clicado).
</output>
