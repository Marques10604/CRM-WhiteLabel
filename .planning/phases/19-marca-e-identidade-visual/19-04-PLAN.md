---
phase: 19-marca-e-identidade-visual
plan: 04
type: execute
wave: 4
depends_on: ["19-03"]
files_modified:
  - src/components/pipeline-lead-card.tsx
  - src/components/pipeline-column.tsx
  - src/components/pipeline-board.tsx
  - src/components/lead-table.tsx
  - src/components/lead-table-toolbar.tsx
  - src/components/lead-table-columns.tsx
  - src/components/lead-form-dialog.tsx
  - src/components/lead-timeline-dialog.tsx
  - src/components/lixeira-table.tsx
  - src/components/csv-upload-dropzone.tsx
  - src/components/csv-column-mapper.tsx
  - src/components/post-import-lead-list.tsx
  - src/components/whatsapp-preview-dialog.tsx
  - src/components/whatsapp-send-button.tsx
autonomous: true
requirements: [BRAND-02]

must_haves:
  truths:
    - "D-06: os 14 arquivos deste plano saem por completo de cor hardcoded para token — refactor completo, não parcial"
    - "O board do pipeline, a lista de leads e o wizard de importação respondem à paleta escolhida"
    - "A borda 'esfriando' do card do pipeline continua legível como aviso, não como cor de marca"
    - "O botão WhatsApp continua verde semântico e não vira a cor primária da marca"
    - "As superfícies do pipeline e das tabelas respondem ao .dark quando ele é forçado"
    - "Nenhum comportamento existente mudou — só classes de cor"
  artifacts:
    - path: "src/components/lead-table.tsx"
      provides: "tabela de leads e botão WhatsApp em tokens"
      contains: "bg-status-success"
    - path: "src/components/pipeline-lead-card.tsx"
      provides: "card do pipeline com borda de aviso tokenizada"
      contains: "border-status-warning"
    - path: "src/components/pipeline-column.tsx"
      provides: "coluna e estado isOver em tokens de superfície"
    - path: "src/components/csv-column-mapper.tsx"
      provides: "checkbox nativo com accent-primary"
      contains: "accent-primary"
    - path: "src/components/whatsapp-preview-dialog.tsx"
      provides: "erro de telefone inválido em text-destructive"
  key_links:
    - from: "src/components/lead-table.tsx"
      to: "src/app/globals.css"
      via: "utilitário bg-status-success do botão WhatsApp"
      pattern: "bg-status-success"
    - from: "src/components/pipeline-lead-card.tsx"
      to: "src/app/globals.css"
      via: "utilitário border-status-warning do estado esfriando"
      pattern: "border-status-warning"
    - from: "src/components/lead-table-toolbar.tsx"
      to: "src/components/etapa-badge.tsx"
      via: "import de STAGE_OPTIONS (contrato preservado no plano 19-03)"
      pattern: "STAGE_OPTIONS"
---

<objective>
Refatorar para tokens as 41 ocorrências de cor hardcoded dos 14 arquivos do pipeline, da lista
de leads e do wizard de importação de CSV.

Purpose: sem este refactor a paleta nova aplicaria só nos botões `<Button>` default e o resto do
app continuaria teal — botão novo com sidebar e pipeline velhos (D-06/D-07). Este é o maior
bloco mecânico da fase; a tabela de-para já está resolvida, o executor aplica sem redecidir.

Output: 14 arquivos sem `#hex`, sem `-[#...]`, sem escala `zinc-*`/`white`/`black` e sem `style`
inline de cor.

Escopo: **refactor COMPLETO de cada arquivo listado** — nenhum destes arquivos é reaberto em
outro plano da fase.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-marca-e-identidade-visual/19-CONTEXT.md
@.planning/phases/19-marca-e-identidade-visual/19-PATTERNS.md
@.planning/phases/19-marca-e-identidade-visual/19-03-SUMMARY.md

<interfaces>
<!-- Tabela de-para autoritativa (19-PATTERNS linhas 342-356 + 19-RESEARCH linhas 369-384). -->
<!-- O executor aplica; não redecide o mapeamento. -->

| Padrão atual | Token destino |
|---|---|
| `bg-[#0D9488] text-white hover:bg-[#0D9488]/90` em `<Button>` | **deletar o className de cor** — `<Button>` default já é `bg-primary text-primary-foreground hover:bg-primary/80` (`src/components/ui/button.tsx:11`) |
| `bg-[#F4F4F5]` | `bg-muted` (superfície interna) |
| `bg-[#E4E4E7]` | `bg-accent` (estado `isOver` do drop) |
| `text-[#0D9488]` / `hover:text-[#0D9488]` | `text-primary` / `hover:text-primary` |
| `bg-[#0D9488]/10 text-[#0D9488]` (avatar/badge) | `bg-primary/10 text-primary` |
| `text-zinc-400` / `text-zinc-500` | `text-muted-foreground` |
| `text-zinc-700` | `text-foreground` |
| `border-zinc-200` / `border-zinc-300` | `border` (a cor vem de `@layer base * { @apply border-border }`) — em campo de formulário, `border-input` |
| `hover:bg-zinc-200/60` | `hover:bg-accent` |
| `bg-white` | `bg-card` |
| `text-[#DC2626]` | `text-destructive` |
| `text-[#B91C1C]` em mensagem de ERRO (não etapa do funil) | `text-destructive` |
| `ring-[#0D9488]` / `focus-visible:ring-[#0D9488]/50` / `focus-visible:border-[#0D9488]` | `ring-ring` / `focus-visible:ring-ring/50` / `focus-visible:border-ring` |
| `accent-[#0D9488]` (checkbox nativo) | `accent-primary` — NUNCA `accent-[var(--primary)]` (Pitfall 6: o utilitário resolve `var(--color-primary)`) |
| `border-[#F59E0B]` (estado "esfriando") | `border-status-warning` |
| `text-[#B45309]` (label "Esfriando") | `text-status-warning-foreground` |
| `bg-[#22C55E] ... hover:bg-[#16A34A]` (botão WhatsApp) | `bg-status-success text-status-success-foreground hover:bg-status-success/90` |

**DECISÃO TRAVADA (Open Question 4 do 19-RESEARCH):** o verde do botão WhatsApp reusa
`--status-success`. NÃO criar um token `--whatsapp` dedicado.

**Escala `--status-*` já disponível** (criada no plano 19-03, não redefinir):
`bg-|text-|border-status-{neutral,info,warning,success,danger}` e os respectivos `-foreground`.

**Anti-padrões travados:** `transition-colors` onde já existe permanece (nunca `transition-all`);
não recriar botão primário com classe; não adicionar `ThemeProvider`/toggle (D-16); nenhuma
mudança de lógica, props, exports, ordem de render, efeitos ou handlers; nenhuma supressão
`eslint-disable` removida ou acrescentada.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refatorar o board do pipeline e a tabela de leads</name>
  <files>src/components/pipeline-lead-card.tsx, src/components/pipeline-column.tsx, src/components/pipeline-board.tsx, src/components/lead-table.tsx</files>
  <read_first>
    - `src/components/pipeline-lead-card.tsx` — linhas ~61–65 (`focus-visible:ring-[#0D9488]`,
      `border-2 border-[#F59E0B]` quando `isEsfriando`, `border border-zinc-200`, `bg-white`)
      e ~104 (`text-[#B45309]` do label "Esfriando").
    - `src/components/pipeline-column.tsx` — linhas ~30–35 (`bg-[#F4F4F5]` da coluna,
      `bg-[#E4E4E7]` no `isOver`, `bg-[#F4F4F5]` do header sticky). Preservar
      `transition-colors`, `flex-1`, `min-w-[200px]` e `gap-3` — as 5 colunas precisam continuar
      cabendo sem scroll horizontal (quick-fix `5b52ffa`).
    - `src/components/pipeline-board.tsx` — a ocorrência única de cor.
    - `src/components/lead-table.tsx` — 8 ocorrências, incluindo o botão WhatsApp da linha ~264
      (`bg-[#22C55E] font-semibold text-white shadow-sm hover:bg-[#16A34A]`) e os wrappers de
      `stopPropagation`, que NÃO podem ser alterados.
    - `src/components/etapa-badge.tsx` (refatorado no plano 19-03) — idioma de referência de
      mapa semântico → classe utilitária.
    - `src/components/ui/button.tsx` linha 11 — a variante `default`.
  </read_first>
  <action>
    Aplicar a tabela de-para do bloco `<interfaces>` nos 4 arquivos, uma edição por ocorrência.

    Pontos que exigem atenção específica:
    - `pipeline-lead-card.tsx`: a borda de "esfriando" é `border-2 border-[#F59E0B]` — manter
      `border-2` e trocar só a cor por `border-status-warning`; o label `text-[#B45309]` vira
      `text-status-warning-foreground`. O `border border-zinc-200` do estado normal vira só
      `border`; `bg-white` vira `bg-card`; `focus-visible:ring-[#0D9488]` vira
      `focus-visible:ring-ring`. Preservar o `cn()` condicional existente e o `truncate` + `title`
      no nome do lead (quick-fix `0a72800`).
    - `pipeline-column.tsx`: coluna `bg-[#F4F4F5]` → `bg-muted`; `isOver` `bg-[#E4E4E7]` →
      `bg-accent`; header sticky `bg-[#F4F4F5]` → `bg-muted`. Zero mudança de layout.
    - `lead-table.tsx`: o botão WhatsApp da linha ~264 vira
      `bg-status-success text-status-success-foreground hover:bg-status-success/90`, mantendo
      `gap-1.5`, `font-semibold` e `shadow-sm`. As demais 7 ocorrências seguem a tabela.
      NÃO tocar nos handlers de `stopPropagation` nem na renderização condicional do ícone
      `History`.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; node scripts/verify-no-hardcoded-colors.cjs 2>&amp;1 | grep -cE 'pipeline-lead-card|pipeline-column|pipeline-board|lead-table\.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-no-hardcoded-colors.cjs 2>&1 | grep -cE 'pipeline-lead-card|pipeline-column|pipeline-board|lead-table\.tsx'` retorna `0`.
    - `grep -c 'border-status-warning' src/components/pipeline-lead-card.tsx` ≥ 1 e
      `grep -c 'border-2' src/components/pipeline-lead-card.tsx` ≥ 1 (espessura preservada).
    - `grep -c 'bg-status-success' src/components/lead-table.tsx` ≥ 1 e
      `grep -cE '22C55E|16A34A' src/components/lead-table.tsx` retorna `0`.
    - `grep -c 'min-w-\[200px\]' src/components/pipeline-column.tsx` ≥ 1 e
      `grep -c 'transition-colors' src/components/pipeline-column.tsx` ≥ 1.
    - `grep -c 'transition-all' src/components/pipeline-lead-card.tsx src/components/pipeline-column.tsx src/components/pipeline-board.tsx src/components/lead-table.tsx` retorna `0` em todos.
    - `git diff -U0 src/components/lead-table.tsx | grep -c 'stopPropagation'` retorna `0`
      (nenhum handler entrou no diff).
    - `npx tsc --noEmit` sai `0` e `npm run lint` sai `0`.
  </acceptance_criteria>
  <done>Pipeline e tabela de leads em tokens; borda "esfriando" e botão WhatsApp com semântica preservada; layout e handlers intactos.</done>
</task>

<task type="auto">
  <name>Task 2: Refatorar toolbar, colunas, dialogs de lead e lixeira</name>
  <files>src/components/lead-table-toolbar.tsx, src/components/lead-table-columns.tsx, src/components/lead-form-dialog.tsx, src/components/lead-timeline-dialog.tsx, src/components/lixeira-table.tsx</files>
  <read_first>
    - `src/components/lead-timeline-dialog.tsx` — 5 ocorrências (o mais denso desta task);
      contém `eslint-disable-next-line react-hooks/set-state-in-effect` documentado e o
      `DeleteNotaDialog` renderizado como IRMÃO do Dialog (não aninhado).
    - `src/components/lead-form-dialog.tsx` — 3 ocorrências. NÃO tocar no
      `startTransition(() => {...})` do `formAction` (quick-fix `260808-h5i`) nem no
      `eslint-disable-next-line react-hooks/refs`.
    - `src/components/lead-table-toolbar.tsx` — 2 ocorrências; importa `STAGE_OPTIONS` de
      `etapa-badge.tsx` (contrato preservado no plano 19-03).
    - `src/components/lead-table-columns.tsx` — 1 ocorrência; contém `DEFAULT_SORTING` e
      `FollowUpDateRange`, que NÃO podem ser movidos (evita import circular com `lead-table.tsx`).
    - `src/components/lixeira-table.tsx` — 1 ocorrência.
    - `src/components/ui/button.tsx` linha 11 e `src/lib/utils.ts` (helper `cn`).
  </read_first>
  <action>
    Aplicar a tabela de-para do bloco `<interfaces>` nos 5 arquivos.

    Pontos específicos:
    - Campos de formulário em `lead-form-dialog.tsx`: borda de input usa `border-input`
      (não o `border` genérico) e o foco usa `focus-visible:ring-ring` /
      `focus-visible:border-ring`.
    - Ícone/texto de exclusão (`text-[#DC2626]`) → `text-destructive`.
    - Botão de ação primária: deletar o `className` de cor, deixando `<Button>` puro; props
      `variant`/`size`/`onClick` permanecem.
    - Preservar TODOS os `eslint-disable-next-line` já documentados (falsos-positivos do React
      Compiler — precedente STATE.md 07-02/09-03/09-04) e todos os handlers.
    - Nenhuma alteração em `DEFAULT_SORTING`, `FollowUpDateRange` ou nos filtros da toolbar.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; node scripts/verify-no-hardcoded-colors.cjs 2>&amp;1 | grep -cE 'lead-table-toolbar|lead-table-columns|lead-form-dialog|lead-timeline-dialog|lixeira-table'</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-no-hardcoded-colors.cjs 2>&1 | grep -cE 'lead-table-toolbar|lead-table-columns|lead-form-dialog|lead-timeline-dialog|lixeira-table'` retorna `0`.
    - `git diff -U0 -- src/components/lead-form-dialog.tsx src/components/lead-timeline-dialog.tsx | grep -c 'eslint-disable'` retorna `0` (nenhuma supressão entrou no diff).
    - `grep -c 'startTransition' src/components/lead-form-dialog.tsx` ≥ 1.
    - `grep -c 'DEFAULT_SORTING' src/components/lead-table-columns.tsx` ≥ 1.
    - `grep -c 'text-destructive' src/components/lead-timeline-dialog.tsx` ≥ 1 (a ação de excluir
      nota continua sinalizada como destrutiva).
    - `git diff -U0` destes 5 arquivos contém apenas linhas de `className`/string de classe —
      nenhum import novo, nenhuma assinatura ou hook alterado.
    - `npx tsc --noEmit` sai `0` e `npm run lint` sai `0`.
  </acceptance_criteria>
  <done>Toolbar, colunas, dialogs de lead e lixeira em tokens, com supressões de lint e lógica intocadas.</done>
</task>

<task type="auto">
  <name>Task 3: Refatorar o wizard de importação e as superfícies de WhatsApp</name>
  <files>src/components/csv-upload-dropzone.tsx, src/components/csv-column-mapper.tsx, src/components/post-import-lead-list.tsx, src/components/whatsapp-preview-dialog.tsx, src/components/whatsapp-send-button.tsx</files>
  <read_first>
    - `src/components/csv-upload-dropzone.tsx` — 4 ocorrências (estados de drag/hover/repouso).
    - `src/components/csv-column-mapper.tsx` — 4 ocorrências, incluindo `accent-[#0D9488]` no
      `<input type="checkbox">` nativo (~148). Preservar o comentário "8 campos fixos"
      (corrigido na Fase 16, IN-01) e `handleToggleExtraColumn`.
    - `src/components/whatsapp-preview-dialog.tsx` — 4 ocorrências, incluindo `text-[#B91C1C]`
      de "telefone inválido" (~172 e ~175). NÃO tocar no `waHref` recomputado a cada render
      (Pitfall 4 da Fase 4) nem no `eslint-disable react-hooks/set-state-in-effect`.
    - `src/components/whatsapp-send-button.tsx` — 1 ocorrência. NÃO alterar o
      `registerWhatsAppContact` fire-and-forget nem o `<a>` estilizado via `buttonVariants()`.
    - `src/components/post-import-lead-list.tsx` — 1 ocorrência.
    - `src/components/csv-import-preview-table.tsx` (refatorado no plano 19-03) — referência de
      `accent-primary` e dos badges de flag.
  </read_first>
  <action>
    Aplicar a tabela de-para do bloco `<interfaces>` nos 5 arquivos.

    Pontos específicos:
    - `csv-column-mapper.tsx`: `accent-[#0D9488]` → `accent-primary` (Pitfall 6). NUNCA
      `accent-[var(--primary)]`, que falha por faltar o prefixo `--color-`.
    - `csv-upload-dropzone.tsx`: o realce de drag-over usa `border-ring` + `bg-accent`; o estado
      de repouso usa `border-input` + `bg-muted`. Manter `border-dashed` e qualquer
      `transition-colors` já existente.
    - `whatsapp-preview-dialog.tsx`: as duas mensagens de telefone inválido viram
      `text-destructive` — é erro de validação, NÃO etapa do funil, então não usa `status-danger`.
    - `whatsapp-send-button.tsx`: se a ocorrência for o verde de WhatsApp, aplicar
      `bg-status-success text-status-success-foreground hover:bg-status-success/90` mantendo o
      `<a>` + `buttonVariants()`; se for cor de marca, seguir a tabela de-para normal.
    - Zero mudança de lógica, props, ordem de efeitos ou supressões de lint.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; rm -rf .next &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-no-hardcoded-colors.cjs 2>&1 | grep -cE 'csv-upload-dropzone|csv-column-mapper|post-import-lead-list|whatsapp-preview-dialog|whatsapp-send-button'` retorna `0`.
    - `grep -c 'accent-primary' src/components/csv-column-mapper.tsx` ≥ 1 e
      `grep -c 'accent-\[' src/components/csv-column-mapper.tsx` retorna `0`.
    - `grep -c 'text-destructive' src/components/whatsapp-preview-dialog.tsx` ≥ 2 (as duas
      mensagens de telefone inválido) e `grep -c 'B91C1C' src/components/whatsapp-preview-dialog.tsx`
      retorna `0`.
    - `grep -c 'registerWhatsAppContact' src/components/whatsapp-send-button.tsx` ≥ 1 e
      `grep -c 'buttonVariants' src/components/whatsapp-send-button.tsx` ≥ 1.
    - `grep -c '8 campos fixos' src/components/csv-column-mapper.tsx` ≥ 1 (comentário da Fase 16
      preservado).
    - Ao fim das 3 tasks, `node scripts/verify-no-hardcoded-colors.cjs` reporta **≤ 45 findings**
      restantes, todos em arquivos do plano 19-05 (`app-sidebar`, `layout`, `template-*`,
      `nicho-manager`, `motivo-perda-*`, `configuracoes-form`, `periodo-selector`, `tarefa-*`,
      `ui/dialog`) — nenhum arquivo dos planos 19-03/19-04 na lista.
    - `npx tsc --noEmit` sai `0`, `npm run lint` sai `0`, `rm -rf .next && npm run build` sai `0`
      com 13+ páginas geradas.
    - `npm run check:contrast` continua saindo `0`.
  </acceptance_criteria>
  <done>Wizard de CSV e superfícies de WhatsApp em tokens; os 14 arquivos deste plano saíram da lista de findings do grep-guard; build verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dados do lead → classe CSS | Estados derivados de dado (`isEsfriando`, `isOver`, telefone inválido) selecionam classes. Nenhuma classe pode ser montada por concatenação de valor vindo do banco. |
| arquivo CSV do parceiro → prévia renderizada | O wizard renderiza conteúdo de CSV externo; este plano não altera esse caminho, só as classes ao redor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-08 | Tampering | seleção de classe por estado (`cn()` condicional em `pipeline-lead-card`, `pipeline-column`, `csv-upload-dropzone`) | mitigate | Classes são literais constantes dentro de `cn(...)`; nenhuma interpolação de valor de dado em nome de classe. `tsc --noEmit` cobre as uniões tipadas |
| T-19-10 | Tampering | refactor mecânico em 14 arquivos com lógica sensível (transação de WhatsApp, `startTransition`, `stopPropagation`, `waHref`) | mitigate | Cada task exige `git diff` contendo apenas linhas de `className`; aceites checam explicitamente `stopPropagation`, `startTransition`, `registerWhatsAppContact`, `buttonVariants` e as supressões de lint intactos |
| T-19-05 | Denial of usability | perda de legibilidade nas superfícies migradas | mitigate | `npm run check:contrast` verde ao fim do plano + checklist D-19 (`19-HUMAN-UAT.md`) na fase |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote instalado (19-RESEARCH §Package Legitimacy Audit: "Audit N/A"). Plano só troca strings de classe. |
</threat_model>

<verification>
1. `node scripts/verify-no-hardcoded-colors.cjs` → ainda exit 1 (RED até o plano 19-05), mas **zero linhas** dos 14 arquivos deste plano e dos 4 do plano 19-03.
2. `npx tsc --noEmit` → exit 0.
3. `npm run lint` → exit 0.
4. `npm run check:contrast` → exit 0.
5. `rm -rf .next && npm run build` → exit 0, 13+ páginas.
6. `npm run test:lead-actions` e `npm run test:interacao-actions` → exit 0 (nenhuma lógica de Server Action foi tocada).
7. `git diff -U0` do plano não contém `stopPropagation`, `startTransition`, `eslint-disable`, `waHref` nem `registerWhatsAppContact`.
</verification>

<success_criteria>
- Os 14 arquivos estão livres de `#hex`, `-[#...]`, escala `zinc/slate/gray/neutral/white/black` e `style` inline de cor.
- A semântica sobreviveu ao refactor: "esfriando" continua aviso (`status-warning`), WhatsApp continua verde (`status-success`), telefone inválido continua erro (`destructive`), drop-target continua realçado (`accent`).
- Layout preservado: 5 colunas do pipeline sem scroll horizontal, nome de lead truncando com reticências.
- Zero mudança de comportamento: handlers, efeitos, transações e supressões de lint idênticos.
- Build, tsc, lint, contraste e os harnesses de Server Action verdes.
</success_criteria>

<output>
Create `.planning/phases/19-marca-e-identidade-visual/19-04-SUMMARY.md` when done.
Registrar a contagem de findings restantes do `verify-no-hardcoded-colors.cjs` e a lista de
arquivos que sobraram — o plano 19-05 fecha exatamente essa lista.
</output>
