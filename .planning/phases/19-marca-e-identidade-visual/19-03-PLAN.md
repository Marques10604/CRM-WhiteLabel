---
phase: 19-marca-e-identidade-visual
plan: 03
type: execute
wave: 3
depends_on: ["19-01", "19-02"]
files_modified:
  - src/app/globals.css
  - src/components/etapa-badge.tsx
  - src/components/csv-import-preview-table.tsx
  - src/components/followup-dashboard.tsx
  - src/app/relatorios/page.tsx
autonomous: true
requirements: [BRAND-02]

must_haves:
  truths:
    - "D-08: a escala --status-* é dedicada e não derivada da marca, definida em light e dark"
    - "D-06: os 4 arquivos deste plano saem por completo de cor hardcoded para token — nenhum é reaberto depois"
    - "D-16: nenhum ThemeProvider nem toggle de tema é adicionado"
    - "As 5 cores de status (novo/contatado/negociação/fechado/perdido) continuam mutuamente distinguíveis depois da troca de paleta"
    - "As mesmas 5 cores semânticas servem etapa do pipeline, urgência do dashboard e flags da prévia de CSV"
    - "As cores de status respondem ao bloco .dark quando ele é forçado"
    - "O sidebar não fica com cor de outra paleta quando .dark é forçado"
    - "Nenhum style inline de cor sobrou em etapa-badge, followup-dashboard ou csv-import-preview-table"
  artifacts:
    - path: "src/app/globals.css"
      provides: "escala --status-* (5 pares) em :root e .dark + 10 mapeamentos --color-status-* no @theme inline + --sidebar-* alinhados"
      contains: "--status-danger-foreground"
    - path: "src/components/etapa-badge.tsx"
      provides: "mapa STAGE_TOKEN por classe utilitária, STAGE_OPTIONS preservado"
      contains: "bg-status-"
    - path: "src/components/csv-import-preview-table.tsx"
      provides: "StatusBadges por classe utilitária + accent-primary"
    - path: "src/components/followup-dashboard.tsx"
      provides: "seções de urgência por classe utilitária, botões primários sem override"
    - path: "src/app/relatorios/page.tsx"
      provides: "faixa de intervalo inválido em token de warning"
  key_links:
    - from: "src/components/etapa-badge.tsx"
      to: "src/app/globals.css"
      via: "utilitários bg-status-*/text-status-*-foreground expostos pelo @theme inline"
      pattern: "bg-status-(neutral|info|warning|success|danger)"
    - from: "src/app/globals.css"
      to: "Tailwind v4"
      via: "@theme inline --color-status-*: var(--status-*)"
      pattern: "--color-status-neutral:\\s*var\\(--status-neutral\\)"
---

<objective>
Criar a escala semântica `--status-*` (D-08), alinhar os `--sidebar-*` à paleta nova (Pitfall 3),
e refatorar por completo os 4 arquivos de maior densidade de cor de status.

Purpose: as cores de status NÃO derivam da marca — verde/vermelho/âmbar/azul precisam continuar
mutuamente distinguíveis ou a leitura do funil quebra (D-08). Hoje elas vivem como
`style={{backgroundColor:"#FEE2E2"}}` inline, que não responde ao `.dark` e não é token. Os
`--sidebar-*` ficam descolados depois do `/brand-design` (o skill não os toca) e o
`.dark --sidebar-primary` ainda carrega o leftover azul-roxo `oklch(0.488 0.243 264.376)` do
default shadcn — quando o plano 19-05 trocar o sidebar para `bg-sidebar`, esse bug fica visível.

Output: 10 tokens `--status-*` em `:root` + 10 em `.dark` + 10 linhas no `@theme inline`,
8 `--sidebar-*` aliasados nos dois blocos, e 29 ocorrências de cor eliminadas em 4 arquivos.

Escopo de cor deste plano: **refactor COMPLETO dos arquivos listados** — cor de status E cor de
marca E neutros. Cada arquivo é aberto uma vez só na fase inteira.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-marca-e-identidade-visual/19-CONTEXT.md
@.planning/phases/19-marca-e-identidade-visual/19-PATTERNS.md
@.planning/phases/19-marca-e-identidade-visual/19-02-SUMMARY.md

<interfaces>
<!-- Tabela de-para autoritativa. O executor NÃO precisa explorar o codebase para descobri-la. -->

**Escala `--status-*` (D-08) — ponto de partida OKLCH, do 19-RESEARCH linhas 410–425.**
São valores fixos, NÃO derivados da marca. O executor pode ajustar o croma/lightness para
harmonizar com a paleta escolhida no 19-02, desde que (a) as 5 famílias continuem
distinguíveis entre si e (b) todo par `-foreground`/base passe AA no `check-contrast.cjs`.

```
:root
  --status-neutral: oklch(0.97 0 0)        --status-neutral-foreground: oklch(0.37 0 0)
  --status-info:    oklch(0.93 0.05 255)   --status-info-foreground:    oklch(0.45 0.16 255)
  --status-warning: oklch(0.94 0.06 85)    --status-warning-foreground: oklch(0.48 0.11 65)
  --status-success: oklch(0.94 0.06 155)   --status-success-foreground: oklch(0.48 0.13 155)
  --status-danger:  oklch(0.93 0.05 25)    --status-danger-foreground:  oklch(0.48 0.19 25)
.dark
  --status-neutral: oklch(0.27 0 0)        --status-neutral-foreground: oklch(0.87 0 0)
  --status-info:    oklch(0.30 0.06 255)   --status-info-foreground:    oklch(0.85 0.10 255)
  --status-warning: oklch(0.32 0.06 70)    --status-warning-foreground: oklch(0.87 0.10 85)
  --status-success: oklch(0.30 0.07 155)   --status-success-foreground: oklch(0.87 0.12 155)
  --status-danger:  oklch(0.30 0.08 25)    --status-danger-foreground:  oklch(0.87 0.12 25)
```

**Mapa hex atual → token de status (19-RESEARCH linhas 388–406):**
| Hex (bg / fg) | Semântica | Token |
|---|---|---|
| `#F4F4F5` / `#3F3F46` | etapa "Novo", urgência "Próximos 7 dias", flag "sem nicho" (`#E4E4E7`) | `status-neutral` |
| `#DBEAFE` / `#1D4ED8` | etapa "Contatado", flag "nicho novo" | `status-info` |
| `#FEF3C7` / `#B45309` | etapa "Negociação", urgência "Hoje", flag "duplicado", badge "cortado em 500" | `status-warning` |
| `#DCFCE7` / `#15803D` | etapa "Fechado" | `status-success` |
| `#FEE2E2` / `#B91C1C` | etapa "Perdido", urgência "Vencidos", flag "telefone inválido" | `status-danger` |

**Grupos de cor de MARCA/neutro (19-PATTERNS linhas 342–356) — aplicáveis nos 4 arquivos:**
| Padrão atual | Token destino |
|---|---|
| `bg-[#0D9488] text-white hover:bg-[#0D9488]/90` em `<Button>` | remover o `className` de cor — o `<Button>` default já é `bg-primary text-primary-foreground hover:bg-primary/80` (`src/components/ui/button.tsx:11`) |
| `bg-[#F4F4F5]` (superfície interna) | `bg-muted` |
| `text-[#0D9488]` / `hover:text-[#0D9488]` | `text-primary` / `hover:text-primary` |
| `bg-[#0D9488]/10 text-[#0D9488]` (avatar) | `bg-primary/10 text-primary` |
| `text-zinc-400` / `text-zinc-500` | `text-muted-foreground` |
| `text-zinc-700` | `text-foreground` |
| `border-zinc-200` / `border-zinc-300` | `border` (o `@layer base * { @apply border-border }` já resolve a cor) ou `border-input` em campos |
| `bg-white` (card) | `bg-card` |
| `text-[#DC2626]` | `text-destructive` |
| `ring-[#0D9488]` / `focus-visible:ring-[#0D9488]/50` / `focus-visible:border-[#0D9488]` | `ring-ring` / `focus-visible:ring-ring/50` / `focus-visible:border-ring` |
| `accent-[#0D9488]` (checkbox nativo) | `accent-primary` |
| `bg-[#E4E4E7]` (pipeline isOver) | `bg-accent` |

**Alias dos `--sidebar-*` (Pitfall 3) — aplicar em `:root` E em `.dark`:**
```
--sidebar: var(--card)                       --sidebar-foreground: var(--card-foreground)
--sidebar-primary: var(--primary)            --sidebar-primary-foreground: var(--primary-foreground)
--sidebar-accent: var(--accent)              --sidebar-accent-foreground: var(--accent-foreground)
--sidebar-border: var(--border)              --sidebar-ring: var(--ring)
```

**Anti-padrões travados:** não usar `transition-all` (manter `transition-colors`); não recriar
botão primário com classe; não derivar status da marca; não adicionar `ThemeProvider`/toggle (D-16).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar a escala --status-* e alinhar --sidebar-* em globals.css</name>
  <files>src/app/globals.css</files>
  <read_first>
    - `src/app/globals.css` (129 linhas) — os blocos `@theme inline` (~6–49), `:root` (~51–84,
      com `--chart-1..5` em ~70–74 e `--sidebar-*` em ~76–83), `.dark` (~86–118, com
      `--sidebar-primary: oklch(0.488 0.243 264.376)` em ~112) e `@layer base` (~120–130),
      já com a paleta do plano 19-02 aplicada.
    - `scripts/check-contrast.cjs` (plano 19-01) — os 15 pares e limiares que esta task tem que
      fazer passar.
    - `.planning/phases/19-marca-e-identidade-visual/19-PATTERNS.md` linhas 82–113.
    - `.planning/phases/19-marca-e-identidade-visual/19-02-SUMMARY.md` — conferir se o skill
      tocou ou não em `--sidebar-*`/`--chart-*`.
  </read_first>
  <action>
    Três edições cirúrgicas em `src/app/globals.css`, preservando toda a estrutura existente:

    1. **`@theme inline`** — adicionar 10 linhas no final do bloco `--color-*`, imediatamente
       antes da escala `--radius-sm..4xl`:
       `--color-status-neutral: var(--status-neutral);` e o par `-foreground`, e o mesmo para
       `info`, `warning`, `success`, `danger`. São essas 10 linhas que habilitam os utilitários
       `bg-status-*`, `text-status-*` e `border-status-*` no Tailwind v4.

    2. **`:root` e `.dark`** — adicionar os 10 pares `--status-*` de cada bloco (valores de
       partida na tabela do `<interfaces>`), agrupados sob um comentário
       `/* escala semântica de status — NÃO derivada da marca (D-08) */`, posicionados depois
       dos `--chart-*` e antes dos `--sidebar-*`.

    3. **`--sidebar-*`** — substituir os 8 valores literais por aliases em `:root` E em `.dark`,
       conforme a tabela do `<interfaces>`. Isso corrige de passagem o leftover azul-roxo
       `oklch(0.488 0.243 264.376)` do `.dark --sidebar-primary` (Pitfall 3).

    NÃO tocar em `--chart-1..5` (grep confirma que nenhum arquivo de `src/` os usa — ficam como
    estão). NÃO mover `:root`/`.dark` para dentro de `@layer base`. NÃO adicionar `ThemeProvider`.

    Se `check-contrast.cjs` reprovar algum par `--status-*-foreground`/`--status-*`, ajustar o
    lightness do foreground (escurecer no light, clarear no dark) mantendo o matiz — nunca
    afrouxar o limiar do script.
  </action>
  <verify>
    <automated>npm run check:contrast &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check:contrast` sai `0` — as 30 checagens (15 pares × `:root` e `.dark`) passam,
      sem nenhum `AUSENTE`.
    - `grep -c -- '--color-status-' src/app/globals.css` retorna `10`.
    - `grep -c -- '--status-danger-foreground:' src/app/globals.css` retorna `2` (`:root` + `.dark`).
    - `grep -c 'oklch(0.488 0.243 264.376)' src/app/globals.css` retorna `0` (leftover corrigido).
    - `grep -c -- '--sidebar-primary: var(--primary)' src/app/globals.css` retorna `2`.
    - `grep -c -- '--chart-1:' src/app/globals.css` retorna `2` (intocados).
    - `grep -c '@layer base' src/app/globals.css` retorna `1` e `:root` continua fora de `@layer`.
    - As 5 famílias continuam distinguíveis: os valores de matiz (H) de `--status-info`,
      `--status-warning`, `--status-success` e `--status-danger` em `:root` são 4 valores
      distintos, com distância ≥ 40 entre os vizinhos mais próximos.
    - `npx tsc --noEmit` sai `0`.
  </acceptance_criteria>
  <done>A escala --status-* existe nos 3 lugares (@theme inline, :root, .dark), os --sidebar-* seguem a paleta nova nos dois modos, e o gate de contraste está verde pela primeira vez.</done>
</task>

<task type="auto">
  <name>Task 2: Refatorar etapa-badge.tsx e csv-import-preview-table.tsx</name>
  <files>src/components/etapa-badge.tsx, src/components/csv-import-preview-table.tsx</files>
  <read_first>
    - `src/components/etapa-badge.tsx` — ARQUIVO-PADRÃO desta onda. `STAGE_CONFIG` nas linhas
      11–35 e o `<Badge>` com `style` inline.
    - `src/components/csv-import-preview-table.tsx` — `StatusBadges` (linhas ~63–108),
      o badge "Cortado em 500 caracteres" (~156) e o `accent-[#0D9488]` do checkbox nativo (~189).
    - `src/components/ui/badge.tsx` — as variantes disponíveis (`variant="outline"` é a usada).
    - `src/lib/utils.ts` — o helper `cn()` (tailwind-merge), idioma universal do repo.
    - `.planning/phases/19-marca-e-identidade-visual/19-PATTERNS.md` linhas 202–271.
  </read_first>
  <action>
    **A) `src/components/etapa-badge.tsx`** — separar rótulo de cor para não quebrar o export
    `STAGE_OPTIONS`, que é consumido pela toolbar da lista de leads:
    - Substituir `STAGE_CONFIG: Record<Stage, {label,bg,text}>` por dois mapas:
      `STAGE_LABEL: Record<Stage, string>` (Novo / Contatado / Negociação / Fechado / Perdido) e
      `STAGE_TOKEN: Record<Stage, string>` com as classes
      `bg-status-neutral text-status-neutral-foreground`, `bg-status-info text-status-info-foreground`,
      `bg-status-warning text-status-warning-foreground`, `bg-status-success text-status-success-foreground`,
      `bg-status-danger text-status-danger-foreground` — nesta ordem para
      novo/contatado/negociacao/fechado/perdido.
    - `STAGE_OPTIONS` passa a derivar de `Object.keys(STAGE_LABEL)`, mantendo o mesmo tipo de
      retorno `{ value, label }[]` e a mesma ordem. O export e a assinatura NÃO mudam.
    - O `<Badge>` perde o `style={{...}}` e ganha
      `className={cn("border-transparent", STAGE_TOKEN[stage])}` — importar `cn` de `@/lib/utils`.

    **B) `src/components/csv-import-preview-table.tsx`** — mesmo idioma:
    - Os 4 badges de `StatusBadges` trocam `style={{backgroundColor,color}}` por
      `className={cn("border-transparent", "<token>")}`: duplicado → `status-warning`,
      nicho novo → `status-info`, sem nicho → `status-neutral`, telefone inválido → `status-danger`.
    - O badge "Cortado em 500 caracteres" → `status-warning`.
    - O `<input type="checkbox">` nativo: `accent-[#0D9488]` → `accent-primary` (Pitfall 6 —
      `accent-primary` resolve para `accent-color: var(--color-primary)` no Tailwind v4;
      NÃO escrever `accent-[var(--primary)]`, que falha por faltar o prefixo `--color-`).
    - Qualquer resto de cor de marca/neutro no arquivo segue a tabela de-para do `<interfaces>`.

    Preservar comportamento: nenhuma mudança de lógica, de props, de nomes de export ou de
    ordem de renderização. `transition-colors` onde já existir permanece.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; node scripts/verify-no-hardcoded-colors.cjs 2>&amp;1 | grep -cE 'etapa-badge|csv-import-preview-table'</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-no-hardcoded-colors.cjs 2>&1 | grep -cE 'etapa-badge|csv-import-preview-table'`
      retorna `0` — os dois arquivos saíram da lista de findings.
    - `grep -c 'style={{' src/components/etapa-badge.tsx src/components/csv-import-preview-table.tsx`
      retorna `0` para ambos.
    - `grep -c 'STAGE_OPTIONS' src/components/etapa-badge.tsx` ≥ 1 e o arquivo continua
      exportando `STAGE_OPTIONS` com o shape `{ value, label }[]`.
    - `grep -c 'accent-primary' src/components/csv-import-preview-table.tsx` ≥ 1 e
      `grep -c 'accent-\[' src/components/csv-import-preview-table.tsx` retorna `0`.
    - Os 5 tokens de etapa aparecem exatamente uma vez cada em `etapa-badge.tsx`:
      `grep -c 'bg-status-' src/components/etapa-badge.tsx` retorna `5`.
    - `npx tsc --noEmit` sai `0` e `npm run lint` sai `0`.
    - Nenhum consumidor quebrou: `npx tsc --noEmit` cobre `lead-table-toolbar.tsx`, que importa
      `STAGE_OPTIONS`.
  </acceptance_criteria>
  <done>Etapa e flags de importação usam a escala --status-* por classe utilitária; zero style inline de cor; STAGE_OPTIONS intacto.</done>
</task>

<task type="auto">
  <name>Task 3: Refatorar followup-dashboard.tsx e relatorios/page.tsx</name>
  <files>src/components/followup-dashboard.tsx, src/app/relatorios/page.tsx</files>
  <read_first>
    - `src/components/followup-dashboard.tsx` — o arquivo de MAIOR densidade (12 ocorrências):
      o mapa `UrgencySection` com `headerBg`/`headerText` (linhas ~42–49 e ~92–117), os
      `style={{backgroundColor: section.headerBg}}` (~175, 179, 185), o `bg-[#F4F4F5]` do
      wrapper de seção (~171), os 2 botões "Novo lead" com
      `bg-[#0D9488] text-white hover:bg-[#0D9488]/90` (~127, ~157) e o card de lead
      `border-zinc-200 bg-white ... focus-visible:ring-[#0D9488]` (~219).
    - `src/app/relatorios/page.tsx` — a faixa de intervalo inválido `border-amber-200 bg-amber-50
      text-amber-900` (~97) e os outros 3 pontos de cor.
    - `src/components/etapa-badge.tsx` (já refatorado na Task 2) — o idioma de referência de
      mapa semântico → classe.
    - `src/components/ui/button.tsx` linha 11 — a variante `default` que torna o override
      de cor desnecessário.
    - `.planning/phases/19-marca-e-identidade-visual/19-PATTERNS.md` linhas 243–260 e 292–294.
  </read_first>
  <action>
    **A) `src/components/followup-dashboard.tsx`:**
    - Trocar os campos `headerBg`/`headerText` (string hex) do mapa de seções por um único
      campo `headerClass: string` com as classes do token:
      vencidos → `"bg-status-danger text-status-danger-foreground"`,
      hoje → `"bg-status-warning text-status-warning-foreground"`,
      próximos7Dias → `"bg-status-neutral text-status-neutral-foreground"`.
      Remover os 3 `style={{ backgroundColor: ... }}` e aplicar `headerClass` via `cn()`.
    - `dateClassName`: `text-[#B91C1C]` → `text-status-danger-foreground`;
      `text-[#B45309]` → `text-status-warning-foreground`; `text-muted-foreground` fica como está.
    - Wrapper de seção `bg-[#F4F4F5]` → `bg-muted`.
    - Os 2 botões "Novo lead": **deletar o `className` de cor inteiro**, deixando `<Button>` puro
      (a variante `default` já é `bg-primary text-primary-foreground hover:bg-primary/80`).
      Props `onClick`/`size`/`variant` que existirem permanecem.
    - Card de lead: `border-zinc-200` → `border`, `bg-white` → `bg-card`,
      `focus-visible:ring-[#0D9488]` → `focus-visible:ring-ring`.
    - Preservar `groupByUrgency`/`buildDashboardItems` e a ordem dos buckets — este plano não
      toca em lógica, só em classes.

    **B) `src/app/relatorios/page.tsx`:**
    - Faixa de intervalo inválido: `border-amber-200 bg-amber-50 text-amber-900` →
      `border-status-warning bg-status-warning text-status-warning-foreground`. A semântica de
      aviso se mantém e passa a responder ao `.dark`. Não alterar a condição `customInvalido`
      que decide exibir a faixa.
    - Os outros 3 pontos de cor do arquivo seguem a tabela de-para do `<interfaces>`.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; node scripts/verify-no-hardcoded-colors.cjs 2>&amp;1 | grep -cE 'followup-dashboard|relatorios'</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-no-hardcoded-colors.cjs 2>&1 | grep -cE 'followup-dashboard|relatorios/page'`
      retorna `0`.
    - `grep -c 'style={{' src/components/followup-dashboard.tsx` retorna `0`.
    - `grep -c 'headerBg\|headerText' src/components/followup-dashboard.tsx` retorna `0` e
      `grep -c 'headerClass' src/components/followup-dashboard.tsx` ≥ 4 (definição + 3 seções).
    - `grep -c 'bg-\[#0D9488\]' src/components/followup-dashboard.tsx` retorna `0` e os 2
      `<Button>` de "Novo lead" não têm nenhuma classe de cor.
    - `grep -c 'amber' src/app/relatorios/page.tsx` retorna `0`.
    - `grep -c 'transition-all' src/components/followup-dashboard.tsx src/app/relatorios/page.tsx`
      retorna `0` (não regredir `transition-colors`).
    - `npx tsc --noEmit` sai `0`, `npm run lint` sai `0`.
    - `npm run test:group-by-urgency` sai `0` (a régua de urgência não foi tocada).
    - `rm -rf .next && npm run build` sai `0` e gera 13+ páginas.
  </acceptance_criteria>
  <done>Dashboard de follow-up e relatórios usam tokens semânticos; os botões primários voltaram ao <Button> default; zero hex/amber/zinc nesses arquivos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `globals.css` → todo o app | Fonte única de verdade de cor. Um valor errado aqui degrada a legibilidade de todas as telas simultaneamente. |
| dados do lead → classe CSS | Os mapas `stage → token` e `urgência → token` são indexados por valores vindos do banco. Chave inesperada não pode virar classe arbitrária. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Denial of usability | escala `--status-*` em `:root` e `.dark` | mitigate | Task 1: `check-contrast.cjs` exige AA (≥4.5) nos 5 pares `-foreground`/base em light E dark; ajuste é no lightness, nunca no limiar do gate |
| T-19-08 | Tampering | `STAGE_TOKEN[stage]` / `headerClass` indexados por valor do banco | mitigate | Tasks 2 e 3: mapas são `Record<Stage, string>` fechados com literais constantes — `Stage` é união tipada e `tsc --noEmit` prova exaustividade; nenhuma classe é construída por concatenação de string vinda de dado |
| T-19-09 | Denial of usability | `.dark --sidebar-primary` com leftover de outra paleta | mitigate | Task 1: os 8 `--sidebar-*` viram aliases dos tokens core nos dois blocos; aceite exige `grep -c 'oklch(0.488 0.243 264.376)'` = 0 |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote instalado (19-RESEARCH §Package Legitimacy Audit: "Audit N/A"). Plano puramente CSS + classes utilitárias. |
</threat_model>

<verification>
1. `npm run check:contrast` → exit 0 (30/30 checagens).
2. `node scripts/verify-no-hardcoded-colors.cjs` → ainda exit 1 (RED até o plano 19-05), mas **sem nenhuma linha** de `etapa-badge`, `csv-import-preview-table`, `followup-dashboard` ou `relatorios/page`.
3. `npx tsc --noEmit` → exit 0.
4. `npm run lint` → exit 0.
5. `npm run test:group-by-urgency` → exit 0.
6. `rm -rf .next && npm run build` → exit 0, 13+ páginas.
7. `grep -c 'style={{' src/components/etapa-badge.tsx src/components/followup-dashboard.tsx src/components/csv-import-preview-table.tsx` → 0 em todos.
</verification>

<success_criteria>
- A escala `--status-*` existe nos 3 lugares (`@theme inline`, `:root`, `.dark`) com 5 famílias mutuamente distinguíveis e todas passando AA nos dois modos.
- Os `--sidebar-*` seguem a paleta nova em light e dark; o leftover azul-roxo do default shadcn sumiu.
- Etapa, urgência e flags de CSV compartilham as mesmas 5 cores semânticas via classe utilitária.
- Zero `style` inline de cor e zero hex nos 4 arquivos do plano.
- `STAGE_OPTIONS` e a lógica de agrupamento por urgência continuam com o mesmo contrato.
- Build, tsc, lint e o harness de urgência verdes.
</success_criteria>

<output>
Create `.planning/phases/19-marca-e-identidade-visual/19-03-SUMMARY.md` when done.
Registrar os valores OKLCH finais dos 10 tokens `--status-*` em cada bloco e a menor razão de
contraste medida — os planos 19-04 e 19-05 reusam esses tokens sem redefini-los.
</output>
