---
phase: 19-marca-e-identidade-visual
plan: 05
type: execute
wave: 5
depends_on: ["19-04"]
files_modified:
  - src/components/app-sidebar.tsx
  - src/app/layout.tsx
  - src/components/template-list.tsx
  - src/components/template-form-dialog.tsx
  - src/components/nicho-manager.tsx
  - src/components/configuracoes-form.tsx
  - src/components/periodo-selector.tsx
  - src/components/motivo-perda-manager.tsx
  - src/components/motivo-perda-combobox.tsx
  - src/components/motivo-perda-dialog.tsx
  - src/components/tarefa-card.tsx
  - src/components/tarefa-form-dialog.tsx
  - src/components/ui/dialog.tsx
autonomous: true
requirements: [BRAND-02, BRAND-03]

must_haves:
  truths:
    - "O nome visível do produto em toda a UI é SOLO, não 'CRM de Leads' nem 'CRM LEADS'"
    - "O ícone de marca no sidebar é a letra S sobre a cor primária"
    - "A aba do navegador mostra SOLO"
    - "O sidebar responde à paleta escolhida e ao .dark forçado, sem cor cravada"
    - "Nenhuma cor hardcoded resta em src/ fora da allowlist documentada"
  artifacts:
    - path: "src/components/app-sidebar.tsx"
      provides: "chrome de navegação em tokens de sidebar + marca SOLO + ícone S"
      contains: "SOLO"
    - path: "src/app/layout.tsx"
      provides: "metadata SOLO e body sem cor cravada"
      contains: "SOLO"
    - path: "src/components/ui/dialog.tsx"
      provides: "overlay do dialog em token, respondendo ao dark"
  key_links:
    - from: "src/components/app-sidebar.tsx"
      to: "src/app/globals.css"
      via: "utilitários bg-sidebar / bg-sidebar-accent / text-sidebar-foreground"
      pattern: "bg-sidebar"
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "@layer base body { @apply bg-background text-foreground } assume a cor do body"
      pattern: "min-h-full flex"
---

<objective>
Fechar o refactor de cor nos 13 arquivos restantes e renomear as duas superfícies visíveis do
produto para SOLO.

Purpose: `app-sidebar.tsx` é o maior nó visual da fase — é ele que hoje crava `bg-[#F4F4F5]`,
`bg-[#0D9488]` e a escala `zinc-*`, e é ele que carrega o nome antigo "CRM LEADS" e o ícone
placeholder "I". Fazer cor e rename no mesmo passo evita reabrir o arquivo. `layout.tsx` fecha o
`bg-white` cravado do `<body>` (D-15: o `@layer base` já aplica `bg-background`) e o
`title`/`description` (D-04). Ao fim deste plano o grep-guard `verify:brand` fica VERDE pela
primeira vez.

Output: 13 arquivos em tokens, nome SOLO nas 2 superfícies visíveis, ícone "S", e
`npm run verify:brand` exit 0.

Fora de escopo (D-05): renomear a pasta do repositório (`crm-leads`), caminhos de CI, imports
internos ou strings de path. O campo `name` do `package.json` é do plano 19-06.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-marca-e-identidade-visual/19-CONTEXT.md
@.planning/phases/19-marca-e-identidade-visual/19-PATTERNS.md
@.planning/phases/19-marca-e-identidade-visual/19-04-SUMMARY.md
@brand.md

<interfaces>
<!-- Tabela de-para autoritativa (19-PATTERNS linhas 342-356). O executor aplica, não redecide. -->

| Padrão atual | Token destino |
|---|---|
| `bg-[#0D9488] text-white hover:bg-[#0D9488]/90` em `<Button>` | **deletar o className de cor** — `<Button>` default já é `bg-primary text-primary-foreground hover:bg-primary/80` (`src/components/ui/button.tsx:11`) |
| `bg-[#F4F4F5]` no `<aside>` do sidebar | `bg-sidebar` |
| `bg-[#F4F4F5]` em superfície interna (não-sidebar) | `bg-muted` |
| `bg-[#0D9488] text-white` no quadrado do ícone de marca | `bg-primary text-primary-foreground` |
| `bg-[#0D9488]/10 font-semibold text-[#0D9488]` no item ativo do nav | `bg-sidebar-accent font-semibold text-sidebar-accent-foreground` |
| `text-zinc-700 hover:bg-zinc-200/60 hover:text-[#0D9488]` no item inativo do nav | `text-sidebar-foreground hover:bg-sidebar-accent hover:text-sidebar-accent-foreground` |
| `text-zinc-400` / `text-zinc-500` | `text-muted-foreground` |
| `text-zinc-700` (fora do sidebar) | `text-foreground` |
| `border-zinc-200` / `border-zinc-300` | `border` — em campo de formulário, `border-input` |
| `bg-white` (card) | `bg-card` |
| `bg-white` no `<body>` de `layout.tsx` | **remover a classe** — `@layer base body { @apply bg-background text-foreground }` já resolve (D-15) |
| `text-[#0D9488]` / `hover:text-[#0D9488]` | `text-primary` / `hover:text-primary` |
| `text-[#DC2626]` | `text-destructive` |
| `ring-[#0D9488]` / `focus-visible:ring-[#0D9488]/50` / `focus-visible:border-[#0D9488]` | `ring-ring` / `focus-visible:ring-ring/50` / `focus-visible:border-ring` |
| `style={{ backgroundColor: "rgba(13, 148, 136, 0.1)", color: "#0D9488" }}` (`template-list.tsx:52`) | `className="bg-primary/10 text-primary"` |
| `bg-black/10` do overlay (`ui/dialog.tsx:34`) | `bg-foreground/10` — decisão travada: converter, NÃO allowlistar (assim o overlay também responde ao `.dark`) |

**Rename — superfícies confirmadas por grep (19-RESEARCH linhas 430–439):**
```
src/app/layout.tsx:18              title: "CRM de Leads"   → "SOLO"
src/app/layout.tsx:19              description             → reescrever mantendo o sentido
src/components/app-sidebar.tsx:42  texto do ícone "I"      → "S"          (D-03)
src/components/app-sidebar.tsx:45  "CRM LEADS"             → "SOLO"       (D-04)
```
`git grep -nE "CRM de Leads|CRM LEADS|CRM Leads" -- src/` retorna EXATAMENTE esses 2 hits de
nome. Nada em `public/`. Nenhuma dessas strings está persistida em `data/crm.db`
(19-RESEARCH §Runtime State Inventory) — nenhuma migração de dado é necessária.

**Preservar em `app-sidebar.tsx`:** `NAV_ITEMS` (10 rotas), `usePathname`, `cn()`, as larguras
fixas `w-[240px]` e `text-[11px]`, `uppercase tracking-[0.08em]`, `transition-colors`.
O header "SOLO" precisa continuar em UMA linha (Pitfall 2).

**Anti-padrões travados:** nunca `transition-all`; não recriar botão primário com classe; não
adicionar `ThemeProvider`/toggle (D-16); zero mudança de lógica, props ou handlers; nenhuma
supressão `eslint-disable` removida ou acrescentada.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refatorar e renomear o sidebar e o layout raiz</name>
  <files>src/components/app-sidebar.tsx, src/app/layout.tsx</files>
  <read_first>
    - `src/components/app-sidebar.tsx` (77 linhas) — `NAV_ITEMS` (linhas 19–30), o `<aside>`
      `bg-[#F4F4F5]` (~36), o quadrado do ícone `bg-[#0D9488] text-white` com o texto "I" (~40–43),
      o `<span>` "CRM LEADS" `text-zinc-500` (~44–46), o rótulo "Principal" `text-zinc-400`,
      e o `cn()` do item de nav com os ramos `isActive` / inativo (~60–67).
    - `src/app/layout.tsx` (39 linhas) — `metadata` (18–19), `<html>` com as variáveis de fonte
      (~30, já atualizado no plano 19-02) e `<body className="min-h-full flex bg-white">` (~32).
    - `src/app/globals.css` — o bloco `@layer base` (~120–130), que já aplica
      `body { @apply bg-background text-foreground }`, e os 8 `--sidebar-*` aliasados no plano 19-03.
    - `brand.md` — a seção Nome/racional, para manter a `description` do metadata coerente com
      o positioning registrado.
    - `.planning/phases/19-marca-e-identidade-visual/19-PATTERNS.md` linhas 314–338.
  </read_first>
  <action>
    **A) `src/components/app-sidebar.tsx`** — as 6 ocorrências de cor pela tabela do
    `<interfaces>`, mais o rename:
    - `<aside>`: `bg-[#F4F4F5]` → `bg-sidebar`. Manter `flex h-full w-[240px] shrink-0 flex-col`.
    - Quadrado do ícone: `bg-[#0D9488] text-white` → `bg-primary text-primary-foreground`; o
      conteúdo textual `I` → `S` (D-03). Manter `aria-hidden="true"`, `rounded-md`, `text-[13px]`
      e `font-bold`.
    - Header: `CRM LEADS` → `SOLO` (D-04); `text-zinc-500` → `text-muted-foreground`. Manter
      `text-[11px] font-bold uppercase tracking-[0.08em]` — "SOLO" é mais curto que "CRM LEADS",
      então não há risco de quebra em 2 linhas.
    - Rótulo "Principal": `text-zinc-400` → `text-muted-foreground`.
    - Item de nav ativo: `bg-[#0D9488]/10 font-semibold text-[#0D9488]` →
      `bg-sidebar-accent font-semibold text-sidebar-accent-foreground`.
    - Item de nav inativo: `text-zinc-700 hover:bg-zinc-200/60 hover:text-[#0D9488]` →
      `text-sidebar-foreground hover:bg-sidebar-accent hover:text-sidebar-accent-foreground`.
    - `NAV_ITEMS`, `usePathname`, `cn()` e `transition-colors` intocados.

    **B) `src/app/layout.tsx`:**
    - `metadata.title`: `"CRM de Leads"` → `"SOLO"`.
    - `metadata.description`: reescrever mantendo o sentido e o positioning do `brand.md` —
      "CRM pra quem trabalha sozinho: organiza leads, pipeline e follow-ups." Sem `openGraph`,
      sem `metadataBase` (deferidos — 19-CONTEXT §Deferred Ideas).
    - `<body className="min-h-full flex bg-white">` → remover a classe `bg-white`, deixando
      `min-h-full flex` (D-15).
    - NÃO tocar nas fontes nem no `className` do `<html>` — já foram resolvidos no plano 19-02.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; git grep -cE "CRM de Leads|CRM LEADS|CRM Leads" -- src/ ; test $? -eq 1 &amp;&amp; echo "nome antigo ausente de src/"</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -nE "CRM de Leads|CRM LEADS|CRM Leads" -- src/` não retorna nenhuma linha (exit 1).
    - `grep -c 'SOLO' src/components/app-sidebar.tsx` ≥ 1 e `grep -c 'SOLO' src/app/layout.tsx` ≥ 1.
    - O quadrado do ícone contém `S` e não `I`:
      `grep -A3 'aria-hidden="true"' src/components/app-sidebar.tsx | grep -qE '^\s*S\s*$'` sai `0`.
    - `grep -c 'bg-sidebar' src/components/app-sidebar.tsx` ≥ 1 e
      `grep -c 'sidebar-accent' src/components/app-sidebar.tsx` ≥ 2 (ramo ativo + hover do inativo).
    - `grep -c 'bg-white' src/app/layout.tsx` retorna `0` e `grep -c 'min-h-full flex' src/app/layout.tsx` ≥ 1.
    - `grep -c 'w-\[240px\]' src/components/app-sidebar.tsx` ≥ 1 e
      `grep -c 'tracking-\[0.08em\]' src/components/app-sidebar.tsx` ≥ 1 (métricas preservadas).
    - `grep -c 'NAV_ITEMS' src/components/app-sidebar.tsx` ≥ 2 e as 10 rotas continuam presentes:
      `grep -cE '"/(leads|importar|pipeline|relatorios|templates|nichos|motivos-perda|lixeira|configuracoes)"' src/components/app-sidebar.tsx` retorna `9` (+ a raiz `/`).
    - `grep -c 'transition-all' src/components/app-sidebar.tsx` retorna `0`.
    - `npx tsc --noEmit` sai `0` e `npm run lint` sai `0`.
  </acceptance_criteria>
  <done>O sidebar consome os tokens --sidebar-*, o produto se chama SOLO na aba do navegador e no header, o ícone é "S", e o body não tem mais cor cravada.</done>
</task>

<task type="auto">
  <name>Task 2: Refatorar templates, nichos, configurações e seletor de período</name>
  <files>src/components/template-list.tsx, src/components/template-form-dialog.tsx, src/components/nicho-manager.tsx, src/components/configuracoes-form.tsx, src/components/periodo-selector.tsx</files>
  <read_first>
    - `src/components/template-list.tsx` — 8 ocorrências; mostra os 3 idiomas juntos nas linhas
      ~45 (`hover:bg-[#F4F4F5]`), ~52 (`style={{ backgroundColor: "rgba(13, 148, 136, 0.1)",
      color: "#0D9488" }}`) e ~62 (`hover:text-[#0D9488]`).
    - `src/components/nicho-manager.tsx` — 7 ocorrências; contém o combobox criável e o
      soft-delete de nicho (quick task `260725-lai`) — lógica intocável.
    - `src/components/configuracoes-form.tsx` — 4 ocorrências; contém `noValidate` no `<form>`
      (quick task `260801-ij4`), a lista dinâmica de intervalos da sequência e um
      `eslint-disable-next-line react-hooks/refs` documentado.
    - `src/components/template-form-dialog.tsx` — 1 ocorrência; é o analog mandatado do padrão
      `form.handleSubmit` do repo, com `eslint-disable` documentado.
    - `src/components/periodo-selector.tsx` — 1 ocorrência; contém o modo custom e o
      `navegarCustom` que só roda em event handlers (T-14-07) — não tocar.
    - `src/components/ui/button.tsx` linha 11 e `src/lib/utils.ts` (`cn`).
  </read_first>
  <action>
    Aplicar a tabela de-para do bloco `<interfaces>` nos 5 arquivos.

    Pontos específicos:
    - `template-list.tsx`: o `style={{...}}` do `<Badge>` some e vira
      `className="bg-primary/10 text-primary"` (mantendo qualquer `border-transparent` /
      `variant="outline"` já presente); `hover:bg-[#F4F4F5]` → `hover:bg-muted`;
      `hover:text-[#0D9488]` → `hover:text-primary`.
    - `configuracoes-form.tsx`: campos usam `border-input` + `focus-visible:ring-ring`;
      preservar `noValidate`, a lista dinâmica de ids em `useRef` e o `eslint-disable`.
    - `nicho-manager.tsx` e `template-form-dialog.tsx`: botões primários perdem o `className`
      de cor; ícones/textos de exclusão viram `text-destructive`.
    - `periodo-selector.tsx`: seguir a tabela; não alterar `items` do `<Select>` (quick task
      `260828-flg`) nem `navegarCustom`.
    - Zero mudança de lógica, props, efeitos ou supressões de lint.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; node scripts/verify-no-hardcoded-colors.cjs 2>&amp;1 | grep -cE 'template-list|template-form-dialog|nicho-manager|configuracoes-form|periodo-selector'</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-no-hardcoded-colors.cjs 2>&1 | grep -cE 'template-list|template-form-dialog|nicho-manager|configuracoes-form|periodo-selector'` retorna `0`.
    - `grep -c 'style={{' src/components/template-list.tsx` retorna `0` e
      `grep -c 'bg-primary/10' src/components/template-list.tsx` ≥ 1.
    - `grep -c 'noValidate' src/components/configuracoes-form.tsx` ≥ 1.
    - `git diff -U0 -- src/components/configuracoes-form.tsx src/components/template-form-dialog.tsx | grep -c 'eslint-disable'` retorna `0`.
    - `grep -c 'items=' src/components/periodo-selector.tsx` ≥ 1 (o fix do rótulo do Select preservado).
    - `git diff -U0` destes 5 arquivos contém apenas linhas de `className`/classe — nenhum import
      novo, nenhuma assinatura, hook ou handler alterado.
    - `npx tsc --noEmit` sai `0` e `npm run lint` sai `0`.
  </acceptance_criteria>
  <done>Templates, nichos, configurações e seletor de período em tokens, com todos os fixes de quick task anteriores preservados.</done>
</task>

<task type="auto">
  <name>Task 3: Refatorar motivos de perda, tarefas e o overlay do dialog — grep-guard verde</name>
  <files>src/components/motivo-perda-manager.tsx, src/components/motivo-perda-combobox.tsx, src/components/motivo-perda-dialog.tsx, src/components/tarefa-card.tsx, src/components/tarefa-form-dialog.tsx, src/components/ui/dialog.tsx</files>
  <read_first>
    - `src/components/motivo-perda-manager.tsx` — 7 ocorrências.
    - `src/components/motivo-perda-combobox.tsx` — 3 ocorrências; é o combobox criável que emite
      string vazia no input oculto (contrato do `z.preprocess` da Fase 11) — não tocar.
    - `src/components/motivo-perda-dialog.tsx` — 1 ocorrência; bloqueia dismiss por Esc/clique-fora
      e tem dedup de fila (quick task `260828-gna`) — não tocar.
    - `src/components/tarefa-card.tsx` — 2 ocorrências.
    - `src/components/tarefa-form-dialog.tsx` — 2 ocorrências.
    - `src/components/ui/dialog.tsx` — o overlay `bg-black/10` (~34) e o `DialogTitle` com
      `className="font-heading ..."` (~125), que consome a decisão D-18 do plano 19-02.
    - `src/lib/csv-encoding.ts` linha ~14 — o `#840` num comentário de mojibake, único item da
      ALLOWLIST do grep-guard (não é cor; não editar).
    - `scripts/verify-no-hardcoded-colors.cjs` — a lista de padrões que precisa ficar vazia.
  </read_first>
  <action>
    Aplicar a tabela de-para do bloco `<interfaces>` nos 6 arquivos.

    Pontos específicos:
    - `ui/dialog.tsx`: o overlay `bg-black/10` vira `bg-foreground/10` — decisão travada, o
      overlay passa a responder ao `.dark` em vez de ser sempre preto, e o arquivo NÃO entra na
      allowlist do guard. NÃO alterar o `font-heading` do `DialogTitle` (a decisão D-18 já foi
      materializada no `@theme inline` pelo plano 19-02) nem a API dos primitivos Base UI.
    - `motivo-perda-*` e `tarefa-*`: botões primários perdem o `className` de cor; ações de
      exclusão viram `text-destructive`; superfícies `bg-white` viram `bg-card`; neutros seguem
      a tabela.
    - Zero mudança de lógica, props, efeitos, dedup de fila ou supressões de lint.

    **Fechamento do refactor:** depois destas edições, `src/` não pode conter mais nenhum
    `#hex`, `-[#...]`, escala neutra Tailwind, escala de cor nomeada ou `style` inline de cor
    fora da allowlist (`src/lib/csv-encoding.ts`). Se o guard ainda apontar algo, corrigir aqui —
    é a última onda de refactor da fase.
  </action>
  <verify>
    <automated>npm run verify:brand &amp;&amp; npm run check:contrast &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; rm -rf .next &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify:brand` sai **`0` pela primeira vez** — zero cor hardcoded e zero nome antigo
      em `src/`. (O único achado remanescente possível é `package.json` `"name": "crm-leads"`, que
      é fechado no plano 19-06 — se o script ainda reportar isso, ele sai 1 e esse é o ÚNICO
      finding aceitável neste ponto; registrar no SUMMARY.)
    - `grep -c 'bg-foreground/10' src/components/ui/dialog.tsx` ≥ 1 e
      `grep -c 'bg-black' src/components/ui/dialog.tsx` retorna `0`.
    - `grep -c 'font-heading' src/components/ui/dialog.tsx` ≥ 1 (inalterado).
    - `git grep -nE '#[0-9a-fA-F]{3,8}\b' -- 'src/**/*.tsx'` não retorna nenhuma linha.
    - `git grep -nE '\b(bg|text|border|ring)-(zinc|slate|gray|neutral|white|black)' -- 'src/**/*.tsx'` não retorna nenhuma linha.
    - `npm run check:contrast` sai `0`.
    - `npx tsc --noEmit` sai `0`, `npm run lint` sai `0`.
    - `rm -rf .next && npm run build` sai `0` com 13+ páginas geradas.
    - Os harnesses de regressão continuam verdes: `npm run test:lead-actions`,
      `npm run test:tarefa-actions`, `npm run test:motivo-perda-actions`,
      `npm run guard:no-hard-delete`, `npm run verify:schema` — todos exit `0`.
  </acceptance_criteria>
  <done>O refactor de cor está completo em src/; o overlay do dialog responde ao dark; o grep-guard de marca fica verde (salvo o package.json, fechado no 19-06); build e harnesses verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `layout.tsx` metadata → HTML servido | `title`/`description` são literais no bundle do servidor; nenhum valor vem de input do usuário. |
| chrome de navegação → todas as rotas | O sidebar é renderizado em toda página; um token errado aqui degrada a navegação inteira. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-11 | Tampering | rename das superfícies visíveis (`layout.tsx`, `app-sidebar.tsx`) | mitigate | Escopo travado por D-05: proibido renomear pasta do repo, imports, paths ou CI. Nenhuma string do produto está persistida em `data/crm.db` (19-RESEARCH §Runtime State Inventory), então não há migração de dado nem risco de corromper registro |
| T-19-05 | Denial of usability | `bg-sidebar`/`sidebar-accent` e overlay `bg-foreground/10` | mitigate | `check-contrast.cjs` valida `sidebar-foreground/sidebar` e `sidebar-accent-foreground/sidebar-accent` em light e dark; overlay convertido para token responde ao `.dark` em vez de escurecer duas vezes |
| T-19-10 | Tampering | refactor mecânico sobre arquivos com fixes de quick task (`configuracoes-form` noValidate, `periodo-selector` items, `motivo-perda-dialog` dedup) | mitigate | Aceites conferem explicitamente `noValidate`, `items=`, supressões de lint e ausência de linhas de lógica no `git diff -U0` |
| T-19-12 | Information Disclosure | `metadata.description` exposta publicamente no HTML | accept | Texto de marketing sem PII; o app é solo, local e não indexado. `openGraph`/`metadataBase` deliberadamente não adicionados |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote instalado (19-RESEARCH §Package Legitimacy Audit: "Audit N/A"). Plano só troca strings de classe e literais de texto. |
</threat_model>

<verification>
1. `npm run verify:brand` → exit 0 (ou exit 1 com o ÚNICO finding sendo `package.json` `"name": "crm-leads"`, fechado no plano 19-06).
2. `git grep -nE "CRM de Leads|CRM LEADS|CRM Leads" -- src/` → sem resultados.
3. `npm run check:contrast` → exit 0.
4. `npx tsc --noEmit` → exit 0.
5. `npm run lint` → exit 0.
6. `rm -rf .next && npm run build` → exit 0, 13+ páginas.
7. `npm run test:lead-actions && npm run test:tarefa-actions && npm run test:motivo-perda-actions && npm run guard:no-hard-delete && npm run verify:schema` → todos exit 0.
8. `grep -c 'bg-white' src/app/layout.tsx` → 0.
</verification>

<success_criteria>
- O produto se chama SOLO na aba do navegador e no header do sidebar; o ícone é "S".
- O sidebar consome `--sidebar-*` e o `<body>` não tem cor cravada — dark mode forçado fica internamente consistente (D-15/D-17).
- Zero cor hardcoded em `src/` fora da allowlist documentada (`src/lib/csv-encoding.ts`).
- O overlay do dialog responde ao `.dark`.
- Nenhum fix de quick task anterior foi revertido; nenhuma lógica mudou.
- Build, tsc, lint, contraste e todos os harnesses de regressão verdes.
</success_criteria>

<output>
Create `.planning/phases/19-marca-e-identidade-visual/19-05-SUMMARY.md` when done.
Registrar o estado final do `verify:brand` (verde, ou o único finding de `package.json`) e a
lista de arquivos com `git diff --stat` da fase de refactor completa (planos 19-03/04/05).
</output>
