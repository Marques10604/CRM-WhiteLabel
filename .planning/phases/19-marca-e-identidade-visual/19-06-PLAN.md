---
phase: 19-marca-e-identidade-visual
plan: 06
type: execute
wave: 6
depends_on: ["19-05"]
files_modified:
  - package.json
  - src/app/icon.svg
  - src/app/favicon.ico
  - .planning/phases/19-marca-e-identidade-visual/19-HUMAN-UAT.md
autonomous: true
requirements: [BRAND-01, BRAND-02, BRAND-03]

must_haves:
  truths:
    - "O nome antigo 'CRM de Leads'/'crm-leads' não aparece em nenhuma superfície visível nem no package.json"
    - "A aba do navegador mostra o ícone da marca (S), não o placeholder do Next"
    - "Todos os sensores automatizados da fase estão verdes ao mesmo tempo"
    - "A não-regressão visual das 10 rotas está registrada — ao vivo se houver RAM, senão por code+data (precedente Fase 18)"
  artifacts:
    - path: "package.json"
      provides: "identidade do pacote alinhada ao nome do produto"
      contains: "\"name\": \"solo\""
    - path: "src/app/icon.svg"
      provides: "favicon da marca — letra S sobre a cor primária"
      min_lines: 3
    - path: ".planning/phases/19-marca-e-identidade-visual/19-HUMAN-UAT.md"
      provides: "checklist D-19 executado ou registrado como code+data"
      contains: "status:"
  key_links:
    - from: "src/app/icon.svg"
      to: "Next.js App Router"
      via: "convenção de arquivo icon.svg em src/app/ gera <link rel=icon> automaticamente"
      pattern: "svg"
    - from: "package.json"
      to: "scripts/verify-no-hardcoded-colors.cjs"
      via: "PARTE B do guard checa o campo name e as strings de nome antigo"
      pattern: "verify:brand"
---

<objective>
Fechar a Fase 19: alinhar o `package.json` ao nome do produto, dar um favicon próprio ao SOLO,
rodar o portão completo de sensores e registrar a não-regressão visual das 10 rotas.

Purpose: BRAND-03 exige que o nome antigo suma de toda superfície e que nenhuma tela tenha
regredido visualmente. Os planos 19-02 a 19-05 entregaram paleta, tipografia, tokens e rename de
UI; falta o último literal (`package.json`), o "cara de produto" que é o favicon (Open Question 1
do RESEARCH — favicon SIM, Open Graph image DIFERIDA), e a prova consolidada.

Output: `"name": "solo"`, `src/app/icon.svg`, os 3 sensores da fase + build + lint + tsc + os
harnesses de regressão todos verdes no mesmo commit, e `19-HUMAN-UAT.md` com resultado.

**A verificação visual ao vivo NÃO é bloqueante.** O host tem 4GB e `npm run dev` + Chrome +
sessão Claude congelou o renderer na Fase 18. O gate real desta fase são os sensores
automatizados; a inspeção visual pura é registrada como `code+data` e diferida se a RAM não
permitir — precedente explícito da Fase 18 (`18-VERIFICATION.md` promovido a `passed` sem
navegador ao vivo).
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-marca-e-identidade-visual/19-CONTEXT.md
@.planning/phases/19-marca-e-identidade-visual/19-VALIDATION.md
@.planning/phases/19-marca-e-identidade-visual/19-05-SUMMARY.md
@.planning/phases/18-auditoria-retroativa-no-navegador/18-VERIFICATION.md
@brand.md

<interfaces>
<!-- Escopo do rename já executado nos planos anteriores; aqui fecha só o que sobrou. -->

Superfícies de rename da fase (19-RESEARCH linhas 430–439):
```
src/app/layout.tsx:18      title "CRM de Leads" → "SOLO"      ✔ plano 19-05
src/app/layout.tsx:19      description                        ✔ plano 19-05
src/components/app-sidebar.tsx:42  ícone "I" → "S"            ✔ plano 19-05
src/components/app-sidebar.tsx:45  "CRM LEADS" → "SOLO"       ✔ plano 19-05
package.json:2             "name": "crm-leads" → "solo"       ← ESTE PLANO
README.md                  criado com "# SOLO"                ✔ plano 19-02
src/app/favicon.ico        placeholder do Next → icon.svg "S" ← ESTE PLANO
```

**NÃO renomear (D-05):** a pasta do repositório continua `crm-leads`; caminhos de CI, imports
internos e strings de path ficam como estão. `package-lock.json` regenera o campo `name` na
próxima instalação — cosmético, o pacote é `private: true`.

**Convenção de ícone do Next.js App Router:** um arquivo `icon.svg` em `src/app/` gera
automaticamente o `<link rel="icon">` no `<head>`. Se `favicon.ico` também existir em `src/app/`,
o navegador tende a servir o `.ico` de `/favicon.ico` — por isso o `.ico` placeholder é removido.

**Portão completo de sensores da fase:**
```
npx tsc --noEmit
npm run lint
rm -rf .next && npm run build          # Turbopack; 13+ páginas
npm run verify:brand                   # grep-guard de cor + nome antigo
npm run verify:brand-md                # brand.md com Paleta/Tipografia/Tom-Voz/Nome+colisão
npm run check:contrast                 # WCAG AA em :root e .dark (15 pares × 2 blocos)
npm run guard:no-hard-delete           # regressão pré-existente
npm run verify:schema                  # regressão pré-existente
npm run test:lead-actions
npm run test:tarefa-actions
npm run test:motivo-perda-actions
npm run test:group-by-urgency
```
O `rm -rf .next` é obrigatório: o cache do Next fica stale depois da troca de fonte e de tokens
(19-RESEARCH §Runtime State Inventory) e um build incremental pode mascarar CSS/fonte fantasma.

**Definição operacional de regressão (D-19) — para a Task 3:**
BLOQUEIA: layout quebrado (elementos sobrepostos ou empurrados pra fora), texto ilegível,
contraste medido < WCAG AA, dark mode bugado quando `.dark` é forçado (texto invisível,
superfície branca cravada), elemento sumido/cortado/truncado errado, header do sidebar
quebrando em 2 linhas, colunas do pipeline com scroll horizontal, badge de etapa quebrando linha.
NUNCA BLOQUEIA: "a cor mudou", "não gostei da combinação", "o teal sumiu", "ficou escuro demais",
qualquer julgamento estético. Mudança de cor é o objetivo declarado da fase.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Renomear o pacote para solo e criar o favicon da marca</name>
  <files>package.json, src/app/icon.svg, src/app/favicon.ico</files>
  <read_first>
    - `package.json` linhas 1–30 — o campo `name` (linha 2) e o bloco `scripts` (com as 3
      entradas adicionadas no plano 19-01), para editar só o `name` sem reescrever o arquivo.
    - `src/app/globals.css` — os valores finais de `--primary` e `--primary-foreground` em
      `:root`, que o SVG precisa reproduzir como cor literal.
    - `brand.md` §Nome e §Paleta — o ícone "S" (D-03) e os valores da paleta.
    - `scripts/verify-no-hardcoded-colors.cjs` PARTE B — a checagem que este rename fecha.
    - `.planning/phases/19-marca-e-identidade-visual/19-RESEARCH.md` linhas 465–470
      (Open Question 1: favicon SIM, Open Graph image DIFERIDA).
  </read_first>
  <action>
    **A) `package.json`:** trocar `"name": "crm-leads"` por `"name": "solo"` na linha 2.
    Edição de UMA linha — não reformatar o arquivo, não tocar em `version`, `private`,
    `scripts` ou nas dependências. Não renomear a pasta do repo nem nada em CI (D-05).

    **B) `src/app/icon.svg`:** criar um SVG quadrado (`viewBox="0 0 32 32"`, `width`/`height`
    32) com um retângulo de cantos arredondados (`rx` ~6, coerente com o `--radius` do tema)
    preenchido com a cor primária da paleta escolhida, e a letra `S` centralizada na cor
    `primary-foreground`, em `font-weight: 700`, `text-anchor: middle` e
    `dominant-baseline: central`.
    Escrever os valores de cor como literais OKLCH ou o equivalente hex — um SVG servido como
    arquivo estático não enxerga as CSS variables do app. Registrar no `brand.md` (seção
    Paleta ou Nome) que o ícone carrega uma cópia literal de `--primary`, e que trocar a paleta
    exige atualizar o SVG à mão.
    Nada de `<script>`, nada de `<image href="http...">`, nada de referência externa dentro do SVG.

    **C) Remover `src/app/favicon.ico`** (placeholder default do Next) com `git rm`, para que o
    `icon.svg` seja de fato o ícone servido.

    **Fora de escopo, registrar como diferido:** Open Graph image e `metadataBase` — precisam de
    arte e o app não é público (19-CONTEXT §Deferred Ideas).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');if(p.name!=='solo')throw new Error('name != solo');" &amp;&amp; test -f src/app/icon.svg &amp;&amp; test ! -f src/app/favicon.ico &amp;&amp; npm run verify:brand</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "console.log(require('./package.json').name)"` imprime `solo`.
    - `git diff -U0 package.json | grep -c '^[+-]' ` retorna `4` ou menos (só a linha `name`
      trocada, mais os marcadores de diff) — nenhuma reformatação ampla.
    - `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` sai `0`.
    - `npm run verify:brand` sai **`0`** — o último finding (`"name": "crm-leads"`) desapareceu.
    - `src/app/icon.svg` existe, casa `/<svg/` e contém a letra `S`; NÃO contém `<script`
      nem `href="http`.
    - `src/app/favicon.ico` não existe mais (`git status` mostra a remoção rastreada).
    - `rm -rf .next && npm run build` sai `0` e o `<head>` da página gerada referencia o ícone:
      `grep -rl 'icon' .next/server/app 2>/dev/null | head -1` retorna algo (ou, alternativamente,
      confirmar por `curl -sI` se um servidor estiver disponível — não obrigatório).
    - `brand.md` registra que o SVG carrega cópia literal da cor primária.
  </acceptance_criteria>
  <done>O pacote se chama solo, o favicon é o "S" da marca, o placeholder do Next saiu, e o grep-guard de marca está verde de ponta a ponta.</done>
</task>

<task type="auto">
  <name>Task 2: Rodar o portão completo de sensores da fase</name>
  <files>.planning/phases/19-marca-e-identidade-visual/19-HUMAN-UAT.md</files>
  <read_first>
    - `.planning/phases/19-marca-e-identidade-visual/19-VALIDATION.md` §"Per-Task Verification
      Map" e §"Sampling Rate" — o contrato de validação que este portão fecha.
    - `package.json` bloco `scripts` — os nomes exatos dos 12 comandos do portão.
    - `.planning/phases/19-marca-e-identidade-visual/19-01-SUMMARY.md` até `19-05-SUMMARY.md` —
      o que cada onda entregou, para conferir cobertura de BRAND-01/02/03.
    - `.planning/phases/18-auditoria-retroativa-no-navegador/18-VERIFICATION.md` — o formato de
      registro `code+data` a ser reusado.
  </read_first>
  <action>
    Executar, em sequência e do zero, os 12 comandos do portão listado no bloco `<interfaces>`,
    começando obrigatoriamente por `rm -rf .next` antes do `npm run build` (cache stale de
    fonte/CSS).

    Antes de rodar o build, fechar processos `node` órfãos — o host tem 4GB e o precedente do
    projeto é que builds falham por falta de memória quando há dev server ou harness pendurado.

    Registrar no `19-HUMAN-UAT.md` uma seção nova `## Portão automatizado (sensores)` com uma
    tabela `Comando | Exit code | Observação` das 12 execuções, com os números reais
    (páginas geradas pelo build, número de checagens do `check:contrast`, número de arquivos
    varridos pelo `verify:brand`). Sem números reais, o registro não vale.

    Se qualquer comando sair diferente de `0`, corrigir a causa raiz nesta task — não afrouxar
    o gate, não allowlistar nada novo sem registrar o motivo no SUMMARY.
  </action>
  <verify>
    <automated>rm -rf .next &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npm run verify:brand &amp;&amp; npm run verify:brand-md &amp;&amp; npm run check:contrast &amp;&amp; npm run guard:no-hard-delete &amp;&amp; npm run verify:schema &amp;&amp; npm run test:lead-actions &amp;&amp; npm run test:tarefa-actions &amp;&amp; npm run test:motivo-perda-actions &amp;&amp; npm run test:group-by-urgency</automated>
  </verify>
  <acceptance_criteria>
    - Os 12 comandos saem `0`, na mesma árvore de trabalho, sem edição entre eles.
    - `npm run build` reporta **13 ou mais páginas** geradas (paridade com a Fase 16/18).
    - `npm run check:contrast` reporta **30 checagens OK** (15 pares × `:root` e `.dark`) e
      nenhum `AUSENTE`.
    - `npm run verify:brand` reporta zero findings e cita o número de arquivos varridos.
    - `npm run verify:brand-md` reporta as 9 checagens OK.
    - `19-HUMAN-UAT.md` ganhou a seção `## Portão automatizado (sensores)` com a tabela dos 12
      comandos preenchida com exit codes e números reais.
    - `git status --porcelain` não lista `.brand-preview/`, `globals.css.bak` nem `.next/`.
  </acceptance_criteria>
  <done>Os 12 sensores da fase estão verdes simultaneamente e o resultado está registrado com números reais.</done>
</task>

<task type="auto">
  <name>Task 3: Executar ou registrar a verificação de não-regressão visual (D-19)</name>
  <files>.planning/phases/19-marca-e-identidade-visual/19-HUMAN-UAT.md</files>
  <read_first>
    - `.planning/phases/19-marca-e-identidade-visual/19-HUMAN-UAT.md` — os 20 cenários de rota
      (10 rotas × light + `.dark` forçado) e os 6 de dialog, autorados no plano 19-01.
    - `.planning/phases/18-auditoria-retroativa-no-navegador/18-VERIFICATION.md` — o molde exato
      do registro `code+data` (leitura da superfície + query só-SELECT no `data/crm.db` +
      harnesses), que foi aceito como `passed` sem navegador ao vivo.
    - `src/components/app-sidebar.tsx` — o `NAV_ITEMS` final, para conferir que as 10 rotas do
      checklist batem com as rotas reais.
    - `.planning/phases/19-marca-e-identidade-visual/19-RESEARCH.md` linhas 329–334 (Pitfall 2:
      os pontos de reflow de fonte que a inspeção precisa olhar).
  </read_first>
  <action>
    Primeiro, tentar a verificação ao vivo: subir `npm run dev`, abrir cada rota no navegador,
    e para o modo escuro adicionar a classe `dark` no `<html>` pelo devtools (D-17 — não existe
    toggle na UI e isso é proposital, D-16). Percorrer os 26 cenários aplicando estritamente a
    definição D-19 do bloco `<interfaces>`.

    **Se a RAM não permitir** (dev server + Chrome + sessão Claude → renderer congelado, como na
    Fase 18): NÃO insistir e NÃO bloquear. Registrar cada cenário pelo método `code+data`:
    - leitura da superfície (o `.tsx` da rota + os componentes que ela monta) confirmando que
      as classes de token estão presentes e que nenhuma largura fixa foi alterada;
    - `npm run build` verde como prova de que a rota gera;
    - `npm run check:contrast` verde como prova do critério de contraste medido (o único item
      do D-19 que é objetivamente mensurável sem navegador);
    - marcar os itens puramente visuais (reflow de fonte, alinhamento, animação) como
      `deferred — inspeção visual pura, requer navegador`, exatamente como a Fase 18 fez.

    Atualizar o frontmatter do `19-HUMAN-UAT.md` para `status: complete` (ao vivo) ou
    `status: complete-code-data` (registro code+data), mantendo `blocking: false`.

    Preencher a coluna `Resultado` dos 26 cenários (`pass` / `deferred` / `issue`) e a coluna
    `Nota` com a evidência concreta de cada um.

    **Qualquer achado que seja regressão real por D-19** (layout quebrado, texto ilegível,
    elemento cortado) vira uma quick task registrada, não uma correção improvisada aqui —
    salvo se for trivial e de uma linha, caso em que corrigir e registrar no SUMMARY.
    **Nenhum achado estético entra** — "a cor mudou" e "não gostei" são explicitamente
    não-regressões (D-19).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const f='.planning/phases/19-marca-e-identidade-visual/19-HUMAN-UAT.md';const t=fs.readFileSync(f,'utf8');if(!/status:\s*complete/.test(t))throw new Error('UAT nao concluido');const pend=(t.match(/\|\s*pending\s*\|/g)||[]).length;if(pend>0)throw new Error(pend+' cenarios ainda pending');console.log('UAT registrado');"</automated>
    <human-check>
      Abrir o app e confirmar, com os próprios olhos, que a identidade nova está aplicada e
      que nada quebrou: o header do sidebar mostra "SOLO" em uma linha com o quadrado "S", a
      aba do navegador mostra "SOLO" e o ícone novo, as 5 colunas do pipeline continuam
      cabendo sem scroll horizontal, os nomes de lead truncam com reticências em vez de
      quebrar linha, e os badges de etapa ficam em uma linha. Julgar SÓ contra a definição
      D-19 — mudança de cor não é regressão, é o objetivo da fase. Se não houver RAM para
      `npm run dev` + Chrome, esta conferência fica diferida e NÃO bloqueia o fechamento.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `19-HUMAN-UAT.md` tem `status: complete` ou `status: complete-code-data` no frontmatter e
      `blocking: false`.
    - Os 26 cenários (20 de rota + 6 de dialog) têm a coluna `Resultado` preenchida — zero
      `pending`.
    - Todo cenário marcado `deferred` traz na coluna `Nota` o motivo (`inspeção visual pura,
      requer navegador`) e a evidência substituta usada (`build` / `check:contrast` / leitura da
      superfície).
    - Todo cenário marcado `issue` referencia uma quick task registrada ou o commit da correção
      trivial.
    - Nenhum cenário foi marcado `issue` por motivo estético — conferir que nenhuma `Nota`
      contém "não gostei", "feio", "prefiro" ou "o teal sumiu".
    - As 10 rotas do checklist batem exatamente com o `NAV_ITEMS` de `app-sidebar.tsx`.
    - Os 12 comandos do portão da Task 2 continuam saindo `0` depois de qualquer correção feita
      nesta task.
  </acceptance_criteria>
  <done>A não-regressão visual das 10 rotas + 6 dialogs está verificada ao vivo ou registrada por code+data, sem nenhum cenário pendente e sem julgamento estético travando a fase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `src/app/icon.svg` → navegador | SVG é executável no contexto do documento quando embutido inline; servido como asset estático ainda pode carregar `<script>` ou referência externa. |
| `package.json` → tooling / npm | Edição programática do manifesto; corromper o JSON quebra todo o ferramental do projeto. |
| árvore de trabalho → git remoto | Artefatos temporários da fase não podem cruzar essa fronteira no commit de fechamento. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-13 | Tampering / Elevation | `src/app/icon.svg` | mitigate | Task 1 proíbe `<script>`, `<foreignObject>` e qualquer `href="http..."` dentro do SVG; aceite verifica por grep. Só formas e texto estático |
| T-19-06 | Tampering | `package.json` | mitigate | Task 1: edição de UMA linha; aceite exige `JSON.parse` limpo e `git diff -U0` com ≤ 4 linhas alteradas |
| T-19-02 | Information Disclosure | `.brand-preview/`, `globals.css.bak`, `.next/` | mitigate | Task 2: aceite exige `git status --porcelain` sem esses caminhos antes do commit de fechamento |
| T-19-05 | Denial of usability | legibilidade final em light e dark | mitigate | Task 2: `check:contrast` com 30 checagens OK é gate BLOQUEANTE; Task 3: checklist D-19 como camada humana não-bloqueante (precedente Fase 18) |
| T-19-14 | Repudiation | registro de verificação sem evidência | mitigate | Task 2 e 3 exigem números reais (páginas do build, contagem de checagens, arquivos varridos) e evidência substituta nomeada em todo cenário `deferred` — proibido registrar "OK" sem lastro |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote instalado em toda a Fase 19 (19-RESEARCH §Package Legitimacy Audit: "Audit N/A"). O `name` do `package.json` muda, mas nenhuma dependência é adicionada; um `npm install` de pacote novo aqui é desvio de plano e exige checkpoint humano bloqueante. |
</threat_model>

<verification>
1. `node -e "console.log(require('./package.json').name)"` → `solo`.
2. `test -f src/app/icon.svg && test ! -f src/app/favicon.ico` → exit 0.
3. `npm run verify:brand` → exit 0, zero findings.
4. `npm run verify:brand-md` → exit 0, 9 checagens.
5. `npm run check:contrast` → exit 0, 30 checagens.
6. `npx tsc --noEmit` → exit 0. `npm run lint` → exit 0.
7. `rm -rf .next && npm run build` → exit 0, 13+ páginas.
8. `npm run guard:no-hard-delete && npm run verify:schema && npm run test:lead-actions && npm run test:tarefa-actions && npm run test:motivo-perda-actions && npm run test:group-by-urgency` → todos exit 0.
9. `git grep -nE "CRM de Leads|CRM LEADS|CRM Leads|crm-leads" -- src/ package.json` → sem resultados.
10. `19-HUMAN-UAT.md` com `status: complete*`, 26 cenários sem `pending`.
11. `git status --porcelain` sem `.brand-preview/`, `*.css.bak` ou `.next/`.
</verification>

<success_criteria>
- BRAND-01 cumprido: `brand.md` documenta paleta, tipografia, tom/voz e o nome SOLO com racional e ressalva de colisão (`verify:brand-md` verde).
- BRAND-02 cumprido: paleta escolhida pelo usuário aplicada em `:root` e `.dark`, tipografia via `next/font`, zero cor hardcoded em `src/`, contraste AA nos dois modos (`verify:brand` + `check:contrast` verdes).
- BRAND-03 cumprido: nome antigo ausente de `src/` e `package.json`, ícone "S", favicon próprio, e não-regressão visual registrada nas 10 rotas + 6 dialogs.
- Nenhuma feature nova, nenhum `ThemeProvider`/toggle (D-16), nenhum pacote npm novo, nenhuma telemetria emitida.
- Escopo do rename respeitado (D-05): pasta do repo, CI, imports e paths intocados.
- Os 12 sensores do portão verdes no mesmo commit.
</success_criteria>

<output>
Create `.planning/phases/19-marca-e-identidade-visual/19-06-SUMMARY.md` when done.
Registrar: o método de verificação usado (ao vivo × code+data), a lista de cenários `deferred`
com o motivo, quaisquer quick tasks abertas, e o snapshot final dos 12 exit codes do portão.
</output>
