---
phase: 19-marca-e-identidade-visual
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-no-hardcoded-colors.cjs
  - scripts/verify-brand-md.cjs
  - scripts/check-contrast.cjs
  - package.json
  - .gitignore
  - .planning/phases/19-marca-e-identidade-visual/19-HUMAN-UAT.md
autonomous: true
requirements: [BRAND-01, BRAND-02, BRAND-03]

must_haves:
  truths:
    - "Existe um comando que falha quando sobra cor hardcoded ou o nome antigo em src/ ou package.json"
    - "Existe um comando que falha quando brand.md não tem as seções Paleta, Tipografia, Tom/Voz e Nome/colisão"
    - "Existe um comando que falha quando algum par de tokens de globals.css fica abaixo de WCAG AA em light ou dark"
    - "Os artefatos do /brand-design (.brand-preview/ e *.css.bak) não podem ser commitados por acidente"
    - "Existe um checklist escrito das 10 rotas com a definição operacional D-19 de regressão visual"
  artifacts:
    - path: "scripts/verify-no-hardcoded-colors.cjs"
      provides: "grep-guard de cor hardcoded + nome antigo (BRAND-02/BRAND-03)"
      min_lines: 120
    - path: "scripts/verify-brand-md.cjs"
      provides: "gate de existência/estrutura do brand.md + backup + next/font (BRAND-01)"
      min_lines: 60
    - path: "scripts/check-contrast.cjs"
      provides: "gate WCAG AA sobre :root e .dark de globals.css (BRAND-02)"
      min_lines: 120
    - path: ".gitignore"
      contains: ".brand-preview"
    - path: "package.json"
      contains: "verify:brand"
    - path: ".planning/phases/19-marca-e-identidade-visual/19-HUMAN-UAT.md"
      provides: "checklist D-19 das 10 rotas em light + .dark forçado"
      min_lines: 60
  key_links:
    - from: "package.json"
      to: "scripts/verify-no-hardcoded-colors.cjs"
      via: "npm script verify:brand"
      pattern: "verify:brand\":\\s*\"node scripts/verify-no-hardcoded-colors.cjs"
    - from: "scripts/check-contrast.cjs"
      to: "src/app/globals.css"
      via: "leitura + parse dos blocos :root e .dark"
      pattern: "globals\\.css"
    - from: "scripts/verify-brand-md.cjs"
      to: "brand.md"
      via: "leitura na raiz do repo"
      pattern: "brand\\.md"
---

<objective>
Construir a Wave 0 da Fase 19: os três sensores automatizados que provam BRAND-01/02/03 sem
navegador, o `.gitignore` que impede o vazamento dos artefatos do `/brand-design`, e o checklist
`19-HUMAN-UAT.md` com a definição operacional D-19.

Purpose: o `19-VALIDATION.md` marca TODAS as verificações da fase como `❌ W0` — nenhum sensor
existe hoje. Sem eles, o refactor de ~114 ocorrências de cor não tem gate mecânico e a fase só
poderia ser fechada "no olho", o que o host de 4GB não permite (precedente Fase 18). Este plano
roda ANTES do `/brand-design` porque o `.gitignore` precisa cobrir `.brand-preview/` e
`*.css.bak` antes de o skill criá-los (T-19-02).

Output: 3 scripts `.cjs`, 3 npm scripts, 2 linhas novas no `.gitignore`, 1 checklist de UAT.

**Estado esperado ao fim deste plano: os 3 sensores saem VERMELHOS (exit 1).** Isso é correto e
desejado — é o estado RED do Nyquist. Eles ficam verdes ao longo das ondas 2–6.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-marca-e-identidade-visual/19-CONTEXT.md
@.planning/phases/19-marca-e-identidade-visual/19-VALIDATION.md
@.planning/phases/19-marca-e-identidade-visual/19-PATTERNS.md

<interfaces>
<!-- Contratos que o executor precisa. NÃO explorar o codebase para descobrir isto. -->

Padrão obrigatório de script `.cjs` neste repo (19-PATTERNS.md §"Scripts .cjs novos", itens 1–10):
1. `#!/usr/bin/env node` na linha 1, `"use strict";` na linha 2
2. Docblock JSDoc extenso em PT-BR (20–45 linhas) explicando o quê / porquê / exit codes
3. Só módulos nativos: `require("node:fs")`, `require("node:path")` — sem dependência externa
4. `const ROOT = path.join(__dirname, "..")` + helper `toRelative(abs)`
5. Contador de falhas + reporter (estilo `findings[]` do guard, ou `check(cond,msg)` do harness)
6. `walk(dir)` recursivo com `fs.readdirSync(dir,{withFileTypes:true})`, pulando
   `IGNORED_DIR_NAMES = new Set(["node_modules",".next","dist"])` e entradas iniciadas por `.`
7. `ALLOWLIST` de caminhos relativos montados com `path.join(...)`
8. `process.exit(0)` no sucesso (`console.log("OK: ...")`) / `process.exit(1)` na falha
9. Registro em `package.json` `scripts` com prefixo `verify:` (gates) ou `test:` (harnesses)
10. NÃO usar `ts-alias-loader.mjs` nem `next-cache-stub` — estes scripts leem `.css`/`.tsx`/`.json`
    como TEXTO (regex), nunca importam módulos de `src/`

Bloco `scripts` atual de `package.json` (linhas 5–26) — precedente exato de nomenclatura:
```
"lint": "eslint",
"guard:no-hard-delete": "node scripts/guard-no-hard-delete.cjs",
"verify:schema": "node scripts/verify-schema.cjs",
"verify:origem-tipo": "node scripts/verify-origem-tipo.cjs",
"verify:motivo-perda": "node scripts/verify-motivo-perda-obrigatorio.cjs",
"test:group-by-urgency": "node scripts/test-group-by-urgency.cjs",
```

Inventário-alvo do grep-guard (medido por `git grep` em 2026-09-02 — 115 matches / 32 arquivos):
followup-dashboard 12 · template-list 8 · lead-table 8 · csv-import-preview-table 8 ·
nicho-manager 7 · motivo-perda-manager 7 · app-sidebar 6 · lead-timeline-dialog 5 ·
etapa-badge 5 · whatsapp-preview-dialog 4 · csv-upload-dropzone 4 · csv-column-mapper 4 ·
configuracoes-form 4 · relatorios/page 4 · pipeline-lead-card 3 · pipeline-column 3 ·
motivo-perda-combobox 3 · lead-form-dialog 3 · tarefa-form-dialog 2 · tarefa-card 2 ·
lead-table-toolbar 2 · e 11 arquivos com 1 match cada (csv-encoding, whatsapp-send-button,
ui/dialog, template-form-dialog, post-import-lead-list, pipeline-board, periodo-selector,
motivo-perda-dialog, lixeira-table, lead-table-columns, layout).

Rotas do `NAV_ITEMS` (`src/components/app-sidebar.tsx:19-30`) para o checklist:
`/` · `/leads` · `/importar` · `/pipeline` · `/relatorios` · `/templates` · `/nichos` ·
`/motivos-perda` · `/lixeira` · `/configuracoes`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar o grep-guard scripts/verify-no-hardcoded-colors.cjs</name>
  <files>scripts/verify-no-hardcoded-colors.cjs</files>
  <read_first>
    - `scripts/guard-no-hard-delete.cjs` (186 linhas) — MOLDE EXATO. Copiar a espinha inteira:
      shebang+strict, docblock, `ROOT`, `toRelative`, `IGNORED_DIR_NAMES`, `walk()`,
      `ALLOWLIST`, acumulador `findings[]`, reporter final, `process.exit`.
    - `scripts/verify-schema.cjs` (142 linhas) — padrão de `fail(msg)` + saída explícita.
    - `.planning/phases/19-marca-e-identidade-visual/19-PATTERNS.md` linhas 448–461
      (especificação deste script) e linhas 420–447 (os 10 itens do padrão comum).
  </read_first>
  <action>
    Criar `scripts/verify-no-hardcoded-colors.cjs` copiando a estrutura de
    `guard-no-hard-delete.cjs` e trocando o miolo.

    PARTE A — varredura de `src/`:
    - `SCAN_ROOTS = ["src"]`, `CODE_EXTENSIONS = new Set([".ts", ".tsx"])`.
    - Aceitar um argumento opcional de CLI (`process.argv[2]`) que substitui `SCAN_ROOTS` por
      esse único diretório. Serve para o teste de fixture do critério de aceite — o default,
      sem argumento, continua sendo `["src"]`.
    - `CODE_PATTERNS` (array de `{ id, re, hint }`, um finding por linha que casar):
      * `HEX` — `/#[0-9a-fA-F]{3,8}\b/`
      * `ARBITRARY` — `/-\[#[0-9a-fA-F]{3,8}\]/`
      * `NEUTRAL_SCALE` — `/\b(bg|text|border|ring|from|via|to|accent|fill|stroke|shadow|outline|divide|ring-offset)-(zinc|slate|gray|neutral|stone|white|black)(-\d{2,3})?\b/`
      * `NAMED_SCALE` — `/\b(bg|text|border|ring|from|via|to|fill|stroke)-(amber|blue|green|red|emerald|yellow|orange|rose|violet|indigo|sky|teal|cyan|lime|fuchsia|pink|purple)-\d{2,3}\b/`
      * `INLINE_STYLE_COLOR` — `/style=\{\{[^}]*\b(backgroundColor|color|borderColor)\s*:/`
      * `OLD_NAME` — `/CRM de Leads|CRM LEADS|CRM Leads/`
    - `ALLOWLIST = [ path.join("src", "lib", "csv-encoding.ts") ]` com comentário explicando
      que o match ali é o code point `#840` dentro de um comentário de mojibake, não uma cor.
      NÃO allowlistar `src/components/ui/dialog.tsx` — o overlay `bg-black/10` será convertido
      para `bg-foreground/10` no plano 19-05.

    PARTE B — checagem direta de `package.json` (o walk não cobre a raiz):
    - Ler `package.json` como texto e adicionar finding se casar
      `/"name"\s*:\s*"crm-leads"/` ou `/CRM de Leads|CRM LEADS|CRM Leads/`.

    Reporter: um `console.error` por finding no formato
    `  <caminho-relativo>:<linha>  [<id>]  <trecho da linha, trim, max 120 chars>`,
    depois resumo `[verify-brand] FALHOU: N ocorrência(s) de cor hardcoded / nome antigo em src/`
    e `process.exit(1)`. No sucesso: `console.log("OK: nenhuma cor hardcoded nem nome antigo em src/ (M arquivos varridos)")` e `process.exit(0)`.

    O próprio script vive em `scripts/`, fora de `SCAN_ROOTS`, então nunca se auto-reporta —
    não precisa de auto-allowlist.
  </action>
  <verify>
    <automated>node scripts/verify-no-hardcoded-colors.cjs; test $? -eq 1 &amp;&amp; echo "RED esperado: OK"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-no-hardcoded-colors.cjs` sai com código `1` (estado RED esperado nesta onda).
    - A saída lista **≥ 110 findings** distribuídos em **≥ 30 arquivos** distintos de `src/`.
    - A saída inclui pelo menos uma linha de `src/components/followup-dashboard.tsx`, uma de
      `src/components/app-sidebar.tsx` e uma de `src/components/etapa-badge.tsx`.
    - A saída inclui o finding `[OLD_NAME]` de `package.json` (`"name": "crm-leads"`) e o de
      `src/app/layout.tsx` (`CRM de Leads`).
    - NÃO aparece nenhuma linha de `src/lib/csv-encoding.ts` (allowlist funcionando).
    - Prova de fixture (o script sai 0 quando o alvo está limpo):
      `FX=$(mktemp -d) && printf 'export const a = "bg-primary text-primary-foreground";\n' > "$FX/a.tsx" && node scripts/verify-no-hardcoded-colors.cjs "$FX"` → exit `0`.
    - `npx eslint scripts/verify-no-hardcoded-colors.cjs` sai `0` (o override de
      `scripts/**/*.cjs` da Fase 17 já desliga `no-require-imports`).
  </acceptance_criteria>
  <done>O grep-guard existe, prova o estado sujo atual (≥110 findings) e prova que sai 0 contra um alvo limpo.</done>
</task>

<task type="auto">
  <name>Task 2: Criar scripts/verify-brand-md.cjs e scripts/check-contrast.cjs</name>
  <files>scripts/verify-brand-md.cjs, scripts/check-contrast.cjs</files>
  <read_first>
    - `scripts/verify-motivo-perda-obrigatorio.cjs` (230 linhas), PARTE B — padrão
      `fs.readFileSync` + asserts sobre texto-fonte.
    - `scripts/test-group-by-urgency.cjs` linhas 33–49 — o contador
      `let failed = 0; function check(cond, msg) { ... }`.
    - `scripts/verify-schema.cjs` (142 linhas) — script standalone sem loader, `fail()` → exit 1.
    - `src/app/globals.css` (129 linhas) — a estrutura real dos blocos `@theme inline` (linhas
      ~6–49), `:root` (~51–84) e `.dark` (~86–118) que o parser precisa reconhecer.
    - `~/.claude/skills/brand-design/references/contrast-rules.md` — os pares e limiares canônicos.
    - `.planning/phases/19-marca-e-identidade-visual/19-PATTERNS.md` linhas 463–496.
  </read_first>
  <action>
    **A) `scripts/verify-brand-md.cjs`** — sem async, sem loader, só `node:fs`/`node:path`.
    Usar o contador `check(cond, msg)`. Asserts, nesta ordem:
    1. `brand.md` existe na raiz do repo (se não existir: `check(false, ...)` e pular os asserts
       de conteúdo sem lançar exceção).
    2. `/##\s+.*paleta/i` — seção Paleta.
    3. `/##\s+.*tipografia/i` — seção Tipografia.
    4. `/tom|voz/i` — seção Tom/Voz.
    5. `/##\s+.*nome/i` E `/SOLO/` — seção Nome (BRAND-01 / D-01).
    6. `/colis|descart|SoloCRM|Salesboom|gosolo/i` — ressalva de colisão (D-02).
    7. `fs.existsSync("src/app/globals.css.bak")` — backup do skill (Passo 5, não-negociável).
    8. `src/app/layout.tsx` casa `/next\/font\/google/`.
    9. `src/app/layout.tsx` casa `/variable:\s*["']--font-(sans|mono|serif|heading)["']/`.
    Final: `process.exit(failed > 0 ? 1 : 0)` com resumo
    `[verify-brand-md] N de M checagens falharam`.

    **B) `scripts/check-contrast.cjs`** — estrutura de `verify-schema.cjs`, lógica de cor nova.
    1. Ler `src/app/globals.css` como texto. Extrair o corpo do bloco `:root { ... }` e do bloco
       `.dark { ... }` por varredura de chaves balanceada (não regex ingênua — os blocos contêm
       `oklch(...)` com parênteses, mas não chaves internas; ainda assim contar `{`/`}`).
    2. Parsear pares `--token: <valor>;` em cada bloco para um `Map`.
    3. Resolver indireção: se o valor casar `/^var\(\s*(--[\w-]+)\s*\)$/`, resolver o alvo no
       MESMO bloco; se ausente ali, cair para o bloco `:root`. Resolver no máximo 3 níveis;
       ciclo ou não-resolvido = falha explícita `token X não resolve para uma cor`.
    4. Converter `oklch(L C H)` / `oklch(L C H / A)` para sRGB de verdade (sem aproximação):
       OKLCH→OKLab (`a = C*cos(H°)`, `b = C*sin(H°)`) → LMS' → LMS (cubo) → linear sRGB pela
       matriz inversa padrão → clamp [0,1] → gama sRGB. Alfa é ignorado (nenhum par testado usa).
       Aceitar `L` em fração (`0.97`) e em porcentagem (`97%`).
    5. Luminância relativa WCAG e razão `(L1+0.05)/(L2+0.05)`.
    6. Validar estes **15 pares em `:root` E em `.dark`** (30 checagens):
       * ≥ 4.5 (texto): `foreground/background`, `card-foreground/card`,
         `popover-foreground/popover`, `primary-foreground/primary`,
         `secondary-foreground/secondary`, `muted-foreground/muted`,
         `accent-foreground/accent`, `sidebar-foreground/sidebar`,
         `sidebar-accent-foreground/sidebar-accent`,
         `status-neutral-foreground/status-neutral`, `status-info-foreground/status-info`,
         `status-warning-foreground/status-warning`, `status-success-foreground/status-success`,
         `status-danger-foreground/status-danger`
       * ≥ 3.0 (componente de UI): `destructive/background`
    7. Token ausente = FALHA explícita, com a mensagem
       `AUSENTE --<token> em <bloco> — a escala --status-* é criada no plano 19-03`.
       Nunca tratar ausência como "pulado".
    8. Saída por par: `OK  <bloco>  --a sobre --b = 7.31 (min 4.5)` ou `FAIL ...`.
       Resumo + `process.exit(failed > 0 ? 1 : 0)`.
  </action>
  <verify>
    <automated>node scripts/verify-brand-md.cjs; test $? -eq 1 &amp;&amp; node scripts/check-contrast.cjs; test $? -eq 1 &amp;&amp; echo "RED esperado: OK"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-brand-md.cjs` sai `1` e a primeira linha de falha cita `brand.md`
      (o arquivo ainda não existe — RED esperado). O script NÃO lança exceção não tratada.
    - `node scripts/check-contrast.cjs` sai `1` e reporta exatamente **10 tokens `--status-*`
      ausentes em `:root`** e **10 em `.dark`** com a mensagem `AUSENTE ... plano 19-03`.
    - Na mesma execução, os pares core do shadcn default **passam**: a saída contém
      `OK  :root  --foreground sobre --background` e `OK  .dark  --foreground sobre --background`.
    - Sanidade numérica da conversão de cor: para o `:root` atual
      (`--background: oklch(1 0 0)` branco, `--foreground` quase preto) a razão reportada é
      **> 15** — se vier perto de 1 ou 21 exato, a conversão OKLCH→sRGB está errada.
    - `node scripts/check-contrast.cjs` reporta a resolução de `var(--x)`: adicionar
      temporariamente `--teste-alias: var(--background);` NÃO é necessário — em vez disso,
      confirmar no log que `--sidebar` (hoje literal) é lido sem erro.
    - `npx eslint scripts/verify-brand-md.cjs scripts/check-contrast.cjs` sai `0`.
  </acceptance_criteria>
  <done>Os dois gates existem, saem VERMELHOS pelo motivo certo (brand.md ausente / --status-* ausente) e a matemática de contraste é comprovadamente correta nos pares que já existem.</done>
</task>

<task type="auto">
  <name>Task 3: Registrar os npm scripts, blindar o .gitignore e autorar 19-HUMAN-UAT.md</name>
  <files>package.json, .gitignore, .planning/phases/19-marca-e-identidade-visual/19-HUMAN-UAT.md</files>
  <read_first>
    - `package.json` linhas 1–26 — o bloco `scripts` e a nomenclatura `verify:`/`test:`/`guard:`.
    - `.gitignore` (44 linhas) — a seção `# next.js` (linhas 16–18) e a seção final
      `# sqlite database ...` (linhas 43–45), para seguir o formato de comentário em PT-BR.
    - `.planning/phases/18-auditoria-retroativa-no-navegador/18-VERIFICATION.md` — o molde
      `code+data` promovido a `passed` sem navegador ao vivo (precedente que este checklist herda).
    - `.planning/phases/19-marca-e-identidade-visual/19-VALIDATION.md` §"Manual-Only Verifications"
      e §"Rotas a cobrir" — texto-fonte da definição D-19 e da lista de rotas.
    - `src/components/app-sidebar.tsx` linhas 19–30 — o array `NAV_ITEMS` real (as 10 rotas).
  </read_first>
  <action>
    **A) `package.json`** — adicionar 3 entradas ao bloco `scripts`, logo após
    `"verify:motivo-perda"`, mantendo o estilo das existentes:
    `"verify:brand": "node scripts/verify-no-hardcoded-colors.cjs"`,
    `"verify:brand-md": "node scripts/verify-brand-md.cjs"`,
    `"check:contrast": "node scripts/check-contrast.cjs"`.
    NÃO tocar no campo `"name"` — o rename para `"solo"` acontece na Task 3 do plano 19-05
    (D-04), onde a PARTE B do guard precisa ficar limpa para o gate daquele plano rodar inteiro.

    **B) `.gitignore`** — acrescentar no fim do arquivo um bloco novo com comentário em PT-BR:
    `# artefatos do skill /brand-design (preview estático + backup do tema)`, e as duas linhas
    `/.brand-preview/` e `*.css.bak`. Isso fecha a ameaça T-19-02 e precisa estar no disco
    ANTES do plano 19-02 rodar o skill.

    **C) `.planning/phases/19-marca-e-identidade-visual/19-HUMAN-UAT.md`** — criar o checklist,
    em PT-BR, com frontmatter `phase: 19`, `status: pending`, `blocking: false`. Seções:
    1. `## Como executar` — `npm run dev`, abrir cada rota; para o modo escuro, adicionar a
       classe `dark` no `<html>` pelo devtools (D-17). Registrar explicitamente que **não existe
       toggle de tema na UI e isso é proposital (D-16)**.
    2. `## Definição operacional de regressão (D-19)` — copiar as duas listas do
       `19-VALIDATION.md`: o que BLOQUEIA (layout quebrado, texto ilegível, contraste medido
       < AA, dark bugado com `.dark` forçado, elemento sumido/cortado, header do sidebar em 2
       linhas, colunas do pipeline com scroll horizontal, badge de etapa quebrando linha) e o
       que NUNCA bloqueia (a cor mudou / não gostei / o teal sumiu / ficou escuro demais /
       qualquer julgamento estético).
    3. `## Cenários` — uma linha de tabela por rota, **20 cenários** (as 10 rotas × light +
       `.dark` forçado), colunas `#`, `Rota`, `Modo`, `O que conferir`, `Resultado`, `Nota`.
       Cada `O que conferir` cita os pontos de reflow do Pitfall 2: em `/` conferir os cabeçalhos
       de urgência e o card de lead; em `/leads` o nome truncando com reticências, os badges de
       etapa em 1 linha e o botão WhatsApp; em `/pipeline` as 5 colunas sem scroll horizontal e
       a borda "esfriando"; em `/importar` os badges de flag da prévia; em `/relatorios` a faixa
       de intervalo inválido; nas demais, o formulário/tabela principal. Em toda rota, conferir
       o header do sidebar "SOLO" em 1 linha e o item ativo legível.
    4. `## Dialogs` — 6 cenários extras: LeadFormDialog, LeadTimelineDialog,
       WhatsAppPreviewDialog, TemplateFormDialog, MotivoPerdaDialog (drop em "Perdido"),
       TarefaFormDialog — conferindo o título do dialog (que usa `font-heading`, D-18) e o overlay.
    5. `## Plano B (host 4GB)` — se `npm run dev` + Chrome não couberem na RAM, registrar cada
       cenário como `code+data` (leitura da superfície + `npm run build` + os 3 sensores) e
       marcar `deferred`, seguindo o precedente da Fase 18. Isto é NÃO-bloqueante.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');const s=p.scripts;if(!s['verify:brand']||!s['verify:brand-md']||!s['check:contrast'])throw new Error('npm scripts ausentes');" &amp;&amp; grep -c 'brand-preview' .gitignore &amp;&amp; npm run verify:brand; test $? -eq 1 &amp;&amp; echo "gates registrados"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify:brand`, `npm run verify:brand-md` e `npm run check:contrast` executam
      (não dão "Missing script") e cada um sai `1` — estado RED esperado.
    - `package.json` continua com `"name": "crm-leads"` (o rename é da Task 3 do plano 19-05).
    - `grep -v '^#' .gitignore | grep -c 'brand-preview'` retorna `1` e
      `grep -v '^#' .gitignore | grep -c 'css.bak'` retorna `1`.
    - `git check-ignore -q .brand-preview/index.html` sai `0` e
      `git check-ignore -q src/app/globals.css.bak` sai `0`.
    - `19-HUMAN-UAT.md` existe com `status: pending`, `blocking: false`, contém as 10 rotas do
      `NAV_ITEMS` (`grep -c '/configuracoes' 19-HUMAN-UAT.md` ≥ 1 para cada uma), tem **≥ 26
      linhas de cenário** (20 rotas + 6 dialogs) e contém a frase literal
      "Mudança de cor é o objetivo declarado da fase".
    - `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` sai `0`
      (JSON não corrompido).
  </acceptance_criteria>
  <done>Os 3 gates estão registrados como npm scripts, os artefatos do skill são inignoráveis pelo git, e o checklist D-19 das 10 rotas + 6 dialogs está escrito e pronto para execução ou registro code+data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill `/brand-design` → repositório | O skill escreve arquivos no repo (`globals.css`, `layout.tsx`, `brand.md`, `.brand-preview/`) e faz um `curl` de telemetria para fora. Este plano constrói as barreiras que serão exercidas no plano 19-02. |
| repositório → git remoto | Artefatos temporários (`.brand-preview/`, `*.css.bak`) não podem cruzar essa fronteira. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02 | Information Disclosure | `.brand-preview/*.html`, `src/app/globals.css.bak` | mitigate | Task 3: `/.brand-preview/` e `*.css.bak` no `.gitignore` ANTES de o skill rodar (plano 19-02); provado por `git check-ignore` no critério de aceite |
| T-19-05 | Denial of usability | tokens de cor de `globals.css` | mitigate | Task 2: `check-contrast.cjs` valida 15 pares × 2 blocos com conversão OKLCH→sRGB real e limiares WCAG AA (4.5 texto / 3.0 UI); token ausente é falha, nunca "pulado" |
| T-19-06 | Tampering | `package.json` (edição programática do bloco `scripts`) | mitigate | Task 3: aceite exige `JSON.parse` limpo e preservação de `"name": "crm-leads"` nesta onda — nenhuma reescrita ampla do arquivo (o rename é da Task 3 do plano 19-05) |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote instalado nesta fase (19-RESEARCH §Package Legitimacy Audit: "Audit N/A"). Os 3 scripts usam só `node:fs`/`node:path`. Qualquer `npm install` novo aqui é desvio de plano e exige checkpoint humano bloqueante antes de instalar. |
</threat_model>

<verification>
1. `node scripts/verify-no-hardcoded-colors.cjs` → exit 1, ≥110 findings, ≥30 arquivos.
2. `node scripts/verify-no-hardcoded-colors.cjs "$(mktemp -d)"` sobre fixture limpa → exit 0.
3. `node scripts/verify-brand-md.cjs` → exit 1 citando `brand.md` ausente.
4. `node scripts/check-contrast.cjs` → exit 1 com 20 tokens `--status-*` ausentes e os pares core em OK.
5. `npm run verify:brand && npm run verify:brand-md && npm run check:contrast` → todos executáveis.
6. `git check-ignore -q .brand-preview/index.html && git check-ignore -q src/app/globals.css.bak` → exit 0.
7. `npx eslint scripts/verify-no-hardcoded-colors.cjs scripts/verify-brand-md.cjs scripts/check-contrast.cjs` → exit 0.
8. `npm run lint` da raiz continua exit 0 (não regredir o ganho da Fase 17).
</verification>

<success_criteria>
- Os 3 sensores existem, seguem o padrão `.cjs` do repo (shebang, strict, docblock PT-BR, só módulos nativos, exit explícito) e estão registrados em `package.json`.
- Cada sensor sai VERMELHO pelo motivo correto e documentado — nenhum falso-verde.
- O grep-guard prova as duas direções: vermelho no `src/` real, verde numa fixture limpa.
- `.brand-preview/` e `*.css.bak` são ignorados pelo git.
- `19-HUMAN-UAT.md` existe, é não-bloqueante, cobre as 10 rotas × 2 modos + 6 dialogs, e carrega a definição D-19 verbatim.
- `npm run lint` continua exit 0.
</success_criteria>

<output>
Create `.planning/phases/19-marca-e-identidade-visual/19-01-SUMMARY.md` when done
</output>
