---
phase: 19-marca-e-identidade-visual
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - brand.md
  - README.md
autonomous: false
requirements: [BRAND-01, BRAND-02]

must_haves:
  truths:
    - "O usuário viu ~6 paletas candidatas num preview HTML no navegador e escolheu uma"
    - "A paleta escolhida está aplicada como CSS variables shadcn em :root e .dark de globals.css"
    - "A tipografia escolhida está ligada via next/font/google em layout.tsx e casa com o @theme inline"
    - "brand.md na raiz documenta paleta, tipografia, tom/voz E o nome SOLO com racional e ressalva de colisão"
    - "Existe backup src/app/globals.css.bak para rollback"
  artifacts:
    - path: "brand.md"
      provides: "documento de marca: paleta, tipografia, tom/voz, nome SOLO + colisão (BRAND-01)"
      contains: "SOLO"
      min_lines: 60
    - path: "src/app/globals.css"
      provides: "tokens shadcn da paleta escolhida em :root e .dark"
      contains: "--primary:"
    - path: "src/app/layout.tsx"
      provides: "par tipográfico via next/font/google"
      contains: "next/font/google"
    - path: "src/app/globals.css.bak"
      provides: "rollback do tema anterior"
    - path: "README.md"
      provides: "identidade mínima do produto na raiz"
      min_lines: 15
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "as chaves variable: --font-* casam com as linhas --font-* do @theme inline"
      pattern: "variable:\\s*[\"']--font-(sans|mono|serif)[\"']"
    - from: "brand.md"
      to: "D-01/D-02"
      via: "seção Nome — SOLO com ressalva de colisão"
      pattern: "(?i)(colis|descart|SoloCRM|Salesboom|gosolo)"
---

<objective>
Rodar o skill `/brand-design` de ponta a ponta com os inputs já travados no CONTEXT, aplicar a
paleta e a tipografia escolhidas pelo usuário, e completar o `brand.md` com o que o skill não
escreve (nome do produto, racional e ressalva de colisão).

Purpose: BRAND-02 exige que o usuário escolha a paleta no navegador — é uma decisão de produto,
não de engenharia (D-11/D-13). E BRAND-01 NÃO é coberto pelo skill sozinho: o
`references/brand-md-template.md` não tem seção de nome/racional/colisão (Pitfall 1), então uma
task explícita aumenta o `brand.md` depois. Sem este plano, os planos 19-03/04/05 não têm paleta
viva para mapear cor → token.

Output: `globals.css` com os 14 tokens core + `--radius` reescritos em `:root` e `.dark`,
`layout.tsx` com o par tipográfico via `next/font`, `globals.css.bak` de rollback, `brand.md`
completo e `README.md` mínimo.

Este plano tem 2 checkpoints de decisão porque o skill é um fluxo interativo único — quebrá-lo
em planos separados perderia o estado da sessão de escolha da paleta.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-marca-e-identidade-visual/19-CONTEXT.md
@.planning/phases/19-marca-e-identidade-visual/19-RESEARCH.md
@.planning/phases/19-marca-e-identidade-visual/19-PATTERNS.md
@.planning/ANALISE-CRM-CONCORRENTE-E-GAPS.md

<interfaces>
<!-- Respostas TRAVADAS da entrevista do skill. O executor NÃO pergunta nada disso ao usuário. -->

Entrevista de 4 perguntas do `/brand-design` — respostas já decididas no 19-CONTEXT.md:
1. Nome + descrição → `SOLO` — "CRM pra quem trabalha sozinho: organiza leads, pipeline e
   follow-ups sem equipe, sem fila, sem SLA." (D-01)
2. Categoria → `tooling/dev` (D-10). Nota do RESEARCH: `palette-recipes.md` não tem essa
   categoria; o skill cai para `infra/data (devtools)` — comportamento de fallback esperado,
   não é bug.
3. Mood → `serious` + `premium` (D-09).
4. Referência de feeling → o CRM do amigo (GS Info Sistemas): dark navy + accent teal + status
   colors, "sóbrio, profissional, sem cara de vibecode". É referência de SENSAÇÃO, **não de
   paleta** — as 6 candidatas podem pivotar livremente (navy, grafite, roxo, etc.) (D-11/D-12).

Estrutura de `src/app/globals.css` que DEVE ser preservada (129 linhas):
- linhas 1–4: `@import "tailwindcss"` / `"tw-animate-css"` / `"shadcn/tailwind.css"` /
  `@custom-variant dark (&:is(.dark *))`
- `@theme inline { ... }` (~6–49): mapa `--color-*: var(--*)`, as 3 linhas de fonte
  (`--font-sans: var(--font-geist-sans)`, `--font-mono: var(--font-geist-mono)`,
  `--font-heading: var(--font-sans)`) e a escala `--radius-sm..4xl` derivada de `--radius`
- `:root { ... }` (~51–84): 14 core + `--chart-1..5` (70–74) + `--sidebar-*` (76–83)
- `.dark { ... }` (~86–118): espelho
- `@layer base { ... }` (~120–130): `* { @apply border-border ... }`,
  `body { @apply bg-background text-foreground }`, `html { @apply font-sans }`

Estado atual de `src/app/layout.tsx` (39 linhas):
```
linha 2   import { Geist, Geist_Mono } from "next/font/google";
linha 8   variable: "--font-geist-sans"
linha 13  variable: "--font-geist-mono"
linha 18  title: "CRM de Leads"
linha 19  description: "CRM pessoal para organizar leads e o funil de vendas."
linha 30  <html lang="pt-BR" className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}>
linha 32  <body className="min-h-full flex bg-white">
```
Neste plano só as fontes mudam. O rename de `title`/`description` e a remoção de `bg-white`
acontecem no plano 19-05 (D-04/D-15) — não antecipar.

`--font-heading` está EM USO: `src/components/ui/dialog.tsx:125` (`DialogTitle`). Trocá-lo
propaga para TODOS os títulos de dialog do app (Pitfall 4).
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Rodar /brand-design passos 0-5 — escolha e aplicação da paleta</name>
  <files>src/app/globals.css, src/app/globals.css.bak</files>
  <read_first>
    - `~/.claude/skills/brand-design/SKILL.md` — o workflow completo (Preamble + Passos 1–9).
    - `~/.claude/skills/brand-design/references/palette-recipes.md` — as paletas curadas e o
      índice por categoria/mood.
    - `~/.claude/skills/brand-design/references/shadcn-integration.md` — o cálculo do token set
      shadcn light+dark e as notas de Tailwind v4 (sem `tailwind.config.js`).
    - `~/.claude/skills/brand-design/references/contrast-rules.md` — os pares e o auto-ajuste AA.
    - `src/app/globals.css` (129 linhas) — a estrutura a preservar.
    - `.planning/phases/19-marca-e-identidade-visual/19-PATTERNS.md` linhas 49–114.
  </read_first>
  <decision>Qual das ~6 paletas candidatas vira a identidade visual do SOLO.</decision>
  <context>
    O usuário decide no preview HTML, não o executor (D-11/D-13). O executor conduz o skill,
    gera o preview e itera até aprovação.

    **Antes de qualquer coisa (T-19-01):** no Preamble, quando o skill perguntar sobre
    telemetria, responder **"No thanks"**. Se ele oferecer gravar preferência, garantir
    `~/.superstack/config.json` com `telemetryTier: "off"`. Nenhum `curl` de POST deve ser
    emitido nesta fase.

    Passar a entrevista com as 4 respostas TRAVADAS do bloco `<interfaces>` — não perguntar de
    novo ao usuário.

    Gerar as 6 candidatas (3 curadas de `palette-recipes.md` na categoria infra/data +
    3 algorítmicas) e escrever `.brand-preview/index.html`. Esse preview é **HTML estático** —
    abrir com `start` no Windows. **NÃO subir `npm run dev`**: o host tem 4GB e dev server +
    Chrome + sessão Claude congela o renderer (precedente Fase 18, Pitfall 5). Se `start`
    falhar, imprimir o caminho `file://` para o usuário abrir na mão.

    Rodar o loop de regeneração sem limite até o usuário aprovar uma paleta (D-13).
  </context>
  <action>
    Depois da aprovação, executar o Passo 5 do skill com estas restrições explícitas:
    1. Backup obrigatório: `cp src/app/globals.css src/app/globals.css.bak` ANTES de escrever
      (não-negociável #4 do skill; é o rollback de T-19-04). O `.gitignore` do plano 19-01 já
      cobre `*.css.bak`.
    2. Reescrever **apenas os valores** dos 14 tokens core + `--radius` DENTRO dos blocos
      `:root` e `.dark` já existentes: `--background --foreground --card --card-foreground
      --popover --popover-foreground --primary --primary-foreground --secondary
      --secondary-foreground --muted --muted-foreground --accent --accent-foreground
      --destructive --border --input --ring --radius`.
    3. **PROIBIDO** mover `:root` ou `.dark` para dentro de `@layer base`. Se o skill oferecer
      "adicionar `@layer base` se não existir", RECUSAR — o arquivo já tem `@layer base` no fim
      e mover os tokens para dentro dele muda a especificidade da cascata
      (19-RESEARCH linha 124 / anti-padrão da linha 288).
    4. **NÃO tocar** em `--chart-1..5` nem nos 8 `--sidebar-*` — eles são alinhados no plano
      19-03 (Pitfall 3). Se o skill os sobrescrever mesmo assim, deixar como ele escreveu e
      anotar no SUMMARY para o 19-03 reconciliar.
    5. **NÃO adicionar** `ThemeProvider` nem toggle de tema (D-16). O bloco `.dark` existe e é
      escrito corretamente, mas continua inacessível pela UI — isso é intencional (D-14/D-17).
    6. Formato de cor: OKLCH (o projeto já está em OKLCH, o skill gera OKLCH — match).
  </action>
  <verify>
    <automated>test -f src/app/globals.css.bak &amp;&amp; node scripts/check-contrast.cjs 2>&amp;1 | grep -v '^#' | grep -c 'AUSENTE --status'</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/globals.css.bak` existe e `git status --porcelain src/app/globals.css.bak` retorna
      vazio (está ignorado pelo git — plano 19-01 T3).
    - `grep -c '@layer base' src/app/globals.css` retorna `1` e `awk '/^:root/{print NR}'`
      mostra que `:root` continua no nível superior, fora de qualquer `@layer`.
    - `--primary` em `:root` mudou em relação ao `.bak`:
      `diff <(grep -- '--primary:' src/app/globals.css.bak) <(grep -- '--primary:' src/app/globals.css)`
      retorna diferença.
    - A paleta NÃO é grayscale: o valor de `--primary` em `:root` tem chroma > 0 (o segundo
      número do `oklch(L C H)` é diferente de `0`). Se o usuário escolher uma paleta
      deliberadamente monocromática, registrar essa escolha explicitamente no SUMMARY.
    - `node scripts/check-contrast.cjs` continua saindo `1` **apenas** por causa dos 20
      `AUSENTE --status-*` (10 em `:root`, 10 em `.dark`) — nenhum par core aparece como `FAIL`.
    - `grep -c -- '--chart-1' src/app/globals.css` retorna `2` (`:root` + `.dark` preservados).
    - `grep -c 'ThemeProvider' src/app/layout.tsx` retorna `0`.
    - `npx tsc --noEmit` sai `0`.
  </acceptance_criteria>
  <resume-signal>O usuário aprova uma paleta no preview (ou pede regeneração — o loop continua até aprovar).</resume-signal>
  <done>A paleta escolhida pelo usuário está em :root e .dark, a estrutura do arquivo está intacta, o backup existe, e os pares core passam AA.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Rodar /brand-design passos 6-8 — tipografia, --font-heading e brand.md</name>
  <files>src/app/layout.tsx, src/app/globals.css, brand.md</files>
  <read_first>
    - `~/.claude/skills/brand-design/references/typography-pairings.md` — os pares por mood.
    - `~/.claude/skills/brand-design/references/typography-preview.md` — o gerador do preview.
    - `~/.claude/skills/brand-design/references/brand-md-template.md` — a estrutura do brand.md.
    - `src/app/layout.tsx` (39 linhas) — o padrão `next/font/google` já montado.
    - `src/components/ui/dialog.tsx` linha 125 — o único consumidor de `font-heading`.
    - `.planning/phases/19-marca-e-identidade-visual/19-PATTERNS.md` linhas 117–166.
    - `.planning/phases/19-marca-e-identidade-visual/19-RESEARCH.md` linhas 329–334 (Pitfall 2)
      e 341–345 (Pitfall 4).
  </read_first>
  <decision>Qual par tipográfico (heading + corpo + mono) o SOLO usa, e se `--font-heading` deixa de aliasar `--font-sans`.</decision>
  <context>
    Candidatos prováveis do blend serious+premium: Inter+JetBrains Mono (A, default seguro),
    Instrument Serif+Inter+JBM (C), IBM Plex Sans+Plex Mono (G), Geist+Geist Mono (B, o atual),
    Manrope+JBM (H), Fraunces+Inter+IBM Plex Mono (F).

    **Risco a comunicar ao usuário no preview (Pitfall 2):** trocar Geist muda a largura de
    avanço das strings e pode reflowar pontos de largura fixa — header do sidebar
    (`text-[11px] uppercase tracking-[0.08em]` em `w-[240px]`), nomes de lead truncados em cards,
    colunas do pipeline (`min-w-[200px]`, 5 colunas sem scroll horizontal — já foi quick-fix
    `5b52ffa`), badges de etapa. Se o usuário estiver indeciso, preferir um sans métricamente
    próximo de Geist (Inter ou Manrope). Manter Geist também é uma escolha válida e consciente.

    **D-18 / Pitfall 4 — decisão travada como default:** manter
    `--font-heading: var(--font-sans)`. Se e somente se o usuário escolher um par com serif
    (C ou F) E aprovar explicitamente títulos de dialog em serif no preview, apontar
    `--font-heading` para a variável do serif. Em qualquer caso, registrar a decisão no
    `brand.md` e no SUMMARY.

    Passo 7 (gradientes): mood `serious` + `premium` → **pular**, conforme o próprio skill.
  </context>
  <action>
    1. Gerar `.brand-preview/typography.html` (estático, abrir com `start`, sem dev server) e
       deixar o usuário escolher o par.
    2. Reescrever o topo de `src/app/layout.tsx`: trocar o import da linha 2 pelas famílias
       escolhidas de `next/font/google`, e nomear as `variable:` como `"--font-sans"` e
       `"--font-mono"` (e `"--font-serif"` se houver serif) — ou seja, **abandonar** os nomes
       antigos `--font-geist-sans` / `--font-geist-mono`. Atualizar o `className` do `<html>`
       (linha 30) para concatenar as novas `.variable`.
       **PROIBIDO** usar `<link rel="stylesheet">` para fonts.googleapis.com ou `@font-face`
       manual — só `next/font/google`, que self-hosta no build (T-19-03).
    3. Reconciliar as 3 linhas de fonte do `@theme inline` de `src/app/globals.css` para
       apontar às variáveis novas: `--font-sans: var(--font-sans)` /
       `--font-mono: var(--font-mono)` / `--font-heading: var(--font-sans)` (ou
       `var(--font-serif)` se a decisão D-18 for serif). As chaves aqui TÊM que casar
       literalmente com as `variable:` do `layout.tsx`.
    4. Pular o Passo 7 (gradientes).
    5. Passo 8: escrever `brand.md` na raiz pelo template do skill (Paleta com os valores OKLCH
       finais e para que serve cada token, Tipografia com o par e o racional, Tom/Voz,
       Dos-and-don'ts). A seção Nome vem na Task 3.
    6. NÃO alterar `title`, `description` (linhas 18–19) nem `bg-white` (linha 32) — plano 19-05.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; grep -qE 'variable:\s*"--font-(sans|mono|serif)"' src/app/layout.tsx &amp;&amp; test -f brand.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'next/font/google' src/app/layout.tsx` retorna `1`.
    - `grep -c 'font-geist' src/app/layout.tsx` retorna `0` e
      `grep -c 'font-geist' src/app/globals.css` retorna `0`.
    - `grep -c 'fonts.googleapis.com' src/app/layout.tsx src/app/globals.css` retorna `0`
      (nenhum fetch de fonte em runtime — T-19-03).
    - Toda `variable:` declarada em `layout.tsx` tem uma linha correspondente no `@theme inline`
      de `globals.css` (conferir par a par; um `--font-serif` órfão em qualquer lado é falha).
    - `grep -c 'font-heading' src/app/globals.css` retorna `1` e o valor está documentado no
      `brand.md` como decisão D-18 (alias de `--font-sans` OU serif consciente).
    - `grep -c 'ThemeProvider\|next-themes' src/app/layout.tsx` retorna `0` (D-16).
    - `src/app/layout.tsx` ainda contém `CRM de Leads` e `bg-white` (o rename é do plano 19-05 —
      antecipar aqui quebra a fronteira dos planos).
    - `npx tsc --noEmit` sai `0` e `npm run lint` sai `0`.
    - `brand.md` existe e casa `/##\s+.*paleta/i`, `/##\s+.*tipografia/i` e `/tom|voz/i`.
  </acceptance_criteria>
  <resume-signal>O usuário aprova um par tipográfico no preview e, se for par com serif, decide sobre títulos de dialog em serif.</resume-signal>
  <done>O par tipográfico está ligado via next/font, as variáveis de fonte casam entre layout.tsx e globals.css, D-18 está decidido e registrado, e o brand.md base existe.</done>
</task>

<task type="auto">
  <name>Task 3: Aumentar brand.md com a seção Nome (BRAND-01), criar README.md e limpar .brand-preview/</name>
  <files>brand.md, README.md</files>
  <read_first>
    - `brand.md` (recém-escrito pelo skill na Task 2) — para inserir a seção nova sem quebrar
      as existentes.
    - `.planning/phases/19-marca-e-identidade-visual/19-CONTEXT.md` linhas 24–27 (D-01, D-02,
      D-03) — texto-fonte do racional e da colisão.
    - `.planning/phases/19-marca-e-identidade-visual/19-RESEARCH.md` linhas 323–328 (Pitfall 1)
      e 357–359 (Pitfall 7 — README não existe).
    - `scripts/verify-brand-md.cjs` (plano 19-01) — os 9 asserts que esta task precisa satisfazer.
    - `package.json` linhas 1–35 — stack e scripts, para o README citar os comandos reais.
  </read_first>
  <action>
    **A) `brand.md` — adicionar a seção `## Nome — SOLO`** (o template do skill não tem nenhuma
    seção de nome; sem esta task BRAND-01 fica sem cumprir — Pitfall 1). Conteúdo obrigatório:
    - **Racional (D-01):** "SOLO", uma palavra, som curto, vira verbo no uso diário ("abre o
      SOLO"). Positioning: CRM pra quem trabalha sozinho — sem equipe, sem fila, sem SLA.
      Registrar que o usuário explorou alternativas (SoloFlow, SoloFunil, SoloPipe, SoloCue,
      Relance, Farol) e decidiu conscientemente por SOLO puro.
    - **Ícone (D-03):** a letra "S" sobre `--primary`, aplicada no header do sidebar.
    - **Ressalva de colisão (D-02) — texto verbatim obrigatório:** *"nome descartável — trocar
      antes de qualquer movimento de produto, landing page ou venda"*. Listar as colisões
      concretas: **Salesboom Solo CRM** (mesma categoria), **SoloCRM** (`solocrm.com`, CRM
      offline para solopreneurs/freelancers, posicionamento quase idêntico), **Solo /
      `gosolo.io`** (plataforma home-service com módulo CRM). Registrar que os domínios
      `solocrm.com` e `gosolo.io` estão tomados, e que o trade-off é aceitável AGORA porque é
      ferramenta interna de um único usuário.
    - **Escopo do rename (D-05):** registrar que a pasta do repositório continua `crm-leads`, e
      que caminhos de CI, imports e strings de path NÃO foram renomeados de propósito.

    **B) `README.md` na raiz — criar** (não existe hoje; Pitfall 7). Mínimo e honesto:
    título `# SOLO`, subtítulo "CRM pra quem trabalha sozinho", 2–3 frases do que o app faz,
    a stack real (Next.js 16 App Router + Turbopack, React 19, TypeScript, Tailwind v4,
    shadcn/ui sobre Base UI, Drizzle + SQLite via better-sqlite3), como rodar
    (`npm install`, `npm run dev`, banco local em `data/crm.db`), e um link para `brand.md`.
    Não inventar features que não existem, não prometer deploy (DEPLOY-01 é outro milestone).

    **C) Limpeza (Passo 9 do skill / T-19-02):** remover o diretório `.brand-preview/` do disco
    depois que as duas escolhas estiverem aplicadas. Manter `src/app/globals.css.bak` (é o
    rollback de T-19-04; está gitignorado).
  </action>
  <verify>
    <automated>npm run verify:brand-md</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify:brand-md` sai `0` — os 9 asserts passam (Paleta, Tipografia, Tom/Voz,
      Nome+SOLO, colisão, `globals.css.bak`, `next/font/google`, `variable: --font-*`).
    - `grep -c 'nome descartável' brand.md` ≥ 1 e `grep -ci 'SoloCRM' brand.md` ≥ 1 e
      `grep -ci 'Salesboom' brand.md` ≥ 1 e `grep -ci 'gosolo' brand.md` ≥ 1.
    - `grep -c 'crm-leads' brand.md` ≥ 1 (o registro de D-05: a pasta do repo NÃO foi renomeada).
    - `README.md` existe, tem ≥ 15 linhas, contém `# SOLO`, `npm run dev` e `data/crm.db`.
    - `test -d .brand-preview` retorna falso (diretório removido).
    - `test -f src/app/globals.css.bak` retorna verdadeiro (backup preservado).
    - `git status --porcelain` não lista `.brand-preview/` nem `globals.css.bak`.
    - `npm run verify:brand` continua saindo `1` (o refactor de cor ainda não aconteceu — RED
      esperado até o plano 19-05).
  </acceptance_criteria>
  <done>BRAND-01 está cumprido: brand.md documenta paleta, tipografia, tom/voz e o nome SOLO com racional e ressalva de colisão verbatim; README existe; o preview temporário foi apagado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill `/brand-design` → repositório | O skill escreve `globals.css`, `layout.tsx`, `brand.md` e `.brand-preview/`. Escrita não-confiável sobre o design system em produção. |
| skill `/brand-design` → internet | O Preamble emite `curl` POST de telemetria para um endpoint Convex, salvo opt-out. |
| app → CDN de fontes | Se a tipografia fosse carregada por `<link>`, o navegador do usuário faria fetch a terceiro em runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Information Disclosure | telemetria do skill `/brand-design` (curl POST) | mitigate | Task 1: responder "No thanks" no Preamble e/ou gravar `~/.superstack/config.json` com `telemetryTier: "off"` ANTES do Passo 1; nenhum ping emitido |
| T-19-03 | Information Disclosure | carregamento de tipografia | mitigate | Task 2: exclusivamente `next/font/google` (self-host no build, zero fetch de runtime, não vaza IP/UA/Referer). Aceite proíbe `fonts.googleapis.com` em `layout.tsx` e `globals.css` |
| T-19-04 | Tampering | escrita do skill sobre `src/app/globals.css` | mitigate | Task 1: `cp globals.css globals.css.bak` obrigatório antes da escrita; `npx tsc --noEmit` + `check-contrast.cjs` como detecção; o `.bak` é o rollback |
| T-19-02 | Information Disclosure | `.brand-preview/` e `globals.css.bak` | mitigate | `.gitignore` já cobre (plano 19-01) + Task 3 apaga `.brand-preview/`; aceite verifica `git status --porcelain` limpo |
| T-19-07 | Tampering | mover `:root`/`.dark` para dentro de `@layer base` | mitigate | Task 1 proíbe explicitamente; aceite confere `@layer base` aparecendo 1× e `:root` no nível superior — mudar a cascata quebraria a especificidade de todo o app |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote instalado (19-RESEARCH §Package Legitimacy Audit: "Audit N/A"; as fontes vêm de `next/font/google`, parte do `next` já instalado). Um `npm install` novo aqui é desvio de plano e exige checkpoint humano bloqueante. |
</threat_model>

<verification>
1. `npm run verify:brand-md` → exit 0.
2. `node scripts/check-contrast.cjs` → exit 1 apenas pelos 20 `AUSENTE --status-*`; zero `FAIL` em par core.
3. `npx tsc --noEmit` → exit 0.
4. `npm run lint` → exit 0.
5. `grep -c 'fonts.googleapis.com' src/app/layout.tsx src/app/globals.css` → 0.
6. `grep -c 'font-geist' src/app/layout.tsx src/app/globals.css` → 0.
7. `grep -c -- '--chart-1' src/app/globals.css` → 2.
8. `test ! -d .brand-preview && test -f src/app/globals.css.bak`.
9. `git status --porcelain` não lista artefatos temporários.
</verification>

<success_criteria>
- O usuário revisou ~6 paletas no preview HTML estático (sem dev server) e aprovou uma (D-11/D-13).
- Os 14 tokens core + `--radius` estão reescritos em `:root` e `.dark`, com a estrutura do arquivo intacta (`@theme inline`, `@layer base`, `--chart-*`, `--sidebar-*` preservados).
- O par tipográfico está ligado via `next/font/google` e as variáveis casam entre `layout.tsx` e `@theme inline`.
- D-18 foi decidido conscientemente e registrado.
- `brand.md` cumpre BRAND-01 integralmente: paleta, tipografia, tom/voz E nome SOLO com racional + ressalva de colisão verbatim.
- `README.md` existe.
- Nenhum `ThemeProvider`/toggle adicionado (D-16); nenhum pacote npm novo; nenhuma telemetria emitida.
</success_criteria>

<output>
Create `.planning/phases/19-marca-e-identidade-visual/19-02-SUMMARY.md` when done.
Registrar no SUMMARY: a paleta escolhida (nome + valores de `--primary`/`--background`), o par
tipográfico escolhido, a decisão D-18, e se o skill tocou ou não em `--sidebar-*`/`--chart-*`
(o plano 19-03 depende dessa informação).
</output>
