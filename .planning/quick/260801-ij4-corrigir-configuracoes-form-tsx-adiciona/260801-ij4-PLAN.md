---
phase: quick-260801-ij4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/configuracoes-form.tsx
  - .planning/phases/07-configura-o-de-dias-parado-por-etapa/07-HUMAN-UAT.md
autonomous: true
requirements: [CONFIG-01]

must_haves:
  truths:
    - "Digitar 0 (ou negativo) em qualquer campo de /configuracoes e clicar em 'Salvar configurações' exibe a mensagem Zod 'Mínimo de 1 dia.' abaixo do campo inválido"
    - "O navegador NÃO exibe mais seu tooltip nativo ('O valor deve ser maior ou igual a 1.') nem intercepta o submit antes do react-hook-form"
    - "Com valor inválido, nada é persistido: nenhuma requisição de Server Action é disparada (comportamento atual preservado)"
    - "Salvar valores válidos continua funcionando igual: toast 'Configurações salvas.', admin permanece em /configuracoes e os campos seguem mostrando os valores salvos (D-02)"
    - "O atributo min={1} permanece nos inputs (semântica/acessibilidade e steppers do navegador), apenas deixa de bloquear o submit"
  artifacts:
    - path: "src/components/configuracoes-form.tsx"
      provides: "Tag de form com noValidate, cedendo a validação client-side inteiramente ao zodResolver"
      contains: "noValidate"
  key_links:
    - from: "src/components/configuracoes-form.tsx (form noValidate)"
      to: "form.handleSubmit(onSubmit) + zodResolver(configuracoesSchema)"
      via: "desabilitar constraint validation nativa para que o handler React rode e popule form.formState.errors"
      pattern: "noValidate"
    - from: "form.formState.errors.diasParado*"
      to: "FieldError errors={[...]} (src/components/ui/field.tsx)"
      via: "FieldError renderiza error.message em um div role=alert quando errors[0]?.message existe"
      pattern: "FieldError errors="
---

<objective>
Corrigir o único gap aberto do UAT humano da Fase 07 (item 3, `07-HUMAN-UAT.md`): a mensagem customizada de validação `"Mínimo de 1 dia."` nunca aparece em `/configuracoes` porque a validação HTML5 nativa (input `type="number" min={1}` dentro de um form sem `noValidate`) intercepta o submit antes do `react-hook-form`/`zodResolver` rodar.

Purpose: alinhar a UX de erro ao que foi especificado em `07-02-PLAN.md` — o admin precisa ver, em português e abaixo do campo, o motivo real da recusa, em vez do tooltip genérico do navegador.
Output: `noValidate` na tag de form de `configuracoes-form.tsx` + gap do UAT atualizado com rastreabilidade.

Escopo fechado: **uma linha de código funcional**. Nenhuma mudança em schema, Server Action, layout, texto de campo ou comportamento de sucesso. Não é lugar para refatorar o formulário.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/07-configura-o-de-dias-parado-por-etapa/07-HUMAN-UAT.md
@.planning/phases/07-configura-o-de-dias-parado-por-etapa/07-SECURITY.md
@src/components/configuracoes-form.tsx

**Evidência da causa raiz (UAT humano, navegador real, 2026-08-01, 3 tentativas consistentes):**
Clicar em "Salvar configurações" com `0` no campo Novo foca o input nativamente e mostra a mensagem do próprio navegador; `input.validationMessage` e `document.activeElement` confirmaram a interceptação nativa. Nenhuma requisição de rede foi disparada — ou seja, o comportamento de segurança já está correto, só a apresentação diverge.

<interfaces>
Contratos já existentes no código. O executor NÃO precisa explorar o codebase.

De `src/lib/validations.ts` (linhas 93-97) — inalterado por este plano:
`configuracoesSchema` = `z.object({ diasParadoNovo | diasParadoContatado | diasParadoNegociacao: z.coerce.number().int().min(1, "Mínimo de 1 dia.") })`, com `ConfiguracoesFormValues = z.input<typeof configuracoesSchema>`.

De `src/components/ui/field.tsx` (linha 176) — inalterado:
`FieldError({ className, children, errors, ...props })` aceita `errors?: Array<{ message?: string } | undefined>` e renderiza um `div` com `role="alert"`, `data-slot="field-error"` e classe `text-sm ... text-destructive` contendo `error.message` quando há exatamente um erro; retorna `null` quando `errors` está vazio. Ou seja, a cadeia de renderização da mensagem **já está pronta e correta** — o que falta é o `handleSubmit` chegar a rodar.

De `src/components/configuracoes-form.tsx` (estado atual, a alterar):
a tag de abertura do form (linhas 74-82) tem `ref={formRef}`, um comentário `eslint-disable-next-line react-hooks/refs`, `onSubmit={form.handleSubmit(onSubmit)}` e `className="flex flex-col gap-4"` — **sem** `noValidate`. Os 3 `Input` (`type="number" min={1} step={1} inputMode="numeric"`) já têm `aria-invalid` e `FieldError errors={[errors.X]}` corretamente ligados.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar noValidate ao form de /configuracoes</name>
  <files>src/components/configuracoes-form.tsx</files>
  <action>
Adicionar o atributo booleano `noValidate` na tag de abertura do form (a que já tem `ref={formRef}` e `onSubmit={form.handleSubmit(onSubmit)}`), desabilitando a constraint validation nativa do navegador para que o `zodResolver` assuma inteiramente a validação client-side e o `FieldError` de cada campo renderize `"Mínimo de 1 dia."`.

Regras de escopo — **não alterar mais nada neste arquivo**:
- Manter `min={1}`, `step={1}`, `inputMode="numeric"` e `aria-invalid` nos 3 `Input`. `min` continua valendo como semântica/acessibilidade e para os steppers do navegador; só deixa de bloquear o submit.
- Manter `onSubmit={form.handleSubmit(onSubmit)}`, o `formRef`, o corpo de `onSubmit()` (`formAction(new FormData(formRef.current))`) e o comentário `eslint-disable-next-line react-hooks/refs` **exatamente como estão** — o FormData bruto do DOM continua sendo o que vai para o servidor (T-07-06).
- Não mexer no `useActionState`, no `useEffect` dos toasts, nos textos de `FieldDescription`/`FieldLabel` nem nas classes de estilo.

Documentar o porquê no **JSDoc do componente** (bloco de comentário acima de `export function ConfiguracoesForm`, hoje nas linhas 30-39), acrescentando um parágrafo curto: `noValidate` existe para que a validação nativa HTML5 do `min={1}` não intercepte o submit antes do `react-hook-form` — sem ele a mensagem Zod `"Mínimo de 1 dia."` nunca renderiza (gap do item 3 de `07-HUMAN-UAT.md`); a autoridade real de validação continua sendo o `safeParse` server-side de `saveConfiguracoes` (T-07-06 / T-07-01), então remover `noValidate` no futuro só devolve o bug de UX. Deixar explícito no texto que este atributo **não deve ser removido**.

Atenção ao gate automatizado: escreva a palavra `noValidate` **apenas** como atributo real na tag de abertura do form; **não** cite o termo dentro do comentário `eslint-disable-next-line` que fica dentro da própria tag (o parágrafo explicativo vai no JSDoc, que está fora da tag). Isso mantém o gate abaixo honesto em vez de auto-satisfeito por prosa.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/components/configuracoes-form.tsx','utf8');const a=s.indexOf('ref={formRef}');const b=s.indexOf('FieldGroup',a);if(a===-1||b===-1){console.error('FAIL: tag de form nao localizada');process.exit(1)}if(!/\bnoValidate\b/.test(s.slice(a,b))){console.error('FAIL: noValidate ausente na tag de form');process.exit(1)}if(!/min=\{1\}/.test(s)){console.error('FAIL: min={1} foi removido dos inputs');process.exit(1)}if((s.match(/FieldError errors=/g)||[]).length!==3){console.error('FAIL: os 3 FieldError devem continuar presentes');process.exit(1)}if(!/formAction\(new FormData\(formRef\.current\)\)/.test(s)){console.error('FAIL: caminho de submissao alterado');process.exit(1)}console.log('OK')"</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npx eslint src/components/configuracoes-form.tsx</automated>
    <human-check>Com o dev server já rodando em http://localhost:3000 (PID registrado no STATE.md — NÃO reiniciar e NÃO rodar `npm run build`, host de 4GB): (1) abrir http://localhost:3000/configuracoes; (2) apagar o valor do campo "Novo" e digitar `0`; (3) clicar em "Salvar configurações". Esperado: a mensagem vermelha "Mínimo de 1 dia." aparece logo abaixo do campo Novo, o navegador NÃO mostra tooltip nativo nem foca o campo com sua própria mensagem, e nenhuma requisição de rede é disparada (aba Network vazia). (4) Corrigir para valores válidos (ex.: 2/3/4) e salvar de novo: toast "Configurações salvas." aparece, a tela continua em /configuracoes e os campos seguem mostrando 2/3/4.</human-check>
  </verify>
  <done>A tag de abertura do form tem `noValidate`; `min={1}`, os 3 `FieldError` e o `formAction(new FormData(formRef.current))` seguem intactos; `npx tsc --noEmit` e `npx eslint` no arquivo passam limpos; o JSDoc do componente explica por que `noValidate` não deve ser removido.</done>
</task>

<task type="auto">
  <name>Task 2: Fechar a rastreabilidade do gap no UAT da Fase 07</name>
  <files>.planning/phases/07-configura-o-de-dias-parado-por-etapa/07-HUMAN-UAT.md</files>
  <action>
Registrar no UAT da Fase 07 que o gap do item 3 recebeu correção, mantendo o histórico honesto (não declarar PASSED sem reteste humano no navegador).

1. No bloco `### 3. Validation error UI (interactive)`, **preservar integralmente o texto atual do `result:` ISSUE** (é a evidência da reprodução) e acrescentar ao final dele a frase: `Fix aplicado no quick task 260801-ij4 (noValidate na tag de form de configuracoes-form.tsx) — aguardando reteste humano no navegador para virar PASSED.`
2. Na seção `## Gaps`, acrescentar ao final do bullet do Item 3: `Status: fix aplicado (quick 260801-ij4), reteste pendente.`
3. **Não alterar** os contadores da seção `## Summary` (`passed: 5` / `issues: 1`) nem o `status: done` do frontmatter — só o reteste humano justifica mudar isso.
4. Atualizar o campo `updated:` do frontmatter para a data/hora corrente em formato ISO 8601 UTC.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/07-configura-o-de-dias-parado-por-etapa/07-HUMAN-UAT.md','utf8');const body=s.split('\n').filter(l=>!l.trim().startsWith('#')).join('\n');const n=(body.match(/260801-ij4/g)||[]).length;if(n<2){console.error('FAIL: referencia ao quick task ausente no item 3 e/ou em Gaps (encontradas: '+n+')');process.exit(1)}if(!/passed: 5/.test(body)||!/issues: 1/.test(body)){console.error('FAIL: contadores do Summary foram alterados indevidamente');process.exit(1)}if(!/O valor deve ser maior ou igual a 1/.test(body)){console.error('FAIL: evidencia original do ISSUE foi apagada');process.exit(1)}console.log('OK')"</automated>
  </verify>
  <done>`07-HUMAN-UAT.md` cita o quick task `260801-ij4` no item 3 e na seção Gaps, a evidência original do ISSUE segue intacta e os contadores do Summary permanecem `passed: 5` / `issues: 1`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → Server Action `saveConfiguracoes` | Fronteira real e inalterada por este plano: FormData bruto do DOM continua chegando não confiável ao servidor |
| formulário do cliente → validação client-side | `noValidate` altera **apenas** qual camada client-side valida (Zod em vez de HTML5); nenhuma das duas é autoridade de segurança |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-Q-ij4-01 | Tampering | `noValidate` no form de `configuracoes-form.tsx` | accept | Remover a constraint validation nativa não amplia a superfície de ataque: ela já era trivialmente burlável por devtools ou POST direto, e `07-SECURITY.md` T-07-06 já documenta a validação client-side como "só UX antecipada". A defesa real permanece intacta e não é tocada por este plano: `configuracoesSchema.safeParse()` dentro de `saveConfiguracoes` (T-07-01), que rejeita valores menores que 1 antes de qualquer escrita. |
| T-Q-ij4-02 | Tampering | fluxo de submissão (`onSubmit` → `formAction`) | mitigate | Task 1 proíbe explicitamente alterar `onSubmit`, `formRef` e o envio de `new FormData(formRef.current)`; o gate automatizado verifica que `min={1}`, os 3 `FieldError` e a chamada `formAction(new FormData(formRef.current))` seguem presentes, garantindo que a mudança é aditiva (um atributo) e não uma reescrita do caminho de submissão. |
| T-Q-ij4-SC | Tampering | npm installs | mitigate | Nenhum pacote novo é necessário (`noValidate` é atributo nativo do DOM). Nenhum comando `npm install` / `npx shadcn add` deve ser executado; o SUMMARY deve confirmar `package.json`/`package-lock.json` inalterados via `git show --stat`. |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo.
- `npx eslint src/components/configuracoes-form.tsx` sem erros (nenhum novo aviso do `react-hooks/refs` — o `eslint-disable-next-line` existente foi preservado).
- Gates `node -e` das duas tasks retornam `OK`.
- `git show --stat` do commit da Task 1 não toca `package.json` nem `package-lock.json`.
- **Não rodar `npm run build`** enquanto o dev server estiver ativo (host de 4GB, precedente de OOM registrado no STATE.md).
</verification>

<success_criteria>
- `src/components/configuracoes-form.tsx` tem `noValidate` na tag de abertura do form e mais nenhuma mudança funcional.
- Digitar `0` em qualquer um dos 3 campos e salvar exibe `"Mínimo de 1 dia."` abaixo do campo (verificação humana no navegador), sem tooltip nativo do navegador e sem persistir nada.
- Salvar valores válidos continua exibindo o toast `"Configurações salvas."` com os campos mantendo os valores salvos.
- `07-HUMAN-UAT.md` rastreia o fix sem inflar o resultado do UAT.
</success_criteria>

<output>
Criar `.planning/quick/260801-ij4-corrigir-configuracoes-form-tsx-adiciona/260801-ij4-SUMMARY.md` ao concluir.
</output>
