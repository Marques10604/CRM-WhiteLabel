---
phase: quick-260720-x41
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/ui/dialog.tsx]
autonomous: false
requirements: [QUICK-x41]
must_haves:
  truths:
    - "Ao abrir o dialog de lead (novo/editar) em um viewport de laptop, o rodape com os botoes Salvar/Cancelar fica visivel e acessivel"
    - "Quando o conteudo do dialog for mais alto que a tela, aparece scroll vertical dentro do dialog"
    - "Dialogs curtos (ex: descartar alteracoes) continuam do tamanho do conteudo, sem scroll desnecessario"
    - "Sizing horizontal e animacoes de abertura/fechamento continuam iguais"
  artifacts:
    - path: "src/components/ui/dialog.tsx"
      provides: "Primitiva DialogContent compartilhada com limite de altura e scroll vertical"
      contains: "max-h-"
  key_links:
    - from: "src/components/lead-form-dialog.tsx"
      to: "src/components/ui/dialog.tsx"
      via: "DialogContent"
      pattern: "DialogContent"
---

<objective>
Corrigir o bug em que o dialog de preenchimento de lead (novo/editar) fica maior que a tela, empurrando o rodape com os botoes Salvar/Cancelar para fora do viewport e tornando-o inalcancavel.

Purpose: A primitiva compartilhada `DialogContent` limita a largura (`max-w-[calc(100%-2rem)]`) mas nao tem nenhuma restricao de altura nem scroll vertical. Em um laptop, o formulario de lead (3 secoes, ~9 campos + calendario) fica mais alto que a tela.

Output: `DialogContent` em `src/components/ui/dialog.tsx` com limite de altura (`max-h-[calc(100vh-2rem)]`) e scroll vertical (`overflow-y-auto`), consistente com o padrao de largura ja existente. Correcao unica que resolve o bug em todos os dialogs do app.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Trecho relevante de src/components/ui/dialog.tsx (linha 53-60). -->
<!-- A className do Popup tem max-w mas nenhum controle de altura/scroll. -->

DialogContent -> DialogPrimitive.Popup className atual:
"fixed top-1/2 left-1/2 z-50 grid w-full max-w-[calc(100%-2rem)] -translate-x-1/2 -translate-y-1/2 gap-4 rounded-xl bg-popover p-4 text-sm text-popover-foreground ring-1 ring-foreground/10 duration-100 outline-none sm:max-w-sm data-open:animate-in data-open:fade-in-0 data-open:zoom-in-95 data-closed:animate-out data-closed:fade-out-0 data-closed:zoom-out-95"

Consumidores: src/components/lead-form-dialog.tsx, src/components/discard-changes-dialog.tsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar limite de altura e scroll vertical ao DialogContent</name>
  <files>src/components/ui/dialog.tsx</files>
  <action>Na className do `DialogPrimitive.Popup` dentro de `DialogContent` (linha ~56), adicionar `max-h-[calc(100vh-2rem)] overflow-y-auto` seguindo o mesmo padrao ja usado para largura (`max-w-[calc(100%-2rem)]`). Fazer o scroll do `DialogContent` inteiro (header + body + footer juntos), pois hoje header/footer nao sao posicionados/sticky separadamente — nao introduzir restruturacao maior (nao dividir em regioes de scroll independentes). NAO alterar nenhuma outra classe existente: manter `w-full`, `max-w-[calc(100%-2rem)]`, `sm:max-w-sm`, os translates de centralizacao e todas as classes de animacao (`data-open:*`, `data-closed:*`). Correcao apenas de CSS — nao tocar em `lead-form-dialog.tsx` nem `discard-changes-dialog.tsx`.</action>
  <verify>
    <automated>grep -q "max-h-\[calc(100vh-2rem)\]" src/components/ui/dialog.tsx && grep -q "overflow-y-auto" src/components/ui/dialog.tsx && grep -q "max-w-\[calc(100%-2rem)\]" src/components/ui/dialog.tsx && echo OK</automated>
  </verify>
  <done>A className do Popup contem `max-h-[calc(100vh-2rem)]` e `overflow-y-auto`; as classes de largura e animacao permanecem intactas.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Adicionei limite de altura (`max-h-[calc(100vh-2rem)]`) e scroll vertical (`overflow-y-auto`) na primitiva compartilhada `DialogContent`, mantendo largura e animacoes.</what-built>
  <how-to-verify>
    1. Rode `npm run dev` e abra o app no navegador em uma janela de laptop (altura normal).
    2. Clique para criar/editar um lead — o dialog de formulario (secoes Contato, Negocio, Acompanhamento) deve abrir.
    3. Confirme que o rodape com os botoes Salvar/Cancelar fica visivel e que, se o conteudo passar da altura da tela, aparece scroll vertical dentro do dialog.
    4. Abra tambem o dialog de descartar alteracoes — deve continuar compacto, do tamanho do conteudo, sem scroll desnecessario.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva o que ficou errado</resume-signal>
</task>

</tasks>

<verification>
- `DialogContent` limita altura a `calc(100vh-2rem)` e ativa `overflow-y-auto`.
- Classes de largura (`max-w-[calc(100%-2rem)]`, `sm:max-w-sm`) e animacao inalteradas.
- Nenhuma alteracao logica em consumidores do dialog.
</verification>

<success_criteria>
- O rodape (Salvar/Cancelar) do dialog de lead fica acessivel em viewport de laptop.
- Conteudo mais alto que a tela rola verticalmente dentro do dialog.
- Dialogs curtos permanecem sem scroll desnecessario.
</success_criteria>

<output>
Create `.planning/quick/260720-x41-corrigir-dialog-modal-de-preenchimento-n/260720-x41-SUMMARY.md` when done
</output>
