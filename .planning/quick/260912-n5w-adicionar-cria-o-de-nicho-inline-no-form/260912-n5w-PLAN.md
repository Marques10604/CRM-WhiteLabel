---
phase: quick-260912-n5w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/actions/nicho-actions.ts
  - src/components/nicho-manager.tsx
  - scripts/test-nicho-actions.cjs
  - package.json
  - src/components/nicho-combobox.tsx
  - src/components/campanha-form-dialog.tsx
autonomous: true
requirements: [QUICK-260912-n5w]

must_haves:
  truths:
    - "No modal Nova campanha, digitar um nome de nicho que não existe faz aparecer a linha de ação `Criar \"<texto>\"` na lista do combobox"
    - "Escolher essa linha (clique ou Enter sobre ela) cria o nicho pela Server Action createNicho e já deixa o nicho recém-criado selecionado no campo, sem fechar o modal nem navegar para /nichos"
    - "O nicho criado por esse caminho é o MESMO registro governado de /nichos (mesma validação de nome, mesma unicidade case/space-insensitive, mesma reativação de nicho soft-deletado)"
    - "Nome duplicado de um nicho ativo não cria nada e mostra a mensagem de erro dentro do próprio combobox"
    - "Os outros dois consumidores do NichoCombobox (lead-form-dialog.tsx e csv-import-preview-table.tsx) continuam sem a opção de criar — comportamento idêntico ao de hoje"
  artifacts:
    - path: "src/actions/nicho-actions.ts"
      provides: "createNicho/renameNicho/softDeleteNicho com shape homogêneo { success: true; id } + revalidação de /campanhas"
      contains: "revalidateNichoRoutes"
    - path: "scripts/test-nicho-actions.cjs"
      provides: "Cobertura automatizada do CRUD de nicho (id retornado, unicidade, reativação, validação)"
      min_lines: 120
    - path: "src/components/nicho-combobox.tsx"
      provides: "Combobox de nicho com criação-na-hora opt-in (prop allowCreate)"
      contains: "CREATE_VALUE"
    - path: "src/components/campanha-form-dialog.tsx"
      provides: "Única superfície que liga allowCreate"
      contains: "allowCreate"
  key_links:
    - from: "src/components/nicho-combobox.tsx"
      to: "src/actions/nicho-actions.ts"
      via: "import { createNicho } + chamada direta dentro de useTransition"
      pattern: "createNicho"
    - from: "src/components/campanha-form-dialog.tsx"
      to: "src/components/nicho-combobox.tsx"
      via: "prop allowCreate"
      pattern: "allowCreate"
    - from: "src/actions/nicho-actions.ts"
      to: "/campanhas"
      via: "revalidatePath após criar/reativar nicho"
      pattern: "revalidatePath\\(\"/campanhas\"\\)"
---

<objective>
Permitir criar um nicho novo de dentro do modal "Nova campanha" (`CampanhaFormDialog` → `NichoCombobox`), sem passar antes por `/nichos`.

Propósito: hoje, ao montar uma campanha de exploração de um nicho ainda não cadastrado, o admin precisa abandonar o modal, ir em `/nichos`, criar, e voltar — atrito puro num fluxo que ele repete a cada nova exploração.

Saída: `NichoCombobox` ganha criação-na-hora **opt-in**, ligada SÓ no formulário de campanha, reusando integralmente a Server Action `createNicho` (nenhum caminho de escrita novo, nenhuma regra de governança duplicada).

**Este é um trabalho de PARIDADE, não de invenção de UX.** O projeto já tem exatamente esse padrão implementado e aceito: `src/components/motivo-perda-combobox.tsx` (plano 11-03, "o primeiro combobox criável do projeto"). O executor deve copiar aquele idioma linha a linha e adaptar para nicho — não desenhar um fluxo novo, não adicionar um sub-modal, não criar primitivo de UI novo.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

Fonte-molde OBRIGATÓRIA (ler antes de escrever qualquer linha):
@src/components/motivo-perda-combobox.tsx
@src/actions/motivo-perda-actions.ts
@scripts/test-motivo-perda-actions.cjs

Arquivos alvo:
@src/actions/nicho-actions.ts
@src/components/nicho-combobox.tsx
@src/components/nicho-manager.tsx
@src/components/campanha-form-dialog.tsx

<interfaces>
<!-- Contratos já existentes no código. Use-os direto — não explore o codebase atrás deles. -->

Estado atual de `src/actions/nicho-actions.ts` (o que muda na Task 1):
- `type ActionState = { success: true } | { errors: { nome: string[] } } | undefined` (tipo LOCAL, não exportado)
- `createNicho(_prevState, formData)` — valida `nichoSchema` sobre `formData.get("nome")`; se existe ativo → `{ errors: { nome: ["Esse nicho já existe."] } }`; se existe soft-deletado → reativa (`deletedAt: null`, regrava a grafia) e devolve `{ success: true }`; senão `db.insert(nichos).values({ nome })`
- Revalidação HOJE é assimétrica: o branch de reativação revalida `/nichos`, `/`, `/leads`, `/pipeline`, `/importar`; o branch de insert revalida só `/nichos` e `/`. **Nenhum dos dois revalida `/campanhas`.**
- `softDeleteNicho(nichoId: number)` e `renameNicho(_prevState, formData)` devolvem `{ success: true }` sem id

Molde de destino, já implementado em `src/actions/motivo-perda-actions.ts`:
- `type ActionState = { success: true; id: number } | { errors: { nome: string[] } } | undefined`
- helper `revalidateMotivoPerdaRoutes()` chamado por todas as mutações
- o comentário de topo daquele arquivo documenta explicitamente a divergência atual ("`createNicho` devolve só `{ success: true }` porque o `NichoCombobox` recebe a lista pronta por prop") — **esse comentário fica obsoleto com este plano e precisa ser atualizado**

Tabela FÍSICA de nicho (divergência deliberada nome-lógico ↔ nome-físico, Fase 13/D-01 — `src/db/schema.ts`):
- export lógico: `nichos` → tabela física: `"subnichos"`
- colunas: `id INTEGER PRIMARY KEY AUTOINCREMENT`, `nome TEXT NOT NULL`, `deleted_at INTEGER`, `created_at INTEGER NOT NULL DEFAULT (unixepoch())`
- índices: `uniqueIndex("subnicho_nome_unique_idx")` sobre `lower(trim(nome))`, `index("subnichos_deleted_at_idx")` sobre `deleted_at`

Assinatura atual de `NichoCombobox` (`src/components/nicho-combobox.tsx`):
```
type NichoComboboxProps = {
  nichos: Nicho[];
  value: number | null;
  onValueChange: (nichoId: number | null) => void;
  name?: string;      // default "nichoId"
  disabled?: boolean;
  invalid?: boolean;
};
```
Filtro anti-soft-delete que NÃO pode ser perdido: `nichos.filter((n) => n.deletedAt === null || n.id === value)` — sem a exceção `id === value`, editar um registro cujo nicho foi removido mostra campo vazio (causa-raiz em `.planning/debug/resolved/nicho-combobox-vazio.md`).

Consumidores do `NichoCombobox` (3): `campanha-form-dialog.tsx:219` (ALVO), `lead-form-dialog.tsx:347` (intocado), `csv-import-preview-table.tsx:209` (intocado).

`/campanhas` (`src/app/campanhas/page.tsx`) é Server Component: faz `db.select().from(nichos)` SEM filtro de `deletedAt` e passa `nichos={todosNichos}` para `CampanhaList` → `CampanhaFormDialog`. Por isso a revalidação de `/campanhas` na Server Action é o que faz o nicho recém-criado aparecer na prop e o label renderizar.
</interfaces>
</context>

<decisoes_travadas>
- **D-01: `allowCreate` é opt-in, default `false`.** Só `campanha-form-dialog.tsx` liga. Motivo: `csv-import-preview-table.tsx` já tem criação automática de nicho por nome dentro de `bulkImportLeads`/`import-actions.ts` — ligar criação manual ali também criaria dois caminhos concorrentes num fluxo em lote. E `lead-form-dialog.tsx` está fora do pedido do usuário. Mudança reversível: ligar nas outras superfícies depois é uma prop a mais.
- **D-02: zero pacote npm novo, zero primitivo shadcn novo.** Tudo sai de `@/components/ui/combobox` + `lucide-react` (`Plus`), exatamente como `motivo-perda-combobox.tsx`.
- **D-03: a criação passa OBRIGATORIAMENTE por `createNicho`.** Proibido `db.insert` direto no componente, proibido duplicar a checagem de unicidade, proibido um schema Zod paralelo. A lista de nichos é governada e compartilhada com leads/CSV.
- **D-04: `createNicho` passa a devolver `id`, e `renameNicho`/`softDeleteNicho` passam a ecoar o id recebido** — shape homogêneo, réplica exata do que `motivo-perda-actions.ts` já faz. O combobox precisa do id para já selecionar o nicho recém-criado/reativado.
</decisoes_travadas>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: createNicho devolve id + revalida /campanhas (+ harness)</name>
  <files>src/actions/nicho-actions.ts, src/components/nicho-manager.tsx, scripts/test-nicho-actions.cjs, package.json</files>
  <behavior>
    Casos cobertos por `scripts/test-nicho-actions.cjs` (espelho de `scripts/test-motivo-perda-actions.cjs`, mesmos 7 casos adaptados):
    - Caso 1: nome novo → `{ success: true, id }` com `id` inteiro positivo, grava exatamente 1 linha, e o id devolvido é o da linha gravada
    - Caso 2: nome de nicho ATIVO já existente, variando caixa e espaços → `{ errors: { nome: ["Esse nicho já existe."] } }`, sem segunda linha
    - Caso 3: `softDeleteNicho` devolve `{ success: true, id }` com o id recebido, preenche `deleted_at`, e a 2ª chamada é no-op (idempotente)
    - Caso 4: recriar um nome soft-deletado REATIVA — devolve o MESMO id da linha original, contagem total inalterada, `deleted_at` volta a NULL, e a grafia recém-digitada é regravada
    - Caso 5: `renameNicho` para nome livre devolve `{ success: true, id }` e persiste
    - Caso 6: `renameNicho` colidindo com OUTRA linha devolve erro e deixa o banco inalterado
    - Caso 7: nome vazio / só espaços → erro de validação, nada gravado
  </behavior>
  <action>
Escreva `scripts/test-nicho-actions.cjs` copiando a ESTRUTURA de `scripts/test-motivo-perda-actions.cjs`: banco SQLite temporário em `os.tmpdir()` (nunca `./data/crm.db`), `process.env.DB_FILE_NAME` setado ANTES de qualquer `await import("@/...")`, os dois `register(...)` na mesma ordem (`ts-alias-loader.mjs` primeiro, `test-support/next-cache-stub-loader.mjs` depois, para que o stub de `next/cache` execute primeiro), helper `check(condition, message)` com contador `failed`, e `process.exit(failed > 0 ? 1 : 0)`.

Atenção ao DDL: a tabela FÍSICA é `subnichos` (não `nichos`) — crie `subnichos` com as 4 colunas e os 2 índices descritos no bloco `<interfaces>`. Importe as actions de `@/actions/nicho-actions`.

Proibido usar `DELETE FROM` em qualquer ponto do harness (o `guard:no-hard-delete` varre `scripts/` e faz match por linha, sem distinguir teste de produção) — isole cada cenário com nomes únicos, como o molde faz.

Registre o script em `package.json` como `"test:nicho-actions": "node scripts/test-nicho-actions.cjs"`, imediatamente ao lado de `test:motivo-perda-actions`.

Em `src/actions/nicho-actions.ts`:
- amplie o `ActionState` local para `{ success: true; id: number } | { errors: { nome: string[] } } | undefined` (D-04)
- extraia um helper `revalidateNichoRoutes()` chamado por TODAS as mutações, unificando a revalidação hoje assimétrica: `/nichos`, `/`, `/leads`, `/pipeline`, `/importar` e — novidade — `/campanhas`. Sem `/campanhas` o nicho recém-criado não volta na prop do Server Component e o campo ficaria com o label vazio.
- `createNicho`: no branch de reativação devolva `{ success: true, id: existing[0].id }`; no branch de insert use `.returning()` para capturar o id inserido (mesmo idioma de `createMotivoPerda`) e devolva-o. Mantenha intactos o `try/catch` de rede de segurança do uniqueIndex e as mensagens de erro atuais, verbatim.
- `renameNicho` e `softDeleteNicho`: ecoem o id recebido no retorno de sucesso.
- atualize o comentário de topo de `src/actions/motivo-perda-actions.ts` que hoje afirma que `createNicho` devolve só `{ success: true }` — essa divergência deixa de existir.

Em `src/components/nicho-manager.tsx`: o `type ActionState` é uma cópia LOCAL do shape — amplie-a para incluir `id: number` no membro de sucesso, mantendo o resto do componente intocado (nenhum consumo novo do id ali).
  </action>
  <verify>
    <automated>npm run test:nicho-actions &amp;&amp; npm run guard:no-hard-delete &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>Os 7 casos do harness passam (exit 0), `guard:no-hard-delete` continua verde, `tsc --noEmit` limpo, e `grep -c 'revalidatePath("/campanhas")' src/actions/nicho-actions.ts` retorna ≥ 1.</done>
</task>

<task type="auto">
  <name>Task 2: NichoCombobox com criação-na-hora opt-in</name>
  <files>src/components/nicho-combobox.tsx</files>
  <action>
Porte o padrão de `src/components/motivo-perda-combobox.tsx` para `src/components/nicho-combobox.tsx`, ativado pela prop nova `allowCreate?: boolean` (default `false`, D-01). Quando `allowCreate` é `false`, o componente deve se comportar EXATAMENTE como hoje (mesmo render, mesmo placeholder "Selecione um nicho", mesma ausência de estado de query/open controlados no caminho de leitura) — os dois consumidores intocados não podem mudar de comportamento.

Com `allowCreate` ligado, replique o idioma do molde:
- sentinela `const CREATE_VALUE = "__criar__"` e item type com `value: number | string`
- estados `query`, `open`, `createError` e `useTransition`
- `baseItems` derivado de `nichos` PRESERVANDO o filtro anti-soft-delete existente `deletedAt === null || id === value` (não reescreva esse filtro nem o comentário que o explica)
- a linha de criação entra na própria lista `items` (não numa lista filtrada à parte), só quando há texto e ele não casa exatamente (trim + lowercase) com um nicho existente — assim o filtro textual embutido do Combobox a mantém visível, já que o label dela é a própria query
- NÃO envolva esse derivado em `useMemo` manual: o molde documenta que listar `query.trim()` como dep dispara o falso-positivo `react-hooks/preserve-manual-memoization` do React Compiler
- `handleCreate(nome)` chama `createNicho(undefined, formData)` DIRETAMENTE dentro de `startTransition` (nunca via `useActionState`, que só exporia o resultado no render seguinte), e em caso de sucesso limpa a query, fecha a lista e chama `onValueChange(result.id)`; em caso de erro popula `createError` com `result.errors.nome[0]` (fallback "Não foi possível criar o nicho.") e mantém a lista aberta
- `onOpenChange` segura a lista aberta enquanto `isPending` (o Combobox tenta fechar sozinho ao "selecionar" a linha Criar) e limpa `createError` ao fechar
- a linha de criação renderiza `<Plus className="size-4 text-primary" />` + `<span className="text-primary">{\`Criar "${item.label}"\`}</span>` — usar token `text-primary`, nunca cor literal (o gate `verify:brand` falha com hex/cor hardcoded)
- `<ComboboxInput>` com `disabled={disabled || isPending}` e placeholder "Selecione ou digite um nicho..." SOMENTE quando `allowCreate` estiver ligado
- `createError` renderizado como `<p className="px-3 pb-2 text-sm text-destructive">` dentro do `<ComboboxContent>`

Atualize o comentário de topo do componente para registrar as duas modalidades (leitura pura vs. criável) e apontar `motivo-perda-combobox.tsx` como precedente.

Não crie arquivo de componente novo, não adicione dependência, não toque em `src/components/ui/combobox.tsx`.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run verify:brand &amp;&amp; grep -v '^\s*\(//\|\*\|/\*\)' src/components/nicho-combobox.tsx | grep -c 'createNicho'</automated>
  </verify>
  <done>`tsc --noEmit` e `npm run lint` saem 0, `verify:brand` continua verde, o grep de `createNicho` fora de comentários retorna ≥ 1, e `grep -c 'deletedAt === null || nicho.id === value\|deletedAt === null' src/components/nicho-combobox.tsx` ≥ 1 (filtro anti-soft-delete preservado).</done>
</task>

<task type="auto">
  <name>Task 3: ligar allowCreate no modal de campanha + gates finais</name>
  <files>src/components/campanha-form-dialog.tsx</files>
  <action>
No `<NichoCombobox>` dentro do `Controller` de `nichoId` (por volta da linha 219), adicione `allowCreate` — é a ÚNICA mudança de comportamento neste arquivo (D-01). Ajuste o `<FieldDescription>` abaixo do campo para comunicar a capacidade nova, algo como "O nicho que essa campanha vai explorar — digite um nome novo para criá-lo na hora."

Não mexa em nada mais do arquivo: o `onSubmit` continua montando `new FormData(formRef.current)` do DOM bruto (a validação autoritativa é no server dentro de `campanhaSchema`), o guarda de descarte (`closeWithDiscardGuard`) fica intocado, e o `eslint-disable-next-line react-hooks/refs` documentado permanece.

Atenção ao guarda de descarte: criar um nicho pelo combobox altera `nichoId` via `field.onChange`, o que marca o form como dirty — isso é o comportamento correto e esperado (o usuário de fato mexeu no form); não adicione exceção nenhuma para isso.

Rode a bateria completa de gates do projeto ao final e registre o resultado no SUMMARY.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run verify:schema &amp;&amp; npm run guard:no-hard-delete &amp;&amp; npm run test:nicho-actions &amp;&amp; npm run test:motivo-perda-actions &amp;&amp; npm run build</automated>
    <human-check>
      Em `/campanhas` → botão de nova campanha: (1) no campo Nicho, digitar um nome que não existe e conferir que aparece a linha `Criar "..."` em teal; (2) pressionar Enter sobre ela e conferir que o campo passa a exibir o nicho recém-criado, com o modal ainda aberto; (3) salvar a campanha e conferir que ela lista com o nicho certo; (4) abrir `/nichos` e conferir que o nicho novo está lá; (5) repetir digitando o nome de um nicho que JÁ existe e conferir que a linha Criar NÃO aparece; (6) conferir nos temas claro e escuro.
    </human-check>
  </verify>
  <done>Todos os gates automatizados saem 0 (incluindo `npm run build`), `grep -c 'allowCreate' src/components/campanha-form-dialog.tsx` ≥ 1, e `lead-form-dialog.tsx` / `csv-import-preview-table.tsx` continuam sem a prop (`grep -c 'allowCreate'` = 0 nos dois).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (NichoCombobox) → Server Action `createNicho` | Texto livre digitado pelo admin cruza para uma escrita no banco a partir de uma superfície NOVA (o modal de campanha), que antes não escrevia em `subnichos` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-N5W-01 | Tampering | `createNicho` chamada do modal de campanha | mitigate | Nenhum caminho de escrita novo: a criação reusa a Server Action existente, que revalida com `nichoSchema` (trim + min(1)) e checa unicidade `lower(trim(nome))` no servidor. O componente NUNCA fala com o banco (D-03) |
| T-N5W-02 | Elevation of Privilege | governança da lista de nichos | mitigate | A superfície nova não ganha poder extra: sem rename, sem delete, sem bypass do uniqueIndex; o `try/catch` de violação de índice (race de duplo-clique) segue no lugar |
| T-N5W-03 | Information Disclosure | mensagem de erro no combobox | accept | As mensagens são as mesmas já exibidas em `/nichos` ("Esse nicho já existe.") — nenhum dado novo exposto; app solo, sem multi-usuário |
| T-N5W-04 | Denial of Service | criação repetida por duplo-clique | mitigate | `isPending` desabilita o input durante a transição e o uniqueIndex barra a duplicata no servidor (molde já provado em 11-03) |
| T-N5W-SC | Tampering | npm/pip/cargo installs | n/a | **Zero instalação de pacote nesta tarefa** (D-02) — nenhum `npm install` é executado, então o gate de legitimidade de pacote não se aplica |
</threat_model>

<verification>
1. `npm run test:nicho-actions` — 7 casos verdes (cobertura que hoje não existe para o CRUD de nicho)
2. `npx tsc --noEmit` e `npm run lint` — exit 0 (o projeto mantém lint da raiz em 0 desde a Fase 17)
3. `npm run build` — build Turbopack limpo
4. `npm run verify:schema`, `npm run guard:no-hard-delete`, `npm run verify:brand` — sensores existentes continuam verdes
5. Isolamento de escopo: `allowCreate` aparece em exatamente 2 arquivos (`nicho-combobox.tsx` e `campanha-form-dialog.tsx`)
</verification>

<success_criteria>
- Criar um nicho inexistente sem sair do modal "Nova campanha" funciona, e o nicho criado já vem selecionado
- A criação passa 100% por `createNicho` — mesma validação, mesma unicidade, mesma reativação de soft-delete que `/nichos`
- `lead-form-dialog.tsx` e `csv-import-preview-table.tsx` inalterados em comportamento
- Nenhuma dependência npm nova, nenhum primitivo de UI novo
- Toda a bateria de gates do projeto verde, incluindo `npm run build`
</success_criteria>

<output>
Criar `.planning/quick/260912-n5w-adicionar-cria-o-de-nicho-inline-no-form/260912-n5w-SUMMARY.md` ao concluir
</output>
