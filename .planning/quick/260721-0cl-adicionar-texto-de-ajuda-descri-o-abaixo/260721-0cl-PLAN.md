---
phase: quick
plan: 260721-0cl
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/lead-form-dialog.tsx
autonomous: true
requirements: [QUICK-260721-0cl]
must_haves:
  truths:
    - "Cada campo do formulário de lead (exceto Nome) exibe uma linha curta de ajuda abaixo do label"
    - "Os campos Canal, Origem e Valor estimado têm texto de ajuda (obrigatórios, pedido explícito do admin)"
    - "O texto de ajuda aparece de forma consistente entre o input/controle e a mensagem de erro"
  artifacts:
    - path: "src/components/lead-form-dialog.tsx"
      provides: "Formulário de lead com FieldDescription em cada campo"
      contains: "FieldDescription"
  key_links:
    - from: "src/components/lead-form-dialog.tsx"
      to: "src/components/ui/field.tsx"
      via: "import de FieldDescription"
      pattern: "FieldDescription"
---

<objective>
Adicionar texto de ajuda/descrição curto abaixo de cada campo do formulário de lead (`lead-form-dialog.tsx`) para reduzir a dúvida de preenchimento do admin — especialmente distinguir "Origem" de "Canal" e explicar o que é "Valor estimado".

Purpose: O admin (único usuário) tinha dúvida sobre o significado de alguns campos. Uma linha de ajuda embaixo do label elimina essa confusão sem exigir treinamento.
Output: `lead-form-dialog.tsx` renderizando `<FieldDescription>` em cada campo, reutilizando o componente já existente em `src/components/ui/field.tsx`.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/components/lead-form-dialog.tsx
@src/components/ui/field.tsx

<interfaces>
<!-- Componente pronto em src/components/ui/field.tsx — importar e usar direto. -->
<!-- FieldDescription é um <p data-slot="field-description"> estilizado com text-sm text-muted-foreground. -->
<!-- É irmão de FieldLabel/FieldError dentro de FieldContent. -->

Exportado por src/components/ui/field.tsx:
  FieldDescription (React.ComponentProps<"p">)

Estrutura atual de cada campo em lead-form-dialog.tsx:
  <Field data-invalid={...}>
    <FieldLabel htmlFor="X">Label</FieldLabel>
    <FieldContent>
      {input/controle}
      <FieldError errors={[errors.X]} />
    </FieldContent>
  </Field>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Importar FieldDescription e adicionar linha de ajuda em cada campo</name>
  <files>src/components/lead-form-dialog.tsx</files>
  <action>
Adicionar `FieldDescription` ao import existente de `@/components/ui/field` (linhas 18-23, que já importa `Field`, `FieldContent`, `FieldError`, `FieldLabel`).

Em cada `<FieldContent>`, inserir um `<FieldDescription>` posicionado logo APÓS o input/controle e ANTES do `<FieldError>` — de forma consistente em todos os campos, seguindo o design de `field.tsx` onde FieldDescription e FieldError são irmãos dentro de FieldContent.

Copy em pt-BR por campo (texto exato acordado com o admin):
- Nome: pular (autoexplicativo). NÃO adicionar FieldDescription — o campo Nome fica sem linha de ajuda para manter o diff enxuto.
- Telefone: "Com DDD."
- Canal (OBRIGATÓRIO): "Por onde você vai abordar esse lead (Instagram ou WhatsApp)."
- Origem (OBRIGATÓRIO): "De onde esse lead veio — ex: indicação, anúncio, evento, parceria com o cowork."
- Sub-nicho: "Categoria do lead (ex: nutricionista, terapeuta)."
- Etapa: "Onde esse lead está no funil de vendas."
- Valor estimado (OBRIGATÓRIO): "Quanto esse negócio pode valer em R$ se fechar — ajuda a ter noção do funil."
- Notas: "Anotações livres sobre esse lead."
- Follow-up: "Data do próximo contato com esse lead."

Restrições:
- Mudança APENAS de copy/JSX + um import. NÃO alterar lógica, schema, defaultValues, Server Actions, nem `src/lib/validations.ts`.
- Manter cada texto em uma linha curta (tom de ferramenta interna solo, não formulário público).
- Não mexer no posicionamento do `<FieldError>`, apenas inserir o `<FieldDescription>` antes dele.
  </action>
  <verify>
    <automated>test $(grep -c "FieldDescription" src/components/lead-form-dialog.tsx) -ge 8 && npx tsc --noEmit</automated>
  </verify>
  <done>
`FieldDescription` está no import; os campos Canal, Origem e Valor estimado têm uma `<FieldDescription>` com o texto exato acordado; os demais campos (exceto Nome) também têm sua linha de ajuda; `npx tsc --noEmit` passa sem erros novos; nenhuma alteração fora de `lead-form-dialog.tsx`.
  </done>
</task>

</tasks>

<verification>
- `git diff --name-only` mostra apenas `src/components/lead-form-dialog.tsx` modificado.
- Abrir o modal "Novo lead" no navegador: cada campo (exceto Nome) exibe uma linha cinza-clara de ajuda entre o controle e a área de erro.
- Os textos de Canal, Origem e Valor estimado batem exatamente com o copy acordado.
- `npx tsc --noEmit` passa.
</verification>

<success_criteria>
- Canal, Origem e Valor estimado exibem o texto de ajuda obrigatório em pt-BR.
- Demais campos (Telefone, Sub-nicho, Etapa, Notas, Follow-up) exibem sua linha de ajuda; Nome permanece sem (autoexplicativo).
- Posicionamento consistente: FieldDescription após o controle, antes de FieldError, em todos os campos.
- Nenhuma mudança de lógica/schema/Server Action.
</success_criteria>

<output>
Create `.planning/quick/260721-0cl-adicionar-texto-de-ajuda-descri-o-abaixo/260721-0cl-SUMMARY.md` when done
</output>
