---
phase: quick
plan: 260725-219
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/app-sidebar.tsx
autonomous: true
requirements: [QUICK-260725-219]
must_haves:
  truths:
    - "O topo do sidebar exibe um selo quadrado teal com a letra 'I' seguido do texto 'CRM LEADS' em estilo de etiqueta discreta (uppercase, ~11px, bold, tracking largo, cor cinza — não teal)"
    - "Acima da lista de navegação aparece o rótulo de seção 'Principal' em uppercase, ~10px, cor muito apagada"
    - "Cada um dos 7 itens de navegação exibe um ícone lucide-react à esquerda do rótulo"
    - "O ícone assume a mesma cor do texto do item (teal quando ativo, cinza quando inativo) — sem cor fixa própria"
    - "O item ativo tem fundo teal suave (tint), não apenas texto colorido"
    - "Os itens têm respiro maior: padding 10px/14px e gap de 12px entre ícone e rótulo"
    - "Nenhum rodapé fake de usuário/avatar foi adicionado"
    - "A paleta teal #0D9488 do app permanece intacta — nenhum indigo (#4f46e5 / #eef0fe) dos sketches vazou para o código"
  artifacts:
    - path: "src/components/app-sidebar.tsx"
      provides: "Sidebar com brand header, rótulo de seção, ícones por item e estado ativo com fundo suave"
      contains: "lucide-react"
      min_lines: 60
  key_links:
    - from: "src/components/app-sidebar.tsx"
      to: "lucide-react"
      via: "import nomeado dos 7 ícones"
      pattern: "from \"lucide-react\""
    - from: "src/components/app-sidebar.tsx"
      to: "NAV_ITEMS"
      via: "cada item carrega um componente de ícone renderizado dentro do Link"
      pattern: "icon:"
---

<objective>
Aplicar no sidebar real do app (`src/components/app-sidebar.tsx`) as decisões visuais já validadas e commitadas nos sketches `001-sidebar-clean` (vencedor: variante C — icon+label spaced) e `004-sidebar-brand-header` (vencedor: variante C — selo discreto uppercase).

Purpose: O sidebar real hoje é uma lista de texto puro, sem identidade nem hierarquia visual. Os sketches já foram explorados e aprovados pelo admin; falta apenas portar estrutura, tipografia e ícones para o componente real — mantendo a paleta teal existente.
Output: `src/components/app-sidebar.tsx` com brand header, rótulo de seção "Principal", ícones lucide-react por item e estado ativo com fundo teal suave. Nenhum outro arquivo tocado, nenhuma dependência nova.
</objective>

<execution_context>
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vencedor/Desktop/crm-leads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/components/app-sidebar.tsx

<interfaces>
<!-- Contratos verificados durante o planejamento. NAO explorar o codebase de novo. -->

Estado atual de `src/components/app-sidebar.tsx` (48 linhas):
- `"use client"`; importa `Link` (next/link), `usePathname` (next/navigation), `cn` (@/lib/utils)
- `NAV_ITEMS` e um array `as const` de `{ href, label }` com 7 itens, nesta ordem:
  `/` Follow-ups | `/leads` Leads | `/importar` Importar | `/pipeline` Pipeline | `/templates` Templates | `/subnichos` Sub-nichos | `/lixeira` Lixeira
- Logica de item ativo ja existente (PRESERVAR exatamente):
  `item.href === "/" ? pathname === "/" : pathname.startsWith(item.href)`
- Container atual: `<aside className="flex h-full w-[240px] shrink-0 flex-col gap-1 bg-[#F4F4F5] px-4 py-8" aria-label="Navegação principal">`
- Montado em `src/app/layout.tsx` como irmao de `<main className="flex-1 px-8 py-8">` dentro de `<body className="min-h-full flex bg-white">` — o layout NAO precisa ser alterado.

lucide-react instalado: 1.25.0. Exports PascalCase confirmados por inspecao do `.d.ts` e de `dist/esm/icons/`:
`Clock`, `Users`, `Upload`, `Kanban`, `MessageSquare`, `Tag`, `Trash2` — todos existem.
Usar `Kanban` para Pipeline (foi verificado; `LayoutGrid` era so o fallback e nao e necessario).

Valores de referencia extraidos dos sketches (variante C de ambos):
- brand mark: 28x28px, radius 6px, fundo = cor primaria, texto branco 13px/700
- brand text (004-C): `font-size: 11px; font-weight: 700; letter-spacing: 0.08em; text-transform: uppercase; color: muted`
- section label: `font-size: 10px; text-transform: uppercase; letter-spacing: 0.06em; color: faint; padding: 12px 14px 6px`
- nav-section: `padding: 4px 14px`
- nav-item (C): `display:flex; gap:12px; padding:10px 14px; border-radius:8px; font-size:14px; font-weight:500; margin-bottom:3px`
- nav-item svg: `18x18px; flex-shrink:0`
- nav-item.active: `background: tint; color: cor primaria; font-weight:600`; `.active svg` herda a mesma cor
- brand header (001-C): `padding: 20px 18px 22px; gap: 10px`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar icones lucide e o novo estilo dos itens de navegacao</name>
  <files>src/components/app-sidebar.tsx</files>
  <action>
Adicionar `import { Clock, Users, Upload, Kanban, MessageSquare, Tag, Trash2 } from "lucide-react";` no topo. lucide-react 1.25.0 ja e dependencia do projeto — NAO instalar nada.

Estender cada entrada de `NAV_ITEMS` com um campo `icon` apontando para o componente (nao string): `/` -> `Clock`, `/leads` -> `Users`, `/importar` -> `Upload`, `/pipeline` -> `Kanban`, `/templates` -> `MessageSquare`, `/subnichos` -> `Tag`, `/lixeira` -> `Trash2`. Manter `as const` e manter href, label e ordem exatamente como estao.

Dentro do `.map`, atribuir o componente de icone a uma variavel com inicial maiuscula antes do `return` (ex.: `const Icon = item.icon;`) para poder renderizar `<Icon />`.

Reescrever o `className` do `<Link>` via `cn(...)`:
- base: `flex items-center gap-3 rounded-lg px-[14px] py-2.5 text-sm font-medium transition-colors`
- ativo: `bg-[#0D9488]/10 font-semibold text-[#0D9488]`
- inativo: `text-zinc-700 hover:bg-zinc-200/60 hover:text-[#0D9488]`

Dentro do `<Link>`, renderizar `<Icon className="h-[18px] w-[18px] shrink-0" aria-hidden="true" />` antes de `{item.label}`. NAO aplicar classe de cor no icone: lucide usa `stroke="currentColor"`, entao a cor herda do estado ativo/inativo do link (requisito 3 do pedido).

Adicionar `aria-current={isActive ? "page" : undefined}` no `<Link>`.

Ajustar o container `<nav>` para `flex flex-col gap-[3px] px-[14px]` (o gap de 3px substitui o `margin-bottom: 3px` do sketch; o padding horizontal 14px vem do `.nav-section` da variante C).

RESTRICOES DURAS: manter a paleta teal `#0D9488`; NAO introduzir `#4f46e5`, `#eef0fe` nem classes `indigo-*` (o indigo dos sketches era apenas placeholder de exploracao visual). Nao tocar em nenhum outro arquivo. Nao adicionar rodape de usuario/avatar.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/components/app-sidebar.tsx','utf8');const need=['from \"lucide-react\"','Clock','Users','Upload','Kanban','MessageSquare','Tag','Trash2','bg-[#0D9488]/10','gap-3','py-2.5','h-[18px]','aria-current'];const bad=['4f46e5','eef0fe','indigo-'];const m=need.filter(t=>!s.includes(t));const b=bad.filter(t=>s.includes(t));if(m.length||b.length){console.error('MISSING:',m,'BANNED:',b);process.exit(1)}console.log('OK')"</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Os 7 itens renderizam icone + rotulo; o item ativo usa fundo `bg-[#0D9488]/10` com texto teal semibold; o icone nao tem cor propria (herda currentColor); `npx tsc --noEmit` limpo; nenhum token indigo presente no arquivo.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar brand header e rotulo de secao "Principal"</name>
  <files>src/components/app-sidebar.tsx</files>
  <action>
Reestruturar o `<aside>` para que o padding passe a ser responsabilidade dos filhos (o padding uniforme `px-4 py-8` atual conflita com os paddings assimetricos da variante C):
- `<aside className="flex h-full w-[240px] shrink-0 flex-col bg-[#F4F4F5]">` — manter a largura fixa `w-[240px]` (nao adotar os 232px do sketch; a largura real do app nao esta em discussao) e o fundo `bg-[#F4F4F5]`. Remover `gap-1`, `px-4` e `py-8`.
- Mover o `aria-label="Navegação principal"` do `<aside>` para o elemento `<nav>` (o rotulo descreve a navegacao, nao o brand header).

Como primeiro filho do `<aside>`, adicionar o brand header:
- wrapper: `<div className="flex items-center gap-2.5 px-[18px] pt-5 pb-[22px]">`
- selo: um `<div>` com `flex h-7 w-7 shrink-0 items-center justify-center rounded-md bg-[#0D9488] text-[13px] font-bold text-white` e `aria-hidden="true"`, contendo a letra `I`
- wordmark: um `<span>` com `text-[11px] font-bold uppercase tracking-[0.08em] text-zinc-500` e o texto literal `CRM LEADS`

IMPORTANTE: o wordmark e cinza (`text-zinc-500`), NAO teal — e um selo discreto (padrao Notion/Attio da variante C do sketch 004). Apenas o quadradinho usa o teal.

Como segundo filho, antes do `<nav>`, adicionar o rotulo de secao:
- `<p className="px-[14px] pt-3 pb-1.5 text-[10px] font-medium uppercase tracking-[0.06em] text-zinc-400">Principal</p>`

NAO adicionar rodape de usuario/avatar (`sidebar-footer` / `avatar` / "Admin" dos sketches): app de admin unico sem sistema de auth, elemento decorativo fora de escopo por decisao explicita do pedido.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/components/app-sidebar.tsx','utf8');const need=['CRM LEADS','Principal','bg-[#0D9488] text-[13px]','tracking-[0.08em]','tracking-[0.06em]','h-7 w-7','w-[240px]','bg-[#F4F4F5]'];const bad=['avatar','Avatar','4f46e5','indigo-','px-4 py-8'];const m=need.filter(t=>!s.includes(t));const b=bad.filter(t=>s.includes(t));if(m.length||b.length){console.error('MISSING:',m,'BANNED:',b);process.exit(1)}console.log('OK')"</automated>
    <automated>npx tsc --noEmit &amp;&amp; npm run build</automated>
    <human-check>Abrir o app no navegador e conferir: (1) selo teal com "I" + "CRM LEADS" cinza no topo; (2) rotulo "Principal" acima da lista; (3) icone a esquerda de cada um dos 7 itens; (4) item da rota atual com fundo teal suave e icone teal; (5) hover cinza claro nos inativos; (6) nenhum rodape de usuario.</human-check>
  </verify>
  <done>Sidebar renderiza brand header + rotulo "Principal" + lista com icones; `npm run build` limpo; nenhuma referencia a avatar/rodape de usuario no arquivo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma) | Alteracao puramente apresentacional em um client component de navegacao. Nao recebe input do usuario, nao le/escreve banco, nao chama server actions, nao interpola dados dinamicos no DOM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-QUICK219-01 | Information Disclosure | app-sidebar.tsx | accept | Sidebar exibe apenas rotulos estaticos hardcoded ("CRM LEADS", nomes de rotas). Nenhum dado de lead/PII e renderizado. |
| T-QUICK219-02 | Tampering | JSX dos itens de navegacao | accept | Nao ha `dangerouslySetInnerHTML` nem interpolacao de conteudo externo; `href` vem de uma constante `as const` no proprio arquivo. |
| T-QUICK219-SC | Tampering | supply chain (npm) | N/A | Nenhum pacote novo instalado. lucide-react@1.25.0 ja e dependencia existente e verificada do projeto — nao ha gate de legitimidade a executar. |
</threat_model>

<verification>
1. `npx tsc --noEmit` limpo.
2. `npm run build` limpo (todas as rotas continuam presentes; o sidebar e montado no root layout, uma quebra afetaria toda pagina).
3. `git diff --name-only` retorna exatamente `src/components/app-sidebar.tsx` (mais o `.planning/`) — nenhum outro arquivo de codigo tocado.
4. `git diff package.json` vazio — nenhuma dependencia adicionada.
5. Grep de sanidade de paleta: nenhuma ocorrencia de `4f46e5`, `eef0fe` ou `indigo-` no arquivo.
</verification>

<success_criteria>
- Brand header (selo teal 28px com "I" + wordmark "CRM LEADS" cinza uppercase 11px/700/0.08em) presente no topo.
- Rotulo de secao "Principal" (uppercase, 10px, tracking 0.06em, cinza apagado) acima da nav.
- Os 7 itens com icones lucide corretos: Clock, Users, Upload, Kanban, MessageSquare, Tag, Trash2.
- Cor do icone herda o estado do item (currentColor), sem classe de cor propria.
- Item ativo com fundo teal suave (`bg-[#0D9488]/10`) alem do texto teal semibold.
- Espacamento generoso aplicado: `px-[14px] py-2.5` e `gap-3` nos itens.
- Sem rodape de usuario/avatar.
- Paleta teal `#0D9488` preservada; zero indigo dos sketches.
- Apenas `src/components/app-sidebar.tsx` modificado; zero dependencias novas.
</success_criteria>

<output>
Criar `.planning/quick/260725-219-implementar-no-sidebar-real-do-app-src-c/260725-219-SUMMARY.md` ao concluir.
</output>
